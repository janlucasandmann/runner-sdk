steps:
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "build"
      - "-t"
      - "${_IMAGE_URI}"
      - "-f"
      - "repos/runner-web-sdk/examples/Dockerfile.platform"
      - "."
images:
  - "${_IMAGE_URI}"
