import {
  createLegacyPlatformShellScript,
} from "./domains/shell/source.mjs";

/**
 * Compatibility renderer for the original inline platform application.
 *
 * This isolates the legacy browser composition from the HTTP host while typed
 * React routes are migrated into apps/platform/client/src. New UI code must not
 * be added here.
 */
export function createLegacyPlatformDocument(bindings) {
  const PLATFORM_SHELL_SCRIPT = createLegacyPlatformShellScript(bindings);
  const {
    AGENTS_PAGE_SCRIPT,
    API_KEYS_APP_SCRIPT_FRAGMENTS,
    API_KEYS_DOMAIN_SCRIPT_FRAGMENTS,
    API_KEYS_PAGE_SCRIPT_FRAGMENTS,
    API_KEYS_RUNTIME_SCRIPT_FRAGMENTS,
    API_KEYS_STYLE_FRAGMENTS,
    APP_HEADER_APP_SCRIPT_FRAGMENTS,
    APP_HEADER_STYLE_FRAGMENTS,
    APP_SIDEBAR_APP_SCRIPT_FRAGMENTS,
    APP_SIDEBAR_STYLE_FRAGMENTS,
    CALENDAR_BROWSER_FOUNDATION_FRAGMENTS,
    CALENDAR_SHELL_SCRIPT_FRAGMENTS,
    CALENDAR_STYLE_FRAGMENTS,
    CALENDAR_VENDOR_HEAD_HTML,
    CONFIGURE_HOME_APP_SCRIPT_FRAGMENTS,
    CONFIGURE_HOME_DOMAIN_SCRIPT_FRAGMENTS,
    CONFIGURE_HOME_PAGE_SCRIPT_FRAGMENTS,
    CONFIGURE_HOME_RUNTIME_SCRIPT_FRAGMENTS,
    CONFIGURE_HOME_STYLE_FRAGMENTS,
    COMPUTE_RESOURCES_PAGE_SCRIPT,
    DEVELOP_HOME_APP_SCRIPT_FRAGMENTS,
    DEVELOP_HOME_PAGE_SCRIPT,
    DEVELOP_HOME_RUNTIME_SCRIPT_FRAGMENTS,
    DEVELOP_HOME_STYLE_FRAGMENTS,
    ENVIRONMENT_CHANGES_CSS,
    ENVIRONMENT_CHANGES_SCRIPT,
    EVALUATIONS_AGENT_SCRIPT_FRAGMENTS,
    EVALUATIONS_AGENT_STYLE_FRAGMENTS,
    EVALUATIONS_APP_SCRIPT_FRAGMENTS,
    FILES_DOMAIN_FRAGMENTS,
    FILES_PAGE_RUNTIME_SCRIPT,
    FILES_PREVIEW_COMPONENTS_SCRIPT,
    FILES_STYLE_FRAGMENTS,
    FINE_TUNING_APP_SCRIPT_FRAGMENTS,
    GUARDRAILS_AGENT_SCRIPT_FRAGMENTS,
    GUARDRAILS_APP_SCRIPT_FRAGMENTS,
    GUARDRAILS_DOMAIN_FRAGMENTS,
    GUARDRAILS_PAGE_RUNTIME_SCRIPT,
    GUARDRAILS_STYLE_FRAGMENTS,
    IMAGINE_APP_SCRIPT_FRAGMENTS,
    IMAGINE_PAGE_CSS,
    IMAGINE_PAGE_SCRIPT,
    IMAGINE_SHELL_STYLE_FRAGMENTS,
    IMAGINE_TEMPLATE_PAGE_CSS,
    IMAGINE_TEMPLATE_PAGE_SCRIPT,
    INFERENCE_APP_SCRIPT_FRAGMENTS,
    INFERENCE_DOMAIN_SCRIPT_FRAGMENTS,
    INFERENCE_PAGE_CSS,
    MARKETPLACE_APP_SCRIPT_FRAGMENTS,
    MARKETPLACE_DOMAIN_SCRIPT_FRAGMENTS,
    MARKETPLACE_PAGE_CSS,
    MARKETPLACE_PAGE_SCRIPT,
    METRONOME_APP_SCRIPT_FRAGMENTS,
    METRONOME_PAGE_CSS,
    METRONOME_PAGE_SCRIPT,
    METRONOME_SHELL_RUNTIME_SCRIPT,
    METRONOME_SHELL_STYLE_FRAGMENTS,
    MODELS_AGENT_SCRIPT_FRAGMENTS,
    MODELS_APP_SCRIPT_FRAGMENTS,
    MODELS_PAGE_CSS,
    MODELS_PAGE_SCRIPT,
    ORGANIZATIONS_APP_SCRIPT_FRAGMENTS,
    ORGANIZATIONS_DOMAIN_SCRIPT_FRAGMENTS,
    ORGANIZATIONS_PAGE_SCRIPT_FRAGMENTS,
    ORGANIZATIONS_RUNTIME_SCRIPT_FRAGMENTS,
    ORGANIZATIONS_STYLE_FRAGMENTS,
    ONBOARDING_SCRIPT,
    PLATFORM_NAVIGATION_GUARD_APP_SCRIPT_FRAGMENTS,
    PLATFORM_UI_PRIMITIVES_CSS,
    PLATFORM_UI_PRIMITIVES_SCRIPT,
    PLAYGROUND_BILLING_CATALOG_SCRIPT,
    PLAYGROUND_EVALUATIONS_CSS,
    PLAYGROUND_EVALUATIONS_SCRIPT,
    PLAYGROUND_FINE_TUNING_CSS,
    PLAYGROUND_FINE_TUNING_SCRIPT,
    PROJECTS_DOMAIN_FOUNDATION_SCRIPT,
    PROJECTS_DOMAIN_RUNTIME_SCRIPT,
    PROJECTS_INTEGRATIONS_RUNTIME_SCRIPT,
    PROJECTS_PAGE_RUNTIME_SCRIPT,
    PROJECTS_STYLE_FRAGMENTS,
    SETTINGS_MODAL_APP_SCRIPT_FRAGMENTS,
    SETTINGS_MODAL_CSS,
    SETTINGS_MODAL_PAGE_SCRIPT,
    SKILLS_PAGE_SCRIPT,
    TEAMS_APP_SCRIPT_FRAGMENTS,
    TEAMS_DOMAIN_SCRIPT_FRAGMENTS,
    TEAMS_PAGE_SCRIPT_FRAGMENTS,
    TEAMS_RUNTIME_SCRIPT_FRAGMENTS,
    TEAMS_STYLE_FRAGMENTS,
    VERSIONING_CORE_SCRIPT,
    VERSION_SIDEBAR_SCRIPT,
    aiosOrigin,
    defaultUpstreamOrigin,
    platformOrigin,
  } = bindings;

  const inlinePlatformDocumentHtml = `<!doctype html>
  <html lang="en">
    <head>
      <meta charset="utf-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1" />
      <title>Agentic Compute Platform</title>
      <link rel="icon" type="image/png" sizes="32x32" href="/img/logos/favicon-32x32.png" />
      <link rel="icon" type="image/png" sizes="16x16" href="/img/logos/favicon-16x16.png" />
      <link rel="apple-touch-icon" sizes="180x180" href="/img/logos/apple-touch-icon.png" />
      <link rel="preconnect" href="https://fonts.googleapis.com" />
      <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
      <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
  ${CALENDAR_VENDOR_HEAD_HTML}
      <link rel="stylesheet" href="https://esm.sh/@xyflow/react@12.8.4/dist/style.css" />
      <link rel="stylesheet" href="/dist/platform-ui/components/composite/data-table/data-table.css" />
      <link rel="stylesheet" href="/dist/platform-ui/components/composite/popup/popup.css" />
      <link rel="stylesheet" href="/dist/platform-ui/components/composite/modal/modal.css" />
      <link rel="stylesheet" href="/dist/platform-ui/pages/styles.css" />
      <style>
        :root {
          --bg-0: #000000;
          --bg-1: #000000;
          --border-subtle: rgba(255, 255, 255, 0.08);
          --text: #f3f6ff;
          --text-dim: rgba(243, 246, 255, 0.68);
          --playground-chart-blue: #0C6FFF;
          --playground-chart-green: #35E8BB;
          --playground-chart-yellow: #FFE717;
          --playground-chart-pink: #FB9CE5;
          --playground-app-bg: #000000;
        }
  
        * { box-sizing: border-box; }
  
        body {
          margin: 0;
          color: var(--text);
          font-family: "Inter", -apple-system, BlinkMacSystemFont, "SF Pro Text", "Segoe UI", sans-serif;
          background: var(--playground-app-bg);
          background-size: cover;
          background-position: center;
          background-repeat: no-repeat;
          width: 100vw;
          height: 100vh;
          overflow: hidden;
        }
  
        button,
        input,
        textarea,
        select {
          font-family: inherit;
        }
  
        button {
          border: 1px solid rgba(255, 255, 255, 0.1);
          box-shadow: none;
        }
  
        aside {
          backdrop-filter: blur(50px);
          -webkit-backdrop-filter: blur(50px);
        }
  
        #app {
          min-height: 100vh;
          background: #000;
        }
  
        .playground-static-boot {
          position: fixed;
          inset: 0;
          z-index: 2147483647;
          display: flex;
          align-items: center;
          justify-content: center;
          background: #000;
          color: #fff;
          overflow: hidden;
        }
  
        .playground-static-boot-card {
          position: relative;
          z-index: 1;
          display: flex;
          flex-direction: column;
          align-items: center;
          gap: 14px;
          text-align: center;
        }
  
        .playground-static-boot-row {
          display: flex;
          align-items: center;
          justify-content: center;
          gap: 16px;
        }
  
        .playground-static-boot-dots {
          display: grid;
          grid-template-columns: repeat(3, 4px);
          gap: 3px;
        }
  
        .playground-static-boot-dots span {
          width: 4px;
          height: 4px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.18);
          animation: playground-static-boot-pulse 900ms ease-in-out infinite;
        }
  
        .playground-static-boot-dots span:nth-child(2) { animation-delay: 80ms; }
        .playground-static-boot-dots span:nth-child(3) { animation-delay: 160ms; }
        .playground-static-boot-dots span:nth-child(4) { animation-delay: 240ms; }
        .playground-static-boot-dots span:nth-child(5) { animation-delay: 320ms; }
        .playground-static-boot-dots span:nth-child(6) { animation-delay: 400ms; }
        .playground-static-boot-dots span:nth-child(7) { animation-delay: 480ms; }
        .playground-static-boot-dots span:nth-child(8) { animation-delay: 560ms; }
        .playground-static-boot-dots span:nth-child(9) { animation-delay: 640ms; }
  
        .playground-static-boot-title {
          font-size: 18px;
          line-height: 1;
          font-weight: 600;
          letter-spacing: -0.01em;
        }
  
        .playground-static-boot-copy {
          max-width: 280px;
          margin: 0;
          color: rgba(255, 255, 255, 0.52);
          font-size: 12px;
          line-height: 1.55;
          font-weight: 400;
        }
  
        @keyframes playground-static-boot-pulse {
          0%, 100% { background: rgba(255, 255, 255, 0.18); }
          35% { background: rgba(255, 255, 255, 1); }
          65% { background: rgba(255, 255, 255, 0.45); }
        }
  
        .page {
          width: 100vw;
          height: 100vh;
          margin: 0;
          border-radius: 0;
          border: 0;
          background: var(--playground-app-bg);
          background-size: cover;
          background-position: center;
          background-repeat: no-repeat;
          overflow: hidden;
          display: grid;
          grid-template-columns: 340px minmax(0, 1fr);
          box-shadow: none;
        }
  
        .left {
          display: flex;
          flex-direction: column;
          border-right: 1px solid rgba(255,255,255,0.12);
          background: #020202;
          padding: 16px;
          overflow: auto;
        }
  
        .brand {
          display: flex;
          align-items: center;
          gap: 10px;
          font-weight: 500;
          margin-bottom: 14px;
        }
  
        .dot {
          width: 26px;
          height: 26px;
          border-radius: 50%;
          background: linear-gradient(to top, #0151a8, #016bdf, #4190e7);
          box-shadow: 0 6px 18px rgba(0, 128, 247, 0.35);
        }
  
        .panel {
          border-radius: 14px;
          border: 1px solid rgba(255,255,255,0.12);
          background: #080808;
          padding: 9px;
        }
  
        .panel h3 {
          margin: 0 0 10px;
          font-size: 12px;
        }
  
        .field {
          display: flex;
          flex-direction: column;
          gap: 5px;
          margin-bottom: 8px;
        }
  
        .field label {
          font-size: 10px;
          color: var(--text-dim);
        }
  
        .field input {
          border-radius: 10px;
          border: 1px solid rgba(255,255,255,0.1);
          background: rgba(255,255,255,0.04);
          color: #fff;
          outline: none;
          height: 34px;
          padding: 0 10px;
          font-size: 12px;
        }
  
        .readonly-value {
          border-radius: 10px;
          border: 1px solid rgba(255,255,255,0.1);
          background: rgba(255,255,255,0.02);
          color: #fff;
          min-height: 30px;
          padding: 8px 10px;
          font-size: 12px;
          line-height: 1.3;
          word-break: break-all;
        }
  
        .status-indicator-stack {
          margin-top: 0;
        }
  
        .status-indicator-list {
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-shell-status-indicators {
          position: absolute;
          left: 336px;
          bottom: 14px;
          z-index: 80;
          width: min(320px, calc(100vw - 352px));
          pointer-events: none;
        }
  
        .playground-shell.sidebar-collapsed .playground-shell-status-indicators {
          left: 14px;
          bottom: 58px;
          width: min(320px, calc(100vw - 28px));
        }
  
        .playground-shell.is-projects-page .playground-shell-status-indicators,
        .playground-shell.is-project-thread-page .playground-shell-status-indicators {
          left: 14px;
          width: min(320px, calc(100vw - 28px));
        }
  
        .status-indicator {
          display: flex;
          align-items: center;
          gap: 8px;
          position: relative;
          z-index: 0;
          padding: 10px;
          border-radius: 10px;
          border: 1px solid transparent;
          background: rgba(0, 0, 0, 0.75);
          box-shadow: 0 5px 8px rgba(0, 0, 0, 0.1);
          -webkit-backdrop-filter: blur(20px);
          backdrop-filter: blur(20px);
          pointer-events: auto;
          opacity: 0;
          transform: translateY(10px);
          transition: opacity 180ms ease, transform 180ms ease;
        }
  
        .status-indicator.is-visible {
          opacity: 1;
          transform: translateY(0);
        }
  
        .status-indicator.is-exiting {
          opacity: 0;
          transform: translateY(-10px);
        }
  
        .status-indicator::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          border-radius: 10px;
          padding: 1px;
          background: linear-gradient(-10deg, rgba(200, 200, 200, 0.15), rgba(255, 255, 255, 0.1), rgba(255, 255, 255, 0.07), rgba(255, 255, 255, 0.3));
          -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
          -webkit-mask-composite: xor;
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .status-indicator-media {
          width: 24px;
          min-width: 24px;
          height: 24px;
          display: flex;
          align-items: center;
          justify-content: center;
        }
  
        .status-indicator-logo {
          max-width: 16px;
          max-height: 16px;
          object-fit: contain;
          display: block;
        }
  
        .status-indicator-body {
          min-width: 0;
          flex: 1;
        }
  
        .status-indicator-title,
        .status-indicator-copy {
          font-size: 12px;
          line-height: 1.25;
        }
  
        .status-indicator-title {
          color: #fff;
          font-weight: 500;
        }
  
        .status-indicator-copy {
          color: rgba(255, 255, 255, 0.7);
          font-weight: 400;
          margin-top: 2px;
        }
  
        .status-indicator-progress {
          position: relative;
          width: 100%;
          height: 4px;
          margin-top: 8px;
          border-radius: 999px;
          overflow: hidden;
          background: rgba(255, 255, 255, 0.08);
        }
  
        .status-indicator-progress-fill {
          height: 100%;
          border-radius: inherit;
          background: linear-gradient(90deg, #7c6df5 0%, #b98cf4 100%);
          transition: width 220ms ease;
        }
  
        .status-indicator-progress-fill.is-indeterminate {
          position: absolute;
          inset: 0 auto 0 0;
          width: 42%;
          animation: playground-status-indicator-progress 1.15s ease-in-out infinite;
        }
  
        .status-indicator-close {
          width: 18px;
          height: 18px;
          padding: 0;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.5);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          flex-shrink: 0;
        }
  
        .status-indicator-close:hover {
          color: rgba(255, 255, 255, 0.8);
        }
  
        .status-indicator-close-icon {
          width: 14px;
          height: 14px;
        }
  
        .status-indicator-empty {
          font-size: 12px;
          color: var(--text-dim);
        }
  
        .toggle-row {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          margin-bottom: 10px;
        }
  
        .toggle-copy {
          display: flex;
          flex-direction: column;
          gap: 2px;
        }
  
        .toggle-copy strong {
          font-size: 12px;
        }
  
        .toggle-copy span {
          color: var(--text-dim);
          font-size: 11px;
          line-height: 1.35;
        }
  
        .toggle-button {
          position: relative;
          width: 50px;
          height: 28px;
          border-radius: 999px;
          border: 1px solid rgba(255,255,255,0.12);
          background: rgba(255,255,255,0.08);
          cursor: pointer;
          transition: background 160ms ease;
        }
  
        .toggle-button[data-active="true"] {
          background: rgba(0,128,247,0.38);
        }
  
        .toggle-button::after {
          content: "";
          position: absolute;
          top: 3px;
          left: 3px;
          width: 20px;
          height: 20px;
          border-radius: 50%;
          background: #fff;
          transition: transform 160ms ease;
        }
  
        .toggle-button[data-active="true"]::after {
          transform: translateX(22px);
        }
  
        .hint {
          margin-top: 8px;
          font-size: 11px;
          color: var(--text-dim);
          line-height: 1.4;
        }
  
        .right {
          min-width: 0;
          height: 100%;
          padding: 12px;
        }
  
  ${APP_SIDEBAR_STYLE_FRAGMENTS.foundation}
  
        .playground-main {
          position: relative;
          z-index: 1;
          min-width: 0;
          height: 100%;
          padding: 0;
          box-sizing: border-box;
          overflow: hidden;
        }
  
        .playground-shell.is-projects-page .playground-main {
          padding: 0;
        }
  
        .playground-shell.is-project-thread-page .playground-main {
          padding: 0;
        }
  
        .playground-shell.is-initial-thread-page .playground-main {
          padding: 0;
        }
  
        .playground-content-shell {
          --playground-content-nav-height: 50px;
          --playground-thread-task-detail-width: min(42vw, 520px);
          --playground-centered-page-max-width: 87.5rem;
          --playground-thread-content-max-width: 56rem;
          position: relative;
          height: 100%;
          min-height: 0;
          display: flex;
          flex-direction: column;
          background: rgba(255, 255, 255, 0.075);
          overflow: hidden;
        }
  
        .playground-content-nav {
          display: grid;
          grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
          align-items: center;
          gap: 16px;
          min-height: 56px;
          padding: 0 5px 0 0;
          background: transparent;
          box-sizing: border-box;
          transition: padding-right 280ms cubic-bezier(0.16, 1, 0.3, 1);
        }
  
        .playground-content-nav,
        .playground-tools-navbar,
        .playground-thread-welcome-navbar,
        .playground-files-browser-header,
        .playground-tasks-project-navbar,
        .playground-settings-plans-navbar {
          background: transparent;
        }
  
        .playground-content-shell.is-thread-task-detail-open > .playground-content-nav,
        .playground-content-shell.is-thread-side-detail-open > .playground-content-nav,
        .playground-content-shell.is-resources-side-detail-open > .playground-content-nav,
        .playground-content-shell.is-metronome-node-detail-open > .playground-content-nav,
        .playground-content-shell:has(> .playground-agent-versions-node-drawer.is-open) > .playground-content-nav,
        .playground-content-shell:has(> .playground-content-body.is-files-page .playground-files-shell.has-preview > .playground-files-preview) > .playground-content-nav,
        .playground-content-shell:has(.playground-tasks-shell.is-detail-open > .playground-tasks-detail-panel.is-project-task-detail) > .playground-content-nav {
          padding-right: calc(6px + var(--playground-thread-task-detail-width));
        }
  
        .playground-content-shell.is-thread-side-detail-open > .playground-content-nav {
          pointer-events: none;
        }
  
        .playground-content-title {
          min-width: 0;
          font-size: 12px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.94);
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-content-nav-left {
          min-width: 0;
          display: flex;
          align-items: center;
          gap: 8px;
        }
  
  ${CONFIGURE_HOME_STYLE_FRAGMENTS.foundation}${DEVELOP_HOME_STYLE_FRAGMENTS.foundation}${CONFIGURE_HOME_STYLE_FRAGMENTS.overviewCards}
  ${DEVELOP_HOME_STYLE_FRAGMENTS.content}      .playground-shell.is-initial-thread-page .playground-content-title {
          color: rgba(255, 255, 255, 0.7);
        }
  
        .playground-content-nav-center {
          display: flex;
          align-items: center;
          justify-content: center;
        }
  
        .playground-content-nav-center .content-mode-switch {
          border: 1px solid rgba(255, 255, 255, 0.1);
          background: transparent;
          z-index: 0;
        }
  
        .playground-content-nav-center .content-mode-switch::before {
          content: none;
          display: none;
        }
  
        .playground-content-nav-center .content-mode-switch > * {
          position: relative;
          z-index: 1;
        }
  
        .playground-content-nav-right {
          display: flex;
          align-items: center;
          justify-content: flex-end;
          gap: 6px;
        }
  
  ${APP_HEADER_STYLE_FRAGMENTS.header}
        .playground-content-shell.is-thread-task-detail-open > .playground-content-nav > .playground-content-nav-center,
        .playground-content-shell.is-thread-task-detail-open > .playground-content-nav > .playground-content-nav-right,
        .playground-content-shell.is-thread-side-detail-open > .playground-content-nav > .playground-content-nav-center,
        .playground-content-shell.is-thread-side-detail-open > .playground-content-nav > .playground-content-nav-right,
        .playground-content-shell.is-resources-side-detail-open > .playground-content-nav > .playground-content-nav-center,
        .playground-content-shell.is-resources-side-detail-open > .playground-content-nav > .playground-content-nav-right,
        .playground-content-shell.is-metronome-node-detail-open > .playground-content-nav > .playground-content-nav-center,
        .playground-content-shell.is-metronome-node-detail-open > .playground-content-nav > .playground-content-nav-right,
        .playground-content-shell:has(> .playground-content-body.is-files-page .playground-files-shell.has-preview > .playground-files-preview) > .playground-content-nav > .playground-content-nav-center,
        .playground-content-shell:has(> .playground-content-body.is-files-page .playground-files-shell.has-preview > .playground-files-preview) > .playground-content-nav > .playground-content-nav-right,
        .playground-content-shell:has(> .playground-content-body.is-files-page .playground-files-shell.has-preview > .playground-files-preview) > .playground-content-nav .playground-top-nav-left-extra,
        .playground-content-shell:has(> .playground-content-body.is-files-page .playground-files-shell.has-preview > .playground-files-preview) > .playground-content-nav .playground-top-nav-left-extra-separator,
        .playground-content-shell:has(.playground-tasks-shell.is-detail-open > .playground-tasks-detail-panel.is-project-task-detail) > .playground-content-nav > .playground-content-nav-center,
        .playground-content-shell:has(.playground-tasks-shell.is-detail-open > .playground-tasks-detail-panel.is-project-task-detail) > .playground-content-nav > .playground-content-nav-right {
          opacity: 0;
          pointer-events: none;
        }
  
        .playground-content-shell.is-thread-task-detail-open .tb-runner-chat .tb-task-preview-card {
          background: rgba(255, 255, 255, 0.1);
        }
  
        .playground-content-shell.is-thread-task-detail-open .tb-runner-chat .tb-task-preview-card:hover:enabled {
          background: rgba(255, 255, 255, 0.12);
        }
  
        .playground-content-shell.is-thread-task-detail-open .tb-runner-chat .tb-task-preview-card:disabled:hover {
          background: rgba(255, 255, 255, 0.1);
        }
  
  ${PROJECTS_STYLE_FRAGMENTS.connectorBrowser}
  
        .content-mode-switch {
          display: flex;
          align-items: center;
          gap: 8px;
          height: 30px;
          padding: 2px;
          box-sizing: border-box;
          border: 1px solid rgba(255, 255, 255, 0.10);
          border-radius: 20px;
          background: transparent;
          backdrop-filter: blur(20px);
          -webkit-backdrop-filter: blur(20px);
          line-height: 1rem;
          position: relative;
          overflow: hidden;
          transition: opacity 180ms ease;
        }
  
        .playground-thread-mode-switch {
          border: 1px solid rgba(255, 255, 255, 0.1);
          background: transparent;
          z-index: 0;
        }
  
        .playground-thread-mode-switch::before {
          content: none;
          display: none;
        }
  
        .playground-thread-mode-switch > * {
          position: relative;
          z-index: 1;
        }
  
        body.tb-runner-document-preview-active .content-mode-switch {
          opacity: 0;
          pointer-events: none;
        }
  
        .playground-thread-details-mode-switch {
          transition: opacity 180ms ease;
        }
  
        .playground-thread-details-mode-switch .platform-switch__option {
          min-width: 64px;
        }
  
        body.tb-runner-document-preview-active .playground-thread-details-mode-switch {
          opacity: 0;
          pointer-events: none;
        }
  
        .content-mode-button {
          min-width: 64px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          padding: 4px 12px;
          border: 0;
          border-radius: 20px;
          background: transparent;
          color: rgba(255, 255, 255, 0.72);
          font-size: 12px;
          font-weight: 400;
          line-height: 1rem;
          cursor: pointer;
          transition: background-color 160ms ease, color 160ms ease, opacity 160ms ease;
        }
  
        .content-mode-button.is-active {
          background: rgba(255, 255, 255, 0.3);
          color: white;
        }
  
        .content-mode-button:disabled {
          opacity: 0.45;
          cursor: default;
        }
  
        .playground-content-menu-button {
          position: relative;
          width: 32px;
          height: 32px;
          padding: 0;
          border: 0;
          border-radius: 10px;
          background: transparent;
          color: rgba(255, 255, 255, 0.7);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          transition: background-color 160ms ease, color 160ms ease;
        }
  
        .playground-content-menu-button:hover {
          background: rgba(255, 255, 255, 0.08);
          color: white;
        }
  
        .playground-content-menu-button[aria-expanded="true"] {
          background: rgba(255, 255, 255, 0.1);
          color: white;
        }
  
        .playground-content-menu-button.is-private-active {
          background: rgba(96, 165, 250, 0.16);
          color: #93c5fd;
          box-shadow: inset 0 0 0 1px rgba(96, 165, 250, 0.54);
        }
  
        .playground-content-menu-button.is-private-active:hover {
          background: rgba(96, 165, 250, 0.22);
          color: #bfdbfe;
        }
  
        .playground-content-menu-icon {
          width: 16px;
          height: 16px;
        }
  
        .playground-content-menu-button.has-notifications::after {
          content: "";
          position: absolute;
          top: 6px;
          right: 6px;
          width: 7px;
          height: 7px;
          border-radius: 999px;
          background: #60a5fa;
          box-shadow: 0 0 0 2px #0b0b0c;
        }
  
        .playground-files-control-button.playground-tasks-nav-issue-button {
          margin-left: 0;
        }
  
        .playground-unified-top-navbar .playground-files-control-button.playground-tasks-nav-issue-button {
          margin-left: 0;
        }
  
        .playground-content-body {
          flex: 1;
          min-height: 0;
          position: relative;
          margin: 0;
          border: 0;
          border-radius: 0;
          background: #000;
          background-clip: padding-box;
          box-sizing: border-box;
          contain: paint;
          overflow: hidden;
          transition: margin-right 280ms cubic-bezier(0.16, 1, 0.3, 1);
        }
  
        .playground-content-body.is-thread-task-detail-open,
        .playground-content-body.is-thread-side-detail-open,
        .playground-content-body.is-metronome-node-detail-open,
        .playground-content-body.is-agent-versions-detail-open {
          margin-right: calc(var(--playground-thread-task-detail-width) + 6px);
        }
  
        .playground-content-body.is-files-page,
        .playground-content-body.is-tasks-page,
        .playground-content-body.is-calendar-page {
          margin: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          background-clip: border-box;
          contain: none;
        }
  
        .playground-content-body.is-guardrails-page.is-agent-versions-detail-open,
        .playground-content-body.is-evaluations-page.is-agent-versions-detail-open {
          margin: 0 calc(var(--playground-thread-task-detail-width) + 6px) 0 0;
        }
  
        .playground-thread-task-drawer {
          position: absolute;
          top: 0;
          right: 6px;
          bottom: 6px;
          z-index: 120;
          width: var(--playground-thread-task-detail-width);
          min-width: 0;
          display: flex;
          flex-direction: column;
          border-left: 0;
          background: transparent;
          opacity: 0;
          transform: translateX(100%);
          pointer-events: none;
          transition:
            transform 280ms cubic-bezier(0.16, 1, 0.3, 1),
            opacity 220ms ease;
        }
  
        .playground-thread-task-drawer.is-open {
          opacity: 1;
          transform: translateX(0);
          pointer-events: auto;
        }
  
  ${METRONOME_SHELL_STYLE_FRAGMENTS.drawer}
  
        .playground-agent-versions-node-drawer {
          z-index: 10050;
        }
  
        .playground-thread-subagent-drawer {
          position: absolute;
          top: 0;
          right: 6px;
          bottom: 6px;
          z-index: 120;
          width: var(--playground-thread-task-detail-width);
          min-width: 0;
          display: flex;
          flex-direction: column;
          border-left: 0;
          background: transparent;
          opacity: 0;
          transform: translateX(100%);
          pointer-events: none;
          transition:
            width 280ms cubic-bezier(0.16, 1, 0.3, 1),
            transform 280ms cubic-bezier(0.16, 1, 0.3, 1),
            opacity 220ms ease;
        }
  
        body.tb-runner-document-preview-maximized .playground-content-shell.is-thread-side-detail-open {
          --playground-thread-task-detail-width: calc(100% - 6px);
        }
  
        body.tb-runner-document-preview-maximized .playground-content-body.is-thread-side-detail-open {
          margin-right: calc(var(--playground-thread-task-detail-width) + 6px);
          contain: none;
          overflow: visible;
        }
  
        body.tb-runner-document-preview-maximized .playground-thread-subagent-drawer {
          width: calc(100% - 6px);
        }
  
        .playground-thread-subagent-drawer.is-open {
          opacity: 1;
          transform: translateX(0);
          pointer-events: auto;
        }
  
        .playground-thread-preview-drawer {
          position: absolute;
          top: 0;
          right: 6px;
          bottom: 6px;
          z-index: 120;
          width: var(--playground-thread-task-detail-width);
          min-width: 0;
          display: flex;
          flex-direction: column;
          border-left: 0;
          background: transparent;
          opacity: 0;
          transform: translateX(100%);
          pointer-events: none;
          transition:
            transform 280ms cubic-bezier(0.16, 1, 0.3, 1),
            opacity 220ms ease;
        }
  
        .playground-thread-preview-drawer.is-open {
          opacity: 1;
          transform: translateX(0);
          pointer-events: auto;
        }
  
        .playground-thread-preview-drawer .tb-runner-document-preview-host,
        .playground-thread-preview-drawer .tb-runner-document-preview-host-inline {
          flex: 1 1 auto;
          min-width: 0;
          min-height: 0;
          height: 100%;
        }
  
        .playground-thread-preview-drawer .tb-attachment-preview-drawer-inline {
          position: relative;
          top: auto;
          right: auto;
          bottom: auto;
          width: 100%;
          height: 100%;
          border-left: 0;
          background: transparent;
          box-shadow: none;
          transform: none;
          opacity: 1;
          pointer-events: auto;
        }
  
        .playground-thread-preview-drawer .tb-attachment-preview-drawer-header {
          height: 56px;
          min-height: 56px;
          padding: 0 10px;
          box-sizing: border-box;
        }
  
        .playground-thread-preview-drawer .tb-attachment-preview-drawer-header-icon-asset,
        .playground-thread-preview-drawer .tb-attachment-preview-drawer-header-icon,
        .playground-thread-subagent-drawer .tb-attachment-preview-drawer-header-icon-asset,
        .playground-thread-subagent-drawer .tb-attachment-preview-drawer-header-icon {
          display: none;
        }
  
        .playground-thread-preview-drawer .tb-attachment-preview-drawer-header-copy,
        .playground-thread-subagent-drawer .tb-attachment-preview-drawer-header-copy {
          gap: 0;
        }
  
        .playground-thread-preview-drawer .tb-attachment-preview-drawer-name,
        .playground-thread-subagent-drawer .tb-attachment-preview-drawer-name,
        .playground-thread-subagent-drawer .tb-subagent-detail-drawer-title {
          font-size: 12px;
          font-weight: 400;
        }
  
        .playground-thread-preview-drawer .tb-attachment-preview-drawer-body {
          padding: 0 10px;
          box-sizing: border-box;
        }
  
        .playground-thread-preview-drawer .tb-attachment-preview-drawer-body,
        .playground-thread-preview-drawer .tb-attachment-preview-pdf,
        .playground-thread-preview-drawer .tb-attachment-preview-image-surface,
        .playground-thread-preview-drawer .tb-attachment-preview-state,
        .playground-thread-preview-drawer .tb-attachment-preview-markdown-shell,
        .playground-thread-preview-drawer .tb-attachment-preview-text,
        .playground-thread-preview-drawer .tb-attachment-preview-docx-shell {
          background: transparent;
        }
  
        .playground-content-shell .playground-thread-preview-drawer .tb-runner-document-preview-host,
        .playground-content-shell .playground-thread-preview-drawer .tb-runner-document-preview-host-inline,
        .playground-content-shell .playground-thread-preview-drawer .tb-attachment-preview-drawer,
        .playground-content-shell .playground-thread-preview-drawer .tb-attachment-preview-drawer-inline,
        .playground-content-shell .playground-thread-preview-drawer .tb-attachment-preview-drawer-header,
        .playground-content-shell .playground-thread-preview-drawer .tb-attachment-preview-drawer-body,
        .playground-content-shell .playground-thread-preview-drawer .tb-attachment-preview-code-shell,
        .playground-content-shell .playground-thread-preview-drawer .tb-attachment-preview-pdf,
        .playground-content-shell .playground-thread-preview-drawer .tb-attachment-preview-image-surface,
        .playground-content-shell .playground-thread-preview-drawer .tb-attachment-preview-state,
        .playground-content-shell .playground-thread-preview-drawer .tb-attachment-preview-markdown-shell,
        .playground-content-shell .playground-thread-preview-drawer .tb-attachment-preview-text,
        .playground-content-shell .playground-thread-preview-drawer .tb-attachment-preview-docx-shell {
          background: transparent !important;
          box-shadow: none !important;
        }
  
        .playground-thread-preview-drawer .tb-attachment-preview-code-shell,
        .playground-thread-subagent-drawer .tb-attachment-preview-code-shell {
          height: 100%;
          min-height: 100%;
          display: flex;
          flex-direction: column;
          min-width: 0;
        }
  
        .playground-thread-preview-drawer .tb-attachment-preview-code-shell .tb-log-card-code,
        .playground-thread-subagent-drawer .tb-attachment-preview-code-shell .tb-log-card-code {
          flex: 1 1 auto;
          min-height: 0;
          height: 100% !important;
        }
  
        .playground-thread-preview-drawer .tb-attachment-preview-code-shell .tb-log-card-code > *,
        .playground-thread-preview-drawer .tb-attachment-preview-code-shell .monaco-editor,
        .playground-thread-preview-drawer .tb-attachment-preview-code-shell .overflow-guard,
        .playground-thread-subagent-drawer .tb-attachment-preview-code-shell .tb-log-card-code > *,
        .playground-thread-subagent-drawer .tb-attachment-preview-code-shell .monaco-editor,
        .playground-thread-subagent-drawer .tb-attachment-preview-code-shell .overflow-guard {
          height: 100% !important;
          min-height: 0;
        }
  
        .playground-thread-subagent-drawer-host {
          flex: 1;
          min-height: 0;
        }
  
        .playground-thread-subagent-drawer-host.tb-runner-chat {
          position: relative;
          width: 100%;
          height: 100%;
          min-width: 0;
          min-height: 0;
          display: flex;
          flex-direction: column;
          grid-template-rows: none;
          background: transparent !important;
        }
  
        .playground-thread-subagent-drawer-host.tb-runner-chat > .tb-subagent-detail-drawer {
          flex: 1 1 auto;
          width: 100%;
          min-width: 0;
          max-width: none;
        }
  
        .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat > .tb-subagent-detail-drawer {
          position: relative;
          top: auto;
          right: auto;
          bottom: auto;
          width: 100%;
          height: 100%;
          border-left: 0;
          background: transparent;
          box-shadow: none;
          transform: none;
          opacity: 1;
          pointer-events: auto;
        }
  
        .playground-thread-subagent-drawer .tb-subagent-detail-drawer-header {
          min-height: 56px;
          padding: 0 10px;
          background: transparent;
        }
  
        .playground-thread-subagent-drawer .tb-subagent-detail-drawer-body {
          padding: 0 10px 10px;
          background: transparent;
        }
  
        .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat > .tb-attachment-preview-drawer {
          position: relative;
          top: auto;
          right: auto;
          bottom: auto;
          z-index: auto;
          width: 100%;
          height: 100%;
          min-height: 0;
          flex: 1 1 auto;
          border-left: 0;
          background: transparent;
          box-shadow: none;
          transform: none;
          opacity: 1;
          pointer-events: auto;
        }
  
        .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat .tb-runner-document-preview-host-inline .tb-attachment-preview-drawer {
          position: relative;
          top: auto;
          right: auto;
          bottom: auto;
          z-index: auto;
          width: 100%;
          height: 100%;
          min-height: 0;
          flex: 1 1 auto;
          border-left: 0;
          background: transparent;
          box-shadow: none;
          transform: none;
          opacity: 1;
          pointer-events: auto;
        }
  
        .playground-thread-subagent-drawer .tb-attachment-preview-drawer-header {
          height: 56px;
          min-height: 56px;
          padding: 0 10px;
          border-bottom: 0;
          background: transparent;
          box-sizing: border-box;
        }
  
        .playground-thread-subagent-drawer .tb-attachment-preview-drawer-body,
        .playground-thread-subagent-drawer .tb-attachment-preview-pdf,
        .playground-thread-subagent-drawer .tb-attachment-preview-image-surface,
        .playground-thread-subagent-drawer .tb-attachment-preview-state,
        .playground-thread-subagent-drawer .tb-attachment-preview-markdown-shell,
        .playground-thread-subagent-drawer .tb-attachment-preview-text,
        .playground-thread-subagent-drawer .tb-attachment-preview-docx-shell {
          background: transparent;
        }
  
        .playground-content-shell .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat > .tb-attachment-preview-drawer,
        .playground-content-shell .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat,
        .playground-content-shell .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat > .tb-subagent-detail-drawer,
        .playground-content-shell .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat > .tb-runner-document-preview-host,
        .playground-content-shell .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat > .tb-runner-document-preview-host-inline,
        .playground-content-shell .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat .tb-runner-document-preview-host,
        .playground-content-shell .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat .tb-runner-document-preview-host-inline,
        .playground-content-shell .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat .tb-runner-document-preview-host-inline .tb-attachment-preview-drawer,
        .playground-content-shell .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat .tb-attachment-preview-drawer-header,
        .playground-content-shell .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat .tb-attachment-preview-drawer-body,
        .playground-content-shell .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat .tb-attachment-preview-code-shell,
        .playground-content-shell .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat .tb-attachment-preview-pdf,
        .playground-content-shell .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat .tb-attachment-preview-image-surface,
        .playground-content-shell .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat .tb-attachment-preview-state,
        .playground-content-shell .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat .tb-attachment-preview-markdown-shell,
        .playground-content-shell .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat .tb-attachment-preview-text,
        .playground-content-shell .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat .tb-attachment-preview-docx-shell,
        .playground-content-shell .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat .tb-subagent-detail-drawer-header,
        .playground-content-shell .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat .tb-subagent-detail-drawer-body,
        .playground-content-shell .playground-thread-subagent-drawer .playground-thread-subagent-drawer-host.tb-runner-chat .tb-subagent-log-shell {
          background: transparent !important;
          box-shadow: none !important;
        }
  
        .playground-thread-task-drawer .playground-tasks-detail-scroll {
          padding-left: 12px;
          padding-right: 12px;
        }
  
        .playground-thread-task-drawer .playground-tasks-comment-dock {
          padding-left: 12px;
          padding-right: 12px;
        }
  
        .playground-thread-task-drawer .tb-runner-chat .tb-log-scroll,
        .playground-thread-subagent-drawer .tb-runner-chat .tb-log-scroll {
          padding-left: 22px;
          padding-right: 22px;
        }
  
        .playground-thread-task-drawer .tb-runner-chat .tb-input-shell,
        .playground-thread-subagent-drawer .tb-runner-chat .tb-input-shell {
          padding-left: 22px;
          padding-right: 22px;
        }
  
        .playground-thread-task-drawer .tb-runner-chat .tb-thread-history-rail,
        .playground-thread-subagent-drawer .tb-runner-chat .tb-thread-history-rail {
          right: 22px;
        }
  
        .playground-thread-runner.is-project-wallpaper-active .tb-log-card-panel {
          background: rgba(255, 255, 255, 0.05);
          -webkit-backdrop-filter: none;
          backdrop-filter: none;
        }
  
        .playground-thread-runner.tb-runner-chat {
          --tb-image-preview-side-width: var(--playground-thread-task-detail-width);
        }
  
        body.tb-runner-document-preview-maximized .playground-thread-runner.tb-runner-chat {
          --tb-image-preview-side-width: 100vw;
        }
  
        .playground-thread-runner.is-project-wallpaper-active .tb-log-file-preview-frame,
        .playground-thread-runner.is-project-wallpaper-active .tb-log-file-preview-topbar,
        .playground-thread-runner.is-project-wallpaper-active .tb-log-terminal-body,
        .playground-thread-runner.is-project-wallpaper-active .tb-runner-diff-surface,
        .playground-thread-runner.is-project-wallpaper-active .tb-runner-diff-surface-topbar,
        .playground-thread-runner.is-project-wallpaper-active .tb-runner-diff-surface-body,
        .playground-thread-runner.is-project-wallpaper-active .tb-runner-diff-surface-fallback,
        .playground-thread-runner.is-project-wallpaper-active .tb-log-task-create-item,
        .playground-thread-runner.is-project-wallpaper-active .tb-log-resource-create-item,
        .playground-thread-runner.is-project-wallpaper-active .tb-subagent-log-prompt,
        .playground-thread-runner.is-project-wallpaper-active .tb-subagent-log-summary,
        .playground-thread-runner.is-project-wallpaper-active .tb-deep-research-log-summary {
          -webkit-backdrop-filter: none;
          backdrop-filter: none;
        }
  
        .playground-thread-runner.is-project-wallpaper-active .tb-log-file-preview-frame,
        .playground-thread-runner.is-project-wallpaper-active .tb-log-file-preview-topbar,
        .playground-thread-runner.is-project-wallpaper-active .tb-log-file-preview-body,
        .playground-thread-runner.is-project-wallpaper-active .tb-log-terminal-body,
        .playground-thread-runner.is-project-wallpaper-active .tb-runner-diff-surface,
        .playground-thread-runner.is-project-wallpaper-active .tb-runner-diff-surface-topbar,
        .playground-thread-runner.is-project-wallpaper-active .tb-runner-diff-surface-body,
        .playground-thread-runner.is-project-wallpaper-active .tb-runner-diff-surface-fallback {
          background: transparent !important;
          box-shadow: none;
        }
  
        .tb-runner-diff-surface,
        .playground-thread-runner.is-project-wallpaper-active .tb-runner-diff-surface {
          background: transparent !important;
        }
  
        .tb-runner-diff-surface-topbar,
        .playground-thread-runner.is-project-wallpaper-active .tb-runner-diff-surface-topbar {
          background: rgba(255, 255, 255, 0.05) !important;
        }
  
        .tb-runner-diff-surface-body,
        .playground-thread-runner.is-project-wallpaper-active .tb-runner-diff-surface-body {
          background: rgba(255, 255, 255, 0.1) !important;
        }
  
        .playground-thread-runner.is-project-wallpaper-active .task-input-box {
          --tb-task-input-base-bg: rgba(255, 255, 255, 0.1);
        }
  
        .playground-shell.is-initial-thread-page .tb-runner-chat.playground-thread-runner .task-input-box {
          --tb-task-input-overlay: transparent;
          --tb-task-input-base-bg: transparent;
        }
  
        .playground-shell:not(.is-initial-thread-page) .tb-runner-chat.playground-thread-runner .task-input-box {
          --tb-task-input-overlay: transparent;
          --tb-task-input-base-bg: rgba(30, 30, 30, 0.9);
          -webkit-backdrop-filter: blur(5px);
          backdrop-filter: blur(5px);
        }
  
        .playground-thread-runner.is-initial-welcome-runner {
          display: flex;
          flex-direction: column;
          align-items: stretch;
          justify-content: center;
          position: relative;
          z-index: 40;
          height: 100%;
          min-height: 0;
          box-sizing: border-box;
          padding: 28px 0;
          overflow: hidden;
        }
  
        .playground-shell.is-initial-thread-page .playground-main {
          z-index: 8;
        }
  
        .playground-thread-runner.is-initial-welcome-runner .tb-popup-backdrop {
          z-index: 70;
        }
  
        .playground-thread-runner.is-initial-welcome-runner .tb-popup-menu {
          z-index: 80;
        }
  
        .playground-thread-runner.is-initial-welcome-runner .workinglogsbox {
          width: 100%;
          flex: 0 0 auto;
          height: auto;
          min-height: 0;
          max-width: 100%;
          overflow: visible;
        }
  
        .playground-thread-runner.is-initial-welcome-runner .tb-log-scroll.is-custom-empty-state {
          flex: 0 0 auto;
          height: auto;
          max-height: none;
          justify-content: flex-start;
          align-items: center;
          overflow: visible;
          padding: 0 20px 0;
        }
  
        .playground-thread-runner.is-initial-welcome-runner .tb-content-width.is-custom-empty-state {
          justify-content: flex-start;
          align-items: stretch;
        }
  
        .playground-thread-runner.is-initial-welcome-runner .tb-input-shell {
          position: static;
          width: 100%;
          flex: 0 0 auto;
          justify-content: center;
          right: auto;
          bottom: auto;
          left: auto;
          padding: 0 20px;
          margin: 24px 0;
        }
  
        .playground-thread-runner.is-initial-welcome-runner .tb-input-width {
          width: min(100%, var(--playground-thread-content-max-width));
          max-width: var(--playground-thread-content-max-width);
        }
  
        .playground-thread-welcome {
          width: min(100%, var(--playground-thread-content-max-width));
          margin: 0 auto;
          display: flex;
          flex-direction: column;
          align-items: stretch;
          gap: 20px;
          padding: 28px 0 48px;
          text-align: left;
          pointer-events: auto;
        }
  
        .playground-thread-welcome-intro {
          padding-top: 0;
          padding-bottom: 0;
          gap: 24px;
        }
  
        .playground-thread-welcome-sections {
          width: min(100%, 692px);
          max-width: 692px;
          padding-top: 0;
        }
  
        .playground-thread-welcome-copy {
          width: 100%;
          min-height: 0;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: flex-start;
          gap: 8px;
          text-align: center;
        }
  
        .playground-thread-welcome-title {
          margin: 0;
          color: rgba(255, 255, 255, 0.96);
          font-size: 36px;
          font-weight: 400;
          line-height: 1.04;
          letter-spacing: -0.04em;
          text-wrap: balance;
        }
  
        .playground-thread-welcome-summary-line {
          margin: 0;
          color: rgba(255, 255, 255, 0.56);
          font-size: 12px;
          line-height: 1.5;
          font-weight: 500;
        }
  
        .playground-thread-welcome-actions {
          display: flex;
          align-items: center;
          justify-content: center;
          gap: 10px;
          flex-wrap: wrap;
          margin-bottom: 28px;
        }
  
        .playground-thread-welcome-thread-grid {
          display: grid;
          grid-template-columns: repeat(3, minmax(0, 1fr));
          gap: 14px;
        }
  
        .playground-thread-welcome-thread-card {
          position: relative;
          display: flex;
          flex-direction: column;
          align-items: flex-start;
          gap: 12px;
          min-height: 110px;
          padding: 14px 48px 14px 16px;
          border: 0;
          border-radius: 15px;
          background: rgba(255, 255, 255, 0.05);
          background-clip: padding-box;
          color: rgba(255, 255, 255, 0.94);
          text-align: left;
          cursor: pointer;
          transition: background 160ms ease, transform 160ms ease;
        }
  
        .playground-thread-welcome-thread-card:hover {
          background: rgba(255, 255, 255, 0.14);
          transform: translateY(-1px);
        }
  
        .playground-thread-welcome-thread-card-top {
          width: 100%;
          display: flex;
          align-items: center;
          justify-content: flex-start;
          gap: 12px;
        }
  
        .playground-thread-welcome-thread-status {
          display: inline-flex;
          align-items: center;
          min-height: 20px;
          padding: 0 8px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.72);
          font-size: 8px;
          font-weight: 600;
          letter-spacing: 0.08em;
          text-transform: uppercase;
        }
  
        .playground-thread-welcome-thread-status.is-running {
          background: rgba(96, 165, 250, 0.1);
          color: #93c5fd;
        }
  
        .playground-thread-welcome-thread-status.is-permission {
          background: rgba(96, 165, 250, 0.1);
          color: #60a5fa;
        }
  
        .playground-thread-welcome-thread-status.is-done {
          background: rgba(74, 222, 128, 0.1);
          color: #86efac;
        }
  
        .playground-thread-welcome-thread-status.is-upcoming {
          background: rgba(196, 181, 253, 0.1);
          color: #c4b5fd;
        }
  
        .playground-thread-welcome-thread-arrow {
          position: absolute;
          top: 50%;
          right: 16px;
          transform: translateY(-50%);
          width: 20px;
          height: 20px;
          color: rgba(255, 255, 255, 0.38);
          flex: 0 0 auto;
        }
  
        .playground-thread-welcome-thread-title {
          margin: 0;
          color: rgba(255, 255, 255, 0.96);
          font-size: 12px;
          font-weight: 500;
          line-height: 1.3;
        }
  
        .playground-thread-welcome-thread-meta {
          margin-top: auto;
          color: rgba(255, 255, 255, 0.64);
          font-size: 10px;
          line-height: 1.5;
        }
  
        .playground-thread-welcome-thread-empty {
          padding: 16px 18px;
          border: 1px dashed rgba(255, 255, 255, 0.12);
          border-radius: 10px;
          color: rgba(255, 255, 255, 0.62);
          font-size: 13px;
        }
  
        .playground-thread-welcome-action {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          min-height: 34px;
          padding: 0 14px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.86);
          font-size: 12px;
          line-height: 1;
          font-weight: 500;
          cursor: pointer;
          transition: background 160ms ease, border-color 160ms ease, color 160ms ease;
        }
  
        .playground-thread-welcome-action:hover {
          border-color: rgba(255, 255, 255, 0.14);
          background: rgba(255, 255, 255, 0.07);
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-thread-welcome-action-icon {
          width: 13px;
          height: 13px;
          color: rgba(255, 255, 255, 0.62);
          flex: 0 0 auto;
        }
  
        .playground-thread-home-section {
          display: flex;
          flex-direction: column;
          gap: 12px;
        }
  
        .playground-thread-home-section-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-thread-home-section-title {
          margin: 0;
          font-size: 16px;
          line-height: 1.2;
          font-weight: 500;
          color: #ffffff;
        }
  
        .playground-thread-home-section-subtitle {
          margin: 0;
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.54);
        }
  
        .playground-thread-home-section-link {
          display: inline-flex;
          align-items: center;
          gap: 6px;
          padding: 0;
          border: 0;
          background: transparent;
          color: #60a5fa;
          font-size: 12px;
          line-height: 1.2;
          font-weight: 500;
          cursor: pointer;
        }
  
        .playground-thread-home-section-link:hover {
          color: #93c5fd;
        }
  
        .playground-thread-home-dashboard-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 14px;
          margin-top: 24px;
          padding-bottom: 12px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-thread-home-dashboard-actions {
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          gap: 16px;
          flex: 0 0 auto;
        }
  
        .playground-thread-home-project-title-button {
          min-width: 0;
          max-width: 100%;
          display: inline-flex;
          align-items: center;
          gap: 6px;
          padding: 0;
          border: 0;
          background: transparent;
          color: #ffffff;
          text-align: left;
          cursor: pointer;
        }
  
        .playground-thread-home-project-title-button:disabled {
          cursor: default;
          opacity: 0.72;
        }
  
        .playground-thread-home-project-title-button .playground-thread-home-section-title {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-thread-home-project-title-chevron {
          width: 15px;
          height: 15px;
          flex: 0 0 auto;
          color: rgba(255, 255, 255, 0.72);
        }
  
        .playground-thread-home-dashboard {
          display: grid;
          grid-template-columns: repeat(3, minmax(0, 220px));
          gap: 24px;
          justify-content: center;
          align-items: start;
        }
  
        .playground-thread-home-project-column {
          min-width: 0;
          display: flex;
          justify-content: center;
        }
  
        .playground-thread-home-calendar-column {
          min-width: 0;
          display: flex;
          justify-content: center;
        }
  
        .playground-thread-home-usage-column {
          min-width: 0;
          display: flex;
          justify-content: center;
        }
  
        .playground-thread-home-empty {
          display: flex;
          align-items: center;
          justify-content: center;
          min-height: 120px;
          padding: 0 18px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 18px;
          background: rgba(255, 255, 255, 0.03);
          color: rgba(255, 255, 255, 0.56);
          font-size: 12px;
          line-height: 1.6;
          text-align: center;
        }
  
        .playground-thread-home-project-surface {
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-thread-widget.playground-thread-home-project-widget {
          width: 220px;
          height: 220px;
          min-height: 0;
          padding: 0;
          border-radius: 25px;
          background: rgba(255, 255, 255, 0.05);
          clip-path: inset(0 round 25px);
          isolation: isolate;
          overflow: hidden;
          gap: 0;
        }
  
        .playground-thread-home-project-widget .playground-thread-widget-tasks-surface {
          background: transparent;
        }
  
        .playground-thread-home-project-name {
          margin: 0 0 12px;
          min-width: 0;
          color: rgba(255, 255, 255, 0.96);
          font-size: 20px;
          line-height: 1.25;
          font-weight: 500;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-thread-home-project-widget .playground-thread-home-project-ticket-list {
          margin-bottom: 0;
        }
  
        .playground-thread-home-project-widget .playground-tasks-backlog-item {
          border: 1px solid rgba(255, 255, 255, 0.05);
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-thread-home-project-widget-empty {
          min-height: 0;
          flex: 1 1 auto;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          gap: 10px;
          padding: 8px 2px 2px;
          color: rgba(255, 255, 255, 0.56);
          font-size: 11px;
          line-height: 1.5;
          text-align: center;
        }
  
        .playground-thread-home-project-widget-empty.is-projects-empty,
        .playground-thread-home-project-widget-empty.is-backlog-empty {
          width: 100%;
          align-items: center;
          gap: 7px;
          padding: 4px 8px 2px;
          color: rgba(255, 255, 255, 0.5);
          text-align: center;
        }
  
        .playground-thread-home-project-widget-empty.is-projects-empty svg,
        .playground-thread-home-project-widget-empty.is-backlog-empty svg {
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-thread-home-project-widget-empty-title {
          color: rgba(255, 255, 255, 0.96);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.2;
        }
  
        .playground-thread-home-project-widget-empty-copy {
          max-width: 176px;
          color: rgba(255, 255, 255, 0.5);
          font-size: 10px;
          font-weight: 400;
          line-height: 1.32;
        }
  
        .playground-thread-home-project-widget-empty.is-projects-empty .playground-tasks-empty-actions,
        .playground-thread-home-project-widget-empty.is-backlog-empty .playground-tasks-empty-actions {
          margin-top: 4px;
        }
  
        .playground-thread-home-project-widget-empty.is-projects-empty .playground-tasks-empty-primary-button,
        .playground-thread-home-project-widget-empty.is-backlog-empty .playground-tasks-empty-primary-button {
          min-height: 28px;
          padding: 0 12px;
          gap: 7px;
          font-size: 11px;
          font-weight: 400;
        }
  
        .playground-thread-home-project-widget-empty.is-projects-empty .playground-tasks-empty-primary-button svg,
        .playground-thread-home-project-widget-empty.is-backlog-empty .playground-tasks-empty-primary-button svg {
          color: #000;
        }
  
        .playground-thread-home-project-empty-card {
          width: 100%;
          height: 260px;
          min-height: 260px;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          gap: 13px;
          padding: 18px;
          border: 1px solid rgba(255, 255, 255, 0.05);
          border-radius: 25px;
          background: rgba(255, 255, 255, 0.05);
          text-align: center;
        }
  
        .playground-thread-home-project-empty-card .playground-tasks-empty-primary-button {
          font-weight: 500;
        }
  
        .playground-thread-home-task-card {
          border-radius: 18px;
        }
  
        .playground-thread-home-task-footer {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-thread-home-task-ticket {
          color: rgba(255, 255, 255, 0.54);
          font-size: 11px;
          line-height: 1.2;
        }
  
        .playground-thread-welcome-widgets {
          width: min(100%, 692px);
          display: grid;
          grid-template-columns: repeat(3, minmax(0, 220px));
          gap: 16px;
          align-items: stretch;
          justify-content: center;
        }
  
        .playground-thread-widget {
          --playground-home-widget-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          position: relative;
          display: flex;
          flex-direction: column;
          border: 0;
          border-radius: 25px;
          background: #171717;
          box-shadow: 0 22px 52px rgba(0, 0, 0, 0.24);
          color: rgba(255, 255, 255, 0.96);
          text-align: left;
          overflow: hidden;
          transition: transform 180ms ease, box-shadow 180ms ease;
        }
  
        .playground-thread-widget::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          z-index: 20;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-home-widget-border);
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-thread-widget.is-clickable {
          cursor: pointer;
          outline: none;
        }
  
        .playground-thread-widget.is-clickable:hover {
          transform: translateY(-1px);
          box-shadow: 0 26px 58px rgba(0, 0, 0, 0.28);
        }
  
        .playground-thread-widget.is-clickable:focus-visible {
          box-shadow: 0 0 0 1px rgba(255, 255, 255, 0.14), 0 24px 56px rgba(0, 0, 0, 0.26);
        }
  
  ${CALENDAR_STYLE_FRAGMENTS.welcomeWidget}
        .playground-thread-widget-tasks {
          width: 220px;
          height: 220px;
          min-height: 0;
          display: flex;
          flex-direction: column;
          gap: 0;
          padding: 0;
          background: #212121;
        }
  
        .playground-thread-widget-tasks > * {
          position: relative;
          z-index: 1;
        }
  
        .playground-thread-widget-tasks-media {
          position: relative;
          z-index: 3;
          width: 100%;
          height: 50px;
          min-height: 0;
          border-radius: 25px 25px 0 0;
          overflow: hidden;
          background-size: cover;
          background-position: center;
          background-repeat: no-repeat;
        }
  
        .playground-thread-widget-tasks-media::after {
          content: "";
          position: absolute;
          inset: 0;
          background: linear-gradient(180deg, rgba(0, 0, 0, 0.08), rgba(0, 0, 0, 0.2));
          pointer-events: none;
        }
  
        .playground-thread-widget-tasks-project-header {
          position: relative;
          z-index: 2;
          height: 100%;
          display: flex;
          align-items: center;
          gap: 9px;
          padding: 0 10px 0 12px;
          background: rgba(9, 9, 10, 0.38);
        }
  
        .playground-thread-widget-tasks-project-icon {
          position: relative;
          width: 28px;
          height: 28px;
          flex: 0 0 28px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border-radius: 50%;
          background: transparent;
          color: rgba(255, 255, 255, 0.94);
          -webkit-backdrop-filter: blur(16px);
          backdrop-filter: blur(16px);
        }
  
        .playground-thread-widget-tasks-project-icon::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          border-radius: 999px;
          padding: 1px;
          background: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-thread-widget-tasks-project-icon svg {
          width: 14px;
          height: 14px;
        }
  
        .playground-thread-widget-tasks-project-name {
          min-width: 0;
          flex: 0 1 auto;
          color: rgba(255, 255, 255, 0.96);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.2;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-thread-widget-tasks-project-switch {
          width: 14px;
          height: 14px;
          flex: 0 0 14px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          padding: 0;
          border: 0;
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.78);
          cursor: pointer;
          -webkit-backdrop-filter: none;
          backdrop-filter: none;
          transition: color 160ms ease;
        }
  
        .playground-thread-widget-tasks-project-switch:hover {
          background: transparent;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-thread-widget-tasks-project-switch svg {
          width: 14px;
          height: 14px;
        }
  
        .playground-thread-widget-tasks-project-menu {
          margin-left: auto;
        }
  
        .playground-thread-widget-tasks-pill {
          position: absolute;
          left: 50%;
          bottom: -22px;
          z-index: 4;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          max-width: calc(100% - 40px);
          min-height: 42px;
          padding: 0 18px;
          border-radius: 10px;
          background: rgba(255, 255, 255, 0.1);
          color: rgba(255, 255, 255, 0.96);
          font-size: 10px;
          font-weight: 400;
          line-height: 1.2;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
          -webkit-backdrop-filter: blur(12px);
          backdrop-filter: blur(12px);
          transform: translateX(-50%);
        }
  
        .playground-thread-widget-tasks-pill::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          border-radius: 10px;
          padding: 1px;
          background: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-thread-widget-tasks-pill-text {
          min-width: 0;
          position: relative;
          z-index: 1;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-thread-widget-tasks-surface {
          position: relative;
          z-index: 2;
          display: flex;
          flex-direction: column;
          gap: 9px;
          flex: 1 1 auto;
          padding: 10px 12px 12px;
          margin-top: 0;
          border-radius: 0 0 25px 25px;
          background: #171717;
          color: rgba(255, 255, 255, 0.96);
          overflow: hidden;
        }
  
        .playground-thread-widget-tasks-surface::before {
          content: "";
          position: absolute;
          inset: 0;
          z-index: 0;
          background-image: var(--playground-project-widget-bg-image, none);
          background-size: cover;
          background-position: center;
          background-repeat: no-repeat;
          filter: blur(50px);
          transform: scale(1.32);
          opacity: 0.62;
          pointer-events: none;
        }
  
        .playground-thread-widget-tasks-surface::after {
          content: "";
          position: absolute;
          inset: 0;
          z-index: 1;
          background: #000;
          -webkit-backdrop-filter: blur(50px);
          backdrop-filter: blur(50px);
          pointer-events: none;
        }
  
        .playground-thread-widget-tasks-surface > * {
          position: relative;
          z-index: 2;
        }
  
        .playground-thread-widget-tasks-progress {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 7px;
        }
  
        .playground-thread-widget-tasks-progress-count {
          display: flex;
          align-items: baseline;
          gap: 1px;
          color: rgba(255, 255, 255, 0.98);
          font-size: 26px;
          font-weight: 500;
          line-height: 1;
        }
  
        .playground-thread-widget-tasks-progress-total {
          display: inline-block;
          margin-left: 2px;
          color: rgba(255, 255, 255, 0.58);
          font-size: 11px;
          font-weight: 400;
          line-height: 1.05;
        }
  
        .playground-thread-widget-tasks-progress-label {
          display: block;
          margin-top: -2px;
          color: rgba(255, 255, 255, 0.58);
          font-size: 11px;
          font-weight: 400;
          line-height: 1.05;
        }
  
        .playground-thread-widget-tasks-progress-line {
          position: relative;
          width: 100%;
          height: 10px;
          margin-top: 3px;
          margin-bottom: 6px;
        }
  
        .playground-thread-widget-tasks-progress-line::before {
          content: "";
          position: absolute;
          left: 0;
          right: 0;
          top: 50%;
          height: 1px;
          background: rgba(255, 255, 255, 0.1);
          transform: translateY(-50%);
        }
  
        .playground-thread-widget-tasks-progress-fill {
          position: absolute;
          left: 0;
          top: 50%;
          width: var(--playground-project-task-progress, 0%);
          height: 1px;
          background: rgba(255, 255, 255, 0.86);
          transform: translateY(-50%);
        }
  
        .playground-thread-widget-tasks-progress-marker {
          position: absolute;
          left: var(--playground-project-task-progress, 0%);
          top: 50%;
          width: 2px;
          height: 12px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.96);
          transform: translate(-50%, -50%);
        }
  
        .playground-thread-widget-tasks-header {
          display: none;
        }
  
        .playground-thread-widget-tasks-title {
          margin: 0;
          color: rgba(255, 255, 255, 0.96);
          min-width: 0;
          font-size: 14px;
          font-weight: 500;
          line-height: 1.15;
          letter-spacing: -0.01em;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-thread-widget-tasks-list {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 7px;
          flex: 1 1 auto;
        }
  
        .playground-thread-widget-tasks-item {
          min-width: 0;
          display: flex;
          align-items: center;
          gap: 10px;
          justify-content: space-between;
        }
  
        .playground-thread-widget-tasks-item-main {
          min-width: 0;
          flex: 1 1 auto;
          display: flex;
          align-items: center;
          gap: 8px;
          padding: 0;
          border: 0;
          background: transparent;
          color: inherit;
          font: inherit;
          text-align: left;
          cursor: pointer;
        }
  
        .playground-thread-widget-tasks-item-main:focus-visible {
          outline: 1px solid rgba(255, 255, 255, 0.16);
          outline-offset: 2px;
          border-radius: 8px;
        }
  
        .playground-thread-widget-tasks-item-priority {
          width: 16px;
          height: 16px;
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          justify-content: center;
        }
  
        .playground-thread-widget-tasks-item-priority .playground-tasks-priority-value-icon {
          width: 14px;
          height: 14px;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-thread-widget-tasks .playground-thread-widget-tasks-item-priority.playground-tasks-priority-value .playground-tasks-priority-value-icon {
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-thread-widget-tasks-item-marker {
          width: 20px;
          height: 20px;
          flex: 0 0 20px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border-radius: 6px;
          background: #2fb676;
          color: #ffffff;
        }
  
        .playground-thread-widget-tasks-item-marker.is-subtask {
          background: #3aa3ff;
        }
  
        .playground-thread-widget-tasks-item-marker svg {
          width: 12px;
          height: 12px;
        }
  
        .playground-thread-widget-tasks-item-equals {
          flex: 0 0 auto;
          color: #f2c84b;
          font-size: 10px;
          font-weight: 500;
          line-height: 1;
        }
  
        .playground-thread-widget-tasks-item-label {
          min-width: 0;
          flex: 1 1 auto;
          color: rgba(255, 255, 255, 0.92);
          font-size: 10px;
          font-weight: 400;
          line-height: 1.25;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-thread-widget-tasks .playground-tasks-backlog-run-button {
          width: 20px;
          min-width: 20px;
          height: 20px;
          min-height: 20px;
          max-height: 20px;
        }
  
        .playground-thread-widget-tasks .playground-tasks-backlog-run-button.is-human-unchecked {
          border: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-thread-widget-tasks .playground-tasks-backlog-run-button svg {
          width: 8px;
          height: 8px;
        }
  
        .playground-thread-widget-today {
          width: 220px;
          height: 220px;
          min-height: 0;
          display: flex;
          flex-direction: column;
          gap: 0;
          padding: 0;
          border-radius: 25px;
          background: rgba(255, 255, 255, 0.075);
        }
  
        .playground-thread-widget-today > * {
          position: relative;
          z-index: 1;
        }
  
        .playground-thread-widget-usage {
          width: 220px;
          height: 220px;
          min-height: 0;
          position: relative;
          display: grid;
          grid-template-columns: minmax(0, 1fr) 64px;
          gap: 0;
          padding: 0;
          box-sizing: border-box;
          overflow: hidden;
          border-radius: 25px;
          border: 0;
          background:
            linear-gradient(
              90deg,
              rgba(255, 255, 255, 0.075) 0%,
              rgba(255, 255, 255, 0.075) var(--usage-remaining, 100%),
              #000 var(--usage-remaining, 100%),
              #000 100%
            ),
            #000;
          color: rgba(255, 255, 255, 0.96);
          cursor: pointer;
        }
  
        .playground-thread-widget-usage-main {
          position: relative;
          z-index: 3;
          min-width: 0;
          height: 100%;
          display: flex;
          flex-direction: column;
          padding: 20px 0 0 20px;
        }
  
        .playground-thread-widget-usage-side {
          position: relative;
          z-index: 2;
          min-width: 0;
          height: 100%;
          background: transparent;
        }
  
        .playground-thread-widget-usage-icon {
          position: absolute;
          top: 25px;
          left: 50%;
          width: 18px;
          height: 18px;
          color: #ffffff;
          transform: translateX(-50%);
        }
  
        .playground-thread-widget-usage-percent {
          color: #ffffff;
          font-size: 26px;
          font-weight: 500;
          line-height: 1;
          letter-spacing: 0;
        }
  
        .playground-thread-widget-usage-caption {
          margin-top: 6px;
          color: rgba(255, 255, 255, 0.72);
          font-size: 10px;
          font-weight: 400;
          line-height: 1;
          text-transform: none;
          white-space: nowrap;
        }
  
        .playground-thread-widget-usage-meter {
          min-width: 0;
          position: absolute;
          left: 0;
          right: -64px;
          bottom: 0;
          height: 112px;
          margin: 0;
          padding: 0;
        }
  
        .playground-thread-widget-usage-meter::before {
          content: "";
          position: absolute;
          left: 12px;
          bottom: 0;
          z-index: 2;
          width: 2px;
          height: 100px;
          border-radius: 999px 999px 0 0;
          background: rgba(255, 255, 255, 0.03);
          pointer-events: none;
        }
  
        .playground-thread-widget-usage-scale {
          position: absolute;
          left: 20px;
          right: 20px;
          top: -6px;
          z-index: 4;
          display: grid;
          grid-template-columns: repeat(3, 1fr);
          align-items: center;
          color: rgba(255, 255, 255, 0.82);
          font-size: 9px;
          font-weight: 400;
          line-height: 1;
          pointer-events: none;
        }
  
        .playground-thread-widget-usage-scale span:nth-child(2) {
          text-align: center;
        }
  
        .playground-thread-widget-usage-scale span:nth-child(3) {
          text-align: right;
        }
  
        .playground-thread-widget-usage-bars {
          height: 100%;
          display: flex;
          align-items: flex-end;
          gap: 6px;
          padding: 34px 14px 0 20px;
          box-sizing: border-box;
          overflow: visible;
        }
  
        .playground-thread-widget-usage-bar {
          position: relative;
          width: 2px;
          height: 100px;
          min-height: 100px;
          flex: 0 0 2px;
          border-radius: 999px 999px 0 0;
          background: rgba(255, 255, 255, 0.03);
          z-index: 2;
        }
  
        .playground-thread-widget-usage-fill {
          position: absolute;
          left: 0;
          bottom: 0;
          z-index: 3;
          width: var(--usage-remaining, 100%);
          height: 22px;
          border-radius: 0 0 0 17px;
          background: #4da3ff;
          pointer-events: none;
        }
  
        .playground-thread-widget-usage-fill::before {
          content: "";
          position: absolute;
          right: 0;
          bottom: 100%;
          width: 1px;
          height: 72px;
          background: #4da3ff;
          pointer-events: none;
        }
  
        .playground-thread-widget-usage-fill::after {
          content: "";
          position: absolute;
          right: -2.5px;
          bottom: calc(100% + 69.5px);
          width: 5px;
          height: 5px;
          border-radius: 50%;
          background: #4da3ff;
        }
  
        .playground-thread-widget-usage-fill.is-full::before,
        .playground-thread-widget-usage-fill.is-full::after {
          display: none;
        }
  
        .playground-thread-widget-usage-empty {
          width: 100%;
          min-height: 76px;
          display: flex;
          align-items: center;
          justify-content: center;
          color: rgba(255, 255, 255, 0.46);
          font-size: 10px;
          line-height: 1.5;
          text-align: center;
        }
  
        .playground-thread-widget-today-header {
          display: flex;
          flex-direction: column;
          align-items: stretch;
          justify-content: flex-start;
          gap: 8px;
          padding: 14px 14px 5px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.06);
          background: transparent;
        }
  
        .playground-thread-widget-today-nav {
          min-width: 0;
          display: grid;
          grid-template-columns: 18px minmax(0, 1fr) 18px;
          align-items: center;
          gap: 8px;
        }
  
        .playground-thread-widget-today-nav-button {
          width: 18px;
          height: 18px;
          min-width: 18px;
          min-height: 18px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          padding: 0;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.7);
          cursor: pointer;
          transition: color 160ms ease, opacity 160ms ease;
        }
  
        .playground-thread-widget-today-nav-button:hover {
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-thread-widget-today-nav-button svg {
          width: 14px;
          height: 14px;
        }
  
        .playground-thread-widget-today-month {
          min-width: 0;
          color: rgba(255, 255, 255, 0.96);
          font-size: 10px;
          font-weight: 500;
          line-height: 1;
          text-align: center;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-thread-widget-today-week {
          display: grid;
          grid-template-columns: repeat(7, minmax(0, 1fr));
          gap: 2px;
        }
  
        .playground-thread-widget-today-day {
          min-width: 0;
          min-height: 44px;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: flex-start;
          gap: 5px;
          padding: 0;
          border: 0;
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.72);
          font: inherit;
          cursor: pointer;
          transition: color 160ms ease, opacity 160ms ease;
        }
  
        .playground-thread-widget-today-day:hover {
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-thread-widget-today-day-weekday {
          color: rgba(255, 255, 255, 0.5);
          font-size: 10px;
          font-weight: 400;
          line-height: 1;
        }
  
        .playground-thread-widget-today-day-number {
          width: 22px;
          height: 22px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border-radius: 999px;
          color: rgba(255, 255, 255, 0.9);
          font-size: 12px;
          font-weight: 500;
          line-height: 1;
          transition: background 160ms ease, color 160ms ease;
        }
  
        .playground-thread-widget-today-day.is-selected .playground-thread-widget-today-day-number {
          background: #4da3ff;
          color: #ffffff;
        }
  
        .playground-thread-widget-today-day.is-today:not(.is-selected) .playground-thread-widget-today-day-number {
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-thread-widget-today-body {
          min-width: 0;
          flex: 1 1 auto;
          display: flex;
          flex-direction: column;
          gap: 0;
          padding: 14px 14px 14px;
          background: #000;
          cursor: pointer;
        }
  
        .playground-thread-widget-today-events {
          min-width: 0;
          flex: 1 1 auto;
          display: flex;
          flex-direction: column;
          gap: 14px;
        }
  
        .playground-thread-widget-today-event {
          min-width: 0;
          width: 100%;
          display: grid;
          grid-template-columns: minmax(0, 1fr) auto;
          align-items: center;
          gap: 8px;
          padding: 0;
          border: 0;
          background: transparent;
          color: inherit;
          font: inherit;
          text-align: left;
          cursor: pointer;
          transition: opacity 160ms ease;
        }
  
        .playground-thread-widget-today-event:hover {
          opacity: 0.86;
        }
  
        .playground-thread-widget-today-event-title {
          margin: 0;
          min-width: 0;
          color: rgba(255, 255, 255, 0.96);
          font-size: 10px;
          font-weight: 500;
          line-height: 1.2;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-thread-widget-today-event-meta {
          display: flex;
          align-items: center;
          justify-content: flex-end;
          gap: 5px;
          min-width: 0;
          max-width: 100px;
          color: rgba(255, 255, 255, 0.6);
          font-size: 10px;
          line-height: 1.2;
        }
  
        .playground-thread-widget-today-event-chip {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          max-width: 100%;
          padding: 0;
          border-radius: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.72);
          font-size: 10px;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-thread-widget-today-event-chip.is-time {
          flex: 0 0 auto;
          color: rgba(255, 255, 255, 0.7);
        }
  
        .playground-thread-widget-today-divider {
          width: 100%;
          height: 1px;
          margin: 0;
          background: rgba(255, 255, 255, 0.08);
        }
  
        .playground-thread-widget-today-action {
          margin-top: auto;
          padding-top: 5px;
          align-self: center;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.96);
          font-size: 12px;
          font-weight: 500;
          line-height: 1.2;
          cursor: pointer;
          transition: opacity 160ms ease, color 160ms ease;
        }
  
        .playground-thread-widget-today-action:hover {
          opacity: 0.86;
          color: rgba(255, 255, 255, 0.8);
        }
  
        .playground-thread-widget-today-empty {
          min-height: 90px;
          display: flex;
          align-items: center;
          justify-content: center;
          margin: 0;
          color: rgba(255, 255, 255, 0.46);
          font-size: 11px;
          line-height: 1.5;
          text-align: center;
        }
  
        .playground-thread-widget-empty {
          display: flex;
          align-items: center;
          justify-content: center;
          padding: 8px 0 2px;
          color: rgba(255, 255, 255, 0.6);
          font-size: 11px;
          font-weight: 400;
          line-height: 1.5;
          text-align: left;
        }
  
        .playground-thread-widget-tasks-footer {
          margin-top: auto;
          display: flex;
          flex-direction: column;
        }
  
        .playground-thread-widget-tasks-action {
          margin-top: auto;
          padding-top: 15px;
          margin-bottom: 2px;
          align-self: center;
          display: inline-flex;
          align-items: center;
          gap: 6px;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.96);
          font-size: 12px;
          font-weight: 500;
          line-height: 1.2;
          cursor: pointer;
          transition: opacity 160ms ease, color 160ms ease;
        }
  
        .playground-thread-widget-tasks-action:hover {
          opacity: 0.86;
          color: rgba(255, 255, 255, 0.8);
        }
  
        @media (max-width: 940px) {
          .playground-thread-welcome {
            padding-right: 0;
            padding-left: 0;
          }
  
          .playground-thread-welcome-thread-grid {
            grid-template-columns: minmax(0, 1fr);
          }
  
          .playground-thread-home-dashboard {
            grid-template-columns: minmax(0, 1fr);
          }
  
          .playground-thread-home-calendar-column {
            justify-content: center;
          }
  
          .playground-thread-home-usage-column {
            justify-content: center;
          }
  
          .playground-thread-welcome-widgets {
            width: min(100%, 220px);
            grid-template-columns: minmax(0, 1fr);
            gap: 14px;
          }
  
          .playground-thread-widget {
            max-width: 220px;
            margin: 0 auto;
          }
  
          .playground-thread-widget.playground-thread-home-project-widget {
            max-width: 220px;
            margin: 0 auto;
          }
  
          .playground-thread-widget-tasks,
          .playground-thread-widget-today,
          .playground-thread-widget-usage {
            width: min(100%, 220px);
          }
        }
  
        .playground-auth-panel {
          display: flex;
          align-items: center;
          justify-content: center;
          width: 100%;
          height: 100%;
          padding: 24px;
        }
  
        .playground-auth-card {
          width: min(420px, 100%);
          display: flex;
          flex-direction: column;
          gap: 12px;
          padding: 22px 20px;
          border-radius: 18px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: #131313;
          box-shadow: 0 18px 60px rgba(0, 0, 0, 0.24);
        }
  
        .playground-auth-title {
          font-size: 18px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-auth-copy {
          font-size: 13px;
          line-height: 1.55;
          color: rgba(255, 255, 255, 0.68);
        }
  
        .playground-auth-error {
          font-size: 12px;
          line-height: 1.5;
          color: #ff8a8a;
        }
  
        .playground-auth-button,
        .settings-auth-button {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 8px;
          width: 100%;
          min-height: 38px;
          padding: 0 14px;
          border: 0;
          border-radius: 12px;
          background: #f3f6ff;
          color: #111;
          font-size: 12px;
          font-weight: 600;
          cursor: pointer;
          transition: opacity 160ms ease, transform 160ms ease;
        }
  
        .playground-auth-button:hover,
        .settings-auth-button:hover {
          opacity: 0.96;
          transform: translateY(-1px);
        }
  
        .playground-auth-button:disabled,
        .settings-auth-button:disabled {
          opacity: 0.65;
          cursor: default;
          transform: none;
        }
  
        .playground-auth-button-icon,
        .settings-auth-icon {
          width: 14px;
          height: 14px;
          flex-shrink: 0;
        }
  
        .settings-secondary-button {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 8px;
          width: 100%;
          min-height: 38px;
          margin-top: 10px;
          padding: 0 14px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 12px;
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.88);
          font-size: 12px;
          font-weight: 600;
          cursor: pointer;
          transition: background-color 160ms ease, border-color 160ms ease, color 160ms ease;
        }
  
        .settings-secondary-button:hover {
          background: rgba(255, 255, 255, 0.08);
          border-color: rgba(255, 255, 255, 0.16);
          color: #fff;
        }
  
        .playground-view-pane {
          position: absolute;
          inset: 0;
          min-width: 0;
          min-height: 0;
          border-radius: inherit;
          overflow: hidden;
          transition: right 280ms cubic-bezier(0.16, 1, 0.3, 1);
        }
  
        .playground-content-body.is-thread-task-detail-open .playground-view-pane,
        .playground-content-body.is-thread-side-detail-open .playground-view-pane {
          right: 0;
        }
  
        .playground-view-pane.is-hidden {
          display: none;
        }
  
        .changes-view {
          height: 100%;
          min-height: 0;
          padding: 0;
        }
  
        .changes-view.is-timeline {
          display: flex;
          align-items: stretch;
          justify-content: center;
        }
  
        .changes-view.is-detail {
          display: flex;
          align-items: stretch;
          justify-content: center;
        }
  
        .changes-panel {
          min-width: 0;
          min-height: 0;
          display: flex;
          flex-direction: column;
          border-radius: 16px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: #0f1011;
          overflow: hidden;
        }
  
        .changes-panel-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          margin: 32px 0 16px;
          padding: 0;
          border-bottom: 0;
        }
  
        .changes-panel-title {
          font-size: 20px;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.92);
        }
  
        .changes-panel-subtitle {
          font-size: 11px;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.48);
        }
  
        .changes-panel-body {
          min-height: 0;
          overflow: auto;
        }
  
        .changes-timeline-panel .changes-panel-body {
          scrollbar-width: none;
        }
  
        .changes-timeline-panel .changes-panel-body::-webkit-scrollbar {
          display: none;
        }
  
        .changes-timeline-panel {
          width: min(980px, 100%);
          min-width: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          overflow: visible;
        }
  
        .changes-timeline-body {
          padding: 0;
        }
  
        .changes-timeline {
          display: flex;
          flex-direction: column;
          gap: 20px;
        }
  
        .changes-timeline-group {
          display: flex;
          flex-direction: column;
          gap: 16px;
        }
  
        .changes-timeline-track {
          position: relative;
          display: flex;
          flex-direction: column;
          gap: 18px;
          padding-left: 0;
        }
  
        .changes-timeline-track::before {
          content: "";
          position: absolute;
          left: 9px;
          top: 0;
          bottom: 0;
          width: 1px;
          background: rgba(255, 255, 255, 0.12);
        }
  
        .changes-step-card {
          position: relative;
          display: flex;
          flex-direction: column;
          gap: 10px;
          border: 0;
          border-radius: 0;
          background: transparent;
          overflow: visible;
        }
  
        .changes-step-card.is-active {
          background: transparent;
        }
  
        .changes-step-card-header {
          position: relative;
          z-index: 1;
          display: grid;
          grid-template-columns: 20px minmax(0, 1fr);
          align-items: start;
          gap: 14px;
        }
  
        .changes-step-card-commit {
          width: 20px;
          height: 20px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          color: rgba(255, 255, 255, 0.88);
        }
  
        .changes-step-card-commit svg {
          width: 16px;
          height: 16px;
        }
  
        .changes-step-card-summary {
          width: 100%;
          display: flex;
          flex-direction: column;
          gap: 8px;
          padding: 0;
          border: 0;
          background: transparent;
          color: white;
          text-align: left;
          cursor: pointer;
        }
  
        .changes-step-card-title {
          font-size: 13px;
          line-height: 1.5;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .changes-step-card-meta {
          display: flex;
          flex-wrap: wrap;
          gap: 8px 12px;
          color: rgba(255, 255, 255, 0.48);
          font-size: 11px;
          font-weight: 500;
        }
  
        .changes-step-card-files {
          display: flex;
          flex-direction: column;
          gap: 6px;
          margin-left: 34px;
          padding: 0;
          border-top: 0;
        }
  
        .changes-step-card-empty {
          padding: 6px 8px 8px;
          color: rgba(255, 255, 255, 0.48);
          font-size: 11px;
        }
  
        .changes-step-file-row {
          width: 100%;
          display: flex;
          flex-direction: column;
          gap: 10px;
          padding: 10px 12px;
          border: 1px solid rgba(255, 255, 255, 0.05);
          border-radius: 12px;
          background: rgba(255, 255, 255, 0.1);
          backdrop-filter: blur(50px);
          -webkit-backdrop-filter: blur(50px);
          color: white;
          transition: background-color 160ms ease;
        }
  
        .changes-step-file-row:hover {
          background: rgba(255, 255, 255, 0.12);
        }
  
        .changes-step-file-row.is-active {
          background: rgba(255, 255, 255, 0.12);
        }
  
        .changes-step-file-row-main {
          width: 100%;
          display: flex;
          flex-direction: column;
          gap: 10px;
          padding: 0;
          border: 0;
          background: transparent;
          color: inherit;
          font: inherit;
          text-align: left;
          cursor: pointer;
        }
  
        .changes-step-file-row-head {
          width: 100%;
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
        }
  
        .changes-step-file-main {
          min-width: 0;
          flex: 1;
          display: flex;
          flex-direction: column;
          gap: 0;
        }
  
        .changes-step-file-main.is-resource {
          gap: 4px;
        }
  
        .changes-step-file-head-main {
          min-width: 0;
          flex: 1;
          display: flex;
          align-items: center;
          gap: 10px;
        }
  
        .changes-step-file-icon {
          width: 24px;
          height: 24px;
          flex-shrink: 0;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border-radius: 999px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.06);
          color: rgba(255, 255, 255, 0.92);
        }
  
        .changes-step-file-icon svg {
          width: 13px;
          height: 13px;
        }
  
        .changes-step-file-name {
          min-width: 0;
          padding: 0 6px;
          font-size: 12px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.96);
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .changes-step-file-path {
          min-width: 0;
          padding: 0 6px;
          font-size: 11px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.5);
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .changes-step-file-right {
          flex-shrink: 0;
          display: inline-flex;
          align-items: center;
          gap: 8px;
          flex-wrap: wrap;
          justify-content: flex-end;
        }
  
        .changes-step-file-kind,
        .changes-step-file-stat {
          display: inline-flex;
          align-items: center;
          flex-shrink: 0;
          font-size: 11px;
          font-weight: 400;
        }
  
        .changes-step-file-kind {
          color: rgba(255, 255, 255, 0.92);
          padding: 2px 7px;
          border-radius: 999px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          background: transparent;
        }
  
        .changes-step-file-stat {
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
        }
  
        .changes-step-file-kind.is-created {
          color: rgba(255, 255, 255, 0.92);
        }
  
        .changes-step-file-kind.is-modified {
          color: rgba(255, 255, 255, 0.92);
        }
  
        .changes-step-file-kind.is-deleted {
          color: rgba(255, 255, 255, 0.92);
        }
  
        .changes-step-file-kind.is-reverted {
          color: #ff7b72;
        }
  
        .changes-step-file-stat.is-added {
          color: #34d67a;
        }
  
        .changes-step-file-stat.is-removed {
          color: #ff7b72;
        }
  
        .changes-step-file-preview {
          display: flex;
          align-items: flex-start;
          width: 100%;
        }
  
        .changes-step-file-preview .tb-runner-image-preview-surface {
          width: 100%;
          justify-content: flex-start;
        }
  
        .changes-step-file-preview .tb-runner-image-preview-surface-image {
          width: auto;
          max-width: 100%;
          max-height: 150px;
          object-fit: contain;
          border-radius: 10px;
        }
  
        .changes-detail-image-preview {
          display: flex;
          justify-content: flex-start;
          align-items: flex-start;
          width: 100%;
        }
  
        .changes-detail-image-preview .tb-runner-image-preview-surface {
          width: 100%;
          justify-content: flex-start;
        }
  
        .changes-detail-image-preview .tb-runner-image-preview-surface-image {
          width: auto;
          max-width: 100%;
          max-height: 500px;
          object-fit: contain;
        }
  
        .changes-empty-state,
        .changes-loading-state {
          color: rgba(255, 255, 255, 0.56);
          font-size: 12px;
          line-height: 1.5;
        }
  
        .changes-empty-state {
          padding: 0;
        }
  
        .changes-loading-state {
          display: flex;
          align-items: center;
          justify-content: center;
          padding: 18px 16px;
        }
  
        .changes-loading-state > span {
          margin-left: 5px;
        }
  
        .changes-detail-header,
        .changes-diff-screen-header {
          display: flex;
          flex-direction: column;
          gap: 10px;
          padding: 16px 18px 14px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .changes-detail-path-row,
        .changes-diff-screen-title-row {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
        }
  
        .changes-diff-title-actions {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          gap: 12px;
          flex-wrap: wrap;
        }
  
        .changes-diff-title-meta {
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          gap: 8px;
          flex-wrap: wrap;
        }
  
        .changes-diff-title-date {
          color: rgba(255, 255, 255, 0.62);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.2;
          white-space: nowrap;
        }
  
        .changes-diff-screen-panel {
          height: 100%;
          width: min(980px, 100%);
          border: 0;
          border-radius: 0;
          background: transparent;
          overflow: visible;
        }
  
        .changes-diff-screen-header {
          gap: 14px;
          margin: 32px 0 16px;
          padding: 0;
          border-bottom: 0;
        }
  
        .changes-diff-screen-header-main {
          display: flex;
          flex-direction: column;
          gap: 10px;
          min-width: 0;
        }
  
        .changes-diff-back-button {
          width: fit-content;
          display: inline-flex;
          align-items: center;
          gap: 6px;
          padding: 0;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.64);
          font-size: 12px;
          font-weight: 400;
          cursor: pointer;
          transition: color 160ms ease;
        }
  
        .changes-diff-back-button svg {
          width: 14px;
          height: 14px;
        }
  
        .changes-diff-back-button:hover {
          color: rgba(255, 255, 255, 0.92);
        }
  
        .changes-detail-path-group {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 4px;
        }
  
        .changes-detail-path {
          min-width: 0;
          font-size: 20px;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.96);
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .changes-detail-path-secondary,
        .changes-detail-context {
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.58);
        }
  
        .changes-diff-stats {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          flex-shrink: 0;
        }
  
        .changes-diff-stat {
          font-size: 11px;
          font-weight: 600;
        }
  
        .changes-diff-stat.additions {
          color: #34d67a;
        }
  
        .changes-diff-stat.deletions {
          color: #ff5f57;
        }
  
        .changes-detail-meta {
          display: flex;
          align-items: center;
          gap: 8px;
          flex-wrap: wrap;
        }
  
        .changes-diff-revert-button {
          display: inline-flex;
          align-items: center;
          gap: 6px;
          padding: 6px 10px;
          border: 1px solid rgba(255, 255, 255, 0.12);
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.82);
          font-size: 12px;
          font-weight: 400;
          line-height: 1;
          cursor: pointer;
          transition: border-color 160ms ease, color 160ms ease, background-color 160ms ease, opacity 160ms ease;
        }
  
        .changes-diff-revert-button svg {
          width: 14px;
          height: 14px;
        }
  
        .changes-diff-revert-button:hover:not(:disabled) {
          border-color: rgba(255, 255, 255, 0.24);
          background: rgba(255, 255, 255, 0.06);
          color: rgba(255, 255, 255, 0.96);
        }
  
        .changes-diff-revert-button:disabled {
          opacity: 0.46;
          cursor: default;
        }
  
        .changes-diff-reverted-label {
          display: inline-flex;
          align-items: center;
          gap: 6px;
          padding: 6px 10px;
          border: 1px solid rgba(255, 255, 255, 0.12);
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.82);
          font-size: 12px;
          font-weight: 400;
          line-height: 1;
        }
  
        .changes-diff-revert-error {
          font-size: 12px;
          line-height: 1.5;
          color: #fca5a5;
        }
  
        .changes-detail-section {
          min-height: 0;
          display: flex;
          flex-direction: column;
          border-top: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .changes-detail-body {
          display: flex;
          flex-direction: column;
          min-height: 0;
          overflow: hidden;
        }
  
        .changes-diff-screen-body {
          min-height: 0;
          overflow: auto;
          padding: 0 0 18px;
        }
  
        .changes-diff-box {
          display: flex;
          flex-direction: column;
          min-height: 0;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 12px;
          overflow: visible;
          background: transparent;
        }
  
        .changes-diff-box-header {
          position: relative;
          z-index: 2;
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          padding: 12px 14px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 12px 12px 0 0;
          background: rgba(255, 255, 255, 0.1);
        }
  
        .changes-diff-box-path {
          min-width: 0;
          font-size: 12px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.92);
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .changes-diff-box-header-right {
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          flex-wrap: wrap;
          gap: 10px;
          flex-shrink: 0;
        }
  
        .changes-diff-stats.is-box-header {
          gap: 6px;
        }
  
        .changes-diff-toggle-group {
          display: inline-flex;
          align-items: center;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 8px;
          overflow: hidden;
          background: rgba(255, 255, 255, 0.03);
        }
  
        .changes-diff-toggle-button {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          padding: 6px 10px;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.68);
          font-size: 11px;
          font-weight: 500;
          cursor: pointer;
          transition: background-color 160ms ease, color 160ms ease;
        }
  
        .changes-diff-toggle-button + .changes-diff-toggle-button {
          border-left: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .changes-diff-toggle-button:hover {
          color: rgba(255, 255, 255, 0.92);
        }
  
        .changes-diff-toggle-button.is-active {
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.98);
        }
  
        .changes-diff-view-menu-shell {
          position: relative;
          display: inline-flex;
          align-items: center;
          flex-shrink: 0;
        }
  
        .changes-diff-menu-trigger {
          width: 28px;
          height: 28px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border: 0;
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.72);
          cursor: pointer;
          transition: background-color 160ms ease, color 160ms ease;
        }
  
        .changes-diff-menu-trigger:hover,
        .changes-diff-menu-trigger.is-active {
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.96);
        }
  
        .changes-diff-menu-trigger svg {
          width: 16px;
          height: 16px;
        }
  
        .changes-diff-view-menu {
          position: absolute;
          top: calc(100% + 8px);
          right: 0;
          z-index: 30;
          min-width: 148px;
        }
  
        .changes-diff-view-menu-item {
          width: 100%;
          display: flex;
          align-items: center;
          gap: 8px;
          padding: 7px 8px;
          border: 0;
          border-radius: 8px;
          background: transparent;
          color: rgba(255, 255, 255, 0.72);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.2;
          text-align: left;
          cursor: pointer;
          transition: background-color 150ms ease, color 150ms ease;
        }
  
        .changes-diff-view-menu-item:hover,
        .changes-diff-view-menu-item.is-active {
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.96);
        }
  
        .changes-diff-view-menu-check {
          width: 14px;
          height: 14px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          flex-shrink: 0;
          color: rgba(255, 255, 255, 0.9);
        }
  
        .changes-diff-box-body {
          overflow: auto;
          border-radius: 0 0 12px 12px;
          background: transparent;
        }
  
        .changes-diff-box-body .monaco-editor,
        .changes-diff-box-body .overflow-guard,
        .changes-diff-box-body .monaco-editor-background,
        .changes-diff-box-body .margin,
        .changes-diff-box-body .monaco-scrollable-element,
        .changes-diff-box-body .lines-content,
        .changes-diff-box-body .view-overlays,
        .changes-diff-box-body .view-zones,
        .changes-diff-box-body .margin-view-overlays,
        .changes-diff-box-body .glyph-margin,
        .changes-diff-box-body .line-numbers,
        .changes-diff-box-body .view-line,
        .changes-diff-box-body [data-keybinding-context] {
          background: transparent !important;
          background-color: transparent !important;
        }
  
        .changes-diff-box-body .scrollbar,
        .changes-diff-box-body .decorationsOverviewRuler,
        .changes-diff-box-body .current-line,
        .changes-diff-box-body .current-line-margin {
          background: transparent !important;
        }
  
        .changes-diff-box-body .monaco-editor .cursor {
          display: none !important;
        }
  
        .changes-detail-section:first-of-type {
          border-top: 0;
        }
  
        .changes-detail-section-header {
          padding: 10px 16px;
          font-size: 11px;
          font-weight: 600;
          letter-spacing: 0.02em;
          text-transform: uppercase;
          color: rgba(255, 255, 255, 0.46);
        }
  
        .changes-code-block {
          margin: 0;
          padding: 0 16px 16px;
          overflow: auto;
          color: rgba(255, 255, 255, 0.88);
          font-size: 12px;
          line-height: 1.6;
          font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
          white-space: pre-wrap;
          word-break: break-word;
        }
  
        .changes-code-block.is-empty {
          font-family: "Inter", -apple-system, BlinkMacSystemFont, "SF Pro Text", "Segoe UI", sans-serif;
          color: rgba(255, 255, 255, 0.56);
        }
  
        .changes-resource-detail-card {
          display: flex;
          flex-direction: column;
          gap: 18px;
          padding: 18px;
          border-radius: 16px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.06);
          backdrop-filter: blur(30px);
          -webkit-backdrop-filter: blur(30px);
        }
  
        .changes-resource-detail-header {
          display: flex;
          align-items: flex-start;
          gap: 12px;
        }
  
        .changes-resource-detail-icon {
          width: 36px;
          height: 36px;
          flex-shrink: 0;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border-radius: 999px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.06);
          color: rgba(255, 255, 255, 0.92);
        }
  
        .changes-resource-detail-icon svg {
          width: 18px;
          height: 18px;
        }
  
        .changes-resource-detail-copy {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 5px;
        }
  
        .changes-resource-detail-label {
          font-size: 11px;
          font-weight: 500;
          letter-spacing: 0.04em;
          text-transform: uppercase;
          color: rgba(255, 255, 255, 0.46);
        }
  
        .changes-resource-detail-name {
          font-size: 18px;
          line-height: 1.35;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .changes-resource-detail-id {
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.56);
        }
  
        .changes-resource-detail-description {
          font-size: 12px;
          line-height: 1.6;
          color: rgba(255, 255, 255, 0.74);
          white-space: pre-wrap;
        }
  
        .changes-resource-detail-meta {
          display: grid;
          grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
          gap: 10px 16px;
        }
  
        .changes-resource-detail-meta-row {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          padding: 10px 12px;
          border-radius: 12px;
          border: 1px solid rgba(255, 255, 255, 0.06);
          background: rgba(255, 255, 255, 0.03);
        }
  
        .changes-resource-detail-meta-label {
          font-size: 11px;
          line-height: 1.4;
          color: rgba(255, 255, 255, 0.46);
        }
  
        .changes-resource-detail-meta-value {
          min-width: 0;
          font-size: 12px;
          line-height: 1.4;
          color: rgba(255, 255, 255, 0.9);
          text-align: right;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .changes-diff-editor-shell {
          margin: 0 16px 16px;
          border-radius: 12px;
          overflow: hidden;
          background: #1a1a1a;
          border: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .changes-diff-editor-shell .monaco-editor,
        .changes-diff-editor-shell .monaco-editor-background,
        .changes-diff-editor-shell .margin,
        .changes-diff-editor-shell .monaco-scrollable-element {
          background: transparent !important;
        }
  
        .changes-split-diff-view {
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 14px;
          overflow: auto;
          background: #0d1117;
        }
  
        .changes-split-diff-hunk,
        .changes-split-diff-note {
          padding: 8px 14px;
          font-size: 12px;
          line-height: 1.5;
          font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
          white-space: pre;
          border-top: 1px solid rgba(255, 255, 255, 0.04);
        }
  
        .changes-split-diff-hunk:first-child,
        .changes-split-diff-note:first-child {
          border-top: 0;
        }
  
        .changes-split-diff-hunk {
          background: rgba(56, 139, 253, 0.14);
          color: #7ab7ff;
        }
  
        .changes-split-diff-note {
          background: rgba(255, 255, 255, 0.03);
          color: rgba(255, 255, 255, 0.54);
        }
  
        .changes-split-diff-row {
          display: grid;
          grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
          min-width: max-content;
          border-top: 1px solid rgba(255, 255, 255, 0.04);
        }
  
        .changes-split-diff-row:first-child {
          border-top: 0;
        }
  
        .changes-split-diff-cell {
          min-width: 0;
          display: grid;
          grid-template-columns: 56px minmax(0, 1fr);
          align-items: stretch;
          background: transparent;
        }
  
        .changes-split-diff-cell.is-right {
          border-left: 1px solid rgba(255, 255, 255, 0.06);
        }
  
        .changes-split-diff-cell.is-added {
          background: rgba(46, 160, 67, 0.16);
        }
  
        .changes-split-diff-cell.is-removed {
          background: rgba(248, 81, 73, 0.16);
        }
  
        .changes-split-diff-cell.is-context {
          background: transparent;
        }
  
        .changes-split-diff-cell.is-empty {
          background: rgba(255, 255, 255, 0.02);
        }
  
        .changes-split-diff-line-number,
        .changes-split-diff-line-content {
          padding-top: 7px;
          padding-bottom: 7px;
          font-size: 12px;
          line-height: 1.5;
          font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
          white-space: pre;
        }
  
        .changes-split-diff-line-number {
          padding-left: 12px;
          padding-right: 8px;
          text-align: right;
          color: rgba(255, 255, 255, 0.34);
          border-right: 1px solid rgba(255, 255, 255, 0.04);
          user-select: none;
        }
  
        .changes-split-diff-cell.is-added .changes-split-diff-line-number {
          color: rgba(125, 230, 156, 0.72);
        }
  
        .changes-split-diff-cell.is-removed .changes-split-diff-line-number {
          color: rgba(255, 153, 143, 0.72);
        }
  
        .changes-split-diff-line-content {
          min-width: 0;
          padding-left: 10px;
          padding-right: 14px;
          color: rgba(255, 255, 255, 0.9);
        }
  
        .changes-split-diff-cell.is-added .changes-split-diff-line-content {
          color: #aff5b4;
        }
  
        .changes-split-diff-cell.is-removed .changes-split-diff-line-content {
          color: #ffd8d3;
        }
  
        .changes-diff-view {
          margin: 0 16px 16px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 12px;
          overflow: auto;
          background: #111111;
          font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
        }
  
        .changes-diff-row {
          display: grid;
          grid-template-columns: 52px 52px 18px minmax(0, 1fr);
          align-items: stretch;
          min-width: max-content;
          border-top: 1px solid rgba(255, 255, 255, 0.04);
        }
  
        .changes-diff-row:first-child {
          border-top: 0;
        }
  
        .changes-diff-row.is-added {
          background: rgba(46, 160, 67, 0.16);
        }
  
        .changes-diff-row.is-removed {
          background: rgba(248, 81, 73, 0.16);
        }
  
        .changes-diff-row.is-hunk {
          background: rgba(56, 139, 253, 0.12);
        }
  
        .changes-diff-row.is-meta,
        .changes-diff-row.is-note {
          background: rgba(255, 255, 255, 0.03);
        }
  
        .changes-diff-line-number,
        .changes-diff-line-prefix,
        .changes-diff-line-content {
          padding-top: 7px;
          padding-bottom: 7px;
          font-size: 12px;
          line-height: 1.5;
          white-space: pre;
        }
  
        .changes-diff-line-number {
          padding-left: 12px;
          padding-right: 8px;
          text-align: right;
          color: rgba(255, 255, 255, 0.34);
          border-right: 1px solid rgba(255, 255, 255, 0.04);
          user-select: none;
        }
  
        .changes-diff-row.is-added .changes-diff-line-number {
          color: rgba(125, 230, 156, 0.72);
        }
  
        .changes-diff-row.is-removed .changes-diff-line-number {
          color: rgba(255, 153, 143, 0.72);
        }
  
        .changes-diff-line-prefix {
          padding-left: 8px;
          padding-right: 0;
          color: rgba(255, 255, 255, 0.72);
          user-select: none;
        }
  
        .changes-diff-row.is-added .changes-diff-line-prefix,
        .changes-diff-row.is-added .changes-diff-line-content {
          color: #aff5b4;
        }
  
        .changes-diff-row.is-removed .changes-diff-line-prefix,
        .changes-diff-row.is-removed .changes-diff-line-content {
          color: #ffd8d3;
        }
  
        .changes-diff-row.is-hunk .changes-diff-line-prefix,
        .changes-diff-row.is-hunk .changes-diff-line-content {
          color: #7ab7ff;
        }
  
        .changes-diff-row.is-meta .changes-diff-line-content,
        .changes-diff-row.is-note .changes-diff-line-content {
          color: rgba(255, 255, 255, 0.54);
        }
  
        .changes-diff-line-content {
          min-width: 0;
          padding-left: 6px;
          padding-right: 16px;
          color: rgba(255, 255, 255, 0.9);
        }
  
        @media (max-width: 1180px) {
          .changes-timeline-panel {
            width: 100%;
          }
        }
  
        .settings-modal-scrim {
          position: fixed;
          inset: 0;
          z-index: 90;
          display: flex;
          align-items: center;
          justify-content: center;
          padding: 24px;
          background: rgba(0, 0, 0, 0.56);
          backdrop-filter: blur(10px);
        }
  
        .settings-modal {
          width: min(560px, 100%);
          max-height: min(84vh, 920px);
          display: flex;
          flex-direction: column;
          border-radius: 20px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: #1f1f1f;
          box-shadow: 0 18px 44px rgba(0, 0, 0, 0.45);
          overflow: hidden;
        }
  
        .settings-modal-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          padding: 18px 20px 14px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .settings-modal-title {
          font-size: 18px;
          font-weight: 600;
          color: #fff;
        }
  
        .settings-modal-close {
          width: 32px;
          height: 32px;
          padding: 0;
          border: 0;
          border-radius: 12px;
          background: transparent;
          color: rgba(255, 255, 255, 0.72);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
        }
  
        .settings-modal-close:hover {
          background: rgba(255, 255, 255, 0.08);
          color: #fff;
        }
  
        .settings-modal-body {
          padding: 18px 20px 20px;
          overflow: auto;
        }
  
        .settings-section {
          border-radius: 16px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.02);
          padding: 14px;
        }
  
        .settings-section + .settings-section {
          margin-top: 12px;
        }
  
        .settings-section-title {
          margin: 0 0 12px;
          font-size: 13px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.78);
        }
  
        .thread-search-scrim,
        .account-menu-scrim,
        .notification-menu-scrim,
        .profile-editor-scrim {
          position: fixed;
          inset: 0;
        }
  
  ${APP_HEADER_STYLE_FRAGMENTS.overlayScrims}
  ${APP_HEADER_STYLE_FRAGMENTS.notificationsScrim}
        .profile-editor-scrim {
          z-index: 96;
          display: flex;
          align-items: center;
          justify-content: center;
          padding: 24px;
          background: rgba(0, 0, 0, 0.58);
          backdrop-filter: blur(12px);
        }
  
  ${TEAMS_STYLE_FRAGMENTS.foundation}${ORGANIZATIONS_STYLE_FRAGMENTS.billing}${TEAMS_STYLE_FRAGMENTS.rolesAndDialogs}${TEAMS_STYLE_FRAGMENTS.responsive}
  ${CONFIGURE_HOME_STYLE_FRAGMENTS.notificationPage}
  ${APP_HEADER_STYLE_FRAGMENTS.notificationsPopup}
  ${APP_HEADER_STYLE_FRAGMENTS.accountMenu}
  
        @keyframes playground-tasks-toolbar-popup-fade-down-in {
          from {
            opacity: 0;
            transform: translateY(-10px);
          }
  
          to {
            opacity: 1;
            transform: translateY(0);
          }
        }
  
  ${APP_HEADER_STYLE_FRAGMENTS.searchModal}
  
        .playground-onboarding-scrim {
          position: fixed;
          inset: 0;
          z-index: 98;
          display: flex;
          align-items: center;
          justify-content: center;
          padding: 28px;
          background: rgba(0, 0, 0, 0.62);
          backdrop-filter: blur(14px);
        }
  
        .playground-onboarding-modal {
          width: min(920px, 100%);
          max-height: min(86vh, 860px);
          display: flex;
          flex-direction: column;
          border-radius: 15px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: linear-gradient(180deg, rgba(36, 36, 36, 0.98) 0%, rgba(28, 28, 28, 0.98) 100%);
          box-shadow: 0 30px 90px rgba(0, 0, 0, 0.48);
          overflow: hidden;
        }
  
        .playground-onboarding-header {
          display: flex;
          flex-direction: column;
          gap: 14px;
          padding: 24px 24px 0;
        }
  
        .playground-onboarding-header-top {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 18px;
        }
  
        .playground-onboarding-header-copy {
          min-width: 0;
          flex: 1;
          display: flex;
          flex-direction: column;
          gap: 14px;
        }
  
        .playground-onboarding-title-wrap {
          display: flex;
          flex-direction: column;
          gap: 8px;
        }
  
        .playground-onboarding-kicker {
          font-size: 10px;
          font-weight: 400;
          letter-spacing: 0.14em;
          text-transform: uppercase;
          color: rgba(255, 255, 255, 0.7);
        }
  
        .playground-onboarding-title {
          margin: 0;
          font-size: 24px;
          font-weight: 500;
          line-height: 1.12;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-onboarding-step-meta {
          width: 100%;
        }
  
        .playground-onboarding-progress {
          width: 100%;
        }
  
        .playground-onboarding-progress-track {
          width: 100%;
          height: 2px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.1);
          overflow: hidden;
        }
  
        .playground-onboarding-progress-fill {
          height: 100%;
          border-radius: inherit;
          background: rgba(255, 255, 255, 1);
          transition: width 180ms ease;
        }
  
        .playground-onboarding-progress-label {
          font-size: 12px;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.58);
        }
  
        .playground-onboarding-close {
          width: 36px;
          height: 36px;
          padding: 0;
          border: 0;
          border-radius: 12px;
          background: transparent;
          color: rgba(255, 255, 255, 0.72);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
        }
  
        .playground-onboarding-close:hover {
          background: rgba(255, 255, 255, 0.08);
          color: #fff;
        }
  
        .playground-onboarding-body {
          min-height: 0;
          overflow: auto;
          padding: 24px;
        }
  
        .playground-onboarding-step {
          display: flex;
          flex-direction: column;
          gap: 22px;
        }
  
        .playground-onboarding-concept-step {
          min-height: 500px;
          display: flex;
          flex-direction: column;
          justify-content: space-between;
          gap: 24px;
        }
  
        .playground-onboarding-concept-hero {
          flex: 1;
          display: grid;
          grid-template-columns: minmax(0, 0.92fr) minmax(280px, 0.88fr);
          align-items: center;
          gap: 24px;
        }
  
        .playground-onboarding-concept-copy-column {
          display: flex;
          flex-direction: column;
          align-items: flex-start;
          justify-content: center;
          gap: 16px;
          text-align: left;
        }
  
        .playground-onboarding-concept-preview-panel {
          width: 100%;
          min-height: 248px;
          border-radius: 5px;
          background:
            linear-gradient(180deg, rgba(8, 15, 26, 0.42) 0%, rgba(8, 15, 26, 0.72) 100%),
            url("/img/agent-profile-pics/starbg.jpeg");
          background-size: cover;
          background-position: center;
          background-repeat: no-repeat;
          box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.04);
          padding-left: 24px;
          padding-top: 24px;
          display: flex;
          align-items: flex-end;
          justify-content: flex-end;
          overflow: hidden;
        }
  
        .playground-onboarding-concept-preview-image {
          width: auto;
          height: auto;
          max-width: 100%;
          border-radius: 5px 0 5px 0;
          display: flex;
          align-items: flex-end;
          justify-content: flex-end;
          overflow: visible;
        }
  
        .playground-onboarding-concept-preview-asset {
          display: block;
          width: auto;
          height: auto;
          border-radius: inherit;
          max-width: min(100%, 560px);
          max-height: 244px;
          object-fit: contain;
          object-position: right bottom;
        }
  
        .playground-onboarding-concept-visual,
        .playground-onboarding-concept-image,
        .playground-onboarding-concept-icon {
          display: none;
        }
  
        .playground-onboarding-concept-title {
          margin: 0;
          font-size: 28px;
          font-weight: 600;
          line-height: 1.12;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-onboarding-concept-copy {
          margin: 0;
          max-width: 420px;
          font-size: 14px;
          line-height: 1.7;
          color: rgba(255, 255, 255, 0.62);
        }
  
        .playground-onboarding-concept-actions {
          display: inline-flex;
          align-items: center;
          gap: 10px;
        }
  
        .playground-onboarding-detail-grid {
          display: grid;
          grid-template-columns: repeat(3, minmax(0, 1fr));
          gap: 12px;
        }
  
        .playground-onboarding-detail-card {
          padding: 16px;
          border-top: 1px solid rgba(255, 255, 255, 0.08);
          padding-left: 0;
        }
  
        .playground-onboarding-detail-card-title {
          margin: 0 0 6px;
          font-size: 13px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.92);
        }
  
        .playground-onboarding-detail-card-copy {
          margin: 0;
          font-size: 12px;
          line-height: 1.6;
          color: rgba(255, 255, 255, 0.58);
        }
  
        .playground-onboarding-plan-panel {
          display: grid;
          grid-template-columns: minmax(0, 1.15fr) minmax(0, 0.85fr);
          gap: 14px;
        }
  
        .playground-onboarding-plan-features {
          display: flex;
          flex-direction: column;
          gap: 10px;
          margin: 0;
          padding: 0;
          list-style: none;
        }
  
        .playground-onboarding-plan-features-inline {
          width: 100%;
          max-width: 420px;
          margin-top: 8px;
        }
  
        .playground-onboarding-plan-feature {
          display: flex;
          align-items: flex-start;
          gap: 10px;
          font-size: 13px;
          line-height: 1.55;
          color: rgba(255, 255, 255, 0.68);
        }
  
        .playground-onboarding-plan-feature-icon {
          width: 15px;
          height: 15px;
          margin-top: 2px;
          color: rgba(255, 255, 255, 0.88);
          flex-shrink: 0;
        }
  
        .playground-onboarding-plan-price {
          display: flex;
          align-items: baseline;
          flex-wrap: wrap;
          gap: 6px;
          font-size: 34px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-onboarding-plan-price-after {
          font-size: 12px;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.52);
        }
  
        .playground-onboarding-plan-price-copy {
          font-size: 12px;
          color: rgba(255, 255, 255, 0.52);
        }
  
        .playground-onboarding-plan-offer {
          display: inline-flex;
          align-items: center;
          gap: 6px;
          margin-bottom: 12px;
          font-size: 11px;
          font-weight: 500;
          letter-spacing: 0.08em;
          text-transform: uppercase;
          color: rgba(255, 255, 255, 0.76);
        }
  
        .playground-onboarding-plan-cta {
          width: 100%;
          margin-top: 16px;
          background: #fff;
          color: #111;
          border-radius: 999px;
        }
  
        .playground-onboarding-hero {
          display: flex;
          align-items: flex-start;
          gap: 18px;
        }
  
        .playground-onboarding-hero-logo {
          width: 52px;
          height: 52px;
          padding: 12px;
          border-radius: 16px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: #111;
          object-fit: contain;
          flex-shrink: 0;
        }
  
        .playground-onboarding-hero-copy {
          min-width: 0;
          flex: 1;
        }
  
        .playground-onboarding-hero-title {
          margin: 0 0 8px;
          font-size: 28px;
          font-weight: 600;
          line-height: 1.12;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-onboarding-hero-text {
          margin: 0;
          font-size: 14px;
          line-height: 1.65;
          color: rgba(255, 255, 255, 0.66);
        }
  
        .playground-onboarding-feature-grid,
        .playground-onboarding-grid,
        .playground-onboarding-connector-grid {
          display: grid;
          gap: 12px;
        }
  
        .playground-onboarding-feature-grid {
          grid-template-columns: repeat(3, minmax(0, 1fr));
        }
  
        .playground-onboarding-grid {
          grid-template-columns: minmax(0, 280px) minmax(0, 1fr);
          align-items: start;
        }
  
        .playground-onboarding-feature-card,
        .playground-onboarding-panel,
        .playground-onboarding-connector-card,
        .playground-onboarding-sdk-card,
        .playground-onboarding-auth-card {
          border-radius: 18px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.03);
        }
  
        .playground-onboarding-feature-card {
          padding: 16px;
        }
  
        .playground-onboarding-feature-icon {
          width: 18px;
          height: 18px;
          margin-bottom: 14px;
          color: rgba(255, 255, 255, 0.92);
        }
  
        .playground-onboarding-feature-title {
          margin: 0 0 6px;
          font-size: 14px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-onboarding-feature-copy,
        .playground-onboarding-panel-copy,
        .playground-onboarding-sdk-note,
        .playground-onboarding-auth-copy {
          margin: 0;
          font-size: 13px;
          line-height: 1.55;
          color: rgba(255, 255, 255, 0.6);
        }
  
        .playground-onboarding-panel {
          padding: 14px;
        }
  
        .playground-onboarding-panel-title {
          margin: 0 0 6px;
          font-size: 13px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.9);
        }
  
        .playground-onboarding-presets {
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-onboarding-preset {
          width: 100%;
          padding: 14px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 16px;
          background: rgba(255, 255, 255, 0.02);
          color: inherit;
          text-align: left;
          cursor: pointer;
          transition: border-color 160ms ease, background-color 160ms ease, transform 160ms ease;
        }
  
        .playground-onboarding-preset:hover {
          border-color: rgba(255, 255, 255, 0.14);
          background: rgba(255, 255, 255, 0.04);
        }
  
        .playground-onboarding-preset.is-active {
          border-color: rgba(125, 176, 255, 0.36);
          background: rgba(125, 176, 255, 0.12);
        }
  
        .playground-onboarding-preset-title {
          margin: 0 0 4px;
          font-size: 14px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-onboarding-preset-copy {
          margin: 0;
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.58);
        }
  
        .playground-onboarding-form {
          display: flex;
          flex-direction: column;
          gap: 14px;
        }
  
        .playground-onboarding-field {
          display: flex;
          flex-direction: column;
          gap: 8px;
        }
  
        .playground-onboarding-label {
          font-size: 12px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.74);
        }
  
        .playground-onboarding-input,
        .playground-onboarding-select,
        .playground-onboarding-textarea {
          width: 100%;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 14px;
          background: #212121;
          color: rgba(255, 255, 255, 0.96);
          outline: none;
          font-size: 13px;
          line-height: 1.5;
        }
  
        .playground-onboarding-input,
        .playground-onboarding-select {
          min-height: 42px;
          padding: 0 14px;
        }
  
        .playground-onboarding-textarea {
          min-height: 168px;
          padding: 12px 14px;
          resize: vertical;
        }
  
        .playground-onboarding-helper {
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.5);
        }
  
        .playground-onboarding-status {
          padding: 10px 12px;
          border-radius: 14px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.03);
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.64);
        }
  
        .playground-onboarding-status.is-success {
          border-color: rgba(88, 211, 126, 0.22);
          background: rgba(88, 211, 126, 0.12);
          color: rgba(195, 255, 208, 0.92);
        }
  
        .playground-onboarding-status.is-error {
          border-color: rgba(248, 113, 113, 0.22);
          background: rgba(248, 113, 113, 0.12);
          color: rgba(255, 205, 205, 0.92);
        }
  
        .playground-onboarding-connector-grid {
          grid-template-columns: repeat(2, minmax(0, 1fr));
        }
  
        .playground-onboarding-connector-card {
          padding: 14px;
          display: flex;
          flex-direction: column;
          gap: 14px;
        }
  
        .playground-onboarding-connector-title-row {
          display: flex;
          align-items: center;
          gap: 12px;
        }
  
        .playground-onboarding-connector-logo {
          width: 18px;
          height: 18px;
          object-fit: contain;
          flex-shrink: 0;
        }
  
        .playground-onboarding-connector-title {
          font-size: 14px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-onboarding-connector-copy {
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.58);
        }
  
        .playground-onboarding-connector-footer {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-onboarding-connector-status {
          font-size: 12px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.56);
        }
  
        .playground-onboarding-connector-status.is-connected {
          color: rgba(195, 255, 208, 0.92);
        }
  
        .playground-onboarding-upload-zone {
          border: 1px dashed rgba(255, 255, 255, 0.14);
          border-radius: 18px;
          background: rgba(255, 255, 255, 0.02);
          min-height: 164px;
          padding: 18px;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          gap: 10px;
          text-align: center;
          cursor: pointer;
          transition: border-color 160ms ease, background-color 160ms ease;
        }
  
        .playground-onboarding-upload-zone.is-dragging {
          border-color: rgba(125, 176, 255, 0.34);
          background: rgba(125, 176, 255, 0.08);
        }
  
        .playground-onboarding-upload-icon {
          width: 18px;
          height: 18px;
          color: rgba(255, 255, 255, 0.92);
        }
  
        .playground-onboarding-upload-title {
          font-size: 14px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-onboarding-upload-copy {
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.56);
        }
  
        .playground-onboarding-upload-list {
          display: flex;
          flex-direction: column;
          gap: 8px;
        }
  
        .playground-onboarding-upload-item {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          padding: 12px 14px;
          border-radius: 14px;
          background: rgba(255, 255, 255, 0.04);
          border: 1px solid rgba(255, 255, 255, 0.06);
        }
  
        .playground-onboarding-upload-item-main {
          min-width: 0;
          flex: 1;
          display: flex;
          align-items: center;
          gap: 10px;
        }
  
        .playground-onboarding-upload-item-name {
          min-width: 0;
          font-size: 13px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.92);
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-onboarding-upload-item-meta {
          flex-shrink: 0;
          font-size: 12px;
          color: rgba(255, 255, 255, 0.52);
        }
  
        .playground-onboarding-sdk-card {
          padding: 18px;
          display: flex;
          flex-direction: column;
          gap: 14px;
          border-radius: 15px;
        }
  
        .playground-onboarding-sdk-title-row,
        .playground-onboarding-key-row {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-onboarding-sdk-title {
          font-size: 14px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-onboarding-key-value {
          min-width: 0;
          flex: 1;
          padding: 12px 14px;
          border-radius: 14px;
          background: #212121;
          border: 1px solid rgba(255, 255, 255, 0.08);
          font-family: ui-monospace, SFMono-Regular, SFMono-Regular, Menlo, monospace;
          font-size: 12px;
          color: rgba(255, 255, 255, 0.88);
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-onboarding-key-actions {
          display: inline-flex;
          align-items: center;
          gap: 8px;
        }
  
        .playground-onboarding-icon-button {
          width: 36px;
          height: 36px;
          padding: 0;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 12px;
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.82);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
        }
  
        .playground-onboarding-icon-button:hover {
          background: rgba(255, 255, 255, 0.08);
          color: #fff;
        }
  
        .playground-onboarding-code {
          margin: 0;
          padding: 16px;
          border-radius: 16px;
          border: 1px solid rgba(255, 255, 255, 0.06);
          background: #111;
          color: rgba(255, 255, 255, 0.9);
          font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
          font-size: 12px;
          line-height: 1.65;
          white-space: pre-wrap;
          word-break: break-word;
        }
  
        .playground-onboarding-footer {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-onboarding-footer-actions {
          display: inline-flex;
          align-items: center;
          gap: 10px;
        }
  
        .playground-onboarding-button {
          min-height: 36px;
          padding: 0 16px;
          border-radius: 999px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.92);
          font-size: 13px;
          font-weight: 500;
          cursor: pointer;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 8px;
        }
  
        .playground-onboarding-button:hover {
          background: rgba(255, 255, 255, 0.08);
        }
  
        .playground-onboarding-button.is-primary {
          background: #fff;
          color: #111;
          border-color: rgba(255, 255, 255, 0.16);
        }
  
        .playground-onboarding-button.is-primary:hover {
          background: rgba(255, 255, 255, 0.92);
        }
  
        .playground-onboarding-button.is-ghost {
          background: transparent;
          border-color: transparent;
          color: rgba(255, 255, 255, 0.56);
        }
  
        .playground-onboarding-button:disabled,
        .playground-onboarding-icon-button:disabled,
        .playground-onboarding-preset:disabled {
          opacity: 0.55;
          cursor: not-allowed;
        }
  
        .playground-onboarding-auth-card {
          width: min(480px, 100%);
          padding: 22px;
          display: flex;
          flex-direction: column;
          gap: 18px;
        }
  
        .playground-onboarding-auth-actions,
        .playground-onboarding-auth-form {
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-onboarding-auth-button {
          width: 100%;
          justify-content: center;
        }
  
        .playground-onboarding-auth-divider {
          display: flex;
          align-items: center;
          gap: 10px;
          font-size: 11px;
          color: rgba(255, 255, 255, 0.38);
          text-transform: uppercase;
          letter-spacing: 0.12em;
        }
  
        .playground-onboarding-auth-divider::before,
        .playground-onboarding-auth-divider::after {
          content: "";
          flex: 1 1 auto;
          height: 1px;
          background: rgba(255, 255, 255, 0.08);
        }
  
        .playground-onboarding-auth-input {
          width: 100%;
          padding: 12px 14px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 14px;
          background: rgba(255, 255, 255, 0.03);
          color: rgba(255, 255, 255, 0.96);
          font-size: 13px;
          outline: none;
          transition: border-color 160ms ease, background-color 160ms ease;
        }
  
        .playground-onboarding-auth-input::placeholder {
          color: rgba(255, 255, 255, 0.36);
        }
  
        .playground-onboarding-auth-input:focus {
          border-color: rgba(255, 255, 255, 0.18);
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-onboarding-auth-help {
          margin: -2px 0 0;
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.5);
        }
  
        .playground-onboarding-auth-title {
          margin: 0;
          font-size: 22px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.98);
        }
  
        @media (max-width: 920px) {
          .playground-onboarding-modal {
            width: min(720px, 100%);
          }
  
          .playground-onboarding-grid,
          .playground-onboarding-feature-grid,
          .playground-onboarding-connector-grid,
          .playground-onboarding-detail-grid,
          .playground-onboarding-plan-panel {
            grid-template-columns: 1fr;
          }
  
          .playground-onboarding-concept-hero {
            grid-template-columns: 1fr;
          }
  
          .playground-onboarding-concept-copy-column {
            gap: 14px;
          }
  
          .playground-onboarding-concept-preview-panel {
            min-height: 200px;
          }
        }
  
        .playground-onboarding-scrim {
          z-index: 2147482000;
          align-items: stretch;
          justify-content: stretch;
          padding: 0;
          background: rgba(0, 0, 0, 0.96);
          backdrop-filter: none;
        }
  
        .playground-onboarding-modal {
          width: 100vw;
          max-height: none;
          height: 100vh;
          position: relative;
          display: grid;
          grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
          overflow: hidden;
          border: 0;
          border-radius: 0;
          background: #000;
          box-shadow: none;
        }
  
        .playground-onboarding-pane {
          min-width: 0;
          min-height: 0;
          position: relative;
          z-index: 1;
          display: flex;
          flex-direction: column;
          padding: 18px 28px 20px;
        }
  
        .playground-onboarding-modal.is-welcome .playground-onboarding-pane.is-config.is-welcome {
          opacity: 0;
          transform: translateX(-100%);
          transition:
            opacity 620ms cubic-bezier(0.22, 1, 0.36, 1),
            transform 620ms cubic-bezier(0.22, 1, 0.36, 1);
          will-change: opacity, transform;
        }
  
        .playground-onboarding-modal.is-welcome.is-welcome-video-started .playground-onboarding-pane.is-config.is-welcome {
          opacity: 1;
          transform: translateX(0);
        }
  
        .playground-onboarding-modal.is-welcome .playground-onboarding-pane.is-explain.is-welcome {
          transition:
            opacity 520ms cubic-bezier(0.22, 1, 0.36, 1),
            transform 520ms cubic-bezier(0.22, 1, 0.36, 1);
          will-change: opacity, transform;
        }
  
        .playground-onboarding-modal.is-onboarding-transition-leaving .playground-onboarding-pane.is-config {
          opacity: 0;
          transform: translateX(-100%);
          transition:
            opacity 520ms cubic-bezier(0.22, 1, 0.36, 1),
            transform 520ms cubic-bezier(0.22, 1, 0.36, 1);
        }
  
        .playground-onboarding-modal.is-welcome.is-onboarding-transition-leaving .playground-onboarding-pane.is-config.is-welcome {
          opacity: 0;
          transform: translateX(-100%);
        }
  
        .playground-onboarding-modal.is-onboarding-transition-leaving .playground-onboarding-pane.is-explain {
          opacity: 0;
          transform: translateX(100%);
          transition:
            opacity 520ms cubic-bezier(0.22, 1, 0.36, 1),
            transform 520ms cubic-bezier(0.22, 1, 0.36, 1);
        }
  
        .playground-onboarding-modal.is-onboarding-transition-entering .playground-onboarding-pane.is-config {
          animation: playgroundOnboardingPaneInLeft 560ms cubic-bezier(0.22, 1, 0.36, 1) both;
        }
  
        .playground-onboarding-modal.is-onboarding-transition-entering .playground-onboarding-pane.is-explain {
          animation: playgroundOnboardingPaneInRight 560ms cubic-bezier(0.22, 1, 0.36, 1) both;
        }
  
        .playground-onboarding-modal.is-onboarding-pane-transition-leaving .playground-onboarding-pane.is-config {
          opacity: 0;
          transform: translateX(-100%);
          transition:
            opacity 520ms cubic-bezier(0.22, 1, 0.36, 1),
            transform 520ms cubic-bezier(0.22, 1, 0.36, 1);
        }
  
        .playground-onboarding-modal.is-onboarding-pane-transition-leaving .playground-onboarding-pane.is-explain {
          opacity: 0;
          transform: translateX(100%);
          transition:
            opacity 520ms cubic-bezier(0.22, 1, 0.36, 1),
            transform 520ms cubic-bezier(0.22, 1, 0.36, 1);
        }
  
        .playground-onboarding-modal.is-onboarding-pane-transition-entering .playground-onboarding-pane.is-config {
          animation: playgroundOnboardingPaneInLeft 560ms cubic-bezier(0.22, 1, 0.36, 1) both;
        }
  
        .playground-onboarding-modal.is-onboarding-pane-transition-entering .playground-onboarding-pane.is-explain {
          animation: playgroundOnboardingPaneInRight 560ms cubic-bezier(0.22, 1, 0.36, 1) both;
        }
  
        .playground-onboarding-modal.is-onboarding-free-exit-active .playground-onboarding-pane.is-config {
          opacity: 0;
          transform: translateX(-100%);
          transition:
            opacity 520ms cubic-bezier(0.22, 1, 0.36, 1),
            transform 520ms cubic-bezier(0.22, 1, 0.36, 1);
        }
  
        .playground-onboarding-modal.is-onboarding-free-exit-active .playground-onboarding-pane.is-explain {
          opacity: 0;
          transform: translateX(100%);
          transition:
            opacity 520ms cubic-bezier(0.22, 1, 0.36, 1),
            transform 520ms cubic-bezier(0.22, 1, 0.36, 1);
        }
  
        .playground-onboarding-modal.is-onboarding-free-exit-fading .playground-onboarding-video-bg {
          opacity: 0;
        }
  
        .playground-onboarding-transition-loader {
          position: absolute;
          inset: 0;
          z-index: 5;
          display: flex;
          align-items: center;
          justify-content: center;
          pointer-events: none;
        }
  
        .playground-onboarding-transition-label {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 12px;
          padding: 12px 16px;
          border-radius: 999px;
          background: rgba(0, 0, 0, 0.36);
          border: 1px solid rgba(255, 255, 255, 0.10);
          color: rgba(255, 255, 255, 0.94);
          font-size: 13px;
          font-weight: 400;
          line-height: 1;
          backdrop-filter: blur(32px);
          -webkit-backdrop-filter: blur(32px);
          opacity: 0;
          transform: scale(0);
          transform-origin: center center;
        }
  
        .playground-onboarding-transition-dot-loader {
          display: inline-flex;
          align-items: center;
          gap: 3px;
        }
  
        .playground-onboarding-transition-dot {
          width: 3px;
          height: 3px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.72);
          animation: playgroundOnboardingTransitionDot 0.95s ease-in-out infinite;
        }
  
        .playground-onboarding-modal.is-onboarding-transition-loading .playground-onboarding-transition-label {
          animation: playgroundOnboardingTransitionLabelIn 260ms cubic-bezier(0.22, 1, 0.36, 1) both;
        }
  
        .playground-onboarding-modal.is-onboarding-transition-hiding-label .playground-onboarding-transition-label {
          animation: playgroundOnboardingTransitionLabelOut 220ms cubic-bezier(0.64, 0, 0.78, 0) both;
        }
  
        .playground-onboarding-pane.is-config {
          background: rgba(0, 0, 0, 0.70);
          backdrop-filter: blur(100px);
          -webkit-backdrop-filter: blur(100px);
        }
  
        .playground-onboarding-pane.is-config.is-welcome {
          padding-top: 18px;
        }
  
        .playground-onboarding-pane.is-explain {
          position: relative;
          overflow: hidden;
          background: transparent;
        }
  
        .playground-onboarding-pane.is-explain > *:not(.playground-onboarding-video-bg) {
          position: relative;
          z-index: 1;
        }
  
        .playground-onboarding-video-bg {
          position: absolute;
          inset: 0;
          z-index: 0;
          overflow: hidden;
          pointer-events: none;
          background: #000;
          opacity: 1;
          transition: opacity 420ms ease;
        }
  
        .playground-onboarding-video-bg-media {
          position: absolute;
          inset: 0;
          width: 100%;
          height: 100%;
          object-fit: cover;
        }
  
        .playground-onboarding-video-bg-video {
          opacity: 0;
          transition: opacity 700ms ease;
        }
  
        .playground-onboarding-video-bg-video.is-ready {
          opacity: 1;
        }
  
        .playground-onboarding-video-bg-overlay {
          position: absolute;
          inset: 0;
          background: linear-gradient(180deg, rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.8));
        }
  
        .playground-onboarding-pane-top,
        .playground-onboarding-pane-bottom,
        .playground-onboarding-back-button,
        .playground-onboarding-step-count,
        .playground-onboarding-progress-group,
        .playground-onboarding-config-heading,
        .playground-onboarding-option-card,
        .playground-onboarding-agent-row,
        .playground-onboarding-connector-row,
        .playground-onboarding-resource-row,
        .playground-onboarding-plan-action-row,
        .playground-onboarding-explain-bullet,
        .playground-onboarding-dots {
          display: flex;
          align-items: center;
        }
  
        .playground-onboarding-pane-top {
          flex: 0 0 auto;
          justify-content: space-between;
          gap: 18px;
          margin-bottom: 24px;
        }
  
        .playground-onboarding-back-button,
        .playground-onboarding-close {
          width: auto;
          height: auto;
          min-height: 32px;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.82);
          font-size: 13px;
          font-weight: 400;
          gap: 8px;
        }
  
        .playground-onboarding-back-button:hover,
        .playground-onboarding-close:hover {
          background: transparent;
          color: #fff;
        }
  
        .playground-onboarding-step-count {
          gap: 8px;
          color: rgba(255, 255, 255, 0.52);
          font-size: 12px;
          font-weight: 400;
        }
  
        .playground-onboarding-config-scroll {
          min-height: 0;
          flex: 1 1 auto;
          overflow: auto;
          padding-right: 2px;
          scrollbar-width: none;
        }
  
        .playground-onboarding-pane.is-welcome .playground-onboarding-config-scroll {
          display: flex;
          align-items: center;
        }
  
        .playground-onboarding-config-scroll::-webkit-scrollbar {
          display: none;
        }
  
        .playground-onboarding-config-stack {
          width: 100%;
          display: flex;
          flex-direction: column;
          gap: 18px;
        }
  
        .playground-onboarding-pane.is-welcome .playground-onboarding-config-stack {
          min-height: 100%;
          justify-content: center;
        }
  
        .playground-onboarding-pane.is-computer .playground-onboarding-config-scroll,
        .playground-onboarding-pane.is-agents .playground-onboarding-config-scroll,
        .playground-onboarding-pane.is-connectors .playground-onboarding-config-scroll,
        .playground-onboarding-pane.is-plan .playground-onboarding-config-scroll {
          display: flex;
          align-items: center;
        }
  
        .playground-onboarding-pane.is-computer .playground-onboarding-config-stack,
        .playground-onboarding-pane.is-agents .playground-onboarding-config-stack,
        .playground-onboarding-pane.is-connectors .playground-onboarding-config-stack,
        .playground-onboarding-pane.is-plan .playground-onboarding-config-stack {
          justify-content: center;
        }
  
        .playground-onboarding-config-heading {
          align-items: flex-start;
          gap: 12px;
          padding-bottom: 14px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.10);
        }
  
        .playground-onboarding-config-heading.is-plain {
          gap: 0;
          padding-bottom: 0;
          border-bottom: 0;
        }
  
        .playground-onboarding-config-icon {
          width: 34px;
          height: 34px;
          border-radius: 10px;
          background: rgba(255, 255, 255, 0.06);
          color: rgba(255, 255, 255, 0.92);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          flex: 0 0 auto;
        }
  
        .playground-onboarding-config-title {
          margin: 0;
          color: rgba(255, 255, 255, 0.96);
          font-size: 20px;
          font-weight: 500;
          line-height: 1.25;
        }
  
        .playground-onboarding-config-copy {
          margin: 4px 0 0;
          max-width: 620px;
          color: rgba(255, 255, 255, 0.58);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.55;
        }
  
        .playground-onboarding-pane.is-agents .playground-onboarding-config-copy {
          max-width: none;
          white-space: nowrap;
        }
  
        .playground-onboarding-section {
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-onboarding-section-title {
          color: rgba(255, 255, 255, 0.86);
          font-size: 12px;
          font-weight: 500;
          line-height: 1.35;
        }
  
        .playground-onboarding-option-grid {
          display: grid;
          grid-template-columns: repeat(3, minmax(0, 1fr));
          gap: 10px;
        }
  
        .playground-onboarding-option-card {
          min-height: 82px;
          align-items: flex-start;
          flex-direction: column;
          gap: 6px;
          padding: 13px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 10px;
          background: rgba(255, 255, 255, 0.04);
          color: inherit;
          text-align: left;
          cursor: pointer;
        }
  
        .playground-onboarding-option-card.is-active {
          border-color: rgba(255, 255, 255, 0.26);
          background: rgba(255, 255, 255, 0.10);
        }
  
        .playground-onboarding-option-title {
          color: rgba(255, 255, 255, 0.94);
          font-size: 13px;
          font-weight: 500;
          line-height: 1.25;
        }
  
        .playground-onboarding-option-copy {
          color: rgba(255, 255, 255, 0.52);
          font-size: 11px;
          font-weight: 400;
          line-height: 1.45;
        }
  
        .playground-onboarding-form-grid {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 12px;
        }
  
        .playground-onboarding-form-grid.is-single {
          grid-template-columns: minmax(0, 1fr);
        }
  
        .playground-onboarding-field {
          gap: 7px;
        }
  
        .playground-onboarding-label {
          color: rgba(255, 255, 255, 0.68);
          font-size: 12px;
          font-weight: 400;
        }
  
        .playground-onboarding-input,
        .playground-onboarding-select,
        .playground-onboarding-textarea {
          border-radius: 8px;
          background: rgba(255, 255, 255, 0.05);
          border-color: rgba(255, 255, 255, 0.10);
        }
  
        .playground-onboarding-select {
          color-scheme: dark;
        }
  
        .playground-onboarding-status {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 6px;
          flex: 0 0 auto;
          border-radius: 10px;
          background: rgba(255, 255, 255, 0.05);
          border-color: rgba(255, 255, 255, 0.10);
        }
  
        .playground-onboarding-agent-list,
        .playground-onboarding-connector-list,
        .playground-onboarding-resource-list {
          display: flex;
          flex-direction: column;
          gap: 8px;
        }
  
        .playground-onboarding-agent-list,
        .playground-onboarding-connector-list {
          gap: 14px;
        }
  
        .playground-onboarding-agent-row,
        .playground-onboarding-connector-row,
        .playground-onboarding-resource-row {
          justify-content: space-between;
          gap: 12px;
          padding: 12px;
          border-radius: 10px;
          background: rgba(255, 255, 255, 0.05);
          border: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-onboarding-agent-row,
        .playground-onboarding-connector-row {
          padding: 0;
          padding-bottom: 12px;
          border: 0;
          border-bottom: 1px solid rgba(255, 255, 255, 0.10);
          border-radius: 0;
          background: transparent;
        }
  
        .playground-onboarding-row-main {
          min-width: 0;
          display: flex;
          align-items: center;
          gap: 11px;
        }
  
        .playground-onboarding-row-icon {
          width: 30px;
          height: 30px;
          border-radius: 9px;
          background: rgba(255, 255, 255, 0.08);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          color: rgba(255, 255, 255, 0.86);
          flex: 0 0 auto;
        }
  
        .playground-onboarding-connector-logo,
        .playground-onboarding-row-avatar {
          width: 32px;
          height: 32px;
          border-radius: 999px;
          object-fit: cover;
          display: block;
          flex: 0 0 auto;
        }
  
        .playground-onboarding-connector-logo {
          border-radius: 0;
          object-fit: contain;
        }
  
        .playground-onboarding-connector-logo.is-github {
          filter: invert(1);
          opacity: 0.9;
        }
  
        .playground-onboarding-connector-logo-fallback {
          width: 32px;
          height: 32px;
          border-radius: 10px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          color: rgba(255, 255, 255, 0.86);
          background: rgba(255, 255, 255, 0.08);
          flex: 0 0 auto;
        }
  
        .playground-onboarding-row-title {
          color: rgba(255, 255, 255, 0.92);
          font-size: 13px;
          font-weight: 500;
          line-height: 1.25;
        }
  
        .playground-onboarding-row-copy {
          margin-top: 3px;
          color: rgba(255, 255, 255, 0.50);
          font-size: 11px;
          font-weight: 400;
          line-height: 1.35;
        }
  
        .playground-onboarding-row-status {
          flex: 0 0 auto;
          color: rgba(255, 255, 255, 0.60);
          font-size: 12px;
          font-weight: 400;
        }
  
        .playground-onboarding-agent-model {
          min-width: 180px;
          flex: 0 0 auto;
          display: flex;
          align-items: center;
          justify-content: flex-end;
          gap: 8px;
          color: rgba(255, 255, 255, 0.62);
          text-align: right;
        }
  
        .playground-onboarding-agent-model-name {
          color: rgba(255, 255, 255, 0.82);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.2;
        }
  
        .playground-onboarding-connector-action {
          flex: 0 0 auto;
          min-height: 30px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          padding: 0 12px;
          border: 1px solid rgba(255, 255, 255, 0.10);
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.06);
          color: rgba(255, 255, 255, 0.84);
          font-size: 12px;
          font-weight: 400;
          cursor: pointer;
        }
  
        .playground-onboarding-connector-action:hover:not(:disabled) {
          background: rgba(255, 255, 255, 0.10);
        }
  
        .playground-onboarding-connector-action.is-connected {
          border-color: transparent;
          background: transparent;
          color: rgba(255, 255, 255, 0.56);
          cursor: default;
        }
  
        .playground-onboarding-pane-bottom {
          flex: 0 0 auto;
          justify-content: space-between;
          gap: 14px;
          padding-top: 14px;
        }
  
        .playground-onboarding-dots {
          gap: 7px;
        }
  
        .playground-onboarding-progress-group {
          gap: 12px;
        }
  
        .playground-onboarding-dot {
          width: 7px;
          min-width: 7px;
          max-width: 7px;
          height: 7px;
          min-height: 7px;
          max-height: 7px;
          flex: 0 0 7px;
          display: block;
          padding: 0;
          border: 0;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.24);
        }
  
        .playground-onboarding-dot.is-active {
          width: 7px;
          min-width: 7px;
          max-width: 7px;
          height: 7px;
          min-height: 7px;
          max-height: 7px;
          background: #fff;
        }
  
        .playground-onboarding-footer-actions {
          gap: 10px;
        }
  
        .playground-onboarding-button {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 8px;
          min-height: 36px;
          padding: 0 16px;
          border-radius: 999px;
          font-weight: 400;
        }
  
        .playground-onboarding-button.is-link {
          padding: 0;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.58);
        }
  
        .playground-onboarding-button.is-link:hover {
          background: transparent;
          color: #fff;
        }
  
        .playground-onboarding-button.is-primary {
          background: #fff;
          color: #000;
        }
  
        .playground-onboarding-button-loader {
          display: inline-flex;
          align-items: center;
          gap: 2px;
        }
  
        .playground-onboarding-button-loader-dot {
          width: 3px;
          height: 3px;
          border-radius: 999px;
          background: currentColor;
          animation: playgroundOnboardingTransitionDot 0.95s ease-in-out infinite;
        }
  
        .playground-onboarding-free-plan-link {
          padding: 0;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.50);
          font-size: 12px;
          font-weight: 400;
          cursor: pointer;
        }
  
        .playground-onboarding-free-plan-link:hover {
          background: transparent;
          color: rgba(255, 255, 255, 0.74);
        }
  
        .playground-onboarding-explain-inner {
          width: 100%;
          margin: auto;
          display: flex;
          flex-direction: column;
          gap: 18px;
        }
  
        .playground-onboarding-pane.is-explain.is-computer .playground-onboarding-pane-top,
        .playground-onboarding-pane.is-explain.is-agents .playground-onboarding-pane-top,
        .playground-onboarding-pane.is-explain.is-connectors .playground-onboarding-pane-top,
        .playground-onboarding-pane.is-explain.is-plan .playground-onboarding-pane-top {
          display: none;
        }
  
        .playground-onboarding-pane.is-explain.is-computer .playground-onboarding-explain-inner,
        .playground-onboarding-pane.is-explain.is-agents .playground-onboarding-explain-inner,
        .playground-onboarding-pane.is-explain.is-connectors .playground-onboarding-explain-inner,
        .playground-onboarding-pane.is-explain.is-plan .playground-onboarding-explain-inner {
          align-items: center;
        }
  
        .playground-onboarding-explain-kicker {
          color: rgba(255, 255, 255, 0.46);
          font-size: 11px;
          font-weight: 500;
          letter-spacing: 0.10em;
          text-transform: uppercase;
        }
  
        .playground-onboarding-explain-title {
          margin: 0;
          color: rgba(255, 255, 255, 0.98);
          font-size: 34px;
          font-weight: 600;
          line-height: 1.08;
          letter-spacing: 0;
        }
  
        .playground-onboarding-explain-copy {
          margin: 0;
          max-width: 560px;
          color: rgba(255, 255, 255, 0.62);
          font-size: 14px;
          font-weight: 400;
          line-height: 1.65;
        }
  
        .playground-onboarding-pane.is-explain.is-computer .playground-onboarding-explain-title,
        .playground-onboarding-pane.is-explain.is-computer .playground-onboarding-explain-copy,
        .playground-onboarding-pane.is-explain.is-computer .playground-onboarding-explain-list,
        .playground-onboarding-pane.is-explain.is-agents .playground-onboarding-explain-title,
        .playground-onboarding-pane.is-explain.is-agents .playground-onboarding-explain-copy,
        .playground-onboarding-pane.is-explain.is-agents .playground-onboarding-explain-list,
        .playground-onboarding-pane.is-explain.is-connectors .playground-onboarding-explain-title,
        .playground-onboarding-pane.is-explain.is-connectors .playground-onboarding-explain-copy,
        .playground-onboarding-pane.is-explain.is-connectors .playground-onboarding-explain-list,
        .playground-onboarding-pane.is-explain.is-plan .playground-onboarding-explain-title,
        .playground-onboarding-pane.is-explain.is-plan .playground-onboarding-explain-copy,
        .playground-onboarding-pane.is-explain.is-plan .playground-onboarding-explain-list {
          width: min(100%, 520px);
          max-width: 520px;
        }
  
        .playground-onboarding-pane.is-explain.is-computer .playground-onboarding-explain-title,
        .playground-onboarding-pane.is-explain.is-agents .playground-onboarding-explain-title,
        .playground-onboarding-pane.is-explain.is-connectors .playground-onboarding-explain-title,
        .playground-onboarding-pane.is-explain.is-plan .playground-onboarding-explain-title {
          font-weight: 500;
        }
  
        .playground-onboarding-explain-visual {
          width: min(180px, 34vw);
          height: auto;
          display: block;
          margin: 0 0 10px;
        }
  
        .playground-onboarding-pane.is-explain.is-computer .playground-onboarding-explain-visual,
        .playground-onboarding-pane.is-explain.is-agents .playground-onboarding-explain-visual {
          width: 260px;
          max-width: min(260px, 54vw);
          margin-left: auto;
          margin-right: auto;
        }
  
        .playground-onboarding-pane.is-explain.is-agents .playground-onboarding-explain-visual {
          border-radius: 999px;
        }
  
        .playground-onboarding-explain-list {
          display: flex;
          flex-direction: column;
          gap: 14px;
          margin-top: 4px;
        }
  
        .playground-onboarding-explain-bullet {
          align-items: flex-start;
          gap: 12px;
        }
  
        .playground-onboarding-explain-bullet-icon {
          width: 18px;
          height: 18px;
          margin-top: 1px;
          color: rgba(255, 255, 255, 0.76);
          flex: 0 0 auto;
        }
  
        .playground-onboarding-explain-bullet-title {
          color: rgba(255, 255, 255, 0.92);
          font-size: 13px;
          font-weight: 500;
          line-height: 1.35;
        }
  
        .playground-onboarding-explain-bullet-copy {
          margin-top: 3px;
          color: rgba(255, 255, 255, 0.52);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.45;
        }
  
        .playground-onboarding-plan-card {
          width: min(100%, 380px);
          padding: 16px;
          border-radius: 15px;
          border: 1px solid rgba(255, 255, 255, 0.10);
          background: rgba(255, 255, 255, 0.05);
          display: flex;
          flex-direction: column;
          gap: 12px;
          margin: 0 auto;
        }
  
        .playground-onboarding-welcome-intro {
          width: 100%;
          min-height: 0;
          display: flex;
          flex-direction: column;
          align-items: flex-start;
          justify-content: center;
          gap: 28px;
          padding: 0 0 42px;
        }
  
        .playground-onboarding-welcome-kicker {
          display: inline-flex;
          align-items: center;
          gap: 10px;
          color: rgba(255, 255, 255, 0.82);
          font-size: 16px;
          font-weight: 500;
          line-height: 1.35;
        }
  
        .playground-onboarding-welcome-kicker-icon {
          color: rgba(255, 255, 255, 0.94);
          flex: 0 0 auto;
        }
  
        .playground-onboarding-welcome-title {
          margin: 0;
          max-width: 660px;
          color: rgba(255, 255, 255, 0.98);
          font-size: clamp(38px, 4.3vw, 58px);
          font-weight: 500;
          line-height: 1.04;
          letter-spacing: 0;
        }
  
        .playground-onboarding-welcome-title-accent {
          color: inherit;
          font-style: normal;
          font-weight: inherit;
        }
  
        .playground-onboarding-welcome-copy {
          margin: 0;
          max-width: 640px;
          color: rgba(255, 255, 255, 0.64);
          font-size: 17px;
          font-weight: 400;
          line-height: 1.55;
        }
  
        .playground-onboarding-welcome-note {
          margin: -12px 0 0;
          max-width: 560px;
          color: rgba(255, 255, 255, 0.42);
          font-size: 13px;
          font-weight: 400;
          line-height: 1.5;
        }
  
        .playground-onboarding-welcome-prompt-stage {
          width: 100%;
          min-height: 100%;
          flex: 1 1 auto;
          display: flex;
          align-items: center;
          justify-content: center;
          pointer-events: none;
        }
  
        .playground-onboarding-welcome-prompt-wrap {
          width: min(100%, 56rem);
          max-width: 56rem;
          position: relative;
          display: flex;
          flex-direction: column;
          padding: 84px 0 98px;
        }
  
        .tb-runner-chat.playground-onboarding-welcome-runner-mock {
          width: 100%;
          max-width: 100%;
          height: auto;
          min-height: 0;
          flex: 0 1 auto;
          display: block;
          overflow: visible;
          background: transparent;
          position: relative;
          z-index: 1;
        }
  
        .playground-onboarding-welcome-prompt-stage .tb-input-width {
          width: 100%;
          max-width: 56rem;
          pointer-events: none;
        }
  
        .playground-onboarding-modal.is-welcome .playground-onboarding-welcome-prompt-stage .embedded-runner-input {
          opacity: 0;
          transform: scale(0);
          transform-origin: center center;
        }
  
        .playground-onboarding-modal.is-welcome.is-welcome-video-started .playground-onboarding-welcome-prompt-stage .embedded-runner-input {
          animation: playgroundOnboardingPromptIn 440ms cubic-bezier(0.22, 1, 0.36, 1) 620ms both;
        }
  
        .playground-onboarding-welcome-prompt-stage .tb-runner-chat.playground-onboarding-welcome-runner-mock .task-input-box {
          --tb-task-input-overlay: transparent;
          --tb-task-input-base-bg: rgba(0, 0, 0, 0.25);
          pointer-events: none;
          background: rgba(0, 0, 0, 0.25);
          backdrop-filter: blur(50px);
          -webkit-backdrop-filter: blur(50px);
        }
  
        .playground-onboarding-welcome-prompt-stage .sidebar-textarea {
          height: 52px;
          overflow: hidden;
        }
  
        .playground-onboarding-welcome-input-guide {
          position: absolute;
          inset: 0;
          z-index: 20;
          opacity: 0;
          pointer-events: none;
        }
  
        .playground-onboarding-modal.is-welcome.is-welcome-video-started .playground-onboarding-welcome-input-guide {
          animation: playgroundOnboardingGuideIn 340ms ease 820ms both;
        }
  
        .playground-onboarding-welcome-input-guide-lines {
          position: absolute;
          inset: 0;
          width: 100%;
          height: 100%;
          overflow: visible;
          z-index: 20;
        }
  
        .playground-onboarding-welcome-input-guide-line {
          stroke: rgba(255, 255, 255, 0.42);
          stroke-width: 1;
          stroke-linecap: round;
          stroke-dasharray: 1 6;
          vector-effect: non-scaling-stroke;
        }
  
        .playground-onboarding-welcome-input-guide-item {
          position: absolute;
          width: var(--playground-onboarding-guide-width, 120px);
          display: flex;
          flex-direction: column;
          align-items: flex-start;
          gap: 5px;
          text-align: left;
          z-index: 21;
        }
  
        .playground-onboarding-welcome-input-guide-item-textarea {
          left: 20px;
          top: 20px;
          --playground-onboarding-guide-width: 270px;
        }
  
        .playground-onboarding-welcome-input-guide-item-attach {
          left: 6px;
          top: 218px;
          --playground-onboarding-guide-width: 78px;
        }
  
        .playground-onboarding-welcome-input-guide-item-context {
          left: 96px;
          top: 218px;
          --playground-onboarding-guide-width: 82px;
        }
  
        .playground-onboarding-welcome-input-guide-item-team {
          left: 208px;
          top: 218px;
          --playground-onboarding-guide-width: 132px;
        }
  
        .playground-onboarding-welcome-input-guide-item-computer {
          right: 48px;
          top: 218px;
          align-items: flex-end;
          text-align: right;
          --playground-onboarding-guide-width: 142px;
        }
  
        .playground-onboarding-welcome-input-guide-item-voice {
          right: 0;
          top: 20px;
          align-items: flex-end;
          text-align: right;
          --playground-onboarding-guide-width: 230px;
        }
  
        .playground-onboarding-welcome-input-guide-title {
          color: rgba(255, 255, 255, 0.84);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.25;
        }
  
        .playground-onboarding-welcome-input-guide-copy {
          max-width: 150px;
          color: rgba(255, 255, 255, 0.46);
          font-size: 11px;
          font-weight: 400;
          line-height: 1.35;
        }
  
        .playground-onboarding-welcome-input-guide-item-textarea .playground-onboarding-welcome-input-guide-copy,
        .playground-onboarding-welcome-input-guide-item-voice .playground-onboarding-welcome-input-guide-copy {
          max-width: 100%;
        }
  
        @keyframes playgroundOnboardingPromptIn {
          from {
            opacity: 0;
            transform: scale(0);
          }
          to {
            opacity: 1;
            transform: scale(1);
          }
        }
  
        @keyframes playgroundOnboardingGuideIn {
          from { opacity: 0; }
          to { opacity: 1; }
        }
  
        @keyframes playgroundOnboardingPaneInLeft {
          from {
            opacity: 0;
            transform: translateX(-100%);
          }
          to {
            opacity: 1;
            transform: translateX(0);
          }
        }
  
        @keyframes playgroundOnboardingPaneInRight {
          from {
            opacity: 0;
            transform: translateX(100%);
          }
          to {
            opacity: 1;
            transform: translateX(0);
          }
        }
  
        @keyframes playgroundOnboardingTransitionLabelIn {
          from {
            opacity: 0;
            transform: scale(0);
          }
          to {
            opacity: 1;
            transform: scale(1);
          }
        }
  
        @keyframes playgroundOnboardingTransitionLabelOut {
          from {
            opacity: 1;
            transform: scale(1);
          }
          to {
            opacity: 0;
            transform: scale(0);
          }
        }
  
        @keyframes playgroundOnboardingTransitionDot {
          0%,
          80%,
          100% {
            opacity: 0.24;
            transform: translateY(0);
          }
          40% {
            opacity: 1;
            transform: translateY(-1px);
          }
        }
  
        @media (prefers-reduced-motion: reduce) {
          .playground-onboarding-modal.is-welcome .playground-onboarding-pane.is-config.is-welcome,
          .playground-onboarding-modal.is-welcome .playground-onboarding-welcome-prompt-stage .embedded-runner-input,
          .playground-onboarding-modal.is-welcome .playground-onboarding-welcome-input-guide,
          .playground-onboarding-modal.is-onboarding-transition-leaving .playground-onboarding-pane,
          .playground-onboarding-modal.is-onboarding-transition-entering .playground-onboarding-pane,
          .playground-onboarding-modal.is-onboarding-pane-transition-leaving .playground-onboarding-pane,
          .playground-onboarding-modal.is-onboarding-pane-transition-entering .playground-onboarding-pane,
          .playground-onboarding-modal.is-onboarding-free-exit-active .playground-onboarding-pane,
          .playground-onboarding-modal.is-onboarding-free-exit-fading .playground-onboarding-video-bg,
          .playground-onboarding-transition-label {
            opacity: 1;
            transform: none;
            transition: none;
            animation: none;
          }
        }
  
        @media (max-width: 1040px) {
          .playground-onboarding-welcome-input-guide-copy {
            display: none;
          }
        }
  
        .playground-onboarding-computer-card {
          display: flex;
          flex-direction: column;
          gap: 16px;
          padding: 20px;
          border-radius: 14px;
          border: 1px solid rgba(255, 255, 255, 0.10);
          background: rgba(255, 255, 255, 0.10);
          overflow: hidden;
        }
  
        .playground-onboarding-computer-section-title {
          color: rgba(255, 255, 255, 0.96);
          font-size: 14px;
          font-weight: 500;
          line-height: 1.35;
        }
  
        .playground-onboarding-computer-upload-zone {
          min-height: 168px;
          border: 1px dashed rgba(255, 255, 255, 0.20);
          border-radius: 10px;
          background: rgba(0, 0, 0, 0.14);
          overflow: hidden;
        }
  
        .playground-onboarding-computer-upload-zone.is-dragging {
          border-color: rgba(255, 255, 255, 0.46);
          background: rgba(255, 255, 255, 0.08);
        }
  
        .playground-onboarding-computer-upload-zone.is-busy {
          opacity: 0.76;
        }
  
        .playground-onboarding-computer-upload-zone.is-filled {
          padding: 12px 14px 14px;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
        }
  
        .playground-onboarding-computer-upload-button {
          width: 100%;
          min-height: 168px;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          gap: 6px;
          padding: 20px 16px;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.92);
          cursor: pointer;
        }
  
        .playground-onboarding-computer-upload-button:disabled {
          cursor: default;
        }
  
        .playground-onboarding-computer-upload-icon {
          width: 18px;
          height: 18px;
          color: rgba(255, 255, 255, 0.84);
        }
  
        .playground-onboarding-computer-upload-title {
          color: rgba(255, 255, 255, 0.96);
          font-size: 12px;
          font-weight: 600;
          line-height: 1.35;
        }
  
        .playground-onboarding-computer-upload-copy {
          color: rgba(255, 255, 255, 0.58);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.35;
        }
  
        .playground-onboarding-computer-upload-zone.is-filled .playground-tasks-attachments-topline {
          justify-content: center;
          text-align: center;
          width: 100%;
        }
  
        .playground-onboarding-computer-upload-attachments-scope.tb-runner-chat {
          width: auto;
          height: auto;
          min-height: 0;
          flex: 0 0 auto;
          display: block;
          overflow: visible;
          background: transparent;
        }
  
        .playground-onboarding-computer-upload-zone.is-filled .playground-onboarding-computer-upload-attachments-scope .runner-attachments {
          width: auto;
          max-width: 100%;
          display: inline-flex;
          justify-content: center;
          flex-wrap: wrap;
          padding: 0;
          margin: 0 auto;
        }
  
        .playground-onboarding-computer-upload-list {
          width: 100%;
          max-width: 420px;
          display: flex;
          flex-direction: column;
          gap: 6px;
          margin-top: 12px;
        }
  
        .playground-onboarding-computer-upload-file {
          min-width: 0;
          display: flex;
          align-items: center;
          justify-content: center;
          gap: 8px;
          color: rgba(255, 255, 255, 0.68);
          font-size: 11px;
          font-weight: 400;
          line-height: 1.35;
        }
  
        .playground-onboarding-computer-upload-error {
          margin-top: 12px;
          color: rgb(252, 165, 165);
          font-size: 11px;
          font-weight: 400;
          line-height: 1.4;
        }
  
        .playground-onboarding-computer-facts {
          display: flex;
          flex-direction: column;
          gap: 0;
        }
  
        .playground-onboarding-computer-fact {
          min-height: 34px;
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 18px;
        }
  
        .playground-onboarding-computer-fact-label {
          color: rgba(255, 255, 255, 0.62);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.35;
        }
  
        .playground-onboarding-computer-fact-value {
          min-width: 0;
          color: rgba(255, 255, 255, 0.92);
          font-size: 12px;
          font-weight: 500;
          line-height: 1.35;
          text-align: right;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-onboarding-computer-select {
          width: auto;
          min-height: 0;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.92);
          font-size: 12px;
          font-weight: 500;
          color-scheme: dark;
        }
  
        .playground-onboarding-plan-price {
          font-size: 34px;
        }
  
        .playground-onboarding-plan-action-row {
          justify-content: flex-start;
          gap: 10px;
          margin-top: 14px;
        }
  
        .playground-onboarding-plan-title-cta {
          align-self: stretch;
          border-radius: 15px;
        }
  
        @media (max-width: 920px) {
          .playground-onboarding-modal {
            grid-template-columns: 1fr;
            overflow: auto;
          }
  
          .playground-onboarding-pane {
            min-height: 50vh;
          }
  
          .playground-onboarding-pane.is-explain {
            border-left: 0;
            border-top: 1px solid rgba(255, 255, 255, 0.15);
          }
  
          .playground-onboarding-option-grid,
          .playground-onboarding-form-grid {
            grid-template-columns: 1fr;
          }
        }
  
        .profile-editor-modal {
          width: min(460px, 100%);
          max-height: min(84vh, 760px);
          display: flex;
          flex-direction: column;
          padding: 18px 18px 20px;
          border-radius: 22px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: #222222;
          box-shadow: 0 24px 80px rgba(0, 0, 0, 0.5);
        }
  
        .profile-editor-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          margin-bottom: 12px;
        }
  
        .profile-editor-title {
          font-size: 16px;
          font-weight: 600;
          color: #fff;
        }
  
        .profile-editor-close {
          width: 32px;
          height: 32px;
          padding: 0;
          border: 0;
          border-radius: 10px;
          background: transparent;
          color: rgba(255, 255, 255, 0.68);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
        }
  
        .profile-editor-close:hover {
          background: rgba(255, 255, 255, 0.08);
          color: #fff;
        }
  
        .profile-editor-avatar-wrap {
          display: flex;
          justify-content: center;
          margin: 10px 0 16px;
        }
  
        .profile-editor-avatar {
          position: relative;
          display: block;
          width: 136px;
          height: 136px;
          overflow: visible;
          flex-shrink: 0;
        }
  
        .profile-editor-avatar-surface {
          position: relative;
          width: 100%;
          height: 100%;
          overflow: hidden;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.2);
          color: #fff;
          display: flex;
          align-items: center;
          justify-content: center;
          font-size: 44px;
          font-weight: 500;
        }
  
        .profile-editor-avatar-fallback {
          width: 100%;
          height: 100%;
          display: flex;
          align-items: center;
          justify-content: center;
          font-size: inherit;
          font-weight: inherit;
          line-height: 1;
          letter-spacing: 0.01em;
          text-align: center;
          user-select: none;
        }
  
        .profile-editor-avatar-trigger {
          position: absolute;
          right: -4px;
          bottom: -4px;
          width: 32px;
          height: 32px;
          padding: 0;
          border: 1px solid rgba(255, 255, 255, 0.16);
          border-radius: 999px;
          background: #2b2b2b;
          color: rgba(255, 255, 255, 0.88);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          z-index: 2;
        }
  
        .profile-editor-avatar-remove {
          position: absolute;
          left: -4px;
          bottom: -4px;
          width: 32px;
          height: 32px;
          padding: 0;
          border: 1px solid rgba(255, 255, 255, 0.16);
          border-radius: 999px;
          background: #2b2b2b;
          color: rgba(255, 255, 255, 0.88);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          z-index: 2;
        }
  
        .profile-editor-avatar-trigger:hover,
        .profile-editor-avatar-remove:hover {
          background: #323232;
        }
  
        .profile-editor-file-input {
          display: none;
        }
  
        .profile-editor-fields {
          display: flex;
          flex-direction: column;
          gap: 12px;
        }
  
        .profile-editor-fields .field input {
          height: 44px;
          border-radius: 10px;
          padding: 0 14px;
          font-size: 13px;
        }
  
        .profile-editor-readonly {
          display: flex;
          align-items: center;
          min-height: 44px;
          padding: 0 14px;
          border-radius: 10px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.86);
          font-size: 13px;
        }
  
        .profile-editor-hint {
          margin-top: 2px;
          font-size: 12px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.56);
          text-align: center;
        }
  
        .profile-editor-error {
          margin-top: 12px;
          font-size: 12px;
          line-height: 1.45;
          color: #ff9c9c;
        }
  
        .profile-editor-actions {
          display: flex;
          justify-content: flex-end;
          gap: 10px;
          margin-top: 18px;
        }
  
        .profile-editor-button {
          min-width: 104px;
          min-height: 38px;
          padding: 0 16px;
          border-radius: 10px;
          font-size: 12px;
          font-weight: 400;
          cursor: pointer;
          transition: opacity 160ms ease;
        }
  
        .profile-editor-button:disabled {
          opacity: 0.62;
          cursor: default;
        }
  
        .profile-editor-button.is-secondary {
          border: 1px solid rgba(255, 255, 255, 0.12);
          background: rgba(255, 255, 255, 0.06);
          color: rgba(255, 255, 255, 0.88);
        }
  
        .profile-editor-button.is-primary {
          border: 0;
          background: #f4f4f4;
          color: #111;
        }
  
        .playground-settings-page {
          height: 100%;
          min-height: 0;
          display: block;
          padding: 42px 50px 56px;
          background: #000;
          overflow: auto;
          box-sizing: border-box;
          scrollbar-width: none;
        }
  
        .playground-settings-page::-webkit-scrollbar {
          display: none;
        }
  
        .playground-settings-shell {
          height: 100%;
          min-height: 0;
          display: grid;
          grid-template-columns: 270px minmax(0, 1fr);
          gap: 0;
          align-items: stretch;
        }
  
        .playground-settings-list-pane {
          background: rgba(16, 16, 16, 0.86);
          backdrop-filter: none;
          -webkit-backdrop-filter: none;
        }
  
        .playground-settings-page .playground-environments-list-pane {
          border-right: 0;
        }
  
        .playground-settings-page .playground-environments-list-header {
          padding: 11px 12px 8px;
        }
  
        .playground-settings-page .playground-files-topbar {
          min-height: 30px;
        }
  
        .playground-settings-page .playground-environments-list-title {
          display: flex;
          align-items: center;
          min-height: 30px;
          font-size: 14px;
          font-weight: 500;
          line-height: 1;
          letter-spacing: -0.01em;
        }
  
        .playground-settings-title-row {
          display: flex;
          align-items: center;
          gap: 5px;
          min-height: 30px;
        }
  
        .playground-settings-back-button {
          width: 28px;
          height: 28px;
          padding: 0;
          border: 0;
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.72);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          transition: background-color 160ms ease, color 160ms ease;
        }
  
        .playground-settings-back-button:hover,
        .playground-settings-back-button:focus-visible {
          background: rgba(255, 255, 255, 0.08);
          color: #fff;
          outline: none;
        }
  
        .playground-settings-back-icon {
          width: 15px;
          height: 15px;
        }
  
        .playground-settings-list-body {
          display: flex;
          flex-direction: column;
          gap: 14px;
          padding: 8px 12px 18px;
        }
  
        .playground-settings-nav-group {
          display: flex;
          flex-direction: column;
          gap: 4px;
        }
  
        .playground-settings-nav-label {
          padding: 0 10px 4px;
          font-size: 12px;
          font-weight: 400;
          letter-spacing: 0;
          color: rgba(255, 255, 255, 1);
        }
  
        .playground-settings-nav-group + .playground-settings-nav-group .playground-settings-nav-label {
          margin-top: 10px;
        }
  
        .playground-settings-nav-item {
          min-width: 0;
        }
  
        .playground-settings-page .playground-environments-list-item {
          gap: 8px;
          min-height: 36px;
          padding: 8px 10px;
          border-radius: 10px;
          color: rgba(255, 255, 255, 0.7);
        }
  
        .playground-settings-page .playground-environments-list-item-icon {
          width: 14px;
          height: 14px;
        }
  
        .playground-settings-page .playground-environments-list-item-copy {
          gap: 0;
        }
  
        .playground-settings-page .playground-environments-list-item-title {
          font-size: 12px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.7);
        }
  
        .playground-settings-page .playground-environments-list-item-meta {
          display: none;
        }
  
        .playground-settings-page .playground-environments-list-item.is-active {
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-settings-page .playground-environments-list-item.is-active .playground-environments-list-item-title,
        .playground-settings-page .playground-environments-list-item.is-active .playground-environments-list-item-icon {
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-settings-nav-item.is-danger:not(.is-active) {
          border-color: rgba(255, 107, 107, 0.1);
        }
  
        .playground-settings-nav-item.is-danger:not(.is-active):hover {
          background: rgba(255, 107, 107, 0.08);
        }
  
        .playground-settings-nav-item.is-danger:not(.is-active) .playground-environments-list-item-icon,
        .playground-settings-nav-item.is-danger:not(.is-active) .playground-environments-list-item-title,
        .playground-settings-nav-item.is-danger:not(.is-active) .playground-environments-list-item-meta {
          color: rgba(255, 142, 142, 0.92);
        }
  
        .playground-settings-detail-scroll {
          gap: 38px;
        }
  
  	      .playground-settings-overview-header {
  	        display: flex;
  	        flex-direction: column;
  	        gap: 0;
  	      }
  
        .playground-settings-overview-title.playground-environments-home-hero-title {
          margin-top: 0;
          margin-bottom: 0;
        }
  
  	      .playground-settings-overview-tabs.playground-agents-overview-tabs {
  	        margin: 16px 0 0;
  	      }
  
        .playground-settings-page .playground-settings-detail-scroll {
          width: min(100%, var(--playground-centered-page-max-width));
          max-width: var(--playground-centered-page-max-width);
          min-height: auto;
          flex: 0 0 auto;
          padding: 0;
          margin: 0 auto;
          overflow: visible;
          align-items: stretch;
        }
  
        .playground-settings-page .playground-settings-detail-scroll > * {
          width: 100%;
          max-width: none;
          margin-left: 0;
          margin-right: 0;
        }
  
        .playground-settings-page.is-embedded {
          min-height: auto;
          height: auto;
          padding: 0;
          overflow: visible;
        }
  
        .playground-settings-page.is-embedded .playground-settings-detail-scroll {
          width: 100%;
          max-width: none;
          gap: 0;
          margin: 0;
        }
  
        .playground-settings-detail-scroll.is-usage {
          scrollbar-width: none;
        }
  
        .playground-settings-detail-scroll.is-usage::-webkit-scrollbar {
          display: none;
        }
  
        .playground-settings-detail-stack {
          display: flex;
          flex-direction: column;
          gap: 16px;
        }
  
        .playground-settings-note {
          display: flex;
          flex-direction: column;
          gap: 10px;
          padding: 14px 16px;
          border-radius: 16px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.03);
          color: rgba(255, 255, 255, 0.72);
          font-size: 12px;
          line-height: 1.6;
        }
  
        .playground-settings-note.is-danger {
          border-color: rgba(255, 107, 107, 0.2);
          background: rgba(255, 107, 107, 0.08);
          color: rgba(255, 196, 196, 0.94);
        }
  
        .playground-settings-note-title {
          font-size: 13px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-settings-note.is-danger .playground-settings-note-title {
          color: #fff3f3;
        }
  
        .playground-settings-actions {
          display: flex;
          flex-wrap: wrap;
          gap: 10px;
        }
  
        .playground-settings-profile-card {
          display: flex;
          align-items: center;
          gap: 14px;
        }
  
        .playground-settings-profile-avatar {
          width: 52px;
          height: 52px;
          border-radius: 16px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          background: rgba(255, 255, 255, 0.06);
          color: rgba(255, 255, 255, 0.96);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          overflow: hidden;
          font-size: 16px;
          font-weight: 700;
          flex-shrink: 0;
        }
  
        .playground-settings-profile-avatar img {
          width: 100%;
          height: 100%;
          object-fit: cover;
        }
  
        .playground-settings-profile-copy {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 4px;
        }
  
        .playground-settings-profile-name {
          font-size: 16px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-settings-profile-meta {
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.58);
        }
  
        .playground-settings-connection-grid {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 14px;
        }
  
        .playground-settings-connection-card {
          display: flex;
          flex-direction: column;
          gap: 14px;
          padding: 16px;
          border-radius: 18px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.03);
        }
  
        .playground-settings-connection-header {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-settings-connection-title {
          font-size: 14px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-settings-connection-copy {
          margin-top: 4px;
          font-size: 12px;
          line-height: 1.55;
          color: rgba(255, 255, 255, 0.58);
        }
  
        .playground-settings-connection-status {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          padding: 5px 9px;
          border-radius: 999px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.03);
          color: rgba(255, 255, 255, 0.68);
          font-size: 11px;
          font-weight: 600;
          white-space: nowrap;
        }
  
        .playground-settings-connection-status.is-connected {
          border-color: rgba(125, 230, 156, 0.2);
          background: rgba(52, 214, 122, 0.14);
          color: #b7f7c9;
        }
  
        .playground-settings-connection-meta {
          display: flex;
          flex-direction: column;
          gap: 6px;
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.72);
        }
  
        .playground-settings-connection-meta-row {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-settings-connection-meta-row span:first-child {
          color: rgba(255, 255, 255, 0.48);
        }
  
        .playground-settings-connection-actions {
          display: flex;
          flex-wrap: wrap;
          gap: 10px;
        }
  
        .playground-settings-integrations-shell {
          width: 100%;
        }
  
        .playground-plugins-page {
          width: min(100%, var(--playground-centered-page-max-width));
          max-width: var(--playground-centered-page-max-width);
          margin: 0 auto;
          display: flex;
          flex-direction: column;
          gap: 20px;
          padding: 0 0 26px;
        }
  
        .playground-plugins-detail {
          height: 100%;
          min-height: 0;
        }
  
        .playground-plugins-detail > .playground-environments-detail-scroll.playground-settings-detail-scroll {
          padding: 42px 44px 56px;
        }
  
        .playground-plugins-section {
          display: flex;
          flex-direction: column;
          gap: 14px;
        }
  
        .playground-plugins-section-header {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 16px;
          margin-top: 24px;
          padding-bottom: 0 !important;
          border-bottom: 0;
        }
  
        .playground-plugins-page .playground-plugins-section > .playground-plugins-section-header {
          padding-bottom: 0;
          border-bottom: 0;
        }
  
        .playground-plugins-section-copy {
          display: flex;
          flex-direction: column;
          gap: 4px;
        }
  
        .playground-plugins-section-title {
          margin: 0;
          font-size: 16px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-overview-new-button {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          padding: 0;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.9);
          font-size: 13px;
          font-weight: 500;
          cursor: pointer;
          transition: color 160ms ease, opacity 160ms ease;
        }
  
        .playground-overview-new-button:hover {
          color: #fff;
        }
  
        .playground-overview-new-button svg {
          flex: 0 0 auto;
        }
  
        .playground-plugins-section-subtitle {
          margin: 0;
          font-size: 12px;
          line-height: 1.6;
          color: rgba(255, 255, 255, 0.48);
        }
  
        .playground-plugins-title {
          margin: 0;
          font-size: 24px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-plugins-subtitle {
          margin: 0;
          font-size: 13px;
          line-height: 1.6;
          color: rgba(255, 255, 255, 0.52);
        }
  
        .playground-plugins-search {
          width: 100%;
          min-height: 36px;
          padding: 0 14px 0 36px;
          border-radius: 999px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.96);
          font-size: 13px;
          outline: none;
        }
  
        .playground-plugins-search::placeholder {
          color: rgba(255, 255, 255, 0.34);
        }
  
        .playground-plugins-search-shell {
          position: relative;
          flex: 0 1 360px;
          width: min(360px, 100%);
          min-width: min(360px, 100%);
          max-width: 360px;
        }
  
        .playground-project-overview-threads-section .playground-plugins-search,
        .playground-project-overview-threads-section .playground-plugins-search-shell {
          background: transparent;
        }
  
        .playground-plugins-search-icon {
          position: absolute;
          left: 12px;
          top: 50%;
          transform: translateY(-50%);
          color: rgba(255, 255, 255, 0.42);
          pointer-events: none;
        }
  
        .playground-plugins-filter {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          min-height: 38px;
          padding: 0 14px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.03);
          color: rgba(255, 255, 255, 0.82);
          font-size: 12px;
          font-weight: 400;
          cursor: pointer;
        }
  
        .playground-plugins-filter-shell {
          position: relative;
        }
  
        .playground-plugins-grid {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 10px 16px;
          align-items: start;
        }
  
        .playground-plugin-row {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 14px;
          width: 100%;
          padding: 12px 14px;
          border: 0;
          border-radius: 12px;
          background: transparent;
          color: inherit;
          cursor: pointer;
          text-align: left;
          transition: background-color 160ms ease, opacity 160ms ease;
        }
  
        .playground-plugin-row:hover {
          background: rgba(255, 255, 255, 0.04);
        }
  
        .playground-plugins-grid > .playground-plugin-row:nth-child(odd) {
          padding-left: 0;
        }
  
        .playground-plugin-row-copy {
          min-width: 0;
          display: flex;
          align-items: center;
          gap: 12px;
        }
  
        .playground-plugin-row-logo-shell {
          position: relative;
          overflow: hidden;
          width: 34px;
          height: 34px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border-radius: 10px;
          background: rgba(255, 255, 255, 0.05);
          border: 0;
          flex: 0 0 auto;
          backdrop-filter: blur(50px);
          -webkit-backdrop-filter: blur(50px);
        }
  
        .playground-plugin-row-logo-shell::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          border-radius: 10px;
          padding: 1px;
          background: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-plugin-row-logo {
          width: 20px;
          height: 20px;
          object-fit: contain;
          display: block;
        }
  
        .playground-plugin-row-logo.is-github {
          filter: brightness(0) invert(1);
        }
  
        .playground-agent-list-avatar {
          width: 100%;
          height: 100%;
          border-radius: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.88);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          overflow: hidden;
          flex: 0 0 auto;
        }
  
        .playground-agent-list-avatar-image {
          width: 100%;
          height: 100%;
          object-fit: cover;
          display: block;
        }
  
        .playground-agent-list-avatar-fallback {
          font-size: 10px;
          font-weight: 500;
          line-height: 1;
          letter-spacing: 0.02em;
          text-transform: uppercase;
          color: rgba(255, 255, 255, 0.7);
        }
  
        .playground-plugin-row-fallback-logo {
          font-size: 11px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.86);
        }
  
        .playground-plugin-row-text {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 2px;
        }
  
        .playground-plugin-row-title {
          font-size: 12px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-plugin-row-description {
          font-size: 12px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.48);
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-plugin-row-state {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          width: 22px;
          height: 22px;
          flex: 0 0 auto;
          border-radius: 999px;
          color: rgba(255, 255, 255, 0.94);
          background: transparent;
        }
  
        .playground-plugin-row-state.is-connected {
          color: rgba(255, 255, 255, 0.96);
          background: transparent;
        }
  
        .playground-plugins-empty {
          padding: 28px 0 0;
          font-size: 13px;
          color: rgba(255, 255, 255, 0.42);
        }
  
        .playground-resources-overview-loading {
          min-height: 160px;
          display: flex;
          align-items: center;
          justify-content: center;
          gap: 8px;
        }
  
        .playground-resources-overview-loading .playground-files-state-loader {
          width: 16px;
          height: 16px;
        }
  
        .playground-plugin-modal-scrim {
          position: fixed;
          inset: 0;
          z-index: 400;
          display: flex;
          align-items: center;
          justify-content: center;
          padding: 24px;
          background: rgba(0, 0, 0, 0.58);
          backdrop-filter: blur(10px);
        }
  
        .playground-plugin-modal {
          width: min(640px, calc(100vw - 32px));
          max-height: min(86vh, 760px);
          overflow: auto;
          padding: 18px 18px 20px;
          border-radius: 18px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: #1a1a1a;
          box-shadow: 0 24px 80px rgba(0, 0, 0, 0.55);
        }
  
        .playground-plugin-modal-header {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 16px;
          padding-bottom: 14px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-plugin-modal-header-copy {
          min-width: 0;
          display: flex;
          align-items: flex-start;
          gap: 14px;
        }
  
        .playground-plugin-modal-title {
          margin: 0;
          font-size: 18px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-plugin-modal-subtitle {
          margin: 6px 0 0;
          font-size: 12px;
          line-height: 1.6;
          color: rgba(255, 255, 255, 0.52);
        }
  
        .playground-plugin-modal-body {
          display: flex;
          flex-direction: column;
          gap: 18px;
          padding-top: 18px;
        }
  
        .playground-plugin-modal-meta {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 10px 16px;
        }
  
        .playground-plugin-modal-meta-item {
          display: flex;
          flex-direction: column;
          gap: 4px;
        }
  
        .playground-plugin-modal-meta-label {
          font-size: 11px;
          color: rgba(255, 255, 255, 0.42);
        }
  
        .playground-plugin-modal-meta-value {
          font-size: 13px;
          color: rgba(255, 255, 255, 0.92);
        }
  
        .playground-plugin-modal-section {
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-plugin-modal-section-title {
          margin: 0;
          font-size: 13px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-plugin-modal-list {
          margin: 0;
          padding: 0;
          list-style: none;
          display: flex;
          flex-direction: column;
          gap: 6px;
          font-size: 12px;
          line-height: 1.55;
          color: rgba(255, 255, 255, 0.7);
        }
  
        .playground-plugin-modal-list li::before {
          content: "• ";
          color: rgba(255, 255, 255, 0.92);
        }
  
        .playground-plugin-modal-actions {
          display: flex;
          flex-wrap: wrap;
          gap: 10px;
        }
  
        .playground-plugin-modal-button {
          min-height: 36px;
          padding: 0 16px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.05);
          color: rgba(255, 255, 255, 0.92);
          font-size: 13px;
          font-weight: 500;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 8px;
          cursor: pointer;
        }
  
        .playground-plugin-modal-button.is-primary {
          border-color: transparent;
          background: #fff;
          color: #111;
        }
  
        .playground-plugin-modal-button.is-destructive {
          background: rgba(239, 68, 68, 0.10);
          color: rgba(248, 113, 113, 0.98);
        }
  
        .playground-plugin-modal-button:disabled {
          opacity: 0.58;
          cursor: default;
        }
  
        @media (max-width: 920px) {
          .playground-plugins-grid {
            grid-template-columns: 1fr;
          }
  
          .playground-plugin-modal-meta {
            grid-template-columns: 1fr;
          }
        }
  
        .playground-settings-integrations-title {
          margin: 0;
          font-size: 18px;
          font-weight: 500;
          color: rgba(255, 255, 255, 1);
        }
  
        .playground-settings-integrations-subtitle {
          margin: 2px 0 24px;
          padding-bottom: 12px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
          font-size: 12px;
          color: rgba(255, 255, 255, 0.44);
        }
  
        .playground-settings-integration-stack {
          display: flex;
          flex-direction: column;
          gap: 16px;
        }
  
        .playground-settings-integration-card {
          position: relative;
          padding: 16px;
          border-radius: 15px;
          background: rgba(255, 255, 255, 0.025);
        }
  
        .playground-settings-integration-card::before {
          content: "";
          position: absolute;
          inset: 0;
          border-radius: 15px;
          padding: 1px;
          background: linear-gradient(-10deg, rgba(200, 200, 200, 0.15), rgba(255, 255, 255, 0.1), rgba(255, 255, 255, 0.07), rgba(255, 255, 255, 0.3));
          -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
          -webkit-mask-composite: xor;
          mask-composite: exclude;
          pointer-events: none;
        }
  
        .playground-settings-integration-card-row {
          display: flex;
          align-items: flex-start;
          gap: 16px;
        }
  
        .playground-settings-integration-icon {
          width: 36px;
          height: 36px;
          display: flex;
          align-items: center;
          justify-content: center;
          border-radius: 10px;
          flex: 0 0 auto;
          color: rgba(255, 255, 255, 1);
        }
  
        .playground-settings-integration-icon.is-github {
          background: rgba(255, 255, 255, 0.08);
        }
  
        .playground-settings-integration-icon.is-notion {
          background: rgba(255, 255, 255, 0.08);
        }
  
        .playground-settings-integration-icon.is-google-drive {
          background: rgba(37, 99, 235, 0.24);
        }
  
        .playground-settings-integration-icon.is-one-drive {
          background: rgba(14, 165, 233, 0.22);
        }
  
        .playground-settings-integration-icon.is-discord {
          border: 1px solid #646ff3;
          box-sizing: border-box;
          background: linear-gradient(to top, #434cb6, #5965f2);
        }
  
        .playground-settings-integration-icon.is-telegram {
          border: 1px solid #0099cc;
          box-sizing: border-box;
          background: linear-gradient(to top, #036699, #0088cc);
        }
  
        .playground-settings-integration-main {
          min-width: 0;
          flex: 1;
        }
  
        .playground-settings-integration-top {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-settings-integration-title {
          margin: 0;
          font-size: 14px;
          font-weight: 500;
          color: rgba(255, 255, 255, 1);
        }
  
        .playground-settings-integration-account-group {
          display: flex;
          align-items: center;
          gap: 12px;
          flex-wrap: wrap;
          justify-content: flex-end;
        }
  
        .playground-settings-integration-account {
          display: inline-flex;
          align-items: center;
          gap: 6px;
          padding: 0 8px;
          font-size: 12px;
          color: rgba(255, 255, 255, 0.92);
        }
  
        .playground-settings-integration-account-mark {
          color: rgb(96, 165, 250);
        }
  
        .playground-settings-integration-unlink {
          display: inline-flex;
          align-items: center;
          gap: 6px;
          border: 0;
          padding: 0;
          background: transparent;
          font-size: 12px;
          color: rgba(156, 163, 175, 0.95);
          cursor: pointer;
          transition: color 160ms ease, opacity 160ms ease;
        }
  
        .playground-settings-integration-unlink:hover {
          color: rgba(255, 255, 255, 1);
        }
  
        .playground-settings-integration-copy {
          margin: 4px 0 0;
          font-size: 12px;
          line-height: 1.55;
          color: rgba(156, 163, 175, 0.95);
        }
  
        .playground-settings-integration-loading {
          display: flex;
          align-items: center;
          justify-content: center;
          padding: 16px 0;
        }
  
        .playground-settings-integration-body {
          margin-top: 14px;
          padding-top: 12px;
          border-top: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-settings-integration-section-label {
          margin: 0 0 6px;
          font-size: 12px;
          color: rgba(209, 213, 219, 0.95);
        }
  
        .playground-settings-integration-list {
          margin: 0 0 12px;
          padding: 0;
          list-style: none;
          display: flex;
          flex-direction: column;
          gap: 4px;
          font-size: 12px;
          font-weight: 500;
          color: rgba(255, 255, 255, 1);
        }
  
        .playground-settings-integration-list li::before {
          content: "• ";
          color: rgba(255, 255, 255, 0.88);
        }
  
        .playground-settings-integration-muted {
          font-size: 12px;
          color: rgba(107, 114, 128, 0.95);
        }
  
        .playground-settings-integration-actions {
          display: flex;
          flex-wrap: wrap;
          gap: 10px;
        }
  
        .playground-settings-integration-button {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          min-height: 36px;
          padding: 0 16px;
          border: 0;
          border-radius: 10px;
          font-size: 14px;
          font-weight: 500;
          color: rgba(255, 255, 255, 1);
          cursor: pointer;
          transition: opacity 160ms ease, background-color 160ms ease, border-color 160ms ease, color 160ms ease;
        }
  
        .playground-settings-integration-button.is-github {
          background: rgb(36, 41, 47);
        }
  
        .playground-settings-integration-button.is-notion {
          background: rgb(24, 24, 27);
        }
  
        .playground-settings-integration-button.is-google-drive {
          background: rgb(26, 115, 232);
        }
  
        .playground-settings-integration-button.is-one-drive {
          background: rgb(0, 120, 212);
        }
  
        .playground-settings-integration-button.is-secondary {
          border: 1px solid rgba(255, 255, 255, 0.1);
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-settings-integration-button:hover {
          opacity: 0.92;
        }
  
        .playground-settings-integration-helper {
          margin: 10px 0 0;
          font-size: 12px;
          color: rgba(107, 114, 128, 0.95);
        }
  
        .playground-settings-integration-inline-code {
          color: rgb(96, 165, 250);
        }
  
        .playground-settings-integration-logo {
          width: 22px;
          height: 22px;
          object-fit: contain;
        }
  
        .playground-settings-integration-logo.is-email {
          width: 36px;
          height: 36px;
        }
  
        .playground-settings-integration-logo.is-discord {
          width: 22px;
          height: 22px;
        }
  
        .playground-settings-integration-logo.is-telegram {
          position: relative;
          left: -1px;
          width: 18px;
          height: 18px;
        }
  
        .playground-settings-integration-message {
          display: flex;
          align-items: center;
          gap: 8px;
          margin-top: 16px;
          font-size: 12px;
        }
  
        .playground-settings-integration-message.is-error {
          color: rgb(248, 113, 113);
        }
  
        .playground-settings-integration-message.is-success {
          color: rgb(74, 222, 128);
        }
  
        .playground-settings-integration-notice {
          padding: 12px;
          border-radius: 10px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          background: rgba(255, 255, 255, 0.04);
          font-size: 12px;
          line-height: 1.55;
        }
  
        .playground-settings-integration-notice.is-warning {
          border-color: rgba(234, 179, 8, 0.3);
          background: rgba(234, 179, 8, 0.1);
          color: rgb(250, 204, 21);
        }
  
        .playground-settings-integration-form-row {
          display: flex;
          gap: 8px;
          flex-wrap: wrap;
        }
  
        .playground-settings-integration-input {
          min-height: 40px;
          padding: 0 14px;
          border-radius: 10px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          background: rgba(255, 255, 255, 0.05);
          color: rgba(255, 255, 255, 1);
          font-size: 14px;
          outline: none;
        }
  
        .playground-settings-integration-input::placeholder {
          color: rgba(107, 114, 128, 0.95);
        }
  
        .playground-settings-integration-input:focus {
          border-color: rgba(255, 255, 255, 0.3);
        }
  
        .playground-settings-integration-input.is-email {
          flex: 1 1 220px;
          max-width: 250px;
        }
  
        .playground-settings-integration-input.is-code {
          flex: 1 1 140px;
          max-width: 150px;
          text-align: center;
          letter-spacing: 0.22em;
        }
  
        .playground-settings-integration-button.is-email {
          background: rgb(59, 130, 246);
        }
  
        .playground-settings-integration-button.is-discord {
          background: rgb(88, 101, 242);
        }
  
        .playground-settings-integration-button.is-telegram {
          background: rgb(0, 136, 204);
        }
  
        .playground-settings-integration-email-address {
          color: rgb(96, 165, 250);
        }
  
        .playground-settings-integration-secondary-link {
          display: inline-flex;
          align-items: center;
          border: 0;
          padding: 0;
          background: transparent;
          color: rgba(156, 163, 175, 0.95);
          font-size: 12px;
          cursor: pointer;
          transition: color 160ms ease;
        }
  
        .playground-settings-integration-secondary-link:hover {
          color: rgba(255, 255, 255, 1);
        }
  
        .playground-settings-integration-steps {
          margin: 0;
          padding-left: 16px;
          display: flex;
          flex-direction: column;
          gap: 4px;
          font-size: 12px;
          color: rgba(156, 163, 175, 0.95);
        }
  
        .playground-settings-integration-steps code {
          color: rgb(96, 165, 250);
        }
  
        .playground-settings-banner {
          display: flex;
          align-items: flex-start;
          gap: 10px;
          padding: 12px 14px;
          border-radius: 14px;
          font-size: 12px;
          line-height: 1.55;
        }
  
        .playground-settings-banner.is-error {
          border: 1px solid rgba(255, 107, 107, 0.24);
          background: rgba(255, 107, 107, 0.08);
          color: #ffc2c2;
        }
  
        .playground-settings-banner.is-success {
          border: 1px solid rgba(97, 231, 143, 0.24);
          background: rgba(61, 193, 107, 0.08);
          color: #c8f6d4;
        }
  
        .playground-settings-banner-icon {
          width: 14px;
          height: 14px;
          flex-shrink: 0;
          margin-top: 2px;
        }
  
        .playground-settings-banner-copy {
          min-width: 0;
        }
  
        .playground-settings-card {
          display: flex;
          flex-direction: column;
          gap: 14px;
          padding: 16px;
          border-radius: 18px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.03);
        }
  
        .playground-settings-card-header,
        .playground-settings-card-footer {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-settings-card-title {
          font-size: 14px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-settings-card-copy,
        .playground-settings-muted-copy {
          font-size: 12px;
          line-height: 1.55;
          color: rgba(255, 255, 255, 0.56);
        }
  
        .playground-settings-card-stack,
        .playground-settings-listing {
          display: flex;
          flex-direction: column;
          gap: 12px;
        }
  
        .playground-settings-emphasis {
          font-size: 14px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-settings-inline-row {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-settings-inline-row-main,
        .playground-settings-inline-actions {
          display: flex;
          align-items: flex-start;
          gap: 12px;
        }
  
        .playground-settings-section-label {
          font-size: 11px;
          font-weight: 600;
          letter-spacing: 0.04em;
          text-transform: uppercase;
          color: rgba(255, 255, 255, 0.4);
        }
  
        .playground-settings-chip-row {
          display: flex;
          flex-wrap: wrap;
          justify-content: flex-end;
          gap: 8px;
        }
  
        .playground-settings-chip {
          display: inline-flex;
          align-items: center;
          min-height: 24px;
          padding: 0 10px;
          border-radius: 999px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          font-size: 11px;
          font-weight: 600;
          line-height: 1;
        }
  
        .playground-settings-chip.is-muted {
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.66);
        }
  
        .playground-settings-chip.is-primary {
          background: rgba(137, 182, 255, 0.14);
          border-color: rgba(137, 182, 255, 0.28);
          color: #c8daff;
        }
  
        .playground-settings-chip.is-success {
          background: rgba(61, 193, 107, 0.14);
          border-color: rgba(97, 231, 143, 0.24);
          color: #c7f2d4;
        }
  
        .playground-settings-chip.is-warning {
          background: rgba(255, 179, 71, 0.12);
          border-color: rgba(255, 179, 71, 0.24);
          color: #ffd9a3;
        }
  
        .playground-settings-plan-grid,
        .playground-settings-kpi-grid {
          display: grid;
          gap: 14px;
        }
  
        .playground-settings-plan-grid {
          grid-template-columns: repeat(3, minmax(0, 1fr));
        }
  
        .playground-settings-kpi-grid {
          grid-template-columns: repeat(4, minmax(0, 1fr));
        }
  
        .playground-settings-plan-card,
        .playground-settings-kpi-card {
          display: flex;
          flex-direction: column;
          gap: 12px;
          padding: 16px;
          border-radius: 18px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.03);
        }
  
        .playground-settings-plan-card.is-current {
          border-color: rgba(137, 182, 255, 0.22);
          background: rgba(137, 182, 255, 0.07);
        }
  
        .playground-settings-plan-name-row {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-settings-plan-name {
          font-size: 18px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-settings-plan-price {
          font-size: 32px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-settings-plan-price-unit {
          font-size: 12px;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.48);
        }
  
        .playground-settings-plan-description {
          font-size: 12px;
          line-height: 1.6;
          color: rgba(255, 255, 255, 0.58);
        }
  
        .playground-settings-plan-action {
          margin-top: auto;
        }
  
        .playground-settings-plan-progress {
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-settings-plan-progress-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-settings-plan-progress-title {
          font-size: 14px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.94);
        }
  
        .playground-settings-plan-progress-copy {
          font-size: 12px;
          color: rgba(255, 255, 255, 0.68);
        }
  
        .playground-settings-plan-progress-track {
          width: 100%;
          height: 4px;
          overflow: hidden;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.2);
        }
  
        .playground-settings-plan-progress-fill {
          height: 100%;
          border-radius: inherit;
          background: rgba(255, 255, 255, 1);
          transition: width 240ms ease;
        }
  
        .playground-settings-plan-progress-fill.is-warning {
          background: rgb(250, 204, 21);
        }
  
        .playground-settings-plan-progress-fill.is-danger {
          background: rgb(248, 113, 113);
        }
  
        .playground-settings-plan-action-warning {
          padding: 12px 14px;
          border-radius: 12px;
          background: rgba(239, 68, 68, 0.2);
          color: rgb(252, 165, 165);
          font-size: 12px;
          line-height: 1.6;
        }
  
        .playground-settings-plan-app-shell {
          display: flex;
          flex-direction: column;
          gap: 24px;
        }
  
        .playground-settings-plans-navbar {
          position: sticky;
          top: 0;
          z-index: 2;
          background: transparent;
          backdrop-filter: blur(20px);
          -webkit-backdrop-filter: blur(20px);
        }
  
        .playground-settings-plans-title {
          font-size: 14px;
          line-height: 1;
          font-weight: 500;
          letter-spacing: -0.01em;
          color: rgba(255, 255, 255, 0.94);
        }
  
        .playground-settings-plans-menu-anchor {
          position: relative;
        }
  
        .playground-settings-plans-overview-grid {
          display: grid;
          grid-template-columns: minmax(0, 0.88fr) minmax(0, 1.32fr);
          gap: 24px;
          align-items: stretch;
        }
  
        .playground-settings-plans-overview-section {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 14px;
        }
  
        .playground-settings-plans-overview-heading {
          margin-top: 12px;
          font-size: 14px;
          line-height: 1;
          font-weight: 500;
          letter-spacing: -0.01em;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-settings-plans-budget-card {
          display: flex;
          flex-direction: column;
          gap: 18px;
        }
  
        .playground-settings-plans-overview-section .playground-settings-plans-budget-card {
          min-height: 194px;
          border-radius: 15px;
        }
  
        .playground-settings-plans-budget-card-top {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 16px;
        }
  
        .playground-settings-plans-budget-copy {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 6px;
          padding-top: 6px;
        }
  
        .playground-settings-plans-budget-period {
          font-size: 12px;
          line-height: 1.2;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.7);
        }
  
        .playground-settings-plans-budget-value {
          display: flex;
          align-items: baseline;
          gap: 6px;
          font-size: 26px;
          line-height: 1;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.94);
        }
  
        .playground-settings-plans-budget-value-total,
        .playground-settings-plans-budget-value-separator {
          font-size: inherit;
          font-weight: inherit;
          color: inherit;
        }
  
        .playground-settings-plans-budget-reset {
          font-size: 12px;
          color: rgba(255, 255, 255, 0.52);
        }
  
        .playground-settings-plans-budget-icon-button {
          width: 40px;
          height: 40px;
          padding: 0;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 15px;
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.88);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          transition: background-color 160ms ease, color 160ms ease;
        }
  
        .playground-settings-plans-budget-icon-button:hover {
          background: rgba(255, 255, 255, 0.08);
          color: #fff;
        }
  
        .playground-settings-plans-budget-icon-button.is-plain {
          width: 32px;
          height: 32px;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
        }
  
        .playground-settings-plans-budget-icon-button.is-plain:hover {
          background: transparent;
        }
  
        .playground-settings-plans-budget-actions {
          display: flex;
          align-items: center;
          gap: 8px;
          flex-wrap: wrap;
          margin-top: auto;
        }
  
        .playground-settings-plans-budget-action {
          min-height: 40px;
          padding: 0 14px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.9);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 8px;
          font-size: 12px;
          font-weight: 400;
          cursor: pointer;
          transition: background-color 160ms ease, color 160ms ease;
        }
  
        .playground-settings-plans-budget-action:hover {
          background: rgba(255, 255, 255, 0.08);
          color: #fff;
        }
  
        .playground-settings-plans-budget-action.is-primary {
          border-color: transparent;
          background: rgba(255, 255, 255, 0.94);
          color: #050505;
        }
  
        .playground-settings-plans-budget-action.is-primary:hover {
          background: #fff;
          color: #050505;
        }
  
        .playground-settings-plans-budget-action-badge {
          min-height: 22px;
          padding: 0 10px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.72);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          font-size: 11px;
          line-height: 1;
        }
  
        .playground-settings-plans-budget-breakdown {
          margin-top: 2px;
          border-top: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-settings-plans-budget-breakdown-row {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 14px;
          min-height: 56px;
          padding: 0 2px;
        }
  
        .playground-settings-plans-budget-breakdown-row + .playground-settings-plans-budget-breakdown-row {
          border-top: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-settings-plans-budget-breakdown-main {
          min-width: 0;
          display: flex;
          align-items: center;
          gap: 12px;
        }
  
        .playground-settings-plans-budget-breakdown-icon {
          width: 28px;
          height: 28px;
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.72);
        }
  
        .playground-settings-plans-budget-breakdown-title {
          min-width: 0;
          font-size: 14px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.9);
        }
  
        .playground-settings-plans-budget-breakdown-copy {
          margin-top: 2px;
          font-size: 12px;
          color: rgba(255, 255, 255, 0.44);
        }
  
        .playground-settings-plans-budget-breakdown-value {
          flex: 0 0 auto;
          font-size: 13px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.88);
        }
  
        .playground-settings-plans-budget-card .playground-settings-usage-balance-track {
          height: 12px;
          border-radius: 5px;
          background: rgba(255, 255, 255, 0.1);
        }
  
        .playground-settings-plans-budget-actions .playground-settings-plans-budget-icon-button.is-trailing {
          margin-left: auto;
        }
  
        .playground-settings-plans-budget-card .playground-settings-usage-balance-fill,
        .playground-settings-plans-budget-card .playground-settings-usage-balance-fill.is-neutral {
          background: rgba(255, 255, 255, 1);
        }
  
        .playground-settings-plans-add-more-button,
        .playground-settings-plans-add-more-button.playground-environments-action-button {
          border-radius: 999px !important;
          background: transparent;
        }
  
        .playground-settings-plans-quick-links-grid {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 12px;
        }
  
        .playground-settings-plans-quick-link {
          min-width: 0;
          min-height: 56px;
          padding: 0 16px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 15px;
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.94);
          display: flex;
          align-items: center;
          gap: 14px;
          text-align: left;
          font-size: 12px;
          font-weight: 400;
          cursor: pointer;
          transition: background-color 160ms ease, border-color 160ms ease, color 160ms ease;
        }
  
        .playground-settings-plans-quick-link:hover {
          border-color: rgba(255, 255, 255, 0.18);
          background: rgba(255, 255, 255, 0.075);
          color: #fff;
        }
  
        .playground-settings-plans-quick-link-icon {
          width: 16px;
          height: 16px;
          flex: 0 0 auto;
          color: rgba(255, 255, 255, 0.66);
        }
  
        .playground-settings-plans-quick-link-label {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-settings-plans-resource-cap-section {
          display: flex;
          flex-direction: column;
          gap: 12px;
        }
  
        .playground-settings-plans-resource-cap-heading {
          font-size: 14px;
          line-height: 1;
          font-weight: 500;
          letter-spacing: -0.01em;
          color: rgba(255, 255, 255, 0.94);
          padding-bottom: 20px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-tasks-detail-facts.playground-settings-plans-resource-cap-facts {
          gap: 0;
          background: transparent;
          border: 0;
          padding: 0;
          border-radius: 0;
          backdrop-filter: none;
          -webkit-backdrop-filter: none;
        }
  
        .playground-tasks-detail-facts.playground-settings-plans-resource-cap-facts::before {
          content: none;
          display: none;
        }
  
        .playground-tasks-detail-facts.playground-settings-plans-resource-cap-facts .playground-tasks-detail-facts-body {
          gap: 10px;
        }
  
        .playground-tasks-detail-facts.playground-settings-plans-resource-cap-facts .playground-tasks-detail-fact {
          grid-template-columns: minmax(220px, 1fr) minmax(0, auto);
          min-height: 34px;
          gap: 24px;
        }
  
        .playground-settings-plans-resource-cap-label {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          gap: 6px;
          position: relative;
        }
  
        .playground-settings-plans-resource-cap-info-trigger {
          width: 16px;
          height: 16px;
          padding: 0;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.42);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
        }
  
        .playground-settings-plans-resource-cap-info-trigger:hover {
          background: transparent;
          color: rgba(255, 255, 255, 0.7);
        }
  
        .playground-settings-plans-resource-cap-info-popover {
          --platform-popup-padding: 10px 12px;
          position: absolute;
          top: calc(100% + 8px);
          left: 0;
          z-index: 6;
          width: 240px;
          font-size: 12px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.74);
        }
  
        .playground-settings-plans-resource-cap-control {
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          gap: 8px;
        }
  
        .playground-settings-input.playground-settings-plans-resource-cap-input {
          width: 116px;
          min-height: auto;
          padding: 0;
          text-align: right;
          border: 0;
          border-radius: 0;
          background: transparent;
          font-size: 13px;
          color: rgba(255, 255, 255, 0.94);
        }
  
        .playground-settings-input.playground-settings-plans-resource-cap-input:focus {
          border: 0;
          background: transparent;
          box-shadow: none;
        }
  
        .playground-settings-input.playground-settings-plans-resource-cap-input::placeholder {
          color: rgba(255, 255, 255, 0.42);
        }
  
        .playground-settings-input.playground-settings-plans-resource-cap-input::-webkit-outer-spin-button,
        .playground-settings-input.playground-settings-plans-resource-cap-input::-webkit-inner-spin-button {
          -webkit-appearance: none;
          margin: 0;
        }
  
        .playground-settings-input.playground-settings-plans-resource-cap-input[type="number"] {
          appearance: textfield;
          -moz-appearance: textfield;
        }
  
        .playground-settings-plans-resource-cap-suffix {
          font-size: 12px;
          color: rgba(255, 255, 255, 0.48);
        }
  
        .playground-settings-plans-quick-actions {
          display: flex;
          flex-direction: column;
          gap: 16px;
        }
  
        .playground-settings-plans-quick-actions-heading {
          margin-top: 20px;
          font-size: 14px;
          line-height: 1;
          font-weight: 500;
          letter-spacing: -0.01em;
          color: rgba(255, 255, 255, 0.94);
          margin-bottom: 26px;
        }
  
        .playground-settings-plans-quick-actions-grid {
          margin-top: 0;
          grid-template-columns: repeat(3, minmax(0, 1fr));
        }
  
        .playground-settings-plans-quick-action-card {
          text-align: left;
        }
  
        .playground-settings-plans-quick-action-card .playground-environments-home-card-visual {
          margin-bottom: 0;
        }
  
        .playground-settings-plans-quick-action-card .playground-environments-home-card-title {
          margin-top: 20px;
        }
  
        .playground-settings-plans-quick-action-card .playground-environments-home-card-title,
        .playground-settings-plans-quick-action-card .playground-environments-home-card-copy {
          text-align: left;
        }
  
  ${INFERENCE_PAGE_CSS}
        .playground-settings-plan-heading {
          margin: 23px 0 6px;
          text-align: center;
        }
  
        .playground-settings-plan-heading-title {
          font-size: 30px;
          font-weight: 600;
          line-height: 1.15;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-settings-plan-heading-copy {
          margin-top: 6px;
          font-size: 14px;
          color: rgba(255, 255, 255, 0.56);
        }
  
        .playground-settings-plan-system-note {
          display: flex;
          align-items: center;
          gap: 12px;
          padding: 16px;
          border-radius: 14px;
          border: 1px solid rgba(59, 130, 246, 0.2);
          background: rgba(59, 130, 246, 0.1);
        }
  
        .playground-settings-plan-system-note.is-cancelled {
          border-color: rgba(249, 115, 22, 0.24);
          background: rgba(249, 115, 22, 0.12);
        }
  
        .playground-settings-plan-system-note-icon {
          width: 30px;
          height: 30px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border-radius: 999px;
          background: rgba(59, 130, 246, 0.16);
          color: rgb(147, 197, 253);
          font-size: 12px;
          font-weight: 700;
          letter-spacing: 0.04em;
        }
  
        .playground-settings-plan-system-note.is-cancelled .playground-settings-plan-system-note-icon {
          background: rgba(249, 115, 22, 0.16);
          color: rgb(251, 146, 60);
        }
  
        .playground-settings-plan-system-note-title {
          font-size: 14px;
          font-weight: 500;
          color: rgb(147, 197, 253);
        }
  
        .playground-settings-plan-system-note.is-cancelled .playground-settings-plan-system-note-title {
          color: rgb(251, 146, 60);
        }
  
        .playground-settings-plan-system-note-copy {
          margin-top: 3px;
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.52);
        }
  
        .playground-settings-plan-card-app {
          position: relative;
          display: flex;
          flex-direction: column;
          gap: 14px;
          padding: 16px;
          border-radius: 15px;
          box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
          backdrop-filter: blur(50px);
        }
  
        .playground-settings-plan-card-app::before {
          content: "";
          position: absolute;
          inset: 0;
          border-radius: 15px;
          padding: 1px;
          background: linear-gradient(-10deg, rgba(200, 200, 200, 0.15), rgba(255, 255, 255, 0.1), rgba(255, 255, 255, 0.07), rgba(255, 255, 255, 0.3));
          -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
          -webkit-mask-composite: xor;
          mask-composite: exclude;
          pointer-events: none;
        }
  
        .playground-settings-plan-card-app.is-highlighted {
          background: rgba(255, 255, 255, 0.1);
          box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
        }
  
        .playground-settings-plan-card-app.is-current {
          box-shadow:
            0 0 0 1px rgba(59, 130, 246, 0.7),
            0 0 0 3px rgba(59, 130, 246, 0.12),
            0 16px 34px rgba(0, 0, 0, 0.16);
          background: rgba(255, 255, 255, 0.075);
        }
  
        .playground-settings-plan-card-app.is-current::before {
          background: linear-gradient(-10deg, rgba(37, 99, 235, 0.6), rgba(96, 165, 250, 0.5), rgba(147, 197, 253, 0.28), rgba(191, 219, 254, 0.72));
        }
  
        .playground-settings-plan-card-app:not(.is-highlighted) {
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-settings-plan-card-app:not(.is-highlighted):hover {
          background: rgba(255, 255, 255, 0.07);
        }
  
        .playground-settings-plan-card-header {
          position: relative;
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 12px;
          min-height: 26px;
        }
  
        .playground-settings-plan-card-title {
          font-size: 14px;
          font-weight: 500;
          font-family: "Inter", -apple-system, BlinkMacSystemFont, "SF Pro Text", "Segoe UI", sans-serif;
          font-style: italic;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-settings-plan-card-badge {
          position: absolute;
          right: 0;
          top: 0;
          padding: 6px 10px;
          border-radius: 10px;
          border: 1px solid rgba(255, 255, 255, 0.2);
          font-size: 12px;
          line-height: 1;
          color: rgba(255, 255, 255, 0.9);
          background: transparent;
        }
  
        .playground-settings-plan-card-badge.is-trial {
          border-color: rgba(59, 130, 246, 0.5);
          color: rgb(96, 165, 250);
        }
  
        .playground-settings-plan-card-badge.is-cancelled {
          border-color: rgba(249, 115, 22, 0.5);
          color: rgb(251, 146, 60);
        }
  
        .playground-settings-plan-card-price {
          font-size: 36px;
          font-weight: 600;
          line-height: 1;
          color: rgba(255, 255, 255, 1);
        }
  
        .playground-settings-plan-card-price-unit {
          font-size: 14px;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.46);
        }
  
        .playground-settings-plan-card-cta {
          margin: 2px 0 0;
        }
  
        .playground-settings-plan-card-cta-button,
        .playground-settings-plan-card-cta-static {
          width: 100%;
          min-height: 40px;
          padding: 0 16px;
          border-radius: 999px;
          font-size: 14px;
          font-weight: 500;
          transition: background-color 160ms ease, color 160ms ease, border-color 160ms ease, opacity 160ms ease;
        }
  
        .playground-settings-plan-card-cta-button {
          border: 1px solid transparent;
          cursor: pointer;
        }
  
        .playground-settings-plan-card-cta-button:disabled {
          opacity: 0.6;
          cursor: not-allowed;
        }
  
        .playground-settings-plan-card-cta-static {
          display: flex;
          align-items: center;
          justify-content: center;
          background: rgba(255, 255, 255, 0.05);
          color: rgba(255, 255, 255, 0.34);
        }
  
        .playground-settings-plan-card-cta-static.is-apple {
          min-height: 44px;
          padding: 10px 12px;
          border: 1px solid rgba(59, 130, 246, 0.2);
          background: rgba(59, 130, 246, 0.1);
          color: rgb(147, 197, 253);
          font-size: 12px;
          line-height: 1.4;
          text-align: center;
        }
  
        .playground-settings-plan-card-cta-button.is-primary {
          background: rgb(59, 130, 246);
          color: white;
        }
  
        .playground-settings-plan-card-cta-button.is-primary:hover {
          background: rgb(37, 99, 235);
        }
  
        .playground-settings-plan-card-cta-button.is-light {
          background: white;
          color: black;
        }
  
        .playground-settings-plan-card-cta-button.is-light:hover {
          background: rgb(243, 244, 246);
        }
  
        .playground-settings-plan-card-cta-button.is-muted {
          border-color: transparent;
          background: rgba(255, 255, 255, 0.1);
          color: rgba(255, 255, 255, 0.72);
        }
  
        .playground-settings-plan-card-cta-button.is-muted:hover {
          background: rgba(255, 255, 255, 0.2);
        }
  
        .playground-settings-plan-card-cta-button.is-danger {
          border-color: rgba(248, 113, 113, 0.3);
          background: rgba(248, 113, 113, 0.1);
          color: rgb(248, 113, 113);
        }
  
        .playground-settings-plan-card-cta-button.is-danger:hover {
          background: rgba(248, 113, 113, 0.2);
        }
  
        .playground-settings-plan-card-description {
          font-size: 14px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.58);
        }
  
        .playground-settings-plan-card-features {
          display: flex;
          flex-direction: column;
          gap: 12px;
          flex: 1 1 auto;
          margin: 0;
          padding: 16px 0 8px;
          list-style: none;
          border-top: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-settings-plan-card-feature {
          display: flex;
          align-items: flex-start;
          gap: 12px;
          font-size: 14px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.52);
        }
  
        .playground-settings-plan-card-feature.is-emphasis {
          color: rgba(255, 255, 255, 0.98);
          font-weight: 500;
        }
  
        .playground-settings-plan-card-feature-icon {
          flex: 0 0 auto;
          margin-top: 2px;
          color: currentColor;
        }
  
        .playground-settings-plan-table {
          display: grid;
          grid-template-columns: repeat(3, minmax(0, 1fr));
          gap: 0;
          border-top: 1px solid rgba(255, 255, 255, 0.1);
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-settings-plan-table-card {
          position: relative;
          min-width: 0;
          min-height: 520px;
          display: flex;
          flex-direction: column;
          padding: 24px 22px;
          background: transparent;
          box-sizing: border-box;
          transition: background-color 160ms ease, box-shadow 160ms ease;
        }
  
        .playground-settings-plan-table-card + .playground-settings-plan-table-card {
          border-left: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-settings-plan-table-card.is-outlined {
          background: rgba(102, 166, 255, 0.08);
          box-shadow: inset 0 0 0 1px rgba(102, 166, 255, 0.72);
        }
  
        .playground-settings-plan-table-heading {
          min-height: 58px;
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-settings-plan-table-name {
          font-size: 16px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-settings-plan-table-badge {
          padding: 6px 9px;
          border-radius: 999px;
          border: 1px solid rgba(255, 255, 255, 0.16);
          color: rgba(255, 255, 255, 0.72);
          font-size: 11px;
          line-height: 1;
          white-space: nowrap;
        }
  
        .playground-settings-plan-table-badge.is-current {
          border-color: rgba(102, 166, 255, 0.52);
          color: rgb(143, 196, 255);
        }
  
        .playground-settings-plan-table-price {
          margin-top: 8px;
          font-size: 36px;
          font-weight: 600;
          line-height: 1;
          color: #fff;
        }
  
        .playground-settings-plan-table-price-unit {
          font-size: 13px;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.48);
        }
  
        .playground-settings-plan-table-description {
          min-height: 68px;
          margin-top: 16px;
          padding-top: 16px;
          border-top: 1px solid rgba(255, 255, 255, 0.1);
          font-size: 13px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.58);
        }
  
        .playground-settings-plan-table-cta {
          margin-top: 16px;
        }
  
        .playground-settings-plan-table-features {
          flex: 1 1 auto;
          margin: 18px 0 0;
          padding: 0 0 4px;
          list-style: none;
        }
  
        .playground-settings-plan-table-feature {
          min-height: 46px;
          display: flex;
          align-items: flex-start;
          gap: 11px;
          padding: 12px 0;
          color: rgba(255, 255, 255, 0.56);
          font-size: 13px;
          line-height: 1.35;
        }
  
        .playground-settings-plan-table-feature + .playground-settings-plan-table-feature {
          border-top: 1px dotted rgba(255, 255, 255, 0.22);
        }
  
        .playground-settings-plan-table-feature.is-emphasis {
          color: rgba(255, 255, 255, 0.96);
          font-weight: 500;
        }
  
        .playground-settings-plan-table-feature-icon {
          flex: 0 0 auto;
          width: 15px;
          height: 15px;
          margin-top: 1px;
          color: currentColor;
        }
  
        .playground-settings-kpi-label {
          font-size: 11px;
          text-transform: uppercase;
          letter-spacing: 0.04em;
          color: rgba(255, 255, 255, 0.42);
        }
  
        .playground-settings-kpi-value {
          font-size: 22px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-settings-empty-state,
        .playground-settings-loading-state {
          min-height: 180px;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          gap: 12px;
          padding: 20px;
          border-radius: 18px;
          border: 1px dashed rgba(255, 255, 255, 0.12);
          background: rgba(255, 255, 255, 0.02);
          text-align: center;
        }
  
        .playground-settings-empty-icon {
          width: 40px;
          height: 40px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border-radius: 12px;
          background: rgba(255, 255, 255, 0.06);
          color: rgba(255, 255, 255, 0.7);
        }
  
        .playground-settings-empty-icon.is-inline {
          width: 36px;
          height: 36px;
          flex-shrink: 0;
        }
  
        .playground-settings-empty-title {
          font-size: 16px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-settings-empty-copy {
          max-width: 420px;
          font-size: 12px;
          line-height: 1.6;
          color: rgba(255, 255, 255, 0.54);
        }
  
        @keyframes spin {
          from {
            transform: rotate(0deg);
          }
  
          to {
            transform: rotate(360deg);
          }
        }
  
        @keyframes spinner-rotate {
          from {
            transform: rotate(0deg);
          }
  
          to {
            transform: rotate(360deg);
          }
        }
  
        @keyframes playground-status-indicator-progress {
          0% {
            transform: translateX(-105%);
          }
  
          100% {
            transform: translateX(260%);
          }
        }
  
        .playground-settings-loading-icon {
          width: 18px;
          height: 18px;
          color: rgba(255, 255, 255, 0.6);
          animation: spin 0.9s linear infinite;
        }
  
        @keyframes settings-dot-pulse {
          0%,
          80%,
          100% {
            opacity: 0.26;
            transform: translateY(0);
          }
  
          40% {
            opacity: 1;
            transform: translateY(-1px);
          }
        }
  
        .playground-settings-dot-loader {
          display: inline-flex;
          align-items: center;
          gap: 2px;
        }
  
        .playground-settings-dot-loader-dot {
          width: 3px;
          height: 3px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.62);
          animation: settings-dot-pulse 1s ease-in-out infinite;
        }
  
        .playground-settings-records-shell {
          width: 100%;
        }
  
        .playground-settings-records-title {
          margin: 0;
          font-size: 18px;
          font-weight: 500;
          color: rgba(255, 255, 255, 1);
        }
  
        .playground-settings-records-subtitle {
          margin: 2px 0 24px;
          font-size: 12px;
          color: rgba(255, 255, 255, 0.44);
        }
  
  ${API_KEYS_STYLE_FRAGMENTS.table}      .playground-settings-records-current {
          margin-bottom: 24px;
        }
  
        .playground-settings-records-current-box {
          position: relative;
          padding: 8px 16px 8px 8px;
          border-radius: 15px;
          box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
          background: rgba(255, 255, 255, 0.025);
        }
  
        .playground-settings-records-current-box::before {
          content: "";
          position: absolute;
          inset: 0;
          border-radius: 15px;
          padding: 1px;
          background: linear-gradient(-10deg, rgba(200, 200, 200, 0.15), rgba(255, 255, 255, 0.05), rgba(255, 255, 255, 0.07), rgba(255, 255, 255, 0.35));
          -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
          -webkit-mask-composite: xor;
          mask-composite: exclude;
          pointer-events: none;
        }
  
        .playground-settings-records-current-row {
          position: relative;
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-settings-records-current-plan {
          display: flex;
          align-items: center;
          gap: 0;
          min-width: 0;
        }
  
        .playground-settings-records-current-logo {
          width: 64px;
          height: 64px;
          border-radius: 8px;
          object-fit: cover;
          flex: 0 0 auto;
        }
  
        .playground-settings-records-current-name {
          font-size: 14px;
          font-weight: 500;
          color: rgba(255, 255, 255, 1);
        }
  
        .playground-settings-records-current-date {
          margin-top: 2px;
          font-size: 12px;
          color: rgba(209, 213, 219, 0.95);
        }
  
        .playground-settings-records-current-actions {
          display: flex;
          align-items: center;
          gap: 12px;
          flex-shrink: 0;
        }
  
        .playground-settings-records-current-status-wrap {
          display: flex;
          align-items: center;
          gap: 16px;
        }
  
        .playground-settings-records-status-badge {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          min-height: 28px;
          padding: 4px 8px;
          border-radius: 10px;
          font-size: 12px;
          font-weight: 500;
          line-height: 1;
          background: rgba(255, 255, 255, 0.1);
          color: rgba(156, 163, 175, 0.95);
        }
  
        .playground-settings-records-status-badge.is-success {
          background: rgba(134, 239, 172, 0.1);
          color: rgb(134, 239, 172);
        }
  
        .playground-settings-records-status-badge.is-warning {
          background: rgba(234, 179, 8, 0.2);
          color: rgb(250, 204, 21);
        }
  
        .playground-settings-records-status-badge.is-danger {
          background: rgba(239, 68, 68, 0.15);
          color: rgb(248, 113, 113);
        }
  
        .playground-settings-records-cancel-button {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          min-width: 72px;
          min-height: 32px;
          padding: 6px 12px;
          border-radius: 8px;
          border: 1px solid rgba(239, 68, 68, 0.3);
          background: transparent;
          color: rgb(248, 113, 113);
          font-size: 12px;
          cursor: pointer;
          transition: background-color 160ms ease, opacity 160ms ease;
        }
  
        .playground-settings-records-cancel-button:hover:not(:disabled) {
          background: rgba(239, 68, 68, 0.1);
        }
  
        .playground-settings-records-cancel-button:disabled {
          opacity: 0.5;
          cursor: not-allowed;
        }
  
        .playground-settings-records-spinner {
          width: 12px;
          height: 12px;
          color: inherit;
          animation: spin 0.9s linear infinite;
        }
  
        .playground-settings-records-loading {
          display: flex;
          align-items: center;
          justify-content: center;
          padding: 48px 0;
        }
  
        .playground-settings-records-loading-copy {
          margin-left: 8px;
          font-size: 12px;
          color: rgba(255, 255, 255, 0.44);
        }
  
        .playground-settings-records-table-wrap {
          overflow-x: auto;
          backdrop-filter: blur(50px);
          -webkit-backdrop-filter: blur(50px);
        }
  
        .playground-settings-records-table {
          width: 100%;
          border-collapse: collapse;
          backdrop-filter: blur(20px);
          -webkit-backdrop-filter: blur(20px);
        }
  
        .playground-settings-records-table-head-row {
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-settings-records-table th {
          padding: 12px 12px 12px 0;
          text-align: left;
          font-size: 12px;
          font-weight: 500;
          color: rgba(156, 163, 175, 0.95);
        }
  
        .playground-settings-records-table td {
          padding: 16px 12px 16px 0;
          border-bottom: 1px solid rgba(255, 255, 255, 0.05);
          font-size: 12px;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-settings-records-table tr:hover td {
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-settings-records-table th.is-right,
        .playground-settings-records-table td.is-right {
          text-align: right;
        }
  
        .playground-settings-records-table td.is-muted {
          color: rgba(156, 163, 175, 0.95);
        }
  
        .playground-settings-records-description-title {
          color: rgba(255, 255, 255, 1);
        }
  
        .playground-settings-records-description-copy {
          margin-top: 3px;
          color: rgba(156, 163, 175, 0.95);
          text-transform: capitalize;
        }
  
        .playground-settings-records-amount {
          font-weight: 600;
          color: rgba(255, 255, 255, 1);
        }
  
        .playground-settings-records-link {
          color: rgba(255, 255, 255, 1);
          text-decoration: none;
          transition: color 160ms ease;
        }
  
        .playground-settings-records-link:hover {
          color: rgba(255, 255, 255, 0.5);
        }
  
        .playground-settings-records-empty-row td {
          padding: 32px 0;
          text-align: center;
          color: rgba(107, 114, 128, 0.95);
        }
  
        .playground-settings-table-wrap {
          overflow-x: auto;
          backdrop-filter: blur(20px);
          -webkit-backdrop-filter: blur(20px);
        }
  
        .playground-settings-table {
          width: 100%;
          border-collapse: collapse;
          backdrop-filter: blur(20px);
          -webkit-backdrop-filter: blur(20px);
        }
  
        .tb-message-markdown-table-wrap {
          overflow-x: auto;
          backdrop-filter: blur(20px);
          -webkit-backdrop-filter: blur(20px);
        }
  
        .tb-message-markdown-table {
          width: 100%;
          border-collapse: collapse;
          backdrop-filter: blur(20px);
          -webkit-backdrop-filter: blur(20px);
        }
  
        .playground-settings-table th,
        .playground-settings-table td {
          padding: 12px 10px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.08);
          font-size: 12px;
          text-align: left;
        }
  
        .playground-settings-table th {
          font-size: 11px;
          font-weight: 600;
          letter-spacing: 0.04em;
          text-transform: uppercase;
          color: rgba(255, 255, 255, 0.42);
        }
  
        .playground-settings-table td {
          color: rgba(255, 255, 255, 0.78);
        }
  
        .playground-settings-table th.is-right,
        .playground-settings-table td.is-right {
          text-align: right;
        }
  
        .playground-settings-table a {
          color: #c8daff;
          text-decoration: none;
        }
  
        .playground-settings-form-grid {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 12px;
        }
  
        .playground-settings-field {
          display: flex;
          flex-direction: column;
          gap: 8px;
        }
  
        .playground-settings-field-span-2 {
          grid-column: span 2;
        }
  
        .playground-settings-label {
          font-size: 12px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.84);
        }
  
        .playground-settings-input,
        .playground-settings-select,
        .playground-settings-textarea {
          width: 100%;
          min-height: 36px;
          padding: 0 12px;
          border-radius: 10px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          background: rgba(255, 255, 255, 0.05);
          color: rgba(255, 255, 255, 0.94);
          font-size: 12px;
          outline: none;
        }
  
        .playground-settings-textarea {
          min-height: 36px;
          padding: 12px;
          resize: vertical;
        }
  
        .playground-settings-input::placeholder,
        .playground-settings-textarea::placeholder {
          color: rgba(255, 255, 255, 0.32);
        }
  
        .playground-settings-input:focus,
        .playground-settings-select:focus,
        .playground-settings-textarea:focus {
          border-color: rgba(137, 182, 255, 0.4);
          background: rgba(255, 255, 255, 0.07);
        }
  
        .playground-settings-input-danger {
          border-color: rgba(255, 107, 107, 0.24);
        }
  
        .playground-settings-toggle-group,
        .playground-settings-form-actions {
          display: flex;
          flex-wrap: wrap;
          gap: 10px;
        }
  
        .playground-settings-account-shell {
          width: 100%;
          max-width: 720px;
          display: flex;
          flex-direction: column;
          gap: 24px;
        }
  
        .playground-settings-account-shell.is-wide {
          max-width: none;
        }
  
        .playground-settings-account-block {
          display: flex;
          flex-direction: column;
          gap: 24px;
        }
  
        .playground-settings-account-inline {
          display: flex;
          align-items: center;
          gap: 8px;
          flex-wrap: wrap;
          margin-top: 8px;
          font-size: 12px;
          color: rgba(255, 255, 255, 0.78);
        }
  
        .playground-settings-account-bullet {
          color: rgba(255, 255, 255, 0.28);
        }
  
        .playground-settings-account-link {
          border: 0;
          padding: 0;
          background: transparent;
          color: rgb(96, 165, 250);
          font-size: 12px;
          cursor: pointer;
          transition: color 160ms ease, opacity 160ms ease;
        }
  
        .playground-settings-account-link:hover {
          color: rgb(147, 197, 253);
        }
  
        .playground-settings-app-primary-button,
        .playground-settings-app-secondary-button {
          min-height: 40px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 8px;
          padding: 0 16px;
          border-radius: 10px;
          font-size: 12px;
          font-weight: 600;
          cursor: pointer;
          transition: background-color 160ms ease, color 160ms ease, border-color 160ms ease, opacity 160ms ease;
        }
  
        .playground-settings-app-primary-button {
          border: 0;
          background: rgba(255, 255, 255, 0.96);
          color: rgba(17, 17, 17, 0.95);
        }
  
        .playground-settings-app-primary-button:hover {
          background: rgba(255, 255, 255, 0.88);
        }
  
        .playground-settings-app-secondary-button {
          border: 1px solid rgba(255, 255, 255, 0.14);
          background: transparent;
          color: rgba(255, 255, 255, 0.86);
        }
  
        .playground-settings-app-secondary-button:hover {
          background: rgba(255, 255, 255, 0.08);
        }
  
        .playground-settings-app-primary-button:disabled,
        .playground-settings-app-secondary-button:disabled,
        .playground-settings-choice-button:disabled,
        .playground-settings-api-key-revoke:disabled,
        .playground-settings-account-signout:disabled {
          opacity: 0.5;
          cursor: not-allowed;
        }
  
        .playground-settings-choice-row {
          display: flex;
          flex-wrap: wrap;
          align-items: center;
          gap: 12px;
        }
  
        .playground-settings-model-entry-row {
          display: flex;
          align-items: center;
          gap: 10px;
        }
  
        .playground-settings-model-entry-row .playground-settings-input {
          flex: 1 1 auto;
        }
  
        .playground-settings-model-entry-row .playground-settings-app-secondary-button {
          flex: 0 0 auto;
          white-space: nowrap;
        }
  
        .playground-settings-choice-button {
          min-height: 38px;
          padding: 0 16px;
          border-radius: 10px;
          border: 1px solid rgba(255, 255, 255, 0.2);
          background: transparent;
          color: rgba(255, 255, 255, 0.92);
          font-size: 12px;
          font-weight: 600;
          cursor: pointer;
          transition: background-color 160ms ease, border-color 160ms ease, color 160ms ease, opacity 160ms ease;
        }
  
        .playground-settings-choice-button:hover {
          background: rgba(255, 255, 255, 0.08);
        }
  
        .playground-settings-choice-button.is-active {
          border-color: rgba(255, 255, 255, 0.88);
          background: rgba(255, 255, 255, 0.96);
          color: rgba(17, 17, 17, 0.95);
        }
  
        .playground-settings-choice-loading {
          display: inline-flex;
          align-items: center;
          gap: 6px;
          font-size: 12px;
          color: rgba(255, 255, 255, 0.54);
        }
  
        .playground-settings-inline-status {
          display: flex;
          align-items: center;
          gap: 8px;
          font-size: 12px;
        }
  
        .playground-settings-inline-status.is-error {
          color: rgb(248, 113, 113);
        }
  
        .playground-settings-inline-status.is-success {
          color: rgb(74, 222, 128);
        }
  
        .playground-settings-account-divider {
          padding-top: 24px;
          margin-top: 4px;
          border-top: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-settings-account-signout {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          min-height: 40px;
          padding: 0 14px;
          border-radius: 10px;
          border: 0;
          background: rgba(239, 68, 68, 0.08);
          color: rgba(248, 113, 113, 0.92);
          font-size: 12px;
          font-weight: 600;
          cursor: pointer;
          transition: background-color 160ms ease, color 160ms ease;
        }
  
        .playground-settings-account-signout:hover {
          background: rgba(239, 68, 68, 0.14);
          color: rgba(252, 165, 165, 0.98);
        }
  
        .playground-settings-account-shell.is-wide .playground-settings-app-primary-button,
        .playground-settings-account-shell.is-wide .playground-settings-app-secondary-button,
        .playground-settings-account-shell.is-wide .playground-settings-choice-button,
        .playground-settings-account-shell.is-wide .playground-settings-danger-button {
          border-radius: 999px;
        }
  
        .playground-settings-account-shell.is-wide .playground-settings-app-primary-button,
        .playground-settings-account-shell.is-wide .playground-settings-danger-button {
          align-self: flex-start;
          width: auto;
        }
  
        .playground-settings-toggle-pill {
          min-height: 34px;
          padding: 0 12px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.7);
          font-size: 12px;
          cursor: pointer;
        }
  
        .playground-settings-toggle-pill.is-active {
          background: #f4f4f4;
          color: #111;
        }
  
        .playground-settings-usage-list,
        .playground-settings-daily-list {
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-settings-usage-row {
          display: grid;
          grid-template-columns: minmax(0, 1fr) auto minmax(120px, 160px);
          gap: 12px;
          align-items: center;
        }
  
        .playground-settings-usage-copy {
          min-width: 0;
        }
  
        .playground-settings-usage-name,
        .playground-settings-usage-value {
          font-size: 13px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.88);
        }
  
        .playground-settings-usage-bar {
          height: 8px;
          border-radius: 999px;
          overflow: hidden;
          background: rgba(255, 255, 255, 0.08);
        }
  
        .playground-settings-usage-bar-fill {
          display: block;
          height: 100%;
          border-radius: inherit;
          background: linear-gradient(90deg, rgba(137, 182, 255, 0.95), rgba(90, 149, 255, 0.7));
        }
  
        .playground-settings-usage-dashboard {
          display: flex;
          flex-direction: column;
          gap: 24px;
        }
  
        .playground-settings-usage-dashboard-header {
          padding-bottom: 14px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-settings-usage-dashboard-title {
          font-size: 18px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-settings-usage-dashboard-copy {
          margin-top: 8px;
          font-size: 12px;
          line-height: 1.6;
          color: rgba(255, 255, 255, 0.52);
        }
  
        .playground-settings-usage-balance-section {
          display: flex;
          flex-direction: column;
          gap: 16px;
        }
  
        .playground-settings-usage-balance-heading {
          font-size: 13px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.86);
        }
  
        .playground-settings-usage-balance-grid {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 16px;
        }
  
        .playground-settings-usage-balance-card {
          display: flex;
          flex-direction: column;
          gap: 18px;
          min-height: 188px;
          padding: 26px;
          border-radius: 28px;
          border: 1px solid rgba(255, 255, 255, 0.06);
          background:
            radial-gradient(circle at top right, rgba(255, 255, 255, 0.04), transparent 42%),
            linear-gradient(180deg, rgba(35, 35, 35, 0.96), rgba(29, 29, 29, 0.98));
          box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.03);
        }
  
        .playground-settings-usage-balance-card.is-static {
          justify-content: space-between;
        }
  
        .playground-settings-usage-balance-card-top,
        .playground-settings-usage-balance-card-main {
          display: flex;
          flex-direction: column;
          gap: 8px;
        }
  
        .playground-settings-usage-balance-card-top {
          flex-direction: row;
          justify-content: space-between;
          align-items: flex-start;
          gap: 14px;
        }
  
        .playground-settings-usage-balance-card-label {
          font-size: 13px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.54);
        }
  
        .playground-settings-usage-balance-card-value {
          font-size: 24px;
          font-weight: 600;
          line-height: 1.2;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-settings-usage-balance-card-subvalue {
          font-size: 13px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.84);
        }
  
        .playground-settings-usage-balance-track {
          position: relative;
          height: 14px;
          border-radius: 999px;
          overflow: hidden;
          background: rgba(255, 255, 255, 0.86);
        }
  
        .playground-settings-usage-balance-fill {
          display: block;
          height: 100%;
          border-radius: inherit;
          background: linear-gradient(90deg, rgba(98, 201, 110, 0.98), rgba(96, 198, 108, 0.86));
        }
  
        .playground-settings-usage-balance-fill.is-neutral {
          background: linear-gradient(90deg, rgba(137, 182, 255, 0.96), rgba(100, 156, 255, 0.82));
        }
  
        .playground-settings-usage-balance-card-footer {
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.46);
        }
  
        .playground-settings-usage-balance-card-action {
          width: 30px;
          height: 30px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border: 0;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.96);
          color: rgba(17, 17, 17, 0.94);
          cursor: pointer;
          transition: transform 160ms ease, opacity 160ms ease;
        }
  
        .playground-settings-usage-balance-card-action:hover {
          transform: translateY(-1px);
          opacity: 0.96;
        }
  
        .playground-settings-usage-balance-note {
          padding: 16px 18px;
          border-radius: 18px;
          border: 1px solid rgba(255, 255, 255, 0.07);
          background: rgba(255, 255, 255, 0.025);
          font-size: 12px;
          line-height: 1.6;
          color: rgba(255, 255, 255, 0.54);
        }
  
        .playground-settings-usage-app-shell {
          display: flex;
          flex-direction: column;
          gap: 16px;
        }
  
        .playground-settings-usage-period-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 16px;
          padding-bottom: 20px;
          margin-bottom: 20px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-settings-usage-period-header.is-chart-footer {
          padding: 12px 0 0;
          margin: 12px 0 0;
          border-top: 0;
          border-bottom: 0;
        }
  
        .playground-settings-usage-period-heading {
          display: flex;
          align-items: center;
          gap: 12px;
          min-width: 0;
          flex-wrap: wrap;
        }
  
        .playground-settings-usage-period-title {
          font-size: 24px;
          line-height: 1;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-settings-usage-app-period {
          font-size: 12px;
          line-height: 1.3;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.92);
        }
  
        .playground-settings-usage-nav {
          display: inline-flex;
          align-items: center;
          gap: 8px;
        }
  
        .playground-settings-usage-nav-button,
        .playground-settings-usage-nav-current {
          min-height: 30px;
          padding: 0 10px;
          border: 0;
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.4);
          font-size: 12px;
          cursor: pointer;
          transition: background-color 160ms ease, color 160ms ease;
        }
  
        .playground-settings-usage-nav-button:hover,
        .playground-settings-usage-nav-current:hover {
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.88);
        }
  
        .playground-settings-usage-nav-button:disabled,
        .playground-settings-usage-nav-current:disabled {
          cursor: not-allowed;
          background: transparent;
          color: rgba(255, 255, 255, 0.22);
        }
  
        .playground-settings-usage-nav-current:disabled {
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.32);
        }
  
        .playground-settings-usage-summary-grid,
        .playground-settings-usage-secondary-grid {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 16px;
        }
  
        .playground-settings-usage-analytics-grid {
          display: grid;
          grid-template-columns: minmax(0, 1fr) minmax(330px, 360px);
          gap: 16px;
          align-items: stretch;
        }
  
        .playground-settings-usage-summary-grid {
          grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
        }
  
        .playground-settings-usage-summary-card,
        .playground-settings-usage-chart-card {
          border-radius: 18px;
          padding: 16px;
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-settings-usage-chart-card {
          display: flex;
          flex-direction: column;
          gap: 14px;
          height: 100%;
          border-radius: 0;
          padding: 0;
          background: transparent;
        }
  
        .playground-settings-usage-analytics-grid > .playground-settings-usage-chart-card {
          min-height: 392px;
        }
  
        .playground-settings-usage-summary-label {
          font-size: 10px;
          font-weight: 500;
          letter-spacing: 0.08em;
          text-transform: uppercase;
          color: rgba(255, 255, 255, 0.38);
        }
  
        .playground-settings-usage-summary-value {
          margin-top: 8px;
          font-size: 18px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.94);
        }
  
        .playground-settings-usage-summary-copy {
          margin-top: 4px;
          font-size: 12px;
          color: rgba(255, 255, 255, 0.45);
        }
  
        .playground-settings-usage-card-header,
        .playground-settings-usage-card-header-legend,
        .playground-settings-usage-legend-row,
        .playground-settings-usage-inline-legend {
          display: flex;
          align-items: center;
        }
  
        .playground-settings-usage-card-header,
        .playground-settings-usage-legend-row {
          justify-content: space-between;
          gap: 16px;
        }
  
        .playground-settings-usage-card-title {
          font-size: 14px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.94);
        }
  
        .playground-settings-usage-card-copy {
          margin-top: 4px;
          font-size: 12px;
          color: rgba(255, 255, 255, 0.42);
        }
  
        .playground-settings-usage-card-header-legend,
        .playground-settings-usage-inline-legend {
          flex-wrap: wrap;
          justify-content: flex-end;
          gap: 14px;
          font-size: 12px;
          color: rgba(255, 255, 255, 0.4);
        }
  
        .playground-settings-usage-legend-item {
          display: inline-flex;
          align-items: center;
          gap: 6px;
        }
  
        .playground-settings-usage-legend-swatch,
        .playground-settings-usage-legend-line {
          display: inline-flex;
          flex: 0 0 auto;
        }
  
        .playground-settings-usage-legend-swatch {
          width: 10px;
          height: 10px;
          border-radius: 3px;
        }
  
        .playground-settings-usage-legend-line {
          width: 12px;
          height: 2px;
          border-radius: 999px;
        }
  
        .playground-settings-usage-chart-frame {
          height: 208px;
          margin-top: 12px;
        }
  
        .playground-settings-usage-chart-frame.is-tall {
          height: 288px;
        }
  
        .playground-settings-usage-chart-frame.is-medium {
          height: 208px;
        }
  
        .playground-settings-usage-chart-svg {
          width: 100%;
          height: 100%;
          display: block;
        }
  
        .playground-settings-usage-chart-empty {
          height: 208px;
          display: flex;
          align-items: center;
          justify-content: center;
          font-size: 13px;
          color: rgba(255, 255, 255, 0.34);
        }
  
        .playground-settings-usage-chart-empty.is-tall {
          height: 288px;
        }
  
        .playground-settings-usage-chart-card.is-source {
          min-height: 260px;
        }
  
        .playground-settings-usage-chart-card.is-donut {
          min-height: 260px;
          min-width: 0;
          width: fit-content;
          max-width: 100%;
          justify-self: end;
        }
  
        .playground-settings-usage-chart-card.is-donut .playground-settings-usage-card-header {
          padding-left: 0;
        }
  
        .playground-settings-usage-resource-table-wrap {
          grid-column: 1 / -1;
          display: flex;
          flex-direction: column;
          gap: 14px;
          padding-top: 0;
        }
  
        .playground-settings-usage-resource-table-wrap .playground-settings-usage-card-header {
          margin-top: 20px;
          padding-top: 0;
          border-top: 0;
        }
  
        .playground-settings-usage-resource-table-shell {
          width: 100%;
        }
  
        .playground-settings-usage-resource-table {
          width: 100%;
          border-collapse: collapse;
        }
  
        .playground-settings-usage-resource-table th,
        .playground-settings-usage-resource-table td {
          padding: 10px 0;
          border-bottom: 1px solid rgba(255, 255, 255, 0.08);
          text-align: left;
        }
  
        .playground-settings-usage-resource-table th {
          font-size: 11px;
          font-weight: 500;
          letter-spacing: 0.06em;
          text-transform: uppercase;
          color: rgba(255, 255, 255, 0.42);
        }
  
        .playground-settings-usage-resource-table th.is-numeric,
        .playground-settings-usage-resource-table td.playground-settings-usage-resource-ct-cell {
          text-align: right;
        }
  
        .playground-settings-usage-resource-table td {
          font-size: 13px;
          color: rgba(255, 255, 255, 0.84);
        }
  
        .playground-settings-usage-resource-name-cell {
          min-width: 0;
        }
  
        .playground-settings-usage-resource-name-cell,
        .playground-settings-usage-resource-type-cell {
          padding-right: 16px;
        }
  
        .playground-settings-usage-resource-name {
          display: block;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.92);
        }
  
        .playground-settings-usage-resource-meta {
          display: block;
          margin-top: 3px;
          font-size: 11px;
          color: rgba(255, 255, 255, 0.38);
        }
  
        .playground-settings-usage-resource-type-cell {
          color: rgba(255, 255, 255, 0.56);
        }
  
        .playground-settings-usage-resource-ct-cell {
          font-weight: 600;
          color: rgba(255, 255, 255, 0.92);
          white-space: nowrap;
        }
  
        .playground-settings-usage-resource-empty-row td {
          color: rgba(255, 255, 255, 0.4);
        }
  
        .playground-settings-usage-donut-layout {
          margin-top: -50px;
          display: grid;
          grid-template-columns: 232px max-content;
          gap: 6px;
          align-items: center;
          justify-content: end;
          width: 100%;
          flex: 1 1 auto;
        }
  
        .playground-settings-usage-donut-frame {
          width: 232px;
          height: 252px;
          margin: 0 auto;
        }
  
        .playground-settings-usage-donut-column {
          display: flex;
          flex-direction: column;
          gap: 12px;
          min-width: 0;
          width: max-content;
          max-width: 100%;
        }
  
        .playground-settings-usage-donut-column.is-right {
          align-items: flex-start;
          justify-self: end;
        }
  
        .playground-settings-usage-donut-legend-item {
          display: flex;
          align-items: flex-start;
          gap: 10px;
          min-width: 0;
        }
  
        .playground-settings-usage-donut-legend-copy {
          display: flex;
          flex-direction: column;
          gap: 4px;
          min-width: 0;
        }
  
        .playground-settings-usage-donut-label {
          font-size: 11px;
          color: rgba(255, 255, 255, 0.58);
          line-height: 1.35;
          word-break: break-word;
        }
  
        .playground-settings-usage-donut-value {
          font-size: 14px;
          line-height: 1;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-settings-usage-donut-subvalue {
          font-size: 11px;
          color: rgba(255, 255, 255, 0.42);
        }
  
        .playground-settings-usage-donut-center-label {
          fill: rgba(255, 255, 255, 0.46);
          font-size: 10px;
          font-weight: 500;
        }
  
        .playground-settings-usage-donut-center-value {
          fill: rgba(255, 255, 255, 0.96);
          font-size: 18px;
          font-weight: 600;
        }
  
        .playground-settings-usage-donut-center-copy {
          fill: rgba(255, 255, 255, 0.4);
          font-size: 9px;
        }
  
        .playground-settings-usage-chart-note {
          margin-top: 12px;
          font-size: 12px;
          color: rgba(255, 255, 255, 0.34);
        }
  
        .playground-settings-usage-source-list {
          margin-top: 14px;
          display: flex;
          flex-direction: column;
          gap: 12px;
        }
  
        .playground-settings-usage-source-row {
          display: flex;
          flex-direction: column;
          gap: 8px;
        }
  
        .playground-settings-usage-source-row-top {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-settings-usage-source-label,
        .playground-settings-usage-source-value {
          font-size: 12px;
          color: rgba(255, 255, 255, 0.72);
        }
  
        .playground-settings-usage-source-value {
          font-weight: 500;
          color: rgba(255, 255, 255, 0.94);
        }
  
        .playground-settings-usage-source-track {
          height: 12px;
          overflow: hidden;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.08);
        }
  
        .playground-settings-usage-source-fill {
          display: block;
          height: 100%;
          border-radius: inherit;
        }
  
        .playground-settings-daily-row {
          display: grid;
          grid-template-columns: repeat(3, minmax(0, 1fr));
          gap: 12px;
          align-items: center;
          padding: 12px 14px;
          border-radius: 14px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.03);
          color: rgba(255, 255, 255, 0.76);
          font-size: 12px;
        }
  
  ${API_KEYS_STYLE_FRAGMENTS.sharedComponents}
        .playground-settings-metric-pill {
          display: inline-flex;
          align-items: center;
          min-height: 26px;
          padding: 0 10px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.05);
          color: rgba(255, 255, 255, 0.72);
          font-size: 11px;
        }
  
        .playground-settings-modal-backdrop {
          position: fixed;
          inset: 0;
          z-index: 320;
          display: flex;
          align-items: center;
          justify-content: center;
          padding: 20px;
          background: rgba(0, 0, 0, 0.62);
        }
  
        .playground-settings-modal {
          position: relative;
          z-index: 1;
          width: min(560px, 100%);
          max-height: calc(100vh - 40px);
          overflow: auto;
          padding: 18px;
          border-radius: 20px;
          border: 1px solid rgba(255, 255, 255, 0.12);
          background: rgba(18, 18, 20, 0.98);
          box-shadow: 0 28px 80px rgba(0, 0, 0, 0.45);
        }
  
        .playground-settings-modal.is-plan-selector {
          width: min(1160px, 100%);
        }
  
        .playground-settings-modal-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          margin-bottom: 16px;
        }
  
  ${CALENDAR_STYLE_FRAGMENTS.upgrade}
        .playground-subscription-success-backdrop {
          position: fixed;
          inset: 0;
          z-index: 340;
          display: flex;
          align-items: center;
          justify-content: center;
          padding: 20px;
          background: rgba(0, 0, 0, 0.68);
          backdrop-filter: blur(12px);
        }
  
        .playground-subscription-success-modal {
          width: min(420px, 100%);
          border-radius: 15px;
          overflow: hidden;
          border: 1px solid rgba(255, 255, 255, 0.1);
          background: #323232;
          box-shadow: 0 12px 30px rgba(0, 0, 0, 0.35);
          backdrop-filter: blur(10px);
        }
  
        .playground-subscription-success-image {
          display: block;
          width: 100%;
          height: 190px;
          object-fit: cover;
          background: #111;
        }
  
        .playground-subscription-success-copy {
          padding: 26px 24px 18px;
          display: flex;
          flex-direction: column;
          align-items: center;
          text-align: center;
          gap: 8px;
        }
  
        .playground-subscription-success-title {
          margin: 0;
          font-size: 28px;
          line-height: 1;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-subscription-success-description {
          margin: 12px 0;
          font-size: 14px;
          color: rgba(255, 255, 255, 0.62);
        }
  
        .playground-subscription-success-actions {
          padding: 18px 24px 18px;
          border-top: 1px solid rgba(255, 255, 255, 0.1);
          display: flex;
          align-items: center;
          justify-content: center;
          gap: 10px;
          width: 100%;
        }
  
        .playground-subscription-success-button {
          flex: 1 1 0;
          min-width: 0;
          min-height: 40px;
          padding: 0 16px;
          border-radius: 999px;
          border: 1px solid rgba(255, 255, 255, 0.14);
          background: transparent;
          color: rgba(255, 255, 255, 0.92);
          font-size: 12px;
          font-weight: 500;
          cursor: pointer;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          transition: background-color 160ms ease, border-color 160ms ease, color 160ms ease;
        }
  
        .playground-subscription-success-button:hover {
          background: rgba(255, 255, 255, 0.08);
          color: #fff;
        }
  
        .playground-subscription-success-button.is-primary {
          border-color: rgba(255, 255, 255, 0.92);
          background: #ffffff;
          color: #0d0d0d;
        }
  
        .playground-subscription-success-button.is-primary:hover {
          background: rgba(255, 255, 255, 0.94);
          color: #000;
        }
  
        @media (max-width: 720px) {
          .playground-subscription-success-modal {
            border-radius: 15px;
          }
  
          .playground-subscription-success-title {
            font-size: 30px;
          }
  
          .playground-subscription-success-actions {
            flex-direction: column;
            align-items: stretch;
          }
  
          .playground-subscription-success-button {
            width: 100%;
          }
        }
  
        .playground-settings-danger-action {
          border-color: rgba(255, 107, 107, 0.2);
          color: #ffb9b9;
        }
  
        .playground-settings-danger-button {
          min-height: 40px;
          padding: 0 16px;
          border: 0;
          border-radius: 12px;
          background: #f4f4f4;
          color: #1a1a1a;
          font-size: 12px;
          font-weight: 600;
          cursor: pointer;
        }
  
        .playground-settings-danger-button:disabled {
          opacity: 0.56;
          cursor: default;
        }
  
  ${FILES_STYLE_FRAGMENTS.foundation}
  ${FILES_STYLE_FRAGMENTS.toolbar}
  ${IMAGINE_SHELL_STYLE_FRAGMENTS.toolbar}
  
  ${GUARDRAILS_STYLE_FRAGMENTS.page}${PLAYGROUND_EVALUATIONS_CSS}
  
  ${FILES_STYLE_FRAGMENTS.content}
  ${FILES_STYLE_FRAGMENTS.preview}
  ${FILES_STYLE_FRAGMENTS.chat}
  ${FILES_STYLE_FRAGMENTS.editor}
  ${FILES_STYLE_FRAGMENTS.contextMenu}
        .playground-environments-page {
          height: 100%;
          min-height: 0;
          padding: 0;
          background: transparent;
        }
  
        .playground-environments-page:not(.playground-agents-page) {
          --playground-environments-nav-top-offset: 10px;
          --playground-environments-nav-row-height: 30px;
          --playground-environments-list-title-size: 14px;
          --playground-environments-detail-title-size: 20px;
        }
  
        .playground-environments-shell {
          height: 100%;
          min-height: 0;
          display: grid;
          grid-template-columns: 270px minmax(0, 1fr);
          background: transparent;
        }
  
        .playground-skills-page .playground-environments-shell {
          margin-top: -5px;
        }
  
        .playground-environments-list-pane {
          min-width: 0;
          min-height: 0;
          display: flex;
          flex-direction: column;
          position: relative;
          z-index: 2;
          background: transparent;
          border-right: 0;
          backdrop-filter: none;
          -webkit-backdrop-filter: none;
        }
  
        .playground-environments-list-header {
          padding-bottom: 6px;
          position: relative;
          z-index: 94;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-list-header {
          padding: var(--playground-environments-nav-top-offset) 12px 8px;
        }
  
        .playground-environments-list-title {
          font-size: 22px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-list-title {
          display: flex;
          align-items: center;
          min-height: var(--playground-environments-nav-row-height);
          font-size: var(--playground-environments-list-title-size);
          font-weight: 500;
          line-height: 1;
          letter-spacing: -0.01em;
        }
  
        .playground-environments-list-title-button {
          padding: 0;
          border: 0;
          background: transparent;
          color: inherit;
          font: inherit;
          letter-spacing: inherit;
          cursor: pointer;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-files-topbar {
          min-height: var(--playground-environments-nav-row-height);
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-files-topbar-actions {
          min-height: var(--playground-environments-nav-row-height);
          gap: 4px;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-files-header-icon-button {
          width: 32px;
          height: 32px;
          border-radius: 10px;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-files-header-icon-button.is-plain {
          width: 28px;
          height: 28px;
          border-radius: 9px;
        }
  
        .playground-agents-list-switch-row {
          width: 100%;
        }
  
        .playground-agents-list-switch {
          display: flex;
          width: 100%;
          max-width: none;
          gap: 8px;
          padding: 2px;
          border: 1px solid rgba(255, 255, 255, 0.05);
          border-radius: 20px;
          background: rgba(255, 255, 255, 0.1);
          line-height: 1rem;
          position: relative;
          overflow: hidden;
        }
  
        .playground-agents-list-switch .content-mode-button {
          flex: 1;
          min-width: 0;
          padding: 4px 12px;
          font-size: 10px;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.72);
        }
  
        .playground-agents-list-switch .content-mode-button.is-active {
          background: rgba(255, 255, 255, 0.25);
          color: white;
        }
  
        .playground-environments-list-body {
          min-height: 0;
          flex: 1;
          overflow: auto;
          padding: 4px 12px 18px;
          display: flex;
          flex-direction: column;
          gap: 4px;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-list-body {
          scrollbar-width: none;
          -ms-overflow-style: none;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-list-body::-webkit-scrollbar {
          display: none;
          width: 0;
          height: 0;
        }
  
        .playground-environments-list-item {
          width: 100%;
          display: flex;
          align-items: center;
          gap: 10px;
          min-height: 56px;
          padding: 10px 12px;
          border: 0;
          border-radius: 14px;
          background: transparent;
          color: rgba(255, 255, 255, 0.92);
          text-align: left;
          cursor: pointer;
          position: relative;
          transition: background-color 160ms ease;
        }
  
        .playground-environments-list-item.has-actions {
          padding-right: 44px;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-list-item {
          gap: 14px;
          min-height: 38px;
          padding: 8px 12px;
          border-radius: 10px;
        }
  
        .playground-environments-list-item:hover {
          background: rgba(255, 255, 255, 0.06);
        }
  
        .playground-environments-list-item.is-active {
          background: rgba(255, 255, 255, 0.1);
        }
  
        .playground-environments-list-item-row {
          position: relative;
        }
  
        .playground-environments-list-item-side {
          position: absolute;
          top: 50%;
          right: 8px;
          display: flex;
          align-items: center;
          justify-content: flex-end;
          min-width: 24px;
          height: 24px;
          transform: translateY(-50%);
        }
  
        .playground-environments-list-item-menu-button {
          width: 24px;
          height: 24px;
          flex-shrink: 0;
          padding: 0;
          border: 0;
          border-radius: 8px;
          background: transparent;
          color: rgba(255, 255, 255, 0.58);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          opacity: 0;
          pointer-events: none;
          transition: background-color 160ms ease, color 160ms ease, opacity 160ms ease;
        }
  
        .playground-environments-list-item-menu-button:hover,
        .playground-environments-list-item-menu-button.is-open {
          background: rgba(255, 255, 255, 0.1);
          color: #fff;
        }
  
        .playground-environments-list-item-row:hover .playground-environments-list-item-menu-button,
        .playground-environments-list-item-row:focus-within .playground-environments-list-item-menu-button,
        .playground-environments-list-item-row.is-active .playground-environments-list-item-menu-button,
        .playground-environments-list-item-menu-button.is-open {
          opacity: 1;
          pointer-events: auto;
        }
  
        .playground-environments-list-item-icon {
          width: 16px;
          height: 16px;
          flex-shrink: 0;
          color: rgba(255, 255, 255, 0.72);
        }
  
        .playground-environments-list-item-copy {
          min-width: 0;
          flex: 1;
          display: flex;
          flex-direction: column;
          gap: 3px;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-list-item-copy {
          align-items: center;
          flex-direction: row;
          gap: 0;
        }
  
        .playground-environments-list-item-title {
          font-size: 13px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.98);
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-list-item-title {
          font-size: 12px;
          font-weight: 500;
          line-height: 1.2;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-list-item-icon {
          width: 14px;
          height: 14px;
        }
  
        .playground-environments-list-item-meta {
          font-size: 11px;
          color: rgba(255, 255, 255, 0.44);
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-list-item-meta {
          font-size: 10px;
        }
  
        .playground-environments-badge {
          flex-shrink: 0;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          min-height: 22px;
          padding: 0 8px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.1);
          color: rgba(255, 255, 255, 0.86);
          font-size: 10px;
          font-weight: 400;
          letter-spacing: 0.02em;
        }
  
        .playground-environments-empty-state {
          min-height: 100%;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          gap: 12px;
          padding: 24px;
          text-align: center;
        }
  
        .playground-environments-empty-title {
          font-size: 18px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-environments-detail {
          min-width: 0;
          min-height: 0;
          display: flex;
          flex-direction: column;
          position: relative;
          z-index: 1;
        }
  
        .playground-environments-detail.has-modal-overlay {
          z-index: 120;
        }
  
        .playground-environments-detail-scroll {
          min-width: 0;
          min-height: 0;
          flex: 1;
          overflow: auto;
          padding: 22px 20px 24px;
          display: flex;
          flex-direction: column;
          gap: 14px;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-detail-scroll {
          padding: var(--playground-environments-nav-top-offset) 18px 24px;
          gap: 24px;
        }
  
        .playground-resources-page.is-computers-view .playground-environments-detail-scroll,
        .playground-resources-page.is-servers-view .playground-environments-detail-scroll,
        .playground-environments-page:not(.playground-agents-page) .playground-environments-detail-scroll {
          scrollbar-width: none;
          -ms-overflow-style: none;
        }
  
        .playground-resources-page.is-computers-view .playground-environments-detail-scroll::-webkit-scrollbar,
        .playground-resources-page.is-servers-view .playground-environments-detail-scroll::-webkit-scrollbar,
        .playground-environments-page:not(.playground-agents-page) .playground-environments-detail-scroll::-webkit-scrollbar {
          display: none;
        }
  
        .playground-environments-detail-empty {
          min-height: 100%;
        }
  
        .playground-environments-detail-header {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 16px;
        }
  
        .playground-environments-detail-header-copy {
          min-width: 0;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-detail-header-copy {
          flex: 1;
          display: flex;
          flex-direction: column;
          gap: 6px;
        }
  
        .playground-environments-detail-title-row {
          display: flex;
          align-items: center;
          gap: 10px;
          min-width: 0;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-detail-title-row {
          min-height: var(--playground-environments-nav-row-height);
          padding-left: 2px;
        }
  
        .playground-environments-detail-title {
          min-width: 0;
          font-size: 26px;
          line-height: 1.1;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.98);
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-detail-title {
          font-size: var(--playground-environments-detail-title-size);
          line-height: 1;
          letter-spacing: -0.01em;
        }
  
        .playground-environments-detail-subtitle {
          margin-top: 8px;
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.5);
          word-break: break-word;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-detail-subtitle {
          margin-top: 0;
          font-size: 11px;
        }
  
        .playground-environments-detail-actions {
          flex-shrink: 0;
          display: inline-flex;
          flex-wrap: wrap;
          justify-content: flex-end;
          gap: 8px;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-detail-actions {
          min-height: var(--playground-environments-nav-row-height);
          align-items: center;
          gap: 6px;
        }
  
        .playground-environments-editor-main {
          min-width: 0;
          min-height: 0;
          flex: 1;
          display: flex;
          flex-direction: column;
          overflow: hidden;
        }
  
        .playground-servers-editor-layout {
          min-width: 0;
          min-height: 0;
          flex: 1;
          display: grid;
          grid-template-columns: minmax(0, 1fr) 0;
          gap: 0;
          box-sizing: border-box;
          padding-bottom: 72px;
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-servers-editor-layout {
          padding-bottom: 0;
        }
  
        .playground-servers-editor-layout.has-preview {
          grid-template-columns: minmax(0, 1fr) minmax(360px, 420px);
          padding-bottom: 0;
        }
  
        .playground-servers-editor-main-column {
          min-width: 0;
          min-height: 0;
          flex: 1;
          display: flex;
          flex-direction: column;
          overflow: hidden;
        }
  
        .playground-servers-editor-topbar {
          min-width: 0;
          flex-shrink: 0;
          display: grid;
          grid-template-columns: minmax(0, 1fr);
          gap: 0;
        }
  
        .playground-servers-editor-topbar.has-preview {
          grid-template-columns: minmax(0, 1fr) minmax(360px, 420px);
        }
  
        .playground-servers-editor-topbar .playground-content-nav {
          min-width: 0;
        }
  
        .playground-servers-editor-sidebar {
          min-width: 0;
          min-height: 0;
          display: flex;
          flex-direction: column;
          padding: 14px 18px 18px 22px;
          border-left: 1px solid rgba(255, 255, 255, 0.08);
          overflow: hidden;
        }
  
        .playground-servers-editor-layout.has-preview .playground-servers-editor-sidebar {
          position: fixed;
          top: 0;
          right: 0;
          bottom: 0;
          z-index: 40;
          width: min(420px, 60vw);
          height: auto;
          padding: 0;
          background: rgba(0, 0, 0, 0.5);
        }
  
        .playground-servers-editor-sidebar-topnav {
          border-left: 1px solid rgba(255, 255, 255, 0.08);
          padding-left: 22px;
        }
  
        .playground-servers-editor-sidebar > .playground-servers-editor-sidebar-topnav {
          flex: 0 0 auto;
          border-left: 0;
          padding-left: 18px;
          padding-right: 18px;
        }
  
        .playground-environments-editor-navbar {
          flex-shrink: 0;
        }
  
        .playground-environments-editor-navbar-title {
          min-width: 0;
          flex: 1;
          display: flex;
          align-items: center;
          gap: 0;
        }
  
        .playground-skills-page .playground-environments-editor-navbar-title {
          min-height: var(--playground-environments-nav-row-height);
          padding-left: 2px;
        }
  
        .playground-skills-detail-page {
          min-width: 0;
          min-height: 0;
        }
  
        .playground-skills-detail-page > .playground-environments-detail-scroll.playground-environments-editor-scroll {
          padding: 42px 44px 56px;
          overflow-x: hidden;
          overflow-y: auto;
        }
  
        .playground-skills-detail-navbar-icon-shell {
          position: relative;
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          min-height: var(--playground-environments-nav-row-height);
          margin-right: 4px;
        }
  
        .playground-skills-detail-navbar-icon-button {
          width: 28px;
          height: 28px;
          padding: 0;
          border: 0;
          border-radius: 10px;
          background: transparent;
          color: rgba(255, 255, 255, 0.96);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          transition: background-color 160ms ease, color 160ms ease;
        }
  
        .playground-skills-detail-navbar-icon-button:hover,
        .playground-skills-detail-navbar-icon-button.is-active {
          background: rgba(255, 255, 255, 0.08);
        }
  
        .playground-skills-detail-navbar-icon-button.is-readonly {
          cursor: default;
        }
  
        .playground-skills-detail-navbar-icon {
          width: 18px;
          height: 18px;
          flex: 0 0 auto;
        }
  
        .playground-skills-page .playground-environments-editor-navbar-copy {
          min-height: var(--playground-environments-nav-row-height);
          justify-content: center;
        }
  
        .playground-tools-navbar-title .playground-environments-editor-navbar-copy {
          justify-content: flex-start;
        }
  
        .playground-resources-page .playground-environments-editor-navbar-title {
          min-height: var(--playground-environments-nav-row-height);
          padding-left: 2px;
        }
  
        .playground-skills-detail-content {
          width: min(100%, var(--playground-centered-page-max-width));
          max-width: var(--playground-centered-page-max-width);
          margin: 0 auto;
          min-height: auto;
          display: flex;
          flex-direction: column;
          gap: 18px;
        }
  
        .playground-plugin-detail-page {
          min-width: 0;
          min-height: 0;
        }
  
        .playground-plugin-detail-content {
          width: min(100%, var(--playground-centered-page-max-width));
          max-width: var(--playground-centered-page-max-width);
          margin: 0 auto;
          display: flex;
          flex-direction: column;
          gap: 18px;
        }
  
        .playground-plugin-detail-header {
          display: flex;
          align-items: center;
          gap: 16px;
          margin: 24px 0 6px;
        }
  
        .playground-plugin-detail-header-icon-shell {
          position: relative;
          width: 44px;
          height: 44px;
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border-radius: 14px;
          background: rgba(255, 255, 255, 0.05);
          border: 0;
          color: rgba(255, 255, 255, 0.96);
          backdrop-filter: blur(50px);
          -webkit-backdrop-filter: blur(50px);
          overflow: hidden;
        }
  
        .playground-plugin-detail-header-icon-shell::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          border-radius: 14px;
          padding: 1px;
          background: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-plugin-detail-header-icon-shell .playground-plugin-row-logo {
          width: 22px;
          height: 22px;
        }
  
        .playground-plugin-detail-description {
          margin-bottom: 4px;
        }
  
        .playground-plugin-detail-heading-group {
          min-width: 0;
          flex: 1 1 auto;
          width: 100%;
          display: flex;
          flex-direction: column;
          justify-content: center;
          gap: 4px;
        }
  
        .playground-plugin-detail-title {
          margin: 0;
          font-size: inherit;
          line-height: inherit;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-plugin-detail-title-main {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          gap: 12px;
        }
  
        .playground-plugin-detail-title-row {
          margin-top: 0;
        }
  
        .playground-plugin-detail-connect-button.is-destructive {
          color: rgba(255, 255, 255, 0.72);
        }
  
        .playground-plugin-detail-connect-button.is-destructive:hover {
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-plugin-detail-connect-button,
        .playground-plugin-detail-connection-panel.playground-plugin-detail-surface,
        .playground-plugin-detail-permission-panel.playground-plugin-detail-surface,
        .playground-plugin-detail-tutorial-card.playground-plugin-detail-surface,
        .playground-plugin-detail-table.playground-plugin-detail-surface {
          --playground-plugin-detail-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          position: relative;
          z-index: 0;
          overflow: hidden;
          border: 0;
          background: transparent;
        }
  
        .playground-plugin-detail-connect-button::before,
        .playground-plugin-detail-connection-panel.playground-plugin-detail-surface::before,
        .playground-plugin-detail-permission-panel.playground-plugin-detail-surface::before,
        .playground-plugin-detail-tutorial-card.playground-plugin-detail-surface::before,
        .playground-plugin-detail-table.playground-plugin-detail-surface::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          z-index: 2;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-plugin-detail-border);
          -webkit-mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          -webkit-mask-clip: content-box, border-box;
          -webkit-mask-composite: xor;
          -webkit-mask-origin: content-box, border-box;
          -webkit-mask-repeat: repeat, repeat;
          -webkit-mask-size: auto, auto;
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-plugin-detail-connect-button > *,
        .playground-plugin-detail-connection-panel .playground-plugin-detail-info-compact,
        .playground-plugin-detail-permission-panel .playground-plugin-detail-permission-list,
        .playground-plugin-detail-tutorial-card > *,
        .playground-plugin-detail-table .playground-plugin-detail-table-row {
          position: relative;
          z-index: 3;
        }
  
        .playground-plugin-detail-subtitle {
          margin: 0;
          min-width: 0;
          flex: 1 1 320px;
          font-size: 13px;
          line-height: 1.55;
          color: rgba(255, 255, 255, 0.56);
        }
  
        .playground-plugin-detail-meta-row {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          flex-wrap: nowrap;
          width: 100%;
        }
  
        .playground-plugin-detail-meta-actions {
          flex: 0 0 auto;
          margin-left: auto;
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          gap: 18px;
          flex-wrap: nowrap;
        }
  
        .playground-plugin-detail-meta-actions .playground-plugin-modal-button {
          min-height: 36px;
          font-size: 12px;
        }
  
        .playground-plugin-detail-status-badge {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          font-size: 12px;
          line-height: 1.35;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.52);
        }
  
        .playground-plugin-detail-stack {
          display: flex;
          flex-direction: column;
          gap: 18px;
        }
  
        .playground-plugin-detail-section {
          display: flex;
          flex-direction: column;
          gap: 14px;
        }
  
        .playground-plugin-detail-included-actions {
          margin-bottom: 24px;
        }
  
        .playground-plugin-detail-tabs.playground-agents-overview-tabs {
          margin-top: 0;
          margin-bottom: 0;
        }
  
        .playground-plugin-detail-tutorial {
          display: flex;
          flex-direction: column;
          gap: 18px;
        }
  
        .playground-plugin-detail-tutorial-grid {
          display: grid;
          grid-template-columns: repeat(3, minmax(0, 1fr));
          gap: 12px;
        }
  
        .playground-plugin-detail-tutorial-card.playground-plugin-detail-surface {
          min-height: 150px;
          padding: 16px;
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-plugin-detail-tutorial-title {
          font-size: 13px;
          line-height: 1.3;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.94);
        }
  
        .playground-plugin-detail-tutorial-copy {
          font-size: 12px;
          line-height: 1.6;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.58);
        }
  
        .playground-plugin-detail-section-title {
          margin: 0;
          font-size: 13px;
          line-height: 1;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.94);
        }
  
        .playground-plugin-detail-surface-title {
          margin-top: 12px;
        }
  
  	      .playground-plugin-detail-copy {
  	        margin: 0;
  	        font-size: 12px;
  	        line-height: 1.65;
  	        color: rgba(255, 255, 255, 0.7);
  	      }
  
  	      .playground-plugin-detail-workflow-card {
  	        position: relative;
  	        overflow: hidden;
  	        display: grid;
  	        grid-template-columns: minmax(160px, 220px) minmax(0, 1fr);
  	        align-items: center;
  	        gap: 18px;
  	        min-height: 58px;
  	        padding: 14px 16px;
  	        border-radius: 10px;
  	        background: rgba(255, 255, 255, 0.05);
  	      }
  
  	      .playground-plugin-detail-workflow-card::before {
  	        content: "";
  	        pointer-events: none;
  	        position: absolute;
  	        inset: 0;
  	        z-index: 2;
  	        border-radius: inherit;
  	        padding: 1px;
  	        background: linear-gradient(
  	          -10deg,
  	          rgba(200, 200, 200, 0.2),
  	          rgba(255, 255, 255, 0.08),
  	          rgba(255, 255, 255, 0.12),
  	          rgba(255, 255, 255, 0.28)
  	        );
  	        mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
  	        mask-clip: content-box, border-box;
  	        mask-composite: exclude;
  	        mask-origin: content-box, border-box;
  	        mask-repeat: repeat, repeat;
  	        mask-size: auto, auto;
  	      }
  
  	      .playground-plugin-detail-workflow-label {
  	        position: relative;
  	        z-index: 3;
  	        font-size: 12px;
  	        line-height: 1.35;
  	        font-weight: 500;
  	        color: rgba(255, 255, 255, 0.92);
  	      }
  
  	      .playground-plugin-detail-workflow-copy {
  	        position: relative;
  	        z-index: 3;
  	        font-size: 12px;
  	        line-height: 1.5;
  	        font-weight: 400;
  	        color: rgba(255, 255, 255, 0.56);
  	      }
  
        .playground-plugin-detail-prompt-card {
          position: relative;
          overflow: hidden;
          min-height: 140px;
          padding: 26px 24px;
          border-radius: 15px;
          display: flex;
          align-items: center;
          justify-content: center;
          background:
            radial-gradient(circle at 18% 20%, rgba(255, 255, 255, 0.14), transparent 30%),
            radial-gradient(circle at 82% 26%, rgba(193, 178, 255, 0.16), transparent 36%),
            linear-gradient(135deg, rgba(31, 45, 115, 0.96), rgba(53, 47, 138, 0.96) 52%, rgba(111, 101, 181, 0.88));
        }
  
        .playground-plugin-detail-prompt-card::before {
          content: "";
          position: absolute;
          inset: 0;
          background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0));
          pointer-events: none;
        }
  
        .playground-plugin-detail-carousel {
          position: relative;
          overflow: hidden;
          min-height: 300px;
          padding: 28px;
          border-radius: 15px;
          background-image:
            linear-gradient(180deg, rgba(255,255,255,0.18), rgba(255,255,255,0.04)),
            url('/img/bg/clouds.jpeg');
          background-size: cover;
          background-position: center;
        }
  
        .playground-plugin-detail-carousel::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          background:
            radial-gradient(circle at 20% 20%, rgba(255,255,255,0.12), transparent 30%),
            linear-gradient(110deg, rgba(3, 7, 18, 0.72), rgba(20, 24, 43, 0.34) 48%, rgba(5, 8, 16, 0.78));
        }
  
        .playground-plugin-detail-carousel-copy,
        .playground-plugin-detail-carousel-stage,
        .playground-plugin-detail-carousel-controls,
        .playground-plugin-detail-carousel-dots {
          position: relative;
          z-index: 1;
        }
  
        .playground-plugin-detail-carousel-stage {
          min-height: 224px;
        }
  
        .playground-plugin-detail-carousel-slide {
          min-height: 224px;
          display: flex;
          align-items: center;
          will-change: transform, opacity;
        }
  
        .playground-plugin-detail-carousel-slide.is-outgoing {
          position: absolute;
          inset: 0;
          pointer-events: none;
        }
  
        .playground-plugin-detail-carousel-slide.is-incoming.is-forward {
          animation: playgroundPluginCapabilityInForward 360ms cubic-bezier(0.2, 0.8, 0.2, 1) both;
        }
  
        .playground-plugin-detail-carousel-slide.is-outgoing.is-forward {
          animation: playgroundPluginCapabilityOutForward 360ms cubic-bezier(0.2, 0.8, 0.2, 1) both;
        }
  
        .playground-plugin-detail-carousel-slide.is-incoming.is-backward {
          animation: playgroundPluginCapabilityInBackward 360ms cubic-bezier(0.2, 0.8, 0.2, 1) both;
        }
  
        .playground-plugin-detail-carousel-slide.is-outgoing.is-backward {
          animation: playgroundPluginCapabilityOutBackward 360ms cubic-bezier(0.2, 0.8, 0.2, 1) both;
        }
  
        @keyframes playgroundPluginCapabilityInForward {
          from {
            opacity: 0;
            transform: translateX(115%);
          }
          to {
            opacity: 1;
            transform: translateX(0);
          }
        }
  
        @keyframes playgroundPluginCapabilityOutForward {
          from {
            opacity: 1;
            transform: translateX(0);
          }
          to {
            opacity: 0;
            transform: translateX(-115%);
          }
        }
  
        @keyframes playgroundPluginCapabilityInBackward {
          from {
            opacity: 0;
            transform: translateX(-115%);
          }
          to {
            opacity: 1;
            transform: translateX(0);
          }
        }
  
        @keyframes playgroundPluginCapabilityOutBackward {
          from {
            opacity: 1;
            transform: translateX(0);
          }
          to {
            opacity: 0;
            transform: translateX(115%);
          }
        }
  
        .playground-plugin-detail-carousel-copy {
          min-width: 0;
          max-width: min(100%, 620px);
          display: flex;
          flex-direction: column;
          gap: 12px;
        }
  
        .playground-plugin-detail-carousel-eyebrow {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          width: fit-content;
          padding: 5px 10px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.72);
          font-size: 10px;
          line-height: 1;
          font-weight: 400;
          text-transform: uppercase;
          letter-spacing: 0;
        }
  
        .playground-plugin-detail-carousel-title {
          margin: 0;
          max-width: 520px;
          font-size: 26px;
          line-height: 1.12;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-plugin-detail-carousel-description {
          margin: 0;
          max-width: 540px;
          font-size: 12px;
          line-height: 1.7;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.66);
        }
  
        .playground-plugin-detail-carousel-learn {
          width: fit-content;
          min-height: 32px;
          margin-top: 4px;
          padding: 0 14px;
          border: 0;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.94);
          color: rgba(0, 0, 0, 0.9);
          font-size: 12px;
          line-height: 1;
          font-weight: 400;
          cursor: pointer;
        }
  
        .playground-plugin-detail-carousel-learn:hover {
          background: #fff;
        }
  
        .playground-plugin-detail-carousel-status {
          width: fit-content;
        }
  
        .playground-plugin-detail-carousel-controls {
          position: absolute;
          right: 22px;
          bottom: 18px;
          display: inline-flex;
          align-items: center;
          gap: 8px;
        }
  
        .playground-plugin-detail-carousel-nav {
          width: 30px;
          height: 30px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border: 0;
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.84);
          cursor: pointer;
        }
  
        .playground-plugin-detail-carousel-nav:hover {
          background: transparent;
          color: #fff;
        }
  
        .playground-plugin-detail-carousel-dots {
          position: absolute;
          left: 28px;
          bottom: 26px;
          display: inline-flex;
          align-items: center;
          gap: 6px;
        }
  
        .playground-plugin-detail-carousel-dot {
          width: 6px;
          height: 6px;
          border: 0;
          border-radius: 999px;
          padding: 0;
          background: rgba(255, 255, 255, 0.28);
          cursor: pointer;
        }
  
        .playground-plugin-detail-carousel-dot.is-active {
          width: 18px;
          background: rgba(255, 255, 255, 0.86);
        }
  
        .playground-plugin-detail-prompt-pill {
          position: relative;
          z-index: 1;
          display: inline-flex;
          align-items: center;
          gap: 10px;
          max-width: min(100%, 620px);
          min-height: 42px;
          padding: 10px 14px;
          border-radius: 10px;
          background: rgba(17, 17, 24, 0.68);
          color: rgba(255, 255, 255, 0.94);
          box-shadow: inset 0 0 0 1px rgba(255,255,255,0.08);
          backdrop-filter: blur(22px);
          -webkit-backdrop-filter: blur(22px);
        }
  
        .playground-plugin-detail-prompt-logo {
          width: 16px;
          height: 16px;
          flex: 0 0 auto;
          object-fit: contain;
        }
  
        .playground-plugin-detail-prompt-logo-shell {
          width: 18px;
          height: 18px;
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          color: rgba(255, 255, 255, 0.86);
        }
  
        .playground-plugin-detail-prompt-copy {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          gap: 8px;
          flex-wrap: wrap;
        }
  
        .playground-plugin-detail-prompt-plugin {
          color: rgba(213, 192, 255, 0.94);
          font-size: 12px;
          font-weight: 500;
        }
  
        .playground-plugin-detail-prompt-text {
          min-width: 0;
          font-size: 12px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.92);
        }
  
        .playground-plugin-detail-surface {
          border-radius: 10px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          background: rgba(255, 255, 255, 0.05);
          overflow: hidden;
        }
  
  	      .playground-plugin-detail-capability-grid {
  	        display: grid;
  	        grid-template-columns: repeat(4, minmax(0, 1fr));
  	        gap: 12px;
  	      }
  
        .playground-plugin-detail-capability-card {
          position: relative;
          min-width: 0;
          min-height: 138px;
          display: flex;
          flex-direction: column;
          gap: 10px;
          padding: 15px;
          border-radius: 12px;
          background: rgba(255, 255, 255, 0.05);
          overflow: hidden;
        }
  
        .playground-plugin-detail-capability-card::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          z-index: 2;
          border-radius: inherit;
          padding: 1px;
          background: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.2),
            rgba(255, 255, 255, 0.08),
            rgba(255, 255, 255, 0.12),
            rgba(255, 255, 255, 0.28)
          );
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-plugin-detail-capability-card.is-unsupported {
          opacity: 0.66;
        }
  
        .playground-plugin-detail-capability-card-top {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 10px;
        }
  
        .playground-plugin-detail-capability-icon {
          width: 28px;
          height: 28px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.06);
          color: rgba(255, 255, 255, 0.82);
        }
  
        .playground-plugin-detail-status-pill {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          min-height: 22px;
          padding: 0 9px;
          border-radius: 999px;
          font-size: 10px;
          line-height: 1;
          font-weight: 400;
          white-space: nowrap;
          color: rgba(255, 255, 255, 0.7);
          background: rgba(255, 255, 255, 0.07);
        }
  
        .playground-plugin-detail-status-pill.is-connected {
          color: rgba(187, 247, 208, 0.96);
          background: rgba(34, 197, 94, 0.16);
        }
  
        .playground-plugin-detail-status-pill.is-pending,
        .playground-plugin-detail-status-pill.is-available {
          color: rgba(255, 231, 153, 0.96);
          background: rgba(234, 179, 8, 0.15);
        }
  
        .playground-plugin-detail-status-pill.is-inactive,
        .playground-plugin-detail-status-pill.is-muted {
          color: rgba(255, 255, 255, 0.46);
          background: rgba(255, 255, 255, 0.06);
        }
  
        .playground-plugin-detail-capability-title {
          font-size: 13px;
          line-height: 1.25;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.94);
        }
  
        .playground-plugin-detail-capability-description {
          font-size: 12px;
          line-height: 1.55;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.56);
        }
  
        .playground-plugin-detail-control-grid {
          display: grid;
          grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
          gap: 12px;
        }
  
        .playground-plugin-detail-connection-panel,
        .playground-plugin-detail-permission-panel {
          padding: 16px;
        }
  
        .playground-plugin-detail-connection-panel.playground-plugin-detail-surface {
          padding: 0;
        }
  
        .playground-plugin-detail-permission-panel.playground-plugin-detail-surface {
          padding: 0;
        }
  
        .playground-plugin-detail-connection-head {
          display: flex;
          align-items: flex-start;
          gap: 12px;
          padding-bottom: 14px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-plugin-detail-status-dot {
          width: 9px;
          height: 9px;
          flex: 0 0 auto;
          margin-top: 4px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.24);
        }
  
        .playground-plugin-detail-status-dot.is-connected {
          background: #22c55e;
          box-shadow: 0 0 0 4px rgba(34, 197, 94, 0.14);
        }
  
        .playground-plugin-detail-status-dot.is-pending,
        .playground-plugin-detail-status-dot.is-available {
          background: #eab308;
          box-shadow: 0 0 0 4px rgba(234, 179, 8, 0.12);
        }
  
        .playground-plugin-detail-connection-copy {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 4px;
        }
  
        .playground-plugin-detail-connection-title,
        .playground-plugin-detail-permission-title {
          font-size: 13px;
          line-height: 1.3;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.94);
        }
  
        .playground-plugin-detail-connection-subtitle {
          font-size: 12px;
          line-height: 1.5;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.54);
        }
  
        .playground-plugin-detail-info-compact {
          margin: 8px -16px -4px;
        }
  
        .playground-plugin-detail-connection-panel .playground-plugin-detail-info-compact {
          margin: 0;
        }
  
        .playground-plugin-detail-permission-head {
          min-height: 35px;
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          padding-bottom: 10px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-plugin-detail-permission-list {
          display: flex;
          flex-direction: column;
          margin: 0;
        }
  
        .playground-plugin-detail-permission-row {
          display: flex;
          gap: 12px;
          padding: 13px 16px;
        }
  
        .playground-plugin-detail-permission-row + .playground-plugin-detail-permission-row {
          border-top: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-plugin-detail-permission-icon {
          width: 24px;
          height: 24px;
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border-radius: 999px;
          color: rgba(255, 255, 255, 0.48);
          background: rgba(255, 255, 255, 0.06);
        }
  
        .playground-plugin-detail-permission-icon.is-connected {
          color: rgba(187, 247, 208, 0.96);
          background: rgba(34, 197, 94, 0.14);
        }
  
        .playground-plugin-detail-permission-icon.is-pending,
        .playground-plugin-detail-permission-icon.is-available {
          color: rgba(255, 231, 153, 0.96);
          background: rgba(234, 179, 8, 0.13);
        }
  
        .playground-plugin-detail-permission-copy {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 3px;
        }
  
        .playground-plugin-detail-permission-name {
          font-size: 12px;
          line-height: 1.35;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.9);
        }
  
        .playground-plugin-detail-permission-description {
          font-size: 11px;
          line-height: 1.5;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.5);
        }
  
        .playground-plugin-detail-table {
          display: flex;
          flex-direction: column;
        }
  
        .playground-plugin-detail-table-row {
          display: grid;
          grid-template-columns: minmax(0, 1fr);
          align-items: center;
          gap: 16px;
          min-height: 72px;
          padding: 0 16px;
        }
  
        .playground-plugin-detail-table-row + .playground-plugin-detail-table-row {
          border-top: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-plugin-detail-feature-main {
          min-width: 0;
          display: flex;
          align-items: flex-start;
          gap: 12px;
        }
  
        .playground-plugin-detail-feature-icon {
          width: 26px;
          height: 26px;
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border-radius: 999px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.52);
        }
  
        .playground-plugin-detail-feature-copy {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 2px;
        }
  
        .playground-plugin-detail-feature-title-row {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          gap: 6px;
          flex-wrap: wrap;
        }
  
        .playground-plugin-detail-feature-title {
          min-width: 0;
          font-size: 13px;
          line-height: 1.35;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.94);
        }
  
        .playground-plugin-detail-feature-kind {
          font-size: 10px;
          line-height: 1;
          color: rgba(255, 255, 255, 0.4);
        }
  
        .playground-plugin-detail-feature-description {
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.56);
        }
  
        .playground-plugin-detail-feature-toggle {
          flex: 0 0 auto;
          pointer-events: none;
        }
  
        .playground-plugin-detail-feature-toggle.toggle-button {
          width: 42px;
          height: 24px;
        }
  
        .playground-plugin-detail-feature-toggle.toggle-button::after {
          top: 2px;
          left: 2px;
          width: 18px;
          height: 18px;
        }
  
        .playground-plugin-detail-feature-toggle.toggle-button[data-active="true"]::after {
          transform: translateX(18px);
        }
  
        .playground-plugin-detail-info-row {
          display: grid;
          grid-template-columns: minmax(120px, 140px) minmax(0, 1fr);
          gap: 16px;
          align-items: center;
          min-height: 40px;
          padding: 0 16px;
        }
  
        .playground-plugin-detail-info-row + .playground-plugin-detail-info-row {
          border-top: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-plugin-detail-info-label {
          font-size: 11px;
          line-height: 1.35;
          color: rgba(255, 255, 255, 0.42);
        }
  
        .playground-plugin-detail-info-value {
          min-width: 0;
          font-size: 12px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.88);
        }
  
        .playground-plugin-detail-info-link {
          display: inline-flex;
          align-items: center;
          gap: 6px;
          color: #60a5fa;
          text-decoration: none;
        }
  
        .playground-plugin-detail-info-link:hover {
          color: #93c5fd;
        }
  
        .playground-plugin-detail-info-link-text {
          min-width: 0;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-plugin-detail-setup {
          display: flex;
          flex-direction: column;
          gap: 14px;
        }
  
  	      @media (max-width: 1100px) {
  	        .playground-plugin-detail-capability-grid {
  	          grid-template-columns: repeat(2, minmax(0, 1fr));
  	        }
  	      }
  
  	      @media (max-width: 900px) {
  	        .playground-plugin-detail-capability-grid,
  	        .playground-plugin-detail-control-grid {
  	          grid-template-columns: 1fr;
  	        }
  
  	        .playground-plugin-detail-workflow-card {
  	          grid-template-columns: 1fr;
  	          align-items: flex-start;
  	          gap: 6px;
  	        }
  	      }
  
        .playground-skills-detail-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 16px;
          margin: 0 0 4px;
        }
  
        .playground-skills-detail-header-main {
          display: flex;
          align-items: center;
          gap: 16px;
          min-width: 0;
        }
  
        .playground-skills-detail-header-icon-shell {
          position: relative;
          width: 44px;
          height: 44px;
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border-radius: 14px;
          background: rgba(255, 255, 255, 0.05);
          border: 0;
          color: rgba(255, 255, 255, 0.96);
          backdrop-filter: blur(50px);
          -webkit-backdrop-filter: blur(50px);
        }
  
        .playground-skills-detail-header-icon-shell::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          border-radius: inherit;
          padding: 1px;
          background: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-skills-detail-header-icon {
          width: 22px;
          height: 22px;
          flex: 0 0 auto;
        }
  
  	      .playground-skills-detail-enable-button {
  	        flex: 0 0 auto;
  	      }
  
        .sidebar-thread-rename-modal.playground-skills-edit-modal {
          width: min(560px, calc(100vw - 32px));
          gap: 14px;
        }
  
        .playground-skills-edit-field,
        .playground-skills-edit-description {
          display: flex;
          flex-direction: column;
          gap: 8px;
        }
  
        .playground-skills-edit-label {
          color: rgba(255, 255, 255, 0.72);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.3;
        }
  
        .playground-skills-edit-title-input {
          font-size: 12px;
        }
  
        .playground-skills-edit-description .playground-tasks-detail-section-header {
          margin-bottom: 0;
        }
  
        .playground-skills-edit-description .playground-tasks-detail-section-title {
          font-size: 12px;
        }
  
        .playground-skills-edit-description-editor {
          min-height: 148px;
          border-radius: 10px;
          background: rgba(255, 255, 255, 0.06);
          overflow: hidden;
        }
  
        .playground-skills-edit-description-input {
          min-height: 148px !important;
          max-height: 260px;
          padding: 12px !important;
          color: rgba(255, 255, 255, 0.78) !important;
          font-size: 12px !important;
          line-height: 1.5 !important;
          resize: vertical;
          overflow: auto;
        }
  
        .playground-resources-detail-content {
          width: min(100%, var(--playground-centered-page-max-width));
          max-width: var(--playground-centered-page-max-width);
          margin: 0 auto;
        }
  
        .playground-resources-page.is-develop-server-kind-page > .playground-environments-detail-scroll.playground-settings-detail-scroll,
        .playground-resources-page.is-develop-configure-page > .playground-environments-detail-scroll.playground-settings-detail-scroll {
          padding: 42px 44px 56px;
        }
  
        .playground-resources-page.is-develop-server-kind-page > .playground-environments-detail-scroll.playground-settings-detail-scroll.is-code-tab {
          padding-bottom: 12px;
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-resources-detail-content,
        .playground-resources-page.is-develop-configure-page .playground-resources-detail-content {
          width: min(100%, var(--playground-centered-page-max-width));
          max-width: var(--playground-centered-page-max-width);
          min-height: 100%;
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-environments-editor-main.playground-tasks-detail-main,
        .playground-resources-page.is-develop-configure-page .playground-environments-editor-main.playground-tasks-detail-main {
          min-height: 0;
          overflow: visible;
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-environments-editor-main.playground-tasks-detail-main.is-code-tab {
          flex: 1 1 0;
          height: 100%;
          max-height: 100%;
          overflow: hidden;
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-environments-detail-scroll.playground-environments-editor-scroll,
        .playground-resources-page.is-develop-configure-page .playground-environments-detail-scroll.playground-environments-editor-scroll {
          padding: 0;
          overflow: visible;
        }
  
        .playground-skills-detail-content > .playground-tasks-detail-description.playground-environments-editor-description:first-child {
          margin-bottom: 22px;
        }
  
        .playground-skills-detail-page .playground-tasks-detail-description {
          min-height: 0;
          overflow: visible;
        }
  
        .playground-skills-detail-page .playground-tasks-detail-description-editor {
          display: block;
          height: auto;
          min-height: 0;
          overflow: visible;
        }
  
        .playground-skills-detail-page .playground-tasks-detail-description-editor.is-preview .playground-tasks-detail-description-preview-scope.tb-runner-chat {
          position: relative;
          inset: auto;
          display: block;
          width: 100%;
          height: auto !important;
          min-height: 0 !important;
          overflow: visible;
        }
  
        .playground-skills-detail-page .playground-tasks-detail-description-editor.is-preview .playground-tasks-detail-description-input {
          position: absolute;
          inset: 0;
          height: 100% !important;
          min-height: 100%;
          padding: 0;
          opacity: 0;
        }
  
        .playground-skills-detail-page .playground-tasks-detail-description-editor.is-editing .playground-tasks-detail-description-input {
          position: relative;
          min-height: 110px;
        }
  
        .playground-skills-detail-page .playground-tasks-detail-description-preview {
          min-height: 0;
          height: auto;
          overflow: visible;
        }
  
        .playground-skills-detail-page .playground-tasks-detail-description-preview > :first-child {
          margin-top: 0;
        }
  
        .playground-skills-detail-page .playground-tasks-detail-description-preview > :last-child {
          margin-bottom: 0;
        }
  
        .playground-skills-detail-page .playground-tasks-detail-description-preview .tb-message-markdown-list {
          margin-top: 8px;
          margin-bottom: 8px;
        }
  
        .playground-skills-detail-page .playground-tasks-detail-description-preview .tb-message-markdown-list-item + .tb-message-markdown-list-item {
          margin-top: 4px;
        }
  
        .playground-skills-detail-page .playground-tasks-detail-description + .playground-tasks-detail-description {
          margin-top: 18px;
        }
  
        .playground-skills-detail-page .playground-tasks-detail-facts {
          margin-top: 12px;
          border: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-skills-detail-tabs {
          margin: 0 0 2px;
        }
  
        .playground-skills-detail-hero-preview {
          display: flex;
          align-items: center;
          margin: 0;
        }
  
        .playground-skills-detail-hero-preview .playground-plugin-detail-carousel-copy {
          align-self: center;
          max-width: min(100%, 720px);
        }
  
        .playground-skills-detail-hero-preview .playground-plugin-detail-carousel-preview {
          min-height: 220px;
          align-self: stretch;
          display: flex;
          align-items: center;
          justify-content: center;
        }
  
        .playground-skills-detail-hero-preview .playground-plugin-detail-carousel-image {
          width: min(100%, 420px);
          min-height: 214px;
          display: flex;
          flex-direction: column;
          overflow: hidden;
          border-radius: 14px;
          background: rgba(8, 10, 18, 0.52);
          box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.08), 0 20px 46px rgba(0, 0, 0, 0.22);
          backdrop-filter: blur(26px);
          -webkit-backdrop-filter: blur(26px);
        }
  
        .playground-skills-detail-hero-preview .playground-plugin-detail-carousel-image-top {
          display: flex;
          align-items: center;
          gap: 5px;
          padding: 10px 12px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-skills-detail-hero-preview .playground-plugin-detail-carousel-image-dot {
          width: 6px;
          height: 6px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.22);
        }
  
        .playground-skills-detail-hero-preview .playground-plugin-detail-carousel-image-body {
          flex: 1;
          min-height: 0;
          display: grid;
          grid-template-columns: 0.86fr 1.14fr;
          gap: 12px;
          padding: 16px;
        }
  
        .playground-skills-detail-hero-preview .playground-plugin-detail-carousel-image-panel {
          min-height: 148px;
          border-radius: 10px;
          background:
            radial-gradient(circle at 24% 24%, rgba(102, 166, 255, 0.18), transparent 32%),
            rgba(255, 255, 255, 0.06);
        }
  
        .playground-skills-detail-hero-preview .playground-plugin-detail-carousel-image-lines {
          display: flex;
          flex-direction: column;
          gap: 9px;
          justify-content: center;
        }
  
        .playground-skills-detail-hero-preview .playground-plugin-detail-carousel-image-line {
          height: 8px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.14);
        }
  
        .playground-skills-detail-hero-preview .playground-plugin-detail-carousel-image-line:nth-child(2) {
          width: 74%;
        }
  
        .playground-skills-detail-hero-preview .playground-plugin-detail-carousel-image-line:nth-child(3) {
          width: 52%;
        }
  
        .playground-skills-detail-hero-preview .playground-plugin-detail-carousel-icon-shell {
          width: 44px;
          height: 44px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          align-self: flex-start;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.1);
          color: rgba(255, 255, 255, 0.92);
        }
  
        .playground-skills-detail-hero-image-icon {
          width: 19px;
          height: 19px;
          flex: 0 0 auto;
        }
  
        .playground-skills-detail-page.is-source-files-tab {
          flex: 1 1 0;
          min-height: 0;
          overflow: hidden;
        }
  
        .playground-skills-detail-page.is-source-files-tab .playground-environments-detail-scroll {
          min-height: 0;
          height: 100%;
          overflow: hidden;
          padding-bottom: 12px;
        }
  
        .playground-skills-detail-page.is-source-files-tab .playground-skills-detail-content {
          height: 100%;
          max-height: 100%;
          min-height: 0;
        }
  
        .playground-skills-detail-metrics.playground-environments-home-metrics {
          margin-top: 0;
          margin-bottom: 0;
          grid-template-columns: minmax(0, 1fr);
        }
  
        .playground-skills-detail-metrics > .playground-tasks-detail-facts {
          grid-column: 1 / -1;
          width: 100%;
          min-width: 0;
          margin-top: 0;
        }
  
        .playground-skills-detail-metrics .playground-skills-details-card.playground-database-overview-chart-block {
          --playground-project-overview-chart-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          position: relative;
          padding: 20px;
          border: 0;
          border-radius: 15px;
          background: transparent;
          overflow: hidden;
          -webkit-backdrop-filter: blur(50px);
          backdrop-filter: blur(50px);
        }
  
        .playground-skills-detail-metrics .playground-skills-details-card.playground-database-overview-chart-block::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          z-index: 5;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-project-overview-chart-border);
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-skills-detail-metrics .playground-skills-details-card.playground-database-overview-chart-block > * {
          position: relative;
          z-index: 1;
        }
  
        .playground-skills-detail-page .playground-skills-details-card .playground-skills-detail-fact-rows {
          margin-top: 0;
          padding-top: 0;
          border-top: 0;
        }
  
        .playground-skills-usage-card {
          --playground-project-overview-chart-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          position: relative;
          box-sizing: border-box;
          width: 100%;
          overflow: hidden;
          border-radius: 15px;
          background: transparent;
          padding: 20px;
          border: 0;
          box-shadow: none;
          display: flex;
          flex-direction: column;
          gap: 16px;
          height: auto;
          min-height: 0;
          -webkit-backdrop-filter: blur(50px);
          backdrop-filter: blur(50px);
        }
  
        .playground-skills-usage-card::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          z-index: 2;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-project-overview-chart-border);
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-skills-usage-card > * {
          position: relative;
          z-index: 1;
        }
  
        .playground-skills-usage-header {
          position: relative;
          z-index: 3;
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 18px;
          margin-bottom: 12px;
        }
  
        .playground-skills-usage-title-group {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 4px;
        }
  
        .playground-skills-usage-title {
          color: rgba(255, 255, 255, 0.96);
          font-size: 14px;
          line-height: 1.2;
          font-weight: 500;
        }
  
        .playground-skills-usage-subtitle {
          color: rgba(255, 255, 255, 0.5);
          font-size: 11px;
          line-height: 1.3;
          font-weight: 400;
        }
  
        .playground-skills-detail-metrics .playground-skills-usage-kpis {
          display: grid;
          grid-template-columns: repeat(3, minmax(0, 1fr));
          gap: 12px;
          width: calc(100% + 40px);
          min-width: 0;
          margin-top: -20px;
          margin-left: -20px;
          margin-right: -20px;
          padding: 20px;
          background: rgba(255, 255, 255, 0.05);
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-skills-detail-metrics .playground-skills-usage-kpi {
          min-width: 0;
          background: transparent;
        }
  
        .playground-skills-usage-chart {
          position: relative;
          z-index: 3;
          min-height: 152px;
        }
  
        .playground-skills-usage-card .playground-settings-usage-chart-card {
          background: transparent;
          border: 0;
          border-radius: 0;
          padding: 0;
        }
  
        .playground-skills-usage-card .playground-project-overview-chart-header {
          display: none;
          padding-top: 0;
        }
  
        .playground-skills-detail-page .playground-skills-detail-fact-rows {
          margin-top: 2px;
          padding-top: 16px;
          border-top: 1px solid rgba(255, 255, 255, 0.08);
          display: flex;
          flex-direction: column;
        }
  
        .playground-skills-detail-page .playground-skills-detail-fact-rows .playground-tasks-detail-fact:first-child {
          border-top: 0;
        }
  
        .playground-skills-detail-page .playground-skills-detail-fact-rows .playground-tasks-detail-fact {
          min-height: 44px;
        }
  
        .playground-skills-usage-svg {
          width: 100%;
          height: 152px;
          display: block;
          overflow: visible;
        }
  
        .playground-skills-usage-grid-line {
          stroke: rgba(255, 255, 255, 0.08);
          stroke-width: 1;
        }
  
        .playground-skills-usage-line {
          fill: none;
          stroke: #66a6ff;
          stroke-width: 2;
          stroke-linecap: round;
          stroke-linejoin: round;
        }
  
        .playground-skills-usage-point {
          fill: #66a6ff;
          stroke: #0a0a0b;
          stroke-width: 1;
        }
  
        .playground-skills-usage-axis-label {
          fill: rgba(255, 255, 255, 0.42);
          font-size: 10px;
          font-weight: 400;
        }
  
        .playground-skills-usage-empty {
          position: absolute;
          inset: 0;
          display: flex;
          align-items: center;
          justify-content: center;
          color: rgba(255, 255, 255, 0.5);
          font-size: 12px;
          line-height: 1.4;
          text-align: center;
          pointer-events: none;
        }
  
        .playground-skills-code-workspace {
          flex: 1 1 0;
          min-height: 0;
          height: auto;
          margin-bottom: 0;
          border: 0;
          box-shadow: 0 18px 44px rgba(0, 0, 0, 0.2);
        }
  
        .playground-skills-code-workspace::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          z-index: 5;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-project-overview-chart-border);
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-skills-code-workspace .playground-servers-code-sidebar {
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-skills-code-workspace .playground-servers-code-editor-main,
        .playground-skills-code-workspace .playground-servers-code-editor-shell {
          background: #000;
        }
  
        .playground-skills-code-workspace .playground-servers-code-editor-status-actions .playground-environments-action-button {
          position: relative;
          z-index: 0;
          overflow: hidden;
          min-height: 30px;
          border: 0;
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.96);
          font-size: 12px;
          font-weight: 400;
          -webkit-backdrop-filter: blur(50px);
          backdrop-filter: blur(50px);
        }
  
        .playground-skills-code-workspace .playground-servers-code-editor-status-actions .playground-environments-action-button::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          border-radius: inherit;
          padding: 1px;
          background: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-skills-code-workspace .playground-servers-code-editor-status-actions .playground-environments-action-button > * {
          position: relative;
          z-index: 1;
        }
  
        .playground-skills-code-workspace .playground-servers-code-editor-status-actions .playground-environments-action-button:hover:not(:disabled),
        .playground-skills-code-workspace .playground-servers-code-editor-status-actions .playground-environments-action-button.is-primary:hover:not(:disabled) {
          background: transparent;
          color: #fff;
        }
  
        @media (max-width: 900px) {
          .playground-skills-detail-hero-preview {
            grid-template-columns: minmax(0, 1fr);
          }
  
          .playground-skills-usage-header {
            flex-direction: column;
          }
  
          .playground-skills-usage-kpis {
            width: 100%;
            min-width: 0;
          }
  
          .playground-skills-code-workspace {
            grid-template-columns: 180px minmax(0, 1fr);
          }
        }
  
        .playground-tools-navbar-back-button {
          min-width: 0;
          padding: 0;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.96);
          display: inline-flex;
          align-items: center;
          gap: 8px;
          cursor: pointer;
        }
  
        .playground-tools-navbar-back-button .playground-content-title {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-skills-detail-navbar-icon-shell .playground-tasks-project-icon-picker {
          top: calc(100% + 10px);
          left: 0;
          z-index: 8;
        }
  
        .playground-environments-editor-navbar-icon {
          width: 28px;
          height: 28px;
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border-radius: 8px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.05);
          color: rgba(255, 255, 255, 0.82);
        }
  
        .playground-environments-editor-navbar-copy {
          min-width: 0;
          flex: 1;
          display: flex;
          flex-direction: column;
          gap: 0;
        }
  
        .playground-environments-editor-title-input {
          height: auto;
          padding: 0;
        }
  
        .playground-environments-editor-navbar-subtitle {
          min-width: 0;
          color: rgba(255, 255, 255, 0.46);
          font-size: 11px;
          line-height: 1.4;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-environments-editor-navbar-actions {
          align-items: center;
          gap: 6px;
          flex-wrap: wrap;
        }
  
        .playground-environments-editor-scroll {
          gap: 42px;
        }
  
        .playground-environments-home-scroll {
          gap: 18px;
          padding-bottom: 10px !important;
        }
  
        .playground-environments-home-content {
          width: min(100%, var(--playground-centered-page-max-width));
          max-width: var(--playground-centered-page-max-width);
          margin: 0 auto;
          min-height: 100%;
          flex: 1 1 auto;
          display: flex;
          flex-direction: column;
          gap: 18px;
        }
  
        .playground-environments-home-hero-title {
          position: relative;
          z-index: 1;
          margin-top: 12px;
          margin-bottom: 5px;
          max-width: 720px;
          font-size: 24px;
          line-height: 1.2;
          font-weight: 500;
          letter-spacing: -0.02em;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-environments-home-navbar.playground-content-nav {
          position: relative;
          z-index: 1;
          display: flex;
          align-items: center;
          justify-content: flex-end;
          min-height: var(--playground-environments-nav-row-height);
          gap: 0;
          padding: 10px 0 8px;
          background: transparent;
        }
  
        .playground-environments-home-navbar .playground-content-nav-right {
          margin-right: -20px;
        }
  
        .playground-environments-home-hero {
          position: relative;
          overflow: visible;
          margin: calc(-1 * var(--playground-environments-nav-top-offset)) 0 0;
          padding: 0 0 28px;
          border: 0;
          border-radius: 0;
          background: transparent;
        }
  
        .playground-environments-home-hero::before {
          content: none;
        }
  
        .playground-environments-home-hero::after {
          content: none;
        }
  
        .playground-environments-home-hero-copy {
          position: relative;
          z-index: 1;
          margin-top: 10px;
          width: 100%;
          padding-bottom: 22px;
          max-width: none;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
          font-size: 12px;
          line-height: 1.6;
          color: rgba(255, 255, 255, 0.72);
        }
  
        .playground-environments-home-summary {
          position: relative;
          z-index: 1;
          width: 100%;
          margin-top: 18px;
          margin-bottom: 32px;
        }
  
        .playground-environments-home-summary-kpis {
          width: 100%;
          display: grid;
          grid-template-columns: repeat(5, minmax(0, 1fr));
          gap: 12px;
        }
  
        .playground-environments-home-summary-kpi {
          min-width: 0;
          display: flex;
          flex-direction: column;
          align-items: flex-start;
          gap: 10px;
          padding: 12px;
          border-radius: 10px;
          border: 1px solid rgba(255, 255, 255, 0.05);
          background: rgba(255, 255, 255, 0.03);
          -webkit-backdrop-filter: blur(20px);
          backdrop-filter: blur(20px);
          text-align: left;
        }
  
        .playground-environments-home-summary-kpi-value {
          font-size: 18px;
          line-height: 1.1;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-environments-home-summary-kpi-label {
          font-size: 12px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.52);
        }
  
        .playground-environments-home-hero-actions {
          position: relative;
          z-index: 1;
          margin-top: 22px;
          display: flex;
          flex-wrap: wrap;
          gap: 10px;
        }
  
        .playground-environments-home-action-button {
          min-height: 38px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 8px;
          padding: 0 16px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.92);
          font-size: 12px;
          font-weight: 500;
          cursor: pointer;
          transition: background-color 160ms ease, border-color 160ms ease, color 160ms ease;
        }
  
        .playground-environments-home-action-button:hover {
          background: rgba(255, 255, 255, 0.09);
          color: #fff;
        }
  
        .playground-environments-home-action-button.is-primary {
          background: #fff;
          border-color: #fff;
          color: #000;
        }
  
        .playground-environments-home-action-button.is-primary:hover {
          background: rgba(255, 255, 255, 0.92);
          border-color: rgba(255, 255, 255, 0.92);
          color: #000;
        }
  
        .playground-environments-home-metrics {
          position: relative;
          z-index: 1;
          margin-top: 10px;
          margin-bottom: 16px;
          display: grid;
          grid-template-columns: minmax(0, 1fr);
          gap: 16px;
          align-items: stretch;
        }
  
        .playground-environments-home-metrics-toolbar {
          display: flex;
          align-items: center;
          justify-content: flex-end;
          margin-top: 18px;
          margin-bottom: 8px;
        }
  
        .playground-environments-home-metrics .playground-tasks-detail-facts {
          display: flex;
          min-height: 100%;
          height: 100%;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          backdrop-filter: none;
          -webkit-backdrop-filter: none;
        }
  
        .playground-environments-home-metrics .playground-tasks-detail-facts::before {
          content: none;
        }
  
        .playground-environments-home-metrics .playground-tasks-detail-facts-body {
          display: flex;
          flex: 1 1 auto;
          height: 100%;
          gap: 0;
        }
  
        .playground-environments-home-metrics .playground-database-overview {
          display: flex;
          flex: 1 1 auto;
          flex-direction: column;
          height: 100%;
        }
  
        .playground-environments-home-metrics .playground-database-overview-chart-block,
        .playground-environments-home-metrics .playground-database-overview-timeseries-card,
        .playground-environments-home-metrics .playground-database-overview-timeseries-chart {
          height: 100%;
        }
  
        .playground-database-overview-timeseries-bar {
          rx: 8px;
          ry: 8px;
        }
  
        .playground-database-overview-timeseries-bar.is-comparison-count {
          fill: var(--playground-chart-blue);
        }
  
        .playground-database-overview-timeseries-bar.is-comparison-cost {
          fill: var(--playground-chart-pink);
        }
  
        .playground-database-overview-kpi-label.is-computers .playground-database-overview-kpi-check,
        .playground-database-overview-timeseries-bar.is-computers {
          background: var(--playground-chart-blue);
          fill: var(--playground-chart-blue);
        }
  
        .playground-database-overview-kpi-label.is-webapps .playground-database-overview-kpi-check,
        .playground-database-overview-timeseries-bar.is-webapps {
          background: var(--playground-chart-green);
          fill: var(--playground-chart-green);
        }
  
        .playground-database-overview-kpi-label.is-functions .playground-database-overview-kpi-check,
        .playground-database-overview-timeseries-bar.is-functions {
          background: var(--playground-chart-yellow);
          fill: var(--playground-chart-yellow);
        }
  
        .playground-database-overview-kpi-label.is-managed .playground-database-overview-kpi-check,
        .playground-database-overview-timeseries-bar.is-managed {
          background: var(--playground-chart-pink);
          fill: var(--playground-chart-pink);
        }
  
        .playground-database-overview-kpi-label.is-resources .playground-database-overview-kpi-check {
          background: var(--playground-chart-green);
        }
  
        .playground-database-overview-timeseries-line.is-computers {
          stroke: var(--playground-chart-blue);
        }
  
        .playground-database-overview-timeseries-dot.is-computers {
          fill: var(--playground-chart-blue);
          stroke: var(--playground-chart-blue);
        }
  
        .playground-database-overview-timeseries-line.is-resources {
          stroke: var(--playground-chart-green);
        }
  
        .playground-database-overview-timeseries-dot.is-resources {
          fill: var(--playground-chart-green);
          stroke: var(--playground-chart-green);
        }
  
        .playground-environments-home-comparison-header {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 16px;
          margin-bottom: 14px;
        }
  
        .playground-environments-home-comparison-copy {
          display: flex;
          flex-direction: column;
          gap: 10px;
          flex: 1 1 auto;
          min-width: 0;
        }
  
        .playground-environments-home-comparison-title {
          font-size: 14px;
          line-height: 1.2;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-environments-home-comparison-legend {
          display: inline-flex;
          align-items: center;
          gap: 16px;
          flex-wrap: wrap;
        }
  
        .playground-environments-home-comparison-legend-item {
          display: inline-flex;
          align-items: center;
          font-size: 10px;
          line-height: 1;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.72);
          white-space: nowrap;
          gap: 6px;
        }
  
        .playground-environments-home-comparison-legend-dot {
          width: 8px;
          height: 8px;
          border-radius: 999px;
          flex: 0 0 auto;
        }
  
        .playground-environments-home-comparison-legend-dot.is-count {
          background: var(--playground-chart-blue);
        }
  
        .playground-environments-home-comparison-legend-dot.is-cost {
          background: var(--playground-chart-pink);
        }
  
        .playground-environments-home-comparison-timescale {
          position: relative;
          flex: 0 0 auto;
        }
  
        .playground-environments-home-comparison-timescale-select {
          appearance: none;
          min-height: 30px;
          padding: 0 28px 0 10px;
          border-radius: 8px;
          border: 1px solid rgba(255, 255, 255, 0.12);
          background: transparent;
          color: rgba(255, 255, 255, 0.94);
          font-size: 11px;
          font-weight: 500;
          outline: none;
          cursor: pointer;
        }
  
        .playground-environments-home-comparison-timescale::after {
          content: "";
          position: absolute;
          right: 10px;
          top: 50%;
          width: 6px;
          height: 6px;
          margin-top: -4px;
          border-right: 1.5px solid rgba(255, 255, 255, 0.72);
          border-bottom: 1.5px solid rgba(255, 255, 255, 0.72);
          transform: rotate(45deg);
          pointer-events: none;
        }
  
        .playground-project-overview-chart-shell {
          width: 100%;
          overflow: hidden;
        }
  
        .playground-agents-home-chartjs-frame {
          width: 100%;
          height: 270px;
          overflow: hidden;
        }
  
        .playground-agents-home-chartjs-canvas {
          display: block;
          width: 100% !important;
          height: 100% !important;
        }
  
        .playground-project-overview-chart-header {
          display: flex;
          flex-direction: column;
          gap: 4px;
        }
  
        .playground-project-overview-chart-header-main {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 16px;
        }
  
        .playground-project-overview-chart-header .playground-environments-home-comparison-timescale-select {
          border-radius: 999px;
        }
  
        .playground-project-overview-chart-title {
          font-size: 14px;
          line-height: 1.2;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-environments-home-comparison-header.is-simple {
          justify-content: flex-start;
        }
  
        .playground-environments-home-card-grid {
          margin-top: -20px;
          display: grid;
          grid-template-columns: repeat(3, minmax(0, 1fr));
          gap: 16px;
        }
  
        .playground-environments-home-card {
          min-width: 0;
          padding: 18px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 15px;
          background: transparent;
          backdrop-filter: blur(24px);
          -webkit-backdrop-filter: blur(24px);
          display: flex;
          flex-direction: column;
          gap: 8px;
          min-height: 164px;
          cursor: pointer;
          transition: border-color 160ms ease, background-color 160ms ease, transform 160ms ease;
        }
  
        .playground-environments-home-card:hover {
          border-color: rgba(255, 255, 255, 0.16);
          background: transparent;
          transform: translateY(-1px);
        }
  
        .playground-environments-home-card.is-command-active {
          border-color: rgba(12, 111, 255, 0.42);
          background: transparent;
        }
  
        .playground-environments-home-card-visual {
          display: flex;
          align-items: center;
          justify-content: flex-start;
          align-self: flex-start;
          color: rgba(255, 255, 255, 0.94);
          margin-bottom: auto;
        }
  
        .playground-environments-home-card-icon {
          width: 18px;
          height: 18px;
        }
  
        .playground-environments-home-card-title {
          margin-top: auto;
          font-size: 14px;
          line-height: 1.2;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.98);
          letter-spacing: -0.01em;
        }
  
        .playground-environments-home-card-copy {
          font-size: 12px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.54);
          max-width: 24ch;
        }
  
        .playground-environments-home-composer-shell,
        .playground-tasks-backlog-composer-shell {
          position: relative;
          margin-top: auto;
          padding: 0;
          background: transparent;
        }
  
        .playground-tasks-backlog-composer-shell {
          margin-bottom: 0;
        }
  
        .playground-environments-home-composer-host {
          flex: 0 0 auto;
          padding: 0 18px 10px;
        }
  
        .playground-environments-home-composer-shell .runner-inline-error {
          margin-top: 12px;
        }
  
        .playground-environments-home-recent {
          padding: 18px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 22px;
          background: rgba(255, 255, 255, 0.03);
          display: flex;
          flex-direction: column;
          gap: 14px;
        }
  
        .playground-environments-home-recent-header {
          display: flex;
          flex-direction: column;
          gap: 5px;
        }
  
        .playground-environments-home-recent-title {
          font-size: 18px;
          line-height: 1.1;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-environments-home-recent-copy {
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.5);
        }
  
        .playground-environments-home-recent-list {
          display: flex;
          flex-direction: column;
        }
  
        .playground-environments-home-recent-row {
          width: 100%;
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 16px;
          padding: 12px 0;
          border: 0;
          border-top: 1px solid rgba(255, 255, 255, 0.06);
          background: transparent;
          color: inherit;
          text-align: left;
          cursor: pointer;
        }
  
        .playground-environments-home-recent-row:first-child {
          border-top: 0;
          padding-top: 0;
        }
  
        .playground-environments-home-recent-row:last-child {
          padding-bottom: 0;
        }
  
        .playground-environments-home-recent-main {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          gap: 12px;
          flex: 1 1 auto;
        }
  
        .playground-environments-home-recent-icon-shell {
          width: 34px;
          height: 34px;
          border-radius: 12px;
          background: rgba(255, 255, 255, 0.06);
          color: rgba(255, 255, 255, 0.88);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          flex: 0 0 auto;
        }
  
        .playground-environments-home-recent-icon {
          width: 16px;
          height: 16px;
        }
  
        .playground-environments-home-recent-copy-wrap {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 4px;
        }
  
        .playground-environments-home-recent-name {
          font-size: 13px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.96);
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-environments-home-recent-subtitle {
          font-size: 11px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.48);
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-environments-home-recent-meta {
          flex: 0 0 auto;
          display: flex;
          flex-direction: column;
          align-items: flex-end;
          gap: 5px;
          text-align: right;
        }
  
        .playground-environments-home-recent-kind {
          display: inline-flex;
          align-items: center;
          min-height: 22px;
          padding: 0 9px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.06);
          color: rgba(255, 255, 255, 0.8);
          font-size: 10px;
          font-weight: 500;
        }
  
        .playground-environments-home-recent-time {
          font-size: 11px;
          color: rgba(255, 255, 255, 0.46);
        }
  
        .playground-environments-home-recent-empty {
          min-height: 120px;
          display: flex;
          align-items: center;
          justify-content: center;
          padding: 12px;
          border: 1px dashed rgba(255, 255, 255, 0.08);
          border-radius: 18px;
          color: rgba(255, 255, 255, 0.48);
          font-size: 12px;
          text-align: center;
        }
  
        @media (max-width: 1200px) {
          .playground-environments-home-metrics,
          .playground-environments-home-card-grid {
            grid-template-columns: repeat(2, minmax(0, 1fr));
          }
        }
  
        @media (max-width: 860px) {
          .playground-environments-home-hero {
            min-height: 220px;
            margin-left: -18px;
            margin-right: -18px;
            padding: 24px 22px 70px;
          }
  
          .playground-environments-home-hero-title {
            font-size: 28px;
          }
  
          .playground-environments-home-metrics,
          .playground-environments-home-card-grid {
            grid-template-columns: minmax(0, 1fr);
          }
  
          .playground-environments-home-summary-kpis {
            display: grid;
            grid-template-columns: repeat(2, minmax(0, 1fr));
            gap: 16px 12px;
          }
  
          .playground-environments-home-summary-kpi {
            align-items: flex-start;
            text-align: left;
          }
  
          .playground-environments-home-recent-row {
            align-items: flex-start;
          }
  
          .playground-environments-home-recent-meta {
            gap: 8px;
          }
        }
  
        .playground-environments-editor-notice {
          margin-top: -2px;
        }
  
        .playground-server-deployment-status-bar {
          position: fixed;
          left: 50%;
          bottom: 16px;
          z-index: 1300;
          transform: translateX(-50%);
          display: flex;
          align-items: center;
          gap: 12px;
          width: min(720px, calc(100vw - 40px));
          min-height: 44px;
          padding: 8px 10px 8px 14px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 999px;
          background: rgba(18, 18, 18, 0.92);
          color: rgba(255, 255, 255, 0.92);
          box-shadow: 0 12px 30px rgba(0, 0, 0, 0.38);
          backdrop-filter: blur(24px);
          animation: playground-server-deployment-status-slide-in 180ms ease-out both;
        }
  
        .playground-server-deployment-status-bar.is-success {
          background: rgba(18, 42, 28, 0.94);
          border-color: rgba(88, 214, 141, 0.22);
        }
  
        .playground-server-deployment-status-bar.is-error {
          background: rgba(56, 18, 22, 0.94);
          border-color: rgba(255, 112, 112, 0.24);
        }
  
        .playground-server-deployment-status-text {
          min-width: 0;
          flex: 1 1 auto;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
          font-size: 12px;
          font-weight: 500;
          line-height: 1.35;
        }
  
        .playground-server-deployment-status-progress {
          flex: 0 0 auto;
          font-size: 11px;
          color: rgba(255, 255, 255, 0.62);
        }
  
        .playground-server-deployment-status-close {
          flex: 0 0 auto;
          width: 26px;
          height: 26px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          padding: 0;
          border: 0;
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.7);
          cursor: pointer;
        }
  
        .playground-server-deployment-status-close:hover {
          color: rgba(255, 255, 255, 0.95);
          background: rgba(255, 255, 255, 0.08);
        }
  
        @keyframes playground-server-deployment-status-slide-in {
          from {
            opacity: 0;
            transform: translate(-50%, 18px);
          }
          to {
            opacity: 1;
            transform: translate(-50%, 0);
          }
        }
  
        .playground-environments-editor-facts {
          margin-top: -12px;
          padding-bottom: 4px;
        }
  
        .playground-agents-profile-section {
          display: flex;
          flex-direction: column;
          align-items: center;
          gap: 10px;
        }
  
        .playground-agents-profile-name-wrap {
          width: min(420px, 100%);
          display: flex;
          justify-content: center;
          margin-top: 2px;
        }
  
        .playground-agents-profile-name-wrap .playground-agents-profile-name-input {
          width: 100%;
          min-width: 0;
          padding: 0;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.96);
          font-size: 24px;
          font-weight: 600;
          line-height: 1.2;
          text-align: center;
        }
  
        .playground-agents-profile-name-input:focus {
          outline: none;
        }
  
        .playground-agents-profile-name-input:disabled {
          opacity: 1;
          -webkit-text-fill-color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-agents-detail-floating-actions.playground-tasks-toolbar-popup-shell {
          position: fixed !important;
          top: 14px !important;
          right: 18px !important;
          bottom: auto !important;
          left: auto !important;
          inset: auto 18px auto auto !important;
          width: 34px;
          min-width: 34px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          align-self: flex-end;
          margin-left: auto;
          transform: none !important;
          z-index: 120;
        }
  
        .playground-agents-detail-floating-actions .playground-content-menu-button {
          width: 34px;
          height: 34px;
          padding: 0;
          border: 0;
          border-radius: 999px;
          background: transparent;
          backdrop-filter: blur(20px);
          -webkit-backdrop-filter: blur(20px);
        }
  
        .playground-agents-detail-floating-actions .playground-content-menu-button:hover,
        .playground-agents-detail-floating-actions .playground-content-menu-button[aria-expanded="true"] {
          background: transparent;
        }
  
        .playground-agents-profile-avatar-wrap {
          margin: 0;
        }
  
        .playground-agents-profile-avatar .profile-editor-avatar-surface {
          position: relative;
          border-radius: 50%;
          background: rgba(255, 255, 255, 0.1);
          backdrop-filter: blur(5px);
          -webkit-backdrop-filter: blur(5px);
        }
  
        .playground-agents-profile-avatar.profile-editor-avatar {
          width: 115px;
          height: 115px;
        }
  
        .playground-agents-profile-avatar .profile-editor-avatar-surface::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          border-radius: 50%;
          padding: 1px;
          background: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-agents-profile-email-group {
          width: min(320px, 100%);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 6px;
          margin-top: 0;
        }
  
        .playground-agents-profile-email-icon {
          width: 14px;
          height: 14px;
          flex: 0 0 auto;
          color: rgba(255, 255, 255, 0.7);
        }
  
        .playground-agents-profile-email-field {
          min-width: 0;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.7);
          font-size: 12px;
          line-height: 1.35;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-agents-profile-actions {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 12px;
          margin-top: 10px;
        }
  
        .playground-agents-profile-action-button {
          position: relative;
          width: 36px;
          min-width: 36px;
          height: 36px;
          padding: 0;
          border: 0;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.92);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 8px;
          backdrop-filter: blur(20px);
          -webkit-backdrop-filter: blur(20px);
          cursor: pointer;
          transition: background-color 160ms ease, color 160ms ease, transform 160ms ease;
        }
  
        .playground-agents-profile-action-button:not(.is-primary)::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          border-radius: 999px;
          padding: 1px;
          background: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-agents-profile-action-button:hover {
          background: rgba(255, 255, 255, 0.12);
        }
  
        .playground-agents-profile-action-button:active {
          transform: translateY(1px);
        }
  
        .playground-agents-profile-action-button.is-primary {
          width: auto;
          min-width: 160px;
          padding: 0 22px;
          background: #fff;
          color: #0d0d0d;
          font-size: 12px;
          font-weight: 400;
        }
  
        .playground-agents-profile-action-button.is-primary:hover {
          background: rgba(255, 255, 255, 0.94);
          color: #000;
        }
  
        .playground-agents-profile-action-button svg {
          flex: 0 0 auto;
        }
  
        .playground-agents-profile-action-button.is-primary svg {
          fill: currentColor;
        }
  
        .playground-agents-avatar-picker-shell.playground-tasks-toolbar-popup-shell {
          position: absolute;
          right: -4px;
          bottom: -4px;
          z-index: 4;
        }
  
        .playground-agents-avatar-picker-menu {
          position: absolute;
          right: -10px;
          top: 40px;
          width: 256px;
          display: grid;
          grid-template-columns: repeat(3, minmax(0, 1fr));
          gap: 10px;
          padding: 12px;
        }
  
        .playground-agents-avatar-picker-option {
          position: relative;
          padding: 0;
          border: 0;
          background: transparent;
          cursor: pointer;
        }
  
        .playground-agents-avatar-picker-option-surface {
          position: relative;
          width: 100%;
          aspect-ratio: 1 / 1;
          overflow: hidden;
          border-radius: 16px;
          background: rgba(255, 255, 255, 0.06);
        }
  
        .playground-agents-avatar-picker-option.is-selected .playground-agents-avatar-picker-option-surface::after {
          content: "";
          position: absolute;
          inset: 0;
          border-radius: 16px;
          box-shadow: inset 0 0 0 2px rgba(255, 255, 255, 0.72);
        }
  
        .playground-agents-avatar-picker-option-image {
          width: 100%;
          height: 100%;
          object-fit: cover;
          display: block;
        }
  
        .playground-environments-editor-fact-value {
          max-width: 100%;
          margin-left: auto;
          color: rgba(255, 255, 255, 0.86);
          font-size: 12px;
          line-height: 1.35;
          text-align: right;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-environments-editor-fact-value.is-id {
          color: rgba(255, 255, 255, 0.62);
          font-size: 11px;
        }
  
        .playground-environments-action-button,
        .playground-environments-add-button {
          min-height: 34px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 8px;
          padding: 0 12px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 10px;
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.88);
          font-size: 12px;
          font-weight: 500;
          cursor: pointer;
          transition: background-color 160ms ease, border-color 160ms ease, color 160ms ease, opacity 160ms ease;
        }
  
        .playground-environments-action-button {
          font-weight: 500 !important;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-action-button,
        .playground-environments-page:not(.playground-agents-page) .playground-environments-add-button {
          min-height: 30px;
          padding: 0 11px;
          font-size: 11px;
        }
  
        .playground-environment-composer-modal .playground-tasks-project-modal-actions .playground-environments-action-button {
          min-height: 34px;
          padding: 0 12px;
          font-size: 12px;
        }
  
        .playground-environments-action-button:hover,
        .playground-environments-add-button:hover {
          background: rgba(255, 255, 255, 0.08);
          color: #fff;
        }
  
        .playground-environments-action-button.is-primary {
          background: rgba(255, 255, 255, 0.1);
        }
  
        .playground-environments-action-button.is-danger {
          color: #ffb0b0;
        }
  
        .playground-environments-action-button:disabled,
        .playground-environments-add-button:disabled,
        .playground-environments-remove-button:disabled {
          opacity: 0.38;
          cursor: default;
        }
  
        .playground-environments-error,
        .playground-environments-success,
        .playground-environments-muted {
          font-size: 12px;
          line-height: 1.45;
        }
  
        .playground-environments-error {
          color: #ff9c9c;
        }
  
        .playground-environments-success {
          color: #9ec7ff;
        }
  
        .playground-environments-muted {
          color: rgba(255, 255, 255, 0.46);
        }
  
        .playground-environments-summary-grid {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 14px;
        }
  
        .playground-environments-summary-card,
        .playground-environments-section,
        .playground-environments-mcp-card {
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 16px;
          background: rgba(255, 255, 255, 0.03);
        }
  
        .playground-environments-summary-card {
          padding: 16px;
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-environments-summary-title,
        .playground-environments-subtitle {
          font-size: 12px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-summary-title,
        .playground-environments-page:not(.playground-agents-page) .playground-environments-subtitle {
          font-size: 11px;
        }
  
        .playground-environments-summary-row {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 12px;
          font-size: 12px;
          color: rgba(255, 255, 255, 0.82);
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-summary-row {
          font-size: 11px;
        }
  
        .playground-environments-summary-row span:first-child {
          color: rgba(255, 255, 255, 0.46);
        }
  
        .playground-environments-toggle-row {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 16px;
        }
  
        .playground-environments-toggle-copy {
          min-width: 0;
        }
  
        .playground-environments-toggle {
          width: 46px;
          height: 26px;
          padding: 0;
          border: 0;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.16);
          position: relative;
          cursor: pointer;
          transition: background-color 160ms ease;
        }
  
        .playground-environments-toggle.is-active {
          background: rgba(57, 136, 255, 0.94);
        }
  
        .playground-environments-toggle-thumb {
          position: absolute;
          top: 3px;
          left: 3px;
          width: 20px;
          height: 20px;
          border-radius: 50%;
          background: white;
          transition: transform 160ms ease;
        }
  
        .playground-environments-toggle.is-active .playground-environments-toggle-thumb {
          transform: translateX(20px);
        }
  
        .playground-environments-section-header {
          width: 100%;
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 12px;
          padding: 16px 18px;
          border: 0;
          background: transparent;
          cursor: pointer;
          text-align: left;
        }
  
        .playground-environments-section-header.is-static {
          cursor: default;
        }
  
        .playground-environments-section-heading {
          min-width: 0;
        }
  
        .playground-environments-section-header-right {
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          gap: 10px;
        }
  
        .playground-environments-section-title {
          font-size: 13px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-section-title {
          font-size: 14px;
          font-weight: 400;
          line-height: 1.3;
        }
  
        .playground-environments-section-copy {
          margin-top: 4px;
          font-size: 12px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.46);
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-section-copy {
          font-size: 12px;
        }
  
        .playground-environments-section-toggle {
          width: 16px;
          height: 16px;
          flex-shrink: 0;
          color: rgba(255, 255, 255, 0.52);
        }
  
        .playground-environments-section-body {
          padding: 0 18px 18px;
          border-top: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-section,
        .playground-environments-page:not(.playground-agents-page) .playground-environments-mcp-card {
          border: 0;
          border-radius: 0;
          background: transparent;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-section-header {
          padding: 0 0 10px;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-section-header.is-static {
          padding-bottom: 6px;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-section.is-runtimes .playground-environments-section-header {
          padding-top: 2px;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-section.is-database-browser .playground-environments-section-header.is-static {
          padding-bottom: 11px;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-section-body {
          padding: 0;
          border-top: 0;
        }
  
        .playground-server-description-section {
          margin-bottom: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
        }
  
        .playground-server-description-section::before {
          content: none;
          display: none;
        }
  
        .playground-server-description-section > .playground-environments-section-body {
          padding: 0;
          border-top: 0;
        }
  
        .playground-server-description-section .playground-tasks-detail-description.playground-environments-editor-description {
          margin-top: 0;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          box-shadow: none;
          backdrop-filter: none;
          -webkit-backdrop-filter: none;
        }
  
        .playground-server-description-section .playground-tasks-detail-description.playground-environments-editor-description::before {
          content: none;
          display: none;
        }
  
        .playground-server-description-section .playground-tasks-detail-section-header {
          align-items: flex-start;
          margin-bottom: 8px;
        }
  
        .playground-server-description-title.playground-tasks-detail-section-title {
          color: rgba(255, 255, 255, 0.92);
          font-size: 14px;
          font-weight: 400;
          line-height: 1.3;
        }
  
        .playground-server-service-url-row {
          display: flex;
          align-items: center;
          gap: 6px;
          min-width: 0;
          margin-top: 4px;
          margin-bottom: 12px;
          color: rgba(255, 255, 255, 0.5);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.25;
        }
  
        .playground-server-service-url-value {
          min-width: 0;
          max-width: min(520px, 52vw);
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-server-detail-navbar.is-function-detail-navbar .playground-server-service-url-row {
          color: #4da3ff;
        }
  
        .playground-server-service-url-copy {
          appearance: none;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          width: 18px;
          height: 18px;
          flex: 0 0 auto;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.54);
          cursor: pointer;
          transition: color 160ms ease;
        }
  
        .playground-server-service-url-copy:hover {
          color: rgba(255, 255, 255, 0.9);
        }
  
        .playground-environments-editor-surface {
          padding: 12px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 8px;
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-environments-editor-surface-stack {
          display: flex;
          flex-direction: column;
          gap: 12px;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-editor-surface {
          padding: 0 0 12px;
          border: 0;
          border-radius: 0;
          background: transparent;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-editor-surface.playground-database-browser-surface {
          padding: 0 12px 12px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 18px;
          background: rgba(255, 255, 255, 0.03);
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-editor-surface.playground-auth-users-surface {
          padding: 12px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 8px;
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-computer-details-card {
          position: relative;
          overflow: visible;
          border: 1px solid rgba(255, 255, 255, 0.05);
          border-radius: 10px;
          background: rgba(255, 255, 255, 0.05);
          padding: 12px;
          backdrop-filter: blur(50px);
          -webkit-backdrop-filter: blur(50px);
        }
  
        .playground-computer-details-card::before {
          content: none;
          display: none;
        }
  
        .playground-computer-detail-content {
          width: min(100%, var(--playground-thread-content-max-width));
          max-width: var(--playground-thread-content-max-width);
          margin: 0 auto;
          display: flex;
          flex-direction: column;
          gap: 24px;
        }
  
        .playground-resources-page.is-develop-configure-page .playground-computer-detail-content {
          width: min(100%, var(--playground-centered-page-max-width));
          max-width: var(--playground-centered-page-max-width);
        }
  
        .playground-server-detail-content {
          width: min(100%, var(--playground-thread-content-max-width));
          max-width: var(--playground-thread-content-max-width);
          margin: 0 auto;
          display: flex;
          flex-direction: column;
          gap: 24px;
        }
  
        .playground-resources-page.is-servers-view .playground-server-detail-content,
        .playground-resources-page.is-develop-server-kind-page .playground-server-detail-content {
          width: min(100%, var(--playground-centered-page-max-width));
          max-width: var(--playground-centered-page-max-width);
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-server-detail-content.is-code-tab {
          min-height: 0;
          height: 100%;
          max-height: 100%;
          flex: 1 1 0;
          gap: 12px;
          overflow: hidden;
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-environments-detail-scroll.playground-environments-editor-scroll.is-code-tab {
          height: 100%;
          min-height: 0;
          flex: 1 1 0;
          overflow: hidden;
          gap: 0;
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-servers-editor-layout.is-code-tab {
          flex: 1 1 0;
          min-height: 0;
          height: 100%;
          max-height: 100%;
          overflow: hidden;
        }
  
        .playground-computer-detail-navbar.playground-content-nav {
          width: min(100%, var(--playground-thread-content-max-width));
          max-width: var(--playground-thread-content-max-width);
          min-height: 0;
          margin: 0 auto;
          padding-left: 0;
          padding-right: 0;
        }
  
        .playground-resources-page.is-develop-configure-page .playground-computer-detail-navbar.playground-content-nav {
          width: min(100%, var(--playground-centered-page-max-width));
          max-width: var(--playground-centered-page-max-width);
          margin: 0 auto 24px;
        }
  
        .playground-server-detail-navbar.playground-content-nav {
          width: min(100%, var(--playground-thread-content-max-width));
          max-width: var(--playground-thread-content-max-width);
          min-height: 0;
          margin: 0 auto;
          padding-left: 0;
          padding-right: 0;
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-server-detail-navbar.playground-content-nav {
          width: min(100%, var(--playground-centered-page-max-width));
          max-width: var(--playground-centered-page-max-width);
          margin: 0 auto 12px;
          align-items: center;
        }
  
        .playground-computer-detail-navbar .playground-environments-editor-title-input {
          font-size: 24px;
          line-height: 1.1;
        }
  
  	      .playground-server-detail-navbar .playground-environments-editor-title-input {
  	        font-size: 18px;
  	        line-height: 1.1;
  	      }
  
  	      .playground-server-detail-title-row {
  	        min-width: 0;
  	        width: 100%;
  	        display: flex;
  	        align-items: center;
  	        gap: 6px;
  	      }
  
  	      .playground-server-detail-title-row .playground-environments-editor-title-input {
  	        flex: 1 1 auto;
  	        min-width: 0;
  	      }
  
  	      .playground-server-detail-navbar .playground-server-function-title-input {
  	        margin-bottom: 6px;
  	      }
  
  	      .playground-server-detail-navbar.is-function-detail-navbar,
  	      .playground-server-detail-navbar.is-auth-detail-navbar,
  	      .playground-server-detail-navbar.is-secrets-detail-navbar,
  	      .playground-server-detail-navbar.is-agent-runtime-detail-navbar {
  	        align-items: flex-start;
  	      }
  
        .playground-server-detail-navbar .playground-database-navbar-back-button {
          align-self: center;
          margin-bottom: 6px;
          font-size: 18px;
          line-height: 1;
          color: rgba(255, 255, 255, 0.7);
        }
  
        .playground-database-export-shell.playground-tasks-toolbar-popup-shell {
          z-index: 74;
          display: inline-flex;
          align-items: center;
        }
  
        .playground-database-export-shell .playground-database-export-menu {
          top: calc(100% + 8px);
          right: 0;
          left: auto;
          width: 220px;
          min-width: 220px;
          transform-origin: top right;
        }
  
  	      .playground-server-detail-navbar .playground-auth-title-input {
  	        margin-bottom: 6px;
  	      }
  
  	      .playground-server-detail-navbar .playground-secrets-title-input,
  	      .playground-server-detail-navbar .playground-agent-runtime-title-input {
  	        margin-bottom: 6px;
  	      }
  
  	      .playground-resource-detail-back-button {
  	        align-self: flex-start;
  	        display: inline-flex;
  	        align-items: center;
  	        gap: 6px;
  	        width: auto;
  	        margin: 0;
  	        padding: 0;
  	        border: 0;
  	        background: transparent;
  	        color: rgba(255, 255, 255, 0.64);
  	        font-size: 12px;
  	        font-weight: 400;
  	        line-height: 1;
  	        cursor: pointer;
  	        transition: color 160ms ease;
  	      }
  
  	      .playground-resource-detail-back-button:hover {
  	        color: rgba(255, 255, 255, 0.96);
  	      }
  
  	      .playground-resource-detail-back-button svg {
  	        width: 12px;
  	        height: 12px;
  	        flex: 0 0 auto;
  	      }
  
  	      .playground-computer-detail-navbar .playground-environments-editor-navbar-actions,
  	      .playground-computer-detail-navbar .playground-environments-desktop-header-actions {
  	        flex-wrap: nowrap;
  	      }
  
        .playground-computer-detail-title.playground-tasks-detail-section-title {
          margin: 0;
        }
  
        .playground-computer-detail-summary.playground-environments-home-summary {
          margin: 0;
          padding-top: 20px;
          border-top: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-computer-detail-summary .playground-environments-home-summary-kpis {
          grid-template-columns: repeat(4, minmax(0, 1fr));
        }
  
        .playground-computer-detail-metrics.playground-environments-home-metrics {
          margin: 0;
          gap: 0;
        }
  
        .playground-server-detail-summary.playground-environments-home-summary {
          margin: 0;
          padding-top: 20px;
          border-top: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-server-detail-summary .playground-environments-home-summary-kpis {
          grid-template-columns: repeat(4, minmax(0, 1fr));
        }
  
        .playground-server-detail-metrics.playground-environments-home-metrics {
          margin: 0;
          gap: 0;
        }
  
        .playground-server-detail-content .playground-tasks-detail-facts.playground-environments-editor-facts.playground-server-details-card {
          margin-top: 0;
        }
  
        .playground-computer-detail-content .playground-computer-details-card {
          margin-top: 0;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          backdrop-filter: none;
          -webkit-backdrop-filter: none;
        }
  
        .playground-computer-detail-content .playground-environments-section,
        .playground-computer-detail-content .playground-environments-mcp-card,
        .playground-computer-detail-content .playground-environments-editor-surface,
        .playground-computer-detail-content .playground-tasks-detail-description.playground-environments-editor-description {
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          box-shadow: none;
          backdrop-filter: none;
          -webkit-backdrop-filter: none;
        }
  
        .playground-server-detail-content .playground-environments-section,
        .playground-server-detail-content .playground-environments-mcp-card,
        .playground-server-detail-content .playground-environments-editor-surface,
        .playground-server-detail-content .playground-tasks-detail-description.playground-environments-editor-description,
        .playground-server-detail-content .playground-tasks-detail-facts,
        .playground-server-detail-content .playground-environments-connections-section {
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          box-shadow: none;
          backdrop-filter: none;
          -webkit-backdrop-filter: none;
        }
  
        .playground-server-detail-content .playground-environments-connections-section {
          margin-bottom: 12px;
        }
  
        .playground-computer-detail-content .playground-environments-section::before,
        .playground-computer-detail-content .playground-environments-mcp-card::before,
        .playground-computer-detail-content .playground-environments-editor-surface::before,
        .playground-computer-detail-content .playground-tasks-detail-description.playground-environments-editor-description::before {
          content: none;
          display: none;
        }
  
        .playground-server-detail-content .playground-environments-section::before,
        .playground-server-detail-content .playground-environments-mcp-card::before,
        .playground-server-detail-content .playground-environments-editor-surface::before,
        .playground-server-detail-content .playground-tasks-detail-description.playground-environments-editor-description::before,
        .playground-server-detail-content .playground-tasks-detail-facts::before,
        .playground-server-detail-content .playground-environments-connections-section::before {
          content: none;
          display: none;
        }
  
        .playground-computer-detail-content .playground-environments-section-header,
        .playground-computer-detail-content .playground-environments-section-header.is-static {
          padding: 0 0 12px;
          margin-top: 12px;
          margin-bottom: 12px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-server-detail-content .playground-environments-section-header,
        .playground-server-detail-content .playground-environments-section-header.is-static,
        .playground-server-detail-content .playground-environments-connections-title,
        .playground-server-detail-content .playground-servers-source-files-header,
        .playground-server-detail-content .playground-auth-users-section-header,
        .playground-server-detail-content .playground-tasks-attachments-toolbar,
        .playground-server-detail-content .playground-agent-runtime-runs-section .playground-auth-users-toolbar {
          padding: 0 0 12px;
          margin-top: 12px;
          margin-bottom: 12px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-server-detail-content .playground-tasks-attachments .playground-tasks-attachments-toolbar,
        .playground-server-detail-content .playground-servers-source-files-section .playground-servers-source-files-header {
          border-bottom: 0;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-computer-detail-content .playground-environments-section-header,
        .playground-environments-page:not(.playground-agents-page) .playground-computer-detail-content .playground-environments-section-header.is-static,
        .playground-environments-page:not(.playground-agents-page) .playground-server-detail-content .playground-environments-section-header,
        .playground-environments-page:not(.playground-agents-page) .playground-server-detail-content .playground-environments-section-header.is-static {
          padding: 0 0 12px;
        }
  
        .playground-computer-detail-content .playground-environments-section-body {
          padding: 0;
          border-top: 0;
        }
  
        .playground-server-detail-content .playground-environments-section-body {
          padding: 0;
          border-top: 0;
        }
  
        .playground-computer-detail-content .playground-environments-stack,
        .playground-computer-detail-content .playground-environments-field-grid {
          padding-top: 0;
        }
  
        .playground-server-detail-content .playground-environments-stack,
        .playground-server-detail-content .playground-environments-field-grid {
          padding-top: 0;
        }
  
        .playground-server-detail-content .playground-tasks-skills {
          padding-top: 0;
        }
  
        .playground-computer-detail-content .playground-environments-package-group .playground-tasks-secondary-copy,
        .playground-computer-detail-content .playground-environments-empty-copy {
          margin-top: 4px;
        }
  
        .playground-server-detail-content .playground-environments-empty-copy,
        .playground-server-detail-content .playground-files-state {
          margin-top: 4px;
        }
  
        .playground-computer-detail-content .playground-tasks-detail-facts-body {
          gap: 0;
        }
  
        .playground-server-detail-content .playground-tasks-detail-facts-body {
          gap: 0;
        }
  
        .playground-computer-detail-content .playground-database-overview {
          gap: 0;
          padding: 0;
        }
  
        .playground-server-detail-content .playground-database-overview {
          gap: 0;
          padding: 0;
        }
  
        .playground-computer-detail-chart-block.playground-database-overview-chart-block {
          display: flex;
          flex-direction: column;
          gap: 16px;
          height: auto;
          min-height: 0;
        }
  
        .playground-resources-page.is-develop-configure-page .playground-computer-detail-metrics .playground-computer-detail-chart-block.playground-database-overview-chart-block {
          --playground-project-overview-chart-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          position: relative;
          padding: 20px;
          border: 0;
          border-radius: 15px;
          background: transparent;
          overflow: hidden;
          -webkit-backdrop-filter: blur(50px);
          backdrop-filter: blur(50px);
        }
  
        .playground-resources-page.is-develop-configure-page .playground-computer-detail-metrics .playground-computer-detail-chart-block.playground-database-overview-chart-block::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          z-index: 5;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-project-overview-chart-border);
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-resources-page.is-develop-configure-page .playground-computer-detail-metrics .playground-computer-detail-chart-block.playground-database-overview-chart-block > * {
          position: relative;
          z-index: 1;
        }
  
        .playground-resources-page.is-develop-configure-page .playground-computer-detail-metrics .playground-project-overview-chart-kpis {
          background: transparent;
          border-bottom: 1px solid rgba(255, 255, 255, 0.15);
        }
  
        .playground-resources-page.is-develop-configure-page .playground-computer-detail-metrics .playground-project-overview-chart-header {
          display: none;
        }
  
        .playground-resources-page.is-develop-configure-page .playground-computer-detail-fact-rows {
          margin-top: 2px;
          padding-top: 16px;
          border-top: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-server-detail-chart-block.playground-database-overview-chart-block {
          display: flex;
          flex-direction: column;
          gap: 16px;
          height: auto;
          min-height: 0;
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-server-detail-metrics .playground-server-detail-chart-block.playground-database-overview-chart-block {
          --playground-project-overview-chart-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          position: relative;
          padding: 20px;
          border: 0;
          border-radius: 15px;
          background: transparent;
          overflow: hidden;
          -webkit-backdrop-filter: blur(50px);
          backdrop-filter: blur(50px);
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-server-detail-metrics .playground-server-detail-chart-block.playground-database-overview-chart-block::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          z-index: 5;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-project-overview-chart-border);
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-server-detail-metrics .playground-server-detail-chart-block.playground-database-overview-chart-block > * {
          position: relative;
          z-index: 1;
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-server-detail-metrics .playground-project-overview-chart-kpis {
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-server-detail-metrics .playground-project-overview-chart-header {
          display: none;
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-server-detail-fact-rows {
          margin-top: 2px;
          padding-top: 16px;
          border-top: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-server-detail-metrics .playground-agent-runtime-settings-card.playground-server-details-card {
          --playground-project-overview-chart-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          position: relative;
          padding: 20px;
          border: 0;
          border-radius: 15px;
          background: #000;
          overflow: visible;
          -webkit-backdrop-filter: blur(50px);
          backdrop-filter: blur(50px);
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-server-detail-metrics .playground-agent-runtime-settings-card.playground-server-details-card::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          z-index: 5;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-project-overview-chart-border);
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-server-detail-metrics .playground-agent-runtime-settings-card.playground-server-details-card > * {
          position: relative;
          z-index: 1;
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-server-detail-metrics .playground-agent-runtime-settings-card .playground-server-detail-fact-rows {
          margin-top: 0;
          padding-top: 0;
          border-top: 0;
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-agent-runtime-skills-section {
          margin-top: 12px;
        }
  
        .playground-server-detail-content .playground-server-invoke-section.playground-server-details-card {
          position: relative;
          display: flex;
          flex-direction: column;
          gap: 16px;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          overflow: visible;
          -webkit-backdrop-filter: blur(50px);
          backdrop-filter: blur(50px);
        }
  
        .playground-server-detail-content .playground-server-invoke-section.playground-server-details-card::before {
          content: none;
          display: none;
        }
  
        .playground-server-detail-content .playground-server-invoke-section.playground-server-details-card > * {
          position: relative;
          z-index: 1;
        }
  
        .playground-server-invoke-title {
          margin-top: 12px;
          margin-bottom: 12px;
          padding: 0 0 12px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
          color: rgba(255, 255, 255, 0.92);
          font-size: 14px;
          font-weight: 400;
          line-height: 1.3;
        }
  
        .playground-server-invoke-auth-note {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 20px;
          padding: 0;
          border-radius: 0;
          background: transparent;
        }
  
        .playground-server-invoke-auth-copy {
          display: flex;
          flex-direction: column;
          gap: 6px;
          max-width: 70%;
          min-width: 0;
          color: rgba(255, 255, 255, 0.64);
          font-size: 13px;
          line-height: 1.45;
        }
  
        .playground-server-invoke-auth-toggle-copy {
          display: flex;
          flex-direction: column;
          gap: 4px;
          min-width: 0;
        }
  
        .playground-server-invoke-auth-label {
          color: rgba(255, 255, 255, 0.92);
          font-size: 13px;
          font-weight: 500;
        }
  
        .playground-server-invoke-auth-copy code {
          display: inline-flex;
          padding: 1px 5px;
          border-radius: 5px;
          background: rgba(255, 255, 255, 0.06);
          color: rgba(255, 255, 255, 0.86);
          font-family: ui-monospace, SFMono-Regular, SFMono, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
          font-size: 0.95em;
        }
  
        .playground-server-invoke-card {
          --playground-project-overview-chart-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          position: relative;
          display: flex;
          flex-direction: column;
          min-width: 0;
          margin-top: 12px;
          overflow: hidden;
          border: 0;
          border-radius: 10px;
          background: transparent;
          -webkit-backdrop-filter: blur(50px);
          backdrop-filter: blur(50px);
        }
  
        .playground-server-invoke-card::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          z-index: 5;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-project-overview-chart-border);
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-server-invoke-card > * {
          position: relative;
          z-index: 1;
        }
  
        .playground-server-invoke-header {
          display: flex;
          align-items: center;
          justify-content: flex-start;
          gap: 16px;
          padding: 14px 16px 0;
          border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-server-invoke-tabs {
          display: flex;
          align-items: center;
          gap: 18px;
          min-width: 0;
          overflow-x: auto;
        }
  
        .playground-server-invoke-tab {
          appearance: none;
          display: inline-flex;
          align-items: center;
          min-height: 28px;
          padding: 0 0 6px;
          border: 0;
          border-bottom: 1px solid transparent;
          background: transparent;
          color: rgba(255, 255, 255, 0.5);
          font-size: 13px;
          line-height: 1;
          font-weight: 400;
          white-space: nowrap;
          cursor: pointer;
          transition: color 160ms ease, border-color 160ms ease, background-color 160ms ease;
        }
  
        .playground-server-invoke-tab:hover {
          color: rgba(255, 255, 255, 0.82);
        }
  
        .playground-server-invoke-tab.is-active {
          color: rgba(255, 255, 255, 0.96);
          border-bottom-color: rgba(255, 255, 255, 0.5);
        }
  
        .playground-server-invoke-tab.is-active::after {
          content: none;
          display: none;
        }
  
        .playground-server-invoke-code-editor {
          height: var(--playground-server-invoke-code-height, auto);
          min-height: 0;
          width: 100%;
        }
  
        .playground-server-invoke-code-editor.playground-code-preview-editor-shell {
          flex: 0 0 auto;
          background: transparent;
        }
  
        .playground-server-invoke-code-editor .monaco-editor,
        .playground-server-invoke-code-editor .monaco-editor-background,
        .playground-server-invoke-code-editor .monaco-scrollable-element,
        .playground-server-invoke-code-editor .monaco-editor .margin,
        .playground-server-invoke-code-editor .margin,
        .playground-server-invoke-code-editor .overflow-guard {
          background: transparent !important;
        }
  
        .playground-server-invoke-code-fallback {
          margin: 0;
          min-height: 180px;
          padding: 14px 16px;
          max-width: 100%;
          overflow-x: auto;
          color: rgba(255, 255, 255, 0.9);
          font-family: ui-monospace, SFMono-Regular, SFMono, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
          font-size: 12px;
          font-weight: 400;
          line-height: 1.55;
          white-space: pre;
        }
  
        .playground-computer-detail-chart-block .playground-settings-usage-chart-card {
          min-height: 0;
        }
  
        .playground-server-detail-chart-block .playground-settings-usage-chart-card {
          min-height: 0;
        }
  
        .playground-computer-detail-chart-kpis.playground-project-overview-summary-kpis,
        .playground-server-detail-chart-kpis.playground-project-overview-summary-kpis {
          grid-template-columns: repeat(4, minmax(0, 1fr));
        }
  
        .playground-server-detail-content .playground-environments-section.is-database-browser {
          padding-top: 0;
        }
  
        .playground-server-detail-content .playground-environments-section.is-database-browser .playground-environments-section-header.is-static {
          border-bottom: 0;
        }
  
  	      .playground-server-detail-content .playground-database-browser-surface.playground-server-details-card {
  	        --playground-project-overview-chart-border: linear-gradient(
  	          -10deg,
  	          rgba(200, 200, 200, 0.25),
  	          rgba(255, 255, 255, 0.1),
  	          rgba(255, 255, 255, 0.15),
  	          rgba(255, 255, 255, 0.375)
  	        );
  	        position: relative;
  	        margin-bottom: 24px;
  	        padding: 20px;
  	        border: 0;
  	        border-radius: 15px;
  	        overflow: hidden;
  	        background: transparent;
  	        -webkit-backdrop-filter: blur(50px);
  	        backdrop-filter: blur(50px);
  	      }
  
  	      .playground-server-detail-content .playground-database-browser-surface.playground-server-details-card::before {
  	        content: "";
  	        pointer-events: none;
  	        position: absolute;
  	        inset: 0;
  	        z-index: 5;
  	        border-radius: inherit;
  	        padding: 1px;
  	        background: var(--playground-project-overview-chart-border);
  	        mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
  	        mask-clip: content-box, border-box;
  	        mask-composite: exclude;
  	        mask-origin: content-box, border-box;
  	        mask-repeat: repeat, repeat;
  	        mask-size: auto, auto;
  	      }
  
  	      .playground-server-detail-content .playground-database-browser-surface.playground-server-details-card > * {
  	        position: relative;
  	        z-index: 1;
  	      }
  
        .playground-server-detail-content .playground-auth-users-section-header {
          border-bottom: 0;
        }
  
  	      .playground-server-detail-content .playground-auth-users-surface.playground-server-details-card {
  	        --playground-project-overview-chart-border: linear-gradient(
  	          -10deg,
  	          rgba(200, 200, 200, 0.25),
  	          rgba(255, 255, 255, 0.1),
  	          rgba(255, 255, 255, 0.15),
  	          rgba(255, 255, 255, 0.375)
  	        );
  	        position: relative;
  	        margin-bottom: 24px;
  	        padding: 20px;
  	        border: 0;
  	        border-radius: 15px;
  	        overflow: hidden;
  	        background: transparent;
  	        -webkit-backdrop-filter: blur(50px);
  	        backdrop-filter: blur(50px);
  	      }
  
  	      .playground-server-detail-content .playground-auth-users-surface.playground-server-details-card::before {
  	        content: "";
  	        pointer-events: none;
  	        position: absolute;
  	        inset: 0;
  	        z-index: 5;
  	        border-radius: inherit;
  	        padding: 1px;
  	        background: var(--playground-project-overview-chart-border);
  	        mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
  	        mask-clip: content-box, border-box;
  	        mask-composite: exclude;
  	        mask-origin: content-box, border-box;
  	        mask-repeat: repeat, repeat;
  	        mask-size: auto, auto;
  	      }
  
  	      .playground-server-detail-content .playground-auth-users-surface.playground-server-details-card > * {
  	        position: relative;
  	        z-index: 1;
  	      }
  
        .playground-computer-detail-fact-rows {
          display: flex;
          flex-direction: column;
          gap: 4px;
          padding-top: 0;
          border-top: 0;
        }
  
        .playground-server-detail-fact-rows {
          display: flex;
          flex-direction: column;
          gap: 4px;
          padding-top: 0;
          border-top: 0;
        }
  
        .playground-server-custom-domain-section {
          --playground-project-overview-control-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          position: relative;
          z-index: 0;
          display: flex;
          flex-direction: column;
          gap: 12px;
          padding: 20px;
          border: 0;
          border-radius: 15px;
          background: #000;
          box-shadow: none;
          margin-top: 24px;
          margin-bottom: 24px;
        }
  
        .playground-server-settings-tab .playground-server-danger-section {
          margin-top: 24px;
        }
  
        .playground-server-custom-domain-section::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-project-overview-control-border);
          -webkit-mask:
            linear-gradient(#000 0 0) content-box,
            linear-gradient(#000 0 0);
          -webkit-mask-composite: xor;
          mask:
            linear-gradient(#000 0 0) content-box,
            linear-gradient(#000 0 0);
          mask-composite: exclude;
          z-index: 1;
        }
  
        .playground-server-custom-domain-section > * {
          position: relative;
          z-index: 2;
        }
  
        .playground-server-custom-domain-add-button {
          --playground-project-overview-control-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          position: relative;
          z-index: 0;
          overflow: hidden;
          border: 0;
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-server-custom-domain-add-button::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-project-overview-control-border);
          -webkit-mask:
            linear-gradient(#000 0 0) content-box,
            linear-gradient(#000 0 0);
          -webkit-mask-composite: xor;
          mask:
            linear-gradient(#000 0 0) content-box,
            linear-gradient(#000 0 0);
          mask-composite: exclude;
          z-index: 1;
        }
  
        .playground-server-custom-domain-add-button > * {
          position: relative;
          z-index: 2;
        }
  
        .playground-server-custom-domain-add-button:hover:not(:disabled) {
          background: rgba(255, 255, 255, 0.04);
        }
  
        .playground-server-custom-domain-header,
        .playground-server-custom-domain-card,
        .playground-server-custom-domain-main,
        .playground-server-custom-domain-actions,
        .playground-server-custom-domain-modal-summary-row,
        .playground-server-custom-domain-record {
          display: flex;
          align-items: center;
        }
  
        .playground-server-custom-domain-header,
        .playground-server-custom-domain-card,
        .playground-server-custom-domain-modal-summary-row {
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-server-custom-domain-subtitle,
        .playground-server-custom-domain-meta,
        .playground-server-custom-domain-empty-copy,
        .playground-server-custom-domain-records-empty {
          color: rgba(255, 255, 255, 0.5);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.45;
        }
  
        .playground-server-custom-domain-subtitle {
          margin-top: 4px;
        }
  
        .playground-server-custom-domain-card,
        .playground-server-custom-domain-empty,
        .playground-server-custom-domain-modal-summary,
        .playground-server-custom-domain-records {
          border: 1px solid rgba(255, 255, 255, 0.06);
          border-radius: 10px;
          background: rgba(0, 0, 0, 0.22);
        }
  
        .playground-server-custom-domain-card {
          position: relative;
          padding: 12px;
          cursor: pointer;
        }
  
        .playground-server-custom-domain-main {
          min-width: 0;
          gap: 10px;
        }
  
        .playground-server-custom-domain-icon {
          width: 28px;
          height: 28px;
          border-radius: 8px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          background: rgba(255, 255, 255, 0.05);
          color: rgba(255, 255, 255, 0.9);
          flex: 0 0 auto;
        }
  
        .playground-server-custom-domain-copy {
          min-width: 0;
        }
  
        .playground-server-custom-domain-name {
          overflow: hidden;
          color: rgba(255, 255, 255, 0.92);
          font-size: 12px;
          font-weight: 500;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-server-custom-domain-actions {
          flex: 0 0 auto;
          gap: 8px;
        }
  
        .playground-server-custom-domain-row-menu-button {
          width: auto;
          min-width: 0;
          height: auto;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          box-shadow: none;
          backdrop-filter: none;
        }
  
        .playground-server-custom-domain-row-menu-button:hover {
          background: transparent;
        }
  
        .playground-server-custom-domain-open-button {
          width: auto;
          min-width: 0;
          height: auto;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          box-shadow: none;
          backdrop-filter: none;
        }
  
        .playground-server-custom-domain-open-button:hover {
          background: transparent;
        }
  
        .playground-server-custom-domain-menu {
          position: absolute;
          top: calc(100% - 6px);
          right: 12px;
          z-index: 12;
          min-width: 132px;
          display: flex;
          flex-direction: column;
          gap: 4px;
        }
  
        .playground-server-custom-domain-menu-row {
          width: 100%;
          min-height: 30px;
          display: flex;
          align-items: center;
          gap: 8px;
          padding: 0 8px;
          border: 0;
          border-radius: 7px;
          background: transparent;
          color: rgba(255, 255, 255, 0.84);
          font-size: 12px;
          font-weight: 400;
          text-align: left;
          cursor: pointer;
        }
  
        .playground-server-custom-domain-menu-row:hover:enabled {
          background: rgba(255, 255, 255, 0.08);
        }
  
        .playground-server-custom-domain-menu-row:disabled {
          opacity: 0.55;
          cursor: default;
        }
  
        .playground-server-custom-domain-menu-row.is-danger {
          color: rgb(255, 146, 146);
        }
  
        .playground-server-custom-domain-status {
          display: inline-flex;
          align-items: center;
          min-height: 24px;
          padding: 0 10px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.72);
          font-size: 12px;
          font-weight: 400;
          white-space: nowrap;
        }
  
        .playground-server-custom-domain-status.is-connected {
          background: rgba(94, 213, 143, 0.16);
          color: rgb(127, 238, 168);
        }
  
        .playground-server-custom-domain-status.is-error {
          background: rgba(255, 105, 105, 0.16);
          color: rgb(255, 146, 146);
        }
  
        .playground-server-custom-domain-empty {
          padding: 16px;
        }
  
        .playground-server-danger-section {
          --playground-project-overview-control-border: linear-gradient(
            -10deg,
            rgba(255, 99, 99, 0.38),
            rgba(255, 255, 255, 0.1),
            rgba(255, 120, 120, 0.18),
            rgba(255, 255, 255, 0.22)
          );
          position: relative;
          z-index: 0;
          overflow: hidden;
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 18px;
          padding: 20px;
          border: 0;
          border-radius: 15px;
          background: rgba(255, 69, 58, 0.08);
          margin-bottom: 24px;
        }
  
        .playground-database-danger-section {
          margin-top: 24px;
        }
  
        .playground-server-danger-section::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-project-overview-control-border);
          -webkit-mask:
            linear-gradient(#000 0 0) content-box,
            linear-gradient(#000 0 0);
          -webkit-mask-composite: xor;
          mask:
            linear-gradient(#000 0 0) content-box,
            linear-gradient(#000 0 0);
          mask-composite: exclude;
          z-index: 1;
        }
  
        .playground-server-danger-section > * {
          position: relative;
          z-index: 2;
        }
  
        .playground-server-danger-copy-row {
          min-width: 0;
          display: flex;
          align-items: flex-start;
          gap: 12px;
        }
  
        .playground-server-danger-icon {
          width: 20px;
          height: 20px;
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border-radius: 0;
          background: transparent;
          color: rgb(255, 126, 116);
        }
  
        .playground-server-danger-title {
          color: rgba(255, 255, 255, 0.96);
          font-size: 13px;
          line-height: 1.25;
          font-weight: 500;
        }
  
        .playground-server-danger-text {
          max-width: 620px;
          margin-top: 6px;
          color: rgba(255, 255, 255, 0.64);
          font-size: 12px;
          line-height: 1.45;
          font-weight: 400;
        }
  
        .playground-server-danger-delete-button {
          flex: 0 0 auto;
          min-height: 32px;
          padding: 0 14px;
          border: 0;
          border-radius: 999px;
          background: rgba(255, 80, 70, 0.16);
          color: rgb(255, 190, 184);
          font-size: 12px;
          line-height: 1;
          font-weight: 500;
          cursor: pointer;
          transition: background-color 160ms ease, color 160ms ease, opacity 160ms ease;
        }
  
        .playground-server-danger-delete-button:hover:not(:disabled) {
          background: rgba(255, 80, 70, 0.24);
          color: #fff;
        }
  
        .playground-server-danger-delete-button:disabled {
          opacity: 0.48;
          cursor: default;
        }
  
        .playground-server-custom-domain-empty-title {
          color: rgba(255, 255, 255, 0.9);
          font-size: 12px;
          font-weight: 500;
        }
  
        .playground-server-custom-domain-empty-copy {
          margin-top: 4px;
        }
  
        .playground-server-custom-domain-modal {
          max-width: min(680px, calc(100vw - 32px));
          max-height: calc(100vh - 40px);
          overflow-y: auto;
        }
  
        .playground-server-custom-domain-modal-body {
          display: flex;
          flex-direction: column;
          gap: 14px;
          padding: 2px 0 0;
        }
  
        .playground-server-custom-domain-field {
          padding-top: 0;
        }
  
        .playground-server-custom-domain-label-row {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-server-custom-domain-label-row .playground-environments-field-label {
          color: rgba(255, 255, 255, 0.9);
          font-size: 12px;
          font-weight: 500;
        }
  
        .playground-server-custom-domain-notice {
          background: rgba(102, 166, 255, 0.08);
          color: rgba(255, 255, 255, 0.74);
        }
  
        .playground-server-custom-domain-modal-summary,
        .playground-server-custom-domain-records {
          padding: 12px;
        }
  
        .playground-server-custom-domain-modal-summary {
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-server-custom-domain-modal-summary-row > span:first-child {
          color: rgba(255, 255, 255, 0.5);
          font-size: 12px;
        }
  
        .playground-server-custom-domain-records {
          display: flex;
          flex-direction: column;
          gap: 8px;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
        }
  
        .playground-server-custom-domain-records-title {
          margin-top: 12px;
          color: rgba(255, 255, 255, 0.9);
          font-size: 12px;
          font-weight: 500;
        }
  
        .playground-server-custom-domain-records-copy {
          color: rgba(255, 255, 255, 0.5);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.45;
        }
  
        .playground-server-custom-domain-record-group-title {
          padding-top: 6px;
          color: rgba(255, 255, 255, 0.5);
          font-size: 12px;
          font-weight: 400;
        }
  
        .playground-server-custom-domain-record {
          min-height: 38px;
          gap: 10px;
          padding: 9px 0 0;
          border-top: 1px solid rgba(255, 255, 255, 0.06);
        }
  
        .playground-server-custom-domain-record-action {
          width: 58px;
          flex: 0 0 58px;
          color: rgba(255, 255, 255, 0.78);
          font-size: 12px;
          font-weight: 400;
        }
  
        .playground-server-custom-domain-record-action.is-remove {
          color: rgb(255, 146, 146);
        }
  
        .playground-server-custom-domain-record-action.is-add {
          color: rgb(127, 238, 168);
        }
  
        .playground-server-custom-domain-record-type {
          width: 56px;
          flex: 0 0 56px;
          color: rgba(255, 255, 255, 0.9);
          font-size: 12px;
          font-weight: 500;
        }
  
        .playground-server-custom-domain-record-name,
        .playground-server-custom-domain-record-value {
          min-width: 0;
          overflow: hidden;
          color: rgba(255, 255, 255, 0.66);
          font-size: 12px;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-server-custom-domain-record-name {
          flex: 0.85 1 0;
        }
  
        .playground-server-custom-domain-record-value {
          display: flex;
          flex: 1.15 1 0;
          align-items: center;
          justify-content: flex-end;
          gap: 8px;
          color: rgba(255, 255, 255, 0.78);
        }
  
        .playground-server-custom-domain-record-value span {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-server-custom-domain-record-copy-button {
          width: auto;
          min-width: 0;
          height: auto;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          box-shadow: none;
          backdrop-filter: none;
        }
  
        .playground-server-custom-domain-record-copy-button:hover {
          background: transparent;
        }
  
        .playground-computer-detail-content .playground-environment-profile-section {
          margin-bottom: 8px;
          padding-bottom: 0;
          border-bottom: 0;
        }
  
        .playground-computer-detail-content .playground-environment-profile-card {
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
        }
  
        .playground-computer-detail-content .playground-environment-profile-card:hover,
        .playground-computer-detail-content .playground-environment-profile-card.is-selected {
          border-color: transparent;
          background: transparent;
        }
  
        .playground-computer-detail-content .playground-environment-profile-card.is-selected .playground-environment-profile-card-label {
          color: rgb(143, 196, 255);
        }
  
        @media (max-width: 980px) {
          .playground-computer-detail-summary .playground-environments-home-summary-kpis {
            grid-template-columns: repeat(2, minmax(0, 1fr));
          }
  
          .playground-server-detail-summary .playground-environments-home-summary-kpis {
            grid-template-columns: repeat(2, minmax(0, 1fr));
          }
        }
  
        .playground-environments-field-grid {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 12px;
          padding-top: 16px;
        }
  
        .playground-environments-field {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 8px;
        }
  
        .playground-environments-field-span {
          grid-column: 1 / -1;
        }
  
        .playground-environments-field-label {
          font-size: 11px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.54);
        }
  
        .playground-environments-input,
        .playground-environments-textarea,
        .playground-environments-select {
          width: 100%;
          min-width: 0;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 12px;
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.94);
          font-size: 12px;
          outline: none;
        }
  
        .playground-environments-input,
        .playground-environments-select {
          height: 38px;
          padding: 0 12px;
        }
  
        .playground-environments-textarea {
          padding: 10px 12px;
          line-height: 1.5;
          resize: vertical;
        }
  
        .playground-environments-select {
          appearance: none;
        }
  
        .playground-environments-stack {
          display: flex;
          flex-direction: column;
          gap: 12px;
          padding-top: 16px;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-field-grid,
        .playground-environments-page:not(.playground-agents-page) .playground-environments-stack {
          padding-top: 0;
        }
  
        .playground-environments-editor-surface .playground-environments-field-grid,
        .playground-environments-editor-surface .playground-environments-stack {
          padding-top: 0;
        }
  
        .playground-servers-source-files-section {
          display: flex;
          flex-direction: column;
          gap: 12px;
        }
  
        .playground-servers-source-files-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-servers-source-files-actions {
          display: inline-flex;
          align-items: center;
          gap: 8px;
        }
  
        .playground-servers-source-files-copy {
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.56);
        }
  
        .playground-servers-source-files-list {
          width: 100%;
          display: flex;
          flex-direction: column;
          gap: 4px;
          margin-top: 12px;
        }
  
        .playground-servers-source-files-toolbar {
          width: 100%;
        }
  
        .playground-servers-source-files-toolbar .playground-tasks-attachments-actions {
          flex: 1 1 auto;
          justify-content: flex-end;
        }
  
        .playground-servers-source-deploy-button.playground-environments-action-button {
          min-height: 30px;
          margin-left: 12px;
          padding: 0 14px;
          border: 0;
          border-radius: 999px;
          background: #fff;
          color: #000;
          font-size: 12px;
          font-weight: 400;
          line-height: 1;
        }
  
        .playground-servers-source-deploy-button.playground-environments-action-button:hover:not(:disabled) {
          background: rgba(255, 255, 255, 0.92);
          color: #000;
        }
  
        .playground-servers-source-new-file-button.playground-environments-action-button {
          margin-left: 8px;
        }
  
        .playground-server-description-section.is-titleless .playground-tasks-detail-section-header {
          justify-content: flex-end;
        }
  
        .playground-server-description-inline-row {
          display: flex;
          align-items: flex-start;
          gap: 12px;
        }
  
        .playground-server-description-inline-row .playground-tasks-detail-description-editor {
          min-width: 0;
          flex: 1 1 auto;
        }
  
        .playground-server-description-inline-row .playground-tasks-detail-format-actions {
          flex: 0 0 auto;
          justify-content: flex-end;
        }
  
        .playground-servers-source-file-row {
          position: relative;
          width: 100%;
          min-height: 74px;
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          padding: 12px 14px;
          border-radius: 16px;
          background: rgba(255, 255, 255, 0.045);
          border: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-servers-source-file-row.is-active {
          border-color: rgba(255, 255, 255, 0.2);
          background: rgba(255, 255, 255, 0.07);
        }
  
        .playground-servers-source-file-main,
        .playground-servers-source-file-actions {
          display: inline-flex;
          align-items: center;
          gap: 10px;
          min-width: 0;
        }
  
        .playground-servers-source-file-main {
          flex: 1 1 auto;
          padding: 0;
          border: 0;
          background: transparent;
          color: inherit;
          text-align: left;
          cursor: pointer;
        }
  
        .playground-servers-source-file-main:disabled {
          cursor: default;
          opacity: 0.72;
        }
  
        .playground-servers-source-file-icon-shell {
          width: 18px;
          height: 18px;
          flex: 0 0 18px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border-radius: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.72);
        }
  
        .playground-servers-source-file-icon-shell img,
        .playground-servers-source-file-icon-shell svg {
          width: 16px;
          height: 16px;
          flex: 0 0 auto;
          object-fit: contain;
        }
  
        .playground-servers-source-file-copy {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 4px;
        }
  
        .playground-servers-source-file-name {
          font-size: 12px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.92);
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-servers-source-file-meta {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          font-size: 11px;
          color: rgba(255, 255, 255, 0.5);
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-servers-source-file-meta-dot {
          width: 3px;
          height: 3px;
          flex: 0 0 auto;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.42);
        }
  
        .playground-servers-source-file-status {
          display: inline-flex;
          align-items: center;
          gap: 5px;
          color: rgb(127, 238, 168);
        }
  
        .playground-servers-source-file-actions-menu-shell {
          position: relative;
          flex: 0 0 auto;
        }
  
        .playground-servers-source-file-menu-button {
          width: 28px;
          height: 28px;
          min-width: 0;
          padding: 0;
          border: 0;
          border-radius: 0;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          background: transparent;
          color: rgba(255, 255, 255, 0.72);
          box-shadow: none;
          backdrop-filter: none;
          cursor: pointer;
        }
  
        .playground-servers-source-file-menu-button:hover {
          background: transparent;
          color: rgba(255, 255, 255, 0.95);
        }
  
        .playground-servers-source-file-menu {
          position: absolute;
          top: calc(100% + 4px);
          right: 0;
          z-index: 20;
        }
  
        .playground-servers-function-snippet,
        .playground-servers-function-response {
          margin: 0;
          padding: 12px;
          border-radius: 10px;
          background: rgba(0, 0, 0, 0.32);
          border: 1px solid rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.88);
          font-size: 12px;
          line-height: 1.6;
          overflow-x: auto;
          white-space: pre-wrap;
          word-break: break-word;
        }
  
        .playground-servers-source-editor {
          display: flex;
          flex-direction: column;
          gap: 0;
          border-radius: 14px;
          overflow: hidden;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.03);
        }
  
        .playground-servers-source-editor-shell {
          min-height: 320px;
        }
  
        .playground-servers-source-editor-textarea {
          height: 100%;
          min-height: 320px;
          border: 0;
          background: transparent;
        }
  
        .playground-servers-source-files-layout {
          display: grid;
          grid-template-columns: minmax(0, 1fr);
          gap: 0;
        }
  
        .playground-servers-source-files-layout.has-preview {
          grid-template-columns: minmax(0, 1fr) minmax(360px, 420px);
        }
  
        .playground-servers-source-files-panel {
          min-width: 0;
        }
  
        .playground-servers-source-preview-sidebar {
          min-width: 0;
          min-height: 0;
          height: 100%;
          flex: 1;
          display: flex;
          flex-direction: column;
        }
  
        .playground-servers-source-preview-copy {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 0;
        }
  
        .playground-servers-source-preview-title {
          color: rgba(255, 255, 255, 0.96);
          font-size: 13px;
          font-weight: 500;
          line-height: 1.2;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-servers-source-preview-path {
          color: rgba(255, 255, 255, 0.5);
          font-size: 11px;
          line-height: 1.35;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-servers-source-preview-actions {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          flex-wrap: wrap;
          justify-content: flex-end;
        }
  
        .playground-servers-source-preview-close-button {
          min-width: 0;
          width: auto;
          height: auto;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.82);
        }
  
        .playground-servers-source-preview-close-button:hover:not(:disabled) {
          background: transparent;
          color: #fff;
        }
  
        .playground-servers-source-preview-status {
          flex: 0 0 auto;
          padding: 10px 18px 12px;
          color: rgba(255, 255, 255, 0.52);
          font-size: 11px;
          line-height: 1.4;
        }
  
        .playground-servers-source-preview-status.is-warning {
          color: #f1c87a;
        }
  
        .playground-servers-source-preview-status.is-success {
          color: #85d6a5;
        }
  
        .playground-servers-source-preview-status.is-error {
          color: #ff9c9c;
        }
  
        .playground-servers-source-preview-body {
          min-height: 0;
          flex: 1;
          display: flex;
          flex-direction: column;
          padding: 0;
        }
  
        .playground-servers-source-preview-editor-shell {
          min-height: 0;
          height: 100%;
          flex: 1;
          background: transparent;
          border-radius: 0;
          overflow: hidden;
        }
  
        .playground-servers-source-preview-editor-shell.playground-code-preview-editor-shell,
        .playground-servers-source-preview-editor-shell.playground-code-preview-editor-shell .monaco-editor,
        .playground-servers-source-preview-editor-shell.playground-code-preview-editor-shell .monaco-editor-background,
        .playground-servers-source-preview-editor-shell.playground-code-preview-editor-shell .monaco-scrollable-element,
        .playground-servers-source-preview-editor-shell.playground-code-preview-editor-shell .margin,
        .playground-servers-source-preview-editor-shell.playground-code-preview-editor-shell .overflow-guard {
          background: transparent !important;
        }
  
        .playground-servers-source-preview-empty {
          min-height: 320px;
          display: flex;
          align-items: center;
          justify-content: center;
          text-align: center;
          color: rgba(255, 255, 255, 0.46);
          font-size: 12px;
          line-height: 1.5;
          padding: 18px;
        }
  
        .playground-servers-code-workspace {
          position: relative;
          flex: 1 1 auto;
          min-height: 480px;
          margin-bottom: 10px;
          display: grid;
          grid-template-columns: 252px minmax(0, 1fr);
          overflow: hidden;
          border-radius: 10px;
          background: #000;
          border: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-server-detail-content.is-code-tab .playground-servers-code-workspace {
          flex: 1 1 0;
          min-height: 0;
          height: auto;
          max-height: 100%;
        }
  
        .playground-servers-code-workspace.is-dragging::after {
          content: "Drop files to upload";
          pointer-events: none;
          position: absolute;
          inset: 10px;
          z-index: 8;
          display: flex;
          align-items: center;
          justify-content: center;
          border: 1px dashed rgba(102, 166, 255, 0.72);
          border-radius: 10px;
          background: rgba(0, 0, 0, 0.64);
          color: rgba(255, 255, 255, 0.86);
          font-size: 12px;
          font-weight: 500;
          backdrop-filter: blur(10px);
        }
  
        .playground-servers-code-sidebar {
          position: relative;
          z-index: 1;
          min-width: 0;
          min-height: 0;
          display: flex;
          flex-direction: column;
          background: rgba(255, 255, 255, 0.05);
          border-right: 1px solid rgba(255, 255, 255, 0.08);
          border-top-left-radius: 10px;
          border-bottom-left-radius: 10px;
        }
  
        .playground-servers-code-sidebar-header {
          min-height: 54px;
          padding: 14px 22px;
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-servers-code-sidebar-title {
          color: rgba(255, 255, 255, 0.42);
          font-size: 11px;
          font-weight: 400;
          letter-spacing: 0;
          line-height: 1;
          text-transform: none;
        }
  
        .playground-servers-code-add-file-button {
          min-height: 26px;
          padding: 0 10px;
          border-radius: 999px;
          font-size: 12px;
          line-height: 1;
        }
  
        .playground-servers-code-add-file-shell {
          position: relative;
          flex: 0 0 auto;
        }
  
        .playground-servers-code-add-file-menu {
          right: 0;
          left: auto;
          top: calc(100% + 8px);
          width: 154px;
          min-width: 154px;
          z-index: 60;
        }
  
        .playground-servers-code-file-list {
          min-height: 0;
          flex: 1;
          overflow: auto;
          padding: 0 0 12px;
        }
  
        .playground-servers-code-file-list::-webkit-scrollbar,
        .playground-servers-code-editor-main::-webkit-scrollbar {
          width: 0;
          height: 0;
        }
  
        .playground-servers-code-file-row {
          width: 100%;
          min-height: 34px;
          display: flex;
          align-items: center;
          gap: 8px;
          padding: 0 14px;
          border: 0;
          border-radius: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.82);
          cursor: pointer;
          text-align: left;
        }
  
        .playground-servers-code-file-row:hover,
        .playground-servers-code-file-row.is-active {
          background: rgba(255, 255, 255, 0.09);
          color: #fff;
        }
  
        .playground-servers-code-file-row:disabled {
          cursor: default;
          color: rgba(255, 255, 255, 0.42);
        }
  
        .playground-servers-code-file-chevron {
          width: 14px;
          height: 14px;
          flex: 0 0 14px;
          color: rgba(255, 255, 255, 0.6);
        }
  
        .playground-servers-code-file-icon {
          width: 16px;
          height: 16px;
          flex: 0 0 16px;
          color: rgba(255, 255, 255, 0.72);
        }
  
        .playground-servers-code-file-name {
          min-width: 0;
          flex: 1 1 auto;
          overflow: hidden;
          color: inherit;
          font-size: 12px;
          font-weight: 400;
          line-height: 1.2;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-servers-code-editor-main {
          position: relative;
          z-index: 1;
          min-width: 0;
          min-height: 0;
          display: flex;
          flex-direction: column;
          background: #000;
          overflow: hidden;
        }
  
        .playground-servers-code-editor-body {
          min-height: 0;
          flex: 1;
          display: flex;
          flex-direction: column;
        }
  
        .playground-servers-code-editor-shell {
          min-height: 0;
          height: 100%;
          flex: 1;
          border-radius: 0;
          background: #000;
        }
  
        .playground-servers-code-editor-shell.playground-code-preview-editor-shell,
        .playground-servers-code-editor-shell.playground-code-preview-editor-shell .monaco-editor,
        .playground-servers-code-editor-shell.playground-code-preview-editor-shell .monaco-editor-background,
        .playground-servers-code-editor-shell.playground-code-preview-editor-shell .monaco-scrollable-element,
        .playground-servers-code-editor-shell.playground-code-preview-editor-shell .margin,
        .playground-servers-code-editor-shell.playground-code-preview-editor-shell .overflow-guard {
          background: #000 !important;
        }
  
        .playground-servers-code-editor-statusbar {
          min-height: 42px;
          flex: 0 0 auto;
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          padding: 0 16px;
          background: rgba(255, 255, 255, 0.035);
          border-top: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-servers-code-editor-status-actions {
          display: inline-flex;
          align-items: center;
          gap: 8px;
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-servers-code-editor-status-actions .playground-environments-action-button {
          --playground-server-code-action-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          position: relative;
          z-index: 0;
          overflow: hidden;
          min-height: 30px;
          border: 0;
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.96);
          font-weight: 400;
          -webkit-backdrop-filter: blur(50px);
          backdrop-filter: blur(50px);
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-servers-code-editor-status-actions .playground-environments-action-button::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-server-code-action-border);
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-servers-code-editor-status-actions .playground-environments-action-button > * {
          position: relative;
          z-index: 1;
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-servers-code-editor-status-actions .playground-environments-action-button:hover:not(:disabled),
        .playground-resources-page.is-develop-server-kind-page .playground-servers-code-editor-status-actions .playground-environments-action-button.is-primary:hover:not(:disabled) {
          background: transparent;
          color: #fff;
        }
  
        .playground-servers-code-empty {
          min-height: 100%;
          flex: 1;
          display: flex;
          align-items: center;
          justify-content: center;
          color: rgba(255, 255, 255, 0.45);
          font-size: 12px;
          line-height: 1.5;
          text-align: center;
          padding: 24px;
        }
  
        .playground-server-deploy-pill-button {
          height: 32px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 6px;
          padding: 0 14px;
          border: 1px solid rgba(255, 255, 255, 0.14);
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.1);
          color: rgba(255, 255, 255, 0.96);
          font-size: 12px;
          font-weight: 400;
          line-height: 1;
          cursor: pointer;
          transition: background-color 160ms ease, border-color 160ms ease, color 160ms ease, opacity 160ms ease;
        }
  
        .playground-server-deploy-pill-button:hover:not(:disabled) {
          border-color: rgba(255, 255, 255, 0.24);
          background: rgba(255, 255, 255, 0.14);
        }
  
        .playground-server-deploy-pill-button.is-primary {
          border-color: rgba(255, 255, 255, 0.92);
          background: rgba(255, 255, 255, 0.96);
          color: #0f1115;
        }
  
        .playground-server-deploy-pill-button.is-primary:hover:not(:disabled) {
          background: #ffffff;
          border-color: #ffffff;
          color: #0f1115;
        }
  
        .playground-server-deploy-pill-button:disabled {
          opacity: 0.46;
          cursor: default;
        }
  
        .playground-servers-deploy-help-modal {
          width: min(720px, calc(100vw - 32px));
        }
  
        .playground-servers-runtime-preview-modal {
          width: min(860px, calc(100vw - 32px));
        }
  
        .playground-servers-deploy-help-body {
          display: flex;
          flex-direction: column;
          gap: 16px;
        }
  
        .playground-servers-deploy-help-copy {
          color: rgba(255, 255, 255, 0.74);
          font-size: 12px;
          line-height: 1.6;
        }
  
        .playground-servers-editor-bottom-bar {
          position: fixed;
          right: 0;
          bottom: 0;
          left: var(--playground-shell-sidebar-width, 0px);
          z-index: 12;
          flex-shrink: 0;
          box-sizing: border-box;
          margin-top: auto;
          padding: 12px 18px 14px;
          border-top: 0;
          background: rgba(0, 0, 0, 0.8);
          backdrop-filter: blur(50px);
          -webkit-backdrop-filter: blur(50px);
        }
  
        .playground-environments-page .playground-servers-editor-bottom-bar {
          left: calc(var(--playground-shell-sidebar-width, 0px) + 270px);
        }
  
        .playground-resources-page .playground-servers-editor-bottom-bar {
          left: var(--playground-shell-sidebar-width, 0px);
        }
  
        .playground-servers-editor-bottom-bar-inner {
          width: min(100%, var(--playground-thread-content-max-width));
          max-width: var(--playground-thread-content-max-width);
          margin: 0 auto;
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 16px;
        }
  
        .playground-servers-editor-bottom-bar-copy {
          min-width: 0;
          flex: 1;
          display: flex;
          flex-direction: column;
          gap: 8px;
        }
  
        .playground-servers-editor-bottom-message {
          min-width: 0;
          color: rgba(255, 255, 255, 0.96);
          font-size: 12px;
          line-height: 1.45;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-servers-editor-bottom-message.is-success {
          color: #85d6a5;
        }
  
        .playground-servers-editor-bottom-message.is-error {
          color: #ff9c9c;
        }
  
        .playground-servers-editor-bottom-progress-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-servers-editor-bottom-progress-label,
        .playground-servers-editor-bottom-progress-value {
          color: rgba(255, 255, 255, 0.8);
          font-size: 12px;
          line-height: 1;
        }
  
        .playground-servers-editor-bottom-progress-value {
          color: rgba(255, 255, 255, 0.56);
        }
  
        .playground-servers-editor-bottom-progress-track {
          position: relative;
          width: 100%;
          height: 6px;
          border-radius: 999px;
          overflow: hidden;
          background: rgba(255, 255, 255, 0.08);
        }
  
        .playground-servers-editor-bottom-progress-fill {
          position: absolute;
          inset: 0 auto 0 0;
          border-radius: inherit;
          background: linear-gradient(90deg, #7d70f4 0%, #e8b0e5 100%);
          transition: width 280ms ease;
        }
  
        .playground-servers-editor-bottom-actions {
          flex-shrink: 0;
          display: inline-flex;
          align-items: center;
          gap: 8px;
        }
  
        .playground-servers-function-deploy-section {
          display: flex;
          flex-direction: column;
          gap: 14px;
          padding: 0;
          background: rgba(255, 255, 255, 0.03);
          border-radius: 18px;
          overflow: hidden;
        }
  
        .playground-servers-function-deploy-toolbar {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          padding: 0 0 14px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-servers-function-deploy-copy {
          padding: 0 0 2px;
        }
  
        .playground-servers-function-preview-stack {
          display: flex;
          flex-direction: column;
          gap: 0;
        }
  
        .playground-servers-function-preview-pane {
          display: flex;
          flex-direction: column;
          gap: 10px;
          padding: 0;
        }
  
        .playground-servers-function-preview-pane + .playground-servers-function-preview-pane {
          padding-top: 14px;
          border-top: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-servers-function-preview-label {
          color: rgba(255, 255, 255, 0.62);
          font-size: 10px;
          font-weight: 400;
          line-height: 1.4;
          letter-spacing: 0.02em;
          text-transform: uppercase;
        }
  
        .playground-servers-function-editor-shell {
          min-height: 188px;
          height: 188px;
          background: transparent;
          border-radius: 0;
          overflow: hidden;
        }
  
        .playground-servers-function-editor-shell.playground-code-preview-editor-shell,
        .playground-servers-function-editor-shell.playground-code-preview-editor-shell .monaco-editor,
        .playground-servers-function-editor-shell.playground-code-preview-editor-shell .monaco-editor-background,
        .playground-servers-function-editor-shell.playground-code-preview-editor-shell .monaco-scrollable-element,
        .playground-servers-function-editor-shell.playground-code-preview-editor-shell .margin,
        .playground-servers-function-editor-shell.playground-code-preview-editor-shell .overflow-guard {
          background: transparent !important;
        }
  
        .playground-database-browser-surface {
          display: flex;
          flex-direction: column;
          gap: 0;
          padding: 20px;
          border-radius: 15px;
          overflow: hidden;
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-environments-connections-section {
          display: flex;
          flex-direction: column;
          gap: 11px;
        }
  
        .playground-environments-connections-title {
          margin: 0;
        }
  
        .playground-servers-runtime-warning-list {
          display: flex;
          flex-direction: column;
          gap: 2px;
        }
  
        .playground-database-browser-selector-row {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 16px;
          min-height: 56px;
          padding: 0;
          border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-database-browser-selector-label {
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          gap: 10px;
          font-size: 12px;
          font-weight: 400;
          line-height: 1.4;
          color: rgba(255, 255, 255, 0.94);
        }
  
        .playground-database-browser-selector-count {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          min-width: 16px;
          height: 16px;
          padding: 0 3px;
          border-radius: 6px;
          position: relative;
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.62);
          font-size: 10px;
          font-weight: 400;
          line-height: 1;
        }
  
        .playground-database-browser-selector-main {
          min-width: 0;
          flex: 1 1 auto;
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          gap: 10px;
        }
  
        .playground-database-browser-select-shell {
          position: relative;
          min-width: min(240px, 46vw);
          max-width: 100%;
        }
  
        .playground-database-browser-select {
          width: 100%;
          min-width: 0;
          height: 40px;
          padding: 0 36px 0 14px;
          border: 0;
          outline: none;
          appearance: none;
          background: transparent;
          color: rgba(255, 255, 255, 0.96);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.4;
          text-align: right;
          cursor: pointer;
        }
  
        .playground-database-browser-select:disabled {
          cursor: default;
          opacity: 0.5;
        }
  
        .playground-database-browser-select-chevron {
          position: absolute;
          top: 50%;
          right: 12px;
          transform: translateY(-50%);
          color: rgba(255, 255, 255, 0.74);
          pointer-events: none;
        }
  
        .playground-database-browser-selector-action {
          width: auto;
          height: auto;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.78);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          transition: background-color 160ms ease, color 160ms ease;
        }
  
        .playground-database-browser-selector-action:hover:not(:disabled) {
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-database-browser-selector-action:disabled {
          opacity: 0.45;
          cursor: default;
        }
  
        .playground-database-browser-fields-card {
          display: flex;
          flex-direction: column;
          gap: 14px;
          min-height: 360px;
          padding: 16px 0 18px;
        }
  
        .playground-database-summary-divider {
          width: 100%;
          height: 1px;
          background: rgba(255, 255, 255, 0.08);
        }
  
        .playground-database-browser-fields-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-database-browser-fields-title {
          font-size: 12px;
          font-weight: 400;
          line-height: 1.4;
          color: rgba(255, 255, 255, 0.94);
        }
  
        .playground-database-browser-fields-actions {
          display: inline-flex;
          align-items: center;
          gap: 8px;
        }
  
        .playground-database-browser-mode-switch {
          float: right;
          position: absolute;
          right: 12px;
          margin-top: -5px;
          width: auto;
          min-width: 120px;
          gap: 6px;
          padding: 2px;
        }
  
        .playground-database-browser-mode-switch .content-mode-button {
          padding: 4px 10px;
          font-size: 10px;
          font-weight: 400;
        }
  
        .playground-database-browser-add-field {
          display: inline-flex;
          align-items: center;
          gap: 6px;
          margin-right: 5px;
          padding: 0;
          border: 0;
          background: transparent;
          color: #8db2ff;
          font-size: 12px;
          font-weight: 400;
          line-height: 1.4;
          cursor: pointer;
        }
  
        .playground-database-browser-add-field:disabled {
          opacity: 0.45;
          cursor: default;
        }
  
        .playground-database-browser-fields-body {
          display: flex;
          flex-direction: column;
          gap: 8px;
        }
  
        .playground-database-browser-field-tree,
        .playground-database-browser-field-children {
          display: flex;
          flex-direction: column;
          gap: 4px;
        }
  
        .playground-database-browser-field-children {
          position: relative;
        }
  
        .playground-database-browser-field-node {
          display: flex;
          flex-direction: column;
          gap: 4px;
        }
  
        .playground-database-browser-field-row {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 10px;
          min-height: 30px;
        }
  
        .playground-database-browser-field-main {
          min-width: 0;
          flex: 1 1 auto;
          display: inline-flex;
          align-items: center;
          gap: 8px;
        }
  
        .playground-database-browser-field-toggle,
        .playground-database-browser-field-toggle-placeholder {
          width: 18px;
          height: 18px;
          flex: 0 0 18px;
        }
  
        .playground-database-browser-field-toggle {
          padding: 0;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.38);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          transition: color 160ms ease, transform 160ms ease;
        }
  
        .playground-database-browser-field-toggle.is-expanded {
          color: rgba(255, 255, 255, 0.74);
        }
  
        .playground-database-browser-field-toggle.is-expanded svg {
          transform: rotate(0deg);
        }
  
        .playground-database-browser-field-toggle svg {
          transform: rotate(-90deg);
          transition: transform 160ms ease;
        }
  
        .playground-database-browser-field-key,
        .playground-database-browser-field-separator,
        .playground-database-browser-field-preview,
        .playground-database-browser-value-static {
          font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
          font-size: 12px;
          line-height: 1.5;
        }
  
        .playground-database-browser-field-key {
          color: rgba(255, 255, 255, 0.7);
        }
  
        .playground-database-browser-field-separator {
          color: rgba(255, 255, 255, 0.56);
        }
  
        .playground-database-browser-field-group {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          gap: 10px;
        }
  
        .playground-database-browser-field-type-pill {
          display: inline-flex;
          align-items: center;
          min-height: 20px;
          padding: 0 7px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.64);
          font-size: 12px;
          font-weight: 400;
          line-height: 1;
          letter-spacing: 0.02em;
          text-transform: uppercase;
        }
  
        .playground-database-browser-field-preview {
          color: rgba(255, 255, 255, 0.58);
        }
  
        .playground-database-browser-field-value-shell {
          min-width: 0;
          flex: 1 1 auto;
          display: flex;
          align-items: center;
        }
  
        .playground-database-browser-value-input,
        .playground-database-browser-value-select {
          width: 100%;
          min-width: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          box-shadow: none;
          color: rgba(255, 255, 255, 1);
          font-size: 12px;
          outline: none;
        }
  
        .playground-database-browser-value-input {
          width: min(420px, 100%);
          height: auto;
          padding: 0;
        }
  
        .playground-database-browser-value-select {
          height: auto;
          padding: 0 18px 0 0;
          appearance: none;
        }
  
        .playground-database-browser-value-static {
          color: rgba(255, 255, 255, 1);
        }
  
        .playground-database-browser-field-actions {
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          gap: 6px;
          opacity: 0;
          pointer-events: none;
          transition: opacity 160ms ease;
        }
  
        .playground-database-browser-field-row:hover .playground-database-browser-field-actions,
        .playground-database-browser-field-row:focus-within .playground-database-browser-field-actions {
          opacity: 1;
          pointer-events: auto;
        }
  
        .playground-database-browser-field-action {
          width: auto;
          height: auto;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.72);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          transition: background-color 160ms ease, color 160ms ease;
        }
  
        .playground-database-browser-field-action:hover {
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-database-browser-field-action.is-danger:hover {
          color: rgba(255, 173, 173, 0.98);
        }
  
        .playground-database-browser-empty-fields {
          padding: 18px 0 4px;
          color: rgba(255, 255, 255, 0.56);
          font-size: 12px;
          line-height: 1.5;
        }
  
        .playground-database-browser-empty-fields.is-root {
          display: flex;
          flex-direction: column;
          align-items: flex-start;
          gap: 14px;
        }
  
        .playground-database-browser-json-editor-shell {
          min-height: 320px;
          height: 320px;
          display: flex;
          border-radius: 0;
          overflow: hidden;
          background: transparent;
        }
  
        .playground-database-browser-json-editor-shell.playground-code-preview-editor-shell,
        .playground-database-browser-json-editor-shell.playground-code-preview-editor-shell .monaco-editor,
        .playground-database-browser-json-editor-shell.playground-code-preview-editor-shell .monaco-editor-background,
        .playground-database-browser-json-editor-shell.playground-code-preview-editor-shell .monaco-scrollable-element,
        .playground-database-browser-json-editor-shell.playground-code-preview-editor-shell .margin,
        .playground-database-browser-json-editor-shell.playground-code-preview-editor-shell .overflow-guard {
          background: transparent !important;
        }
  
        .playground-database-browser-modal {
          width: min(520px, 100%);
        }
  
        .playground-database-browser-modal-title-row {
          position: relative;
          flex: 1;
          min-width: 0;
          display: flex;
          align-items: center;
          gap: 14px;
        }
  
        .playground-database-browser-modal-title {
          color: rgba(255, 255, 255, 0.98);
          font-size: 17px;
          font-weight: 400;
          line-height: 1.2;
        }
  
        .playground-database-browser-modal-copy {
          margin: 0 0 16px;
          color: rgba(255, 255, 255, 0.58);
          font-size: 12px;
          line-height: 1.55;
        }
  
        .playground-database-browser-modal-grid {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 14px 16px;
          align-items: start;
        }
  
        .playground-database-browser-modal-grid.is-single-column {
          grid-template-columns: minmax(0, 1fr);
        }
  
        .playground-database-browser-modal-field--full {
          grid-column: 1 / -1;
        }
  
        .playground-database-browser-modal-select-shell {
          position: relative;
        }
  
        .playground-database-browser-modal-value-row {
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-database-browser-modal-value-input {
          width: 100%;
        }
  
        .playground-database-browser-modal-hint {
          min-height: 40px;
          display: flex;
          align-items: center;
          padding: 0 12px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 12px;
          background: rgba(255, 255, 255, 0.03);
          color: rgba(255, 255, 255, 0.58);
          font-size: 12px;
          line-height: 1.45;
        }
  
        .playground-database-browser-modal-error {
          color: #ff9797;
          font-size: 12px;
          line-height: 1.4;
        }
  
        .playground-database-browser-modal .playground-tasks-project-modal-label,
        .playground-database-browser-modal .playground-environments-input,
        .playground-database-browser-modal .playground-environments-select,
        .playground-database-browser-modal .playground-environments-textarea {
          font-size: 12px;
        }
  
        .playground-auth-users-toolbar {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          margin-bottom: 14px;
        }
  
        .playground-auth-users-section-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          margin-bottom: 5px;
        }
  
        .playground-auth-users-search {
          position: relative;
          min-width: 0;
          flex: 1 1 auto;
          display: flex;
          align-items: center;
        }
  
        .playground-auth-users-search-icon {
          position: absolute;
          top: 50%;
          left: 14px;
          transform: translateY(-50%);
          color: rgba(255, 255, 255, 0.42);
          pointer-events: none;
        }
  
        .playground-auth-users-search-input {
          width: 100%;
          min-width: 0;
          height: 30px;
          padding: 0 14px 0 38px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.96);
          font-size: 12px;
          outline: none;
        }
  
        .playground-auth-users-search-input::placeholder {
          color: rgba(255, 255, 255, 0.42);
        }
  
        .playground-auth-users-toolbar-actions {
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          gap: 8px;
        }
  
        .playground-auth-users-add-button {
          min-height: 30px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 8px;
          padding: 0 12px;
          border: 1px solid rgba(255, 255, 255, 0.92);
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.96);
          color: #0f1115;
          font-size: 11px;
          font-weight: 500;
          cursor: pointer;
          transition: background-color 160ms ease, border-color 160ms ease, color 160ms ease, opacity 160ms ease;
        }
  
        .playground-auth-users-add-button:hover:not(:disabled) {
          background: #ffffff;
          border-color: #ffffff;
          color: #0f1115;
        }
  
        .playground-auth-users-add-button:disabled {
          opacity: 0.38;
          cursor: default;
        }
  
        .playground-auth-users-refresh-button {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 0;
          padding: 0;
          padding-right: 0;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.62);
          cursor: pointer;
          transition: color 160ms ease, opacity 160ms ease;
        }
  
        .playground-auth-users-refresh-button:hover:not(:disabled) {
          color: rgba(255, 255, 255, 0.92);
        }
  
        .playground-auth-users-refresh-button:disabled {
          opacity: 0.38;
          cursor: default;
        }
  
        .playground-auth-users-table-shell {
          overflow: visible;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
        }
  
        .playground-auth-users-table {
          width: 100%;
          border-collapse: collapse;
          table-layout: fixed;
        }
  
        .playground-auth-users-table.is-secrets-table .playground-secrets-table-col-name {
          width: 44%;
        }
  
        .playground-auth-users-table.is-secrets-table .playground-secrets-table-col-value {
          width: 20%;
        }
  
        .playground-auth-users-table.is-secrets-table .playground-secrets-table-col-updated {
          width: 15%;
        }
  
        .playground-auth-users-table.is-secrets-table .playground-secrets-table-col-accessed {
          width: 16%;
        }
  
        .playground-auth-users-table.is-secrets-table .playground-secrets-table-col-actions {
          width: 52px;
        }
  
        .playground-auth-users-table th,
        .playground-auth-users-table td {
          padding: 12px 14px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.06);
          text-align: left;
          vertical-align: middle;
        }
  
        .playground-auth-users-table th {
          color: rgba(255, 255, 255, 0.56);
          font-size: 11px;
          font-weight: 400;
          line-height: 1.4;
        }
  
        .playground-auth-users-table td {
          color: rgba(255, 255, 255, 0.86);
          font-size: 12px;
          line-height: 1.45;
        }
  
        .playground-auth-users-table tbody tr:last-child td {
          border-bottom: 0;
        }
  
        .playground-auth-users-table tbody tr:hover td {
          background: rgba(255, 255, 255, 0.03);
        }
  
        .playground-auth-users-table tbody tr.is-clickable {
          cursor: pointer;
        }
  
        .playground-auth-users-table tbody tr.is-clickable:focus-visible td {
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-auth-users-cell {
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-auth-users-table th.is-actions,
        .playground-auth-users-table td.is-actions {
          width: 52px;
          text-align: right;
        }
  
        .playground-auth-users-cell.is-secret-name {
          white-space: normal;
        }
  
        .playground-auth-users-secret-name-title,
        .playground-auth-users-secret-description {
          display: block;
          min-width: 0;
          max-width: 100%;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-auth-users-secret-name-title {
          color: rgba(255, 255, 255, 0.94);
          font-size: 12px;
          font-weight: 600;
          line-height: 1.35;
        }
  
        .playground-auth-users-secret-description {
          margin-top: 2px;
          color: rgba(255, 255, 255, 0.48);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.4;
        }
  
        .playground-auth-users-actions {
          display: flex;
          align-items: center;
          justify-content: flex-end;
          gap: 4px;
        }
  
        .playground-auth-users-cell.is-identifier,
        .playground-auth-users-cell.is-uid {
          color: rgba(255, 255, 255, 0.94);
        }
  
        .playground-auth-users-provider-list {
          display: inline-flex;
          align-items: center;
          gap: 6px;
          min-width: 0;
          flex-wrap: wrap;
        }
  
        .playground-auth-users-provider-pill {
          display: inline-flex;
          align-items: center;
          min-height: 20px;
          padding: 0 8px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.82);
          font-size: 10px;
          font-weight: 500;
          line-height: 1;
          letter-spacing: 0.02em;
        }
  
        .playground-auth-users-provider-pill.is-google {
          background: rgba(113, 171, 255, 0.16);
          color: rgba(190, 219, 255, 0.98);
        }
  
        .playground-auth-users-provider-pill.is-microsoft {
          background: rgba(84, 214, 183, 0.16);
          color: rgba(196, 253, 239, 0.98);
        }
  
        .playground-auth-users-provider-pill.is-email {
          min-height: 0;
          padding: 0;
          border-radius: 0;
          background: transparent;
          gap: 6px;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-auth-users-provider-pill.is-phone,
        .playground-auth-users-provider-pill.is-github {
          background: rgba(255, 122, 181, 0.16);
          color: rgba(255, 213, 233, 0.98);
        }
  
        .playground-auth-users-provider-more {
          color: rgba(255, 255, 255, 0.42);
          font-size: 10px;
          line-height: 1;
        }
  
        .playground-servers-analytics-summary,
        .playground-servers-analytics-page {
          display: flex;
          flex-direction: column;
          gap: 16px;
        }
  
        .playground-servers-analytics-summary-grid {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 12px;
        }
  
        .playground-servers-analytics-chart-block {
          min-width: 0;
        }
  
        .playground-servers-analytics-kpi-grid {
          display: grid;
          grid-template-columns: repeat(4, minmax(0, 1fr));
          gap: 10px;
        }
  
        .playground-servers-analytics-kpi {
          min-width: 0;
          padding: 2px 0 0;
        }
  
        .playground-servers-analytics-kpi-value {
          color: rgba(255, 255, 255, 0.96);
          font-size: 24px;
          font-weight: 500;
          line-height: 1.1;
          letter-spacing: -0.02em;
        }
  
        .playground-servers-analytics-kpi-label {
          margin-top: 8px;
          color: rgba(255, 255, 255, 0.52);
          font-size: 11px;
          line-height: 1.4;
        }
  
        .playground-servers-analytics-logs-surface {
          display: flex;
          flex-direction: column;
          gap: 14px;
        }
  
        .playground-servers-logs-section-header.playground-plugins-section-header {
          margin-top: 0;
          padding-bottom: 0;
          border-bottom: 0;
        }
  
        .playground-servers-logs-section-header .playground-plugins-section-title {
          margin-top: 0;
        }
  
        .playground-servers-deployments-surface.is-web-app-history .playground-plugins-section-header {
          align-items: center;
        }
  
        .playground-server-detail-tabs.playground-agents-overview-tabs {
          justify-content: space-between;
          gap: 16px;
        }
  
        .playground-server-detail-tab-actions {
          margin-left: auto;
          display: inline-flex;
          align-items: center;
          gap: 8px;
        }
  
        .playground-server-detail-docs-tab {
          --playground-server-detail-docs-tab-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          position: relative;
          z-index: 0;
          min-height: 32px;
          padding: 0 12px;
          border: 0;
          border-radius: 999px;
          background: transparent;
          overflow: hidden;
          color: rgba(255, 255, 255, 0.9);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 5px;
          white-space: nowrap;
          cursor: pointer;
        }
  
        .playground-server-detail-docs-tab::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-server-detail-docs-tab-border);
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-server-detail-docs-tab > * {
          position: relative;
          z-index: 1;
        }
  
        .playground-server-detail-docs-tab:hover:not(:disabled) {
          background: rgba(255, 255, 255, 0.04);
          color: #fff;
        }
  
        .playground-server-detail-docs-tab:disabled {
          opacity: 0.38;
          cursor: default;
        }
  
        .playground-server-detail-docs-tab-icon {
          width: 11px;
          height: 11px;
          flex: 0 0 auto;
        }
  
        .playground-auth-users-toolbar .playground-server-detail-docs-tab {
          height: 30px;
          min-height: 30px;
          padding: 0 12px;
          gap: 8px;
          box-sizing: border-box;
          font-size: 11px;
          line-height: 1;
        }
  
        .playground-servers-analytics-log-tabs {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          flex-wrap: wrap;
        }
  
        .playground-servers-analytics-log-list {
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-servers-analytics-log-row {
          padding: 12px 14px;
          border-radius: 14px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.03);
        }
  
        .playground-servers-analytics-log-row.is-request {
          padding-bottom: 10px;
        }
  
        .playground-servers-analytics-log-row-top {
          display: flex;
          align-items: center;
          gap: 8px;
          flex-wrap: wrap;
        }
  
        .playground-servers-analytics-log-pill {
          display: inline-flex;
          align-items: center;
          min-height: 22px;
          padding: 0 8px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.12);
          color: rgba(255, 255, 255, 0.92);
          font-size: 10px;
          font-weight: 500;
          line-height: 1;
          text-transform: uppercase;
          letter-spacing: 0.04em;
        }
  
        .playground-servers-analytics-log-pill.is-success {
          background: rgba(94, 213, 143, 0.16);
          color: rgb(127, 238, 168);
        }
  
        .playground-servers-analytics-log-pill.is-info {
          background: rgba(102, 166, 255, 0.16);
          color: rgb(143, 196, 255);
        }
  
        .playground-servers-analytics-log-pill.is-warning {
          background: rgba(255, 191, 102, 0.16);
          color: rgb(255, 209, 129);
        }
  
        .playground-servers-analytics-log-pill.is-error {
          background: rgba(255, 105, 105, 0.16);
          color: rgb(255, 146, 146);
        }
  
        .playground-servers-logs-table.playground-agents-overview-table {
          min-width: 820px;
        }
  
        .playground-servers-logs-table .playground-agents-overview-table-value {
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-servers-logs-loading-state.playground-files-state {
          min-height: 96px;
          padding: 34px 24px;
          overflow: visible;
        }
  
        .playground-servers-logs-table-shell {
          overflow-y: visible;
        }
  
        .playground-servers-logs-table-row {
          cursor: pointer;
        }
  
        .playground-servers-logs-table-row.is-expanded {
          background: rgba(255, 255, 255, 0.02);
        }
  
        .playground-servers-logs-table-detail-row td {
          padding-top: 0;
        }
  
        .playground-servers-logs-expanded-detail {
          margin: 0 0 4px;
          padding: 0 0 14px;
          white-space: pre-wrap;
          word-break: break-word;
          color: rgba(255, 255, 255, 0.74);
          font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
          font-size: 12px;
          font-weight: 400;
          line-height: 1.55;
        }
  
        .playground-servers-logs-expanded-editor-shell {
          min-height: 140px;
          margin: 0 0 8px;
          border-radius: 10px;
          overflow: hidden;
          background: rgba(0, 0, 0, 0.2);
        }
  
        .playground-servers-logs-expanded-editor-shell.playground-code-preview-editor-shell,
        .playground-servers-logs-expanded-editor-shell.playground-code-preview-editor-shell .monaco-editor,
        .playground-servers-logs-expanded-editor-shell.playground-code-preview-editor-shell .monaco-editor-background,
        .playground-servers-logs-expanded-editor-shell.playground-code-preview-editor-shell .monaco-scrollable-element,
        .playground-servers-logs-expanded-editor-shell.playground-code-preview-editor-shell .margin,
        .playground-servers-logs-expanded-editor-shell.playground-code-preview-editor-shell .overflow-guard {
          background: transparent !important;
        }
  
        .playground-servers-logs-load-more-row {
          padding: 14px 20px 16px;
          display: flex;
          justify-content: center;
        }
  
        .playground-servers-logs-load-more-row .playground-environments-action-button {
          border-radius: 999px;
        }
  
        .playground-servers-logs-table-message {
          display: -webkit-box;
          overflow: hidden;
          white-space: normal;
          word-break: break-word;
          -webkit-box-orient: vertical;
          -webkit-line-clamp: 2;
        }
  
        .playground-servers-analytics-log-method,
        .playground-servers-analytics-log-latency,
        .playground-servers-analytics-log-time {
          color: rgba(255, 255, 255, 0.56);
          font-size: 11px;
          line-height: 1.4;
        }
  
        .playground-servers-analytics-log-path {
          margin-top: 8px;
          color: rgba(255, 255, 255, 0.88);
          font-size: 12px;
          line-height: 1.45;
          word-break: break-all;
        }
  
        .playground-servers-analytics-log-message {
          margin: 10px 0 0;
          white-space: pre-wrap;
          word-break: break-word;
          color: rgba(255, 255, 255, 0.82);
          font-size: 12px;
          line-height: 1.55;
          font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
        }
  
        .playground-environments-subsection {
          display: flex;
          flex-direction: column;
          gap: 12px;
        }
  
        .playground-environments-package-group,
        .playground-environments-dockerfile-actions {
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-environments-package-group-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-environments-package-group .playground-tasks-skills-list {
          gap: 8px;
        }
  
        .playground-environments-package-composer {
          display: grid;
          grid-template-columns: minmax(0, 1fr) auto auto;
          gap: 8px;
          align-items: center;
        }
  
        .playground-environments-package-composer-cancel {
          width: 32px;
          height: 32px;
          padding: 0;
          border: 0;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.66);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          transition: background-color 160ms ease, color 160ms ease;
        }
  
        .playground-environments-package-composer-cancel:hover {
          background: rgba(255, 255, 255, 0.12);
          color: rgba(255, 255, 255, 0.9);
        }
  
        .playground-environments-inline-row {
          display: grid;
          grid-template-columns: minmax(0, 1fr) auto;
          gap: 10px;
          align-items: center;
        }
  
        .playground-environments-inline-row-paired {
          display: grid;
          grid-template-columns: minmax(0, 1fr) minmax(0, 1fr) auto;
          gap: 10px;
          align-items: center;
        }
  
        .playground-environments-inline-row-top {
          align-items: flex-start;
          grid-template-columns: minmax(0, 1fr) auto;
        }
  
        .playground-environments-remove-button {
          width: 34px;
          height: 34px;
          padding: 0;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 10px;
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.58);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          transition: background-color 160ms ease, color 160ms ease;
        }
  
        .playground-environments-remove-button:hover {
          background: rgba(255, 255, 255, 0.08);
          color: #fff;
        }
  
        .playground-environments-secret-field {
          position: relative;
        }
  
        .playground-environments-secret-field .playground-environments-input {
          padding-right: 52px;
        }
  
        .playground-environments-secret-indicator {
          position: absolute;
          top: 50%;
          right: 12px;
          transform: translateY(-50%);
          font-size: 11px;
          color: rgba(255, 255, 255, 0.38);
        }
  
        .playground-environments-runtime-row {
          min-height: 32px;
        }
  
        .playground-environments-runtimes-section {
          padding-top: 14px;
        }
  
        .playground-environments-runtime-service {
          min-width: 0;
        }
  
        .playground-environments-runtime-service .playground-tasks-connector-service {
          gap: 0;
        }
  
        .playground-environments-runtime-value-button {
          width: auto;
          min-width: 120px;
          max-width: 100%;
          margin-left: auto;
          padding: 0;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.86);
          font-family: inherit;
          font-size: 12px;
          line-height: 1.35;
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          gap: 6px;
          text-align: right;
          cursor: pointer;
        }
  
        .playground-environments-runtime-value-button:hover {
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-environments-runtime-value-button.is-empty {
          color: rgba(255, 255, 255, 0.62);
        }
  
        .playground-environments-runtime-value-label {
          min-width: 0;
          text-align: right;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-environments-runtime-value-button svg {
          flex: 0 0 auto;
        }
  
        .playground-environments-runtime-popup-shell {
          position: relative;
          display: flex;
          justify-content: flex-end;
          width: 100%;
        }
  
        .playground-environments-runtime-popup-shell.is-open {
          z-index: 60;
        }
  
        .playground-environments-runtime-popup-shell .playground-tasks-toolbar-popup-menu {
          top: calc(100% + 8px);
          bottom: auto;
          left: auto;
          right: 0;
          width: 240px;
          min-width: 240px;
          max-height: min(320px, calc(100vh - 180px));
          display: flex;
          flex-direction: column;
          overflow-y: auto;
          transform-origin: top right;
        }
  
        .playground-environments-runtime-popup-shell.is-open .playground-tasks-toolbar-popup-menu {
          z-index: 61;
        }
  
        .playground-environments-runtime-popup-shell .tb-popup-row {
          width: 100%;
          display: flex;
          align-items: center;
          gap: 12px;
          padding: 12px 16px;
          border: 0;
          background: transparent;
          color: white;
          cursor: pointer;
          text-align: left;
          transition: background-color 160ms ease;
        }
  
        .playground-environments-runtime-popup-shell .tb-popup-row:hover,
        .playground-environments-runtime-popup-shell .tb-popup-row.selected {
          background: rgba(255, 255, 255, 0.1);
        }
  
        .playground-environments-runtime-popup-shell .tb-popup-row-select.selected {
          background: transparent;
        }
  
        .playground-environments-runtime-popup-shell .tb-popup-check-slot {
          width: 16px;
          height: 16px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          flex-shrink: 0;
        }
  
        .playground-environments-runtime-popup-shell .tb-popup-check {
          color: white;
        }
  
        .playground-environments-runtime-toggle,
        .playground-environments-radio-option {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          font-size: 12px;
          color: rgba(255, 255, 255, 0.88);
        }
  
        .playground-environments-runtime-label {
          font-weight: 500;
        }
  
        .playground-environments-radio-row {
          display: inline-flex;
          align-items: center;
          gap: 18px;
        }
  
        .playground-environments-mcp-card {
          padding: 14px;
          display: flex;
          flex-direction: column;
          gap: 12px;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-mcp-card.playground-environments-editor-surface {
          padding: 12px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 8px;
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-computer-detail-content .playground-environments-mcp-card.playground-environments-editor-surface {
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
        }
  
        .playground-environments-empty-copy {
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.42);
        }
  
        .playground-environments-dockerfile-actions {
          flex-direction: row;
          flex-wrap: wrap;
          padding-top: 2px;
        }
  
        .playground-environments-dockerfile-preview {
          margin: 0;
          padding: 14px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 14px;
          background: rgba(0, 0, 0, 0.36);
          color: rgba(255, 255, 255, 0.82);
          font-size: 11px;
          line-height: 1.55;
          white-space: pre-wrap;
          word-break: break-word;
          overflow: auto;
        }
  
        .playground-environments-desktop-header-actions {
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          flex-wrap: wrap;
          gap: 8px;
        }
  
        .playground-environments-desktop-header-button {
          min-height: 28px;
          padding: 0 12px;
          border: 0;
          border-radius: 999px;
          background: #fff;
          color: #000;
          font-size: 12px;
          font-weight: 400;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 6px;
        }
  
        .playground-environments-desktop-header-button:hover:not(:disabled) {
          background: rgba(255, 255, 255, 0.92);
          color: #000;
        }
  
        .playground-environments-desktop-header-button.is-secondary {
          background: rgba(255, 255, 255, 0.1);
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-environments-desktop-header-button.is-secondary:hover:not(:disabled) {
          background: rgba(255, 255, 255, 0.16);
          color: #fff;
        }
  
        .playground-environments-desktop-status {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          min-height: 30px;
          padding: 0 10px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.72);
          font-size: 12px;
          line-height: 1;
          white-space: nowrap;
        }
  
        .playground-environments-desktop-status.is-running {
          background: rgba(52, 211, 153, 0.18);
          color: rgba(167, 243, 208, 0.96);
        }
  
        .playground-environments-desktop-status.is-starting {
          background: rgba(250, 204, 21, 0.16);
          color: rgba(254, 240, 138, 0.96);
        }
  
        .playground-environments-desktop-status.is-error {
          background: rgba(248, 113, 113, 0.16);
          color: rgba(254, 202, 202, 0.96);
        }
  
        .playground-computer-detail-navbar .playground-environments-desktop-header-button.playground-environments-action-button,
        .playground-computer-detail-navbar .playground-environments-desktop-status {
          --playground-computer-detail-control-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          position: relative;
          z-index: 0;
          overflow: hidden;
          min-height: 30px;
          border: 0;
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.96);
          -webkit-backdrop-filter: blur(50px);
          backdrop-filter: blur(50px);
        }
  
        .playground-computer-detail-navbar .playground-environments-desktop-header-button.playground-environments-action-button::before,
        .playground-computer-detail-navbar .playground-environments-desktop-status::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-computer-detail-control-border);
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-computer-detail-navbar .playground-environments-desktop-header-button.playground-environments-action-button > * {
          position: relative;
          z-index: 1;
        }
  
        .playground-computer-detail-navbar .playground-environments-desktop-header-button.playground-environments-action-button:hover:not(:disabled),
        .playground-computer-detail-navbar .playground-environments-desktop-header-button.playground-environments-action-button.is-secondary:hover:not(:disabled) {
          background: transparent;
          color: #fff;
        }
  
        .playground-computer-detail-navbar .playground-environments-desktop-status.is-running {
          --playground-computer-detail-control-border: linear-gradient(
            -10deg,
            rgba(52, 211, 153, 0.55),
            rgba(52, 211, 153, 0.12),
            rgba(167, 243, 208, 0.18),
            rgba(167, 243, 208, 0.45)
          );
          background: transparent;
          color: rgba(167, 243, 208, 0.96);
        }
  
        .playground-computer-detail-navbar .playground-environments-desktop-status.is-starting {
          --playground-computer-detail-control-border: linear-gradient(
            -10deg,
            rgba(250, 204, 21, 0.55),
            rgba(250, 204, 21, 0.12),
            rgba(254, 240, 138, 0.18),
            rgba(254, 240, 138, 0.45)
          );
          background: transparent;
          color: rgba(254, 240, 138, 0.96);
        }
  
        .playground-computer-detail-navbar .playground-environments-desktop-status.is-error {
          --playground-computer-detail-control-border: linear-gradient(
            -10deg,
            rgba(248, 113, 113, 0.55),
            rgba(248, 113, 113, 0.12),
            rgba(254, 202, 202, 0.18),
            rgba(254, 202, 202, 0.45)
          );
          background: transparent;
          color: rgba(254, 202, 202, 0.96);
        }
  
        .playground-computer-detail-navbar .playground-environments-desktop-status.is-stopped {
          --playground-computer-detail-control-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.24),
            rgba(255, 255, 255, 0.08),
            rgba(255, 255, 255, 0.12),
            rgba(255, 255, 255, 0.3)
          );
          background: transparent;
          color: rgba(255, 255, 255, 0.66);
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-server-detail-navbar .playground-environments-action-button {
          --playground-server-detail-control-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          position: relative;
          z-index: 0;
          overflow: hidden;
          min-height: 30px;
          border: 0;
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.96);
          -webkit-backdrop-filter: blur(50px);
          backdrop-filter: blur(50px);
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-server-detail-navbar .playground-environments-action-button::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-server-detail-control-border);
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-server-detail-navbar .playground-environments-action-button > * {
          position: relative;
          z-index: 1;
        }
  
        .playground-resources-page.is-develop-server-kind-page .playground-server-detail-navbar .playground-environments-action-button:hover:not(:disabled),
        .playground-resources-page.is-develop-server-kind-page .playground-server-detail-navbar .playground-environments-action-button.is-primary:hover:not(:disabled) {
          background: transparent;
          color: #fff;
        }
  
        .playground-servers-deployments-surface.is-web-app-history .playground-environments-action-button {
          --playground-server-detail-control-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          position: relative;
          z-index: 0;
          overflow: hidden;
          min-height: 30px;
          border: 0;
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.96);
          -webkit-backdrop-filter: blur(50px);
          backdrop-filter: blur(50px);
        }
  
        .playground-servers-deployments-surface.is-web-app-history .playground-environments-action-button::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-server-detail-control-border);
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-servers-deployments-surface.is-web-app-history .playground-environments-action-button > * {
          position: relative;
          z-index: 1;
        }
  
        .playground-servers-deployments-surface.is-web-app-history .playground-environments-action-button:hover:not(:disabled) {
          background: transparent;
          color: #fff;
        }
  
        .playground-environments-desktop-section {
          display: flex;
          flex-direction: column;
          gap: 12px;
        }
  
        .playground-environments-desktop-viewer-shell {
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-environments-desktop-overlay {
          position: fixed;
          inset: 0;
          z-index: 240;
          display: flex;
          flex-direction: column;
          gap: 0;
          padding: 0;
          background: #000;
        }
  
        .playground-environments-desktop-overlay-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          padding: 14px 18px 12px;
          background: #000;
        }
  
        .playground-environments-desktop-overlay-title {
          min-width: 0;
          display: flex;
          align-items: center;
        }
  
        .playground-environments-desktop-overlay-label {
          font-size: 14px;
          line-height: 1.2;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.96);
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-environments-desktop-overlay-actions {
          display: flex;
          align-items: center;
          justify-content: flex-end;
          gap: 8px;
          flex-wrap: nowrap;
        }
  
        .playground-environments-desktop-overlay-body {
          min-height: 0;
          flex: 1 1 auto;
          display: flex;
          flex-direction: column;
          gap: 0;
          background: #000;
        }
  
        .playground-environments-desktop-overlay-footer {
          display: flex;
          align-items: center;
          justify-content: center;
          gap: 10px;
          padding: 12px 18px 14px;
          background: #000;
        }
  
        .playground-environments-desktop-viewer {
          position: relative;
          width: 100%;
          overflow: hidden;
          border: 0;
          border-radius: 0;
          background: #000;
          min-height: 240px;
          display: flex;
          align-items: center;
          justify-content: center;
        }
  
        .playground-environments-desktop-overlay .playground-environments-desktop-viewer-shell {
          min-height: 0;
          flex: 1 1 auto;
          background: #000;
        }
  
        .playground-environments-desktop-overlay .playground-environments-desktop-viewer {
          min-height: 0;
          flex: 1 1 auto;
        }
  
        .playground-environments-desktop-image {
          display: block;
          width: 100%;
          height: auto;
          user-select: none;
          cursor: crosshair;
          background: #000;
        }
  
        .playground-environments-desktop-iframe {
          display: block;
          width: 100%;
          height: 100%;
          min-height: 100%;
          border: 0;
          background: #000;
        }
  
        .playground-environments-desktop-placeholder {
          min-height: 240px;
          width: 100%;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          gap: 10px;
          color: rgba(255, 255, 255, 0.52);
          font-size: 12px;
        }
  
        .playground-environments-desktop-placeholder-copy {
          display: flex;
          flex-direction: column;
          align-items: center;
          gap: 4px;
          text-align: center;
        }
  
        .playground-environments-desktop-placeholder-title {
          font-size: 13px;
          line-height: 1.35;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.92);
        }
  
        .playground-environments-desktop-placeholder-subtitle {
          font-size: 12px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.54);
        }
  
        .playground-environments-desktop-loading {
          position: absolute;
          top: 12px;
          right: 12px;
          width: 28px;
          height: 28px;
          border-radius: 999px;
          background: rgba(10, 10, 14, 0.62);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          color: rgba(255, 255, 255, 0.88);
        }
  
        .playground-environments-desktop-hint {
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.42);
        }
  
        .playground-environments-desktop-restart-button {
          min-height: 28px;
          padding: 0 12px;
          border: 0;
          border-radius: 999px;
          background: #fff;
          color: #000;
          font-size: 12px;
          font-weight: 400;
        }
  
        .playground-environments-desktop-restart-button:hover:not(:disabled) {
          background: rgba(255, 255, 255, 0.92);
          color: #000;
        }
  
        .playground-environments-desktop-close-button {
          min-height: auto;
          padding: 0;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.82);
        }
  
        .playground-environments-desktop-close-button:hover:not(:disabled) {
          background: transparent;
          color: #fff;
        }
  
        .playground-environments-desktop-footer-button {
          min-height: auto;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
        }
  
        .playground-environments-desktop-footer-button:hover:not(:disabled) {
          background: transparent;
        }
  
        .playground-environments-desktop-footer-icon {
          display: block;
          width: 42px;
          height: 42px;
          border-radius: 13px;
          object-fit: cover;
          box-shadow: 0 10px 24px rgba(0, 0, 0, 0.28);
        }
  
        .playground-environments-desktop-footer-glyph {
          width: 42px;
          height: 42px;
          padding: 9px;
          border-radius: 13px;
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.92);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          box-shadow: 0 10px 24px rgba(0, 0, 0, 0.28);
        }
  
        .playground-environments-desktop-footer-button:disabled .playground-environments-desktop-footer-icon {
          opacity: 0.4;
        }
  
        .playground-environments-desktop-footer-button:disabled .playground-environments-desktop-footer-glyph {
          opacity: 0.4;
        }
  
        .playground-environments-desktop-input-row {
          display: grid;
          grid-template-columns: minmax(0, 1fr) auto auto auto;
          gap: 8px;
          align-items: center;
        }
  
        .playground-environments-desktop-input {
          min-width: 0;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-environments-desktop-section .playground-environments-action-button {
          min-height: 34px;
        }
  
        .playground-environments-spin {
          animation: spinner-rotate 1s linear infinite;
        }
  
        .playground-agents-page {
          height: 100%;
          min-height: 0;
          background: transparent;
          --playground-environments-nav-top-offset: 10px;
          --playground-environments-nav-row-height: 30px;
          --playground-environments-list-title-size: 14px;
          --playground-environments-detail-title-size: 20px;
        }
  
        .playground-agents-page .playground-environments-list-header {
          padding: var(--playground-environments-nav-top-offset) 12px 8px;
        }
  
        .playground-agents-page .playground-environments-list-title {
          display: flex;
          align-items: center;
          min-height: var(--playground-environments-nav-row-height);
          font-size: var(--playground-environments-list-title-size);
          font-weight: 500;
          line-height: 1;
          letter-spacing: -0.01em;
        }
  
        .playground-agents-page .playground-files-topbar {
          min-height: var(--playground-environments-nav-row-height);
        }
  
        .playground-agents-page .playground-files-topbar-actions {
          min-height: var(--playground-environments-nav-row-height);
          gap: 4px;
        }
  
        .playground-agents-page .playground-files-header-icon-button {
          width: 32px;
          height: 32px;
          border-radius: 10px;
        }
  
        .playground-agents-page .playground-files-header-icon-button.is-plain {
          width: 28px;
          height: 28px;
          border-radius: 9px;
        }
  
        .playground-agents-page .playground-environments-list-body {
          scrollbar-width: none;
          -ms-overflow-style: none;
        }
  
        .playground-agents-page .playground-environments-list-body::-webkit-scrollbar {
          display: none;
          width: 0;
          height: 0;
        }
  
        .playground-agents-page .playground-environments-list-item {
          gap: 14px;
          min-height: 38px;
          padding: 8px 12px;
          border-radius: 10px;
        }
  
        .playground-agents-page .playground-environments-list-item-copy {
          align-items: center;
          flex-direction: row;
          gap: 0;
        }
  
        .playground-agents-page .playground-environments-list-item-title {
          font-size: 12px;
          font-weight: 500;
          line-height: 1.2;
          color: rgba(255, 255, 255, 0.7);
        }
  
        .playground-agents-page .playground-environments-list-item-meta {
          display: none;
        }
  
        .playground-agents-page .playground-environments-list-item-icon {
          width: 14px;
          height: 14px;
          color: rgba(255, 255, 255, 0.7);
        }
  
        .playground-agents-page .playground-environments-list-item.is-active .playground-environments-list-item-title,
        .playground-agents-page .playground-environments-list-item.is-active .playground-environments-list-item-icon {
          color: rgba(255, 255, 255, 1);
        }
  
        .playground-agents-list-section {
          display: flex;
          flex-direction: column;
          gap: 4px;
        }
  
        .playground-agents-list-section.is-system {
          margin-top: 10px;
        }
  
        .playground-agents-list-section + .playground-agents-list-section {
          margin-top: 10px;
        }
  
        .playground-agents-page .sidebar-thread-section-chevron {
          margin-right: 1px;
        }
  
        .playground-agents-page .playground-environments-detail-scroll {
          gap: 24px;
        }
  
        .playground-agents-detail-content {
          width: min(100%, var(--playground-thread-content-max-width));
          max-width: var(--playground-thread-content-max-width);
          margin: 0 auto;
          display: flex;
          flex-direction: column;
          gap: 24px;
        }
  
        .playground-agents-detail-main-pane .playground-environments-detail-scroll {
          scrollbar-width: none;
          -ms-overflow-style: none;
        }
  
        .playground-agents-detail-main-pane .playground-environments-detail-scroll::-webkit-scrollbar {
          display: none;
        }
  
        .playground-agents-detail-content .playground-agents-profile-avatar.profile-editor-avatar {
          width: 100px;
          height: 100px;
        }
  
        .playground-agents-page .playground-environments-detail-title-row {
          min-height: var(--playground-environments-nav-row-height);
          padding-left: 2px;
        }
  
        .playground-agents-page .playground-environments-detail-title {
          font-size: var(--playground-environments-detail-title-size);
          line-height: 1;
          letter-spacing: -0.01em;
        }
  
        .playground-agents-page .playground-environments-home-hero-title {
          margin-top: 44px;
        }
  
        .playground-agents-page .playground-environments-detail-actions {
          min-height: var(--playground-environments-nav-row-height);
          align-items: center;
          gap: 6px;
        }
  
        .playground-agents-page .playground-environments-section,
        .playground-agents-page .playground-environments-mcp-card {
          border: 0;
          border-radius: 0;
          background: transparent;
        }
  
        .playground-agents-page .playground-environments-section-header {
          padding: 0 0 10px;
        }
  
        .playground-agents-page .playground-environments-section-header.is-static {
          padding-bottom: 6px;
        }
  
        .playground-agents-page .playground-environments-section[data-section-id="model"] > .playground-environments-section-header.is-static {
          padding-bottom: 11px;
        }
  
        .playground-agents-page .playground-environments-section[data-section-id="model"],
        .playground-agents-page .playground-environments-section[data-section-id="model"] > .playground-environments-section-body {
          position: relative;
          overflow: visible;
          z-index: 4;
        }
  
        .playground-agents-page .playground-environments-section-body {
          padding: 0;
          border-top: 0;
        }
  
        .playground-agents-page .playground-environments-field-grid,
        .playground-agents-page .playground-environments-stack {
          padding-top: 4px;
        }
  
        .playground-agents-page .playground-environments-section-title {
          font-size: 14px;
          font-weight: 400;
          line-height: 1.3;
        }
  
        .playground-agents-page .playground-environments-section-copy {
          font-size: 12px;
        }
  
        .playground-agents-page .playground-environments-editor-surface,
        .playground-agents-page .playground-agents-model-hint,
        .playground-agents-page .playground-agents-skill-card,
        .playground-agents-page .playground-agents-team-card,
        .playground-agents-page .playground-agents-team-hint,
        .playground-agents-page .playground-agents-team-readonly {
          padding: 12px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 8px;
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-agents-page .playground-tasks-detail-facts {
          position: relative;
          overflow: visible;
          z-index: 4;
          border: 1px solid rgba(255, 255, 255, 0.05);
          border-radius: 10px;
          background: rgba(255, 255, 255, 0.05);
          padding: 12px;
          backdrop-filter: blur(50px);
          -webkit-backdrop-filter: blur(50px);
        }
  
        .playground-agents-page .playground-tasks-detail-facts.is-popover-open {
          z-index: 220;
        }
  
        .playground-agents-page .playground-tasks-detail-facts::before {
          content: none;
          display: none;
        }
  
        .playground-agents-detail-layout {
          min-width: 0;
          min-height: 0;
          flex: 1;
          display: grid;
          grid-template-columns: minmax(0, 1fr);
          overflow: hidden;
          transition: grid-template-columns 240ms ease;
        }
  
        .playground-agents-detail-layout.has-assistant {
          grid-template-columns: minmax(0, 1fr) 0;
        }
  
        .playground-agents-detail-layout.has-assistant.is-assistant-open {
          grid-template-columns: minmax(0, 1fr) 400px;
        }
  
        .playground-agents-detail-layout.is-creation-setup {
          grid-template-columns: minmax(0, 1fr);
        }
  
        .playground-agents-page.is-creation-setup .playground-environments-shell {
          grid-template-columns: minmax(0, 1fr);
        }
  
        .playground-agents-page.is-creation-setup .playground-environments-list-pane {
          display: none;
        }
  
        .playground-agents-detail-main-pane {
          min-width: 0;
          min-height: 0;
          display: flex;
          flex-direction: column;
          overflow: hidden;
        }
  
        .playground-agents-assistant-sidebar {
          min-width: 0;
          min-height: 0;
          width: 400px;
          display: flex;
          flex-direction: column;
          background: #000;
          overflow: hidden;
          opacity: 0;
          pointer-events: none;
          transform: translateX(100%);
          transition: transform 240ms ease, opacity 180ms ease;
          will-change: transform, opacity;
        }
  
        .playground-agents-detail-layout.is-assistant-open .playground-agents-assistant-sidebar {
          opacity: 1;
          pointer-events: auto;
          transform: translateX(0);
        }
  
        .playground-agents-versions-sidebar {
          border-left: 0;
        }
  
        .playground-agents-versions-sidebar .playground-metronome-inspector-body {
          padding-top: 0;
        }
  
        .playground-agents-versions-sidebar .playground-metronome-publish-row-main:disabled {
          cursor: default;
        }
  
        .playground-agents-versions-sidebar .playground-metronome-publish-row-action {
          min-width: 0;
        }
  
        .playground-agents-versions-sidebar .playground-metronome-publish-section-footer {
          margin-top: 10px;
          display: flex;
          align-items: center;
        }
  
        .playground-agents-version-compare-button {
          width: 100%;
          justify-content: center;
          border: 0 !important;
          border-radius: 0 !important;
          background: transparent !important;
          box-shadow: none !important;
          -webkit-backdrop-filter: none;
          backdrop-filter: none;
        }
  
        .playground-agents-version-compare-button:hover:not(:disabled),
        .playground-agents-version-compare-button:focus-visible {
          background: transparent !important;
        }
  
        .playground-agents-version-compare-button::before {
          content: none;
          display: none;
        }
  
        .playground-version-changes-page {
          width: 100%;
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 14px;
        }
  
  ${GUARDRAILS_STYLE_FRAGMENTS.versionChanges}      .playground-version-changes-header {
          display: flex;
          flex-direction: column;
          gap: 12px;
          padding-bottom: 14px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-version-changes-title-row {
          min-width: 0;
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 16px;
        }
  
        .playground-version-changes-title-copy {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 5px;
        }
  
        .playground-version-changes-title-link {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          gap: 8px;
          align-self: flex-start;
          padding: 0;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.96);
          text-align: left;
          cursor: pointer;
        }
  
        .playground-version-changes-title-link:hover .playground-version-changes-title,
        .playground-version-changes-title-link:hover .playground-version-changes-title-back-icon {
          color: rgba(255, 255, 255, 1);
        }
  
        .playground-version-changes-title-back-icon {
          flex: 0 0 auto;
          color: rgba(255, 255, 255, 0.72);
          transition: color 160ms ease;
        }
  
        .playground-version-changes-title {
          margin: 0;
          color: rgba(255, 255, 255, 0.96);
          font-size: 18px;
          line-height: 1.15;
          font-weight: 500;
          letter-spacing: 0;
          transition: color 160ms ease;
        }
  
        .playground-version-changes-subtitle,
        .playground-version-changes-compare-labels {
          color: rgba(255, 255, 255, 0.56);
          font-size: 12px;
          line-height: 1.4;
          font-weight: 400;
        }
  
        .playground-version-changes-summary {
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          gap: 8px;
          color: rgba(255, 255, 255, 0.62);
          font-size: 11px;
          line-height: 1;
          font-weight: 400;
        }
  
        .playground-version-changes-summary .is-additions {
          color: #3fb950;
        }
  
        .playground-version-changes-summary .is-deletions {
          color: #f85149;
        }
  
        .playground-version-changes-compare-labels {
          display: flex;
          align-items: center;
          gap: 8px;
          min-width: 0;
        }
  
        .playground-version-changes-compare-row {
          min-width: 0;
          display: flex;
          align-items: flex-end;
          justify-content: space-between;
          gap: 12px;
          flex-wrap: wrap;
        }
  
        .playground-version-changes-compare-selectors {
          min-width: 0;
          display: flex;
          align-items: flex-end;
          gap: 10px;
          flex-wrap: wrap;
          flex: 1 1 auto;
        }
  
        .playground-version-changes-compare-actions {
          margin-left: auto;
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          gap: 8px;
          flex: 0 0 auto;
        }
  
        .playground-version-changes-select-shell {
          min-width: 190px;
          display: flex;
          flex-direction: column;
          gap: 0;
        }
  
        .playground-version-changes-select-label {
          color: rgba(255, 255, 255, 0.52);
          font-size: 10px;
          line-height: 1;
          font-weight: 400;
        }
  
        .playground-version-changes-select-control-wrap {
          position: relative;
          min-width: 0;
          display: flex;
          align-items: center;
        }
  
        .playground-version-changes-select-control,
        .playground-version-changes-select {
          width: 100%;
          height: 32px;
          appearance: none;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.92);
          font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
          font-size: 12px;
          line-height: 1;
          font-weight: 400;
          padding: 0 30px 0 12px;
          outline: none;
        }
  
        .playground-version-changes-select-control:focus,
        .playground-version-changes-select:focus {
          border-color: rgba(255, 255, 255, 0.22);
        }
  
        .playground-version-changes-select-control-wrap > svg {
          position: absolute;
          right: 11px;
          color: rgba(255, 255, 255, 0.62);
          pointer-events: none;
        }
  
        .playground-version-changes-select-arrow,
        .playground-version-changes-compare-arrow {
          color: rgba(255, 255, 255, 0.48);
          font-size: 13px;
          line-height: 32px;
          padding-bottom: 1px;
        }
  
        .playground-version-changes-file-list {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 12px;
        }
  
        .playground-version-changes-file-card {
          min-width: 0;
        }
  
        .playground-version-changes-file-card .tb-runner-diff-surface {
          overflow: hidden;
          border: 1px solid rgba(255, 255, 255, 0.15);
          border-radius: 10px;
        }
  
        .playground-version-changes-empty {
          min-height: 180px;
          display: flex;
          align-items: center;
          justify-content: center;
          color: rgba(255, 255, 255, 0.56);
          font-size: 12px;
          line-height: 1.4;
        }
  
        @media (max-width: 720px) {
          .playground-version-changes-title-row {
            flex-direction: column;
          }
  
          .playground-version-changes-select-shell {
            min-width: min(100%, 260px);
          }
        }
  
        .tb-runner-chat.playground-agents-assistant-runner {
          flex: 1 1 auto;
          min-width: 0;
          min-height: 0;
          width: 100%;
          height: 100%;
          background: #000;
        }
  
        .tb-runner-chat.playground-agents-assistant-runner .runnerintegrationtopnav {
          display: none;
        }
  
        .tb-runner-chat.playground-agents-assistant-runner .tb-log-scroll,
        .tb-runner-chat.playground-agents-assistant-runner .tb-input-shell {
          padding-left: 18px;
          padding-right: 18px;
        }
  
        .tb-runner-chat.playground-agents-assistant-runner .tb-thread-history-rail {
          right: 18px;
        }
  
        .tb-runner-chat.playground-agents-assistant-runner .tb-log-scroll.is-custom-empty-state {
          display: flex;
          align-items: center;
          justify-content: center;
          padding: 28px 18px;
        }
  
        .tb-runner-chat.playground-agents-assistant-runner .tb-content-width.is-custom-empty-state {
          width: 100%;
          max-width: none;
        }
  
        .playground-agents-assistant-empty-state {
          width: 100%;
          margin-top: -55px;
          display: flex;
          flex-direction: column;
          align-items: center;
          gap: 14px;
        }
  
        .playground-agents-assistant-empty-title {
          width: 100%;
          margin: 0;
          padding-bottom: 12px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
          color: rgba(255, 255, 255, 0.94);
          font-size: 18px;
          font-weight: 500;
          line-height: 1.35;
          text-align: center;
        }
  
        .playground-agents-assistant-empty-actions {
          width: min(360px, 100%);
          display: flex;
          align-items: center;
          justify-content: center;
          flex-direction: column;
          gap: 0;
        }
  
        .playground-agents-assistant-empty-button {
          width: 100%;
          height: 40px;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.7);
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 16px;
          font-family: inherit;
          font-size: 12px;
          font-weight: 400;
          line-height: 1;
          cursor: pointer;
          text-align: left;
          transition: color 160ms ease, opacity 160ms ease;
        }
  
        .playground-agents-assistant-empty-button:hover:not(:disabled) {
          color: #fff;
        }
  
        .playground-agents-assistant-empty-button-icon {
          width: 14px;
          height: 14px;
          flex: 0 0 auto;
        }
  
        .playground-agents-assistant-empty-button:disabled {
          opacity: 0.45;
          cursor: default;
        }
  
        .playground-agents-creation-thread-detail {
          overflow: hidden;
        }
  
        .playground-agents-creation-setup-pane {
          min-width: 0;
          min-height: 0;
          display: flex;
          flex-direction: column;
          overflow: hidden;
        }
  
        .tb-runner-chat.playground-agents-creation-runner {
          flex: 1 1 auto;
          min-width: 0;
          min-height: 0;
          width: 100%;
          height: 100%;
        }
  
        .tb-runner-chat.playground-agents-creation-runner .tb-log-scroll.is-custom-empty-state {
          position: relative;
          display: flex;
          align-items: center;
          justify-content: center;
          padding: 28px;
        }
  
        .tb-runner-chat.playground-agents-creation-runner .tb-content-width.is-custom-empty-state {
          width: min(100%, 760px);
          max-width: 760px;
        }
  
        .tb-runner-chat.playground-agents-creation-runner .task-input-box {
          --tb-task-input-outline: rgba(255, 255, 255, 0.1);
          --tb-task-input-border: rgba(255, 255, 255, 0.1);
        }
  
        .tb-runner-chat.playground-agents-creation-runner .task-input-box:has(.sidebar-textarea:focus) {
          --tb-task-input-outline: rgba(77, 163, 255, 0.42);
          --tb-task-input-border: linear-gradient(-10deg, rgba(77, 163, 255, 0.24), rgba(141, 197, 255, 0.58), rgba(77, 163, 255, 0.3), rgba(77, 163, 255, 0.66));
        }
  
        .playground-agents-creation-form {
          width: 100%;
          margin: 0 auto;
          display: flex;
          flex-direction: column;
          gap: 0;
        }
  
        .tb-runner-chat.playground-agents-creation-runner .playground-agents-creation-form > .playground-resource-detail-back-button {
          position: absolute;
          top: 28px;
          left: 28px;
          z-index: 2;
          margin: 0;
        }
  
        .playground-agents-creation-form .playground-agents-profile-avatar-wrap {
          margin-top: -55px;
        }
  
        .playground-agents-creation-form .playground-agents-profile-name-input::placeholder {
          color: rgba(255, 255, 255, 0.5);
          opacity: 1;
        }
  
        .playground-agents-creation-form .playground-agents-profile-actions,
        .playground-agents-creation-form .playground-agents-profile-email-group {
          display: none;
        }
  
        .playground-agents-creation-config-box {
          --playground-agents-creation-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          width: 100%;
          margin-top: 20px;
          padding: 20px;
          position: relative;
          z-index: 0;
          border: 0;
          border-radius: 15px;
          background: transparent;
          display: flex;
          flex-direction: column;
          gap: 6px;
          overflow: hidden;
        }
  
        .playground-agents-creation-config-box::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-agents-creation-border);
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-agents-creation-config-box > * {
          position: relative;
          z-index: 1;
        }
  
        .playground-agents-creation-config-box .playground-tasks-detail-description.playground-environments-editor-description {
          margin-top: 0;
          padding-top: 0;
        }
  
        .playground-agents-creation-instructions-section .playground-tasks-detail-section-header {
          margin-bottom: 8px;
        }
  
        .playground-agents-creation-instructions-section .playground-tasks-detail-description-editor {
          height: 88px;
          min-height: 88px;
          max-height: 88px;
          overflow: hidden;
          cursor: text;
        }
  
        .playground-agents-creation-instructions-section .playground-tasks-detail-description-input,
        .playground-agents-creation-instructions-section .playground-tasks-detail-description-preview {
          height: 88px;
          min-height: 88px;
          max-height: 88px;
          padding-bottom: 0;
        }
  
        .playground-agents-creation-instructions-section .playground-tasks-detail-description-preview-scope.tb-runner-chat {
          overflow-y: auto;
          scrollbar-width: none;
          pointer-events: auto;
        }
  
        .playground-agents-creation-instructions-section .playground-tasks-detail-description-input {
          height: 88px !important;
          overflow-y: auto;
          scrollbar-width: none;
        }
  
        .playground-agents-creation-instructions-section .playground-tasks-detail-description-preview-scope.tb-runner-chat::-webkit-scrollbar,
        .playground-agents-creation-instructions-section .playground-tasks-detail-description-input::-webkit-scrollbar {
          display: none;
        }
  
        .playground-agents-creation-instructions-section .playground-tasks-detail-description-editor.is-preview .playground-tasks-detail-description-input {
          pointer-events: none;
        }
  
        .playground-agents-creation-settings {
          width: 100%;
        }
  
        .playground-agents-creation-actions {
          display: flex;
          align-items: center;
          justify-content: flex-end;
          gap: 8px;
          margin-top: 2px;
        }
  
        .playground-agents-creation-action-button {
          min-height: 30px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 6px;
          padding: 0 14px;
          border-radius: 999px;
          border: 1px solid transparent;
          font-size: 12px;
          font-weight: 400;
          line-height: 1;
          font-family: inherit;
          cursor: pointer;
          transition: background-color 160ms ease, border-color 160ms ease, color 160ms ease, opacity 160ms ease;
        }
  
        .playground-agents-creation-action-button:disabled {
          opacity: 0.45;
          cursor: default;
        }
  
        .playground-agents-creation-action-button.is-secondary {
          --playground-agents-creation-button-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          position: relative;
          z-index: 0;
          border: 0;
          background: transparent;
          color: #fff;
          overflow: hidden;
        }
  
        .playground-agents-creation-action-button.is-secondary::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-agents-creation-button-border);
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-agents-creation-action-button.is-secondary:hover:not(:disabled) {
          background: rgba(255, 255, 255, 0.04);
        }
  
        .playground-agents-creation-action-button.is-primary {
          border-color: #fff;
          background: #fff;
          color: #000;
        }
  
        .playground-agents-creation-action-button.is-primary:hover:not(:disabled) {
          background: rgba(255, 255, 255, 0.92);
          border-color: rgba(255, 255, 255, 0.92);
        }
  
        .playground-agents-creation-form .playground-environments-editor-notice {
          margin-top: 16px;
        }
  
        .playground-computer-creation-scroll {
          align-items: center;
        }
  
        .playground-computer-creation-form {
          width: min(100%, 760px);
          max-width: 760px;
          justify-content: center;
        }
  
        .playground-computer-creation-form .playground-agents-profile-avatar-wrap {
          margin-top: 0;
        }
  
        .playground-computer-creation-form .playground-agents-profile-name-wrap {
          margin-top: 8px;
        }
  
        .playground-computer-creation-avatar .profile-editor-avatar-surface {
          color: #fff;
          background: rgba(255, 255, 255, 0.06);
        }
  
        .playground-computer-creation-config-box {
          gap: 18px;
        }
  
        .playground-computer-creation-header {
          display: flex;
          flex-direction: column;
          gap: 6px;
          width: 100%;
          min-width: 0;
        }
  
        .playground-computer-creation-title {
          margin: 0;
          color: rgba(255, 255, 255, 0.96);
          font-size: 16px;
          font-weight: 500;
          line-height: 1.25;
        }
  
        .playground-computer-creation-subtitle {
          margin: 0;
          color: rgba(255, 255, 255, 0.7);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.45;
        }
  
        .playground-computer-creation-name-row {
          width: 100%;
          min-width: 0;
          padding-bottom: 12px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-computer-creation-name-input {
          display: block;
          width: 100%;
          min-width: 0;
          padding: 0;
          border: 0;
          outline: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.96);
          font-size: 20px;
          font-weight: 500;
          line-height: 1.2;
          text-align: left;
        }
  
        .playground-computer-creation-name-input::placeholder {
          color: rgba(255, 255, 255, 0.5);
          opacity: 1;
        }
  
        .playground-computer-creation-field {
          display: flex;
          flex-direction: column;
          gap: 10px;
          min-width: 0;
        }
  
        .playground-computer-creation-label {
          margin: 0;
        }
  
        .playground-computer-creation-settings {
          width: 100%;
        }
  
  	      .playground-computer-creation-actions {
  	        margin-top: 0;
  	      }
  
  	      .playground-server-creation-form .playground-environments-editor-surface {
  	        width: 100%;
  	        padding: 0;
  	        border: 0;
  	        background: transparent;
  	        box-shadow: none;
  	      }
  
  	      .playground-server-creation-form .playground-tasks-detail-facts {
  	        padding: 0;
  	        border: 0;
  	        border-radius: 0;
  	        background: transparent;
  	        box-shadow: none;
  	        -webkit-backdrop-filter: none;
  	        backdrop-filter: none;
  	      }
  
  	      .playground-server-creation-form .playground-tasks-detail-facts::before {
  	        content: none;
  	        display: none;
  	      }
  
  	      .playground-server-creation-form .playground-tasks-detail-fact {
  	        padding-left: 0;
  	        padding-right: 0;
  	      }
  
  	      .playground-tasks-project-modal.playground-agents-permission-modal {
  	        width: min(800px, calc(100vw - 48px));
  	        max-height: min(700px, calc(100vh - 48px));
  	        display: flex;
  	        flex-direction: column;
  	        overflow: visible;
  	        scrollbar-width: none;
  	      }
  
        .playground-agents-permission-modal .playground-agents-permission-modal-body {
          gap: 18px;
          max-height: min(488px, calc(100vh - 260px));
          overflow: auto;
          scrollbar-width: none;
        }
  
        .playground-agents-permission-modal::-webkit-scrollbar,
        .playground-agents-permission-modal .playground-agents-permission-modal-body::-webkit-scrollbar {
          display: none;
        }
  
        .playground-agents-nav-create-button {
          min-height: 30px;
          padding: 0 10px;
          background: linear-gradient(to top, #082673, #1D59BE);
        }
  
        .playground-agents-nav-create-button:hover,
        .playground-agents-nav-create-button:focus-visible,
        .playground-agents-nav-create-button.is-active {
          background: linear-gradient(to top, #082673, #1D59BE);
        }
  
        @media (max-width: 980px) {
          .playground-agents-detail-layout.has-assistant,
          .playground-agents-detail-layout.is-assistant-open {
            grid-template-columns: minmax(0, 1fr);
          }
  
          .playground-agents-assistant-sidebar {
            position: absolute;
            top: 0;
            right: 0;
            bottom: 0;
            z-index: 12;
            width: min(400px, 100%);
            box-shadow: -18px 0 40px rgba(0, 0, 0, 0.24);
          }
        }
  
        .playground-agents-page .playground-environments-home-metrics .playground-tasks-detail-facts {
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          backdrop-filter: none;
          -webkit-backdrop-filter: none;
        }
  
        .playground-agents-page .playground-environments-home-metrics .playground-tasks-detail-facts::before {
          content: none;
        }
  
        .playground-agents-page .playground-environments-home-card-grid {
          margin-top: 20px;
        }
  
        .playground-agents-page .playground-environments-home-card {
          text-align: left;
        }
  
        .playground-agents-page .playground-environments-home-card-title,
        .playground-agents-page .playground-environments-home-card-copy {
          width: 100%;
          text-align: left;
        }
  
        .playground-agents-page .playground-agents-model-hint,
        .playground-agents-page .playground-agents-team-hint {
          margin-top: 0;
        }
  
        .playground-agents-model-hint {
          margin-top: 10px;
          padding: 12px 14px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 14px;
          background: rgba(255, 255, 255, 0.03);
        }
  
        .playground-agents-model-hint-title {
          font-size: 12px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-agents-model-hint-copy {
          margin-top: 4px;
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.48);
        }
  
        .playground-agents-model-panel {
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          position: relative;
          overflow: visible;
        }
  
        .playground-agents-model-header {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 16px 24px;
          padding-bottom: 19px;
          margin-bottom: 0;
          border-bottom: 0;
        }
  
        .playground-agents-model-header .playground-environments-field {
          min-width: 0;
        }
  
        .playground-agents-model-header .playground-environments-field-span {
          grid-column: 1 / -1;
        }
  
        .playground-agents-model-select-row {
          display: grid;
          grid-template-columns: minmax(0, 1fr) auto;
          align-items: center;
          gap: 16px;
          min-width: 0;
        }
  
        .playground-agents-model-select-label {
          font-size: 12px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.44);
          line-height: 1.3;
          white-space: nowrap;
        }
  
        .playground-agents-model-select-popup {
          min-width: 0;
        }
  
        .playground-agents-model-select-popup .playground-environments-runtime-value-button {
          width: auto;
          min-width: 0;
          max-width: 100%;
          justify-content: flex-end;
        }
  
        .playground-agents-model-select-popup .playground-environments-runtime-value-label {
          text-align: right;
        }
  
        .playground-agents-model-select-popup.is-open,
        .playground-agents-model-select-popup.is-open .playground-tasks-toolbar-popup-menu {
          z-index: 260;
        }
  
        .playground-agents-model-picker-trigger {
          width: 100%;
          justify-content: space-between;
          gap: 10px;
        }
  
        .playground-agents-model-picker-trigger-copy {
          min-width: 0;
          display: flex;
          align-items: center;
          gap: 10px;
          margin-left: auto;
          justify-content: flex-end;
        }
  
        .playground-agents-model-picker-trigger-labels {
          min-width: 0;
          display: flex;
          align-items: center;
          justify-content: flex-end;
          text-align: right;
        }
  
        .playground-agents-model-picker-provider {
          font-size: 11px;
          line-height: 1.2;
          color: rgba(255, 255, 255, 0.44);
        }
  
        .playground-agents-model-provider-icon-shell {
          width: auto;
          height: auto;
          flex-shrink: 0;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          overflow: visible;
          border: 0;
          border-radius: 0;
          background: transparent;
          padding: 0;
        }
  
        .playground-agents-model-provider-icon-shell.is-card {
          width: auto;
          height: auto;
          grid-area: icon;
          align-self: center;
        }
  
        .playground-agents-model-provider-icon {
          width: 14px;
          height: 14px;
          object-fit: contain;
          display: block;
        }
  
        .playground-agents-model-provider-icon-shell.is-card .playground-agents-model-provider-icon {
          width: 20px;
          height: 20px;
        }
  
        .playground-agents-model-provider-icon.is-openai {
          filter: brightness(0) invert(1);
        }
  
        .playground-agents-model-picker-modal {
          max-height: min(700px, calc(100vh - 48px));
          display: flex;
          flex-direction: column;
          overflow: visible;
          scrollbar-width: none;
        }
  
        .playground-agents-model-picker-search-row {
          width: 100%;
          display: flex;
          align-items: center;
          gap: 10px;
          margin: 0 0 12px;
        }
  
        .playground-agents-model-picker-search-field {
          position: relative;
          flex: 1 1 auto;
          min-width: 0;
        }
  
        .playground-agents-model-picker-search-icon {
          position: absolute;
          top: 50%;
          left: 12px;
          width: 14px;
          height: 14px;
          color: rgba(255, 255, 255, 0.4);
          transform: translateY(-50%);
          pointer-events: none;
        }
  
        .playground-agents-model-picker-search-input {
          width: 100%;
          padding-left: 36px;
          border-radius: 999px;
        }
  
        .playground-agents-model-picker-filter-anchor {
          flex: 0 0 auto;
        }
  
        .playground-agents-model-picker-filter-anchor .playground-tasks-toolbar-popup-menu {
          min-width: 250px;
          width: 250px;
          padding: 12px;
          box-sizing: border-box;
        }
  
        .playground-agents-model-picker-filter-button {
          min-height: 36px;
          border-radius: 999px;
        }
  
        .playground-agents-model-picker-filter-anchor .playground-files-toolbar-menu-title {
          padding: 0 0 10px;
        }
  
        .playground-agents-model-picker-filter-anchor .playground-files-toolbar-menu-item {
          border-radius: 10px;
        }
  
        .playground-agents-model-picker-modal .playground-agents-model-picker-body {
          flex: 1 1 auto;
          min-height: 0;
          gap: 18px;
          max-height: min(488px, calc(100vh - 260px));
          overflow: auto;
          scrollbar-width: none;
        }
  
        .playground-agents-model-picker-modal::-webkit-scrollbar,
        .playground-agents-model-picker-modal .playground-agents-model-picker-body::-webkit-scrollbar {
          display: none;
        }
  
        .playground-agents-model-picker-grid {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 14px;
        }
  
        .playground-agents-model-picker-groups {
          display: flex;
          flex-direction: column;
          gap: 18px;
        }
  
        .playground-agents-model-picker-group {
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-agents-model-picker-group-title {
          font-size: 12px;
          line-height: 1.3;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.62);
        }
  
        .playground-agents-model-picker-empty {
          min-height: 180px;
          display: flex;
          align-items: center;
          justify-content: center;
          padding: 24px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 10px;
          background: rgba(255, 255, 255, 0.03);
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.56);
          text-align: center;
        }
  
        .playground-agents-model-picker-card {
          width: 100%;
          min-width: 0;
          position: relative;
          display: grid;
          grid-template-columns: auto minmax(0, 1fr) auto;
          grid-template-areas:
            "icon title badge"
            "description description description"
            "footer footer footer";
          align-items: start;
          gap: 10px 12px;
          padding: 16px 18px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 10px;
          background: transparent;
          color: rgba(255, 255, 255, 0.96);
          text-align: left;
          cursor: pointer;
          transition: background-color 160ms ease, border-color 160ms ease, transform 160ms ease, box-shadow 160ms ease;
        }
  
        .playground-agents-model-picker-card:hover:not(:disabled) {
          border-color: rgba(255, 255, 255, 0.14);
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-agents-model-picker-card.is-selected:hover:not(:disabled) {
          border-color: rgba(77, 163, 255, 0.66);
          background: rgba(77, 163, 255, 0.08);
        }
  
        .playground-agents-model-picker-card.is-selected {
          border-color: rgba(77, 163, 255, 0.66);
          background: rgba(77, 163, 255, 0.08);
        }
  
        .playground-agents-model-picker-card:disabled {
          opacity: 0.45;
          cursor: not-allowed;
        }
  
        .playground-agents-model-picker-card-title-row {
          grid-area: title;
          min-width: 0;
          display: flex;
          align-items: center;
          gap: 8px;
          align-self: center;
        }
  
        .playground-agents-model-picker-card-title {
          min-width: 0;
          font-size: 12px;
          line-height: 1.2;
          font-weight: 500;
          letter-spacing: -0.01em;
          color: rgba(255, 255, 255, 0.98);
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-agents-model-picker-card-cost-badge {
          grid-area: badge;
          justify-self: end;
          align-self: center;
          flex-shrink: 0;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          font-size: 10px;
          font-weight: 500;
          color: #ffffff;
        }
  
        .playground-agents-model-picker-card-cost-badge.is-low {
          color: #ffffff;
        }
  
        .playground-agents-model-picker-card-cost-badge.is-baseline {
          color: #ffffff;
        }
  
        .playground-agents-model-picker-card-cost-badge.is-high {
          color: #ffffff;
        }
  
        .playground-agents-model-picker-card-description {
          grid-area: description;
          width: 100%;
          font-size: 10px;
          line-height: 1.4;
          color: rgba(255, 255, 255, 0.7);
          display: -webkit-box;
          -webkit-line-clamp: 2;
          -webkit-box-orient: vertical;
          overflow: hidden;
        }
  
        .playground-agents-model-picker-card-footer {
          grid-area: footer;
          min-width: 0;
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 10px;
        }
  
        .playground-agents-model-picker-card-context {
          flex-shrink: 0;
          padding: 6px 12px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.08);
          font-size: 10px;
          font-weight: 500;
          line-height: 1.3;
          color: rgba(255, 255, 255, 0.94);
        }
  
        .playground-agents-model-picker-card-check {
          display: none;
        }
  
        .playground-agents-model-picker-card-brains {
          display: inline-flex;
          align-items: center;
          gap: 6px;
          flex-shrink: 0;
        }
  
        .playground-agents-model-picker-card-custom-label {
          flex-shrink: 0;
          font-size: 10px;
          line-height: 1.3;
          color: rgba(255, 255, 255, 0.52);
        }
  
        .playground-agents-model-picker-card-brain {
          width: 12px;
          height: 12px;
          color: rgba(255, 255, 255, 0.34);
        }
  
        .playground-agents-model-picker-card-brain.is-active {
          color: rgba(255, 255, 255, 0.96);
        }
  
        @media (max-width: 860px) {
          .playground-agents-model-picker-grid {
            grid-template-columns: minmax(0, 1fr);
          }
        }
  
        .playground-agents-model-metrics {
          margin-top: 0;
          display: grid;
          grid-template-columns: repeat(3, minmax(0, 1fr));
          gap: 10px;
          align-items: stretch;
        }
  
        .playground-agents-model-metrics.playground-agents-model-metrics-surface {
          position: relative;
          isolation: isolate;
          padding: 20px;
          border: 0;
          border-radius: 20px;
          background: rgba(255, 255, 255, 0.05);
          backdrop-filter: blur(50px);
          -webkit-backdrop-filter: blur(50px);
        }
  
        .playground-agents-model-metrics.playground-agents-model-metrics-surface::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          z-index: 0;
          border-radius: 20px;
          padding: 1px;
          background: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-agents-model-metrics.playground-agents-model-metrics-surface > * {
          position: relative;
          z-index: 1;
        }
  
        .playground-agents-model-metric {
          min-height: 64px;
          display: flex;
          flex-direction: column;
          justify-content: center;
          align-items: flex-start;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
        }
  
        .playground-agents-model-metric-label {
          font-size: 12px;
          color: rgba(255, 255, 255, 0.42);
        }
  
        .playground-agents-model-metric-value {
          margin-top: 6px;
          font-size: 12px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.94);
        }
  
        .playground-agents-model-metric.is-stack {
          justify-content: center;
        }
  
        .playground-agents-model-metric.is-stack .playground-agents-model-metric-value {
          margin-top: 0;
          font-size: 20px;
          line-height: 1.05;
        }
  
        .playground-agents-model-metric.is-stack .playground-agents-model-metric-label {
          margin-top: 6px;
        }
  
        .playground-agents-model-inline-copy {
          display: inline-flex;
          align-items: baseline;
          gap: 6px;
          margin-top: 0;
          text-align: left;
          white-space: nowrap;
        }
  
        .playground-agents-model-inline-copy .playground-agents-model-metric-label,
        .playground-agents-model-inline-copy .playground-agents-model-metric-value {
          margin-top: 0;
        }
  
        .playground-agents-intelligence-metric {
          width: 120px;
        }
  
        .playground-agents-intelligence-bulbs {
          display: flex;
          align-items: center;
          gap: 2px;
        }
  
        .playground-agents-intelligence-bulb {
          width: 24px;
          height: 24px;
          color: rgba(255, 255, 255, 0.35);
        }
  
        .playground-agents-intelligence-bulb.is-active {
          color: #fff;
        }
  
        .playground-agents-intelligence-copy {
          margin-top: 8px;
          font-size: 12px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.98);
          line-height: 1.25;
        }
  
        .playground-agents-intelligence-copy span {
          color: rgba(255, 255, 255, 0.5);
        }
  
        .playground-agents-instructions {
          min-height: 280px;
          font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
        }
  
        .playground-agents-skills-grid {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 12px;
          padding-top: 16px;
        }
  
        .playground-agents-skill-card {
          display: flex;
          flex-direction: column;
          gap: 12px;
          padding: 14px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 14px;
          background: rgba(255, 255, 255, 0.03);
        }
  
        .playground-agents-skill-card.is-selected {
          border-color: rgba(255, 255, 255, 0.22);
          background: rgba(255, 255, 255, 0.08);
        }
  
        .playground-agents-skill-card-header {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 14px;
        }
  
        .playground-agents-skill-card-copy {
          min-width: 0;
        }
  
        .playground-agents-skill-card-title {
          font-size: 12px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-agents-skill-card-description {
          margin-top: 4px;
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.46);
        }
  
        .playground-agents-skill-card.is-disabled {
          opacity: 0.72;
        }
  
        .playground-agents-readonly-note {
          margin-top: 12px;
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.46);
        }
  
        .playground-agents-empty-actions {
          display: flex;
          flex-wrap: wrap;
          gap: 10px;
          justify-content: center;
        }
  
        .playground-agents-team-hint {
          display: flex;
          align-items: flex-start;
          gap: 12px;
          padding: 14px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 14px;
          background: rgba(255, 255, 255, 0.03);
          color: rgba(255, 255, 255, 0.92);
        }
  
        .playground-agents-team-hint-copy {
          min-width: 0;
        }
  
        .playground-agents-team-hint-title {
          font-size: 12px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-agents-team-hint-description {
          margin-top: 4px;
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.5);
        }
  
        .playground-agents-team-readonly {
          min-height: 42px;
          display: flex;
          align-items: center;
          padding: 0 14px;
          border-radius: 14px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.03);
          color: rgba(255, 255, 255, 0.78);
          font-size: 12px;
          font-weight: 500;
        }
  
        .playground-agents-team-members-header {
          display: flex;
          align-items: baseline;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-agents-team-members-title {
          font-size: 12px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-agents-team-members-copy {
          font-size: 11px;
          color: rgba(255, 255, 255, 0.46);
        }
  
        .playground-agents-team-grid {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 12px;
        }
  
        .playground-agents-team-card {
          width: 100%;
          display: flex;
          flex-direction: column;
          gap: 8px;
          padding: 14px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 14px;
          background: rgba(255, 255, 255, 0.03);
          color: rgba(255, 255, 255, 0.92);
          cursor: pointer;
          text-align: left;
          transition: border-color 160ms ease, background-color 160ms ease, transform 160ms ease;
        }
  
        .playground-agents-team-card:hover {
          border-color: rgba(255, 255, 255, 0.14);
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-agents-team-card.is-selected {
          border-color: rgba(138, 214, 255, 0.42);
          background: rgba(71, 166, 255, 0.12);
        }
  
        .playground-agents-team-card-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-agents-team-card-title {
          font-size: 12px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-agents-team-card-copy {
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.48);
        }
  
        .playground-agents-team-selected-list {
          display: flex;
          flex-wrap: wrap;
          gap: 8px;
        }
  
        .playground-agents-team-selected-pill {
          display: inline-flex;
          align-items: center;
          min-height: 24px;
          padding: 0 10px;
          border-radius: 999px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.78);
          font-size: 11px;
          font-weight: 500;
        }
  
        .playground-agents-empty-badge {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          min-height: 22px;
          padding: 0 8px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.06);
          color: rgba(255, 255, 255, 0.48);
          font-size: 10px;
          font-weight: 600;
          letter-spacing: 0.02em;
        }
  
  ${PROJECTS_STYLE_FRAGMENTS.core}
  
        .playground-database-summary-card {
          padding: 20px;
        }
  
        .playground-database-summary-header {
          display: flex;
          flex-direction: column;
          gap: 4px;
        }
  
        .playground-database-summary-title {
          color: rgba(255, 255, 255, 0.96);
          font-size: 12px;
          font-weight: 500;
          line-height: 1.35;
        }
  
        .playground-database-summary-copy {
          color: rgba(255, 255, 255, 0.56);
          font-size: 12px;
          line-height: 1.45;
        }
  
        .playground-database-overview {
          display: flex;
          flex-direction: column;
          gap: 14px;
          padding: 4px 0 10px;
        }
  
        .playground-database-overview .playground-servers-analytics-kpi-grid {
          grid-template-columns: repeat(auto-fit, minmax(92px, 1fr));
          gap: 8px;
        }
  
        .playground-database-overview-metrics {
          display: grid;
          grid-template-columns: repeat(3, minmax(0, 1fr));
          gap: 14px;
        }
  
        .playground-database-overview-metric {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 5px;
        }
  
        .playground-database-overview-value {
          font-size: 17px;
          line-height: 1;
          font-weight: 500;
          letter-spacing: -0.02em;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-database-overview-label {
          display: inline-flex;
          align-items: center;
          gap: 5px;
          font-size: 11px;
          line-height: 1.2;
          font-weight: 400;
          color: #fff;
          white-space: nowrap;
        }
  
        .playground-database-overview-label svg {
          width: 12px;
          height: 12px;
          flex: 0 0 auto;
        }
  
        .playground-database-overview-label.is-collections svg {
          color: rgba(120, 111, 255, 0.92);
        }
  
        .playground-database-overview-label.is-documents svg {
          color: rgba(170, 138, 255, 0.94);
        }
  
        .playground-database-overview-label.is-density svg {
          color: rgba(237, 170, 255, 0.94);
        }
  
        .playground-database-overview-chart-block {
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-database-overview-chart-grid {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 12px;
        }
  
        .playground-database-overview-timeseries-card {
          display: flex;
          flex-direction: column;
          gap: 0;
          min-height: 178px;
        }
  
        .playground-database-overview-timeseries-legend {
          display: inline-flex;
          align-items: center;
          flex-wrap: wrap;
          gap: 14px;
        }
  
        .playground-database-overview-timeseries-series {
          display: inline-flex;
          align-items: center;
          gap: 8px;
        }
  
        .playground-database-overview-timeseries-label {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          color: rgba(255, 255, 255, 0.72);
          font-size: 12px;
          line-height: 1.35;
        }
  
        .playground-database-overview-timeseries-check {
          width: 18px;
          height: 18px;
          border-radius: 4px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          color: rgba(0, 0, 0, 0.92);
          flex-shrink: 0;
        }
  
        .playground-database-overview-kpi-label {
          margin-top: 6px;
          display: inline-flex;
          align-items: center;
          gap: 6px;
          color: rgba(255, 255, 255, 0.72);
          font-size: 10px;
          line-height: 1.2;
          white-space: nowrap;
          background: transparent;
          border: 0;
          padding: 0;
          cursor: pointer;
          text-align: left;
        }
  
        .playground-database-overview-kpi-label.is-static {
          cursor: default;
          pointer-events: none;
        }
  
        .playground-database-overview-kpi-check {
          width: 14px;
          height: 14px;
          border-radius: 3px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          color: rgba(0, 0, 0, 0.92);
          flex-shrink: 0;
        }
  
        .playground-database-overview-kpi-label.is-inactive {
          color: rgba(255, 255, 255, 0.38);
        }
  
        .playground-database-overview-kpi-label.is-inactive .playground-database-overview-kpi-check {
          background: transparent;
          border: 1px solid rgba(255, 255, 255, 0.18);
          color: transparent;
        }
  
        .playground-database-overview-kpi-label:focus-visible {
          outline: 2px solid rgba(255, 255, 255, 0.18);
          outline-offset: 2px;
          border-radius: 5px;
        }
  
        .playground-database-overview-kpi-check svg {
          width: 9px;
          height: 9px;
        }
  
        .playground-database-overview-kpi-label.is-reads .playground-database-overview-kpi-check {
          background: var(--playground-chart-blue);
        }
  
        .playground-database-overview-kpi-label.is-requests .playground-database-overview-kpi-check {
          background: var(--playground-chart-blue);
        }
  
        .playground-database-overview-kpi-label.is-success .playground-database-overview-kpi-check {
          background: var(--playground-chart-green);
        }
  
        .playground-database-overview-kpi-label.is-latency .playground-database-overview-kpi-check {
          background: var(--playground-chart-yellow);
        }
  
        .playground-database-overview-kpi-label.is-errors .playground-database-overview-kpi-check {
          background: var(--playground-chart-pink);
        }
  
        .playground-database-overview-kpi-label.is-failures .playground-database-overview-kpi-check {
          background: var(--playground-chart-yellow);
        }
  
        .playground-database-overview-kpi-label.is-writes .playground-database-overview-kpi-check {
          background: var(--playground-chart-yellow);
        }
  
        .playground-database-overview-kpi-label.is-deletes .playground-database-overview-kpi-check {
          background: var(--playground-chart-pink);
        }
  
        .playground-database-overview-kpi-label.is-collections .playground-database-overview-kpi-check {
          background: var(--playground-chart-blue);
        }
  
        .playground-database-overview-kpi-label.is-documents .playground-database-overview-kpi-check {
          background: var(--playground-chart-green);
        }
  
        .playground-database-overview-timeseries-chart {
          min-width: 0;
          flex: 1 1 auto;
        }
  
        .playground-database-overview-timeseries-frame {
          width: 100%;
          height: 100%;
        }
  
        .playground-database-overview-timeseries-svg {
          width: 100%;
          height: 100%;
          display: block;
        }
  
        .playground-database-overview-timeseries-grid-line {
          stroke: rgba(255, 255, 255, 0.10);
          stroke-width: 1;
        }
  
        .playground-database-overview-timeseries-axis-label {
          fill: rgba(255, 255, 255, 0.46);
          font-size: 10px;
        }
  
        .playground-database-overview-timeseries-line {
          fill: none;
          stroke-width: 2;
          stroke-linecap: round;
          stroke-linejoin: round;
        }
  
        .playground-database-overview-timeseries-dot {
          stroke: rgba(19, 19, 19, 0.92);
          stroke-width: 1.5;
        }
  
        .playground-database-overview-timeseries-series.is-reads .playground-database-overview-timeseries-check {
          background: var(--playground-chart-blue);
        }
  
        .playground-database-overview-timeseries-line.is-reads {
          stroke: var(--playground-chart-blue);
        }
  
        .playground-database-overview-timeseries-line.is-requests {
          stroke: var(--playground-chart-blue);
        }
  
        .playground-database-overview-timeseries-dot.is-reads {
          fill: var(--playground-chart-blue);
          stroke: var(--playground-chart-blue);
        }
  
        .playground-database-overview-timeseries-dot.is-requests {
          fill: var(--playground-chart-blue);
          stroke: var(--playground-chart-blue);
        }
  
        .playground-database-overview-timeseries-series.is-writes .playground-database-overview-timeseries-check {
          background: var(--playground-chart-yellow);
        }
  
        .playground-database-overview-timeseries-line.is-writes {
          stroke: var(--playground-chart-yellow);
        }
  
        .playground-database-overview-timeseries-dot.is-writes {
          fill: var(--playground-chart-yellow);
          stroke: var(--playground-chart-yellow);
        }
  
        .playground-database-overview-timeseries-line.is-failures {
          stroke: var(--playground-chart-yellow);
        }
  
        .playground-database-overview-timeseries-dot.is-failures {
          fill: var(--playground-chart-yellow);
          stroke: var(--playground-chart-yellow);
        }
  
        .playground-database-overview-timeseries-line.is-success {
          stroke: var(--playground-chart-green);
        }
  
        .playground-database-overview-timeseries-dot.is-success {
          fill: var(--playground-chart-green);
          stroke: var(--playground-chart-green);
        }
  
        .playground-database-overview-timeseries-line.is-errors {
          stroke: var(--playground-chart-pink);
        }
  
        .playground-database-overview-timeseries-dot.is-errors {
          fill: var(--playground-chart-pink);
          stroke: var(--playground-chart-pink);
        }
  
        .playground-database-overview-timeseries-line.is-compute,
        .playground-database-overview-timeseries-line.is-resource-4 {
          stroke: var(--playground-chart-pink);
        }
  
        .playground-database-overview-timeseries-dot.is-compute,
        .playground-database-overview-timeseries-dot.is-resource-4 {
          fill: var(--playground-chart-pink);
          stroke: var(--playground-chart-pink);
        }
  
        .playground-database-overview-timeseries-line.is-resources,
        .playground-database-overview-timeseries-line.is-resource-3 {
          stroke: var(--playground-chart-green);
        }
  
        .playground-database-overview-timeseries-dot.is-resources,
        .playground-database-overview-timeseries-dot.is-resource-3 {
          fill: var(--playground-chart-green);
          stroke: var(--playground-chart-green);
        }
  
        .playground-database-overview-timeseries-line.is-resource-1 {
          stroke: var(--playground-chart-blue);
        }
  
        .playground-database-overview-timeseries-dot.is-resource-1 {
          fill: var(--playground-chart-blue);
          stroke: var(--playground-chart-blue);
        }
  
        .playground-database-overview-timeseries-line.is-resource-2 {
          stroke: var(--playground-chart-yellow);
        }
  
        .playground-database-overview-timeseries-dot.is-resource-2 {
          fill: var(--playground-chart-yellow);
          stroke: var(--playground-chart-yellow);
        }
  
        .playground-database-overview-timeseries-series.is-deletes .playground-database-overview-timeseries-check {
          background: var(--playground-chart-pink);
        }
  
        .playground-database-overview-timeseries-line.is-deletes {
          stroke: var(--playground-chart-pink);
        }
  
        .playground-database-overview-timeseries-dot.is-deletes {
          fill: var(--playground-chart-pink);
          stroke: var(--playground-chart-pink);
        }
  
        .playground-database-overview-timeseries-series.is-collections .playground-database-overview-timeseries-check {
          background: var(--playground-chart-blue);
        }
  
        .playground-database-overview-timeseries-line.is-collections {
          stroke: var(--playground-chart-blue);
        }
  
        .playground-database-overview-timeseries-dot.is-collections {
          fill: var(--playground-chart-blue);
          stroke: var(--playground-chart-blue);
        }
  
        .playground-database-overview-timeseries-series.is-documents .playground-database-overview-timeseries-check {
          background: var(--playground-chart-green);
        }
  
        .playground-database-overview-timeseries-line.is-documents {
          stroke: var(--playground-chart-green);
        }
  
        .playground-database-overview-timeseries-dot.is-documents {
          fill: var(--playground-chart-green);
          stroke: var(--playground-chart-green);
        }
  
        .playground-mission-control-run-button {
          width: 100%;
          min-height: 36px;
          margin-top: 8px;
          margin-bottom: 5px;
          padding: 0 14px;
          border: 0;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.96);
          color: #000;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 8px;
          font-size: 12px;
          line-height: 1;
          font-weight: 500;
          cursor: pointer;
          transition: 0.2s all;
        }
  
        .playground-mission-control-run-button:hover:not(:disabled) {
          background: rgba(255, 255, 255, 0.7);
        }
  
        .playground-mission-control-run-button:disabled {
          opacity: 0.56;
          cursor: default;
        }
  
        .playground-mission-control-run-button-icon {
          width: 13px;
          height: 13px;
          flex: 0 0 auto;
        }
  
        .playground-tasks-detail-fact {
          display: grid;
          grid-template-columns: minmax(88px, 110px) minmax(0, 1fr);
          min-height: 30px;
          align-items: center;
          gap: 12px;
        }
  
        .playground-tasks-detail-fact-label {
          font-size: 12px;
          font-weight: 400;
          color: #ffffff;
          line-height: 1.35;
        }
  
        .playground-tasks-detail-fact-control {
          min-width: 0;
          display: flex;
          align-items: center;
          justify-content: flex-end;
        }
  
        .playground-tasks-type-control {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          gap: 8px;
        }
  
        .playground-tasks-type-parent-button {
          max-width: 180px;
          min-height: 28px;
          padding: 0;
          border: 0;
          background: transparent;
          color: #66a6ff;
          font-size: 12px;
          line-height: 1.35;
          text-align: right;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
          cursor: pointer;
        }
  
        .playground-tasks-type-parent-button:hover {
          color: #8cbbff;
        }
  
        .playground-tasks-type-parent-button:disabled,
        .playground-tasks-detail-fact-button:disabled,
        .playground-tasks-attachments-environment-button:disabled,
        .playground-tasks-skills-manage-button:disabled {
          opacity: 0.46;
          cursor: default;
        }
  
        .playground-tasks-priority-value {
          display: inline-flex;
          align-items: center;
          gap: 6px;
          min-width: 0;
          color: rgba(255, 255, 255, 0.88);
        }
  
        .playground-tasks-priority-value-icon {
          width: 14px;
          height: 14px;
          flex: 0 0 auto;
        }
  
        .playground-tasks-priority-bars-icon {
          width: 14px !important;
          height: 10px !important;
          display: inline-flex;
          align-items: flex-end;
          justify-content: center;
          gap: 2px;
        }
  
        .playground-tasks-priority-bars-bar {
          width: 2px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.3);
        }
  
        .playground-tasks-priority-bars-bar:nth-child(1) {
          height: 4px;
        }
  
        .playground-tasks-priority-bars-bar:nth-child(2) {
          height: 7px;
        }
  
        .playground-tasks-priority-bars-bar:nth-child(3) {
          height: 10px;
        }
  
        .playground-tasks-priority-bars-bar.is-active {
          background: rgba(255, 255, 255, 0.92);
        }
  
        .playground-tasks-priority-value-text {
          white-space: nowrap;
        }
  
        .playground-tasks-priority-value.is-low .playground-tasks-priority-value-icon,
        .playground-tasks-priority-value.is-medium .playground-tasks-priority-value-icon,
        .playground-tasks-priority-value.is-high .playground-tasks-priority-value-icon,
        .playground-tasks-priority-value.is-critical .playground-tasks-priority-value-icon {
          color: rgba(255, 255, 255, 0.92);
        }
  
        .playground-tasks-schedule-anchor {
          position: relative;
          display: flex;
          align-items: center;
          justify-content: flex-end;
          width: 100%;
          min-height: 32px;
        }
  
        .playground-tasks-detail-fact-select {
          width: auto;
          min-width: 0;
          max-width: 100%;
          margin-left: auto;
          height: 32px;
          padding: 0 0 0 12px;
          border: 0;
          border-radius: 0;
          background: transparent;
          font-size: 12px;
          text-align: right;
          text-align-last: right;
        }
  
        .playground-tasks-detail-priority-value {
          margin-left: auto;
          justify-content: flex-end;
        }
  
        .playground-tasks-detail-priority-select {
          color: inherit;
          padding-left: 0;
        }
  
        .playground-tasks-detail-fact-button {
          width: auto;
          max-width: 100%;
          min-height: 32px;
          margin-left: auto;
          padding: 0;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.86);
          font-size: 12px;
          line-height: 1.35;
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          text-align: right;
          cursor: pointer;
        }
  
        .playground-tasks-detail-fact-button:hover {
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-tasks-detail-fact-button.is-empty {
          color: rgba(255, 255, 255, 0.62);
        }
  
        .playground-tasks-detail-select-shell.playground-environments-runtime-popup-shell {
          width: auto;
          max-width: 100%;
        }
  
        .playground-tasks-detail-select-shell.playground-environments-runtime-popup-shell.is-open {
          z-index: 260;
        }
  
        .playground-skills-deep-research-model-shell.playground-environments-runtime-popup-shell.is-open {
          z-index: 420;
        }
  
        .playground-skills-deep-research-model-shell.playground-environments-runtime-popup-shell.is-open .playground-tasks-toolbar-popup-menu {
          z-index: 421;
        }
  
        .playground-tasks-detail-select-shell .playground-tasks-toolbar-popup-menu {
          top: calc(100% + 8px);
          right: 0;
          left: auto;
          bottom: auto;
          width: 280px;
          min-width: 220px;
          max-width: min(320px, calc(100vw - 40px));
          transform-origin: top right;
        }
  
        .playground-tasks-detail-select-trigger {
          gap: 6px;
        }
  
        .playground-tasks-detail-select-trigger.is-active {
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-tasks-detail-select-trigger-label {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-skills-defaults-card {
          --playground-project-overview-chart-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          margin-top: 12px;
          margin-bottom: 18px;
          position: relative;
          overflow: hidden;
          border: 0;
          background: transparent;
          border-radius: 15px;
          -webkit-backdrop-filter: blur(50px);
          backdrop-filter: blur(50px);
        }
  
        .playground-skills-defaults-card::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          z-index: 2;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-project-overview-chart-border);
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-skills-defaults-card > * {
          position: relative;
          z-index: 3;
        }
  
        .playground-skills-defaults-card .playground-tasks-detail-facts-body {
          display: flex;
          flex-direction: column;
          gap: 0;
        }
  
        .playground-skills-defaults-heading {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 16px;
          padding: 16px 18px 14px;
          margin-bottom: 12px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-skills-defaults-title {
          margin: 0;
          color: rgba(255, 255, 255, 0.94);
          font-size: 14px;
          font-weight: 500;
          line-height: 1.35;
        }
  
        .playground-skills-defaults-description {
          margin: 6px 0 0;
          max-width: 620px;
          color: rgba(255, 255, 255, 0.58);
          font-size: 12px;
          line-height: 1.45;
        }
  
        .playground-skills-defaults-row {
          padding-inline: 18px;
        }
  
        .playground-tasks-detail-person-value {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          gap: 8px;
        }
  
        .playground-tasks-detail-person-avatar,
        .playground-tasks-detail-person-menu-avatar {
          width: 22px;
          height: 22px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.1);
          color: rgba(255, 255, 255, 0.88);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          overflow: hidden;
          flex: 0 0 auto;
        }
  
        .playground-tasks-detail-person-menu-avatar {
          width: 24px;
          height: 24px;
        }
  
        .playground-tasks-detail-person-avatar-image,
        .playground-tasks-detail-person-menu-avatar-image {
          width: 100%;
          height: 100%;
          object-fit: cover;
          display: block;
        }
  
        .playground-tasks-detail-person-avatar-fallback,
        .playground-tasks-detail-person-menu-avatar-fallback {
          font-size: 10px;
          font-weight: 500;
          line-height: 1;
          letter-spacing: 0.02em;
          text-transform: uppercase;
          color: rgba(255, 255, 255, 0.7);
        }
  
        .playground-tasks-detail-type-value {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          gap: 6px;
          color: rgba(255, 255, 255, 0.88);
        }
  
        .playground-tasks-detail-type-icon {
          width: 14px;
          height: 14px;
          flex: 0 0 auto;
          color: rgba(255, 255, 255, 0.92);
        }
  
        .playground-tasks-detail-type-prefix {
          min-width: 0;
          white-space: nowrap;
        }
  
        .playground-tasks-detail-type-ticket {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
          color: #66a6ff;
        }
  
        .playground-tasks-detail-select-trigger-chevron {
          flex: 0 0 auto;
          width: 14px;
          height: 14px;
          color: rgba(255, 255, 255, 0.46);
        }
  
        .playground-tasks-detail-select-trigger.is-active .playground-tasks-detail-select-trigger-chevron {
          color: rgba(255, 255, 255, 0.72);
        }
  
        .playground-tasks-detail-assignee-shell .tb-popup-menu-inline-agent {
          top: calc(100% + 8px);
          right: 0;
          left: auto;
          bottom: auto;
          width: 280px;
          min-width: 220px;
          max-width: min(320px, calc(100vw - 40px));
          max-height: min(320px, calc(100vh - 180px));
          display: flex;
          flex-direction: column;
          overflow: hidden;
          transform-origin: top right;
        }
  
        .playground-tasks-detail-assignee-shell .tb-popup-menu-title {
          padding: 12px 16px 0;
        }
  
        .playground-tasks-detail-assignee-shell .tb-popup-panel-section-attach-header {
          padding: 0 16px 0;
        }
  
        .playground-tasks-detail-assignee-shell .tb-popup-menu-inline-body {
          flex: 1;
          min-height: 0;
          overflow-y: auto;
          scrollbar-width: none;
          -ms-overflow-style: none;
        }
  
        .playground-tasks-detail-assignee-shell .tb-popup-menu-inline-body::-webkit-scrollbar {
          display: none;
          width: 0;
          height: 0;
        }
  
        .playground-tasks-detail-assignee-shell .tb-popup-menu-inline-body-agent {
          margin-top: 12px;
          padding-top: 12px;
          border-top: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-tasks-detail-assignee-shell .tb-popup-menu-inline-empty {
          padding: 0 16px 12px;
        }
  
        .playground-tasks-detail-assignee-shell .tb-popup-row-select .tb-popup-check-slot {
          order: 2;
          margin-left: auto;
        }
  
        .playground-tasks-detail-assignee-shell .tb-popup-row-agent.selected .tb-popup-icon {
          color: white;
        }
  
        .playground-tasks-detail-assignee-shell .tb-popup-row-agent .playground-tasks-toolbar-popup-item-copy {
          flex: 1 1 auto;
          min-width: 0;
        }
  
        .playground-tasks-toolbar-popup-shell .playground-tasks-toolbar-popup-menu-environment {
          scrollbar-width: none;
          -ms-overflow-style: none;
        }
  
        .playground-tasks-toolbar-popup-shell .playground-tasks-toolbar-popup-menu-environment::-webkit-scrollbar {
          display: none;
          width: 0;
          height: 0;
        }
  
        .playground-tasks-detail-select-popup-label-slot {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          gap: 8px;
        }
  
        .playground-tasks-schedule-panel {
          position: absolute;
          top: calc(100% + 10px);
          right: 0;
          z-index: 20;
          width: min(320px, calc(100vw - 72px));
          overflow: hidden;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 15px;
          background: #323232;
          box-shadow: 0 12px 30px rgba(0, 0, 0, 0.35);
          backdrop-filter: blur(10px);
        }
  
        .playground-tasks-schedule-panel.is-enter {
          animation: playground-tasks-schedule-in 180ms cubic-bezier(0.16, 1, 0.3, 1) both;
          transform-origin: top right;
        }
  
        .playground-tasks-schedule-panel.is-exit {
          animation: playground-tasks-schedule-out 180ms cubic-bezier(0.4, 0, 1, 1) both;
          transform-origin: top right;
          pointer-events: none;
        }
  
        .playground-tasks-schedule-topbar {
          display: grid;
          grid-template-columns: 32px 1fr 32px;
          align-items: center;
          gap: 12px;
          padding: 12px 16px 0;
        }
  
        .playground-tasks-schedule-topbar-title {
          text-align: center;
          color: white;
          font-size: 14px;
          line-height: 1.2;
        }
  
        .playground-tasks-schedule-topbar-button {
          width: 16px;
          height: 16px;
          min-width: 16px;
          min-height: 16px;
          padding: 0;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border: 0;
          border-radius: 999px;
          background: transparent;
          color: white;
          line-height: 0;
          cursor: pointer;
          transition: opacity 160ms ease;
        }
  
        .playground-tasks-schedule-topbar-button:hover:enabled {
          opacity: 0.9;
        }
  
        .playground-tasks-schedule-topbar-button.is-close {
          justify-self: start;
        }
  
        .playground-tasks-schedule-topbar-button.is-confirm {
          justify-self: end;
        }
  
        .playground-tasks-schedule-topbar-icon {
          width: 16px;
          height: 16px;
          flex-shrink: 0;
        }
  
        .playground-tasks-schedule-body {
          margin-top: 12px;
          padding: 12px 16px;
          border-top: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-tasks-schedule-panel .tb-popup-panel-section-attach-header {
          padding: 0;
        }
  
        .playground-tasks-schedule-panel .tb-popup-panel-section-divider {
          padding: 16px 0 0;
        }
  
        .playground-tasks-schedule-panel .tb-popup-field-row {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-tasks-schedule-panel .tb-popup-field-row-followup {
          margin-top: 12px;
        }
  
        .playground-tasks-schedule-panel .tb-popup-field-label {
          color: rgba(255, 255, 255, 0.72);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.2;
        }
  
        .playground-tasks-schedule-panel .tb-popup-link-button {
          padding: 0;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.72);
          font-size: 12px;
          cursor: pointer;
        }
  
        .playground-tasks-schedule-panel .tb-popup-link-button:hover {
          color: white;
        }
  
        .playground-tasks-schedule-panel .tb-popup-link-button-inline {
          display: inline-flex;
          align-items: center;
          gap: 6px;
        }
  
        .playground-tasks-schedule-panel .tb-popup-link-chevron {
          width: 14px;
          height: 14px;
          flex: 0 0 auto;
        }
  
        .playground-tasks-schedule-panel .tb-popup-select-wrap-schedule {
          margin-top: 10px;
          border-radius: 20px;
          background: transparent;
        }
  
        .playground-tasks-schedule-panel .tb-popup-select-wrap-schedule::before {
          display: none;
        }
  
        .playground-tasks-schedule-panel .tb-popup-select-schedule {
          position: relative;
          z-index: 1;
          width: 100%;
          height: 28px;
          padding: 0 12px;
          border: 0;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.1);
          color: white;
          color-scheme: dark;
          font-family: inherit;
          font-size: 12px;
          font-weight: 500;
        }
  
        .playground-tasks-schedule-panel .tb-popup-select-schedule::-webkit-calendar-picker-indicator {
          display: none;
          -webkit-appearance: none;
        }
  
        .playground-tasks-schedule-panel .tb-popup-preset-list {
          display: flex;
          flex-direction: column;
          gap: 6px;
        }
  
        .playground-tasks-schedule-panel .tb-popup-preset-row {
          width: 100%;
          min-height: 36px;
          display: flex;
          align-items: center;
          gap: 8px;
          padding: 0 10px;
          border: 0;
          border-radius: 10px;
          background: transparent;
          color: white;
          cursor: pointer;
          text-align: left;
        }
  
        .playground-tasks-schedule-panel .tb-popup-preset-row:hover,
        .playground-tasks-schedule-panel .tb-popup-preset-row.selected {
          background: rgba(255, 255, 255, 0.08);
        }
  
        .playground-tasks-schedule-row {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          margin-bottom: 10px;
        }
  
        .playground-tasks-schedule-label {
          color: rgba(255, 255, 255, 0.72);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.2;
        }
  
        .playground-tasks-schedule-remove {
          padding: 0;
          border: 0;
          background: transparent;
          color: #d1d5db;
          font-size: 12px;
          font-weight: 400;
          line-height: 1.2;
          display: inline-flex;
          align-items: center;
          gap: 6px;
          cursor: pointer;
        }
  
        .playground-tasks-schedule-remove:hover {
          color: white;
        }
  
        .playground-tasks-schedule-remove-icon {
          width: 14px;
          height: 14px;
          flex-shrink: 0;
        }
  
        .playground-tasks-schedule-input {
          width: 100%;
          height: 28px;
          padding: 0 12px;
          border: 0;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.1);
          color: white;
          color-scheme: dark;
          font-family: inherit;
          font-size: 12px;
          font-weight: 500;
          outline: none;
        }
  
        .playground-tasks-schedule-input::-webkit-calendar-picker-indicator {
          display: none;
          -webkit-appearance: none;
        }
  
        @keyframes playground-tasks-schedule-in {
          from {
            opacity: 0;
            transform: translateY(-10px);
          }
  
          to {
            opacity: 1;
            transform: translateY(0);
          }
        }
  
        @keyframes playground-tasks-schedule-out {
          from {
            opacity: 1;
            transform: translateY(0);
          }
  
          to {
            opacity: 0;
            transform: translateY(-10px);
          }
        }
  
        .playground-tasks-detail-description {
          margin-top: 22px;
          padding: 2px 0 0;
        }
  
        .playground-tasks-detail-panel.is-project-task-detail .playground-tasks-detail-description {
          padding: 2px 0 0;
          background: transparent;
        }
  
        .playground-tasks-detail-panel.is-project-task-detail .playground-tasks-detail-description-editor {
          overflow: visible;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
        }
  
        .playground-tasks-detail-panel.is-project-task-detail .playground-tasks-detail-description-editor > * {
          position: relative;
          z-index: 1;
        }
  
        .playground-tasks-detail-panel.is-project-task-detail .playground-tasks-detail-description-editor::before {
          content: none;
          display: none;
        }
  
        .playground-tasks-detail-description-editor {
          position: relative;
          min-height: 36px;
        }
  
        .playground-tasks-detail-section-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          margin-bottom: 0;
        }
  
        .playground-tasks-detail-section-header .playground-tasks-detail-section-title {
          margin-bottom: 0;
        }
  
        .playground-tasks-detail-section-title {
          margin: 0 0 10px;
          color: rgba(255, 255, 255, 0.92);
          font-size: 12px;
          font-weight: 500;
          line-height: 1.3;
        }
  
        .playground-tasks-detail-format-actions {
          display: inline-flex;
          align-items: center;
          gap: 4px;
          flex-wrap: wrap;
        }
  
        .playground-tasks-detail-format-button {
          width: 28px;
          height: 28px;
          padding: 0;
          border: 0;
          border-radius: 8px;
          background: transparent;
          color: rgba(255, 255, 255, 0.62);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          transition: background-color 150ms ease, color 150ms ease;
        }
  
        .playground-tasks-detail-format-button:hover:not(:disabled) {
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-tasks-detail-format-button:disabled {
          opacity: 0.38;
          cursor: not-allowed;
        }
  
        .playground-tasks-detail-description-input {
          display: block;
          width: 100%;
          min-width: 0;
          min-height: 36px;
          padding: 0 0 14px;
          border: 0;
          border-radius: 0;
          outline: none;
          background: transparent;
          color: transparent;
          caret-color: rgba(255, 255, 255, 0.96);
          font-family: inherit;
          font-size: 12px;
          line-height: 1.6;
          box-sizing: border-box;
          resize: none;
          overflow: hidden;
          position: relative;
          z-index: 2;
        }
  
        .playground-tasks-detail-description-input.is-editing {
          color: rgba(255, 255, 255, 0.7);
        }
  
        .playground-tasks-detail-description-input.is-preview {
          color: transparent;
        }
  
        .playground-tasks-detail-description-input::placeholder {
          color: transparent;
        }
  
        .playground-tasks-detail-description-input.is-editing::placeholder {
          color: rgba(255, 255, 255, 0.38);
        }
  
        .playground-tasks-detail-description-input[readonly] {
          cursor: default;
        }
  
        .playground-tasks-detail-description-preview-scope.tb-runner-chat {
          position: absolute;
          inset: 0;
          display: block;
          grid-template-rows: none;
          width: 100%;
          height: 100%;
          min-height: 100%;
          flex: none;
          overflow: visible;
          background: transparent;
          pointer-events: none;
          z-index: 1;
        }
  
        .playground-tasks-detail-shell .playground-tasks-detail-description-preview-scope.tb-runner-chat {
          position: relative;
          inset: auto;
          height: auto;
          min-height: 36px;
        }
  
        .playground-tasks-detail-description-editor.is-editing .playground-tasks-detail-description-preview-scope.tb-runner-chat {
          display: none;
        }
  
        .playground-tasks-detail-description-preview {
          padding: 0 0 14px;
          color: rgba(255, 255, 255, 0.7);
          font-size: 12px;
          line-height: 1.6;
          min-height: 36px;
        }
  
        .playground-tasks-detail-shell .playground-tasks-detail-description-preview {
          padding-bottom: 9px;
        }
  
        .playground-tasks-detail-shell .playground-tasks-detail-description-input {
          padding-bottom: 9px;
        }
  
        .playground-tasks-detail-shell .playground-tasks-detail-description-input.is-preview {
          position: absolute;
          inset: 0;
          height: 100% !important;
          min-height: 100%;
          padding-bottom: 0;
        }
  
        .playground-tasks-detail-description-preview.tb-message-markdown,
        .playground-tasks-detail-description-preview .tb-message-markdown,
        .playground-tasks-detail-description-preview .tb-message-markdown-paragraph,
        .playground-tasks-detail-description-preview .tb-message-markdown-list,
        .playground-tasks-detail-description-preview .tb-message-markdown-heading {
          font-size: 12px;
          line-height: 1.6;
          color: rgba(255, 255, 255, 0.7);
        }
  
        .playground-tasks-detail-description-placeholder {
          color: rgba(255, 255, 255, 0.38);
        }
  
        .playground-tasks-detail-markdown-underline {
          text-decoration-thickness: 1px;
          text-underline-offset: 2px;
        }
  
        .playground-tasks-attachments {
          padding-top: 12px;
        }
  
        .playground-tasks-attachments-toolbar {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          margin-bottom: 10px;
        }
  
        .playground-tasks-attachments-toolbar .playground-tasks-detail-section-title {
          margin-bottom: 0;
        }
  
        .playground-tasks-attachments-actions {
          display: flex;
          align-items: center;
          gap: 8px;
        }
  
        .playground-tasks-attachments-environment-button {
          min-height: 0;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          color: #66a6ff;
          font-size: 12px;
          font-weight: 400;
          line-height: 1.2;
        }
  
        .playground-tasks-attachments-environment-button:hover:not(:disabled) {
          background: transparent;
          color: #66a6ff;
        }
  
        .playground-tasks-detail-shell .playground-environments-action-button.playground-tasks-attachments-environment-button,
        .playground-tasks-detail-shell .playground-environments-action-button.playground-tasks-skills-manage-button {
          min-height: 0 !important;
          height: auto;
          padding: 0 !important;
          border: 0 !important;
          border-radius: 0 !important;
          background: transparent !important;
          background-color: transparent !important;
          box-shadow: none !important;
          color: #66a6ff !important;
          font-size: 12px !important;
          font-weight: 400 !important;
          line-height: 1.2;
        }
  
        .playground-tasks-detail-shell .playground-environments-action-button.playground-tasks-attachments-environment-button::before,
        .playground-tasks-detail-shell .playground-environments-action-button.playground-tasks-skills-manage-button::before {
          content: none !important;
          display: none !important;
        }
  
        .playground-tasks-detail-shell .playground-environments-action-button.playground-tasks-attachments-environment-button:hover:not(:disabled),
        .playground-tasks-detail-shell .playground-environments-action-button.playground-tasks-skills-manage-button:hover:not(:disabled),
        .playground-tasks-detail-shell .playground-environments-action-button.playground-tasks-skills-manage-button.is-active {
          background: transparent !important;
          background-color: transparent !important;
          color: #66a6ff !important;
        }
  
        .playground-tasks-skills {
          padding-top: 14px;
        }
  
        .playground-tasks-skills-popup-shell.tb-runner-chat {
          position: relative;
          display: block;
          width: auto;
          height: auto;
          max-width: none;
          min-height: 0;
          overflow: visible;
          flex: 0 0 auto;
        }
  
        .playground-tasks-skills-popup-shell .tb-popup-menu-skills {
          position: absolute;
          top: calc(100% + 8px);
          right: 0;
          bottom: auto;
          left: auto;
          z-index: 30;
        }
  
        .playground-tasks-skills-manage-button {
          min-height: 0;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          color: #66a6ff;
          font-size: 12px;
          font-weight: 400;
          line-height: 1.2;
        }
  
        .playground-tasks-skills-manage-button:hover:not(:disabled),
        .playground-tasks-skills-manage-button.is-active {
          background: transparent;
          color: #66a6ff;
        }
  
        .playground-tasks-skills-list {
          display: flex;
          flex-wrap: wrap;
          gap: 8px;
        }
  
        .playground-tasks-skill-pill {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          min-height: 30px;
          padding: 0 8px 0 10px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.84);
          font-size: 12px;
          font-weight: 500;
          line-height: 1.35;
        }
  
        .playground-tasks-skill-pill-icon {
          width: 14px;
          height: 14px;
          flex-shrink: 0;
          color: rgba(255, 255, 255, 0.68);
        }
  
        .playground-tasks-skill-pill-label {
          white-space: nowrap;
        }
  
        .playground-tasks-skill-pill-remove {
          width: 18px;
          height: 18px;
          padding: 0;
          border: 0;
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.58);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
        }
  
        .playground-tasks-skill-pill-remove:hover {
          color: rgba(255, 255, 255, 0.92);
          background: rgba(255, 255, 255, 0.08);
        }
  
        .playground-tasks-connectors {
          padding-top: 14px;
        }
  
        .playground-tasks-connectors-list {
          display: flex;
          flex-direction: column;
          gap: 6px;
        }
  
        .playground-tasks-comments {
          padding-top: 14px;
        }
  
        .playground-tasks-subtasks {
          padding-top: 14px;
        }
  
        .playground-tasks-subtasks-add-button {
          flex: 0 0 24px;
          width: 24px;
          min-width: 24px;
          height: 24px;
          min-height: 24px;
          max-height: 24px;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          box-shadow: none;
          color: rgba(255, 255, 255, 0.7);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
        }
  
        .playground-tasks-detail-shell .playground-tasks-subtasks-add-button,
        .playground-tasks-detail-shell .playground-environments-action-button.playground-tasks-subtasks-add-button {
          flex: 0 0 24px !important;
          width: 24px !important;
          min-width: 24px !important;
          height: 24px !important;
          min-height: 24px !important;
          max-height: 24px !important;
          padding: 0 !important;
          border: 0 !important;
          border-radius: 0 !important;
          background: transparent !important;
          background-color: transparent !important;
          box-shadow: none !important;
          color: rgba(255, 255, 255, 0.82) !important;
        }
  
        .playground-environments-page:not(.playground-agents-page) .playground-tasks-subtasks-add-button {
          width: 24px;
          min-width: 24px;
          height: 24px;
          min-height: 24px;
          max-height: 24px;
          padding: 0;
        }
  
        .playground-tasks-subtasks-add-button::before {
          content: none !important;
          display: none !important;
        }
  
        .playground-tasks-subtasks-add-button:hover:not(:disabled),
        .playground-tasks-subtasks-add-button.is-active {
          background: transparent !important;
          background-color: transparent !important;
          color: rgba(255, 255, 255, 0.95) !important;
        }
  
        .playground-tasks-subtasks-add-button svg {
          display: block;
          width: 14px;
          height: 14px;
          color: currentColor;
          stroke: currentColor;
          opacity: 1;
        }
  
        .playground-tasks-subtasks-list {
          display: flex;
          flex-direction: column;
          gap: 6px;
        }
  
        .playground-tasks-subtask-row {
          width: 100%;
          min-width: 0;
          min-height: 34px;
          padding: 8px 10px;
          border: 0;
          border-radius: 10px;
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.8);
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          text-align: left;
          cursor: pointer;
        }
  
        .playground-tasks-subtask-main {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          gap: 8px;
        }
  
        .playground-tasks-subtask-ticket {
          flex: 0 0 auto;
          color: #66a6ff;
          font-size: 12px;
          font-weight: 500;
          line-height: 1;
        }
  
        .playground-tasks-subtask-title {
          min-width: 0;
          color: rgba(255, 255, 255, 0.9);
          font-size: 12px;
          line-height: 1.35;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-tasks-subtask-meta {
          flex: 0 0 auto;
          color: rgba(255, 255, 255, 0.48);
          font-size: 11px;
          line-height: 1.3;
        }
  
        .playground-tasks-comments .playground-tasks-detail-section-title {
          margin-bottom: 14px;
          padding-bottom: 0;
          border-bottom: 0;
        }
  
        .playground-tasks-comments-toolbar .playground-tasks-detail-section-title {
          margin-bottom: 0;
        }
  
        .playground-tasks-comments-list {
          display: flex;
          flex-direction: column;
          gap: 14px;
          margin-bottom: 14px;
        }
  
        .playground-tasks-comments > .playground-tasks-comment-dock {
          margin-top: 12px;
          padding: 0;
          background: transparent;
        }
  
        .playground-tasks-comment {
          display: grid;
          grid-template-columns: auto minmax(0, 1fr);
          gap: 10px;
          align-items: flex-start;
        }
  
        .playground-tasks-comment-avatar {
          width: 28px;
          height: 28px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.1);
          color: rgba(255, 255, 255, 0.88);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          overflow: hidden;
          flex-shrink: 0;
        }
  
        .playground-tasks-comment-avatar-image {
          width: 100%;
          height: 100%;
          object-fit: cover;
          display: block;
        }
  
        .playground-tasks-comment-avatar-fallback {
          font-size: 11px;
          font-weight: 500;
          letter-spacing: 0.02em;
          text-transform: uppercase;
        }
  
        .playground-tasks-comment-body {
          min-width: 0;
        }
  
        .playground-tasks-comment-meta {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          margin-bottom: 4px;
          min-width: 0;
        }
  
        .playground-tasks-comment-author {
          min-width: 0;
          color: rgba(255, 255, 255, 0.9);
          font-size: 12px;
          font-weight: 500;
          line-height: 1.35;
        }
  
        .playground-tasks-comment-time {
          flex: 0 0 auto;
          margin-left: auto;
          text-align: right;
          color: rgba(255, 255, 255, 0.42);
          font-size: 11px;
          line-height: 1.35;
        }
  
        .playground-tasks-comment-text {
          color: rgba(255, 255, 255, 0.72);
          font-size: 12px;
          line-height: 1.55;
          word-break: break-word;
        }
  
        .playground-tasks-comment-text.tb-message-markdown,
        .playground-tasks-comment-text .tb-message-markdown,
        .playground-tasks-comment-text .tb-message-markdown-paragraph,
        .playground-tasks-comment-text .tb-message-markdown-list,
        .playground-tasks-comment-text .tb-message-markdown-heading {
          margin: 0;
          color: rgba(255, 255, 255, 0.72);
          font-size: 12px;
          line-height: 1.55;
        }
  
        .playground-tasks-comment-text .tb-message-markdown-paragraph + .tb-message-markdown-paragraph,
        .playground-tasks-comment-text .tb-message-markdown-paragraph + .tb-message-markdown-list,
        .playground-tasks-comment-text .tb-message-markdown-list + .tb-message-markdown-paragraph {
          margin-top: 6px;
        }
  
        .playground-tasks-comment-text .tb-message-markdown-list {
          padding-left: 18px;
        }
  
        .playground-tasks-comment-text .tb-message-markdown-list-item + .tb-message-markdown-list-item {
          margin-top: 3px;
        }
  
        .playground-tasks-comment-text .tb-message-markdown-strong {
          color: rgba(255, 255, 255, 0.9);
          font-weight: 600;
        }
  
        .playground-tasks-comment-text .tb-message-markdown-link {
          color: #66a6ff;
          text-decoration: none;
        }
  
        .playground-tasks-comment-text .tb-message-markdown-inline-code {
          padding: 1px 4px;
          border-radius: 5px;
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.86);
          font-family: "SFMono-Regular", Consolas, "Liberation Mono", monospace;
          font-size: 11px;
        }
  
        .playground-tasks-comment-text .tb-message-markdown-pre {
          margin: 6px 0 0;
          padding: 9px 10px;
          border-radius: 8px;
          background: rgba(255, 255, 255, 0.06);
          color: rgba(255, 255, 255, 0.82);
          overflow: auto;
        }
  
        .playground-tasks-comment-empty {
          margin-bottom: 0;
        }
  
        .playground-tasks-comment-dock {
          flex: 0 0 auto;
          padding: 8px 18px 10px;
          background: linear-gradient(to top, rgba(0, 0, 0, 0.98) 74%, rgba(0, 0, 0, 0));
          position: relative;
          z-index: 1;
        }
  
        .playground-tasks-schedule-detail-shell .playground-tasks-comment-dock {
          background: transparent;
        }
  
        .playground-tasks-detail-main.is-project-wallpaper-active .playground-tasks-comment-dock {
          background: transparent;
        }
  
        .playground-tasks-comment-feedback {
          padding: 0 2px 10px;
        }
  
        .playground-tasks-comment-runner {
          width: 100%;
        }
  
        .playground-tasks-comment-bar {
          position: relative;
          display: flex;
          align-items: flex-end;
          gap: 8px;
          padding: 0 8px 0 14px;
          min-height: 48px;
          border-radius: 24px;
          background: rgba(15, 15, 15, 0.92);
          box-shadow: none;
          backdrop-filter: blur(50px);
        }
  
        .playground-tasks-detail-main.is-project-wallpaper-active .playground-tasks-comment-bar {
          background: transparent;
        }
  
        .playground-tasks-comment-bar::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          border-radius: 24px;
          padding: 1px;
          background: linear-gradient(-10deg, rgba(200, 200, 200, 0.15), rgba(255, 255, 255, 0.1), rgba(255, 255, 255, 0.07), rgba(255, 255, 255, 0.3));
          -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
          -webkit-mask-composite: xor;
          mask-composite: exclude;
        }
  
        .playground-tasks-comment-bar.is-review-mode::before {
          background: linear-gradient(-10deg, rgba(246, 190, 93, 0.7), rgba(246, 190, 93, 0.42), rgba(255, 224, 153, 0.5));
        }
  
        .playground-tasks-detail-shell .playground-tasks-comment-bar {
          min-height: 42px;
          padding: 0 7px 0 12px;
          border-radius: 21px;
          gap: 7px;
        }
  
        .playground-tasks-detail-shell .playground-tasks-comment-bar::before {
          border-radius: 21px;
        }
  
        .playground-tasks-detail-shell .playground-tasks-comment-mode-token {
          margin-bottom: 11px;
          font-size: 12px;
          line-height: 1.35;
        }
  
        .playground-tasks-detail-shell .playground-tasks-comment-input {
          min-height: 40px;
          padding: 11px 0 9px;
          font-size: 12px;
          line-height: 1.35;
        }
  
        .playground-tasks-detail-shell .playground-tasks-comment-send-button {
          width: 32px;
          height: 32px;
          margin-bottom: 5px;
        }
  
        .playground-tasks-detail-shell .playground-tasks-comment-send-icon {
          width: 14px;
          height: 14px;
        }
  
        .playground-tasks-comment-mode-token {
          flex: 0 0 auto;
          align-self: flex-end;
          margin-bottom: 14px;
          color: #f6be5d;
          font-size: 14px;
          line-height: 1.45;
          font-weight: 500;
          white-space: nowrap;
        }
  
        .playground-tasks-comment-input {
          flex: 1 1 auto;
          width: 100%;
          min-width: 0;
          min-height: 48px;
          padding: 14px 0 12px;
          border: 0;
          outline: none;
          resize: none;
          overflow: hidden;
          background: transparent;
          color: white;
          font-family: inherit;
          font-size: 14px;
          line-height: 1.45;
          box-sizing: border-box;
        }
  
        .playground-tasks-comment-input::placeholder {
          color: rgba(255, 255, 255, 0.5);
        }
  
        .playground-tasks-comment-send-button {
          flex: 0 0 auto;
          align-self: flex-end;
          margin-bottom: 6px;
          position: relative;
          width: 36px;
          height: 36px;
          padding: 0;
          border: 0;
          border-radius: 999px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          background: radial-gradient(rgba(255, 255, 255, 0.2), rgba(255, 255, 255, 0.2), rgba(255, 255, 255, 0.25));
          box-shadow: 0 2px 5px rgba(0, 0, 0, 0.25);
          color: white;
          cursor: pointer;
          backdrop-filter: blur(20px);
          transition: filter 160ms ease, opacity 160ms ease;
        }
  
        .playground-tasks-comment-send-button::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          border-radius: 999px;
          padding: 1px;
          background: linear-gradient(-10deg, rgba(200, 200, 200, 0.15), rgba(255, 255, 255, 0.1), rgba(255, 255, 255, 0.07), rgba(255, 255, 255, 0.3));
          -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
          -webkit-mask-composite: xor;
          mask-composite: exclude;
        }
  
        .playground-tasks-comment-send-button:hover:enabled {
          filter: brightness(1.08);
        }
  
        .playground-tasks-comment-send-button:disabled {
          opacity: 0.45;
          cursor: default;
        }
  
        .playground-tasks-comment-send-icon {
          width: 15px;
          height: 15px;
        }
  
        .playground-tasks-connector-row {
          display: grid;
          grid-template-columns: minmax(0, 1fr) minmax(0, auto);
          align-items: center;
          gap: 12px;
          min-height: 32px;
        }
  
        .playground-tasks-connector-service {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          gap: 8px;
        }
  
        .playground-tasks-connector-service-icon {
          width: 14px;
          height: 14px;
          flex-shrink: 0;
          object-fit: contain;
        }
  
        .playground-tasks-connector-service-icon.is-github,
        .tb-file-browser-source-brand-icon.is-github,
        .tb-file-browser-auth-icon.is-github,
        .tb-file-browser-item-icon.is-github {
          filter: invert(1);
        }
  
        .playground-tasks-connector-service-label {
          font-size: 12px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.56);
          line-height: 1.35;
        }
  
        .playground-tasks-attachments-surface.tb-runner-chat {
          display: block;
          width: auto;
          height: auto;
          max-width: none;
          min-height: 0;
          overflow: visible;
          flex: 0 0 auto;
          border-radius: 10px;
          background: transparent;
        }
  
        .playground-tasks-attachments-surface.tb-runner-chat .tb-popup-dropzone {
          min-height: 168px;
          border-radius: 10px;
          background: transparent;
        }
  
        .playground-tasks-attachments-dropzone {
          position: relative;
          padding: 0;
          align-items: stretch;
          justify-content: flex-start;
          gap: 0;
          overflow: hidden;
        }
  
        .playground-tasks-attachments-dropzone.is-filled {
          padding: 12px 14px 14px;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
        }
  
        .playground-tasks-attachments-empty-button {
          width: 100%;
          min-height: 168px;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          gap: 6px;
          padding: 20px 16px;
          border: 0;
          border-radius: 10px;
          background: transparent;
          color: inherit;
          cursor: pointer;
        }
  
        .playground-tasks-attachments-topline {
          display: inline-flex;
          align-items: center;
          flex-wrap: wrap;
          gap: 6px;
          min-height: 20px;
          margin-bottom: 14px;
          color: rgba(255, 255, 255, 0.7);
          font-size: 12px;
          line-height: 1.35;
        }
  
        .playground-tasks-attachments-dropzone.is-filled .playground-tasks-attachments-topline {
          justify-content: center;
          text-align: center;
          width: 100%;
        }
  
        .playground-tasks-attachments-topline .tb-popup-dropzone-icon {
          width: 14px;
          height: 14px;
          color: rgba(255, 255, 255, 0.78);
        }
  
        .playground-tasks-attachments-browse {
          padding: 0;
          border: 0;
          background: transparent;
          color: #4da3ff;
          font-size: 12px;
          line-height: 1.35;
          cursor: pointer;
        }
  
        .playground-tasks-attachments-browse:hover {
          color: #78bbff;
        }
  
        .playground-tasks-attachments-surface.tb-runner-chat .runner-attachments {
          padding-top: 0;
        }
  
        .playground-tasks-attachments-dropzone.is-filled .runner-attachments {
          width: auto;
          max-width: 100%;
          display: inline-flex;
          justify-content: center;
          flex-wrap: wrap;
          padding: 0;
          margin: 0 auto;
        }
  
        .playground-tasks-attachments-status {
          color: rgba(255, 255, 255, 0.42);
          font-size: 12px;
          line-height: 1.45;
        }
  
        .playground-tasks-file-browser-modal {
          width: min(760px, 100%);
          height: min(680px, calc(100vh - 48px));
        }
  
        .playground-tasks-file-browser-shell {
          position: relative;
          z-index: 1;
          height: 100%;
          display: flex;
          flex-direction: column;
          min-height: 0;
        }
  
        .playground-tasks-file-browser-search-wrap {
          padding: 0 12px 12px;
        }
  
        .playground-tasks-confirm-scrim {
          position: fixed;
          inset: 0;
          z-index: 10001;
          display: flex;
          align-items: center;
          justify-content: center;
          padding: 24px;
          background: rgba(0, 0, 0, 0.58);
        }
  
        .playground-tasks-confirm-dialog {
          width: min(420px, 100%);
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 16px;
          background: rgba(18, 18, 18, 0.96);
          box-shadow: 0 24px 60px rgba(0, 0, 0, 0.42);
          padding: 18px;
          display: flex;
          flex-direction: column;
          gap: 12px;
        }
  
        .playground-tasks-confirm-title {
          color: rgba(255, 255, 255, 0.94);
          font-size: 15px;
          font-weight: 500;
          line-height: 1.35;
        }
  
        .playground-tasks-confirm-copy {
          color: rgba(255, 255, 255, 0.58);
          font-size: 12px;
          line-height: 1.5;
        }
  
        .playground-tasks-confirm-actions {
          display: flex;
          justify-content: flex-end;
          gap: 8px;
        }
  
        .playground-tasks-parent-picker-dialog {
          width: min(480px, 100%);
        }
  
        .playground-tasks-parent-picker-list {
          display: flex;
          flex-direction: column;
          gap: 6px;
          max-height: min(52vh, 360px);
          overflow: auto;
        }
  
        .playground-tasks-parent-picker-row {
          width: 100%;
          min-width: 0;
          padding: 10px 12px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 12px;
          background: rgba(255, 255, 255, 0.03);
          color: rgba(255, 255, 255, 0.86);
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          cursor: pointer;
          text-align: left;
        }
  
        .playground-tasks-parent-picker-row:hover {
          background: rgba(255, 255, 255, 0.06);
        }
  
        .playground-tasks-parent-picker-row-main {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 4px;
        }
  
        .playground-tasks-parent-picker-row-title {
          min-width: 0;
          color: rgba(255, 255, 255, 0.92);
          font-size: 13px;
          font-weight: 500;
          line-height: 1.35;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-tasks-parent-picker-row-copy {
          color: rgba(255, 255, 255, 0.5);
          font-size: 11px;
          line-height: 1.35;
        }
  
        .playground-tasks-parent-picker-row-ticket {
          flex: 0 0 auto;
          color: #66a6ff;
          font-size: 12px;
          font-weight: 500;
          line-height: 1;
        }
  
        .playground-tasks-detail-header {
          display: flex;
          flex-direction: column;
          align-items: stretch;
          gap: 12px;
        }
  
        .playground-tasks-detail-header-copy {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 6px;
        }
  
        .playground-tasks-detail-title-row {
          display: flex;
          align-items: center;
          gap: 10px;
          min-width: 0;
          flex-wrap: wrap;
        }
  
        .playground-tasks-detail-meta-row {
          display: flex;
          align-items: center;
          gap: 8px;
          min-height: 30px;
          flex-wrap: wrap;
        }
  
        .playground-tasks-detail-actions {
          display: flex;
          align-items: center;
          gap: 8px;
          flex-wrap: wrap;
          justify-content: flex-start;
        }
  
        .playground-tasks-detail-menu-item-danger,
        .playground-tasks-detail-menu-item-danger .playground-files-toolbar-menu-item-copy span:first-child,
        .playground-tasks-detail-menu-item-danger .playground-tasks-toolbar-popup-item-copy span:first-child {
          color: #ffb0b0;
        }
  
        .playground-tasks-header {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 16px;
        }
  
        .playground-tasks-header.is-projects-home {
          align-items: center;
          padding: 2px 0 12px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.08);
          margin-bottom: 17px;
        }
  
        .playground-tasks-title-block {
          min-width: 0;
        }
  
        .playground-tasks-title {
          font-size: 28px;
          line-height: 1.05;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-tasks-title.is-projects-home {
          font-size: 15px;
          line-height: 1.2;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.94);
        }
  
        .playground-tasks-copy {
          margin-top: 8px;
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.52);
          max-width: 760px;
        }
  
        .playground-tasks-toolbar {
          display: flex;
          align-items: center;
          gap: 10px;
          flex-wrap: wrap;
          justify-content: flex-end;
        }
  
        .playground-tasks-home-width {
          width: min(100%, var(--playground-centered-page-max-width));
          max-width: var(--playground-centered-page-max-width);
          margin: 0 auto;
        }
  
        .playground-tasks-project-breadcrumb {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          margin-bottom: 10px;
        }
  
        .playground-tasks-project-back {
          display: inline-flex;
          align-items: center;
          gap: 6px;
        }
  
        .playground-tasks-nav {
          width: fit-content;
          gap: 8px;
          padding: 2px;
          border: 1px solid rgba(255, 255, 255, 0.05);
          background: rgba(255, 255, 255, 0.1);
          backdrop-filter: blur(20px);
          -webkit-backdrop-filter: blur(20px);
        }
  
        .playground-tasks-nav .content-mode-button {
          min-width: 64px;
          padding: 4px 12px;
          font-size: 12px;
          font-weight: 400;
        }
  
        .playground-tasks-banner {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 16px;
          padding: 14px 16px;
          border: 1px solid rgba(115, 188, 255, 0.18);
          border-radius: 16px;
          background: rgba(49, 126, 210, 0.14);
        }
  
        .playground-tasks-banner-copy {
          font-size: 12px;
          line-height: 1.5;
          color: rgba(233, 244, 255, 0.84);
        }
  
        .playground-tasks-summary-grid {
          display: grid;
          grid-template-columns: repeat(4, minmax(0, 1fr));
          gap: 12px;
        }
  
        .playground-tasks-summary-card {
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 16px;
          background: rgba(255, 255, 255, 0.03);
          padding: 16px;
          display: flex;
          flex-direction: column;
          gap: 8px;
        }
  
        .playground-tasks-summary-label {
          font-size: 11px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.52);
          text-transform: uppercase;
          letter-spacing: 0.04em;
        }
  
        .playground-tasks-summary-value {
          font-size: 26px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-tasks-summary-copy {
          font-size: 12px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.56);
        }
  
        .playground-tasks-project-modal-backdrop {
          position: fixed;
          inset: 0;
          z-index: 180;
          display: flex;
          align-items: center;
          justify-content: center;
          padding: 24px;
          background: rgba(0, 0, 0, 0.58);
          backdrop-filter: blur(10px);
        }
  
        .playground-tasks-project-modal-backdrop.playground-tasks-project-initial-setup-backdrop {
          z-index: 10020;
          background: rgba(0, 0, 0, 0);
          backdrop-filter: none;
          -webkit-backdrop-filter: none;
          transition: background-color 75ms linear !important;
        }
  
        .playground-tasks-project-modal-backdrop.playground-tasks-project-initial-setup-backdrop.is-visible,
        .playground-tasks-project-modal-backdrop.playground-tasks-project-issue-backdrop.is-visible,
        .playground-tasks-project-modal-backdrop.playground-mission-control-modal-backdrop.is-visible {
          background: rgba(0, 0, 0, 0.5);
        }
  
        .playground-tasks-project-modal-backdrop.playground-tasks-project-issue-backdrop,
        .playground-tasks-project-modal-backdrop.playground-mission-control-modal-backdrop {
          z-index: 10020;
          background: rgba(0, 0, 0, 0);
          backdrop-filter: none;
          -webkit-backdrop-filter: none;
          transition: background-color 75ms linear !important;
        }
  
        .playground-tasks-project-modal-backdrop.playground-project-overview-outcome-editor-backdrop {
          z-index: 10040;
        }
  
        .playground-agent-runtime-thread-modal-backdrop {
          z-index: 140;
        }
  
        .playground-tasks-project-modal {
          width: min(820px, calc(100vw - 48px));
          padding: 12px 16px 16px;
          border-radius: 15px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          background: rgba(255, 255, 255, 0.1);
          box-shadow: 0 12px 30px rgba(0, 0, 0, 0.35);
          backdrop-filter: blur(50px);
          -webkit-backdrop-filter: blur(50px);
        }
  
        .playground-tasks-project-modal-top {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 16px;
          margin-bottom: 14px;
        }
  
        .playground-tasks-project-modal-name-row {
          position: relative;
          flex: 1;
          min-width: 0;
          display: flex;
          align-items: center;
          gap: 14px;
          padding-bottom: 0;
        }
  
        .playground-tasks-project-modal-icon-trigger {
          width: 30px;
          height: 30px;
          padding: 0;
          border: 0;
          border-radius: 10px;
          background: transparent;
          color: rgba(255, 255, 255, 0.96);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          transition: background-color 160ms ease, color 160ms ease;
        }
  
        .playground-tasks-project-modal-icon-trigger:hover,
        .playground-tasks-project-modal-icon-trigger.is-active {
          background: rgba(255, 255, 255, 0.08);
        }
  
        .playground-tasks-project-composer-modal .playground-tasks-project-modal-icon-trigger:hover,
        .playground-tasks-project-composer-modal .playground-tasks-project-modal-icon-trigger.is-active {
          background: transparent;
        }
  
        .playground-tasks-project-composer-modal {
          border: 0 !important;
          background: transparent !important;
          box-shadow: none !important;
          backdrop-filter: none !important;
          -webkit-backdrop-filter: none !important;
        }
  
        .playground-tasks-project-composer-modal::before,
        .playground-tasks-project-composer-modal::after {
          content: none !important;
          display: none !important;
        }
  
        .playground-tasks-project-composer-modal .playground-tasks-project-modal-icon-trigger,
        .playground-tasks-project-composer-modal .playground-tasks-project-modal-icon-trigger:hover,
        .playground-tasks-project-composer-modal .playground-tasks-project-modal-icon-trigger.is-active,
        .playground-tasks-project-composer-modal .playground-tasks-project-modal-icon-trigger:focus-visible {
          background: transparent !important;
          box-shadow: none !important;
        }
  
        .playground-tasks-project-modal-name-input {
          flex: 1;
          min-width: 0;
          height: 36px;
          padding: 0;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.98);
          font-size: 17px;
          line-height: 1.2;
          font-weight: 400;
          outline: none;
        }
  
        .playground-tasks-project-modal-name-input::placeholder {
          color: rgba(255, 255, 255, 0.44);
        }
  
        .playground-tasks-project-composer-modal .playground-tasks-project-modal-top {
          align-items: center;
        }
  
        .playground-tasks-project-modal-environment-picker {
          flex: 0 1 280px;
          min-width: 0;
          display: flex;
          justify-content: flex-end;
        }
  
        .playground-tasks-project-modal-environment-picker .playground-tasks-detail-select-shell {
          width: 100%;
          max-width: 280px;
        }
  
        .playground-tasks-project-modal-environment-button {
          width: 100%;
          margin-left: 0;
          justify-content: flex-end;
          gap: 8px;
        }
  
        .playground-tasks-project-modal-environment-icon {
          width: 14px;
          height: 14px;
          flex: 0 0 auto;
          color: rgba(255, 255, 255, 0.84);
        }
  
        .playground-tasks-project-modal-close {
          flex: 0 0 auto;
          width: auto;
          min-width: 0;
          height: 36px;
          padding-left: 0;
          padding-right: 0;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.72);
        }
  
        .playground-tasks-project-icon-picker {
          position: absolute;
          top: calc(100% + 12px);
          left: 0;
          width: 224px;
          padding: 12px;
          display: grid;
          grid-template-columns: repeat(4, minmax(0, 1fr));
          gap: 8px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 18px;
          background: rgba(14, 14, 16, 0.98);
          box-shadow: 0 24px 60px rgba(0, 0, 0, 0.45);
          z-index: 2;
        }
  
        .playground-tasks-project-icon-option {
          width: 100%;
          aspect-ratio: 1;
          padding: 0;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 14px;
          background: rgba(255, 255, 255, 0.03);
          color: rgba(255, 255, 255, 0.8);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          transition: border-color 160ms ease, background-color 160ms ease, color 160ms ease, transform 160ms ease;
        }
  
        .playground-tasks-project-icon-option:hover {
          border-color: rgba(255, 255, 255, 0.18);
          color: rgba(255, 255, 255, 0.96);
          transform: translateY(-1px);
        }
  
        .playground-tasks-project-icon-option.is-active {
          border-color: rgba(255, 255, 255, 0.2);
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-tasks-project-modal-field {
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-tasks-project-modal-label {
          font-size: 13px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.94);
        }
  
        .playground-tasks-project-modal-textarea {
          width: 100%;
          min-height: 130px;
          padding: 14px 16px;
          border: 1px solid rgba(255, 255, 255, 0.12);
          border-radius: 16px;
          background: rgba(255, 255, 255, 0.02);
          color: rgba(255, 255, 255, 0.94);
          font: inherit;
          font-size: 14px;
          line-height: 1.55;
          resize: none;
          outline: none;
        }
  
        .playground-tasks-project-modal-textarea::placeholder {
          color: rgba(255, 255, 255, 0.42);
        }
  
        .playground-tasks-issue-modal {
          width: min(720px, calc(100vw - 48px));
          max-height: calc(100vh - 48px);
          display: flex;
          flex-direction: column;
        }
  
        .playground-tasks-issue-modal-body {
          min-height: 0;
          overflow: auto;
          display: grid;
          gap: 14px;
          padding-right: 4px;
        }
  
        .playground-tasks-project-modal.playground-tasks-project-issue-modal .playground-tasks-project-modal-top {
          align-items: center;
        }
  
        .playground-tasks-project-modal.playground-tasks-project-issue-modal .playground-tasks-project-modal-icon-trigger,
        .playground-tasks-project-modal.playground-tasks-project-issue-modal .playground-tasks-project-modal-icon-trigger:hover,
        .playground-tasks-project-modal.playground-tasks-project-issue-modal .playground-tasks-project-modal-icon-trigger.is-active,
        .playground-tasks-project-modal.playground-tasks-project-issue-modal .playground-tasks-project-modal-icon-trigger:focus-visible {
          background: transparent !important;
          box-shadow: none !important;
        }
  
        .playground-tasks-project-modal.playground-tasks-project-issue-modal .playground-tasks-issue-details-section {
          overflow: visible;
          border: 0;
          border-radius: 10px;
          background: transparent;
          box-shadow: none;
          padding: 0;
          backdrop-filter: none;
          -webkit-backdrop-filter: none;
        }
  
        .playground-tasks-issue-modal-title-input {
          min-width: 80px;
        }
  
        .playground-tasks-issue-modal-computer-picker {
          flex: 0 1 280px;
          min-width: 180px;
        }
  
        .playground-tasks-issue-modal-grid {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 12px;
        }
  
        .playground-tasks-issue-modal-field.is-full {
          grid-column: 1 / -1;
        }
  
        .playground-tasks-issue-modal-input,
        .playground-tasks-issue-modal-select {
          width: 100%;
          height: 38px;
          padding: 0 12px;
          border: 1px solid rgba(255, 255, 255, 0.12);
          border-radius: 12px;
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.94);
          font: inherit;
          font-size: 13px;
          outline: none;
        }
  
        .playground-tasks-issue-modal-select {
          color-scheme: dark;
        }
  
        .playground-tasks-issue-modal-input::placeholder,
        .playground-tasks-issue-modal-textarea::placeholder {
          color: rgba(255, 255, 255, 0.42);
        }
  
        .playground-tasks-issue-modal-textarea {
          min-height: 118px;
          resize: vertical;
        }
  
        .playground-tasks-issue-modal-check-row {
          min-height: 38px;
          display: inline-flex;
          align-items: center;
          gap: 10px;
          padding: 0 12px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 12px;
          background: rgba(255, 255, 255, 0.035);
          color: rgba(255, 255, 255, 0.82);
          font-size: 13px;
          cursor: pointer;
        }
  
        .playground-tasks-issue-modal-check-row input {
          width: 14px;
          height: 14px;
          margin: 0;
        }
  
        @media (max-width: 720px) {
          .playground-tasks-project-modal.playground-tasks-project-issue-modal .playground-tasks-project-modal-top {
            flex-wrap: wrap;
          }
  
          .playground-tasks-issue-modal-computer-picker {
            flex: 1 1 100%;
            justify-content: flex-start;
          }
  
          .playground-tasks-issue-modal-grid {
            grid-template-columns: 1fr;
          }
        }
  
        .playground-tasks-project-modal-description {
          margin-top: 18px;
        }
  
        .playground-mission-control-setup-form .playground-tasks-project-modal-description {
          border-top: 1px solid rgba(255, 255, 255, 0.1);
          padding-top: 12px;
        }
  
        .playground-tasks-project-wallpaper-field {
          margin-top: 16px;
        }
  
        .playground-tasks-project-wallpaper-picker {
          width: 100%;
          margin: 0 0 14px;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          gap: 8px;
          color: #ffffff;
          font-size: 12px;
          line-height: 1;
          font-weight: 400;
        }
  
        .playground-tasks-project-wallpaper-picker-preview {
          position: relative;
          width: 100%;
          height: 188px;
          overflow: hidden;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 15px;
          background-size: cover;
          background-position: center;
          background-repeat: no-repeat;
        }
  
        .playground-tasks-project-wallpaper-picker-preview-image {
          position: absolute;
          inset: 0;
          background-size: cover;
          background-position: center;
          background-repeat: no-repeat;
          will-change: transform;
        }
  
        .playground-tasks-project-wallpaper-picker-preview.is-next .playground-tasks-project-wallpaper-picker-preview-image.is-outgoing {
          animation: playgroundProjectWallpaperSlideOutLeft 360ms ease both;
        }
  
        .playground-tasks-project-wallpaper-picker-preview.is-next .playground-tasks-project-wallpaper-picker-preview-image.is-incoming {
          animation: playgroundProjectWallpaperSlideInRight 360ms ease both;
        }
  
        .playground-tasks-project-wallpaper-picker-preview.is-prev .playground-tasks-project-wallpaper-picker-preview-image.is-outgoing {
          animation: playgroundProjectWallpaperSlideOutRight 360ms ease both;
        }
  
        .playground-tasks-project-wallpaper-picker-preview.is-prev .playground-tasks-project-wallpaper-picker-preview-image.is-incoming {
          animation: playgroundProjectWallpaperSlideInLeft 360ms ease both;
        }
  
        .playground-tasks-project-wallpaper-picker-controls {
          width: 100%;
          display: flex;
          align-items: center;
          justify-content: center;
          gap: 12px;
        }
  
        .playground-tasks-project-wallpaper-picker-button {
          width: 24px;
          height: 24px;
          padding: 0;
          border: 0;
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.86);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          transition: color 160ms ease;
        }
  
        .playground-tasks-project-wallpaper-picker-button:hover {
          color: #ffffff;
        }
  
        .playground-tasks-project-wallpaper-picker-label {
          color: #ffffff;
          font-size: 12px;
          line-height: 1;
          font-weight: 400;
          min-width: 110px;
          text-align: center;
        }
  
        .playground-tasks-project-blueprint-section {
          display: flex;
          flex-direction: column;
          gap: 10px;
          margin: 0 0 14px;
          position: relative;
        }
  
        .playground-tasks-project-blueprint-heading {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-tasks-project-blueprint-active {
          min-width: 0;
          color: rgba(255, 255, 255, 0.56);
          font-size: 12px;
          line-height: 1.2;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-tasks-project-blueprint-trigger {
          width: 100%;
          min-height: 44px;
          padding: 8px 10px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 12px;
          background: rgba(255, 255, 255, 0.05);
          color: #ffffff;
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          text-align: left;
          cursor: pointer;
          transition: background-color 160ms ease, border-color 160ms ease;
        }
  
        .playground-tasks-project-blueprint-trigger:hover {
          border-color: rgba(255, 255, 255, 0.18);
          background: rgba(255, 255, 255, 0.07);
        }
  
        .playground-tasks-project-blueprint-trigger-main {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          gap: 10px;
        }
  
        .playground-tasks-project-blueprint-trigger-copy {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 2px;
        }
  
        .playground-tasks-project-blueprint-trigger-title {
          overflow: hidden;
          color: rgba(255, 255, 255, 0.96);
          font-size: 13px;
          line-height: 1.2;
          font-weight: 500;
          white-space: nowrap;
          text-overflow: ellipsis;
        }
  
        .playground-tasks-project-blueprint-trigger-description {
          overflow: hidden;
          color: rgba(255, 255, 255, 0.5);
          font-size: 12px;
          line-height: 1.25;
          white-space: nowrap;
          text-overflow: ellipsis;
        }
  
        .playground-tasks-project-blueprint-chevron {
          width: 16px;
          height: 16px;
          flex: 0 0 auto;
          color: rgba(255, 255, 255, 0.5);
        }
  
        .playground-tasks-project-blueprint-section.is-open {
          z-index: 120;
        }
  
        .playground-tasks-toolbar-popup-shell .playground-tasks-toolbar-popup-menu.playground-tasks-project-blueprint-popover {
          right: 0;
          width: min(460px, calc(100vw - 48px));
          min-width: min(460px, calc(100vw - 48px));
          max-height: min(196px, calc(100vh - 160px));
          padding: 8px;
          overflow-y: auto;
        }
  
        .playground-tasks-project-blueprint-popover .playground-tasks-project-blueprint-grid {
          grid-template-columns: 1fr;
          gap: 6px;
        }
  
        .playground-tasks-project-blueprint-popover .playground-tasks-project-blueprint-option {
          border-color: transparent;
          border-radius: 10px;
          background: transparent;
        }
  
        .playground-tasks-project-blueprint-popover .playground-tasks-project-blueprint-option:hover,
        .playground-tasks-project-blueprint-popover .playground-tasks-project-blueprint-option.is-active {
          border-color: rgba(255, 255, 255, 0.1);
          background: rgba(255, 255, 255, 0.08);
          transform: none;
        }
  
        .playground-tasks-project-blueprint-modal {
          width: min(680px, calc(100vw - 48px));
          max-height: min(760px, calc(100vh - 48px));
          display: flex;
          flex-direction: column;
          overflow: hidden;
        }
  
        .playground-tasks-project-blueprint-modal-backdrop {
          z-index: 10020;
        }
  
        .playground-tasks-project-blueprint-modal-body {
          min-height: 0;
          overflow: auto;
          scrollbar-width: none;
        }
  
        .playground-tasks-project-blueprint-modal-body::-webkit-scrollbar {
          display: none;
        }
  
        .playground-tasks-project-blueprint-grid {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 8px;
        }
  
        .playground-tasks-project-blueprint-option {
          width: 100%;
          min-width: 0;
          padding: 10px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 12px;
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.9);
          display: flex;
          align-items: flex-start;
          gap: 10px;
          text-align: left;
          cursor: pointer;
          transition: background-color 160ms ease, border-color 160ms ease, transform 160ms ease;
        }
  
        .playground-tasks-project-blueprint-option:hover {
          border-color: rgba(255, 255, 255, 0.18);
          background: rgba(255, 255, 255, 0.07);
          transform: translateY(-1px);
        }
  
        .playground-tasks-project-blueprint-option.is-active {
          border-color: rgba(255, 255, 255, 0.28);
          background: rgba(255, 255, 255, 0.1);
        }
  
        .playground-tasks-project-blueprint-icon {
          width: 28px;
          height: 28px;
          flex: 0 0 auto;
          border-radius: 9px;
          background: color-mix(in srgb, var(--project-blueprint-accent, #66A6FF) 22%, transparent);
          color: var(--project-blueprint-accent, #66A6FF);
          display: inline-flex;
          align-items: center;
          justify-content: center;
        }
  
        .playground-tasks-project-blueprint-copy {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 4px;
        }
  
        .playground-tasks-project-blueprint-title {
          color: #ffffff;
          font-size: 13px;
          line-height: 1.2;
          font-weight: 500;
        }
  
        .playground-tasks-project-blueprint-description {
          color: rgba(255, 255, 255, 0.56);
          font-size: 12px;
          line-height: 1.35;
        }
  
        .playground-tasks-project-blueprint-chips {
          display: flex;
          flex-wrap: wrap;
          gap: 6px;
        }
  
        .playground-tasks-project-blueprint-chip {
          min-width: 0;
          border-radius: 999px;
          padding: 3px 7px;
          background: rgba(255, 255, 255, 0.06);
          color: rgba(255, 255, 255, 0.64);
          font-size: 10px;
          line-height: 1.2;
          font-weight: 500;
          white-space: nowrap;
        }
  
        .playground-tasks-project-lead-selector {
          width: 100%;
          min-height: 52px;
          padding: 8px 10px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 12px;
          background: rgba(255, 255, 255, 0.04);
          color: #ffffff;
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          text-align: left;
          cursor: pointer;
          transition: background-color 160ms ease, border-color 160ms ease;
        }
  
        .playground-tasks-project-lead-selector:hover {
          border-color: rgba(255, 255, 255, 0.18);
          background: rgba(255, 255, 255, 0.07);
        }
  
        .playground-tasks-project-lead-label {
          flex: 0 0 auto;
          color: rgba(255, 255, 255, 0.5);
          font-size: 12px;
          line-height: 1.2;
        }
  
        .playground-tasks-project-lead-person {
          min-width: 0;
          margin-left: auto;
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          gap: 9px;
        }
  
        .playground-tasks-project-lead-avatar {
          width: 28px;
          height: 28px;
          flex: 0 0 28px;
          border-radius: 999px;
          object-fit: cover;
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.72);
          display: inline-flex;
          align-items: center;
          justify-content: center;
        }
  
        .playground-tasks-project-lead-copy {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 2px;
        }
  
        .playground-tasks-project-lead-name,
        .playground-tasks-project-lead-email {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-tasks-project-lead-name {
          color: rgba(255, 255, 255, 0.94);
          font-size: 13px;
          line-height: 1.2;
          font-weight: 500;
        }
  
        .playground-tasks-project-lead-email {
          color: rgba(255, 255, 255, 0.45);
          font-size: 11px;
          line-height: 1.2;
        }
  
        .playground-tasks-project-lead-chevron {
          width: 16px;
          height: 16px;
          flex: 0 0 auto;
          color: rgba(255, 255, 255, 0.5);
        }
  
        .playground-tasks-project-composer-modal.playground-tasks-project-initial-setup-modal {
          --tb-runner-input-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          width: min(640px, calc(100vw - 48px));
          position: relative;
          overflow: visible;
          padding: 16px;
          border: 0 !important;
          border-radius: 25px;
          background: rgba(30, 30, 30, 0.5) !important;
          box-shadow: 0 24px 70px rgba(0, 0, 0, 0.48) !important;
          backdrop-filter: blur(20px) !important;
          -webkit-backdrop-filter: blur(20px) !important;
          transform-origin: center;
          opacity: 0.5;
          transform: scale(0.5);
          transition: opacity 75ms linear, transform 75ms linear !important;
          will-change: opacity, transform;
        }
  
        .playground-tasks-project-composer-modal.playground-tasks-project-initial-setup-modal.is-visible,
        .playground-tasks-project-modal.playground-tasks-project-issue-modal.is-visible,
        .playground-tasks-project-modal.playground-mission-control-modal.is-visible {
          opacity: 1;
          transform: scale(1);
        }
  
        .playground-tasks-project-modal.playground-tasks-project-issue-modal,
        .playground-tasks-project-modal.playground-mission-control-modal {
          --tb-runner-input-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          position: relative;
          overflow: visible;
          border: 0 !important;
          border-radius: 25px;
          background: rgba(30, 30, 30, 0.5) !important;
          box-shadow: 0 24px 70px rgba(0, 0, 0, 0.48) !important;
          backdrop-filter: blur(20px) !important;
          -webkit-backdrop-filter: blur(20px) !important;
          transform-origin: center;
          opacity: 0.5;
          transform: scale(0.5);
          transition: opacity 75ms linear, transform 75ms linear !important;
          will-change: opacity, transform;
        }
  
        .playground-tasks-project-modal.playground-tasks-project-issue-modal::before,
        .playground-tasks-project-modal.playground-mission-control-modal::before {
          content: "" !important;
          pointer-events: none;
          position: absolute;
          inset: 0;
          z-index: 5;
          display: block !important;
          border-radius: inherit;
          padding: 1px;
          background: var(--tb-task-input-border, var(--tb-runner-input-border));
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-tasks-project-modal.playground-tasks-project-issue-modal::after,
        .playground-tasks-project-modal.playground-mission-control-modal::after {
          content: none !important;
          display: none !important;
        }
  
        .playground-tasks-project-composer-modal.playground-tasks-project-initial-setup-modal::before {
          content: "" !important;
          pointer-events: none;
          position: absolute;
          inset: 0;
          z-index: 5;
          display: block !important;
          border-radius: inherit;
          padding: 1px;
          background: var(--tb-task-input-border, var(--tb-runner-input-border));
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-tasks-project-composer-modal.playground-tasks-project-initial-setup-modal::after {
          content: none !important;
          display: none !important;
        }
  
        .playground-tasks-project-initial-setup-modal .playground-tasks-project-modal-top {
          align-items: center;
          margin-bottom: 16px;
        }
  
        .playground-tasks-project-initial-setup-body {
          display: flex;
          flex-direction: column;
          gap: 14px;
        }
  
        .playground-tasks-project-initial-setup-field {
          display: flex;
          flex-direction: column;
          gap: 8px;
        }
  
        .playground-tasks-project-initial-setup-label {
          color: rgba(255, 255, 255, 0.56);
          font-size: 12px;
          line-height: 1.2;
          font-weight: 500;
        }
  
        .playground-tasks-project-initial-setup-modal .playground-tasks-project-blueprint-section {
          margin: 0;
          gap: 0;
        }
  
        .playground-tasks-project-initial-setup-modal .playground-environments-runtime-popup-shell.is-open {
          z-index: 130;
        }
  
        .playground-tasks-project-initial-setup-modal .playground-tasks-project-blueprint-icon {
          background: transparent;
          color: #ffffff;
        }
  
        .playground-tasks-project-initial-setup-modal .playground-tasks-project-lead-selector {
          min-height: 48px;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
        }
  
        .playground-tasks-project-initial-setup-modal .playground-tasks-project-lead-selector:hover {
          border-color: rgba(255, 255, 255, 0.1);
          background: transparent;
        }
  
        .playground-tasks-project-initial-setup-modal .playground-tasks-project-lead-chevron {
          display: none;
        }
  
        .playground-tasks-project-initial-setup-goal-editor {
          margin-top: 0;
          padding-top: 0;
        }
  
        .playground-tasks-project-initial-setup-modal .playground-tasks-project-modal-actions {
          margin-top: 16px;
        }
  
        .playground-tasks-project-modal.playground-mission-control-modal {
          width: min(720px, calc(100vw - 48px));
          height: 700px;
          max-height: calc(100vh - 48px);
          padding: 16px;
          display: flex;
          flex-direction: column;
          box-sizing: border-box;
          background: linear-gradient(to bottom, black, rgba(30,30,30,0.5), rgba(30,30,30,0.5)) !important;
        }
  
        .playground-tasks-project-modal.playground-mission-control-modal:not(.playground-project-overview-outcome-editor-modal):not(.playground-tasks-release-modal) {
          height: 600px;
        }
  
        .playground-mission-control-modal .playground-tasks-project-modal-top {
          align-items: center;
          justify-content: flex-end;
          margin-bottom: 4px;
          padding-bottom: 0;
          border-bottom: 0;
        }
  
        .playground-mission-control-modal-body {
          flex: 1 1 auto;
          min-height: 0;
          display: flex;
          flex-direction: column;
          gap: 14px;
          overflow: visible;
        }
  
        .playground-mission-control-modal-context {
          flex: 1 1 auto;
          min-height: 0;
          max-height: none;
          display: flex;
          flex-direction: column;
          gap: 14px;
          overflow: auto;
          padding-right: 4px;
          scrollbar-width: none;
        }
  
        .playground-mission-control-modal-context::-webkit-scrollbar {
          display: none;
        }
  
        .playground-mission-control-modal-context-editor {
          margin-top: 0;
          padding-top: 0;
        }
  
        .playground-mission-control-modal-outcomes-editor {
          margin-bottom: 84px;
        }
  
        .playground-mission-control-modal-outcomes-list {
          display: flex;
          flex-direction: column;
          gap: 8px;
        }
  
        .playground-mission-control-modal-outcome-row {
          position: relative;
          display: flex;
          align-items: center;
          gap: 8px;
          min-height: 40px;
          padding: 6px 8px 6px 12px;
          border-radius: 10px;
          background: rgba(255, 255, 255, 0.05);
          overflow: visible;
        }
  
        .playground-mission-control-modal-outcome-copy {
          flex: 1 1 auto;
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 2px;
        }
  
        .playground-mission-control-modal-outcome-input {
          min-width: 0;
          height: 28px;
          padding: 0;
          border: 0;
          outline: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.86);
          font: inherit;
          font-size: 12px;
          line-height: 1.4;
        }
  
        .playground-mission-control-modal-outcome-input::placeholder {
          color: rgba(255, 255, 255, 0.36);
        }
  
        .playground-mission-control-modal-outcome-add,
        .playground-mission-control-modal-outcome-menu-trigger {
          width: 28px;
          height: 28px;
          padding: 0;
          border: 0;
          border-radius: 8px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          background: transparent;
          color: rgba(255, 255, 255, 0.68);
          cursor: pointer;
          transition: background-color 150ms ease, color 150ms ease;
        }
  
        .playground-mission-control-modal-outcome-add:hover,
        .playground-mission-control-modal-outcome-menu-trigger:hover,
        .playground-mission-control-modal-outcome-menu-shell.is-open .playground-mission-control-modal-outcome-menu-trigger {
          background: rgba(255, 255, 255, 0.08);
          color: #ffffff;
        }
  
        .playground-mission-control-modal-outcome-menu-shell {
          flex: 0 0 auto;
          z-index: 65;
        }
  
        .playground-mission-control-modal-outcome-menu {
          width: 210px;
          min-width: 210px;
          z-index: 66;
        }
  
        .playground-mission-control-modal-outcomes-empty {
          width: 100%;
          min-height: 40px;
          padding: 0 12px;
          border: 1px dashed rgba(255, 255, 255, 0.12);
          border-radius: 10px;
          background: rgba(255, 255, 255, 0.03);
          color: rgba(255, 255, 255, 0.5);
          font: inherit;
          font-size: 12px;
          line-height: 1.4;
          cursor: pointer;
          transition: border-color 150ms ease, background-color 150ms ease, color 150ms ease;
        }
  
        .playground-mission-control-modal-outcomes-empty:hover {
          border-color: rgba(255, 255, 255, 0.2);
          background: rgba(255, 255, 255, 0.06);
          color: rgba(255, 255, 255, 0.78);
        }
  
        .playground-mission-control-modal-composer {
          flex: 0 0 auto;
          min-width: 0;
          margin-top: auto;
          overflow: visible;
          position: relative;
          z-index: 20;
        }
  
        .playground-tasks-project-modal.playground-project-overview-outcome-editor-modal {
          height: 600px;
          overflow: visible;
        }
  
        .playground-mission-control-modal.playground-project-overview-outcome-editor-modal .playground-tasks-project-modal-top {
          align-items: center;
          justify-content: space-between;
          gap: 14px;
          margin-bottom: 14px;
          padding-bottom: 12px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-project-overview-outcome-editor-title-input {
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-project-overview-outcome-editor-modal .playground-tasks-project-modal-icon-trigger,
        .playground-project-overview-outcome-editor-modal .playground-tasks-project-modal-icon-trigger:hover,
        .playground-project-overview-outcome-editor-modal .playground-tasks-project-modal-icon-trigger.is-active {
          background: transparent !important;
          box-shadow: none !important;
        }
  
        .playground-project-overview-outcome-editor-shell {
          overflow: visible;
        }
  
        .playground-project-overview-outcome-editor-body {
          gap: 14px;
        }
  
        .playground-project-overview-outcome-editor-body.playground-mission-control-modal-context {
          overflow: visible;
        }
  
        .playground-project-overview-outcome-editor-modal .playground-tasks-project-modal-field {
          display: flex;
          flex-direction: column;
          gap: 8px;
        }
  
        .playground-project-overview-outcome-editor-modal .playground-tasks-detail-description {
          margin-top: 0;
          padding-top: 0;
        }
  
        .playground-project-overview-outcome-editor-modal .playground-tasks-detail-description + .playground-tasks-detail-description {
          margin-top: 0;
        }
  
        .playground-project-overview-outcome-editor-modal .playground-environments-textarea {
          border-color: rgba(255, 255, 255, 0.1);
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-project-overview-outcome-editor-modal .playground-tasks-project-modal-actions {
          flex: 0 0 auto;
          margin-top: 0;
        }
  
        .playground-project-overview-outcome-milestone-field {
          position: relative;
          overflow: visible;
        }
  
        .playground-project-overview-outcome-milestone-title-row {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          min-width: 0;
        }
  
        .playground-project-overview-outcome-milestone-add {
          width: 24px;
          height: 24px;
          padding: 0;
          border: 0;
          border-radius: 8px;
          background: transparent;
          color: rgba(255, 255, 255, 0.68);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
        }
  
        .playground-project-overview-outcome-milestone-add:hover,
        .playground-project-overview-outcome-milestone-picker-shell.is-open .playground-project-overview-outcome-milestone-add {
          background: rgba(255, 255, 255, 0.08);
          color: #fff;
        }
  
        .playground-project-overview-outcome-milestone-picker-shell {
          position: relative;
          z-index: 80;
        }
  
        .playground-project-overview-outcome-milestone-menu {
          right: 0;
          width: 260px;
          min-width: 260px;
          max-height: 260px;
          overflow-y: auto;
          z-index: 90;
        }
  
        .playground-project-overview-outcome-milestone-row {
          cursor: default;
        }
  
        .playground-project-overview-outcome-milestone-row + .playground-project-overview-outcome-milestone-row {
          margin-top: 8px;
        }
  
        .playground-project-overview-outcome-milestone-row .playground-tasks-backlog-item-content {
          gap: 12px;
        }
  
        .playground-project-overview-outcome-milestone-row .playground-tasks-backlog-meta {
          margin-left: auto;
        }
  
        .playground-project-overview-outcome-milestone-remove {
          flex: 0 0 auto;
          width: 28px;
          height: 28px;
          padding: 0;
          border: 0;
          border-radius: 8px;
          background: transparent;
          color: rgba(255, 255, 255, 0.56);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
        }
  
        .playground-project-overview-outcome-milestone-remove:hover {
          background: rgba(255, 255, 255, 0.08);
          color: #fff;
        }
  
        .playground-mission-control-studio-body {
          background: transparent;
          gap: 0;
        }
  
        .playground-project-wallpaper-transition-layer {
          position: fixed;
          top: 0;
          right: 0;
          bottom: 0;
          left: 0;
          z-index: 0;
          pointer-events: none;
          overflow: hidden;
        }
  
        .playground-project-wallpaper-transition-image {
          position: absolute;
          inset: 0;
          background-size: cover;
          background-position: center;
          background-repeat: no-repeat;
          will-change: transform;
        }
  
        .playground-project-wallpaper-transition-layer.is-next .playground-project-wallpaper-transition-image.is-outgoing {
          animation: playgroundProjectWallpaperSlideOutLeft 360ms ease both;
        }
  
        .playground-project-wallpaper-transition-layer.is-next .playground-project-wallpaper-transition-image.is-incoming {
          animation: playgroundProjectWallpaperSlideInRight 360ms ease both;
        }
  
        .playground-project-wallpaper-transition-layer.is-prev .playground-project-wallpaper-transition-image.is-outgoing {
          animation: playgroundProjectWallpaperSlideOutRight 360ms ease both;
        }
  
        .playground-project-wallpaper-transition-layer.is-prev .playground-project-wallpaper-transition-image.is-incoming {
          animation: playgroundProjectWallpaperSlideInLeft 360ms ease both;
        }
  
        @keyframes playgroundProjectWallpaperSlideOutLeft {
          from { transform: translateX(0); }
          to { transform: translateX(-100%); }
        }
  
        @keyframes playgroundProjectWallpaperSlideInRight {
          from { transform: translateX(100%); }
          to { transform: translateX(0); }
        }
  
        @keyframes playgroundProjectWallpaperSlideOutRight {
          from { transform: translateX(0); }
          to { transform: translateX(100%); }
        }
  
        @keyframes playgroundProjectWallpaperSlideInLeft {
          from { transform: translateX(-100%); }
          to { transform: translateX(0); }
        }
  
        .playground-tasks-page.has-project-wallpaper-transition .playground-tasks-shell,
        .playground-tasks-page.has-project-wallpaper-transition .playground-environments-page {
          position: relative;
          z-index: 1;
        }
  
        .playground-tasks-project-wallpaper-toggle {
          margin-top: 14px;
        }
  
        .playground-tasks-project-modal-default-environment {
          margin-top: 18px;
        }
  
        .playground-tasks-project-modal-default-environment .playground-tasks-detail-fact-label {
          color: rgba(255, 255, 255, 0.92);
          font-size: 14px;
          line-height: 1.3;
        }
  
        .playground-tasks-project-modal-attachments {
          margin-top: 8px;
          padding-top: 2px;
        }
  
        .playground-tasks-project-modal-connectors {
          margin-top: 0;
          padding-top: 0;
        }
  
        .playground-tasks-project-modal-connectors .playground-tasks-project-modal-plugins-header {
          margin-top: 18px;
        }
  
        .playground-tasks-project-modal-connectors .playground-tasks-secondary-copy {
          max-width: 720px;
        }
  
        .playground-tasks-project-modal-connectors .playground-tasks-connectors-list {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 8px 24px;
          padding: 12px 0 0;
        }
  
        .playground-tasks-project-modal-connectors .playground-tasks-connector-row {
          min-width: 0;
        }
  
        .playground-tasks-project-modal-connectors .playground-tasks-connector-service-label {
          color: #ffffff;
        }
  
        .playground-tasks-project-modal-attachments .tb-popup-dropzone,
        .playground-tasks-project-modal-attachments .playground-tasks-attachments-empty-button {
          min-height: 128px;
        }
  
        .playground-tasks-project-modal-preview {
          margin-top: 12px;
        }
  
        .playground-tasks-project-wallpaper-toggle-title {
          font-size: 13px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.94);
        }
  
        .playground-tasks-project-wallpaper-grid {
          display: grid;
          grid-template-columns: repeat(4, minmax(0, 1fr));
          gap: 12px;
        }
  
        .playground-tasks-project-wallpaper-option {
          position: relative;
          width: 100%;
          height: 100px;
          padding: 0;
          overflow: hidden;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 14px;
          background: rgba(255, 255, 255, 0.04);
          cursor: pointer;
          transition: border-color 160ms ease, transform 160ms ease, box-shadow 160ms ease;
        }
  
        .playground-tasks-project-wallpaper-option:hover {
          border-color: rgba(255, 255, 255, 0.2);
          transform: translateY(-1px);
        }
  
        .playground-tasks-project-wallpaper-option.is-active {
          border-color: rgba(255, 255, 255, 0.92);
          box-shadow: 0 0 0 1px rgba(255, 255, 255, 0.08);
        }
  
        .playground-tasks-project-wallpaper-option-image {
          width: 100%;
          height: 100%;
          display: block;
          object-fit: cover;
        }
  
        .playground-tasks-project-wallpaper-option-overlay {
          position: absolute;
          inset: 0;
          display: flex;
          align-items: center;
          justify-content: center;
          color: white;
          background: rgba(0, 0, 0, 0.26);
        }
  
        .playground-tasks-project-wallpaper-option-label {
          position: absolute;
          left: 0;
          right: 0;
          bottom: 0;
          padding: 16px 10px 8px;
          background: linear-gradient(180deg, transparent, rgba(0, 0, 0, 0.78));
          font-size: 11px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.96);
          text-align: left;
        }
  
        .playground-tasks-project-modal-actions {
          display: flex;
          align-items: center;
          justify-content: flex-end;
          gap: 10px;
          margin-top: 16px;
        }
  
        .playground-tasks-project-modal-actions .playground-environments-action-button {
          height: 36px;
          min-height: 36px;
          padding: 0 18px;
          border-radius: 999px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          line-height: 1;
          text-align: center;
        }
  
        .playground-tasks-project-modal-actions .playground-environments-action-button > span {
          line-height: 1;
        }
  
        .playground-tasks-project-modal-actions .playground-environments-action-button.is-primary:not(:disabled),
        .playground-tasks-project-modal-actions .playground-environments-action-button.is-primary:not(:disabled):hover {
          border: 0;
          background: #ffffff;
          color: #111111;
        }
  
        .playground-tasks-project-modal-error {
          margin-top: 12px;
          font-size: 12px;
          line-height: 1.45;
          color: #ffb0b0;
        }
  
        .playground-environment-composer-modal {
          width: min(540px, 100%);
        }
  
        .playground-environment-composer-modal .playground-tasks-project-modal-name-row {
          padding-bottom: 0;
        }
  
        .playground-environment-composer-modal-body {
          display: flex;
          flex-direction: column;
          gap: 16px;
        }
  
        .playground-environment-composer-modal .playground-environments-editor-surface {
          background: transparent;
          border: 0;
          border-radius: 0;
          padding: 0;
          box-shadow: none;
        }
  
        .playground-environment-composer-modal .playground-environments-editor-surface .playground-tasks-detail-facts {
          padding: 0;
          border: 0;
          background: transparent;
        }
  
        .playground-environment-composer-modal .playground-environments-editor-surface .playground-tasks-detail-facts::before {
          content: none;
          display: none;
        }
  
        .playground-environment-composer-modal .playground-environments-editor-surface .playground-tasks-detail-facts-body {
          gap: 12px;
        }
  
        .playground-environment-composer-modal .playground-environments-editor-surface .playground-tasks-detail-fact {
          min-height: 24px;
        }
  
        .playground-environment-composer-modal .playground-environments-editor-surface .playground-environments-input.playground-tasks-detail-fact-select {
          height: auto;
          min-height: 0;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          box-shadow: none;
        }
  
        .playground-environment-composer-modal .playground-tasks-detail-description-editor {
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
          margin-bottom: 10px;
        }
  
        .playground-environment-composer-toggle-row {
          display: grid;
          grid-template-columns: minmax(0, 1fr) auto;
          align-items: center;
          gap: 16px;
        }
  
        .playground-environment-composer-toggle-help {
          min-width: 0;
          font-size: 12px;
          font-weight: 400;
          line-height: 1.4;
          color: rgba(255, 255, 255, 0.56);
        }
  
        .playground-environment-profile-section {
          display: flex;
          flex-direction: column;
          gap: 10px;
          padding-bottom: 14px;
          margin-bottom: 4px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-environment-profile-selector {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 10px;
        }
  
        .playground-environment-profile-selector.is-compact {
          gap: 8px;
        }
  
        .playground-environment-profile-card {
          display: flex;
          flex-direction: column;
          align-items: flex-start;
          gap: 8px;
          width: 100%;
          padding: 13px 14px;
          border-radius: 16px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.03);
          color: rgba(255, 255, 255, 0.88);
          text-align: left;
          cursor: pointer;
          transition: border-color 160ms ease, background-color 160ms ease, color 160ms ease;
        }
  
        .playground-environment-profile-card:hover {
          border-color: rgba(255, 255, 255, 0.16);
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-environment-profile-card.is-selected {
          border-color: rgba(57, 136, 255, 0.44);
          background: rgba(57, 136, 255, 0.1);
        }
  
        .playground-environment-profile-card:disabled {
          opacity: 0.5;
          cursor: default;
        }
  
        .playground-environment-profile-selector.is-compact .playground-environment-profile-card {
          padding: 11px 12px;
          gap: 6px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 10px;
        }
  
        .playground-environment-profile-selector.is-compact .playground-environment-profile-card.is-selected {
          border-color: rgb(143, 196, 255);
        }
  
        .playground-environment-profile-card-top {
          width: 100%;
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 8px;
        }
  
        .playground-environment-profile-card-label {
          font-size: 13px;
          font-weight: 600;
          line-height: 1.2;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-environment-profile-card-rate {
          font-size: 11px;
          line-height: 1.2;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.62);
          white-space: nowrap;
        }
  
        .playground-environment-profile-card-meta,
        .playground-environment-profile-card-footer {
          font-size: 11px;
          line-height: 1.35;
          color: rgba(255, 255, 255, 0.58);
        }
  
        .playground-environment-profile-card-copy {
          font-size: 11px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.7);
        }
  
        .playground-environment-profile-note {
          font-size: 11px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.52);
        }
  
        .playground-environment-composer-runtime-facts {
          display: flex;
          flex-direction: column;
          gap: 4px;
        }
  
        .playground-environment-composer-runtime-facts .playground-tasks-detail-fact {
          min-height: 32px;
        }
  
        .playground-agent-composer-modal {
          width: min(750px, 100%);
          max-height: min(80vh, 820px);
          overflow: visible;
          display: flex;
          flex-direction: column;
        }
  
        .playground-agent-composer-modal-body {
          display: flex;
          flex-direction: column;
          gap: 16px;
          flex: 1 1 auto;
          min-height: 0;
          max-height: calc(min(80vh, 820px) - 152px);
          overflow-y: auto;
          overflow-x: visible;
          padding-right: 2px;
          position: relative;
          z-index: 2;
        }
  
        .playground-settings-topup-modal-backdrop {
          z-index: 320;
        }
  
        .playground-settings-topup-modal {
          width: min(560px, 100%);
        }
  
        .playground-settings-topup-modal .playground-tasks-project-modal-name-row {
          align-items: flex-start;
          gap: 12px;
        }
  
        .playground-settings-topup-modal-title-shell {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 4px;
          padding-top: 2px;
        }
  
        .playground-settings-topup-modal-title {
          font-size: 17px;
          line-height: 1.2;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-settings-topup-modal-subtitle {
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.58);
        }
  
        .playground-settings-topup-modal-body {
          gap: 14px;
        }
  
        .playground-settings-topup-note {
          padding: 12px 14px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 14px;
          background: rgba(255, 255, 255, 0.04);
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.68);
        }
  
        .playground-settings-topup-package-grid {
          display: grid;
          grid-template-columns: repeat(auto-fit, minmax(148px, 1fr));
          gap: 10px;
        }
  
        .playground-settings-topup-package-card {
          display: flex;
          flex-direction: column;
          align-items: flex-start;
          gap: 8px;
          width: 100%;
          min-height: 132px;
          padding: 14px;
          border-radius: 16px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.03);
          color: rgba(255, 255, 255, 0.86);
          text-align: left;
          cursor: pointer;
          transition: border-color 160ms ease, background-color 160ms ease, transform 160ms ease, color 160ms ease;
        }
  
        .playground-settings-topup-package-card:hover {
          border-color: rgba(255, 255, 255, 0.16);
          background: rgba(255, 255, 255, 0.05);
          transform: translateY(-1px);
        }
  
        .playground-settings-topup-package-card.is-selected {
          border-color: rgba(255, 255, 255, 0.22);
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-settings-topup-package-card-name {
          font-size: 12px;
          line-height: 1.2;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.72);
        }
  
        .playground-settings-topup-package-card.is-selected .playground-settings-topup-package-card-name {
          color: rgba(255, 255, 255, 0.82);
        }
  
        .playground-settings-topup-package-card-tokens {
          font-size: 16px;
          line-height: 1.2;
          font-weight: 600;
          color: inherit;
        }
  
        .playground-settings-topup-package-card-price {
          font-size: 12px;
          line-height: 1.3;
          color: rgba(255, 255, 255, 0.62);
        }
  
        .playground-settings-topup-package-card-copy {
          margin-top: auto;
          font-size: 11px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.52);
        }
  
        .playground-settings-topup-package-card.is-selected .playground-settings-topup-package-card-price,
        .playground-settings-topup-package-card.is-selected .playground-settings-topup-package-card-copy {
          color: rgba(255, 255, 255, 0.66);
        }
  
        .playground-settings-topup-summary {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 16px;
          padding: 14px;
          border-radius: 16px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.04);
        }
  
        .playground-settings-topup-summary-copy {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 4px;
        }
  
        .playground-settings-topup-summary-label {
          font-size: 12px;
          line-height: 1.2;
          color: rgba(255, 255, 255, 0.56);
        }
  
        .playground-settings-topup-summary-value {
          font-size: 17px;
          line-height: 1.2;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-settings-topup-summary-description {
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.56);
        }
  
        .playground-settings-topup-summary-badge {
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          gap: 8px;
          min-height: 34px;
          padding: 0 12px;
          border-radius: 999px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          background: rgba(255, 255, 255, 0.04);
          font-size: 12px;
          line-height: 1;
          color: rgba(255, 255, 255, 0.68);
          white-space: nowrap;
        }
  
        .playground-settings-trigger-composer-modal {
          width: min(560px, 100%);
        }
  
        .playground-settings-trigger-composer-modal .playground-agent-composer-modal-body {
          gap: 18px;
          padding-top: 4px;
        }
  
        .playground-settings-trigger-composer-modal .playground-tasks-project-modal-field {
          gap: 12px;
        }
  
        .playground-settings-trigger-composer-modal .playground-settings-toggle-group {
          gap: 12px;
        }
  
        .playground-settings-trigger-composer-modal .playground-settings-toggle-pill {
          min-height: 36px;
        }
  
        .playground-settings-trigger-composer-modal .playground-settings-trigger-composer-textarea {
          min-height: 110px;
        }
  
        .playground-settings-trigger-connect-card {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 16px;
        }
  
        .playground-skill-composer-modal {
          width: min(560px, 100%);
        }
  
        .playground-skill-composer-modal-body {
          display: flex;
          flex-direction: column;
          gap: 16px;
        }
  
        .playground-tasks-release-modal {
          width: min(560px, 100%);
          padding: 12px 16px 16px;
          border-radius: 15px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          background: #323232;
          box-shadow: 0 12px 30px rgba(0, 0, 0, 0.35);
          backdrop-filter: blur(10px);
        }
  
        .playground-tasks-project-modal.playground-tasks-release-modal {
          height: 500px;
          overflow: visible;
        }
  
        .playground-tasks-project-modal-backdrop.playground-tasks-release-modal-backdrop {
          z-index: 10040;
        }
  
        .playground-mission-control-modal.playground-tasks-release-modal .playground-tasks-project-modal-top {
          align-items: center;
          justify-content: space-between;
          gap: 14px;
          margin-bottom: 14px;
          padding-bottom: 12px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-tasks-release-modal .playground-tasks-project-modal-icon-trigger,
        .playground-tasks-release-modal .playground-tasks-project-modal-icon-trigger:hover,
        .playground-tasks-release-modal .playground-tasks-project-modal-icon-trigger.is-active {
          background: transparent !important;
          box-shadow: none !important;
        }
  
        .playground-tasks-release-modal-title-input {
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-tasks-release-modal-body,
        .playground-tasks-release-modal-context {
          overflow: visible;
        }
  
        .playground-tasks-release-modal-top {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 16px;
          margin-bottom: 14px;
        }
  
        .playground-tasks-release-modal-name-row {
          position: relative;
          flex: 1;
          min-width: 0;
          display: flex;
          align-items: center;
          gap: 14px;
          padding-bottom: 4px;
        }
  
        .playground-tasks-release-modal-field {
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-tasks-release-modal-field .playground-tasks-detail-section-title {
          margin-bottom: 0;
        }
  
        .playground-tasks-release-modal-date-row {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 12px;
          margin-top: 0;
        }
  
        .playground-tasks-release-modal-input {
          width: 100%;
          height: 42px;
          padding: 0 14px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.05);
          color: rgba(255, 255, 255, 0.94);
          font: inherit;
          font-size: 12px;
          color-scheme: dark;
          outline: none;
        }
  
        .playground-tasks-release-modal-input::placeholder {
          color: rgba(255, 255, 255, 0.42);
        }
  
        .playground-tasks-release-modal-input[type="date"]::-webkit-calendar-picker-indicator {
          filter: invert(1) brightness(1.08);
          opacity: 0.92;
          cursor: pointer;
        }
  
        .playground-tasks-release-modal-textarea {
          min-height: 110px;
        }
  
        .playground-tasks-release-modal-description {
          margin-top: 0;
          padding-top: 0;
        }
  
        .playground-tasks-release-modal .playground-tasks-project-modal-actions {
          flex: 0 0 auto;
          margin-top: 0;
        }
  
        .playground-tasks-project-grid {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 14px;
        }
  
        .playground-tasks-project-card {
          display: flex;
          flex-direction: column;
          min-width: 0;
          overflow: hidden;
          cursor: pointer;
          border: 0;
          border-radius: 22px;
          background-position: center;
          background-size: cover;
          background-repeat: no-repeat;
        }
  
        .playground-tasks-project-card-hero {
          padding: 18px 18px 16px;
          min-height: 156px;
          display: flex;
          flex-direction: column;
          justify-content: flex-end;
          gap: 0;
        }
  
        .playground-tasks-project-card-top {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 12px;
          margin-bottom: 12px;
        }
  
        .playground-tasks-project-card-actions {
          flex: 0 0 auto;
          align-self: flex-start;
          z-index: 2;
        }
  
        .playground-tasks-project-card-icon {
          position: relative;
          width: 42px;
          height: 42px;
          border-radius: 50%;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.92);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          -webkit-backdrop-filter: blur(20px);
          backdrop-filter: blur(20px);
        }
  
        .playground-tasks-project-card-icon::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          border-radius: 50%;
          padding: 1px;
          background: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-tasks-project-card-kicker {
          font-size: 12px;
          font-weight: 400;
          letter-spacing: 0.04em;
          color: rgba(255, 255, 255, 0.56);
          margin-bottom: 12px;
        }
  
        .playground-tasks-project-card-title {
          font-size: 22px;
          line-height: 1.1;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-tasks-project-card-copy {
          font-size: 13px;
          line-height: 1.55;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.7);
          max-width: 520px;
          margin-top: 10px;
          display: -webkit-box;
          -webkit-box-orient: vertical;
          -webkit-line-clamp: 3;
          overflow: hidden;
        }
  
        .playground-tasks-project-card-copy .tb-message-markdown-paragraph,
        .playground-tasks-project-card-copy .tb-message-markdown-list,
        .playground-tasks-project-card-copy .tb-message-markdown-heading {
          margin: 0;
          font-size: inherit;
          line-height: inherit;
          color: inherit;
        }
  
        .playground-tasks-project-card-copy .tb-message-markdown-strong,
        .playground-tasks-project-card-copy .tb-message-markdown-em,
        .playground-tasks-project-card-copy .tb-message-markdown-inline-code,
        .playground-tasks-project-card-copy .tb-message-markdown-link {
          color: inherit;
        }
  
        .playground-tasks-project-card-body {
          padding: 16px 18px 18px;
          display: flex;
          flex-direction: column;
          gap: 0;
        }
  
        .playground-tasks-project-card-metrics {
          display: flex;
          align-items: center;
          gap: 8px;
          flex-wrap: wrap;
        }
  
        .playground-tasks-project-card .playground-tasks-chip {
          border: 0;
          background: rgba(0, 0, 0, 0.22);
          color: rgba(255, 255, 255, 0.82);
          font-weight: 400;
          -webkit-backdrop-filter: blur(16px);
          backdrop-filter: blur(16px);
        }
  
        .playground-tasks-project-card-footer {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          flex-wrap: wrap;
        }
  
        .playground-tasks-project-card-updated {
          font-size: 11px;
          color: rgba(255, 255, 255, 0.48);
        }
  
        .playground-tasks-project-context {
          display: flex;
          flex-direction: column;
          gap: 14px;
        }
  
        .playground-tasks-project-meta-row {
          display: flex;
          align-items: center;
          gap: 8px;
          flex-wrap: wrap;
        }
  
        .playground-tasks-project-panel-grid {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 12px;
        }
  
        .playground-tasks-project-panel {
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 18px;
          background: rgba(255, 255, 255, 0.03);
          padding: 16px;
          display: flex;
          flex-direction: column;
          gap: 12px;
          min-width: 0;
        }
  
  ${PROJECTS_STYLE_FRAGMENTS.overview}
  ${ENVIRONMENT_CHANGES_CSS}
  ${IMAGINE_TEMPLATE_PAGE_CSS}
  ${IMAGINE_PAGE_CSS}
  ${METRONOME_PAGE_CSS}
  
        .playground-agents-page .playground-environments-home-summary,
        .playground-agents-overview-page .playground-environments-home-summary,
        .playground-resources-page .playground-environments-home-summary {
          margin-bottom: 18px;
        }
  
        .playground-agents-page .playground-environments-home-metrics,
        .playground-agents-overview-page .playground-environments-home-metrics,
        .playground-resources-page .playground-environments-home-metrics,
        .playground-settings-page .playground-environments-home-metrics {
          margin-top: 0;
          margin-bottom: 0;
          grid-template-columns: minmax(0, 1fr);
        }
  
        .playground-agents-page .playground-environments-home-metrics > .playground-tasks-detail-facts,
        .playground-agents-overview-page .playground-environments-home-metrics > .playground-tasks-detail-facts,
        .playground-resources-page .playground-environments-home-metrics > .playground-tasks-detail-facts,
        .playground-settings-page .playground-environments-home-metrics > .playground-tasks-detail-facts {
          grid-column: 1 / -1;
          width: 100%;
          min-width: 0;
        }
  
        .playground-agents-page .playground-project-overview-summary-kpi,
        .playground-agents-overview-page .playground-project-overview-summary-kpi,
        .playground-resources-page .playground-project-overview-summary-kpi,
        .playground-settings-page .playground-project-overview-summary-kpi {
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          -webkit-backdrop-filter: none;
          backdrop-filter: none;
        }
  
        .playground-agents-page .playground-plugin-row-logo-shell,
        .playground-agents-overview-page .playground-plugin-row-logo-shell {
          border-radius: 5px;
          background: transparent;
        }
  
        .playground-agents-page .playground-plugin-row-logo-shell::before,
        .playground-agents-overview-page .playground-plugin-row-logo-shell::before {
          content: none;
        }
  
        .playground-agents-overview-page .playground-agents-overview-search-row {
          border-bottom: 0;
        }
  
        .playground-agents-page .playground-agents-overview-table-shell,
        .playground-agents-overview-page .playground-agents-overview-table-shell,
        .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview .playground-resources-overview-table-shell {
          padding: 0;
          border-radius: 0;
        }
  
        .playground-agents-page .playground-agents-overview-table-shell::before,
        .playground-agents-overview-page .playground-agents-overview-table-shell::before,
        .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview .playground-resources-overview-table-shell::before {
          content: none;
          display: none;
        }
  
        .playground-agents-overview-table-controls {
          min-width: 0;
          flex: 1 1 auto;
          display: flex;
          align-items: center;
          gap: 10px;
        }
  
        .playground-agents-overview-tabs {
          margin: 16px 0 18px;
          padding-bottom: 0;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
          display: flex;
          align-items: center;
          justify-content: flex-start;
        }
  
        .playground-agents-overview-page .playground-environments-home-content {
          width: min(100%, var(--playground-thread-content-max-width));
          max-width: var(--playground-thread-content-max-width);
        }
  
        .playground-team-overview-page.playground-agents-overview-page .playground-environments-home-content {
          width: min(100%, var(--playground-centered-page-max-width));
          max-width: var(--playground-centered-page-max-width);
        }
  
        .playground-agents-configure-hero .playground-develop-server-kind-header {
          margin-bottom: 12px;
        }
  
        .playground-project-overview-summary-title.playground-develop-title {
          font-size: 16px !important;
        }
  
        .playground-agents-overview-page.is-develop-configure-page .playground-agents-configure-hero.playground-environments-home-hero {
          padding-bottom: 0;
        }
  
        .playground-agents-overview-page.is-develop-configure-page .playground-develop-server-kind-metrics.playground-environments-home-metrics {
          margin-bottom: 0;
          overflow: hidden;
          position: relative;
          z-index: 0;
        }
  
        .playground-team-overview-page.is-develop-configure-page .playground-team-overview-empty-metrics.playground-environments-home-metrics {
          height: 364px;
          min-height: 364px;
          max-height: 364px;
          margin-top: 12px;
          margin-bottom: 0;
          display: block;
          overflow: hidden;
          position: relative;
          z-index: 0;
        }
  
  ${ORGANIZATIONS_STYLE_FRAGMENTS.overview}
        .playground-agents-overview-analytics-card.playground-project-overview-progress-combo-card {
          margin: 0;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          box-shadow: none;
          overflow: visible;
          -webkit-backdrop-filter: none;
          backdrop-filter: none;
        }
  
        .playground-agents-overview-analytics-card.playground-project-overview-progress-combo-card::before {
          content: none;
          display: none;
        }
  
        .playground-agents-overview-analytics-card .playground-project-overview-progress-combo-metric-dot.is-agents {
          background: rgb(143, 196, 255);
        }
  
        .playground-agents-overview-analytics-card .playground-project-overview-progress-combo-metric-dot.is-teams {
          background: rgb(103, 80, 255);
        }
  
        .playground-agents-overview-analytics-card .playground-project-overview-progress-combo-metric-dot.is-agent-ct {
          background: #7effff;
        }
  
        .playground-agents-overview-analytics-card .playground-project-overview-progress-combo-metric-dot.is-team-ct {
          background: #c5a3ff;
        }
  
        .playground-agents-overview-analytics-card .playground-project-overview-progress-combo-metric-dot.is-total-ct {
          background: #9ff6ce;
        }
  
        .playground-resource-type-overview-analytics-card .playground-resource-type-overview-chart-card.playground-settings-usage-chart-card {
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          box-shadow: none;
        }
  
        .playground-resource-type-overview-analytics-card .playground-resource-type-overview-chart-card .playground-project-overview-chart-shell {
          height: 270px;
          border: 0;
          background: transparent;
        }
  
        .playground-resource-type-overview-page .playground-develop-server-kind-header {
          margin-bottom: 12px;
        }
  
        .playground-resource-type-overview-page .playground-resource-type-overview-metrics {
          z-index: 0;
        }
  
        .playground-develop-home-overview-analytics-card .playground-project-overview-progress-combo-chart {
          min-height: 288px;
        }
  
        .playground-develop-home-overview-analytics-card .playground-settings-usage-chart-frame,
        .playground-develop-home-overview-analytics-card .playground-settings-usage-chart-loading-frame,
        .playground-develop-home-overview-analytics-card .playground-settings-usage-chart-empty {
          min-height: 288px;
        }
  
        .playground-agents-overview-list-section.playground-project-overview-panel-plain.playground-plugins-section {
          margin-top: 0 !important;
          gap: 0 !important;
          position: relative;
          z-index: 2;
          overflow: visible;
        }
  
        .playground-agents-overview-list-section .playground-plugins-section-header {
          margin-top: 0;
        }
  
        .playground-agents-overview-list-section .playground-plugins-search-row {
          margin-bottom: 12px;
          position: relative;
          z-index: 120;
          align-items: center;
        }
  
        .playground-agents-overview-list-section .playground-project-overview-threads-table {
          position: relative;
          z-index: 0;
        }
  
        .playground-agents-overview-list-section .playground-plugins-search-row .playground-tasks-toolbar-popup-shell {
          z-index: 130;
        }
  
        .playground-agents-overview-list-section .playground-plugins-search-row .playground-tasks-toolbar-popup-menu {
          z-index: 131;
        }
  
        .playground-agents-overview-list-section,
        .playground-agents-overview-list-section .playground-project-overview-threads-table,
        .playground-agents-overview-list-section .playground-project-overview-thread-list,
        .playground-agents-overview-list-section .playground-project-overview-threads-table-row,
        .playground-agents-overview-list-section .playground-project-overview-thread-cell.is-actions {
          overflow: visible !important;
        }
  
        .playground-agents-overview-list-section .playground-project-overview-threads-table-header,
        .playground-agents-overview-list-section .playground-project-overview-threads-table-row {
          grid-template-columns: 21px minmax(220px, 1.35fr) minmax(220px, 1fr) minmax(180px, 0.82fr) minmax(100px, 0.42fr) 28px;
          gap: 12px;
          width: 100%;
          max-width: 100%;
          box-sizing: border-box;
        }
  
        .playground-agents-overview-list-section .playground-project-overview-threads-table-row {
          min-height: 58px;
          padding-top: 12px;
          padding-bottom: 12px;
        }
  
        .playground-agents-overview-page .playground-agents-overview-list-section.playground-agents-detail-threads-section.playground-evaluations-runs-section .playground-project-overview-threads-table-header,
        .playground-agents-overview-page .playground-agents-overview-list-section.playground-agents-detail-threads-section.playground-evaluations-runs-section .playground-project-overview-threads-table-row {
          grid-template-columns: 21px minmax(240px, 1.05fr) minmax(230px, 0.9fr) minmax(190px, 0.72fr) minmax(100px, 0.38fr) 28px !important;
          gap: 12px;
          padding-right: 0;
        }
  
        .playground-agents-overview-list-section .playground-project-overview-threads-table-header,
        .playground-agents-overview-list-section .playground-project-overview-threads-table-row,
        .playground-agents-overview-list-section .playground-project-overview-threads-table-header *,
        .playground-agents-overview-list-section .playground-project-overview-threads-table-row * {
          font-size: 12px;
        }
  
        .playground-project-overview-threads-table-header {
          font-weight: 400;
        }
  
        .playground-agents-overview-sortable-header {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          gap: 4px;
        }
  
        .playground-agents-overview-sortable-header.is-active {
          color: #fff;
        }
  
        .playground-agents-overview-sortable-header-label {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-agents-overview-column-sort-button {
          width: 18px;
          height: 18px;
          min-width: 18px;
          min-height: 18px;
          padding: 0;
          border: 0;
          border-radius: 6px;
          background: transparent;
          color: rgba(255, 255, 255, 0.3);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          flex: 0 0 auto;
        }
  
        .playground-agents-overview-column-sort-button:hover,
        .playground-agents-overview-column-sort-button:focus-visible {
          outline: none;
        }
  
        .playground-agents-overview-sort-icon {
          position: relative;
          display: inline-block;
          width: 14px;
          height: 14px;
          color: rgba(255, 255, 255, 0.3);
        }
  
        .playground-agents-overview-sort-icon-layer {
          position: absolute;
          inset: 0;
          width: 14px;
          height: 14px;
          color: rgba(255, 255, 255, 0.3);
        }
  
        .playground-agents-overview-sort-icon-layer.is-top {
          clip-path: inset(0 0 50% 0);
        }
  
        .playground-agents-overview-sort-icon-layer.is-bottom {
          clip-path: inset(50% 0 0 0);
        }
  
        .playground-agents-overview-sort-icon.is-ascending .playground-agents-overview-sort-icon-layer.is-bottom,
        .playground-agents-overview-sort-icon.is-descending .playground-agents-overview-sort-icon-layer.is-top {
          color: #fff;
        }
  
        .playground-evaluations-runs-section .playground-agents-overview-select-checkbox {
          border-color: rgba(255, 255, 255, 0.2);
        }
  
        .playground-agents-overview-list-section .playground-project-overview-thread-list > .playground-project-overview-threads-table-row:last-child,
        .playground-computers-overview-list-section .playground-project-overview-thread-list > .playground-project-overview-threads-table-row:last-child {
          border-bottom: 0;
        }
  
        .playground-agents-overview-list-section .playground-project-overview-thread-cell,
        .playground-agents-overview-list-section .playground-plugin-row-title,
        .playground-agents-overview-list-section .playground-agents-overview-model-name,
        .playground-agents-overview-list-section .playground-guardrails-creator-label,
        .playground-agents-overview-list-section .playground-agents-overview-table-value {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-agents-overview-list-section .playground-project-overview-thread-cell.is-name {
          color: rgba(255, 255, 255, 0.9);
        }
  
        .playground-agents-overview-list-section .playground-project-overview-threads-table-header > div:first-child,
        .playground-agents-overview-list-section .playground-project-overview-thread-cell.is-select {
          display: flex;
          align-items: center;
          justify-content: center;
        }
  
  	      .playground-agents-overview-select-checkbox {
  	        position: relative;
  	        width: 14px;
  	        height: 14px;
  	        margin-left: 5px;
  	        padding: 0;
  	        border: 1px solid rgba(255, 255, 255, 0.2);
  	        border-radius: 5px;
  	        background: transparent;
          appearance: none;
          -webkit-appearance: none;
          cursor: pointer;
          flex: 0 0 auto;
          transition: border-color 160ms ease, background 160ms ease;
        }
  
        .playground-agents-overview-select-checkbox::after {
          content: "";
          position: absolute;
          inset: 3px;
          border-radius: 2px;
          background: rgba(255, 255, 255, 0.92);
          opacity: 0;
          transform: scale(0.6);
          transition: opacity 160ms ease, transform 160ms ease;
        }
  
        .playground-agents-overview-select-checkbox:hover,
        .playground-agents-overview-select-checkbox.is-selected {
          border-color: rgba(255, 255, 255, 0.35);
          background: rgba(255, 255, 255, 0.1);
        }
  
        .playground-agents-overview-select-checkbox.is-selected::after {
          opacity: 1;
          transform: scale(1);
        }
  
        .playground-agents-overview-select-checkbox.is-partial::after {
          inset: 5px 3px;
          border-radius: 999px;
          opacity: 1;
          transform: scale(1);
        }
  
        .playground-agents-overview-list-section .playground-project-overview-thread-cell.is-actions,
        .playground-agents-overview-list-section .playground-project-overview-threads-table-header > div:last-child {
          justify-self: end;
        }
  
        .playground-agents-overview-list-section .playground-project-overview-thread-cell.is-actions {
          width: 100%;
          display: flex;
          align-items: center;
          justify-content: flex-end;
        }
  
        .playground-agents-overview-list-section .playground-agents-overview-name-cell {
          gap: 8px;
        }
  
        .playground-agents-overview-list-section .playground-agents-overview-table-avatar {
          width: 20px;
          height: 20px;
          border-radius: 50%;
        }
  
        .playground-agents-overview-list-section .playground-agents-overview-table-avatar-fallback {
          font-size: 10px;
        }
  
        .playground-agents-overview-list-section .playground-guardrails-creator-avatar {
          width: 20px;
          height: 20px;
        }
  
        .playground-agents-overview-list-section .playground-guardrails-creator-avatar.is-system img {
          width: 14px;
          height: 14px;
        }
  
        .playground-project-overview-threads-section.playground-agents-overview-list-section .playground-project-overview-threads-table-header,
        .playground-project-overview-threads-section.playground-agents-overview-list-section .playground-project-overview-threads-table-row {
          grid-template-columns: 21px minmax(0, 1.3fr) minmax(0, 0.95fr) minmax(0, 0.95fr) minmax(0, 0.9fr) minmax(86px, 0.62fr) 28px;
          gap: 12px;
          padding-right: 0;
        }
  
        .playground-project-overview-threads-section.playground-agents-overview-table-section {
          position: relative;
          isolation: isolate;
          overflow: visible !important;
        }
  
        .playground-project-overview-threads-section.playground-agents-overview-table-section::before {
          content: none;
        }
  
        .playground-project-overview-threads-section .playground-thread-overview-list-table {
          position: relative;
          isolation: isolate;
          width: 100%;
          margin: 0 0 24px;
          padding: 0 18px 6px;
          background: rgba(255, 255, 255, 0.075);
          border: 1px solid rgba(255, 255, 255, 0.075);
          border-radius: 15px;
          overflow: visible !important;
          box-sizing: border-box;
        }
  
        .playground-project-overview-threads-section .playground-project-overview-threads-sticky-table-header {
          position: relative;
          z-index: 240;
          margin: 0 -18px;
          padding: 12px 18px 0;
          border-radius: 15px 15px 0 0;
          background: #121212;
          -webkit-backdrop-filter: none;
          backdrop-filter: none;
        }
  
        .playground-project-overview-threads-section .playground-project-overview-threads-toolbar {
          display: flex;
          flex-direction: row;
          align-items: center;
          justify-content: flex-start;
          flex-wrap: nowrap;
          gap: 10px;
          width: 100%;
          margin: 0;
          padding: 0 0 12px;
          border-bottom: 0;
        }
  
        .playground-project-overview-threads-section .playground-project-overview-threads-toolbar .playground-plugins-search-shell {
          flex: 0 1 340px;
          width: min(340px, 100%);
          min-width: min(220px, 100%);
          max-width: 340px;
          background: rgba(255, 255, 255, 0.025) !important;
        }
  
        .playground-project-overview-threads-section .playground-project-overview-threads-toolbar .playground-plugins-toolbar-controls {
          flex: 0 0 auto;
          flex-wrap: nowrap;
        }
  
        .playground-project-overview-threads-section .playground-project-overview-threads-toolbar .playground-project-overview-toolbar-action {
          flex: 0 0 auto;
          margin-left: auto;
        }
  
        .playground-project-overview-threads-section .playground-thread-overview-list-table .playground-project-overview-thread-list {
          width: calc(100% + 24px);
          margin-left: -12px;
          padding: 0 12px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 10px;
          background: #000;
          overflow: visible !important;
          box-sizing: border-box;
        }
  
        .playground-project-overview-threads-section .playground-thread-overview-list-table .playground-project-overview-threads-table-header {
          padding-top: 12px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-project-overview-threads-section.playground-agents-overview-list-section .playground-project-overview-threads-table-row.is-selected {
          background: rgba(255, 255, 255, 0.025);
        }
  
        .playground-resources-page.is-develop-configure-page.is-computers-view .playground-environments-home-content {
          width: min(100%, var(--playground-centered-page-max-width));
          max-width: var(--playground-centered-page-max-width);
        }
  
        .playground-resources-page.is-develop-configure-page.is-computers-view .playground-develop-server-kind-header {
          margin-bottom: 12px;
        }
  
        .playground-computers-overview-analytics-card.playground-project-overview-progress-combo-card {
          margin: 0;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          box-shadow: none;
          overflow: visible;
          -webkit-backdrop-filter: none;
          backdrop-filter: none;
        }
  
        .playground-computers-overview-analytics-card.playground-project-overview-progress-combo-card::before {
          content: none;
          display: none;
        }
  
        .playground-computers-overview-analytics-card .playground-project-overview-progress-combo-metric-dot.is-computers {
          background: #7effff;
        }
  
        .playground-computers-overview-analytics-card .playground-project-overview-progress-combo-metric-dot.is-resources {
          background: #c5a3ff;
        }
  
        .playground-computers-overview-analytics-card .playground-project-overview-progress-combo-metric-dot.is-computer-ct {
          background: #8fc4ff;
        }
  
        .playground-computers-overview-analytics-card .playground-project-overview-progress-combo-metric-dot.is-resource-ct {
          background: #6750ff;
        }
  
        .playground-computers-overview-analytics-card .playground-project-overview-progress-combo-metric-dot.is-total-ct {
          background: #9ff6ce;
        }
  
        .playground-computers-overview-analytics-card .playground-computers-overview-chart-card.playground-settings-usage-chart-card {
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          box-shadow: none;
        }
  
        .playground-computers-overview-analytics-card .playground-computers-overview-chart-card .playground-project-overview-chart-shell {
          height: 270px;
          border: 0;
          background: transparent;
        }
  
        .playground-resources-page.is-develop-configure-page.is-computers-view .playground-computers-overview-metrics {
          z-index: 0;
        }
  
        .playground-computers-overview-list-section.playground-project-overview-panel-plain.playground-plugins-section {
          margin-top: 0 !important;
          gap: 0 !important;
          overflow: visible;
        }
  
        .playground-computers-overview-list-section,
        .playground-computers-overview-list-section .playground-project-overview-threads-table,
        .playground-computers-overview-list-section .playground-project-overview-thread-list,
        .playground-computers-overview-list-section .playground-project-overview-threads-table-row,
        .playground-computers-overview-list-section .playground-project-overview-thread-cell.is-actions {
          overflow: visible !important;
        }
  
        .playground-computers-overview-list-section .playground-project-overview-threads-table {
          position: relative;
          z-index: 0;
        }
  
        .playground-resources-page.is-develop-configure-page .playground-computers-overview-list-section.playground-agents-detail-threads-section.playground-evaluations-runs-section .playground-project-overview-threads-table-header,
        .playground-resources-page.is-develop-configure-page .playground-computers-overview-list-section.playground-agents-detail-threads-section.playground-evaluations-runs-section .playground-project-overview-threads-table-row {
          grid-template-columns: 21px minmax(180px, 1fr) minmax(150px, 0.68fr) minmax(92px, 0.4fr) minmax(92px, 0.4fr) 28px !important;
          gap: 12px;
          width: 100%;
          max-width: 100%;
          box-sizing: border-box;
          padding-right: 0;
        }
  
        .playground-computers-overview-list-section .playground-project-overview-threads-table-header,
        .playground-computers-overview-list-section .playground-project-overview-threads-table-row,
        .playground-computers-overview-list-section .playground-project-overview-threads-table-header *,
        .playground-computers-overview-list-section .playground-project-overview-threads-table-row * {
          font-size: 12px;
        }
  
        .playground-computers-overview-list-section .playground-project-overview-thread-cell,
        .playground-computers-overview-list-section .playground-resources-overview-name-title,
        .playground-computers-overview-list-section .playground-agents-overview-table-value {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-computers-overview-list-section .playground-agents-overview-table-value.is-right {
          text-align: left;
        }
  
        .playground-computers-overview-list-section .playground-resources-overview-name-cell {
          gap: 8px;
          padding-right: 0;
        }
  
        .playground-computers-overview-list-section .playground-resources-overview-table-icon {
          width: 20px;
          height: 20px;
          flex-basis: 20px;
        }
  
        .playground-computers-overview-list-section .playground-resources-overview-table-icon svg {
          width: 16px;
          height: 16px;
        }
  
        .playground-computers-overview-list-section .playground-resources-overview-name-copy {
          gap: 0;
        }
  
        .playground-computers-overview-list-section .playground-resources-overview-name-description {
          display: none;
        }
  
        .playground-computers-overview-list-section .playground-project-overview-thread-cell.is-name {
          color: rgba(255, 255, 255, 0.9);
        }
  
        .playground-computers-overview-list-section .playground-project-overview-threads-table-header > div:first-child,
        .playground-computers-overview-list-section .playground-project-overview-thread-cell.is-select {
          display: flex;
          align-items: center;
          justify-content: center;
        }
  
        .playground-computers-overview-list-section .playground-project-overview-thread-cell.is-actions,
        .playground-computers-overview-list-section .playground-project-overview-threads-table-header > div:last-child {
          justify-self: end;
        }
  
        .playground-computers-overview-list-section .playground-project-overview-thread-cell.is-actions {
          width: 100%;
          display: flex;
          align-items: center;
          justify-content: flex-end;
        }
  
        .playground-resources-overview-section > .playground-plugins-section-header {
          padding-bottom: 0;
          border-bottom: 0;
        }
  
        .playground-agents-observability-section {
          display: flex;
          flex-direction: column;
          gap: 14px;
        }
  
        .playground-agents-observability-section > .playground-plugins-section-header {
          padding-bottom: 0;
          border-bottom: 0;
        }
  
        .playground-agents-observability-section .playground-plugins-toolbar-controls {
          flex: 1 1 0;
        }
  
        .playground-agents-observability-refresh-button {
          margin-left: auto;
        }
  
        .playground-agents-observability-table-shell {
          width: 100%;
        }
  
        .playground-agents-overview-table-shell.is-static .playground-agents-overview-table tbody tr {
          cursor: default;
        }
  
        .playground-agents-overview-table-shell.is-static .playground-agents-overview-table tbody tr:hover,
        .playground-agents-overview-table-shell.is-static .playground-agents-overview-table tbody tr:focus-visible {
          background: transparent;
        }
  
        .playground-agents-observability-load-more-row {
          display: flex;
          justify-content: center;
          padding-top: 2px;
        }
  
        .playground-agents-observability-load-more-button {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 8px;
          min-height: 36px;
          padding: 0 14px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.05);
          color: rgba(255, 255, 255, 0.82);
          font: inherit;
          font-size: 12px;
          font-weight: 400;
          cursor: pointer;
          transition: background-color 160ms ease, border-color 160ms ease, color 160ms ease;
        }
  
        .playground-agents-observability-load-more-button:hover,
        .playground-agents-observability-load-more-button:focus-visible {
          border-color: rgba(255, 255, 255, 0.18);
          background: rgba(255, 255, 255, 0.08);
          color: #fff;
          outline: none;
        }
  
        .playground-agents-observability-status {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          min-height: 22px;
          padding: 0 8px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.07);
          color: rgba(255, 255, 255, 0.72);
          font-size: 12px;
          font-weight: 400;
          line-height: 1;
          text-transform: capitalize;
        }
  
        .playground-agents-observability-status.is-completed {
          background: rgba(53, 232, 187, 0.12);
          color: rgba(160, 255, 229, 0.92);
        }
  
        .playground-agents-observability-status.is-running,
        .playground-agents-observability-status.is-permission_asked {
          background: rgba(102, 166, 255, 0.14);
          color: rgba(190, 218, 255, 0.94);
        }
  
        .playground-agents-observability-status.is-failed,
        .playground-agents-observability-status.is-cancelled {
          background: rgba(255, 100, 125, 0.13);
          color: rgba(255, 185, 197, 0.95);
        }
  
        .playground-agents-model-access {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          min-height: 22px;
          padding: 0 8px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.07);
          color: rgba(255, 255, 255, 0.72);
          font-size: 12px;
          font-weight: 400;
          line-height: 1;
        }
  
        .playground-agents-model-access.is-available {
          background: rgba(102, 166, 255, 0.14);
          color: rgba(190, 218, 255, 0.94);
        }
  
        .playground-agents-model-access.is-locked {
          background: rgba(255, 255, 255, 0.06);
          color: rgba(255, 255, 255, 0.42);
        }
  
        .playground-agents-model-brains {
          display: inline-flex;
          align-items: center;
          gap: 6px;
        }
  
        .playground-agents-model-brain {
          width: 12px;
          height: 12px;
          color: rgba(255, 255, 255, 0.28);
        }
  
        .playground-agents-model-brain.is-active {
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-agents-observability-detail-row td {
          padding: 0 20px 20px !important;
          border-bottom: 1px solid rgba(255, 255, 255, 0.06);
        }
  
        .playground-agents-observability-detail {
          display: grid;
          grid-template-columns: minmax(420px, 1.08fr) minmax(320px, 0.92fr);
          gap: 0;
          margin-top: 2px;
          padding: 0;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 12px;
          background: rgba(255, 255, 255, 0.04);
          overflow: hidden;
        }
  
        .playground-agents-observability-detail.is-loading,
        .playground-agents-observability-detail.is-error {
          display: block;
          padding: 14px;
          color: rgba(255, 255, 255, 0.62);
          font-size: 12px;
          line-height: 1.5;
        }
  
        .playground-agents-observability-tree,
        .playground-agents-observability-panel {
          min-width: 0;
          display: flex;
          flex-direction: column;
        }
  
        .playground-agents-observability-tree {
          max-height: 640px;
          overflow: auto;
          background: rgba(0, 0, 0, 0.18);
        }
  
        .playground-agents-observability-panel {
          max-height: 640px;
          overflow: auto;
          padding: 0;
          border-left: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(0, 0, 0, 0.12);
        }
  
        .playground-agents-observability-node {
          width: 100%;
          min-width: 0;
          display: grid;
          grid-template-columns: 24px minmax(0, 1fr) 76px 220px;
          gap: 12px;
          align-items: center;
          padding: 10px 14px;
          border: 0;
          border-bottom: 1px solid rgba(255, 255, 255, 0.06);
          border-radius: 0;
          background: transparent;
          color: inherit;
          text-align: left;
          cursor: pointer;
          transition: background-color 160ms ease;
        }
  
        .playground-agents-observability-node:hover,
        .playground-agents-observability-node:focus-visible,
        .playground-agents-observability-node.is-selected {
          background: rgba(255, 255, 255, 0.06);
          outline: none;
        }
  
        .playground-agents-observability-node-icon {
          width: 24px;
          height: 24px;
          border-radius: 999px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          background: rgba(255, 255, 255, 0.08);
          color: #fff;
        }
  
        .playground-agents-observability-node.is-thread .playground-agents-observability-node-icon {
          background: rgba(102, 166, 255, 0.20);
        }
  
        .playground-agents-observability-node.is-tool .playground-agents-observability-node-icon {
          background: rgba(53, 232, 187, 0.15);
        }
  
        .playground-agents-observability-node.is-permission .playground-agents-observability-node-icon {
          background: rgba(255, 231, 23, 0.16);
        }
  
        .playground-agents-observability-node-icon svg {
          width: 13px;
          height: 13px;
        }
  
        .playground-agents-observability-node-copy {
          min-width: 0;
        }
  
        .playground-agents-observability-node-title {
          min-width: 0;
          color: rgba(255, 255, 255, 0.92);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.35;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-agents-observability-node-duration {
          color: rgba(255, 255, 255, 0.58);
          font-size: 12px;
          font-weight: 400;
          line-height: 1;
          text-align: right;
          white-space: nowrap;
        }
  
        .playground-agents-observability-node-bar {
          position: relative;
          height: 8px;
          border-radius: 999px;
          background: rgba(0, 0, 0, 0.58);
          overflow: hidden;
        }
  
        .playground-agents-observability-node-bar-fill {
          position: absolute;
          top: 0;
          bottom: 0;
          min-width: 2px;
          border-radius: inherit;
          background: #66a6ff;
        }
  
        .playground-agents-observability-panel-header,
        .playground-agents-observability-panel-section {
          padding: 18px 18px 0;
        }
  
        .playground-agents-observability-panel-section {
          padding-bottom: 18px;
          border-top: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-agents-observability-panel-header {
          padding-bottom: 18px;
        }
  
        .playground-agents-observability-panel-kicker {
          display: flex;
          flex-wrap: wrap;
          align-items: center;
          gap: 8px;
          margin-top: 12px;
        }
  
        .playground-agents-observability-panel-chip {
          display: inline-flex;
          align-items: center;
          gap: 5px;
          min-height: 24px;
          padding: 0 9px;
          border-radius: 7px;
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.72);
          font-size: 12px;
          font-weight: 400;
        }
  
        .playground-agents-observability-property-list {
          display: flex;
          flex-direction: column;
          gap: 12px;
        }
  
        .playground-agents-observability-property-row {
          display: grid;
          grid-template-columns: minmax(90px, 0.45fr) minmax(0, 1fr);
          gap: 12px;
          align-items: flex-start;
        }
  
        .playground-agents-observability-property-label {
          color: rgba(255, 255, 255, 0.48);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.4;
        }
  
        .playground-agents-observability-property-value {
          min-width: 0;
          color: rgba(255, 255, 255, 0.78);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.45;
          overflow-wrap: anywhere;
        }
  
        .playground-agents-observability-raw {
          max-height: 220px;
          overflow: auto;
          margin: 0;
          padding: 12px;
          border-radius: 10px;
          background: rgba(0, 0, 0, 0.24);
          color: rgba(255, 255, 255, 0.68);
          font: 400 12px/1.5 "SFMono-Regular", Consolas, "Liberation Mono", monospace;
          white-space: pre-wrap;
        }
  
        .playground-agents-observability-node-meta,
        .playground-agents-observability-panel-copy,
        .playground-agents-observability-empty {
          color: rgba(255, 255, 255, 0.52);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.45;
        }
  
        .playground-agents-observability-panel-title {
          color: rgba(255, 255, 255, 0.94);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.35;
        }
  
        .playground-agents-observability-facts {
          display: grid;
          grid-template-columns: repeat(4, minmax(0, 1fr));
          gap: 8px;
        }
  
        .playground-agents-observability-fact {
          min-width: 0;
          padding: 10px;
          border-radius: 10px;
          background: rgba(0, 0, 0, 0.22);
        }
  
        .playground-agents-observability-fact-label {
          color: rgba(255, 255, 255, 0.5);
          font-size: 12px;
          line-height: 1.35;
        }
  
        .playground-agents-observability-fact-value {
          margin-top: 4px;
          color: rgba(255, 255, 255, 0.92);
          font-size: 12px;
          line-height: 1.35;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-agents-observability-message {
          max-height: 140px;
          overflow: auto;
          padding: 10px;
          border-radius: 10px;
          background: rgba(0, 0, 0, 0.22);
          color: rgba(255, 255, 255, 0.72);
          font-size: 12px;
          line-height: 1.5;
          white-space: pre-wrap;
        }
  
        .playground-agents-observability-actions {
          display: flex;
          align-items: center;
          justify-content: flex-end;
          gap: 8px;
          padding: 0 18px 18px;
        }
  
        .playground-thread-observability-view {
          width: 100%;
          height: 100%;
          overflow: auto;
          padding: 24px;
          display: flex;
          flex-direction: column;
          min-height: 0;
          scrollbar-width: none;
        }
  
        .playground-thread-observability-view::-webkit-scrollbar {
          display: none;
        }
  
        .playground-thread-observability-card {
          width: min(100%, var(--playground-centered-page-max-width));
          max-width: var(--playground-centered-page-max-width);
          height: 100%;
          min-height: 0;
          margin: 0 auto;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          display: flex;
          flex-direction: column;
        }
  
        .playground-thread-observability-card > .playground-plugins-section-header {
          flex: 0 0 auto;
          padding-bottom: 10px;
          border-bottom: 0;
        }
  
        .playground-thread-observability-toolbar {
          flex: 0 0 auto;
          margin-bottom: 10px;
          border-bottom: 0;
        }
  
        .playground-thread-observability-card > .playground-agents-observability-detail {
          display: block;
          flex: 1 1 auto;
          min-height: 0;
          height: auto;
          max-height: none;
          border: 0;
          border-radius: 0;
          background: transparent;
          overflow: visible;
        }
  
        .playground-thread-observability-card .playground-agents-observability-tree {
          max-height: none;
          height: auto;
          overflow: visible;
          background: transparent;
        }
  
        .playground-thread-observability-card .playground-agents-observability-node {
          grid-template-columns: 24px minmax(0, 1fr) auto;
          padding: 12px 12px;
          border-bottom-color: rgba(255, 255, 255, 0.11);
        }
  
        .playground-thread-observability-card .playground-agents-observability-node-icon {
          background: transparent;
        }
  
        .playground-thread-observability-card .playground-agents-observability-node-icon .playground-permission-mini-ring-icon {
          width: 24px;
          height: 24px;
        }
  
        .playground-thread-observability-card .playground-agents-observability-node-icon .playground-permission-mini-ring-icon svg,
        .playground-thread-observability-action-icon .playground-permission-mini-ring-icon svg {
          width: 10px;
          height: 10px;
        }
  
        .playground-thread-observability-card .playground-agents-observability-node.is-selected {
          background: rgba(255, 255, 255, 0.10);
        }
  
        .playground-thread-observability-summary {
          display: grid;
          grid-template-columns: repeat(4, minmax(0, 1fr));
          gap: 8px;
          margin-bottom: 10px;
        }
  
        .playground-thread-observability-summary-item {
          min-width: 0;
          padding: 10px 12px;
          border-radius: 10px;
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-thread-observability-summary-label {
          display: block;
          color: rgba(255, 255, 255, 0.52);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.3;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-thread-observability-summary-value {
          display: block;
          margin-top: 6px;
          color: rgba(255, 255, 255, 0.94);
          font-size: 14px;
          font-weight: 500;
          line-height: 1.1;
        }
  
        .playground-thread-observability-card .playground-agents-observability-node-meta {
          display: block;
          margin-top: 6px;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-agents-observability-node-badges {
          display: flex;
          align-items: center;
          flex-wrap: wrap;
          gap: 6px;
          margin-top: 6px;
        }
  
        .playground-agents-observability-node-badge {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          min-height: 20px;
          padding: 0 7px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.07);
          color: rgba(255, 255, 255, 0.62);
          font-size: 11px;
          font-weight: 400;
          line-height: 1;
          white-space: nowrap;
        }
  
        .playground-agents-observability-node-badge.is-ring-2 {
          background: rgba(102, 166, 255, 0.14);
          color: rgba(168, 205, 255, 0.9);
        }
  
        .playground-agents-observability-node-badge.is-ring-3 {
          background: rgba(255, 174, 102, 0.14);
          color: rgba(255, 208, 168, 0.94);
        }
  
        .playground-thread-observability-card .playground-agents-observability-node-duration {
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          gap: 7px;
          color: rgba(255, 255, 255, 0.68);
        }
  
        .playground-thread-observability-card .playground-agents-observability-node-bar {
          display: none;
        }
  
        .playground-thread-observability-expanded {
          padding: 16px 18px 22px 52px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.11);
          background: rgba(255, 255, 255, 0.10);
        }
  
        .playground-thread-observability-expanded-header {
          display: flex;
          flex-direction: column;
          gap: 10px;
          padding-bottom: 16px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.12);
        }
  
        .playground-thread-observability-expanded-title {
          color: rgba(255, 255, 255, 0.94);
          font-size: 13px;
          font-weight: 500;
          line-height: 1.35;
        }
  
        .playground-thread-observability-expanded-chips {
          display: flex;
          flex-wrap: wrap;
          align-items: center;
          gap: 12px;
        }
  
        .playground-thread-observability-expanded-chip {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          gap: 6px;
          color: rgba(255, 255, 255, 0.66);
          font-size: 12px;
          line-height: 1.2;
        }
  
        .playground-thread-observability-expanded-chip.is-kind {
          min-height: 24px;
          padding: 0 10px;
          border-radius: 7px;
          background: rgba(77, 163, 255, 0.88);
          color: #fff;
        }
  
        .playground-thread-observability-expanded-chip.is-ring {
          min-height: 24px;
          padding: 0 10px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.76);
        }
  
        .playground-thread-observability-expanded-chip.is-ring-2 {
          background: rgba(102, 166, 255, 0.16);
          color: rgba(168, 205, 255, 0.92);
        }
  
        .playground-thread-observability-expanded-chip.is-ring-3 {
          background: rgba(255, 174, 102, 0.16);
          color: rgba(255, 208, 168, 0.94);
        }
  
        .playground-thread-observability-expanded-chip.is-decision-status {
          min-height: 24px;
          padding: 0 10px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.76);
        }
  
        .playground-thread-observability-expanded-chip.is-decision-status.is-verified {
          background: rgba(45, 212, 132, 0.14);
          color: rgba(128, 241, 181, 0.92);
        }
  
        .playground-thread-observability-expanded-chip.is-decision-status.is-needs-review {
          background: rgba(255, 231, 23, 0.12);
          color: rgba(255, 238, 123, 0.9);
        }
  
        .playground-thread-observability-expanded-chip.is-decision-status.is-superseded {
          background: rgba(255, 255, 255, 0.05);
          color: rgba(255, 255, 255, 0.48);
        }
  
        .playground-thread-observability-expanded-chip svg {
          width: 13px;
          height: 13px;
          flex: 0 0 auto;
        }
  
        .playground-thread-observability-expanded-actions {
          display: flex;
          align-items: center;
          justify-content: flex-end;
          gap: 10px;
          padding-top: 14px;
        }
  
        .playground-thread-observability-expanded-actions .playground-environments-action-button {
          gap: 7px;
        }
  
        .playground-thread-observability-expanded-actions .playground-files-state-loader {
          width: 14px;
          height: 14px;
        }
  
        .playground-thread-observability-expanded-error {
          min-width: 0;
          color: rgba(255, 114, 114, 0.9);
          font-size: 12px;
          line-height: 1.35;
          overflow-wrap: anywhere;
        }
  
        .playground-thread-observability-expanded-warning {
          margin-top: 12px;
          padding: 10px 12px;
          border-radius: 10px;
          background: rgba(255, 255, 255, 0.05);
          color: rgba(255, 255, 255, 0.58);
          font-size: 12px;
          line-height: 1.45;
        }
  
        .playground-thread-observability-expanded-section {
          padding-top: 16px;
        }
  
        .playground-thread-observability-expanded-section-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          margin-bottom: 12px;
          color: rgba(255, 255, 255, 0.86);
          font-size: 12px;
          font-weight: 500;
          line-height: 1.35;
        }
  
        .playground-thread-observability-expanded-section-count {
          color: rgba(255, 255, 255, 0.48);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.35;
          white-space: nowrap;
        }
  
        .playground-thread-observability-action-list {
          display: flex;
          flex-direction: column;
          gap: 0;
          border-radius: 10px;
          overflow: hidden;
          background: rgba(0, 0, 0, 0.18);
        }
  
        .playground-thread-observability-action-item {
          min-width: 0;
          display: grid;
          grid-template-columns: 22px minmax(0, 1fr) auto;
          align-items: start;
          gap: 12px;
          padding: 12px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-thread-observability-action-item:last-child {
          border-bottom: 0;
        }
  
        .playground-thread-observability-action-icon {
          width: 22px;
          height: 22px;
          min-width: 22px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.88);
        }
  
        .playground-thread-observability-action-item.is-ring-2 .playground-thread-observability-action-icon {
          background: transparent;
          color: rgba(168, 205, 255, 0.95);
        }
  
        .playground-thread-observability-action-item.is-ring-3 .playground-thread-observability-action-icon {
          background: transparent;
          color: rgba(255, 180, 195, 0.95);
        }
  
        .playground-thread-observability-action-icon .playground-permission-mini-ring-icon {
          width: 22px;
          height: 22px;
        }
  
        .playground-thread-observability-action-copy {
          min-width: 0;
        }
  
        .playground-thread-observability-action-title {
          min-width: 0;
          color: rgba(255, 255, 255, 0.9);
          font-size: 12px;
          font-weight: 500;
          line-height: 1.35;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-thread-observability-action-summary {
          margin-top: 5px;
          color: rgba(255, 255, 255, 0.56);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.45;
          overflow-wrap: anywhere;
        }
  
        .playground-thread-observability-action-resources {
          display: flex;
          flex-wrap: wrap;
          align-items: center;
          gap: 6px;
          margin-top: 8px;
        }
  
        .playground-thread-observability-action-resources span {
          min-width: 0;
          max-width: 220px;
          display: inline-block;
          padding: 0;
          color: rgba(102, 166, 255, 0.9);
          font-size: 11px;
          line-height: 1.3;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-thread-observability-action-meta {
          display: flex;
          flex-direction: column;
          align-items: flex-end;
          gap: 7px;
          color: rgba(255, 255, 255, 0.46);
          font-size: 11px;
          line-height: 1.2;
          white-space: nowrap;
        }
  
        .playground-thread-observability-action-ring {
          min-height: 20px;
          display: inline-flex;
          align-items: center;
          padding: 0 7px;
          border-radius: 999px;
          background: rgba(57, 184, 119, 0.14);
          color: rgba(151, 241, 194, 0.92);
        }
  
        .playground-thread-observability-action-ring.is-ring-2 {
          background: rgba(18, 95, 251, 0.16);
          color: rgba(168, 205, 255, 0.95);
        }
  
        .playground-thread-observability-action-ring.is-ring-3 {
          background: rgba(247, 71, 107, 0.16);
          color: rgba(255, 180, 195, 0.95);
        }
  
        .playground-thread-observability-expanded-properties {
          display: flex;
          flex-direction: column;
          gap: 13px;
          padding-bottom: 16px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.10);
        }
  
        .playground-thread-observability-expanded-property {
          display: grid;
          grid-template-columns: minmax(120px, 0.42fr) minmax(0, 1fr);
          gap: 16px;
          align-items: start;
        }
  
        .playground-thread-observability-expanded-property-label {
          color: rgba(255, 255, 255, 0.58);
          font-size: 12px;
          line-height: 1.4;
        }
  
        .playground-thread-observability-expanded-property-value {
          min-width: 0;
          color: rgba(255, 255, 255, 0.78);
          font-size: 12px;
          line-height: 1.45;
          text-align: right;
          overflow-wrap: anywhere;
        }
  
        .playground-thread-observability-expanded-message,
        .playground-thread-observability-expanded-raw {
          max-height: 260px;
          overflow: auto;
          color: rgba(255, 255, 255, 0.74);
          font-size: 12px;
          line-height: 1.55;
          white-space: pre-wrap;
        }
  
        .playground-thread-observability-expanded-raw {
          margin: 0;
          color: rgba(255, 255, 255, 0.58);
          font-family: "SFMono-Regular", Consolas, "Liberation Mono", monospace;
        }
  
        .playground-thread-observability-expanded-raw-editor {
          min-height: 160px;
          max-height: 420px;
          border: 0;
          border-radius: 8px;
          overflow: hidden;
          background: transparent;
          scrollbar-width: none;
        }
  
        .playground-thread-observability-expanded-raw-editor::-webkit-scrollbar {
          display: none;
        }
  
        .playground-thread-observability-expanded-raw-editor .monaco-editor,
        .playground-thread-observability-expanded-raw-editor .monaco-editor-background,
        .playground-thread-observability-expanded-raw-editor .monaco-scrollable-element,
        .playground-thread-observability-expanded-raw-editor .margin,
        .playground-thread-observability-expanded-raw-editor .overflow-guard {
          background: transparent !important;
        }
  
        .playground-thread-observability-expanded-section.is-raw-event {
          margin-top: 16px;
          border-top: 1px solid rgba(255, 255, 255, 0.10);
        }
  
        @media (max-width: 980px) {
          .playground-agents-observability-detail {
            grid-template-columns: minmax(0, 1fr);
          }
  
          .playground-agents-observability-panel {
            padding-left: 0;
            border-left: 0;
            border-top: 1px solid rgba(255, 255, 255, 0.08);
            padding-top: 0;
          }
  
          .playground-agents-observability-node {
            grid-template-columns: 24px minmax(0, 1fr) 68px;
          }
  
          .playground-agents-observability-node-bar {
            display: none;
          }
  
          .playground-agents-observability-facts {
            grid-template-columns: repeat(2, minmax(0, 1fr));
          }
  
          .playground-thread-observability-summary {
            grid-template-columns: repeat(2, minmax(0, 1fr));
          }
        }
  
        .playground-resources-page .playground-resources-overview-search-row {
          border-bottom: 0;
        }
  
        .playground-resources-overview-create-button {
          margin-left: auto;
        }
  
        .playground-resources-page.is-computers-view .playground-resources-overview-create-button {
          min-height: 30px;
          padding: 0 14px;
          border-radius: 999px;
          background: linear-gradient(to top, #082673, #1D59BE);
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-resources-page.is-computers-view .playground-resources-overview-create-button:hover {
          color: #fff;
          background: linear-gradient(to top, #082673, #1D59BE);
        }
  
        .playground-resources-overview-settings-stack {
          width: 100%;
        }
  
        .playground-resources-overview-settings-card {
          width: 100%;
        }
  
        .playground-resources-overview-estimate-card {
          width: 100%;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 15px;
          background: rgba(255, 255, 255, 0.05);
          padding: 18px;
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 18px;
        }
  
        .playground-resources-overview-estimate-copy {
          display: flex;
          flex-direction: column;
          gap: 6px;
          min-width: 0;
        }
  
        .playground-resources-overview-estimate-label {
          color: rgba(255, 255, 255, 0.5);
          font-size: 12px;
          font-weight: 400;
        }
  
        .playground-resources-overview-estimate-value {
          color: #fff;
          font-size: 26px;
          font-weight: 500;
          letter-spacing: 0;
        }
  
        .playground-resources-overview-estimate-note {
          color: rgba(255, 255, 255, 0.5);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.35;
        }
  
        .playground-resources-overview-estimate-breakdown {
          display: flex;
          flex-wrap: wrap;
          justify-content: flex-end;
          gap: 8px;
          max-width: 52%;
        }
  
        .playground-resources-overview-estimate-chip {
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.05);
          color: rgba(255, 255, 255, 0.72);
          font-size: 12px;
          font-weight: 400;
          line-height: 1;
          padding: 7px 9px;
          white-space: nowrap;
        }
  
        .playground-agents-overview-page .playground-environments-home-scroll,
        .playground-resources-page .playground-environments-home-scroll {
          scrollbar-width: none;
        }
  
        .playground-agents-overview-page .playground-environments-home-scroll::-webkit-scrollbar,
        .playground-resources-page .playground-environments-home-scroll::-webkit-scrollbar {
          display: none;
        }
  
        .playground-agents-overview-table-shell,
        .playground-resources-overview-table-shell {
          width: 100%;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 15px;
          overflow-x: auto;
          scrollbar-width: none;
        }
  
        .playground-agents-overview-table-shell::-webkit-scrollbar,
        .playground-resources-overview-table-shell::-webkit-scrollbar {
          display: none;
        }
  
        .playground-agents-overview-table,
        .playground-resources-overview-table {
          width: 100%;
          min-width: 980px;
          table-layout: fixed;
          border-collapse: collapse;
        }
  
        .playground-agents-overview-table th,
        .playground-resources-overview-table th {
          padding: 12px 0;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
          color: rgba(255, 255, 255, 0.45);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.35;
          text-align: left;
        }
  
        .playground-agents-overview-table th.is-right,
        .playground-resources-overview-table th.is-right {
          text-align: right;
        }
  
        .playground-agents-overview-table th.is-action,
        .playground-resources-overview-table th.is-action,
        .playground-agents-overview-table td.playground-overview-table-action-cell,
        .playground-resources-overview-table td.playground-overview-table-action-cell {
          text-align: right;
        }
  
        .playground-agents-overview-table th:first-child,
        .playground-resources-overview-table th:first-child {
          padding-left: 20px;
        }
  
        .playground-agents-overview-table th:last-child,
        .playground-resources-overview-table th:last-child {
          padding-right: 20px;
        }
  
        .playground-agents-overview-table td,
        .playground-resources-overview-table td {
          padding: 16px 0;
          border-bottom: 1px solid rgba(255, 255, 255, 0.06);
          vertical-align: middle;
        }
  
        .playground-agents-overview-table td:first-child,
        .playground-resources-overview-table td:first-child {
          padding-left: 20px;
        }
  
        .playground-agents-overview-table td:last-child,
        .playground-resources-overview-table td:last-child {
          padding-right: 20px;
        }
  
        .playground-agents-overview-table tbody tr,
        .playground-resources-overview-table tbody tr {
          cursor: pointer;
          transition: background-color 160ms ease;
        }
  
        .playground-agents-overview-table tbody tr:hover,
        .playground-agents-overview-table tbody tr:focus-visible,
        .playground-resources-overview-table tbody tr:hover,
        .playground-resources-overview-table tbody tr:focus-visible {
          background: rgba(255, 255, 255, 0.02);
          outline: none;
        }
  
        .playground-agents-overview-name-cell,
        .playground-resources-overview-name-cell {
          min-width: 0;
          display: flex;
          align-items: center;
          gap: 12px;
          padding-right: 28px;
        }
  
        .playground-agents-overview-table-avatar,
        .playground-resources-overview-table-icon {
          width: 36px;
          height: 36px;
          border-radius: 5px;
          background: transparent;
          color: rgba(255, 255, 255, 0.88);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          overflow: hidden;
          flex: 0 0 auto;
        }
  
        .playground-resources-overview-table-icon {
          color: rgba(255, 255, 255, 0.9);
        }
  
        .playground-agents-overview-table-avatar-image {
          width: 100%;
          height: 100%;
          object-fit: cover;
          display: block;
        }
  
        .playground-agents-overview-table-avatar-fallback {
          font-size: 11px;
          font-weight: 500;
          line-height: 1;
          color: rgba(255, 255, 255, 0.7);
        }
  
        .playground-agents-overview-table-avatar.is-team {
          border-radius: 5px;
          background: transparent;
          color: rgba(143, 196, 255, 0.9);
        }
  
        .playground-agents-overview-name-copy,
        .playground-agents-overview-model-copy,
        .playground-resources-overview-name-copy {
          min-width: 0;
          display: flex;
          flex-direction: column;
        }
  
        .playground-agents-overview-name-copy,
        .playground-resources-overview-name-copy {
          gap: 4px;
        }
  
        .playground-agents-overview-name-title,
        .playground-resources-overview-name-title {
          overflow: hidden;
          color: rgba(255, 255, 255, 0.96);
          font-size: 13px;
          font-weight: 500;
          line-height: 1.35;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-agents-overview-name-description,
        .playground-resources-overview-name-description {
          display: -webkit-box;
          overflow: hidden;
          -webkit-box-orient: vertical;
          -webkit-line-clamp: 2;
          color: rgba(255, 255, 255, 0.48);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.45;
        }
  
        .playground-agents-overview-model-cell {
          min-width: 0;
          display: flex;
          align-items: center;
          gap: 10px;
          padding-right: 24px;
        }
  
        .playground-agents-overview-table-model-icon {
          width: 18px;
          height: 18px;
          color: rgba(255, 255, 255, 0.8);
        }
  
        .playground-agents-overview-table-model-icon .playground-agents-model-provider-icon {
          width: 16px;
          height: 16px;
        }
  
        .playground-agents-overview-model-provider {
          overflow: hidden;
          color: rgba(255, 255, 255, 0.45);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.35;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-agents-overview-model-name {
          overflow: hidden;
          color: rgba(255, 255, 255, 0.78);
          font-size: 13px;
          font-weight: 400;
          line-height: 1.35;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-agents-overview-table-value {
          color: rgba(255, 255, 255, 0.58);
          font-size: 13px;
          font-weight: 400;
          line-height: 1.35;
          white-space: nowrap;
        }
  
        .playground-agents-overview-table-value.is-right {
          text-align: right;
        }
  
        .playground-resources-overview-table .playground-agents-overview-table-value,
        .playground-resources-overview-name-description {
          color: rgba(255, 255, 255, 0.74);
        }
  
        .playground-resources-overview-published {
          color: rgba(255, 255, 255, 0.74);
        }
  
        .playground-resources-overview-published.is-yes {
          color: rgba(255, 255, 255, 0.9);
        }
  
        .playground-overview-table-action-cell {
          position: relative;
        }
  
        .playground-overview-table-action-button {
          width: 28px;
          height: 28px;
          margin-left: auto;
          padding: 0;
          border: 0;
          border-radius: 8px;
          background: transparent;
          color: rgba(255, 255, 255, 0.58);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          transition: background-color 160ms ease, color 160ms ease;
        }
  
        .playground-overview-table-action-button:hover,
        .playground-overview-table-action-button.is-open {
          background: rgba(255, 255, 255, 0.06);
          color: #fff;
        }
  
        .playground-overview-table-action-icon {
          width: 16px;
          height: 16px;
          flex-shrink: 0;
        }
  
        .playground-agents-list-action-menu-shell {
          width: 220px;
          z-index: 361;
          pointer-events: none;
        }
  
        .playground-tasks-toolbar-popup-shell.playground-tasks-toolbar-popup-shell-portal .playground-agents-list-action-menu {
          width: 220px;
          min-width: 220px;
          max-height: min(360px, calc(100vh - 24px));
          transform-origin: top right;
          pointer-events: auto;
        }
  
        .playground-agents-list-action-menu.is-closing {
          pointer-events: none;
          animation: playground-agent-list-action-menu-fade-out 90ms ease both;
        }
  
        .playground-agents-list-action-menu .tb-popup-row.is-danger {
          color: #ff9c9c;
        }
  
        .playground-agents-list-action-menu .tb-popup-row:disabled {
          opacity: 0.45;
          cursor: default;
        }
  
  	      .playground-agents-overview-page .playground-tasks-toolbar-popup-shell .playground-agents-overview-toolbar-menu,
  	      .playground-team-page .playground-tasks-toolbar-popup-shell .playground-agents-overview-toolbar-menu,
  	      .playground-resources-page.is-develop-configure-page .playground-tasks-toolbar-popup-shell .playground-computers-overview-toolbar-menu {
          top: calc(100% + 8px);
          left: 0;
          right: auto;
          bottom: auto;
          width: 220px;
          min-width: 220px;
          max-height: min(360px, calc(100vh - 24px));
          transform-origin: top left;
          pointer-events: auto;
          overflow: hidden;
        }
  
  	      .playground-agents-overview-page .playground-tasks-toolbar-popup-shell .playground-agents-overview-toolbar-menu.is-closing,
  	      .playground-team-page .playground-tasks-toolbar-popup-shell .playground-agents-overview-toolbar-menu.is-closing,
  	      .playground-resources-page.is-develop-configure-page .playground-tasks-toolbar-popup-shell .playground-computers-overview-toolbar-menu.is-closing {
          pointer-events: none;
          animation: playground-agent-list-action-menu-fade-out 90ms ease both;
        }
  
        .playground-agents-page .playground-environments-home-metrics .playground-database-overview-chart-block,
        .playground-agents-page .playground-environments-home-metrics .playground-database-overview-timeseries-card,
        .playground-agents-page .playground-environments-home-metrics .playground-database-overview-timeseries-chart,
        .playground-agents-overview-page .playground-environments-home-metrics .playground-database-overview-chart-block,
        .playground-agents-overview-page .playground-environments-home-metrics .playground-database-overview-timeseries-card,
        .playground-agents-overview-page .playground-environments-home-metrics .playground-database-overview-timeseries-chart,
        .playground-resources-page .playground-environments-home-metrics .playground-database-overview-chart-block,
        .playground-resources-page .playground-environments-home-metrics .playground-database-overview-timeseries-card,
        .playground-resources-page .playground-environments-home-metrics .playground-database-overview-timeseries-chart {
          box-sizing: border-box;
          width: 100%;
          padding: 20px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 15px;
          background: linear-gradient(to top, rgba(5, 24, 58, 0.5), rgba(5, 24, 58, 0.9));
        }
  
        .playground-agents-page .playground-environments-home-metrics .playground-database-overview-chart-block,
        .playground-agents-overview-page .playground-environments-home-metrics .playground-database-overview-chart-block,
        .playground-resources-page .playground-environments-home-metrics .playground-database-overview-chart-block {
          overflow: hidden;
        }
  
  	      .playground-settings-page .playground-environments-home-metrics .playground-database-overview-chart-block {
  	        box-sizing: border-box;
  	        width: 100%;
  	        padding: 20px;
  	        border: 1px solid rgba(255, 255, 255, 0.075);
          border-radius: 15px;
  	        background: linear-gradient(to top, rgba(5, 24, 58, 0.5), rgba(5, 24, 58, 0.9));
  	        overflow: hidden;
  	      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-environments-detail-scroll.playground-environments-home-scroll,
  		      .playground-resources-page.is-develop-configure-page .playground-environments-detail-scroll.playground-environments-home-scroll {
  		        padding: 42px 44px 56px;
  		      }
  
  		      .playground-resources-page.playground-agents-overview-page.is-develop-server-kind-page .playground-environments-detail-scroll.playground-environments-home-scroll,
  		      .playground-resources-page.playground-agents-overview-page.is-develop-configure-page .playground-environments-detail-scroll.playground-environments-home-scroll {
  		        padding-top: 0;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page.is-computers-view .playground-environments-detail-scroll.playground-environments-home-scroll,
  		      .playground-resources-page.is-develop-configure-page.is-computers-view .playground-environments-detail-scroll.playground-environments-home-scroll {
  		        padding-top: 0;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-environments-home-content,
  		      .playground-resources-page.is-develop-configure-page .playground-environments-home-content {
  		        width: min(100%, var(--playground-centered-page-max-width));
  		        max-width: var(--playground-centered-page-max-width);
  		        gap: 24px;
  		      }
  
  	      .playground-resources-page.is-develop-server-kind-page .playground-environments-home-hero,
  	      .playground-resources-page.is-develop-configure-page .playground-environments-home-hero {
  	        margin: 0;
  	        padding: 0;
  	        display: flex;
  	        flex-direction: column;
  	        gap: 0;
  	      }
  
  	      .playground-resources-page.is-develop-server-kind-page .playground-develop-server-kind-header,
  	      .playground-resources-page.is-develop-configure-page .playground-develop-server-kind-header {
  	        margin: 0 0 12px;
  	        padding-bottom: 12px;
  	      }
  
  	      .playground-resources-page.playground-agents-overview-page.is-develop-server-kind-page .playground-develop-server-kind-header,
  	      .playground-resources-page.playground-agents-overview-page.is-develop-configure-page .playground-develop-server-kind-header {
  	        padding-top: 42px;
  	      }
  
  	      .playground-resources-page.is-develop-server-kind-page.is-computers-view .playground-develop-server-kind-header,
  	      .playground-resources-page.is-develop-configure-page.is-computers-view .playground-develop-server-kind-header {
  	        padding-top: 42px;
  	      }
  
  	      .playground-resources-page.is-develop-server-kind-page .playground-develop-server-kind-tabs.playground-agents-overview-tabs.playground-project-overview-tabs,
  	      .playground-resources-page.is-develop-configure-page .playground-develop-server-kind-tabs.playground-agents-overview-tabs.playground-project-overview-tabs {
  	        margin: 16px 0 0;
  	      }
  
  	      .playground-resources-page.is-develop-configure-page .playground-agents-configure-hero .playground-develop-server-kind-tabs.playground-agents-overview-tabs.playground-project-overview-tabs {
  	        margin-top: 12px;
  	      }
  
  	      .playground-resources-page.is-develop-configure-page.is-computers-view .playground-develop-server-kind-hero .playground-develop-server-kind-tabs.playground-agents-overview-tabs.playground-project-overview-tabs {
  	        margin-top: 12px;
  	      }
  
  	      .playground-resources-page.is-develop-configure-page .playground-agents-configure-analytics-title-row {
  	        margin-top: 24px;
  	      }
  
  	      .playground-resources-page.is-develop-configure-page.is-computers-view .playground-develop-server-kind-operational-metrics > .playground-develop-server-metrics-title-row,
  	      .playground-resources-page.is-develop-configure-page.is-computers-view .playground-develop-server-kind-metrics > .playground-develop-server-metrics-title-row {
  	        margin-top: 24px;
  	      }
  
  	      .playground-resources-page.is-develop-server-kind-page .playground-develop-server-kind-toolbar,
  	      .playground-resources-page.is-develop-configure-page .playground-develop-server-kind-toolbar {
  	        margin-top: 18px;
  	        margin-bottom: 24px;
  	      }
  
  	      .playground-resources-page.is-develop-server-kind-page .playground-develop-server-kind-metrics.playground-environments-home-metrics,
  	      .playground-resources-page.is-develop-configure-page .playground-develop-server-kind-metrics.playground-environments-home-metrics {
  	        margin-top: 12px;
  	        margin-bottom: 0;
  	        max-height: 364px;
  	        gap: 10px;
  	      }
  
  	      .playground-resources-page.is-develop-server-kind-page .playground-develop-server-kind-metrics .playground-database-overview-chart-block,
  	      .playground-resources-page.is-develop-configure-page .playground-develop-server-kind-metrics .playground-database-overview-chart-block {
  	        --playground-project-overview-chart-border: linear-gradient(
  	          -10deg,
  	          rgba(200, 200, 200, 0.25),
  	          rgba(255, 255, 255, 0.1),
  	          rgba(255, 255, 255, 0.15),
  	          rgba(255, 255, 255, 0.375)
  	        );
  	        position: relative;
  	        box-sizing: border-box;
  	        width: 100%;
  	        padding: 20px;
  	        border: 0;
  	        border-radius: 15px;
  	        background: transparent;
  	        overflow: hidden;
  	        -webkit-backdrop-filter: blur(50px);
  	        backdrop-filter: blur(50px);
  	      }
  
  	      .playground-resources-page.is-develop-server-kind-page .playground-develop-server-kind-metrics .playground-database-overview-chart-block::before,
  	      .playground-resources-page.is-develop-configure-page .playground-develop-server-kind-metrics .playground-database-overview-chart-block::before {
  	        content: "";
  	        pointer-events: none;
  	        position: absolute;
  	        inset: 0;
  	        z-index: 5;
  	        border-radius: inherit;
  	        padding: 1px;
  	        background: var(--playground-project-overview-chart-border);
  	        mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
  	        mask-clip: content-box, border-box;
  	        mask-composite: exclude;
  	        mask-origin: content-box, border-box;
  	        mask-repeat: repeat, repeat;
  	        mask-size: auto, auto;
  	      }
  
  	      .playground-resources-page.is-develop-server-kind-page .playground-develop-server-kind-metrics .playground-database-overview-chart-block > *,
  	      .playground-resources-page.is-develop-configure-page .playground-develop-server-kind-metrics .playground-database-overview-chart-block > * {
  	        position: relative;
  	        z-index: 1;
  	      }
  
  	      .playground-resources-page.is-develop-server-kind-page .playground-develop-server-kind-metrics .playground-database-overview-timeseries-card,
  	      .playground-resources-page.is-develop-server-kind-page .playground-develop-server-kind-metrics .playground-database-overview-timeseries-chart,
  	      .playground-resources-page.is-develop-configure-page .playground-develop-server-kind-metrics .playground-database-overview-timeseries-card,
  	      .playground-resources-page.is-develop-configure-page .playground-develop-server-kind-metrics .playground-database-overview-timeseries-chart {
  	        height: auto;
  	      }
  
  	      .playground-resources-page.is-develop-server-kind-page .playground-develop-server-kind-operational-metrics .playground-database-overview-timeseries-card,
  	      .playground-resources-page.is-develop-server-kind-page .playground-develop-server-kind-operational-metrics .playground-database-overview-timeseries-chart,
  	      .playground-resources-page.is-develop-configure-page .playground-develop-server-kind-operational-metrics .playground-database-overview-timeseries-card,
  	      .playground-resources-page.is-develop-configure-page .playground-develop-server-kind-operational-metrics .playground-database-overview-timeseries-chart {
  	        padding: 0;
  	        border: 0;
  	        border-radius: 0;
  	        background: transparent;
  	      }
  
  	      .playground-resources-page.is-develop-server-kind-page .playground-develop-server-kind-operational-metrics .playground-database-overview-timeseries-frame,
  	      .playground-resources-page.is-develop-configure-page .playground-develop-server-kind-operational-metrics .playground-database-overview-timeseries-frame {
  	        border: 0;
  	        background: transparent;
  	      }
  
  	      .playground-resources-page.is-develop-server-kind-page .playground-develop-server-kind-metrics .playground-project-overview-chart-kpis,
  	      .playground-resources-page.is-develop-configure-page .playground-develop-server-kind-metrics .playground-project-overview-chart-kpis {
  	        background: rgba(255, 255, 255, 0.05);
  	      }
  
  	      .playground-agents-overview-page.is-develop-configure-page .playground-develop-server-kind-metrics .playground-project-overview-chart-kpis,
  	      .playground-resources-page.is-develop-configure-page .playground-develop-server-kind-metrics .playground-project-overview-chart-kpis {
  	        background: transparent;
  	      }
  
  		      .playground-resources-page.is-develop-server-kind-page .is-develop-server-kind-list,
  		      .playground-resources-page.is-develop-configure-page .is-develop-server-kind-list {
  		        margin-top: 0;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .is-develop-server-kind-list {
  		        margin-bottom: 24px;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-server-kind-table-toolbar,
  		      .playground-resources-page.is-develop-configure-page .playground-develop-server-kind-table-toolbar {
  		        display: flex;
  		        align-items: center;
  		        justify-content: space-between;
  		        gap: 16px;
  		        padding: 0;
  		        margin: 0 0 14px;
  		        border-bottom: 0;
  		      }
  
  		      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list.playground-agents-overview-table-section {
  		        position: relative;
  		        isolation: isolate;
  		        gap: 0;
  		        margin-bottom: 24px;
  		        padding: 0 5px 18px;
  		        border: 1px solid rgba(255, 255, 255, 0.1);
  		        border-radius: 10px;
  		        overflow: visible;
  		        background: rgba(255, 255, 255, 0.05);
  		        -webkit-backdrop-filter: none;
  		        backdrop-filter: none;
  		      }
  
  		      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list.playground-agents-overview-table-section::before {
  		        content: none;
  		      }
  
  		      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list.playground-agents-overview-table-section > * {
  		        position: relative;
  		        z-index: 1;
  		      }
  
  		      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list.playground-agents-overview-table-section > .playground-develop-server-kind-table-toolbar {
  		        flex-direction: row;
  		        align-items: center;
  		        justify-content: flex-start;
  		        gap: 10px;
  		        margin: 0;
  		        padding: 0 0 12px;
  		        border-bottom: 0;
  		        z-index: 200;
  		      }
  
  		      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list.playground-agents-overview-table-section > .playground-agents-overview-list-table {
  		        z-index: 0;
  		      }
  
  		      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list.playground-agents-overview-table-section > .playground-develop-server-kind-table-toolbar .playground-tasks-toolbar-popup-shell {
  		        z-index: 210;
  		      }
  
  		      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list.playground-agents-overview-table-section > .playground-develop-server-kind-table-toolbar .playground-tasks-toolbar-popup-menu {
  		        z-index: 211;
  		      }
  
  		      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list.playground-agents-overview-table-section .playground-agents-overview-sticky-table-header .playground-tasks-toolbar-popup-shell {
  		        z-index: 250;
  		      }
  
  		      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list.playground-agents-overview-table-section .playground-agents-overview-sticky-table-header .playground-tasks-toolbar-popup-menu {
  		        z-index: 251;
  		      }
  
  		      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list.playground-agents-overview-table-section > .playground-plugins-empty {
  		        margin: 0;
  		        padding: 28px 24px;
  		      }
  
  		      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list.playground-agents-overview-table-section .playground-agents-overview-table-shell {
  		        margin: 0;
  		        border: 0;
  		        border-radius: 0;
  		        background: transparent;
  		        -webkit-backdrop-filter: none;
  		        backdrop-filter: none;
  		      }
  
  		      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list.playground-agents-overview-table-section .playground-agents-overview-table-shell::before {
  		        content: none;
  		      }
  
  	      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list.playground-agents-overview-list-section.playground-agents-overview-table-section {
  	        margin-top: 0 !important;
  	        overflow: visible !important;
  	        background: rgba(255, 255, 255, 0.075);
  	        border: 1px solid rgba(255, 255, 255, 0.075);
  	        border-radius: 15px;
  	        padding-bottom: 6px;
  	      }
  
  	      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list.playground-agents-overview-list-section.playground-agents-overview-table-section .playground-agents-overview-sticky-table-header {
  	        position: sticky;
  	        top: 0;
  	        z-index: 240;
  	        background: #121212;
  	        margin: 0 -18px;
  	        padding: 12px 18px 0;
  	        border-radius: 15px 15px 0 0;
  	        -webkit-backdrop-filter: none;
  	        backdrop-filter: none;
  	      }
  
  	      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list.playground-agents-overview-list-section.playground-agents-overview-table-section .playground-agents-overview-sticky-table-header .playground-develop-server-kind-table-toolbar {
  	        flex-direction: row;
  	        align-items: center;
  	        justify-content: flex-start;
  	        gap: 10px;
  	        margin: 0;
  	        padding: 0 0 12px;
  	        border-bottom: 0;
  	        background: transparent;
  	        z-index: 1;
  	      }
  
  	      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list.playground-agents-overview-list-section.playground-agents-overview-table-section .playground-agents-overview-sticky-table-header .playground-develop-server-kind-table-controls {
  	        width: 100%;
  	        margin-left: 0;
  	        justify-content: flex-start;
  	        align-items: center;
  	        flex-wrap: nowrap;
  	      }
  
  		      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list.playground-agents-overview-list-section.playground-agents-overview-table-section .playground-agents-overview-sticky-table-header .playground-develop-server-kind-search-shell {
  		        flex: 0 1 340px;
  		        width: min(340px, 100%);
  		        min-width: min(280px, 100%);
  		        max-width: 340px;
  		        background: rgba(255, 255, 255, 0.025) !important;
  		      }
  
  		      .playground-team-overview-page.is-develop-configure-page .playground-team-overview-table-section .playground-team-overview-sticky-table-header .playground-develop-server-kind-table-toolbar {
  		        flex-wrap: nowrap !important;
  		      }
  
  			      .playground-team-overview-page.is-develop-configure-page .playground-team-overview-table-section .playground-team-overview-toolbar-row {
  			        position: relative;
  			        z-index: 501;
  			        display: flex !important;
  			        align-items: center;
  			        justify-content: flex-start !important;
  			        flex-direction: row !important;
  			        flex-wrap: nowrap !important;
  			        gap: 10px;
  			        width: 100% !important;
  			        padding: 0 0 12px !important;
  			        margin: 0 !important;
  			        border-bottom: 0 !important;
  			        pointer-events: auto;
  			      }
  
  			      .playground-team-overview-page.is-develop-configure-page .playground-team-overview-table-section .playground-team-overview-toolbar-row > *,
  			      .playground-team-overview-page.is-develop-configure-page .playground-team-overview-table-section .playground-team-overview-toolbar-row .playground-plugins-toolbar-controls,
  			      .playground-team-overview-page.is-develop-configure-page .playground-team-overview-table-section .playground-team-overview-toolbar-row .playground-files-toolbar-anchor,
  			      .playground-team-overview-page.is-develop-configure-page .playground-team-overview-table-section .playground-team-overview-toolbar-row .playground-develop-server-kind-search-shell,
  			      .playground-team-overview-page.is-develop-configure-page .playground-team-overview-table-section .playground-team-overview-toolbar-row .playground-plugins-search,
  			      .playground-team-overview-page.is-develop-configure-page .playground-team-overview-table-section .playground-team-overview-toolbar-row button {
  			        position: relative;
  			        z-index: 502;
  			        pointer-events: auto;
  			      }
  
  		      .playground-team-overview-page.is-develop-configure-page .playground-team-overview-table-section .playground-team-overview-sticky-table-header .playground-develop-server-kind-table-controls {
  		        flex: 1 1 auto !important;
  		        width: auto !important;
  		        min-width: 0;
  		        flex-wrap: nowrap !important;
  		      }
  
  			      .playground-team-overview-page.is-develop-configure-page .playground-team-overview-table-section .playground-team-overview-sticky-table-header .playground-develop-server-kind-search-shell {
  			        flex: 0 1 340px !important;
  			        width: min(340px, 100%) !important;
  			        min-width: 0 !important;
  			        max-width: 340px !important;
  			        background: rgba(255, 255, 255, 0.025) !important;
  			      }
  
  		      .playground-team-overview-page.is-develop-configure-page .playground-team-overview-table-section .playground-team-overview-sticky-table-header .playground-plugins-toolbar-controls {
  		        flex: 0 0 auto;
  		        flex-wrap: nowrap !important;
  		      }
  
  		      .playground-team-overview-page.is-develop-configure-page .playground-team-overview-table-section .playground-team-overview-sticky-table-header .playground-agents-overview-toolbar-create-button {
  		        flex: 0 0 auto !important;
  		        margin-left: auto;
  		      }
  
  		      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list.playground-agents-overview-list-section.playground-agents-overview-table-section .playground-project-overview-threads-table-header {
  		        position: relative;
  	        z-index: 1;
  	        background: transparent;
  	        padding-top: 0;
  	        border-bottom: 0;
  	        -webkit-backdrop-filter: none;
  	        backdrop-filter: none;
  	      }
  
  	      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list.playground-agents-overview-list-section.playground-agents-overview-table-section .playground-project-overview-threads-table-header.playground-agents-overview-column-header {
  	        margin-top: 12px;
  	        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  	      }
  
  	      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list.playground-agents-overview-list-section.playground-agents-overview-table-section .playground-project-overview-threads-table {
  		        background: transparent;
  		        border: none;
  		        border-radius: 0;
  		        padding-left: 0;
  		        padding-right: 0;
  		        width: 100%;
  		        margin-left: 0;
  		        overflow: visible !important;
  		      }
  
  	      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list.playground-agents-overview-list-section.playground-agents-overview-table-section .playground-project-overview-thread-list {
  		        background: #000;
  		        border-radius: 10px;
  		        padding-left: 12px;
  		        padding-right: 12px;
  		        width: calc(100% + 24px);
  		        margin-left: -12px;
  		        border: 1px solid rgba(255, 255, 255, 0.1);
  		        overflow: visible !important;
  		      }
  
  	      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list.playground-agents-overview-list-section.playground-agents-overview-table-section .playground-project-overview-threads-table-row {
  		        overflow: visible !important;
  		      }
  
  ${CONFIGURE_HOME_STYLE_FRAGMENTS.notificationsTable}
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list {
  		        position: relative;
  		        isolation: isolate;
  		        z-index: 3;
  		        gap: 0;
  		        margin-bottom: 24px;
  		        padding: 0 5px 5px;
  		        border: 1px solid rgba(255, 255, 255, 0.075);
  		        border-radius: 15px;
  		        overflow: visible !important;
  		        background: rgba(255, 255, 255, 0.075);
  		        -webkit-backdrop-filter: none;
  		        backdrop-filter: none;
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list::before {
  		        content: none;
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list > * {
  		        position: relative;
  		        z-index: 1;
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list > .playground-develop-server-kind-table-toolbar {
  		        flex-direction: row;
  		        align-items: center;
  		        justify-content: flex-start;
  		        gap: 10px;
  		        margin: 0;
  		        padding: 0 0 12px;
  		        border-bottom: 0;
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list > .playground-plugins-empty {
  		        margin: 0;
  		        padding: 28px 24px;
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-computers-overview-sticky-table-header {
  		        position: sticky;
  		        top: 0;
  		        z-index: 500;
  		        margin: 0 -18px;
  		        padding: 12px 18px 0;
  		        border-radius: 15px 15px 0 0;
  		        background: #121212;
  		        -webkit-backdrop-filter: none;
  		        backdrop-filter: none;
  		        pointer-events: auto;
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-computers-overview-toolbar-row {
  		        position: relative;
  		        z-index: 501;
  		        display: flex;
  		        align-items: center;
  		        gap: 10px;
  		        width: 100%;
  		        pointer-events: auto;
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-computers-overview-toolbar-row > *,
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-computers-overview-toolbar-row .playground-develop-server-kind-table-controls,
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-computers-overview-toolbar-row .playground-plugins-toolbar-controls,
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-computers-overview-toolbar-row .playground-files-toolbar-anchor,
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-computers-overview-toolbar-row .playground-develop-server-kind-search-shell,
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-computers-overview-toolbar-row .playground-plugins-search,
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-computers-overview-toolbar-row button {
  		        position: relative;
  		        z-index: 502;
  		        pointer-events: auto;
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-computers-overview-toolbar-row .playground-resources-overview-create-button {
  		        position: relative;
  		        z-index: 502;
  		        flex: 0 0 auto;
  		        pointer-events: auto;
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-computers-overview-sticky-table-header .playground-develop-server-kind-table-toolbar {
  		        flex-direction: row;
  		        align-items: center;
  		        justify-content: flex-start;
  		        gap: 10px;
  		        margin: 0;
  		        padding: 0 0 12px;
  		        border-bottom: 0;
  		        background: transparent;
  		        z-index: 1;
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-computers-overview-sticky-table-header .playground-develop-server-kind-table-controls {
  		        flex: 1 1 auto;
  		        width: auto;
  		        margin-left: 0;
  		        justify-content: flex-start;
  		        align-items: center;
  		        flex-wrap: nowrap;
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-computers-overview-sticky-table-header .playground-develop-server-kind-search-shell {
  		        flex: 0 1 340px;
  		        width: min(340px, 100%);
  		        min-width: min(280px, 100%);
  		        max-width: 340px;
  		        background: rgba(255, 255, 255, 0.025) !important;
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-computers-overview-sticky-table-header .playground-tasks-toolbar-popup-shell {
  		        z-index: 520;
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-computers-overview-sticky-table-header .playground-tasks-toolbar-popup-menu {
  		        z-index: 521;
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-computers-overview-sticky-table-header .playground-project-overview-threads-table-header {
  		        position: relative;
  		        z-index: 1;
  		        background: transparent;
  		        padding-top: 0;
  		        -webkit-backdrop-filter: none;
  		        backdrop-filter: none;
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-project-overview-threads-table-header.playground-computers-overview-column-header {
  		        margin-top: 12px;
  		        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-project-overview-threads-table {
  		        position: relative;
  		        z-index: 0;
  		        background: transparent;
  		        border: none;
  		        border-radius: 0;
  		        padding-left: 0;
  		        padding-right: 0;
  		        width: 100%;
  		        margin-left: 0;
  		        overflow: visible !important;
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-project-overview-thread-list {
  		        position: relative;
  		        z-index: 0;
  		        background: #000;
  		        border-radius: 10px;
  		        padding-left: 12px;
  		        padding-right: 12px;
  		        width: calc(100% + 24px);
  		        margin-left: -12px;
  		        border: 1px solid rgba(255, 255, 255, 0.1);
  		        overflow: visible !important;
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-project-overview-threads-table-row {
  		        min-height: 58px;
  		        padding-top: 12px;
  		        padding-bottom: 12px;
  		        overflow: visible !important;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section.is-develop-server-kind-list {
  		        z-index: 3;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-develop-resource-overview-sticky-table-header {
  		        pointer-events: auto;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-develop-resource-overview-toolbar-row {
  		        position: relative;
  		        z-index: 501;
  		        display: flex !important;
  		        align-items: center;
  		        justify-content: flex-start;
  		        flex-direction: row !important;
  		        flex-wrap: nowrap !important;
  		        gap: 10px;
  		        width: 100%;
  		        padding: 0 0 12px !important;
  		        margin: 0 !important;
  		        border-bottom: 0 !important;
  		        background: transparent;
  		        pointer-events: auto;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-develop-resource-overview-toolbar-row > *,
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-develop-resource-overview-toolbar-row .playground-develop-server-kind-table-controls,
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-develop-resource-overview-toolbar-row .playground-plugins-toolbar-controls,
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-develop-resource-overview-toolbar-row .playground-files-toolbar-anchor,
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-develop-resource-overview-toolbar-row .playground-develop-server-kind-search-shell,
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-develop-resource-overview-toolbar-row .playground-plugins-search,
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-develop-resource-overview-toolbar-row button {
  		        position: relative;
  		        z-index: 502;
  		        pointer-events: auto;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-develop-resource-overview-toolbar-row .playground-develop-server-kind-table-controls {
  		        flex: 1 1 auto;
  		        width: auto;
  		        min-width: 0;
  		        margin-left: 0;
  		        justify-content: flex-start;
  		        align-items: center;
  		        flex-wrap: nowrap;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-develop-resource-overview-toolbar-row .playground-develop-server-kind-search-shell {
  		        flex: 0 1 340px;
  		        width: min(340px, 100%);
  		        min-width: min(280px, 100%);
  		        max-width: 340px;
  		        background: rgba(255, 255, 255, 0.025) !important;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-develop-resource-overview-toolbar-row .playground-agents-overview-toolbar-create-button {
  		        flex: 0 0 auto;
  		        margin-left: auto;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-tasks-toolbar-popup-shell {
  		        z-index: 520;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-tasks-toolbar-popup-menu {
  		        z-index: 521;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-project-overview-threads-table-header,
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-project-overview-threads-table-row {
  		        grid-template-columns: 21px minmax(240px, 1.1fr) minmax(124px, 0.46fr) minmax(112px, 0.38fr) minmax(112px, 0.38fr) minmax(112px, 0.38fr) 28px !important;
  		        gap: 12px;
  		        width: 100%;
  		        max-width: 100%;
  		        box-sizing: border-box;
  		        padding-right: 0;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-project-overview-threads-table-header.playground-develop-resource-overview-column-header {
  		        margin-top: 12px;
  		        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  		      }
  
  		      .playground-develop-resource-overview-column-header.is-database > div:nth-child(5),
  		      .playground-develop-resource-overview-column-header.is-database > div:nth-child(6) {
  		        display: flex;
  		        justify-content: flex-end;
  		        text-align: right;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-project-overview-thread-cell,
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-resources-overview-name-title,
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-agents-overview-table-value {
  		        min-width: 0;
  		        overflow: hidden;
  		        text-overflow: ellipsis;
  		        white-space: nowrap;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-resources-overview-name-cell {
  		        gap: 8px;
  		        padding-right: 0;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-resources-overview-table-icon {
  		        width: 20px;
  		        height: 20px;
  		        flex-basis: 20px;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-resources-overview-table-icon svg {
  		        width: 16px;
  		        height: 16px;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-resources-overview-name-copy {
  		        gap: 0;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-resources-overview-name-description {
  		        display: none;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-project-overview-threads-table-header > div:first-child,
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-project-overview-thread-cell.is-select {
  		        display: flex;
  		        align-items: center;
  		        justify-content: center;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-project-overview-thread-cell.is-actions,
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-project-overview-threads-table-header > div:last-child {
  		        justify-self: end;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-resource-overview-table-section .playground-project-overview-thread-cell.is-actions {
  		        width: 100%;
  		        display: flex;
  		        align-items: center;
  		        justify-content: flex-end;
  		      }
  
  ${API_KEYS_STYLE_FRAGMENTS.page}
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-server-kind-table-title,
  		      .playground-resources-page.is-develop-configure-page .playground-develop-server-kind-table-title {
  		        flex: 0 0 auto;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-server-kind-table-controls,
  		      .playground-resources-page.is-develop-configure-page .playground-develop-server-kind-table-controls {
  		        min-width: 0;
  		        margin-left: auto;
  		        display: flex;
  		        align-items: center;
  		        justify-content: flex-end;
  		        gap: 10px;
  		      }
  
  		      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list > .playground-develop-server-kind-table-toolbar .playground-develop-server-kind-table-title {
  		        width: 100%;
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list > .playground-develop-server-kind-table-toolbar .playground-develop-server-kind-table-title {
  		        width: 100%;
  		      }
  
  		      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list > .playground-develop-server-kind-table-toolbar .playground-develop-server-kind-table-controls {
  		        width: 100%;
  		        margin-left: 0;
  		        justify-content: flex-start;
  		        align-items: center;
  		        flex-wrap: nowrap;
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list > .playground-develop-server-kind-table-toolbar .playground-develop-server-kind-table-controls {
  		        width: 100%;
  		        margin-left: 0;
  		        justify-content: flex-start;
  		      }
  
  		      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list > .playground-develop-server-kind-table-toolbar .playground-develop-server-kind-search-shell {
  		        flex: 1 1 auto;
  		        min-width: 0;
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list > .playground-develop-server-kind-table-toolbar .playground-develop-server-kind-search-shell {
  		        flex: 1 1 auto;
  		        min-width: 0;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-server-kind-search-shell,
  		      .playground-resources-page.is-develop-configure-page .playground-develop-server-kind-search-shell {
  		        --playground-project-overview-control-border: linear-gradient(
  		          -10deg,
  		          rgba(200, 200, 200, 0.25),
  		          rgba(255, 255, 255, 0.1),
  		          rgba(255, 255, 255, 0.15),
  		          rgba(255, 255, 255, 0.375)
  		        );
  		        position: relative;
  		        overflow: hidden;
  		        border-radius: 999px;
  		        -webkit-backdrop-filter: blur(20px);
  		        backdrop-filter: blur(20px);
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-server-kind-search-shell::before,
  		      .playground-resources-page.is-develop-configure-page .playground-develop-server-kind-search-shell::before {
  		        content: "";
  		        pointer-events: none;
  		        position: absolute;
  		        inset: 0;
  		        z-index: 2;
  		        border-radius: inherit;
  		        padding: 1px;
  		        background: var(--playground-project-overview-control-border);
  		        mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
  		        mask-clip: content-box, border-box;
  		        mask-composite: exclude;
  		        mask-origin: content-box, border-box;
  		        mask-repeat: repeat, repeat;
  		        mask-size: auto, auto;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .playground-develop-server-kind-search-shell .playground-plugins-search,
  			      .playground-resources-page.is-develop-configure-page .playground-develop-server-kind-search-shell .playground-plugins-search {
  			        border: 0;
  			        background: rgba(255, 255, 255, 0.04);
  			      }
  
  		      .playground-team-overview-page.is-develop-configure-page .playground-team-overview-table-section .playground-team-overview-toolbar-row .playground-develop-server-kind-search-shell .playground-plugins-search-icon {
  		        display: block !important;
  		        position: absolute !important;
  		        left: 12px !important;
  		        top: 50% !important;
  		        width: 14px !important;
  		        height: 14px !important;
  		        transform: translateY(-50%) !important;
  		        z-index: 504 !important;
  		        color: rgba(255, 255, 255, 0.42) !important;
  		        opacity: 1 !important;
  		        pointer-events: none !important;
  		      }
  
  		      .playground-team-overview-page.is-develop-configure-page .playground-team-overview-table-section .playground-team-overview-toolbar-row .playground-develop-server-kind-search-shell .playground-plugins-search-icon,
  		      .playground-team-overview-page.is-develop-configure-page .playground-team-overview-table-section .playground-team-overview-toolbar-row .playground-develop-server-kind-search-shell .playground-plugins-search-icon * {
  		        stroke: currentColor !important;
  		      }
  
  			      .playground-project-overview-threads-section .playground-plugins-search,
  			      .playground-project-overview-threads-section .playground-plugins-search-shell {
  			        background: transparent !important;
  			      }
  
  			      .playground-resources-page.is-develop-server-kind-page .is-develop-server-kind-list .playground-resources-overview-table-shell,
  			      .playground-resources-page.is-develop-configure-page .is-develop-server-kind-list .playground-resources-overview-table-shell,
  			      .playground-resources-page.is-develop-configure-page .is-develop-server-kind-list .playground-agents-overview-table-shell {
  		        --playground-project-overview-chart-border: linear-gradient(
  		          -10deg,
  		          rgba(200, 200, 200, 0.25),
  		          rgba(255, 255, 255, 0.1),
  		          rgba(255, 255, 255, 0.15),
  		          rgba(255, 255, 255, 0.375)
  		        );
  		        position: relative;
  		        border: 0;
  		        border-radius: 15px;
  		        background: transparent;
  		        -webkit-backdrop-filter: blur(50px);
  			        backdrop-filter: blur(50px);
  			      }
  
  			      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-resources-overview-table-shell,
  			      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list .playground-agents-overview-table-shell {
  			        margin-bottom: 24px;
  			      }
  
  		      .playground-agents-overview-page.is-develop-configure-page .playground-plugins-section.is-develop-server-kind-list .playground-agents-overview-table-shell {
  		        margin-bottom: 0;
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-resources-overview-table-shell {
  		        margin: 0;
  		        border: 0;
  		        border-radius: 0;
  		        background: transparent;
  		        -webkit-backdrop-filter: none;
  		        backdrop-filter: none;
  		      }
  
  		      .playground-resources-page.is-develop-configure-page .playground-resources-overview-section.is-computers-overview.is-develop-server-kind-list .playground-resources-overview-table-shell::before {
  		        content: none;
  		      }
  
  			      .playground-resources-page.is-develop-server-kind-page .is-develop-server-kind-list .playground-resources-overview-table-shell::before,
  			      .playground-resources-page.is-develop-configure-page .is-develop-server-kind-list .playground-resources-overview-table-shell::before,
  		      .playground-resources-page.is-develop-configure-page .is-develop-server-kind-list .playground-agents-overview-table-shell::before {
  		        content: "";
  		        pointer-events: none;
  		        position: absolute;
  		        inset: 0;
  		        z-index: 5;
  		        border-radius: inherit;
  		        padding: 1px;
  		        background: var(--playground-project-overview-chart-border);
  		        mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
  		        mask-clip: content-box, border-box;
  		        mask-composite: exclude;
  		        mask-origin: content-box, border-box;
  		        mask-repeat: repeat, repeat;
  		        mask-size: auto, auto;
  		      }
  
  		      .playground-resources-page.is-develop-server-kind-page .is-develop-server-kind-list .playground-resources-overview-table,
  		      .playground-resources-page.is-develop-configure-page .is-develop-server-kind-list .playground-resources-overview-table,
  		      .playground-resources-page.is-develop-configure-page .is-develop-server-kind-list .playground-agents-overview-table {
  		        position: relative;
  		        z-index: 1;
  		      }
  
  	      .playground-settings-page .playground-environments-home-metrics .playground-project-overview-chart-kpis {
  	        background: rgba(0, 0, 0, 0.35);
  	      }
  
        .playground-settings-usage-top-chart {
          width: 100%;
        }
  
        .playground-settings-page .playground-settings-usage-top-chart .playground-database-overview-chart-block {
          --playground-project-overview-chart-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          position: relative;
          box-sizing: border-box;
          width: 100%;
          padding: 20px;
          border: 0;
          border-radius: 15px;
          background: transparent;
          overflow: hidden;
          -webkit-backdrop-filter: blur(50px);
          backdrop-filter: blur(50px);
        }
  
        .playground-settings-page .playground-settings-usage-top-chart .playground-database-overview-chart-block::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          z-index: 5;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-project-overview-chart-border);
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-settings-page .playground-settings-usage-top-chart .playground-database-overview-chart-block > * {
          position: relative;
          z-index: 1;
        }
  
        .playground-settings-page .playground-settings-usage-top-chart .playground-project-overview-chart-kpis {
          width: calc(100% + 40px);
          margin-top: -20px;
          margin-left: -20px;
          margin-right: -20px;
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-settings-usage-chart-kpis {
          grid-template-columns: repeat(5, minmax(0, 1fr));
          gap: 0;
          padding: 0;
        }
  
        .playground-settings-page .playground-settings-usage-chart-kpi {
          appearance: none;
          min-height: 68px;
          padding: 16px 20px 14px;
          border: 0;
          border-bottom: 1px solid transparent;
          background: transparent;
          color: inherit;
          font: inherit;
          text-align: left;
          cursor: pointer;
          transition: background-color 160ms ease, border-color 160ms ease, color 160ms ease;
        }
  
        .playground-settings-usage-chart-kpi.is-active {
          background: rgba(255, 255, 255, 0.05);
          border-bottom-color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-settings-usage-chart-kpi:focus-visible {
          outline: 2px solid rgba(255, 255, 255, 0.22);
          outline-offset: -2px;
        }
  
        .playground-settings-usage-chart-panel {
          padding-top: 0;
        }
  
        .playground-settings-usage-chart-panel .playground-settings-usage-chart-frame {
          margin-top: 6px;
        }
  
        .playground-settings-usage-chart-loading-frame {
          height: 288px;
          min-height: 288px;
          box-sizing: border-box;
          margin-top: 6px;
          border: 0;
          border-radius: 0;
          background: transparent;
        }
  
        .playground-agents-page .playground-environments-home-metrics .playground-project-overview-chart-kpis,
        .playground-agents-overview-page .playground-environments-home-metrics .playground-project-overview-chart-kpis,
        .playground-resources-page .playground-environments-home-metrics .playground-project-overview-chart-kpis {
          background: rgba(0, 0, 0, 0.35);
        }
  
        .playground-agents-overview-page .playground-environments-home-metrics .playground-project-overview-chart-kpis {
          margin-bottom: 6px;
        }
  
        .playground-agents-page .playground-environments-home-metrics .playground-settings-usage-chart-card,
        .playground-agents-overview-page .playground-environments-home-metrics .playground-settings-usage-chart-card,
        .playground-resources-page .playground-environments-home-metrics .playground-settings-usage-chart-card {
          position: relative;
        }
  
        .playground-overview-chart-loading {
          position: absolute;
          inset: 0;
          z-index: 2;
          display: flex;
          align-items: center;
          justify-content: center;
          pointer-events: none;
        }
  
        .playground-overview-chart-loading-icon {
          width: 22px;
          height: 22px;
          color: rgba(255, 255, 255, 0.82);
          animation: spinner-rotate 1s linear infinite;
        }
  
        .playground-agents-detail-summary.playground-environments-home-summary {
          margin: 0 0 18px;
          padding-top: 20px;
          border-top: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-agents-page .playground-agents-detail-main-pane .playground-environments-editor-main.playground-tasks-detail-main,
        .playground-agents-detail-assistant-page .playground-agents-detail-main-pane .playground-environments-editor-main.playground-tasks-detail-main {
          min-height: 0;
          overflow: visible;
        }
  
        .playground-agents-page .playground-agents-detail-main-pane .playground-environments-detail-scroll,
        .playground-agents-detail-assistant-page .playground-agents-detail-main-pane > .playground-environments-detail-scroll.playground-settings-detail-scroll {
          padding: 0 44px 56px;
          overflow-x: hidden;
        }
  
        .playground-agents-detail-assistant-page .playground-agents-detail-main-pane .playground-environments-detail-scroll.playground-environments-editor-scroll {
          padding: 0;
          overflow: visible;
        }
  
        .playground-agents-page .playground-agents-detail-content,
        .playground-agents-detail-assistant-page .playground-agents-detail-content {
          width: min(100%, var(--playground-centered-page-max-width));
          max-width: var(--playground-centered-page-max-width);
        }
  
        .playground-agents-page .playground-agents-detail-main-pane .playground-agents-detail-content,
        .playground-agents-detail-assistant-page .playground-agents-detail-main-pane .playground-agents-detail-content {
          padding-top: 42px;
        }
  
        .playground-agents-detail-content .playground-tasks-detail-facts,
        .playground-agents-detail-content .playground-environments-section,
        .playground-agents-detail-content .playground-environments-mcp-card,
        .playground-agents-detail-content .playground-environments-editor-surface,
        .playground-agents-detail-content .playground-tasks-detail-description.playground-environments-editor-description {
          margin-top: 0;
          padding: 0 0 3px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 15px;
          background: rgba(255, 255, 255, 0.075);
          box-shadow: none;
          overflow: hidden !important;
          backdrop-filter: none;
          -webkit-backdrop-filter: none;
        }
  
        .playground-agents-detail-content .playground-tasks-detail-facts::before,
        .playground-agents-detail-content .playground-environments-section::before,
        .playground-agents-detail-content .playground-environments-mcp-card::before,
        .playground-agents-detail-content .playground-environments-editor-surface::before,
        .playground-agents-detail-content .playground-tasks-detail-description.playground-environments-editor-description::before {
          content: none;
          display: none;
        }
  
        .playground-agents-detail-content .playground-environments-section-header,
        .playground-agents-detail-content .playground-environments-section-header.is-static {
          padding: 0 0 12px;
          margin-top: 12px;
          margin-bottom: 12px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-agents-detail-content .playground-environments-section-body {
          padding: 0;
          border-top: 0;
        }
  
        .playground-agents-detail-content .playground-environments-stack,
        .playground-agents-detail-content .playground-environments-field-grid {
          padding-top: 0;
        }
  
        .playground-agents-detail-content .playground-tasks-detail-facts-body,
        .playground-agents-detail-content .playground-database-overview {
          gap: 0;
          padding: 0;
        }
  
        .playground-agents-detail-content .playground-agents-profile-section {
          position: relative;
          z-index: 320;
          width: 100%;
          min-width: 0;
          display: flex;
          flex-direction: row;
          align-items: center;
          justify-content: flex-start;
          gap: 14px;
        }
  
        .playground-agents-detail-content .playground-agents-profile-avatar-wrap {
          position: relative;
          z-index: 321;
          flex: 0 0 auto;
          overflow: visible;
        }
  
        .playground-agents-detail-content .playground-agents-profile-avatar.profile-editor-avatar {
          z-index: 322;
          width: 48px;
          height: 48px;
          overflow: visible;
        }
  
        .playground-agents-detail-avatar-surface-button.profile-editor-avatar-surface {
          padding: 0;
          border: 0;
          font: inherit;
          cursor: pointer;
          -webkit-appearance: none;
          appearance: none;
        }
  
        .playground-agents-detail-avatar-surface-button.profile-editor-avatar-surface:hover {
          background: rgba(255, 255, 255, 0.14);
        }
  
        .playground-agents-detail-avatar-surface-button.profile-editor-avatar-surface:focus-visible {
          outline: 2px solid rgba(255, 255, 255, 0.72);
          outline-offset: 3px;
        }
  
        .playground-agents-detail-content .playground-agents-avatar-picker-shell.playground-tasks-toolbar-popup-shell {
          left: 0;
          right: auto;
          top: 0;
          bottom: auto;
          z-index: 2147482000;
          width: 100%;
          height: 100%;
          pointer-events: none;
        }
  
        .playground-agents-detail-content .playground-agents-avatar-picker-menu {
          left: 0;
          right: auto;
          top: calc(100% + 8px);
          z-index: 2147482001;
          pointer-events: auto;
        }
  
        .playground-agents-profile-copy {
          min-width: 0;
          flex: 1 1 auto;
          display: flex;
          flex-direction: column;
          align-items: flex-start;
          gap: 7px;
        }
  
        .playground-agents-detail-profile-actions,
        .playground-agents-detail-sidebar-controls {
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          gap: 8px;
        }
  
        .playground-agents-detail-profile-actions {
          margin-left: auto;
        }
  
        .playground-tags-detail-profile-actions.playground-agents-detail-profile-actions,
        .playground-tags-detail-sidebar-controls.playground-agents-detail-sidebar-controls {
          justify-content: flex-end;
          gap: 6px;
        }
  
        .playground-tags-detail-sidebar-controls.playground-agents-detail-sidebar-controls {
          width: auto;
          margin-left: auto;
          margin-top: 2px;
        }
  
        .playground-tags-detail-profile-actions .playground-plugin-detail-connect-button,
        .playground-tags-detail-sidebar-controls .playground-plugin-detail-connect-button {
          margin-right: 0;
          min-height: 30px;
          padding-left: 11px;
          padding-right: 11px;
          font-size: 12px;
          font-weight: 400;
        }
  
        .playground-tags-detail-profile-actions .playground-project-overview-sidebar-toggle,
        .playground-tags-detail-sidebar-controls .playground-project-overview-sidebar-toggle {
          margin: 0;
        }
  
        .playground-agents-detail-header-publish-button.playground-metronome-create-button {
          flex: 0 0 auto;
          min-width: 0;
        }
  
        .playground-agents-detail-version-selector-row {
          width: 100%;
          display: flex;
          align-items: center;
          justify-content: flex-start;
          gap: 24px;
          margin: 0;
          min-width: 0;
        }
  
        .playground-agents-detail-publish-split-shell.playground-tasks-toolbar-popup-shell {
          z-index: 74;
          display: inline-flex;
          align-items: center;
        }
  
        .playground-agents-detail-version-selector-shell.playground-agents-detail-publish-split-shell.playground-tasks-toolbar-popup-shell {
          z-index: 73;
        }
  
        .playground-agents-detail-version-selector-shell {
          position: relative;
          display: inline-flex;
          align-items: center;
          flex: 0 0 auto;
          width: max-content;
          max-width: 100%;
          min-width: 0;
        }
  
        .playground-agents-detail-version-selector-control {
          flex: 0 0 auto;
          width: auto;
          max-width: 100%;
          -webkit-backdrop-filter: blur(20px);
          backdrop-filter: blur(20px);
        }
  
        .playground-agents-detail-version-selector-button.platform-button {
          max-width: min(260px, 52vw);
        }
  
        .playground-agents-detail-top-controls-actions.playground-agents-detail-sidebar-controls {
          width: auto;
          margin: 0;
          justify-content: flex-end;
          gap: 6px;
        }
  
        .playground-agents-detail-publish-split-control.playground-metronome-create-button {
          padding: 0;
          gap: 0;
          cursor: default;
        }
  
        .playground-agents-detail-header-publish-button.playground-agents-detail-publish-split-control:not(.playground-agents-detail-version-selector-control):not(.playground-computer-detail-header-publish-button):not(.playground-server-detail-header-publish-button):not(.playground-guardrails-publish-button):not(.playground-evaluations-publish-button) {
          background: linear-gradient(to top, #082673, #1D59BE);
        }
  
        .playground-computer-detail-header-publish-button.playground-agents-detail-publish-split-control:not(.playground-agents-detail-version-selector-control),
        .playground-server-detail-header-publish-button.playground-agents-detail-publish-split-control:not(.playground-agents-detail-version-selector-control) {
          background: linear-gradient(to top, #082673, #1D59BE);
        }
  
        .playground-agents-detail-header-publish-button.playground-agents-detail-publish-split-control:not(.playground-agents-detail-version-selector-control):not(.playground-computer-detail-header-publish-button):not(.playground-server-detail-header-publish-button):not(.playground-guardrails-publish-button):not(.playground-evaluations-publish-button):hover,
        .playground-agents-detail-header-publish-button.playground-agents-detail-publish-split-control:not(.playground-agents-detail-version-selector-control):not(.playground-computer-detail-header-publish-button):not(.playground-server-detail-header-publish-button):not(.playground-guardrails-publish-button):not(.playground-evaluations-publish-button):focus-within,
        .playground-agents-detail-header-publish-button.playground-agents-detail-publish-split-control:not(.playground-agents-detail-version-selector-control):not(.playground-computer-detail-header-publish-button):not(.playground-server-detail-header-publish-button):not(.playground-guardrails-publish-button):not(.playground-evaluations-publish-button).is-active,
        .playground-computer-detail-header-publish-button.playground-agents-detail-publish-split-control:not(.playground-agents-detail-version-selector-control):hover,
        .playground-computer-detail-header-publish-button.playground-agents-detail-publish-split-control:not(.playground-agents-detail-version-selector-control):focus-within,
        .playground-computer-detail-header-publish-button.playground-agents-detail-publish-split-control:not(.playground-agents-detail-version-selector-control).is-active,
        .playground-server-detail-header-publish-button.playground-agents-detail-publish-split-control:not(.playground-agents-detail-version-selector-control):hover,
        .playground-server-detail-header-publish-button.playground-agents-detail-publish-split-control:not(.playground-agents-detail-version-selector-control):focus-within,
        .playground-server-detail-header-publish-button.playground-agents-detail-publish-split-control:not(.playground-agents-detail-version-selector-control).is-active {
          background: linear-gradient(to top, #082673, #1D59BE);
        }
  
        .playground-agents-detail-publish-split-control.is-disabled {
          opacity: 0.5;
        }
  
        .playground-agents-detail-publish-main,
        .playground-agents-detail-publish-chevron {
          position: relative;
          z-index: 1;
          height: 30px;
          min-height: 30px;
          border: 0;
          background: transparent;
          color: inherit;
          font: inherit;
          font-size: 12px;
          font-weight: 400;
          line-height: 1;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
        }
  
        .playground-agents-detail-publish-main {
          gap: 8px;
          padding: 0 10px 0 14px;
        }
  
        .playground-agents-detail-version-selector-main.playground-agents-detail-publish-main {
          min-width: 0;
          max-width: min(260px, 52vw);
        }
  
        .playground-agents-detail-version-selector-icon {
          width: 13px;
          height: 13px;
          flex: 0 0 auto;
          color: currentColor;
        }
  
        .playground-agents-detail-version-selector-label {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-agents-version-count-label {
          height: 30px;
          min-height: 30px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 6px;
          margin-left: 6px;
          color: rgba(255, 255, 255, 0.82);
          font: inherit;
          font-size: 12px;
          font-weight: 400;
          line-height: 1;
          white-space: nowrap;
          user-select: none;
        }
  
        .playground-agents-version-count-icon {
          width: 13px;
          height: 13px;
          flex: 0 0 auto;
          color: currentColor;
        }
  
        .playground-agents-tags-control-shell.playground-tasks-toolbar-popup-shell {
          position: relative;
          display: inline-flex;
          align-items: center;
          margin-left: 4px;
          width: auto;
          height: auto;
          min-height: 0;
          overflow: visible;
        }
  
        .playground-agents-tags-control-button {
          height: 30px;
          min-height: 30px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 6px;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.82);
          font: inherit;
          font-size: 12px;
          font-weight: 400;
          line-height: 1;
          cursor: pointer;
        }
  
        .playground-agents-tags-control-button::before {
          content: none;
          display: none;
        }
  
        .playground-agents-tags-control-button:hover,
        .playground-agents-tags-control-button.is-active {
          color: white;
          background: transparent;
        }
  
        .playground-agents-tags-control-icon {
          width: 13px;
          height: 13px;
          flex: 0 0 auto;
        }
  
        .playground-agents-tags-count {
          min-width: 16px;
          height: 16px;
          padding: 0 5px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.1);
          color: rgba(255, 255, 255, 0.7);
          font-size: 10px;
          font-weight: 400;
          line-height: 16px;
          text-align: center;
        }
  
        .playground-agents-tags-control-shell.playground-tasks-toolbar-popup-shell .playground-agents-tags-menu {
          right: auto;
          left: 0;
          width: 260px;
          min-width: 260px;
          padding: 10px;
          transform-origin: top left;
        }
  
        .playground-agents-tags-menu-content {
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-agents-tags-form {
          display: flex;
          align-items: center;
          gap: 8px;
        }
  
        .playground-agents-tags-input {
          flex: 1 1 auto;
          min-width: 0;
          height: 28px;
          padding: 0 10px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.06);
          color: rgba(255, 255, 255, 0.92);
          font: inherit;
          font-size: 12px;
          outline: none;
        }
  
        .playground-agents-tags-input::placeholder {
          color: rgba(255, 255, 255, 0.36);
        }
  
        .playground-agents-tags-add-button {
          width: 28px;
          height: 28px;
          padding: 0;
          border: 0;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.1);
          color: rgba(255, 255, 255, 0.9);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
        }
  
        .playground-agents-tags-add-button:disabled {
          opacity: 0.42;
          cursor: not-allowed;
        }
  
        .playground-agents-tags-list {
          display: flex;
          flex-wrap: wrap;
          gap: 6px;
        }
  
        .playground-agents-tags-chip {
          max-width: 100%;
          min-height: 24px;
          display: inline-flex;
          align-items: center;
          gap: 5px;
          padding: 0 6px 0 9px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.06);
          color: rgba(255, 255, 255, 0.86);
          font-size: 12px;
          line-height: 1;
        }
  
        .playground-agents-tags-chip-label {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-agents-tags-chip-remove {
          width: 16px;
          height: 16px;
          padding: 0;
          border: 0;
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.58);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
        }
  
        .playground-agents-tags-chip-remove:hover {
          color: white;
        }
  
        .playground-agents-tags-empty {
          color: rgba(255, 255, 255, 0.44);
          font-size: 12px;
          line-height: 1.4;
        }
  
        .playground-agents-detail-publish-chevron {
          width: 30px;
          padding: 0;
        }
  
        .playground-agents-detail-publish-main:disabled,
        .playground-agents-detail-publish-chevron:disabled {
          cursor: not-allowed;
        }
  
        .playground-agents-detail-publish-main:focus-visible,
        .playground-agents-detail-publish-chevron:focus-visible {
          outline: none;
        }
  
        .playground-agents-detail-publish-divider {
          position: relative;
          z-index: 1;
          width: 1px;
          height: 14px;
          flex: 0 0 1px;
          background: rgba(255, 255, 255, 0.18);
        }
  
  	      .playground-agents-detail-publish-split-shell .playground-agents-detail-publish-menu {
  	        top: calc(100% + 8px);
  	        right: 0;
  	        left: auto;
  	        width: 268px;
  	        min-width: 268px;
  	        max-height: min(260px, calc(100vh - 160px));
  	        transform-origin: top right;
  	      }
  
  	      .playground-agents-detail-publish-menu .tb-popup-row {
  	        padding: 10px 14px;
  	      }
  
  	      .playground-agents-detail-version-selector-shell .playground-agents-detail-version-selector-menu.playground-agents-detail-publish-menu {
  	        right: auto !important;
  	        left: 0 !important;
  	        width: 284px;
  	        min-width: 284px;
  	        max-height: min(340px, calc(100vh - 190px));
  	        overflow: hidden;
  	        transform-origin: top left;
  	      }
  
        .playground-agents-detail-version-selector-list {
          max-height: min(278px, calc(100vh - 248px));
          overflow-y: auto;
          overscroll-behavior: contain;
        }
  
        .playground-agents-detail-version-selector-empty {
          padding: 12px 14px;
          color: rgba(255, 255, 255, 0.46);
          font-size: 12px;
          line-height: 1.35;
        }
  
        .playground-agents-detail-version-selector-footer {
          border-top: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-agents-detail-version-selector-new-button.tb-popup-row {
          width: 100%;
          align-items: center;
        }
  
        .playground-agents-detail-version-selector-option.tb-popup-row {
          align-items: center;
          gap: 10px;
        }
  
        .playground-agents-detail-version-selector-option-check {
          width: 14px;
          height: 14px;
          flex: 0 0 14px;
          color: rgba(255, 255, 255, 0.9);
          display: inline-flex;
          align-items: center;
          justify-content: center;
        }
  
        .playground-agents-detail-version-selector-option-copy {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 2px;
        }
  
        .playground-agents-detail-version-selector-option-title {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
          color: rgba(255, 255, 255, 0.92);
          font-size: 12px;
          font-weight: 500;
          line-height: 1.25;
        }
  
        .playground-agents-detail-version-selector-option-meta {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
          color: rgba(255, 255, 255, 0.46);
          font-size: 11px;
          font-weight: 400;
          line-height: 1.25;
        }
  
  	      .playground-agents-detail-publish-menu .playground-agents-detail-publish-menu-shortcut {
  	        margin-left: auto;
  	        flex: 0 0 auto;
  	        color: rgba(255, 255, 255, 0.42);
  	        font-size: 10px;
  	        font-weight: 400;
  	        line-height: 1;
  	        letter-spacing: 0;
  	        font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  	      }
  
  	      .playground-agents-detail-publish-menu .tb-popup-row:disabled {
  	        cursor: not-allowed;
  	        color: rgba(255, 255, 255, 0.42);
  	      }
  
        .playground-agents-detail-publish-menu .tb-popup-row:disabled:hover {
          background: transparent;
        }
  
        .playground-agents-detail-content .playground-agents-profile-name-wrap {
          width: auto;
          max-width: 100%;
          align-items: center;
          gap: 12px;
          justify-content: flex-start;
          margin-top: 0;
        }
  
        .playground-agents-detail-content .playground-agents-profile-name-wrap .playground-agents-profile-name-input {
          width: auto;
          max-width: min(620px, 100%);
          min-width: 2ch;
          flex: 0 1 auto;
          field-sizing: content;
          font-size: 18px;
          line-height: 1;
          font-weight: 500;
          letter-spacing: 0;
          text-align: left;
        }
  
        .playground-agents-visibility-label {
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          min-height: 22px;
          padding: 0 9px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 999px;
          color: white;
          font-size: 12px;
          font-weight: 400;
          line-height: 1;
        }
  
        .playground-tags-detail-profile-title.playground-agents-profile-name-input {
          margin: 0;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-tags-detail-avatar-surface.profile-editor-avatar-surface {
          color: rgba(255, 255, 255, 0.88);
        }
  
        .playground-tags-detail-profile-section .playground-agents-profile-avatar-wrap {
          display: none;
        }
  
        .playground-agents-detail-content.is-agent-overview-general {
          width: 100%;
          max-width: none;
        }
  
        .playground-tags-detail-content.playground-plugin-detail-content.playground-agents-detail-content.is-agent-overview-general {
          width: min(100%, var(--playground-centered-page-max-width));
          max-width: var(--playground-centered-page-max-width);
          margin-left: auto;
          margin-right: auto;
        }
  
        .playground-computer-detail-content.playground-agents-detail-content.is-agent-overview-general {
          width: min(100%, var(--playground-centered-page-max-width));
          max-width: var(--playground-centered-page-max-width);
          margin-left: auto;
          margin-right: auto;
        }
  
        .playground-computer-detail-main .playground-environments-detail-scroll.playground-environments-editor-scroll {
          padding: 0;
          overflow: visible;
        }
  
        .playground-computer-detail-profile-section.playground-agents-profile-section {
          width: 100%;
          min-width: 0;
        }
  
        .playground-computer-detail-profile-avatar.profile-editor-avatar {
          width: 48px;
          height: 48px;
        }
  
        .playground-computer-detail-avatar-surface.profile-editor-avatar-surface {
          color: rgba(255, 255, 255, 0.88);
        }
  
        .playground-computer-detail-profile-title.playground-agents-profile-name-input {
          margin: 0;
        }
  
        .playground-computer-detail-profile-actions.playground-agents-detail-profile-actions,
        .playground-computer-detail-sidebar-controls.playground-agents-detail-sidebar-controls {
          justify-content: flex-end;
          gap: 6px;
        }
  
        .playground-computer-detail-sidebar-controls.playground-agents-detail-sidebar-controls {
          width: auto;
          margin-left: auto;
          margin-top: 2px;
        }
  
        .playground-computer-detail-top-controls-actions.playground-computer-detail-sidebar-controls.playground-agents-detail-sidebar-controls {
          width: auto;
          margin: 0;
          justify-content: flex-end;
          gap: 6px;
        }
  
        .playground-computer-detail-tabs .playground-project-overview-chart-tabs {
          width: 100%;
          gap: 24px;
        }
  
        .playground-computer-detail-advanced-tab .playground-environments-section,
        .playground-computer-detail-advanced-tab .playground-environments-mcp-card,
        .playground-computer-detail-advanced-tab .playground-environments-editor-surface,
        .playground-computer-detail-advanced-tab .playground-tasks-detail-description {
          border: 0 !important;
        }
  
        .playground-computer-detail-advanced-tab .playground-environments-section {
          margin-bottom: 24px;
        }
  
        .playground-computer-detail-advanced-tab .playground-environments-section-header,
        .playground-computer-detail-advanced-tab .playground-environments-section-header.is-static {
          border-bottom: 1px solid rgba(255, 255, 255, 0.1) !important;
        }
  
        .playground-computer-detail-advanced-tab .playground-environments-section-title {
          font-size: 12px;
        }
  
        .playground-computer-detail-profile-actions .playground-plugin-detail-connect-button,
        .playground-computer-detail-sidebar-controls .playground-plugin-detail-connect-button {
          margin-right: 0;
          min-height: 30px;
          padding-left: 11px;
          padding-right: 11px;
          font-size: 12px;
          font-weight: 400;
        }
  
        .playground-computer-detail-profile-actions .playground-project-overview-sidebar-toggle,
        .playground-computer-detail-sidebar-controls .playground-project-overview-sidebar-toggle {
          margin: 0;
        }
  
        .playground-agents-detail-overview-layout.playground-project-overview-layout {
          column-gap: 42px;
          row-gap: 20px;
        }
  
        .playground-agents-detail-overview-layout.is-sidebar-collapsed {
          grid-template-columns: minmax(0, 1fr) minmax(0, 0px);
          column-gap: 0;
        }
  
        .playground-agents-detail-overview-layout.is-sidebar-collapsed > .playground-agents-detail-sidebar {
          opacity: 0;
          transform: translateX(18px);
          visibility: hidden;
          pointer-events: none;
          transition-delay: 0s, 0s, 180ms;
        }
  
        .playground-agents-detail-overview-main.playground-project-overview-main {
          gap: 20px;
          backdrop-filter: blur(20px);
          -webkit-backdrop-filter: blur(20px);
        }
  
        .playground-agents-detail-overview-main.playground-project-overview-main.is-permissions-tab {
          backdrop-filter: none;
          -webkit-backdrop-filter: none;
        }
  
        .playground-agents-detail-chart-surface.playground-project-overview-chart-surface {
          margin: 0 0 24px;
        }
  
        .playground-agents-detail-chart-card.playground-project-overview-chart-card {
          gap: 12px;
        }
  
        .playground-agents-detail-chart-card .playground-project-overview-chart-kpis {
          margin-bottom: 12px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-agents-detail-chart-card .playground-project-overview-chart-header {
          display: flex;
        }
  
        .playground-agents-detail-chart-card .playground-settings-usage-chart-empty {
          min-height: 270px;
        }
  
        .playground-agents-detail-threads-section .playground-project-overview-threads-table-header,
        .playground-agents-detail-threads-section .playground-project-overview-threads-table-row {
          grid-template-columns: minmax(0, 2fr) minmax(86px, 0.62fr) minmax(0, 1.08fr) minmax(0, 1.2fr) minmax(82px, 0.62fr) 28px;
        }
  
        .playground-agents-detail-threads-section .playground-project-overview-threads-table {
          max-width: 100%;
          min-width: 0;
          overflow: hidden;
        }
  
        .playground-team-members-table-section.playground-agents-detail-threads-section .playground-project-overview-threads-table-header,
        .playground-team-members-table-section.playground-agents-detail-threads-section .playground-project-overview-threads-table-row {
          grid-template-columns: 21px minmax(260px, 1.35fr) minmax(150px, 0.52fr) minmax(120px, 0.38fr) minmax(120px, 0.38fr) 28px !important;
          gap: 12px;
        }
  
        .playground-team-members-table-section.playground-agents-detail-threads-section .playground-project-overview-threads-table {
          overflow: visible !important;
        }
  
        .playground-team-members-table-section.playground-agents-detail-threads-section .playground-project-overview-thread-list {
          overflow: visible !important;
        }
  
        .playground-team-members-table-section.playground-agents-detail-threads-section .playground-team-members-sticky-table-header {
          z-index: 500;
        }
  
  ${EVALUATIONS_AGENT_STYLE_FRAGMENTS.page}
        .playground-agents-detail-threads-section .playground-project-overview-thread-cell.is-source,
        .playground-agents-detail-threads-section .playground-project-overview-thread-cell.is-environment,
        .playground-agents-detail-threads-section .playground-project-overview-thread-cell.is-triggered-by {
          font-size: 12px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.62);
          overflow: visible;
          text-overflow: clip;
        }
  
        .playground-agents-detail-threads-section .playground-project-overview-thread-cell.is-source {
          color: rgba(255, 255, 255, 0.74);
          white-space: nowrap;
        }
  
        .playground-agents-detail-threads-section .playground-project-overview-thread-cell.is-date {
          white-space: nowrap;
        }
  
        .playground-agents-detail-thread-triggered-by {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          gap: 8px;
          max-width: 100%;
        }
  
        .playground-agents-detail-thread-user-avatar {
          width: 20px;
          height: 20px;
          flex: 0 0 20px;
          border-radius: 999px;
          overflow: hidden;
          font-size: 9px;
          font-weight: 500;
        }
  
        .playground-agents-detail-thread-user-avatar-image {
          width: 100%;
          height: 100%;
          object-fit: cover;
        }
  
        .playground-agents-detail-thread-user-avatar-fallback {
          width: 100%;
          height: 100%;
          display: flex;
          align-items: center;
          justify-content: center;
          background: rgba(255, 255, 255, 0.12);
          color: rgba(255, 255, 255, 0.84);
        }
  
        .playground-agents-detail-thread-triggered-by-label {
          min-width: 0;
          color: rgba(255, 255, 255, 0.72);
          white-space: normal;
          overflow: visible;
          text-overflow: clip;
        }
  
  ${GUARDRAILS_STYLE_FRAGMENTS.agentIntegration}      .playground-project-overview-progress-combo-card {
          margin-top: 0;
        }
  
        .playground-project-overview-progress-combo-metric-value {
          font-size: 24px;
          font-weight: 400;
        }
  
        .playground-agents-detail-progress-combo-card .playground-project-overview-progress-combo-metric-dot.is-total-runs {
          background: #7effff;
          box-shadow: 0 0 0 3px rgba(126, 255, 255, 0.12);
        }
  
        .playground-agents-detail-progress-combo-card .playground-project-overview-progress-combo-metric-dot.is-cost {
          background: rgb(95, 112, 230);
          box-shadow: 0 0 0 3px rgba(95, 112, 230, 0.12);
        }
  
        .playground-agents-detail-progress-combo-card .playground-project-overview-progress-combo-metric-dot.is-avg-ct {
          background: #4da3ff;
          box-shadow: 0 0 0 3px rgba(77, 163, 255, 0.12);
        }
  
        .playground-agents-detail-progress-combo-card .playground-project-overview-progress-combo-metric-dot.is-success-rate {
          background: #54e5a6;
          box-shadow: 0 0 0 3px rgba(84, 229, 166, 0.12);
        }
  
        .playground-computer-detail-progress-combo-card .playground-project-overview-progress-combo-metric-dot.is-runs {
          background: #7effff;
          box-shadow: 0 0 0 3px rgba(126, 255, 255, 0.12);
        }
  
        .playground-computer-detail-progress-combo-card .playground-project-overview-progress-combo-metric-dot.is-success-rate {
          background: #54e5a6;
          box-shadow: 0 0 0 3px rgba(84, 229, 166, 0.12);
        }
  
        .playground-computer-detail-progress-combo-card .playground-project-overview-progress-combo-metric-dot.is-runtime {
          background: #4da3ff;
          box-shadow: 0 0 0 3px rgba(77, 163, 255, 0.12);
        }
  
        .playground-computer-detail-progress-combo-card .playground-project-overview-progress-combo-metric-dot.is-failed {
          background: #f87171;
          box-shadow: 0 0 0 3px rgba(248, 113, 113, 0.12);
        }
  
        .playground-computer-detail-progress-combo-card .playground-environments-home-comparison-timescale {
          margin: 0;
        }
  
        .playground-computer-detail-progress-combo-chart .playground-settings-usage-chart-block,
        .playground-computer-detail-progress-combo-chart .playground-settings-usage-chart {
          width: 100%;
        }
  
        .playground-agents-detail-instructions-section {
          margin-bottom: 42px;
          overflow: visible !important;
        }
  
        .playground-agents-detail-content .playground-tasks-detail-description.playground-environments-editor-description.playground-agents-detail-instructions-section {
          overflow: visible !important;
        }
  
        .playground-agents-detail-instructions-section.is-header-stuck {
          border-top-left-radius: 0 !important;
          border-top-right-radius: 0 !important;
        }
  
        .playground-agents-detail-instructions-section .playground-tasks-detail-section-header {
          position: sticky;
          top: 0;
          z-index: 8;
          background: #121212;
          border-top-left-radius: 15px;
          border-top-right-radius: 15px;
          padding: 12px 20px 10px;
          border-bottom: 0;
        }
  
        .playground-agents-detail-instructions-section.is-header-stuck .playground-tasks-detail-section-header {
          border-top-left-radius: 0;
          border-top-right-radius: 0;
        }
  
        .playground-tasks-detail-section-header.is-static-transparent {
          position: static !important;
          top: auto !important;
          z-index: auto !important;
          background: transparent !important;
        }
  
        .playground-agents-detail-instructions-section .playground-tasks-detail-description-editor {
          display: block;
          box-sizing: border-box;
          width: calc(100% - 6px);
          height: auto;
          min-height: 0;
          margin-left: 3px;
          padding-top: 20px;
          padding-left: 20px;
          padding-right: 20px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 10px;
          background: black;
          overflow: visible;
        }
  
        .playground-agents-detail-instructions-section .playground-tasks-detail-description-editor.is-preview .playground-tasks-detail-description-preview-scope.tb-runner-chat {
          position: relative;
          inset: auto;
          display: block;
          width: 100%;
          height: auto !important;
          min-height: 0 !important;
          overflow: visible;
        }
  
        .playground-agents-detail-instructions-section .playground-tasks-detail-description-editor.is-preview .playground-tasks-detail-description-input {
          position: absolute;
          inset: 0;
          height: 100% !important;
          min-height: 100%;
          padding: 0;
          opacity: 0;
        }
  
        .playground-agents-detail-instructions-section .playground-tasks-detail-format-actions {
          flex-wrap: nowrap;
        }
  
        .playground-agents-detail-instructions-toolbar-divider {
          width: 1px;
          height: 16px;
          margin: 0 2px;
          flex: 0 0 1px;
          background: rgba(255, 255, 255, 0.14);
        }
  
        .playground-agents-detail-instructions-section .playground-tasks-detail-format-button:disabled {
          color: rgba(255, 255, 255, 0.28);
          opacity: 1;
        }
  
        .playground-agents-detail-sidebar .playground-project-overview-sidebar-row {
          grid-template-columns: minmax(90px, 0.85fr) minmax(0, 1.35fr);
        }
  
        .playground-agents-detail-sidebar.playground-project-overview-sidebar {
          top: 12px;
          margin-top: -4px;
          overflow: visible;
        }
  
        .playground-agents-detail-sidebar-controls {
          width: 100%;
          min-height: 30px;
          margin-bottom: 0;
        }
  
        .playground-agents-detail-sidebar-copy-button {
          flex: 0 0 auto;
          width: 18px;
          height: 18px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border: 0;
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.62);
          padding: 0;
          cursor: pointer;
          transition: color 160ms ease, background 160ms ease;
        }
  
        .playground-agents-detail-sidebar-copy-button:hover,
        .playground-agents-detail-sidebar-copy-button:focus-visible {
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.92);
          outline: none;
        }
  
        .playground-agents-detail-sidebar-copy-button:disabled {
          opacity: 0.35;
          cursor: default;
        }
  
        .playground-agents-detail-sidebar-copy-value {
          min-width: 0;
          width: 100%;
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          gap: 8px;
        }
  
        .playground-agents-detail-sidebar-copy-value .playground-environments-editor-fact-value {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
          text-align: right;
        }
  
        .playground-agents-detail-sidebar > .playground-project-overview-sidebar-card {
          -webkit-backdrop-filter: blur(20px);
          backdrop-filter: blur(20px);
        }
  
        .playground-agents-detail-sidebar > .playground-agents-detail-about-card,
        .playground-agents-detail-sidebar > .playground-agents-detail-owner-card,
        .playground-agents-detail-sidebar > .playground-agents-detail-actions-card,
        .playground-agents-detail-sidebar > .playground-agents-detail-permissions-card {
          padding: 10px 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          box-shadow: none;
          -webkit-backdrop-filter: none;
          backdrop-filter: none;
        }
  
        .playground-agents-detail-sidebar > .playground-tags-detail-properties-card {
          position: relative;
          z-index: 1;
        }
  
        .playground-agents-detail-sidebar > .playground-tags-detail-properties-card.is-computer-popup-open {
          z-index: 560;
          overflow: visible;
        }
  
        .playground-agents-detail-sidebar .playground-project-overview-sidebar-row-value {
          justify-content: flex-start;
        }
  
        .playground-agents-detail-sidebar .playground-tags-detail-provider-value {
          justify-content: flex-end;
          text-align: right;
        }
  
        .playground-agents-detail-sidebar .playground-tags-detail-provider-value .playground-plugin-detail-info-link {
          max-width: 100%;
          margin-left: auto;
          justify-content: flex-end;
        }
  
        .playground-agents-detail-sidebar .playground-tags-detail-provider-value .playground-environments-editor-fact-value {
          text-align: right;
        }
  
        .playground-computer-detail-sidebar .playground-project-overview-sidebar-row-value {
          justify-content: flex-end;
          text-align: right;
        }
  
        .playground-computer-detail-sidebar .playground-environments-editor-fact-value {
          text-align: right;
        }
  
        .playground-computer-detail-sidebar .playground-environments-editor-fact-value.is-id {
          max-width: 100%;
        }
  
        .playground-computer-detail-sidebar .playground-computer-detail-profile-row.playground-project-overview-sidebar-row {
          align-items: flex-start;
          grid-template-columns: minmax(90px, 0.85fr) minmax(0, 1.35fr);
        }
  
        .playground-computer-detail-sidebar .playground-computer-detail-profile-value {
          min-width: 0;
          justify-content: flex-end;
          overflow: visible;
        }
  
        .playground-computer-profile-select-popup.playground-agents-model-select-popup {
          width: 100%;
        }
  
        .playground-computer-profile-select-popup .playground-tasks-toolbar-popup-menu {
          right: 0;
          left: auto;
          width: min(220px, calc(100vw - 32px));
          min-width: 190px;
        }
  
        .playground-computer-detail-sidebar .playground-environment-profile-selector.is-compact {
          width: 100%;
          max-width: 160px;
          margin-left: auto;
          grid-template-columns: minmax(0, 1fr);
          gap: 6px;
        }
  
        .playground-computer-detail-sidebar .playground-environment-profile-card {
          min-width: 0;
          padding: 8px 9px;
          border-radius: 8px;
          text-align: left;
        }
  
        .playground-computer-detail-sidebar .playground-environment-profile-card-top,
        .playground-computer-detail-sidebar .playground-environment-profile-card-meta {
          min-width: 0;
        }
  
        .playground-computer-detail-sidebar-status-value.playground-project-overview-sidebar-row-value,
        .playground-computer-detail-sidebar-toggle-value.playground-project-overview-sidebar-row-value {
          overflow: visible;
        }
  
        .playground-computer-detail-sidebar-status.playground-environments-desktop-status {
          min-height: 24px;
          padding: 0;
          border-radius: 0;
          background: transparent;
          font-size: 12px;
          font-weight: 400;
        }
  
        .playground-computer-detail-sidebar-status.playground-environments-desktop-status.is-running {
          color: rgba(167, 243, 208, 0.96);
        }
  
        .playground-computer-detail-sidebar-status.playground-environments-desktop-status.is-starting {
          color: rgba(254, 240, 138, 0.96);
        }
  
        .playground-computer-detail-sidebar-status.playground-environments-desktop-status.is-error {
          color: rgba(254, 202, 202, 0.96);
        }
  
        .playground-computer-detail-sidebar-status.playground-environments-desktop-status.is-stopped {
          color: rgba(255, 255, 255, 0.66);
        }
  
        .playground-computer-detail-sidebar-action.playground-project-overview-sidebar-resource-row {
          min-height: 34px;
        }
  
        .playground-computer-detail-add-icon-button {
          width: 18px;
          height: 18px;
          padding: 0;
          border: 0;
          border-radius: 0;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          background: transparent;
          color: rgba(255, 255, 255, 0.7);
          cursor: pointer;
        }
  
        .playground-computer-detail-add-icon-button:hover,
        .playground-computer-detail-add-icon-button:focus-visible {
          background: transparent;
          color: rgba(255, 255, 255, 0.94);
          outline: none;
        }
  
        .playground-computer-runtime-versions-box.playground-tasks-detail-description {
          min-height: 0;
          margin: 0;
        }
  
        .playground-computer-runtime-versions-box.playground-mission-control-modal-outcomes-editor {
          margin-bottom: 0;
        }
  
        .playground-computer-runtime-versions-box .playground-tasks-connectors-list {
          gap: 0;
        }
  
        .playground-computer-runtime-versions-box .playground-tasks-connector-row {
          min-height: 38px;
          padding: 3px 0;
        }
  
        .playground-computer-api-modal.playground-tasks-project-modal {
          width: min(760px, calc(100vw - 32px));
        }
  
        .playground-computer-api-modal-body.playground-tasks-issue-modal-body {
          padding-top: 0;
        }
  
        .playground-computer-api-config-row {
          min-width: 0;
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 14px;
          margin-bottom: 12px;
        }
  
        .playground-computer-api-config-label {
          flex: 0 0 auto;
          color: rgba(255, 255, 255, 0.54);
          font-size: 12px;
          font-weight: 400;
        }
  
        .playground-computer-api-agent-select-popup.playground-agents-model-select-popup,
        .playground-agent-api-environment-select-popup.playground-agents-model-select-popup {
          min-width: 0;
          flex: 1 1 auto;
          display: inline-flex;
          justify-content: flex-end;
          z-index: 80;
        }
  
        .playground-computer-api-agent-select-popup.playground-agents-model-select-popup .playground-environments-runtime-value-button,
        .playground-agent-api-environment-select-popup.playground-agents-model-select-popup .playground-environments-runtime-value-button {
          width: min(320px, 100%);
        }
  
        .playground-computer-api-agent-select-popup .playground-tasks-toolbar-popup-menu,
        .playground-agent-api-environment-select-popup .playground-tasks-toolbar-popup-menu {
          right: 0;
          left: auto;
          width: min(320px, calc(100vw - 64px));
        }
  
        .playground-computer-api-agent-select-popup .tb-popup-row,
        .playground-agent-api-environment-select-popup .tb-popup-row {
          min-width: 0;
        }
  
        .playground-computer-api-card.playground-server-invoke-card {
          margin: 0;
          border: 1px solid rgba(255, 255, 255, 0.1);
          background: rgba(0, 0, 0, 0.5);
          -webkit-backdrop-filter: none;
          backdrop-filter: none;
        }
  
        .playground-computer-api-card.playground-server-invoke-card::before {
          content: none;
          display: none;
        }
  
        .playground-computer-api-header.playground-server-invoke-header {
          align-items: center;
        }
  
        .playground-computer-api-copy-button.playground-settings-icon-button {
          flex: 0 0 auto;
        }
  
        .playground-computer-api-code-editor.playground-server-invoke-code-editor {
          min-height: 240px;
          max-height: min(460px, calc(100vh - 300px));
          background: rgba(0, 0, 0, 0.5);
        }
  
        .playground-computer-api-code.playground-server-invoke-code-fallback {
          min-height: 240px;
          max-height: min(460px, calc(100vh - 260px));
          overflow: auto;
          margin: 0;
          background: rgba(0, 0, 0, 0.5);
          white-space: pre;
        }
  
        .playground-agents-detail-sidebar .playground-tags-detail-sidebar-actions {
          display: flex;
          flex-direction: column;
        }
  
        .playground-agents-detail-sidebar .playground-tags-detail-sidebar-action {
          min-width: 0;
          display: flex;
          align-items: flex-start;
          gap: 10px;
          padding: 10px 0;
        }
  
        .playground-agents-detail-sidebar .playground-tags-detail-sidebar-action + .playground-tags-detail-sidebar-action {
          border-top: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-agents-detail-sidebar .playground-tags-detail-sidebar-action-icon.playground-plugin-detail-feature-icon {
          width: 24px;
          height: 24px;
          flex-basis: 24px;
          border: 0;
          border-radius: 0;
          padding: 0;
          background: transparent;
        }
  
        .playground-agents-detail-sidebar .playground-tags-detail-sidebar-action-copy {
          gap: 3px;
        }
  
        .playground-agents-detail-sidebar .playground-tags-detail-sidebar-action-copy .playground-plugin-detail-feature-title {
          font-size: 12px;
          line-height: 1.35;
        }
  
        .playground-agents-detail-sidebar .playground-tags-detail-sidebar-action-copy .playground-plugin-detail-feature-description {
          font-size: 11px;
          line-height: 1.45;
        }
  
        .playground-agents-detail-sidebar-permission-row.playground-project-overview-sidebar-row {
          width: 100%;
          border: 0;
          background: transparent;
          color: inherit;
          font: inherit;
          text-align: left;
          cursor: pointer;
        }
  
        .playground-agents-detail-sidebar-permission-row:hover .playground-project-overview-sidebar-row-label,
        .playground-agents-detail-sidebar-permission-row:focus-visible .playground-project-overview-sidebar-row-label {
          color: rgba(255, 255, 255, 0.86);
        }
  
        .playground-agents-detail-sidebar-permission-row:focus-visible {
          outline: none;
        }
  
        .playground-agents-detail-sidebar-permission-row:focus-visible .playground-agents-detail-sidebar-permission-value {
          color: #fff;
        }
  
        .playground-agents-detail-sidebar .playground-agents-detail-sidebar-permission-value {
          justify-content: flex-end;
          overflow: visible;
        }
  
        .playground-agents-detail-permission-rings {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          gap: 7px;
        }
  
        .playground-agents-detail-permission-ring {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          flex: 0 0 auto;
        }
  
        .playground-agents-detail-permission-rings .playground-permission-mini-ring-icon {
          width: 26px;
          height: 26px;
        }
  
        .playground-agents-detail-sidebar .playground-environments-runtime-value-button {
          min-width: 0;
          max-width: 100%;
          width: 100%;
          min-height: 28px;
          padding: 0;
          border: 0;
          background: transparent;
        }
  
        .playground-agents-detail-sidebar .playground-environments-runtime-value-button:hover {
          background: transparent;
        }
  
        .playground-agents-detail-sidebar .playground-agents-model-picker-trigger-copy,
        .playground-agents-detail-sidebar .playground-agents-model-picker-trigger-labels,
        .playground-agents-detail-sidebar .playground-environments-runtime-value-label,
        .playground-agents-detail-sidebar .playground-environments-editor-fact-value {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-agents-detail-sidebar .playground-environments-editor-fact-value.is-id {
          max-width: 100%;
        }
  
        .playground-agents-detail-sidebar .playground-agents-model-select-popup {
          width: 100%;
        }
  
        .playground-tags-detail-computer-popup.playground-platform-popup-shell,
        .playground-tags-detail-computer-popup.playground-platform-popup-shell .playground-platform-popup-menu,
        .playground-tags-detail-computer-popup.playground-environments-runtime-popup-shell.is-open,
        .playground-tags-detail-computer-popup.playground-environments-runtime-popup-shell.is-open .playground-tasks-toolbar-popup-menu {
          z-index: 520;
        }
  
        .playground-tags-detail-computer-popup.playground-platform-popup-shell .playground-platform-popup-menu {
          background: rgba(30, 30, 30, 0.9);
          -webkit-backdrop-filter: none;
          backdrop-filter: none;
        }
  
        .playground-tags-detail-computer-popup .playground-tasks-toolbar-popup-menu {
          background: rgba(30, 30, 30, 0.9);
          -webkit-backdrop-filter: none;
          backdrop-filter: none;
        }
  
        .playground-tags-detail-environment-menu.tb-popup-menu-inline-workspace {
          width: min(280px, calc(100vw - 32px));
          max-height: min(360px, calc(100vh - 156px));
          display: flex;
          flex-direction: column;
          overflow: hidden;
          padding: 10px 0 8px;
        }
  
        .playground-tags-detail-environment-menu .tb-popup-panel-section-attach-header {
          flex: 0 0 auto;
          padding: 0 12px 8px;
        }
  
        .playground-tags-detail-environment-menu .platform-switch__option {
          min-width: 0;
        }
  
        .playground-tags-detail-environment-menu .tb-popup-menu-inline-body {
          flex: 1 1 auto;
          min-height: 0;
          max-height: 292px;
          overflow-y: auto;
          padding: 4px 0 0;
          scrollbar-width: thin;
          scrollbar-color: rgba(255, 255, 255, 0.24) transparent;
        }
  
        .playground-tags-detail-environment-menu .tb-popup-menu-inline-body::-webkit-scrollbar {
          width: 8px;
        }
  
        .playground-tags-detail-environment-menu .tb-popup-menu-inline-body::-webkit-scrollbar-thumb {
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.22);
        }
  
        .playground-tags-detail-environment-menu .tb-popup-row {
          width: 100%;
          display: flex;
          align-items: center;
          gap: 12px;
          padding: 10px 14px;
          border: 0;
          background: transparent;
          color: white;
          cursor: pointer;
          text-align: left;
          transition: background-color 160ms ease;
        }
  
        .playground-tags-detail-environment-menu .tb-popup-row:hover,
        .playground-tags-detail-environment-menu .tb-popup-row.selected {
          background: rgba(255, 255, 255, 0.1);
        }
  
        .playground-tags-detail-environment-menu .tb-popup-row:disabled {
          opacity: 0.45;
          cursor: default;
        }
  
        .playground-tags-detail-environment-menu .tb-popup-row:disabled:hover {
          background: transparent;
        }
  
        .playground-tags-detail-environment-menu .tb-popup-icon {
          width: 14px;
          height: 14px;
          flex: 0 0 auto;
          color: rgba(255, 255, 255, 0.62);
        }
  
        .playground-tags-detail-environment-menu .tb-popup-label {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
          color: white;
          font-size: 14px;
          font-weight: 500;
        }
  
        .playground-tags-detail-environment-menu .tb-popup-check-slot {
          order: 2;
          margin-left: auto;
          width: 16px;
          height: 16px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          flex: 0 0 auto;
        }
  
        .playground-tags-detail-environment-menu .tb-popup-check {
          color: white;
        }
  
        .playground-tags-detail-environment-menu .tb-popup-menu-inline-empty {
          padding: 10px 14px 12px;
        }
  
        .playground-tags-detail-environment-menu .tb-popup-empty-state {
          color: rgba(255, 255, 255, 0.6);
          font-size: 12px;
          line-height: 1.4;
        }
  
        .playground-tags-detail-label-with-help {
          display: inline-flex;
          align-items: center;
          min-width: 0;
          gap: 5px;
        }
  
        .playground-tags-default-environment-help {
          position: relative;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          flex: 0 0 auto;
          width: 14px;
          height: 14px;
          color: rgba(255, 255, 255, 0.5);
          cursor: help;
        }
  
        .playground-tags-default-environment-help:hover,
        .playground-tags-default-environment-help:focus-visible {
          color: rgba(255, 255, 255, 0.82);
        }
  
        .playground-tags-default-environment-tooltip {
          position: absolute;
          left: 50%;
          bottom: calc(100% + 9px);
          width: 236px;
          padding: 8px 10px;
          border: 1px solid rgba(255, 255, 255, 0.14);
          border-radius: 8px;
          background: rgba(30, 30, 30, 0.92);
          color: rgba(255, 255, 255, 0.84);
          font-size: 11px;
          font-weight: 400;
          line-height: 1.35;
          text-transform: none;
          letter-spacing: 0;
          text-align: left;
          white-space: normal;
          box-shadow: 0 16px 42px rgba(0, 0, 0, 0.36);
          opacity: 0;
          pointer-events: none;
          transform: translate(-50%, 3px);
          transition: opacity 140ms ease, transform 140ms ease;
          z-index: 900;
        }
  
        .playground-tags-default-environment-tooltip::before {
          content: "";
          position: absolute;
          left: 50%;
          top: 100%;
          width: 8px;
          height: 8px;
          border-right: 1px solid rgba(255, 255, 255, 0.14);
          border-bottom: 1px solid rgba(255, 255, 255, 0.14);
          background: rgba(30, 30, 30, 0.92);
          transform: translate(-50%, -4px) rotate(45deg);
        }
  
        .playground-tags-default-environment-help:hover .playground-tags-default-environment-tooltip,
        .playground-tags-default-environment-help:focus-visible .playground-tags-default-environment-tooltip {
          opacity: 1;
          transform: translate(-50%, 0);
        }
  
        .playground-agents-detail-sidebar-actions {
          display: flex;
          flex-direction: column;
          gap: 2px;
        }
  
        .playground-agents-detail-sidebar-action.playground-project-overview-sidebar-resource-row {
          grid-template-columns: 18px minmax(0, 1fr);
        }
  
        .playground-agents-detail-sidebar .playground-project-overview-sidebar-row,
        .playground-agents-detail-sidebar .playground-project-overview-sidebar-resource-row,
        .playground-agents-detail-sidebar .playground-agents-detail-permission-meter {
          padding-left: 0 !important;
          padding-right: 0 !important;
        }
  
        .playground-agents-detail-sidebar .playground-project-overview-sidebar-row:hover,
        .playground-agents-detail-sidebar .playground-project-overview-sidebar-row:focus-visible,
        .playground-agents-detail-sidebar .playground-project-overview-sidebar-resource-row:hover,
        .playground-agents-detail-sidebar .playground-project-overview-sidebar-resource-row:focus-visible,
        .playground-agents-detail-sidebar .playground-agents-detail-permission-meter:hover,
        .playground-agents-detail-sidebar .playground-agents-detail-permission-meter:focus-visible,
        .playground-agents-detail-sidebar .playground-environments-runtime-value-button:hover,
        .playground-agents-detail-sidebar .playground-environments-runtime-value-button:focus-visible {
          background: transparent !important;
          box-shadow: none !important;
        }
  
        .playground-agents-detail-about-row.playground-project-overview-sidebar-resource-row {
          min-height: 32px;
          grid-template-columns: 18px minmax(0, 1fr);
        }
  
  	      .playground-agents-detail-about-row.is-agent-id.playground-project-overview-sidebar-resource-row,
  	      .playground-agents-detail-about-row.is-model.playground-project-overview-sidebar-resource-row {
  	        grid-template-columns: minmax(0, 1fr);
  	      }
  
  	      .playground-agents-detail-about-row.is-voice.playground-project-overview-sidebar-resource-row {
  	        width: auto;
  	        grid-template-columns: max-content 14px;
  	        column-gap: 4px;
  	      }
  
        .playground-agents-detail-about-value {
          min-width: 0;
          display: flex;
          align-items: center;
          justify-content: flex-start;
          color: rgba(255, 255, 255, 0.86);
        }
  
  	      .playground-agents-detail-about-row.is-model .playground-agents-detail-about-value,
  	      .playground-agents-detail-about-row.is-voice .playground-agents-detail-about-value,
  	      .playground-agents-detail-about-row.is-model .playground-environments-runtime-value-button {
  	        width: auto;
  	        margin-left: 0;
  	        justify-content: flex-start;
  	        text-align: left;
  	      }
  
  	      .playground-agents-detail-about-row.is-model .playground-agents-model-picker-trigger-copy,
  	      .playground-agents-detail-about-row.is-model .playground-agents-model-picker-trigger-labels,
  	      .playground-agents-detail-about-row.is-model .playground-environments-runtime-value-label,
  	      .playground-agents-detail-about-row.is-voice .playground-environments-runtime-value-label {
  	        text-align: left;
  	      }
  
  	      .playground-agents-detail-voice-select-popup.playground-environments-runtime-popup-shell {
  	        width: max-content;
  	        display: inline-flex;
  	        align-self: flex-start;
  	        justify-content: flex-start;
  	      }
  
  	      .playground-agents-detail-voice-select-popup .playground-agents-detail-about-row {
  	        width: auto;
  	      }
  
  	      .playground-agents-detail-voice-select-trigger {
  	        appearance: none;
  	        display: inline-grid;
  	        border: 0;
  	        background: transparent;
  	        color: rgba(255, 255, 255, 0.86);
  	        cursor: pointer;
  	      }
  
  	      .playground-agents-detail-voice-select-trigger:disabled {
  	        cursor: default;
  	        opacity: 0.48;
  	      }
  
  	      .playground-agents-detail-voice-select-value {
  	        width: auto;
  	        min-width: 0;
  	        display: inline-flex;
  	        align-items: center;
  	        gap: 8px;
  	      }
  
  	      .playground-agents-detail-voice-select-value .playground-project-overview-sidebar-resource-icon {
  	        width: 16px;
  	        height: 16px;
  	        display: inline-flex;
  	        align-items: center;
  	        justify-content: center;
  	        color: rgba(255, 255, 255, 0.68);
  	      }
  
  	      .playground-agents-detail-voice-select-chevron {
  	        width: 14px;
  	        height: 14px;
  	        flex: 0 0 auto;
  	        margin-left: 0;
  	        color: rgba(255, 255, 255, 0.46);
  	      }
  
  	      .playground-agents-detail-voice-select-menu.playground-tasks-toolbar-popup-menu {
  	        right: auto;
  	        left: 0;
  	        width: 220px;
  	        min-width: 220px;
  	      }
  
        .playground-agents-detail-about-model-readonly {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          justify-content: flex-start;
          gap: 10px;
          color: rgba(255, 255, 255, 0.86);
          font-size: 12px;
          line-height: 1.35;
          font-weight: 400;
        }
  
        .playground-agents-detail-about-model-readonly-label {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
          text-align: left;
        }
  
        .playground-agents-detail-about-email {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
          font-size: 12px;
          line-height: 1.35;
          font-weight: 400;
        }
  
        .playground-agents-detail-about-email {
          color: #fff;
        }
  
        .playground-agents-detail-about-row.is-email .playground-project-overview-sidebar-resource-label,
        .playground-agents-detail-about-row.is-email .playground-agents-detail-about-email {
          padding-left: 0;
          padding-right: 0;
        }
  
        .playground-agents-detail-sidebar-action:disabled {
          opacity: 0.42;
          cursor: default;
        }
  
        .playground-agents-detail-permissions-card .playground-project-overview-sidebar-card-header {
          margin-bottom: 2px;
        }
  
        .playground-agents-detail-permission-bars {
          display: flex;
          align-items: flex-start;
          gap: 4px;
          margin-top: 16px;
          margin-bottom: 2px;
        }
  
        .playground-agents-detail-permission-meter {
          flex: 1 1 0;
          min-width: 0;
          padding: 0;
          border: 0;
          background: transparent;
          color: inherit;
          font: inherit;
          text-align: left;
          display: flex;
          flex-direction: column;
          gap: 6px;
          cursor: pointer;
        }
  
        .playground-agents-detail-permission-meter:focus-visible {
          outline: none;
        }
  
        .playground-agents-detail-permission-meter-copy {
          min-width: 0;
          margin-top: 2px;
          display: flex;
          align-items: center;
          justify-content: flex-start;
          gap: 5px;
        }
  
        .playground-agents-detail-permission-meter-swatch {
          width: 8px;
          height: 8px;
          flex: 0 0 8px;
          border-radius: 2px;
          background: var(--agent-permission-bar-active);
        }
  
        .playground-agents-detail-permission-meter-title {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
          font-size: 11px;
          line-height: 1.2;
        }
  
        .playground-agents-detail-permission-meter-title {
          color: rgba(255, 255, 255, 0.72);
          font-weight: 500;
        }
  
        .playground-agents-detail-permission-meter-track {
          width: 100%;
          height: 6px;
          border-radius: 2px;
          overflow: hidden;
          background: linear-gradient(
            90deg,
            var(--agent-permission-bar-active) 0%,
            var(--agent-permission-bar-active) var(--agent-permission-bar-progress),
            var(--agent-permission-bar-muted) var(--agent-permission-bar-progress),
            var(--agent-permission-bar-muted) 100%
          );
        }
  
        .playground-agents-detail-owner-popup-shell {
          width: 100%;
          z-index: 88;
        }
  
        .playground-agents-detail-sidebar-owner-row.playground-project-overview-sidebar-row {
          width: 100%;
          border: 0;
          background: transparent;
          color: inherit;
          font: inherit;
          text-align: left;
          cursor: pointer;
        }
  
        .playground-agents-detail-sidebar-owner-row.is-compact.playground-project-overview-sidebar-row {
          grid-template-columns: minmax(0, 1fr) 14px;
          min-height: 32px;
        }
  
        .playground-agents-detail-sidebar-owner-row.is-compact .playground-agents-detail-owner-value {
          justify-content: flex-start;
        }
  
        .playground-agents-detail-sidebar-owner-row:hover .playground-project-overview-sidebar-row-label,
        .playground-agents-detail-sidebar-owner-row:focus-visible .playground-project-overview-sidebar-row-label {
          color: rgba(255, 255, 255, 0.86);
        }
  
        .playground-agents-detail-sidebar-owner-row:focus-visible {
          outline: none;
        }
  
        .playground-agents-detail-owner-value {
          min-width: 0;
          display: flex;
          align-items: center;
          justify-content: flex-end !important;
          gap: 8px;
        }
  
        .playground-agents-detail-owner-member-cell.playground-team-member-cell {
          min-width: 0;
          max-width: 100%;
          justify-content: flex-end;
          gap: 8px;
        }
  
        .playground-agents-detail-owner-member-cell .playground-team-member-avatar {
          width: 20px;
          height: 20px;
          flex: 0 0 20px;
          font-size: 8px;
        }
  
        .playground-agents-detail-owner-member-cell .playground-team-member-copy {
          min-width: 0;
          text-align: right;
        }
  
        .playground-agents-detail-owner-member-cell .playground-team-table-title {
          max-width: 132px;
          color: rgba(255, 255, 255, 0.86);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.35;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-agents-detail-sidebar-owner-row.is-compact .playground-agents-detail-owner-value {
          justify-content: flex-start !important;
        }
  
        .playground-agents-detail-sidebar-owner-row.is-compact .playground-agents-detail-owner-member-cell.playground-team-member-cell {
          justify-content: flex-start;
        }
  
        .playground-agents-detail-sidebar-owner-row.is-compact .playground-agents-detail-owner-member-cell .playground-team-member-copy,
        .playground-agents-detail-sidebar-owner-row.is-compact .playground-agents-detail-owner-member-cell .playground-team-table-title {
          text-align: left;
        }
  
        .playground-agents-detail-owner-avatar,
        .playground-agents-detail-owner-option-avatar {
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          overflow: hidden;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.9);
          font-weight: 500;
        }
  
        .playground-agents-detail-owner-avatar {
          width: 20px;
          height: 20px;
          font-size: 9px;
        }
  
        .playground-agents-detail-owner-option-avatar {
          width: 24px;
          height: 24px;
          font-size: 10px;
        }
  
        .playground-agents-detail-owner-avatar-image,
        .playground-agents-detail-owner-option-avatar-image {
          width: 100%;
          height: 100%;
          object-fit: cover;
          display: block;
        }
  
        .playground-agents-detail-owner-copy {
          min-width: 0;
          display: flex;
          flex-direction: column;
          align-items: flex-end;
          gap: 2px;
        }
  
        .playground-agents-detail-owner-name,
        .playground-agents-detail-owner-email {
          max-width: 100%;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
          text-align: right;
        }
  
        .playground-agents-detail-owner-name {
          color: rgba(255, 255, 255, 0.9);
          font-size: 12px;
          font-weight: 500;
        }
  
        .playground-agents-detail-owner-email {
          color: rgba(255, 255, 255, 0.44);
          font-size: 10px;
          font-weight: 400;
        }
  
        .playground-agents-detail-owner-popup-shell .playground-agents-detail-owner-menu {
          top: calc(100% + 8px);
          right: 0;
          left: auto;
          width: 260px;
          min-width: 260px;
          max-height: min(320px, calc(100vh - 180px));
          overflow: auto;
          transform-origin: top right;
          scrollbar-width: none;
        }
  
        .playground-agents-detail-owner-popup-shell .playground-agents-detail-owner-menu::-webkit-scrollbar {
          display: none;
        }
  
        .playground-agents-detail-owner-option.tb-popup-row {
          grid-template-columns: 24px minmax(0, 1fr) 14px;
          align-items: center;
          gap: 10px;
          min-height: 42px;
          padding: 8px 12px;
        }
  
        .playground-agents-detail-owner-option .platform-selector__option-copy {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 2px;
        }
  
        .playground-agents-detail-owner-option .platform-selector__option-label {
          color: rgba(255, 255, 255, 0.92);
          font-size: 12px;
          font-weight: 500;
        }
  
        .playground-agents-detail-owner-option .platform-selector__option-description {
          color: rgba(255, 255, 255, 0.48);
          font-size: 10px;
          font-weight: 400;
        }
  
        .playground-agents-detail-owner-menu-empty {
          padding: 13px 14px;
          color: rgba(255, 255, 255, 0.52);
          font-size: 11px;
          line-height: 1.45;
        }
  
        .playground-tasks-project-modal.playground-agents-send-team-modal {
          width: min(520px, calc(100vw - 48px));
          max-height: min(560px, calc(100vh - 48px));
          display: flex;
          flex-direction: column;
          overflow: visible;
        }
  
        .playground-agents-send-team-modal .playground-tasks-project-modal-top {
          gap: 12px;
        }
  
        .playground-agents-send-team-modal-body {
          display: flex;
          flex-direction: column;
          gap: 10px;
          max-height: min(350px, calc(100vh - 250px));
          overflow: auto;
          scrollbar-width: none;
        }
  
        .playground-agents-send-team-modal-body::-webkit-scrollbar {
          display: none;
        }
  
        .playground-agents-send-team-list {
          display: flex;
          flex-direction: column;
          gap: 8px;
        }
  
        .playground-agents-send-team-option {
          width: 100%;
          min-height: 52px;
          display: grid;
          grid-template-columns: 26px minmax(0, 1fr) 18px;
          align-items: center;
          gap: 12px;
          padding: 10px 12px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 10px;
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.92);
          text-align: left;
          cursor: pointer;
          transition: background-color 160ms ease, border-color 160ms ease, color 160ms ease;
        }
  
        .playground-agents-send-team-option:hover:not(:disabled) {
          border-color: rgba(255, 255, 255, 0.16);
          background: rgba(255, 255, 255, 0.07);
        }
  
        .playground-agents-send-team-option.is-selected {
          border-color: rgba(77, 163, 255, 0.66);
          background: rgba(77, 163, 255, 0.08);
        }
  
        .playground-agents-send-team-option:disabled {
          opacity: 0.58;
          cursor: default;
        }
  
        .playground-agents-send-team-option-icon {
          width: 26px;
          height: 26px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.06);
          color: rgba(255, 255, 255, 0.86);
        }
  
        .playground-agents-send-team-option-copy {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 3px;
        }
  
        .playground-agents-send-team-option-title {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
          color: rgba(255, 255, 255, 0.94);
          font-size: 12px;
          font-weight: 500;
        }
  
        .playground-agents-send-team-option-meta {
          color: rgba(255, 255, 255, 0.48);
          font-size: 10px;
          font-weight: 400;
        }
  
        .playground-agents-send-team-empty {
          min-height: 120px;
          display: flex;
          align-items: center;
          justify-content: center;
          gap: 10px;
          padding: 18px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 10px;
          background: rgba(255, 255, 255, 0.03);
          color: rgba(255, 255, 255, 0.54);
          font-size: 12px;
          line-height: 1.45;
          text-align: center;
        }
  
        .playground-agents-send-team-actions {
          margin-top: 16px;
        }
  
        .playground-agents-detail-summary .playground-environments-home-summary-kpis,
        .playground-agents-detail-chart-kpis.playground-project-overview-summary-kpis {
          grid-template-columns: repeat(5, minmax(0, 1fr));
        }
  
        .playground-agents-detail-tabs.playground-agents-overview-tabs {
          margin: -4px 0 0;
          grid-column: 1 / -1;
          width: 100%;
        }
  
        .playground-agents-detail-tabs .playground-project-overview-chart-tabs {
          width: 100%;
        }
  
        .playground-agents-detail-top-controls {
          grid-column: 1 / -1;
          min-width: 0;
          display: grid;
          grid-template-columns: minmax(0, 1fr) minmax(280px, 340px);
          align-items: center;
          column-gap: 42px;
          transition:
            grid-template-columns 260ms cubic-bezier(0.16, 1, 0.3, 1),
            column-gap 260ms cubic-bezier(0.16, 1, 0.3, 1);
        }
  
        .playground-agents-detail-overview-layout.is-sidebar-collapsed > .playground-agents-detail-top-controls {
          grid-template-columns: minmax(0, 1fr) auto;
          column-gap: 12px;
        }
  
        .playground-agents-detail-tabs .playground-project-overview-chart-tabs {
          gap: 24px;
        }
  
        .playground-agents-detail-tabs .playground-project-overview-chart-tab {
          gap: 6px;
        }
  
        .playground-agents-detail-tab-icon {
          width: 13px;
          height: 13px;
          flex: 0 0 auto;
        }
  
        .playground-computer-detail-filebase-tab.playground-project-overview-chart-tab {
          display: inline-flex;
          align-items: center;
          gap: 6px;
        }
  
        .playground-computer-detail-filebase-tab.playground-project-overview-chart-tab svg {
          flex: 0 0 auto;
        }
  
        .playground-agents-detail-metrics.playground-environments-home-metrics {
          margin: 0;
          gap: 0;
        }
  
        .playground-agents-detail-chart-block.playground-database-overview-chart-block {
          display: flex;
          flex-direction: column;
          gap: 16px;
          height: auto;
          min-height: 0;
        }
  
        .playground-agents-page .playground-agents-detail-metrics .playground-agents-detail-chart-block.playground-database-overview-chart-block,
        .playground-agents-detail-assistant-page .playground-agents-detail-metrics .playground-agents-detail-chart-block.playground-database-overview-chart-block {
          --playground-project-overview-chart-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          position: relative;
          padding: 20px;
          border: 0;
          border-radius: 15px;
          background: transparent;
          overflow: hidden;
          -webkit-backdrop-filter: blur(50px);
          backdrop-filter: blur(50px);
        }
  
        .playground-agents-page .playground-agents-detail-metrics .playground-agents-detail-chart-block.playground-database-overview-chart-block::before,
        .playground-agents-detail-assistant-page .playground-agents-detail-metrics .playground-agents-detail-chart-block.playground-database-overview-chart-block::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          z-index: 5;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-project-overview-chart-border);
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-agents-page .playground-agents-detail-metrics .playground-agents-detail-chart-block.playground-database-overview-chart-block > *,
        .playground-agents-detail-assistant-page .playground-agents-detail-metrics .playground-agents-detail-chart-block.playground-database-overview-chart-block > * {
          position: relative;
          z-index: 1;
        }
  
        .playground-agents-page .playground-agents-detail-metrics .playground-project-overview-chart-kpis,
        .playground-agents-detail-assistant-page .playground-agents-detail-metrics .playground-project-overview-chart-kpis {
          background: transparent;
          border-bottom: 1px solid rgba(255, 255, 255, 0.15);
        }
  
        .playground-agents-page .playground-agents-detail-metrics .playground-project-overview-chart-header,
        .playground-agents-detail-assistant-page .playground-agents-detail-metrics .playground-project-overview-chart-header {
          display: none;
        }
  
        .playground-agents-detail-chart-block .playground-settings-usage-chart-card {
          min-height: 0;
        }
  
        .playground-agents-detail-fact-rows {
          display: flex;
          flex-direction: column;
          gap: 4px;
          margin-top: 2px;
          padding-top: 16px;
          border-top: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        @media (max-width: 980px) {
          .playground-agents-detail-summary .playground-environments-home-summary-kpis,
          .playground-agents-detail-chart-kpis.playground-project-overview-summary-kpis {
            grid-template-columns: repeat(2, minmax(0, 1fr));
          }
        }
  
        .playground-tasks-project-panel-header {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-tasks-project-panel-title {
          font-size: 13px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.95);
        }
  
        .playground-tasks-project-list {
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-tasks-project-row {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 12px;
          padding: 12px 14px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 14px;
          background: rgba(255, 255, 255, 0.03);
        }
  
        .playground-tasks-project-row-main {
          min-width: 0;
        }
  
        .playground-tasks-project-row-title {
          font-size: 13px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.94);
        }
  
        .playground-tasks-project-row-copy {
          margin-top: 4px;
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.5);
        }
  
        .playground-tasks-project-workspace {
          flex: 1;
          min-height: 100%;
          display: flex;
          flex-direction: column;
        }
  
        .playground-tasks-project-workspace-detail {
          flex: 1;
          min-height: 0;
        }
  
        .playground-tasks-project-navbar-title {
          min-width: 0;
          display: flex;
          align-items: center;
          gap: 6px;
        }
  
        .playground-tasks-project-navbar {
          min-height: 56px;
          padding: 0 5px 0 0;
          box-sizing: border-box;
        }
  
        .playground-tasks-project-navbar-title .playground-content-title {
          min-width: 0;
        }
  
        .playground-tasks-project-navbar-actions {
          gap: 4px;
        }
  
        .playground-tasks-project-nav-switch {
          width: fit-content;
        }
  
        .playground-tasks-project-home-navbar {
          min-height: 56px;
          padding: 0 5px 0 0;
          margin-bottom: 12px;
          border-bottom: 0;
          box-sizing: border-box;
        }
  
        .playground-tools-navbar {
          min-height: 56px;
          padding: 0 20px;
          margin-bottom: 0;
          border-bottom: 1px solid rgba(255, 255, 255, 0.075);
          background: #000;
          box-sizing: border-box;
        }
  
        .playground-tasks-project-home-navbar .playground-tasks-project-navbar-title,
        .playground-tasks-project-home-navbar .playground-content-nav-right,
        .playground-tasks-project-home-navbar .playground-content-nav-center {
          min-height: 0;
          height: auto;
          align-items: center;
        }
  
        .playground-tools-navbar-title,
        .playground-tools-navbar .playground-content-nav-right,
        .playground-tools-navbar .playground-content-nav-center {
          min-height: 0;
          height: auto;
          align-items: center;
        }
  
        .playground-tools-navbar-title {
          min-width: 0;
          display: flex;
          align-items: center;
          gap: 6px;
        }
  
        .playground-tools-navbar-actions {
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          gap: 8px;
          flex-wrap: nowrap;
          white-space: nowrap;
        }
  
        .playground-tools-overview-controls-slot,
        .playground-tools-skills-nav-actions-slot,
        .playground-resource-overview-controls-slot,
        .playground-resources-nav-actions-slot {
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          gap: 8px;
          flex-wrap: nowrap;
          white-space: nowrap;
        }
  
        .playground-tasks-project-workspace .playground-environments-detail-scroll {
          padding: 0 18px;
        }
  
        .playground-tasks-project-title-back {
          flex: 0 0 auto;
        }
  
        .playground-tasks-project-workspace-scroll {
          gap: 14px;
        }
  
        .playground-environments-detail-scroll.playground-tasks-project-workspace-scroll {
          padding-bottom: 0;
        }
  
        .playground-environments-detail-scroll.playground-tasks-project-workspace-scroll:not(.is-calendar) {
          scrollbar-width: none;
        }
  
        .playground-environments-detail-scroll.playground-tasks-project-workspace-scroll:not(.is-calendar)::-webkit-scrollbar {
          display: none;
        }
  
        .playground-environments-detail-scroll.playground-tasks-project-workspace-scroll.is-mission-control-setup {
          padding: 0;
          gap: 0;
          overflow: hidden;
        }
  
        .playground-mission-control-studio-backdrop {
          position: fixed;
          inset: 0;
          z-index: 10000;
          display: flex;
          flex-direction: column;
          background: rgba(6, 6, 10, 0.96);
          backdrop-filter: blur(18px);
          -webkit-backdrop-filter: blur(18px);
        }
  
        .playground-mission-control-studio {
          width: 100%;
          height: 100%;
          min-width: 0;
          min-height: 0;
          display: flex;
          flex-direction: column;
          overflow: hidden;
        }
  
        .playground-mission-control-studio-header {
          min-height: 58px;
          padding: 0 22px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 18px;
        }
  
        .playground-mission-control-studio-title-row {
          min-width: 0;
          display: flex;
          align-items: center;
          gap: 12px;
        }
  
        .playground-mission-control-studio-title-copy {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 2px;
        }
  
        .playground-mission-control-studio-title {
          overflow: hidden;
          color: rgba(255, 255, 255, 0.98);
          font-size: 15px;
          line-height: 1.25;
          font-weight: 500;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-mission-control-studio-subtitle {
          overflow: hidden;
          color: rgba(255, 255, 255, 0.48);
          font-size: 12px;
          line-height: 1.3;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-mission-control-studio-back {
          flex: 0 0 auto;
        }
  
        .playground-mission-control-studio-header-actions {
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          gap: 10px;
        }
  
        .playground-mission-control-studio-body {
          min-width: 0;
          min-height: 0;
          flex: 1 1 auto;
          display: grid;
          grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
          gap: 0;
          background: transparent;
          overflow: hidden;
        }
  
        .playground-mission-control-studio-settings,
        .playground-mission-control-studio-chat {
          min-width: 0;
          min-height: 0;
          background: rgba(6, 6, 10, 0.96);
          overflow: hidden;
        }
  
        .playground-mission-control-studio-settings {
          display: flex;
          flex-direction: column;
          background: rgba(255, 255, 255, 0.05) !important;
        }
  
        .playground-mission-control-studio-section-header {
          flex: 0 0 auto;
          padding: 20px 22px 12px;
        }
  
        .playground-mission-control-studio-section-nav {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 16px;
          margin-bottom: 10px;
        }
  
        .playground-mission-control-studio-back-link {
          padding: 0;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.68);
          font: inherit;
          font-size: 13px;
          line-height: 1.2;
          font-weight: 400;
          display: inline-flex;
          align-items: center;
          gap: 6px;
          cursor: pointer;
          transition: color 160ms ease;
        }
  
        .playground-mission-control-studio-back-link:hover {
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-mission-control-studio-save-button {
          min-height: 28px;
          padding: 0 14px;
          border: 0;
          border-radius: 999px;
          background: #ffffff;
          color: #000000;
          font: inherit;
          font-size: 13px;
          line-height: 1.2;
          font-weight: 400;
          cursor: pointer;
          transition: color 160ms ease, opacity 160ms ease;
        }
  
        .playground-mission-control-studio-save-button:hover:not(:disabled) {
          color: #000000;
          opacity: 0.72;
        }
  
        .playground-mission-control-studio-save-button:disabled {
          cursor: default;
          opacity: 0.38;
        }
  
        .playground-mission-control-studio-section-title {
          color: rgba(255, 255, 255, 0.96);
          font-size: 14px;
          line-height: 1.3;
          font-weight: 500;
        }
  
        .playground-mission-control-studio-section-copy {
          margin-top: 4px;
          color: rgba(255, 255, 255, 0.5);
          font-size: 12px;
          line-height: 1.45;
        }
  
        .playground-mission-control-studio-settings-scroll {
          min-height: 0;
          flex: 1 1 auto;
          display: flex;
          flex-direction: column;
          overflow: auto;
          padding: 0 22px 22px;
          background: transparent;
          scrollbar-width: none;
        }
  
        .playground-mission-control-studio-settings-scroll::-webkit-scrollbar {
          display: none;
        }
  
        .playground-mission-control-studio-settings-footer {
          flex: 0 0 auto;
          padding: 12px 22px 20px;
          display: flex;
          justify-content: flex-end;
        }
  
        .playground-mission-control-studio-chat {
          display: flex;
          flex-direction: column;
          border-left: 1px solid rgba(255, 255, 255, 0.15);
        }
  
        .playground-mission-control-studio-chat-header {
          flex: 0 0 auto;
          min-height: 64px;
          padding: 16px 22px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.08);
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 18px;
        }
  
        .playground-mission-control-studio-chat-copy {
          min-width: 0;
        }
  
        .playground-mission-control-studio-chat-title {
          color: rgba(255, 255, 255, 0.96);
          font-size: 14px;
          line-height: 1.3;
          font-weight: 500;
        }
  
        .playground-mission-control-studio-chat-subtitle {
          margin-top: 4px;
          color: rgba(255, 255, 255, 0.5);
          font-size: 12px;
          line-height: 1.4;
        }
  
        .playground-mission-control-setup-pane {
          width: 100%;
          height: 100%;
          min-width: 0;
          min-height: 0;
          display: flex;
          flex: 1 1 auto;
          flex-direction: column;
          overflow: hidden;
        }
  
        .tb-runner-chat.playground-mission-control-setup-runner {
          width: 100%;
          height: 100%;
          min-width: 0;
          min-height: 0;
          flex: 1 1 auto;
          background: transparent;
        }
  
        .tb-runner-chat.playground-mission-control-setup-runner .tb-popup-modal-scrim,
        .tb-runner-chat.playground-mission-control-setup-runner .tb-file-browser-scrim {
          background: transparent;
        }
  
        .tb-runner-chat.playground-mission-control-setup-runner .tb-log-scroll.is-custom-empty-state {
          display: flex;
          align-items: center;
          justify-content: center;
          padding: 28px;
        }
  
        .tb-runner-chat.playground-mission-control-setup-runner .tb-content-width.is-custom-empty-state {
          width: min(100%, 620px);
          max-width: 620px;
        }
  
        .tb-runner-chat.playground-mission-control-setup-runner .task-input-box {
          --tb-task-input-overlay: transparent;
          --tb-task-input-outline: rgba(255, 255, 255, 0.1);
          --tb-task-input-border: rgba(255, 255, 255, 0.1);
          --tb-task-input-base-bg: rgba(30, 30, 30, 0.9);
          -webkit-backdrop-filter: blur(5px);
          backdrop-filter: blur(5px);
        }
  
        .tb-runner-chat.playground-mission-control-setup-runner .task-input-box:has(.sidebar-textarea:focus) {
          --tb-task-input-outline: rgba(255, 255, 255, 0.1);
          --tb-task-input-border: rgba(255, 255, 255, 0.1);
        }
  
        .playground-mission-control-modal .playground-mission-control-setup-pane,
        .playground-mission-control-modal .tb-runner-chat.playground-mission-control-setup-runner {
          width: 100%;
          min-width: 0;
          height: auto;
          min-height: 0;
          flex: 0 0 auto;
          display: block;
          overflow: visible;
        }
  
        .playground-mission-control-modal .tb-runner-chat.playground-mission-control-setup-runner .workinglogsbox {
          display: none !important;
        }
  
        .playground-mission-control-modal .tb-runner-chat.playground-mission-control-setup-runner .tb-input-shell {
          position: static;
          left: auto;
          right: auto;
          bottom: auto;
          padding: 0;
          background: transparent;
          pointer-events: auto;
        }
  
        .playground-mission-control-modal .tb-runner-chat.playground-mission-control-setup-runner .tb-input-width,
        .playground-mission-control-modal .tb-runner-chat.playground-mission-control-setup-runner .embedded-runner-input,
        .playground-mission-control-modal .tb-runner-chat.playground-mission-control-setup-runner .tb-composer-textarea-shell {
          width: 100%;
          max-width: none;
          background: transparent !important;
        }
  
        .playground-mission-control-setup-form {
          width: 100%;
          height: 100%;
          min-height: 100%;
          margin: 0 auto;
          display: flex;
          flex-direction: column;
          gap: 0;
        }
  
        .playground-mission-control-setup-form .playground-tasks-project-composer-modal {
          width: 100%;
          height: 100%;
          min-height: 100%;
          flex: 1 1 auto;
          display: flex;
          flex-direction: column;
          padding: 0;
          border: 0 !important;
          border-radius: 0;
          background: transparent !important;
          background-color: transparent !important;
          box-shadow: none !important;
          backdrop-filter: none !important;
          -webkit-backdrop-filter: none !important;
        }
  
        .playground-mission-control-setup-form .playground-tasks-project-composer-modal::before,
        .playground-mission-control-setup-form .playground-tasks-project-composer-modal::after {
          content: none !important;
          display: none !important;
        }
  
        .playground-mission-control-setup-form .playground-tasks-project-modal-icon-trigger,
        .playground-mission-control-setup-form .playground-tasks-project-modal-icon-trigger:hover,
        .playground-mission-control-setup-form .playground-tasks-project-modal-icon-trigger.is-active,
        .playground-mission-control-setup-form .playground-tasks-project-modal-icon-trigger:focus-visible {
          background: transparent !important;
          border-color: transparent !important;
          box-shadow: none !important;
        }
  
        .playground-mission-control-setup-form .playground-tasks-project-modal-icon-trigger::before,
        .playground-mission-control-setup-form .playground-tasks-project-modal-icon-trigger::after {
          content: none !important;
          display: none !important;
        }
  
        .playground-mission-control-setup-form .playground-tasks-project-modal-top {
          margin-bottom: 12px;
        }
  
        .playground-mission-control-setup-form .playground-tasks-project-modal-actions {
          margin-top: auto;
          padding-top: 16px;
          padding-bottom: 0;
          background: transparent;
          display: flex;
          justify-content: flex-end;
          flex: 0 0 auto;
        }
  
        .playground-mission-control-studio .playground-tasks-project-modal,
        .playground-mission-control-studio .playground-tasks-project-composer-modal {
          border-color: transparent !important;
          background: transparent !important;
          background-color: transparent !important;
          box-shadow: none !important;
          backdrop-filter: none !important;
          -webkit-backdrop-filter: none !important;
        }
  
        .playground-mission-control-studio .playground-tasks-project-modal::before,
        .playground-mission-control-studio .playground-tasks-project-modal::after,
        .playground-mission-control-studio .playground-tasks-project-composer-modal::before,
        .playground-mission-control-studio .playground-tasks-project-composer-modal::after {
          content: none !important;
          display: none !important;
        }
  
        .playground-mission-control-studio .playground-tasks-project-modal-icon-trigger,
        .playground-mission-control-studio .playground-tasks-project-modal-icon-trigger:hover,
        .playground-mission-control-studio .playground-tasks-project-modal-icon-trigger.is-active,
        .playground-mission-control-studio .playground-tasks-project-modal-icon-trigger:focus-visible {
          background: transparent !important;
          border-color: transparent !important;
          box-shadow: none !important;
        }
  
        .playground-mission-control-studio .playground-tasks-project-modal-icon-trigger::before,
        .playground-mission-control-studio .playground-tasks-project-modal-icon-trigger::after {
          content: none !important;
          display: none !important;
        }
  
        .playground-mission-control-studio .playground-tasks-project-blueprint-icon {
          background: transparent !important;
        }
  
        .playground-mission-control-setup-empty-card {
          width: min(100%, 560px);
          margin: 0 auto;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          text-align: left;
        }
  
        .playground-mission-control-setup-empty-kicker {
          color: rgba(255, 255, 255, 0.48);
          font-size: 11px;
          line-height: 1.2;
          font-weight: 500;
          text-transform: uppercase;
          letter-spacing: 0.04em;
        }
  
        .playground-mission-control-setup-empty-title {
          margin-top: 10px;
          color: rgba(255, 255, 255, 0.98);
          font-size: 24px;
          line-height: 1.1;
          font-weight: 500;
        }
  
        .playground-mission-control-setup-empty-copy {
          margin-top: 12px;
          color: rgba(255, 255, 255, 0.58);
          font-size: 13px;
          line-height: 1.55;
        }
  
        .playground-mission-control-capability-list {
          margin-top: 20px;
          display: flex;
          flex-direction: column;
          gap: 18px;
        }
  
        .playground-mission-control-capability-item {
          display: grid;
          grid-template-columns: 22px minmax(0, 1fr);
          gap: 12px;
          align-items: start;
        }
  
        .playground-mission-control-capability-icon {
          width: 18px;
          height: 18px;
          margin-top: 1px;
          color: rgba(255, 255, 255, 0.56);
        }
  
        .playground-mission-control-capability-title {
          color: rgba(255, 255, 255, 0.92);
          font-size: 14px;
          line-height: 1.35;
          font-weight: 500;
        }
  
        .playground-mission-control-capability-copy {
          margin-top: 4px;
          color: rgba(255, 255, 255, 0.5);
          font-size: 13px;
          line-height: 1.45;
        }
  
        .playground-mission-control-project-modal {
          width: min(820px, 100%);
          max-height: min(72vh, 780px);
          margin: 0 auto;
          overflow: auto;
          scrollbar-width: none;
        }
  
        .playground-mission-control-project-modal::-webkit-scrollbar {
          width: 0;
          height: 0;
          display: none;
        }
  
        .playground-mission-control-project-modal .playground-tasks-project-modal-top {
          margin-bottom: 0;
        }
  
        .playground-mission-control-project-connectors {
          margin-top: 18px;
        }
  
        .playground-mission-control-project-attachments {
          margin-top: 18px;
        }
  
        .playground-mission-control-project-attachment-list {
          display: flex;
          flex-wrap: wrap;
          gap: 8px;
        }
  
        .playground-mission-control-setup-title {
          margin: 0;
          text-align: center;
          font-size: 24px;
          line-height: 1.08;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-mission-control-setup-box {
          width: 100%;
          margin-top: 20px;
          padding: 20px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 15px;
          background: rgba(255, 255, 255, 0.1);
          box-sizing: border-box;
          display: flex;
          flex-direction: column;
          gap: 18px;
        }
  
        .playground-mission-control-setup-section {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 12px;
        }
  
        .playground-mission-control-setup-section-title {
          width: 100%;
          padding-bottom: 12px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
          font-size: 12px;
          line-height: 1.2;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.92);
        }
  
        .playground-mission-control-setup-description,
        .playground-mission-control-setup-empty {
          font-size: 12px;
          line-height: 1.6;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.62);
        }
  
        .playground-mission-control-setup-description p {
          margin: 0;
        }
  
        .playground-mission-control-setup-list {
          display: flex;
          flex-direction: column;
          gap: 2px;
        }
  
        .playground-mission-control-setup-row {
          min-height: 30px;
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 16px;
        }
  
        .playground-mission-control-setup-row-label {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          gap: 8px;
          font-size: 12px;
          line-height: 1.3;
          color: rgba(255, 255, 255, 0.84);
        }
  
        .playground-mission-control-setup-row-icon {
          width: 14px;
          height: 14px;
          flex: 0 0 auto;
          object-fit: contain;
        }
  
        .playground-mission-control-setup-row-value {
          min-width: 0;
          max-width: 52%;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
          font-size: 12px;
          line-height: 1.3;
          color: rgba(255, 255, 255, 0.62);
          text-align: right;
        }
  
        .playground-mission-control-setup-row.is-empty .playground-mission-control-setup-row-value {
          color: rgba(255, 255, 255, 0.42);
        }
  
        .playground-mission-control-setup-attachments {
          display: flex;
          flex-wrap: wrap;
          gap: 8px;
        }
  
        .playground-mission-control-setup-attachment {
          min-width: 0;
          max-width: 100%;
          height: 28px;
          padding: 0 10px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.08);
          display: inline-flex;
          align-items: center;
          gap: 7px;
          font-size: 12px;
          line-height: 1;
          color: rgba(255, 255, 255, 0.74);
        }
  
        .playground-mission-control-setup-attachment span {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
        }
  
        .playground-mission-control-setup-attachment-icon {
          width: 13px;
          height: 13px;
          flex: 0 0 auto;
        }
  
        .playground-mission-control-setup-error {
          margin: 0;
        }
  
        .playground-content-body.is-tasks-page .playground-environments-page:not(.playground-agents-page) .playground-environments-detail-scroll.playground-tasks-project-workspace-scroll,
        .playground-content-body.is-calendar-page .playground-environments-page:not(.playground-agents-page) .playground-environments-detail-scroll.playground-tasks-project-workspace-scroll {
          position: relative;
          margin: 0;
          padding: 42px 50px 56px;
          border: 0;
          border-radius: 0;
          background: #000;
          background-image: none !important;
          background-clip: padding-box;
          box-sizing: border-box;
          gap: 14px;
        }
  
        .playground-content-body.is-tasks-page .playground-environments-page:not(.playground-agents-page) .playground-environments-detail-scroll.playground-tasks-project-workspace-scroll.is-backlog {
          padding-bottom: 10px;
        }
  
        .playground-content-body.is-tasks-page .playground-environments-page.playground-tasks-ticket-screen:not(.playground-agents-page) .playground-environments-detail-scroll.playground-tasks-project-workspace-scroll {
          padding-right: 0;
          padding-bottom: 10px;
        }
  
        .playground-content-body.is-tasks-page .playground-environments-page:not(.playground-agents-page) .playground-environments-detail-scroll.playground-tasks-project-workspace-scroll::before,
        .playground-content-body.is-calendar-page .playground-environments-page:not(.playground-agents-page) .playground-environments-detail-scroll.playground-tasks-project-workspace-scroll::before {
          content: none !important;
          display: none !important;
        }
  
        .playground-content-body.is-tasks-page .playground-environments-page:not(.playground-agents-page) .playground-environments-detail-scroll.playground-tasks-project-workspace-scroll.is-mission-control-setup,
        .playground-content-body.is-calendar-page .playground-environments-page:not(.playground-agents-page) .playground-environments-detail-scroll.playground-tasks-project-workspace-scroll.is-mission-control-setup {
          padding: 0;
          gap: 0;
          background: transparent !important;
        }
  
        .playground-mission-control-studio .playground-mission-control-studio-settings-scroll,
        .playground-mission-control-studio .playground-mission-control-setup-form,
        .playground-mission-control-studio .playground-tasks-project-composer-modal,
        .playground-mission-control-studio .playground-tasks-project-modal-top,
        .playground-mission-control-studio .playground-tasks-project-modal-attachments,
        .playground-mission-control-studio .playground-tasks-project-modal-connectors {
          background: transparent !important;
        }
  
        .playground-mission-control-studio .playground-mission-control-setup-form,
        .playground-mission-control-studio .playground-tasks-project-composer-modal {
          min-height: 0;
          flex: 1 1 auto;
        }
  
        .playground-mission-control-studio .playground-mission-control-studio-settings {
          background: rgba(255, 255, 255, 0.05) !important;
        }
  
        .playground-mission-control-studio .playground-tasks-project-blueprint-section,
        .playground-mission-control-setup-form .playground-tasks-project-blueprint-section {
          margin: 0 0 14px;
          gap: 0;
        }
  
        .playground-mission-control-studio .playground-tasks-project-blueprint-section::before,
        .playground-mission-control-studio .playground-tasks-project-blueprint-section::after,
        .playground-mission-control-setup-form .playground-tasks-project-blueprint-section::before,
        .playground-mission-control-setup-form .playground-tasks-project-blueprint-section::after {
          content: none !important;
          display: none !important;
        }
  
        .playground-mission-control-studio .playground-tasks-project-blueprint-heading,
        .playground-mission-control-studio .playground-tasks-project-blueprint-active,
        .playground-mission-control-studio .playground-tasks-project-blueprint-chips,
        .playground-mission-control-studio .playground-tasks-project-blueprint-chip,
        .playground-mission-control-studio .playground-tasks-project-blueprint-section > .playground-tasks-detail-section-title,
        .playground-mission-control-setup-form .playground-tasks-project-blueprint-heading,
        .playground-mission-control-setup-form .playground-tasks-project-blueprint-active,
        .playground-mission-control-setup-form .playground-tasks-project-blueprint-chips,
        .playground-mission-control-setup-form .playground-tasks-project-blueprint-chip,
        .playground-mission-control-setup-form .playground-tasks-project-blueprint-section > .playground-tasks-detail-section-title {
          display: none !important;
        }
  
        .playground-mission-control-studio .playground-tasks-project-modal-icon-trigger,
        .playground-mission-control-setup-form .playground-tasks-project-modal-icon-trigger {
          background: transparent !important;
          background-color: transparent !important;
          border: 0 !important;
          box-shadow: none !important;
        }
  
        .playground-mission-control-studio .playground-tasks-project-modal-icon-trigger::before,
        .playground-mission-control-studio .playground-tasks-project-modal-icon-trigger::after,
        .playground-mission-control-setup-form .playground-tasks-project-modal-icon-trigger::before,
        .playground-mission-control-setup-form .playground-tasks-project-modal-icon-trigger::after {
          content: none !important;
          display: none !important;
        }
  
        .playground-mission-control-studio .playground-tasks-project-modal-attachments,
        .playground-mission-control-studio .playground-tasks-project-modal-connectors,
        .playground-mission-control-studio .playground-tasks-detail-description,
        .playground-mission-control-studio .playground-tasks-detail-description-editor,
        .playground-mission-control-studio .playground-tasks-detail-description-preview-scope,
        .playground-mission-control-setup-form .playground-tasks-project-modal-attachments,
        .playground-mission-control-setup-form .playground-tasks-project-modal-connectors,
        .playground-mission-control-setup-form .playground-tasks-detail-description,
        .playground-mission-control-setup-form .playground-tasks-detail-description-editor,
        .playground-mission-control-setup-form .playground-tasks-detail-description-preview-scope {
          background: transparent !important;
        }
  
        .playground-mission-control-studio .playground-tasks-project-blueprint-trigger .playground-tasks-project-blueprint-icon,
        .playground-mission-control-studio .playground-tasks-project-blueprint-option .playground-tasks-project-blueprint-icon,
        .playground-mission-control-setup-form .playground-tasks-project-blueprint-trigger .playground-tasks-project-blueprint-icon,
        .playground-mission-control-setup-form .playground-tasks-project-blueprint-option .playground-tasks-project-blueprint-icon {
          background: transparent !important;
          border: 0 !important;
          box-shadow: none !important;
        }
  
  ${CALENDAR_STYLE_FRAGMENTS.standaloneContext}
        @media (max-width: 980px) {
          .playground-mission-control-studio-header {
            padding: 0 14px;
          }
  
          .playground-mission-control-studio-body {
            grid-template-columns: minmax(0, 1fr);
            grid-template-rows: minmax(320px, 45vh) minmax(0, 1fr);
          }
  
          .playground-mission-control-studio-settings {
            border-bottom: 1px solid rgba(255, 255, 255, 0.08);
          }
  
          .playground-mission-control-studio-section-header,
          .playground-mission-control-studio-chat-header {
            padding-left: 16px;
            padding-right: 16px;
          }
  
          .playground-mission-control-studio-settings-scroll {
            padding-left: 16px;
            padding-right: 16px;
          }
  
          .playground-mission-control-studio-settings-footer {
            padding-left: 16px;
            padding-right: 16px;
          }
        }
  
        @media (max-width: 640px) {
          .playground-mission-control-studio-body {
            grid-template-rows: minmax(300px, 42vh) minmax(0, 1fr);
          }
  
          .playground-mission-control-studio-subtitle,
          .playground-mission-control-studio-chat-subtitle,
          .playground-mission-control-studio-section-copy {
            display: none;
          }
        }
  
  ${CALENDAR_STYLE_FRAGMENTS.standaloneSurface}
        .playground-tasks-project-menu-item.is-danger,
        .playground-tasks-project-menu-item.is-danger .playground-files-toolbar-menu-item-copy span:first-child {
          color: #ffb0b0;
        }
  
        .playground-tasks-secondary-copy {
          font-size: 12px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.48);
        }
  
        .playground-tasks-backlog-view {
          width: min(100%, var(--playground-thread-content-max-width));
          max-width: var(--playground-thread-content-max-width);
          height: auto;
          flex: 1;
          min-height: 0;
          margin: 0 auto;
          display: flex;
          flex-direction: column;
          gap: 0;
        }
  
        .playground-tasks-backlog-header {
          display: flex;
          flex-direction: column;
          align-items: stretch;
          gap: 10px;
          margin-top: 0;
          padding: 4px 0 10px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-tasks-backlog-header-row {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 16px;
        }
  
        .playground-tasks-backlog-header-row.is-secondary {
          align-items: flex-start;
        }
  
        .playground-tasks-backlog-header-row.is-tertiary {
          align-items: center;
        }
  
        .playground-tasks-backlog-header-main {
          min-width: 0;
          display: flex;
          align-items: center;
          gap: 12px;
        }
  
        .playground-tasks-backlog-header-copy {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 4px;
        }
  
        .playground-tasks-backlog-heading {
          font-size: 18px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.94);
          line-height: 1.2;
        }
  
        .playground-tasks-backlog-open-count {
          display: inline-flex;
          align-items: center;
          flex: 0 0 auto;
        }
  
        .playground-tasks-backlog-heading-count {
          backdrop-filter: blur(20px);
          -webkit-backdrop-filter: blur(20px);
        }
  
        .playground-tasks-backlog-description {
          min-width: 0;
          flex: 1 1 auto;
          max-width: none;
          font-size: 12px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.7);
          white-space: normal;
          overflow: visible;
          text-overflow: clip;
        }
  
        .playground-tasks-backlog-description .tb-message-markdown-paragraph,
        .playground-tasks-backlog-description .tb-message-markdown-list,
        .playground-tasks-backlog-description .tb-message-markdown-heading {
          margin: 0;
          font-size: inherit;
          line-height: inherit;
          color: inherit;
        }
  
        .playground-tasks-backlog-description .tb-message-markdown-strong,
        .playground-tasks-backlog-description .tb-message-markdown-em,
        .playground-tasks-backlog-description .tb-message-markdown-inline-code,
        .playground-tasks-backlog-description .tb-message-markdown-link {
          color: inherit;
        }
  
        .playground-tasks-backlog-primary-actions,
        .playground-tasks-backlog-secondary-actions,
        .playground-tasks-backlog-tertiary-actions {
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          gap: 8px;
          flex: 0 0 auto;
        }
  
        .playground-tasks-backlog-primary-actions {
          margin-left: auto;
        }
  
        .playground-tasks-backlog-secondary-actions {
          align-self: flex-start;
        }
  
        .playground-tasks-backlog-header-row.is-tertiary .playground-tasks-backlog-secondary-actions {
          flex: 1 1 auto;
          min-width: 0;
          align-items: stretch;
          justify-content: flex-start;
        }
  
        .playground-tasks-backlog-header-row.is-tertiary .playground-plugins-search-shell {
          --playground-tasks-backlog-search-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          flex: 0 1 360px;
          width: min(360px, 100%);
          min-width: 0;
          max-width: 360px;
          position: relative;
          z-index: 0;
          overflow: hidden;
          border-radius: 999px;
        }
  
        .playground-tasks-backlog-header-row.is-tertiary .playground-plugins-search-shell::before {
          content: "";
          pointer-events: none;
          position: absolute;
          inset: 0;
          z-index: 2;
          border-radius: inherit;
          padding: 1px;
          background: var(--playground-tasks-backlog-search-border);
          mask-image: linear-gradient(#fff 0 0), linear-gradient(#fff 0 0);
          mask-clip: content-box, border-box;
          mask-composite: exclude;
          mask-origin: content-box, border-box;
          mask-repeat: repeat, repeat;
          mask-size: auto, auto;
        }
  
        .playground-tasks-backlog-header-row.is-tertiary .playground-plugins-search {
          position: relative;
          z-index: 1;
          min-height: 32px;
          border: 0;
          background: rgba(255, 255, 255, 0.05) !important;
          font-size: 12px;
          -webkit-backdrop-filter: blur(50px);
          backdrop-filter: blur(50px);
        }
  
        .playground-tasks-backlog-header-row.is-tertiary .playground-plugins-search-icon {
          z-index: 3;
        }
  
        .playground-tasks-backlog-header-row.is-tertiary .playground-tasks-backlog-search-shell {
          box-sizing: border-box;
          border: 1px solid rgba(255, 255, 255, 0.1);
          background: #000;
        }
  
        .playground-tasks-backlog-header-row.is-tertiary .playground-tasks-backlog-search-shell::before {
          content: none;
          display: none;
        }
  
        .playground-tasks-backlog-header-row.is-tertiary .playground-tasks-backlog-search-shell .playground-plugins-search {
          background: #000 !important;
        }
  
        .playground-tasks-backlog-header-row.is-tertiary .playground-files-control-button {
          min-height: 32px;
        }
  
        .playground-tasks-backlog-project-actions {
          flex-wrap: wrap;
        }
  
        .playground-tasks-backlog-tertiary-actions {
          margin-left: auto;
        }
  
        .playground-tasks-backlog-secondary-actions .playground-files-control-button.is-backlog-sort {
          padding-left: 0;
        }
  
        .playground-tasks-backlog-secondary-actions .playground-files-control-button.is-board-filter {
          padding-left: 0;
        }
  
        .playground-tasks-backlog-sort-shell .playground-tasks-toolbar-popup-menu,
        .playground-tasks-backlog-filter-shell .playground-tasks-toolbar-popup-menu,
        .playground-tasks-board-filter-shell .playground-tasks-toolbar-popup-menu {
          left: 0;
          right: auto;
          transform-origin: top left;
        }
  
        .playground-tasks-release-control-row {
          gap: 8px;
        }
  
        .playground-tasks-toolbar-popup-shell {
          position: relative;
          z-index: 31;
        }
  
        .playground-tasks-toolbar-popup-shell.playground-tasks-toolbar-popup-shell-portal {
          position: fixed;
          z-index: 360;
        }
  
        .playground-tasks-toolbar-popup-shell .tb-popup-menu {
          position: absolute;
          z-index: 32;
        }
  
        .playground-tasks-toolbar-popup-shell .playground-tasks-toolbar-popup-menu {
          top: calc(100% + 8px);
          bottom: auto;
          left: auto;
          right: 0;
          width: 260px;
          min-width: 260px;
          max-height: min(360px, calc(100vh - 180px));
          display: flex;
          flex-direction: column;
          overflow-y: auto;
          transform-origin: top right;
        }
  
        .playground-tasks-toolbar-popup-shell.playground-tasks-toolbar-popup-shell-portal .playground-tasks-toolbar-popup-menu {
          top: 0;
          left: 0;
          right: auto;
          bottom: auto;
          transform-origin: top left;
        }
  
        .playground-tasks-toolbar-popup-shell .playground-tasks-toolbar-popup-menu-wide {
          width: 272px;
          min-width: 272px;
        }
  
        .playground-tasks-toolbar-popup-shell .playground-tasks-release-picker-menu {
          width: min(360px, calc(100vw - 24px));
          min-width: min(360px, calc(100vw - 24px));
          max-height: min(560px, calc(100vh - 140px));
          overflow: hidden;
        }
  
        .playground-tasks-release-picker-menu,
        .playground-tasks-release-picker-menu * {
          font-size: 12px;
          font-weight: 400;
        }
  
        .playground-tasks-release-picker-body {
          min-height: 0;
          overflow: auto;
          padding: 6px;
        }
  
        .playground-tasks-release-picker-footer {
          padding: 6px;
          border-top: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-tasks-release-picker-item.is-selected {
          background: rgba(255, 255, 255, 0.08);
        }
  
        .playground-tasks-release-picker-item.is-selected .notification-menu-icon {
          color: #fff;
        }
  
        .playground-tasks-release-picker-create {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 8px;
          background: transparent;
        }
  
        .playground-tasks-release-picker-create:hover:not(:disabled) {
          background: transparent;
        }
  
        .playground-tasks-release-picker-menu > * {
          position: relative;
          z-index: 6;
        }
  
        .playground-tasks-toolbar-popup-shell .playground-tasks-project-search-menu {
          width: 320px;
          min-width: 320px;
          max-height: min(420px, calc(100vh - 140px));
          padding: 0;
          overflow: hidden;
        }
  
        .playground-tasks-project-search-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          padding: 12px 16px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-tasks-project-search-title {
          font-size: 13px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-tasks-project-search-close {
          width: 24px;
          height: 24px;
          padding: 0;
          border: 0;
          border-radius: 999px;
          background: transparent;
          color: rgba(255, 255, 255, 0.48);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          transition: background-color 160ms ease, color 160ms ease;
        }
  
        .playground-tasks-project-search-close:hover {
          background: rgba(255, 255, 255, 0.08);
          color: #fff;
        }
  
        .playground-tasks-project-search-body {
          min-height: 0;
          flex: 1;
          padding: 14px 16px 16px;
          overflow: auto;
        }
  
        .playground-tasks-project-search-hint {
          padding: 18px 8px 4px;
          text-align: center;
          font-size: 12px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.42);
        }
  
        .playground-tasks-toolbar-popup-shell.playground-tasks-backlog-sort-shell .playground-tasks-toolbar-popup-menu,
        .playground-tasks-toolbar-popup-shell.playground-tasks-backlog-filter-shell .playground-tasks-toolbar-popup-menu,
        .playground-tasks-toolbar-popup-shell.playground-tasks-board-filter-shell .playground-tasks-toolbar-popup-menu,
        .playground-tasks-toolbar-popup-shell.playground-environments-search-shell .playground-tasks-toolbar-popup-menu {
          left: 0;
          right: auto;
          transform-origin: top left;
        }
  
        .playground-tasks-toolbar-popup-shell.playground-environments-search-shell {
          z-index: 95;
        }
  
        .playground-tasks-toolbar-popup-shell.playground-environments-search-shell .playground-tasks-toolbar-popup-menu {
          z-index: 96;
        }
  
        .playground-resources-overview-section .playground-resources-overview-search-row,
        .playground-resources-overview-section .playground-develop-server-kind-table-toolbar {
          position: relative;
          z-index: 120;
        }
  
        .playground-resources-overview-section .playground-tasks-toolbar-popup-shell {
          z-index: 121;
        }
  
        .playground-resources-overview-section .playground-tasks-toolbar-popup-menu {
          z-index: 122;
        }
  
        .playground-tasks-toolbar-popup-shell .playground-tasks-toolbar-popup-menu-animate-down-in {
          animation: playground-tasks-toolbar-popup-fade-down-in 180ms cubic-bezier(0.16, 1, 0.3, 1) both;
        }
  
        .playground-tasks-backlog-context-menu-shell {
          position: fixed;
          z-index: 96;
          pointer-events: none;
        }
  
        .playground-tasks-backlog-context-menu-shell .playground-tasks-backlog-context-menu {
          top: 0;
          left: 0;
          right: auto;
          max-height: min(360px, calc(100vh - 24px));
          transform-origin: top left;
          pointer-events: auto;
        }
  
        .playground-tasks-toolbar-popup-shell .tb-popup-menu-title {
          padding: 12px 16px;
          color: rgba(255, 255, 255, 0.65);
          font-size: 12px;
          line-height: 1.2;
        }
  
        .playground-tasks-toolbar-popup-shell .tb-popup-row {
          width: 100%;
          display: flex;
          align-items: center;
          gap: 12px;
          padding: 12px 16px;
          border: 0;
          background: transparent;
          color: white;
          cursor: pointer;
          text-align: left;
          transition: background-color 160ms ease;
        }
  
        .playground-tasks-toolbar-popup-shell .tb-popup-row:hover,
        .playground-tasks-toolbar-popup-shell .tb-popup-row.selected {
          background: rgba(255, 255, 255, 0.1);
        }
  
        .playground-tasks-toolbar-popup-shell .tb-popup-row-select.selected {
          background: transparent;
        }
  
        .playground-tasks-toolbar-popup-shell .tb-popup-icon {
          color: rgba(255, 255, 255, 0.6);
          flex: 0 0 auto;
        }
  
        .playground-tasks-toolbar-popup-shell .tb-popup-check-slot {
          width: 16px;
          height: 16px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          flex-shrink: 0;
        }
  
        .playground-tasks-toolbar-popup-shell .tb-popup-check {
          color: white;
        }
  
        .playground-thread-nav-popup-shell {
          position: relative;
          z-index: 35;
        }
  
        .playground-thread-nav-popup-shell .playground-thread-nav-popup-menu {
          top: calc(100% + 8px);
          right: 0;
          left: auto;
          bottom: auto;
          width: 280px;
          min-width: 280px;
          max-height: min(720px, calc(100vh - 72px));
          transform-origin: top right;
        }
  
        .playground-thread-nav-popup-static-row {
          cursor: default;
          user-select: text;
        }
  
        .playground-thread-nav-popup-static-row:hover {
          background: transparent;
        }
  
        .playground-thread-nav-popup-fact {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          width: 100%;
          min-width: 0;
        }
  
        .playground-thread-nav-popup-fact-label {
          color: rgba(255, 255, 255, 0.72);
          font-size: 12px;
          line-height: 1.2;
          flex: 0 0 auto;
        }
  
        .playground-thread-nav-popup-fact-value {
          min-width: 0;
          color: rgba(255, 255, 255, 0.94);
          font-size: 12px;
          line-height: 1.2;
          text-align: right;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-thread-nav-popup-divider {
          height: 1px;
          margin: 4px 16px;
          background: rgba(255, 255, 255, 0.08);
          pointer-events: none;
        }
  
        .playground-thread-nav-popup-thread-id {
          font-family: ui-monospace, SFMono-Regular, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
          word-break: break-all;
        }
  
        .playground-thread-nav-popup-link-button {
          min-width: 0;
          padding: 0;
          border: 0;
          background: transparent;
          color: rgba(255, 255, 255, 0.94);
          font-size: 12px;
          line-height: 1.2;
          text-align: left;
          cursor: pointer;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-thread-nav-popup-link-button:hover {
          color: #66a6ff;
        }
  
        .playground-thread-task-list-popup-shell {
          position: relative;
          z-index: 35;
        }
  
        .playground-thread-task-list-popup-shell .playground-thread-task-list-popup-menu {
          top: calc(100% + 8px);
          right: 0;
          left: auto;
          bottom: auto;
          width: 360px;
          min-width: 320px;
          max-height: min(620px, calc(100vh - 72px));
          overflow: hidden;
          transform-origin: top right;
        }
  
        .playground-thread-task-list-popup-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          padding: 14px 16px 10px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-thread-task-list-popup-title {
          color: rgba(255, 255, 255, 0.96);
          font-size: 13px;
          font-weight: 500;
          line-height: 1.2;
        }
  
        .playground-thread-task-list-popup-count {
          color: rgba(255, 255, 255, 0.62);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.2;
          white-space: nowrap;
        }
  
        .playground-thread-task-list-popup-items {
          display: flex;
          flex-direction: column;
          max-height: min(460px, calc(100vh - 170px));
          overflow-y: auto;
          padding: 4px 0 10px;
        }
  
        .playground-thread-task-list-popup-item {
          display: flex;
          align-items: flex-start;
          gap: 10px;
          padding: 9px 16px;
          color: rgba(255, 255, 255, 0.9);
          font-size: 12px;
          line-height: 1.35;
          min-width: 0;
        }
  
        .playground-thread-task-list-popup-item-icon {
          width: 15px;
          height: 15px;
          margin-top: 1px;
          flex: 0 0 auto;
          color: #ffffff;
        }
  
        .playground-thread-task-list-popup-item-icon.is-complete {
          color: #ffffff;
        }
  
        .playground-thread-task-list-popup-item-text {
          min-width: 0;
          flex: 1 1 auto;
          max-width: 100%;
          white-space: normal;
          overflow-wrap: anywhere;
          word-break: break-word;
        }
  
        .playground-thread-task-list-popup-item-text.is-complete {
          color: rgba(255, 255, 255, 0.55);
          text-decoration: line-through;
        }
  
        .playground-thread-task-list-popup-state {
          padding: 12px 16px 16px;
          color: rgba(255, 255, 255, 0.62);
          font-size: 12px;
          line-height: 1.4;
        }
  
        .playground-tasks-toolbar-popup-item-copy {
          min-width: 0;
          flex: 1;
          display: flex;
          flex-direction: column;
          gap: 3px;
        }
  
        .playground-tasks-toolbar-popup-item-copy span:first-child {
          font-size: 12px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-tasks-toolbar-popup-item-copy span + span {
          font-size: 11px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.48);
        }
  
        .playground-tasks-toolbar-popup-item-copy .playground-team-access-option-description {
          color: rgba(255, 255, 255, 0.5);
        }
  
        .playground-agents-detail-action-menu-meta {
          display: flex;
          flex-direction: column;
          gap: 8px;
          padding: 12px 14px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
  
        .playground-agents-detail-action-menu-meta-row {
          min-width: 0;
          display: grid;
          grid-template-columns: 70px minmax(0, 1fr);
          gap: 12px;
          align-items: baseline;
        }
  
        .playground-agents-detail-action-menu-meta-label {
          color: rgba(255, 255, 255, 0.46);
          font-size: 11px;
          line-height: 1.3;
          font-weight: 400;
        }
  
        .playground-agents-detail-action-menu-meta-value {
          min-width: 0;
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
          color: rgba(255, 255, 255, 0.9);
          font-size: 11px;
          line-height: 1.3;
          font-weight: 400;
          text-align: right;
        }
  
        .playground-tasks-backlog-list {
          flex: 1;
          min-height: 0;
          display: flex;
          flex-direction: column;
          overflow: auto;
          scrollbar-width: none;
          gap: 8px;
          padding-top: 17px;
          padding-bottom: 16px;
        }
  
        .playground-tasks-backlog-list::-webkit-scrollbar {
          display: none;
        }
  
        .playground-tasks-backlog-item {
          --playground-tasks-backlog-item-border: linear-gradient(
            -10deg,
            rgba(200, 200, 200, 0.25),
            rgba(255, 255, 255, 0.1),
            rgba(255, 255, 255, 0.15),
            rgba(255, 255, 255, 0.375)
          );
          position: relative;
          width: 100%;
          min-width: 0;
          padding: 10px;
          border: 1px solid rgba(255, 255, 255, 0.05);
          border-radius: 10px;
          background: rgba(255, 255, 255, 0.05);
          backdrop-filter: blur(20px);
          -webkit-backdrop-filter: blur(20px);
          color: rgba(255, 255, 255, 0.72);
          text-align: left;
          cursor: pointer;
          transition: color 160ms ease, background-color 160ms ease, border-color 160ms ease;
        }
  
        .playground-tasks-backlog-item:focus,
        .playground-tasks-backlog-item:focus-visible {
          outline: none;
          box-shadow: none;
        }
  
        .playground-tasks-backlog-item::before,
        .playground-tasks-backlog-item::after {
          content: "";
          position: absolute;
          pointer-events: none;
        }
  
        .playground-tasks-backlog-item::before {
          content: none;
          display: none;
        }
  
        .playground-tasks-backlog-item::after {
          left: 0;
          right: 0;
          top: var(--playground-tasks-drop-marker-top, auto);
          bottom: var(--playground-tasks-drop-marker-bottom, auto);
          height: 2px;
          border-radius: 999px;
          background: transparent;
          transition: background-color 150ms ease, box-shadow 150ms ease;
        }
  
        .playground-tasks-backlog-item.is-drop-before {
          --playground-tasks-drop-marker-top: 0;
        }
  
        .playground-tasks-backlog-item.is-drop-after {
          --playground-tasks-drop-marker-bottom: 0;
        }
  
        .playground-tasks-backlog-item.is-drop-after::after {
          transform: translateY(50%);
          background: #66a6ff;
          box-shadow: 0 0 0 1px rgba(102, 166, 255, 0.2);
        }
  
        .playground-tasks-backlog-item.is-drop-before::after {
          transform: translateY(-50%);
          background: #66a6ff;
          box-shadow: 0 0 0 1px rgba(102, 166, 255, 0.2);
        }
  
        .playground-tasks-backlog-item.is-subtask {
          background: transparent;
        }
  
        .playground-tasks-backlog-item.is-draggable {
          cursor: grab;
        }
  
        .playground-tasks-backlog-item.is-dragging {
          opacity: 0.5;
          background: var(--playground-task-color-surface-active, rgba(255, 255, 255, 0.1));
        }
  
        .playground-tasks-backlog-item.is-status-menu-open {
          z-index: 120;
        }
  
        .playground-tasks-backlog-tree-node {
          min-width: 0;
        }
  
        .playground-tasks-backlog-children {
          display: flex;
          flex-direction: column;
          gap: 8px;
          margin-top: 8px;
          margin-left: 11px;
          padding-left: 20px;
        }
  
        .playground-tasks-backlog-item-content {
          width: 100%;
          min-width: 0;
          display: flex;
          align-items: center;
          gap: 12px;
        }
  
        .playground-tasks-mission-control-card {
          margin-top: 10px;
        }
  
        .playground-tasks-mission-control-surface {
          display: flex;
          flex-direction: column;
          gap: 10px;
          min-height: 94px;
          cursor: default;
        }
  
        .playground-tasks-mission-control-header {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 14px;
        }
  
        .playground-tasks-mission-control-title {
          min-width: 0;
          flex: 1 1 auto;
          font-size: 14px;
          line-height: 1.35;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-tasks-mission-control-actions {
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          gap: 8px;
        }
  
        .playground-tasks-mission-control-button {
          min-height: 30px;
          padding: 0 12px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.05);
          color: rgba(255, 255, 255, 0.88);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 8px;
          font-size: 12px;
          font-weight: 500;
          cursor: pointer;
          transition: background-color 160ms ease, border-color 160ms ease, color 160ms ease, opacity 160ms ease;
        }
  
        .playground-tasks-mission-control-button:hover {
          background: rgba(255, 255, 255, 0.08);
          border-color: rgba(255, 255, 255, 0.16);
        }
  
        .playground-tasks-mission-control-button.is-primary {
          background: rgba(72, 132, 255, 0.18);
          border-color: rgba(72, 132, 255, 0.28);
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-tasks-mission-control-button.is-primary:hover {
          background: rgba(72, 132, 255, 0.24);
          border-color: rgba(72, 132, 255, 0.36);
        }
  
        .playground-tasks-mission-control-button[disabled] {
          opacity: 0.5;
          cursor: not-allowed;
        }
  
        .playground-tasks-mission-control-button-spinner {
          width: 12px;
          height: 12px;
          flex-shrink: 0;
        }
  
        .playground-tasks-mission-control-summary {
          font-size: 12px;
          line-height: 1.6;
          font-weight: 400;
          color: rgba(255, 255, 255, 0.78);
        }
  
        .playground-tasks-mission-control-summary.is-empty {
          color: rgba(255, 255, 255, 0.56);
        }
  
        .playground-tasks-mission-control-error {
          font-size: 11px;
          line-height: 1.5;
          color: rgba(255, 128, 128, 0.88);
        }
  
        .playground-tasks-backlog-leading {
          flex: 1 1 auto;
          min-width: 0;
          display: flex;
          align-items: center;
          gap: 7px;
        }
  
        .playground-tasks-backlog-main {
          flex: 1 1 auto;
          min-width: 0;
          display: flex;
          align-items: center;
          gap: 7px;
        }
  
        .playground-tasks-backlog-section {
          display: flex;
          flex-direction: column;
          gap: 8px;
        }
  
        .playground-tasks-backlog-section.is-release-drop-target {
          margin-inline: -10px;
          padding: 8px 10px;
          border-radius: 14px;
          background: rgba(102, 166, 255, 0.08);
          box-shadow: inset 0 0 0 1px rgba(102, 166, 255, 0.22);
        }
  
        .playground-tasks-backlog-section + .playground-tasks-backlog-section {
          margin-top: 8px;
        }
  
        .playground-tasks-backlog-section-header {
          display: flex;
          align-items: baseline;
          justify-content: space-between;
          gap: 8px;
          min-width: 0;
          flex-wrap: wrap;
        }
  
        .playground-tasks-backlog-section-copy-group {
          min-width: 0;
          display: inline-flex;
          align-items: baseline;
          gap: 8px;
          flex-wrap: wrap;
          flex: 1 1 auto;
        }
  
        .playground-tasks-backlog-section-title {
          color: rgba(255, 255, 255, 1);
          font-size: 12px;
          line-height: 1.35;
          font-weight: 500;
        }
  
        .playground-tasks-backlog-section-copy {
          color: rgba(255, 255, 255, 0.7);
          font-size: 12px;
          line-height: 1.4;
          font-weight: 400;
        }
  
        .playground-tasks-backlog-section-meta {
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          gap: 6px;
          color: rgba(255, 255, 255, 0.7);
          font-size: 12px;
          line-height: 1.4;
          font-weight: 400;
          white-space: nowrap;
          text-align: right;
        }
  
        .playground-tasks-backlog-section-date,
        .playground-tasks-release-header-date {
          white-space: nowrap;
        }
  
        .playground-tasks-release-edit-button {
          width: 22px;
          height: 22px;
          padding: 0;
          border: 0;
          border-radius: 6px;
          background: transparent;
          color: rgba(255, 255, 255, 0.48);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          cursor: pointer;
          transition: background-color 160ms ease, color 160ms ease;
        }
  
        .playground-tasks-release-edit-button:hover {
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.92);
        }
  
        .playground-tasks-backlog-project-icon {
          width: 20px;
          height: 20px;
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border-radius: 5px;
          color: white;
        }
  
        .playground-tasks-backlog-project-icon svg {
          width: 12px;
          height: 12px;
          filter: drop-shadow(0 1px 1.5px rgba(0, 0, 0, 0.28));
        }
  
        .playground-tasks-backlog-project-icon.is-task {
          background: linear-gradient(180deg, #39b877 0%, #2b8b59 100%);
        }
  
        .playground-tasks-backlog-project-icon.is-subtask {
          background: linear-gradient(180deg, #4f7fc5 0%, #1e4585 100%);
        }
  
        .playground-tasks-backlog-ticket {
          flex: 0 0 auto;
          color: rgba(255, 255, 255, 0.7);
          font-size: 12px;
          font-weight: 400;
          line-height: 1;
        }
  
        .playground-tasks-backlog-item:hover {
          background: var(--playground-task-color-surface-hover, rgba(255, 255, 255, 0.07));
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-tasks-backlog-item.is-active {
          background: var(--playground-task-color-surface-active, rgba(255, 255, 255, 0.1));
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-tasks-project-workspace .playground-tasks-backlog-view .playground-tasks-backlog-item {
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-tasks-project-workspace .playground-tasks-backlog-view .playground-tasks-backlog-item::before {
          content: none;
          display: none;
        }
  
        .playground-tasks-project-workspace .playground-tasks-backlog-view .playground-tasks-backlog-item:hover {
          background: rgba(255, 255, 255, 0.07);
        }
  
        .playground-tasks-project-workspace .playground-tasks-backlog-view .playground-tasks-backlog-item.is-active {
          background: rgba(255, 255, 255, 0.1);
        }
  
        .playground-tasks-backlog-title {
          display: block;
          flex: 1 1 auto;
          min-width: 0;
          color: rgba(255, 255, 255, 0.94);
          font-size: 12px;
          line-height: 1.45;
          font-weight: 400;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-tasks-backlog-title-input {
          display: block;
          flex: 1 1 auto;
          width: 100%;
          min-width: 0;
          margin: 0;
          padding: 0;
          border: 0;
          outline: none;
          background: transparent;
          color: rgba(255, 255, 255, 0.94);
          font-size: 12px;
          font-weight: 400;
          line-height: 1.45;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
  
        .playground-tasks-backlog-title-input::placeholder {
          color: rgba(255, 255, 255, 0.38);
        }
  
        .playground-tasks-backlog-priority {
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          width: 14px;
          height: 14px;
        }
  
        .playground-tasks-backlog-priority .playground-tasks-priority-value-icon {
          width: 14px;
          height: 14px;
        }
  
        .playground-tasks-backlog-meta {
          flex: 0 0 auto;
          margin-left: auto;
          min-width: 0;
          display: flex;
          align-items: center;
          gap: 10px;
        }
  
        .playground-tasks-backlog-status,
        .playground-tasks-backlog-assignee {
          flex: 0 0 auto;
          font-size: 12px;
          line-height: 1.35;
          color: rgba(255, 255, 255, 0.56);
          white-space: nowrap;
          overflow: visible;
          text-overflow: clip;
          text-align: right;
        }
  
        .playground-tasks-backlog-status {
          padding: 2px 6px;
          border-radius: 5px;
          color: #fff;
          font-size: 10px;
          font-weight: 400;
        }
  
        .playground-tasks-backlog-status-control {
          position: relative;
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          z-index: 110;
        }
  
        .playground-tasks-backlog-status-control.is-open {
          z-index: 170;
        }
  
        .playground-tasks-backlog-status-button {
          margin: 0;
          padding: 0;
          border: 0;
          background: transparent;
          color: #fff;
          display: inline-flex;
          align-items: center;
          gap: 6px;
          font-size: 12px;
          line-height: 1.35;
          font-weight: 400;
          cursor: pointer;
          transition: opacity 150ms ease;
        }
  
        .playground-tasks-backlog-status-button:hover:not(:disabled) {
          opacity: 0.7;
        }
  
        .playground-tasks-backlog-status-button[disabled] {
          opacity: 0.45;
          cursor: not-allowed;
        }
  
        .playground-tasks-backlog-status-chevron {
          width: 12px;
          height: 12px;
          flex: 0 0 auto;
          color: #fff;
        }
  
        .playground-tasks-backlog-status-label {
          padding: 0;
          border: 0;
          background: transparent;
          color: #fff;
          font-size: 12px;
          line-height: 1.35;
          font-weight: 400;
          white-space: nowrap;
        }
  
        .playground-tasks-toolbar-popup-shell .playground-tasks-backlog-status-menu {
          width: 176px;
          min-width: 176px;
          z-index: 171;
        }
  
        .playground-tasks-backlog-assignee {
          font-weight: 400;
        }
  
        .playground-tasks-backlog-assignee-shell {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          min-width: 0;
        }
  
        .playground-tasks-backlog-assignee-avatar {
          width: 22px;
          height: 22px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.1);
          color: rgba(255, 255, 255, 0.88);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          overflow: hidden;
          flex: 0 0 auto;
        }
  
        .playground-tasks-backlog-assignee-avatar-image {
          width: 100%;
          height: 100%;
          object-fit: cover;
          display: block;
        }
  
        .playground-tasks-backlog-assignee-avatar-fallback {
          font-size: 10px;
          font-weight: 500;
          line-height: 1;
          letter-spacing: 0.02em;
          text-transform: uppercase;
          color: rgba(255, 255, 255, 0.7);
        }
  
        .playground-tasks-backlog-status.is-todo {
          color: rgba(102, 166, 255, 1);
          background: rgba(102, 166, 255, 0.15);
        }
  
        .playground-tasks-backlog-status.is-in-progress {
          color: rgba(57, 184, 119, 1);
          background: rgba(57, 184, 119, 0.15);
        }
  
  	      .playground-tasks-backlog-status.is-blocked {
  	        color: rgba(211, 91, 91, 1);
  	        background: rgba(211, 91, 91, 0.15);
  	      }
  
  	      .playground-tasks-backlog-status.is-in-review {
  	        color: rgba(246, 190, 93, 1);
  	        background: rgba(246, 190, 93, 0.15);
  	      }
  
  	      .playground-tasks-backlog-status.is-done {
          color: rgba(255, 255, 255, 0.78);
          background: rgba(255, 255, 255, 0.12);
        }
  
        .playground-tasks-backlog-assignee.is-unassigned {
          color: rgba(255, 255, 255, 0.4);
        }
  
        .playground-tasks-backlog-run-button {
          flex: 0 0 auto;
          margin-left: 0;
          width: 24px;
          min-width: 24px;
          height: 24px;
          min-height: 24px;
          max-height: 24px;
          padding: 0;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 999px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          background: transparent;
          color: rgba(255, 255, 255, 0.92);
          cursor: pointer;
          transition: background-color 150ms ease, color 150ms ease, border-color 150ms ease;
        }
  
        .playground-tasks-backlog-run-button svg {
          width: 12px;
          height: 12px;
        }
  
        .playground-tasks-backlog-run-button:hover:not(:disabled) {
          background: rgba(255, 255, 255, 0.12);
        }
  
        .playground-tasks-backlog-run-button:disabled {
          opacity: 0.48;
          cursor: not-allowed;
        }
  
        .playground-tasks-backlog-run-button.is-human-unchecked {
          border: 1px solid rgba(255, 255, 255, 0.2);
          background: transparent;
          color: rgba(255, 255, 255, 0.72);
        }
  
        .playground-tasks-backlog-run-button.is-human-unchecked::before {
          display: none;
        }
  
        .playground-tasks-backlog-run-button.is-human-unchecked:hover:not(:disabled) {
          background: transparent;
          border-color: rgba(255, 255, 255, 0.28);
        }
  
        .playground-tasks-release-item {
          width: 100%;
          padding: 14px 16px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 16px;
          background: rgba(255, 255, 255, 0.03);
          color: rgba(255, 255, 255, 0.92);
          text-align: left;
          cursor: pointer;
          transition: border-color 160ms ease, background-color 160ms ease;
        }
  
        .playground-tasks-release-item:hover {
          border-color: rgba(255, 255, 255, 0.14);
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-tasks-release-item-main {
          display: flex;
          flex-direction: column;
          gap: 8px;
          min-width: 0;
        }
  
        .playground-tasks-release-item-title-row {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-tasks-release-item-title {
          min-width: 0;
          font-size: 14px;
          font-weight: 500;
          line-height: 1.3;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-tasks-release-item-copy {
          font-size: 13px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.64);
        }
  
        .playground-tasks-release-item-meta {
          display: flex;
          align-items: center;
          gap: 10px;
          flex-wrap: wrap;
          font-size: 12px;
          line-height: 1.4;
          color: rgba(255, 255, 255, 0.48);
        }
  
        .playground-tasks-release-status {
          flex: 0 0 auto;
        }
  
        .playground-tasks-release-backlog-header-main {
          display: flex;
          flex-direction: column;
          gap: 8px;
          flex: 1 1 auto;
          min-width: 0;
        }
  
        .playground-tasks-release-backlog-title-row {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          min-width: 0;
          width: 100%;
        }
  
        .playground-tasks-release-header-meta {
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          gap: 6px;
          color: rgba(255, 255, 255, 0.7);
          font-size: 12px;
          line-height: 1.4;
          font-weight: 400;
          text-align: right;
        }
  
        .playground-tasks-release-header-meta.is-inline {
          justify-content: flex-start;
          text-align: left;
        }
  
        .playground-tasks-release-name-pill {
          display: inline-flex;
          align-items: center;
          min-height: 24px;
          padding: 0 10px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.78);
          font-size: 11px;
          font-weight: 500;
        }
  
        .playground-tasks-release-backlog-copy {
          display: block;
          width: 100%;
          font-size: 12px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.56);
        }
  
        .playground-tasks-backlog-heading-count {
          position: relative;
          top: -2px;
        }
  
        .playground-tasks-backlog-empty {
          min-height: 260px;
          border: 1px dashed rgba(255, 255, 255, 0.08);
          border-radius: 24px;
          background: rgba(255, 255, 255, 0.02);
        }
  
        .playground-tasks-empty-mission-control-button {
          font-weight: 500;
        }
  
        .playground-tasks-backlog-feedback {
          margin-top: 14px;
        }
  
        .playground-tasks-backlog-composer-shell,
        .playground-tasks-backlog-composer-shell .tb-runner-chat.playground-tasks-backlog-runner,
        .playground-tasks-backlog-composer-shell .tb-runner-chat.playground-tasks-backlog-runner .tb-input-shell,
        .playground-tasks-backlog-composer-shell .tb-runner-chat.playground-tasks-backlog-runner .tb-input-width,
        .playground-tasks-backlog-composer-shell .tb-runner-chat.playground-tasks-backlog-runner .embedded-runner-input,
        .playground-tasks-backlog-composer-shell .tb-runner-chat.playground-tasks-backlog-runner .tb-composer-textarea-shell,
        .playground-tasks-backlog-composer-shell .tb-runner-chat.playground-tasks-backlog-runner .task-input-controls {
          background: transparent !important;
        }
  
        .playground-tasks-backlog-composer-shell .tb-runner-chat.playground-tasks-backlog-runner .task-input-box {
          --tb-runner-input-bg: transparent;
          --tb-task-input-overlay: transparent;
          --tb-task-input-base-bg: transparent;
          background: transparent !important;
          box-shadow: none;
          -webkit-backdrop-filter: blur(50px);
          backdrop-filter: blur(50px);
        }
  
        .playground-environments-home-composer-shell .tb-runner-chat.playground-environments-home-runner .task-input-box {
          --tb-task-input-base-bg: transparent;
        }
  
        .tb-runner-chat.playground-environments-home-runner,
        .tb-runner-chat.playground-tasks-backlog-runner {
          width: 100%;
          min-width: 0;
          display: block;
          flex: 0 0 auto;
          height: auto;
          min-height: 0;
          overflow: visible;
        }
  
        .tb-runner-chat.playground-environments-home-runner .workinglogsbox,
        .tb-runner-chat.playground-tasks-backlog-runner .workinglogsbox {
          display: none !important;
        }
  
        .tb-runner-chat.playground-environments-home-runner .tb-input-shell,
        .tb-runner-chat.playground-tasks-backlog-runner .tb-input-shell {
          position: static;
          right: auto;
          bottom: auto;
          padding: 0;
          background: none;
          pointer-events: auto;
        }
  
        .tb-runner-chat.playground-environments-home-runner .tb-input-width,
        .tb-runner-chat.playground-tasks-backlog-runner .tb-input-width {
          width: 100%;
          max-width: none;
        }
  
        .tb-runner-chat.playground-environments-home-runner .embedded-runner-input,
        .tb-runner-chat.playground-tasks-backlog-runner .embedded-runner-input {
          width: 100%;
        }
  
        .tb-runner-chat.playground-environments-home-runner .tb-context-indicator-anchor,
        .tb-runner-chat.playground-tasks-backlog-runner .tb-context-indicator-anchor {
          display: none;
        }
  
        .playground-tasks-search {
          width: min(320px, 100%);
          height: 38px;
          padding: 0 12px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 12px;
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.92);
          font-size: 12px;
          outline: none;
        }
  
        .playground-tasks-view-section {
          display: flex;
          flex-direction: column;
          gap: 14px;
        }
  
        .playground-tasks-list {
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-tasks-card,
        .playground-tasks-calendar-entry {
          width: 100%;
          display: flex;
          flex-direction: column;
          gap: 10px;
          padding: 14px 16px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 16px;
          background: rgba(255, 255, 255, 0.03);
          color: rgba(255, 255, 255, 0.92);
          text-align: left;
          cursor: pointer;
          transition: border-color 160ms ease, background-color 160ms ease, transform 160ms ease;
        }
  
        .playground-tasks-card:hover,
        .playground-tasks-calendar-entry:hover {
          border-color: rgba(255, 255, 255, 0.14);
          background: rgba(255, 255, 255, 0.05);
        }
  
        .playground-tasks-card.is-active,
        .playground-tasks-calendar-entry.is-active {
          border-color: rgba(138, 214, 255, 0.42);
          background: rgba(71, 166, 255, 0.12);
        }
  
        .playground-tasks-lane-card {
          width: 100%;
          display: flex;
          flex-direction: column;
          gap: 0;
          padding: 12px;
          border: 1px solid rgba(255, 255, 255, 0.05);
          border-radius: 8px;
          background: var(--playground-task-color-surface, rgba(255, 255, 255, 0.05));
          box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.15);
          color: rgba(255, 255, 255, 0.92);
          text-align: left;
          cursor: pointer;
          transition: background-color 160ms ease, box-shadow 160ms ease;
        }
  
        .playground-tasks-lane-card:focus,
        .playground-tasks-lane-card:focus-visible {
          outline: none;
          box-shadow: none;
        }
  
        .playground-tasks-lane-card:hover {
          background: var(--playground-task-color-surface-hover, rgba(255, 255, 255, 0.07));
        }
  
        .playground-tasks-lane-card.is-active {
          background: var(--playground-task-color-surface-active, rgba(255, 255, 255, 0.1));
          box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.15);
        }
  
        .playground-tasks-card-header,
        .playground-tasks-lane-card-header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
        }
  
        .playground-tasks-card-title,
        .playground-tasks-lane-card-title {
          font-size: 12px;
          line-height: 1.35;
          font-weight: 500;
          color: white;
          min-width: 0;
          display: -webkit-box;
          -webkit-box-orient: vertical;
          -webkit-line-clamp: 1;
          overflow: hidden;
          text-overflow: ellipsis;
          word-break: break-word;
          overflow-wrap: anywhere;
        }
  
        .playground-tasks-card-title.is-complete,
        .playground-tasks-lane-card-title.is-complete,
        .playground-tasks-backlog-title.is-complete {
          text-decoration-line: line-through;
          text-decoration-thickness: 1px;
          text-decoration-color: white;
        }
  
        .playground-tasks-card-copy,
        .playground-tasks-lane-card-copy {
          font-size: 12px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.7);
          font-weight: 400;
          display: -webkit-box;
          -webkit-box-orient: vertical;
          -webkit-line-clamp: 2;
          overflow: hidden;
          word-break: break-word;
          overflow-wrap: anywhere;
        }
  
        .playground-tasks-card-copy .tb-message-markdown-paragraph,
        .playground-tasks-card-copy .tb-message-markdown-list,
        .playground-tasks-card-copy .tb-message-markdown-heading {
          margin: 0;
          font-size: inherit;
          line-height: inherit;
          color: inherit;
        }
  
        .playground-tasks-card-copy .tb-message-markdown-strong,
        .playground-tasks-card-copy .tb-message-markdown-em,
        .playground-tasks-card-copy .tb-message-markdown-inline-code,
        .playground-tasks-card-copy .tb-message-markdown-link {
          color: inherit;
        }
  
        .playground-tasks-lane-card-copy {
          margin-top: 4px;
        }
  
        .playground-tasks-lane-card-copy .tb-message-markdown-paragraph,
        .playground-tasks-lane-card-copy .tb-message-markdown-list,
        .playground-tasks-lane-card-copy .tb-message-markdown-heading {
          margin: 0;
          font-size: inherit;
          line-height: inherit;
          color: inherit;
        }
  
        .playground-tasks-lane-card-copy .tb-message-markdown-strong,
        .playground-tasks-lane-card-copy .tb-message-markdown-em,
        .playground-tasks-lane-card-copy .tb-message-markdown-inline-code,
        .playground-tasks-lane-card-copy .tb-message-markdown-link {
          color: inherit;
        }
  
        .playground-tasks-board-assignee-avatar {
          width: 22px;
          height: 22px;
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.1);
          color: rgba(255, 255, 255, 0.88);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          overflow: hidden;
          flex: 0 0 auto;
        }
  
        .playground-tasks-board-assignee-avatar-image {
          width: 100%;
          height: 100%;
          object-fit: cover;
          display: block;
        }
  
        .playground-tasks-board-assignee-avatar-fallback {
          font-size: 10px;
          font-weight: 500;
          line-height: 1;
          letter-spacing: 0.02em;
          text-transform: uppercase;
          color: rgba(255, 255, 255, 0.7);
        }
  
        .playground-tasks-card-meta,
        .playground-tasks-card-actions,
        .playground-tasks-lane-card-meta {
          display: flex;
          align-items: center;
          gap: 8px;
          flex-wrap: wrap;
        }
  
        .playground-tasks-chip,
        .playground-tasks-sprint-pill {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          min-height: 24px;
          padding: 0 10px;
          border-radius: 999px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.05);
          color: rgba(255, 255, 255, 0.76);
          font-size: 11px;
          font-weight: 600;
        }
  
        .playground-tasks-chip.is-priority-urgent {
          color: #ffd5d5;
          border-color: rgba(255, 167, 167, 0.26);
          background: rgba(255, 96, 96, 0.12);
        }
  
        .playground-tasks-chip.is-priority-high {
          color: #ffe5c1;
          border-color: rgba(255, 192, 108, 0.24);
          background: rgba(255, 176, 89, 0.12);
        }
  
        .playground-tasks-chip.is-status-done {
          color: #d8f7e6;
          border-color: rgba(120, 255, 182, 0.2);
          background: rgba(64, 201, 124, 0.12);
        }
  
        .playground-tasks-meta-copy {
          font-size: 11px;
          color: rgba(255, 255, 255, 0.44);
        }
  
        .playground-tasks-lane-card-bottom {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          width: 100%;
          min-width: 0;
          margin-top: 10px;
        }
  
        .playground-tasks-lane-card-meta-left {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          gap: 8px;
          flex: 1 1 auto;
        }
  
        .playground-tasks-lane-card-type-badge {
          width: 20px;
          height: 20px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border-radius: 5px;
          flex: 0 0 auto;
          color: white;
        }
  
        .playground-tasks-lane-card-type-badge svg {
          width: 12px;
          height: 12px;
          filter: drop-shadow(0 1px 1.5px rgba(0, 0, 0, 0.28));
        }
  
        .playground-tasks-lane-card-type-badge.is-task {
          background: linear-gradient(180deg, #39b877 0%, #2b8b59 100%);
        }
  
        .playground-tasks-lane-card-type-badge.is-subtask {
          background: linear-gradient(180deg, #4f7fc5 0%, #1e4585 100%);
        }
  
        .playground-tasks-lane-card-priority {
          width: 13px;
          height: 13px;
          flex: 0 0 auto;
          display: inline-flex;
          align-items: center;
          justify-content: center;
        }
  
        .playground-tasks-lane-card-priority .playground-tasks-priority-value-icon {
          width: 13px;
          height: 13px;
        }
  
        .playground-tasks-lane-card-status {
          flex: 0 0 auto;
          color: rgba(255, 255, 255, 0.7);
          font-size: 12px;
          line-height: 1.2;
          font-weight: 400;
          white-space: nowrap;
        }
  
        .playground-tasks-lane-card-ticket {
          flex: 0 0 auto;
          color: var(--playground-task-color-accent, rgba(255, 255, 255, 0.92));
          font-size: 12px;
          font-weight: 500;
        }
  
        .playground-tasks-detail-color-value {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          gap: 8px;
          color: rgba(255, 255, 255, 0.88);
        }
  
        .playground-tasks-detail-color-swatch {
          width: 10px;
          height: 10px;
          border-radius: 999px;
          flex: 0 0 auto;
          background: var(--playground-task-color-accent, #016bcb);
          box-shadow: inset 0 0 0 1px var(--playground-task-color-border, rgba(1, 107, 203, 0.2));
        }
  
        .playground-tasks-board-toolbar,
        .playground-tasks-calendar-toolbar {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          flex-wrap: wrap;
        }
  
        .playground-tasks-board-toolbar {
          padding-bottom: 4px;
        }
  
  ${CALENDAR_STYLE_FRAGMENTS.toolbarLayout}
        .playground-tasks-board-toolbar-main {
          display: inline-flex;
          align-items: center;
          gap: 10px;
          flex-wrap: wrap;
          min-width: 0;
          flex: 1 1 auto;
        }
  
  ${CALENDAR_STYLE_FRAGMENTS.toolbarMain}
        .playground-tasks-board-heading {
          font-size: 15px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.94);
        }
  
        .playground-tasks-board-toolbar-actions {
          display: inline-flex;
          align-items: center;
          justify-content: flex-end;
          gap: 8px;
          flex-wrap: wrap;
        }
  
  ${CALENDAR_STYLE_FRAGMENTS.toolbarActions}
        .playground-tasks-board-sections {
          display: flex;
          flex-direction: column;
          gap: 18px;
          overflow-x: auto;
          scrollbar-width: none;
        }
  
        .playground-tasks-board-sections::-webkit-scrollbar {
          display: none;
        }
  
        .playground-tasks-board-release-section {
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-tasks-board-release-section-header {
          display: flex;
          align-items: baseline;
          gap: 8px;
          min-width: 0;
          flex-wrap: wrap;
        }
  
        .playground-tasks-sprint-pill-row {
          display: flex;
          align-items: center;
          gap: 8px;
          flex-wrap: wrap;
        }
  
        .playground-tasks-sprint-pill {
          cursor: pointer;
          transition: background-color 160ms ease, border-color 160ms ease, color 160ms ease;
        }
  
        .playground-tasks-sprint-pill.is-active {
          background: rgba(255, 255, 255, 0.16);
          color: rgba(255, 255, 255, 0.98);
        }
  
        .playground-tasks-board-header {
          display: grid;
          grid-template-columns: repeat(5, minmax(150px, 1fr));
          gap: 12px;
          padding-bottom: 10px;
          border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-tasks-board-header-cell {
          min-width: 0;
          padding: 0 6px 0 0;
          font-size: 12px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.72);
        }
  
        .playground-tasks-board-grid {
          display: grid;
          grid-template-columns: repeat(5, minmax(150px, 1fr));
          gap: 12px;
          align-items: stretch;
        }
  
        .playground-tasks-board-release-box {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 12px;
          min-height: 120px;
          padding: 12px;
          border: 1px dashed rgba(255, 255, 255, 0.1);
          border-radius: 5px;
          background: rgba(255, 255, 255, 0.025);
          backdrop-filter: blur(50px);
          -webkit-backdrop-filter: blur(50px);
          transition: background-color 160ms ease, box-shadow 160ms ease;
        }
  
        .playground-tasks-board-release-box-header {
          min-width: 0;
          display: inline-flex;
          align-items: center;
          gap: 10px;
        }
  
        .playground-tasks-board-release-box-title {
          min-width: 0;
          font-size: 10px;
          line-height: 1.2;
          font-weight: 400;
          letter-spacing: 0.06em;
          text-transform: uppercase;
          color: rgba(255, 255, 255, 0.76);
        }
  
        .playground-tasks-board-release-box-count {
          min-width: 20px;
          height: 20px;
          padding: 0 8px;
          border-radius: 6px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.72);
          font-size: 10px;
          line-height: 1;
          font-weight: 500;
        }
  
        .playground-tasks-lane {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 12px;
          flex: 1 1 auto;
          padding: 0;
          border: 0;
          border-radius: 0;
          background: transparent;
          min-height: 100%;
          transition: none;
        }
  
        .playground-tasks-board-release-box.is-drop-target {
          background: rgba(102, 166, 255, 0.12);
          box-shadow: inset 0 0 0 1px rgba(102, 166, 255, 0.28);
        }
  
        .playground-tasks-lane-list {
          display: flex;
          flex-direction: column;
          gap: 10px;
        }
  
        .playground-tasks-board-release-lane-body {
          min-height: 84px;
        }
  
        .playground-tasks-lane-card.is-draggable {
          cursor: grab;
        }
  
        .playground-tasks-lane-card.is-dragging {
          opacity: 0.5;
        }
  
        .playground-tasks-empty {
          min-height: 220px;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          gap: 12px;
          padding: 24px;
          border: 1px dashed rgba(255, 255, 255, 0.12);
          border-radius: 18px;
          background: rgba(255, 255, 255, 0.02);
          backdrop-filter: blur(50px);
          -webkit-backdrop-filter: blur(50px);
          text-align: center;
        }
  
        .playground-tasks-empty-title {
          font-size: 14px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.96);
        }
  
        .playground-tasks-empty-copy {
          max-width: 450px;
          font-size: 12px;
          line-height: 1.55;
          color: rgba(255, 255, 255, 0.5);
        }
  
        .playground-tasks-empty-actions {
          display: flex;
          align-items: center;
          justify-content: center;
          gap: 10px;
          margin-top: 2px;
        }
  
        .playground-tasks-empty-primary-button {
          min-height: 34px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 8px;
          padding: 0 14px;
          border: 0;
          border-radius: 999px;
          background: #fff;
          color: #000;
          font-size: 12px;
          font-weight: 600;
          cursor: pointer;
          transition: transform 160ms ease, opacity 160ms ease;
        }
  
        .playground-tasks-empty-primary-button:hover {
          opacity: 0.92;
        }
  
        .playground-tasks-empty-primary-button:active {
          transform: translateY(1px);
        }
  
        .playground-tasks-loading-state {
          min-height: 120px;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          gap: 10px;
          padding: 24px 0;
          background: transparent;
          text-align: center;
        }
  
        .playground-tasks-loading-copy {
          font-size: 12px;
          line-height: 1.45;
          color: rgba(255, 255, 255, 0.56);
        }
  
        .playground-tasks-mission-control-empty-state {
          min-height: 100%;
          flex: 1;
          display: flex;
          align-items: center;
          justify-content: center;
        }
  
  ${CALENDAR_STYLE_FRAGMENTS.scheduler}
        .playground-tasks-sprint-composer {
          display: grid;
          grid-template-columns: repeat(4, minmax(0, 1fr)) auto;
          gap: 10px;
          align-items: end;
          padding: 14px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 18px;
          background: rgba(255, 255, 255, 0.03);
        }
  
  ${CALENDAR_STYLE_FRAGMENTS.legacyGrid}
        .playground-tasks-dependency-list,
        .playground-tasks-thread-list {
          display: flex;
          flex-wrap: wrap;
          gap: 8px;
        }
  
        .playground-tasks-thread-pill,
        .playground-tasks-dependency-pill {
          display: inline-flex;
          align-items: center;
          gap: 6px;
          min-height: 28px;
          padding: 0 10px;
          border-radius: 999px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          background: rgba(255, 255, 255, 0.04);
          color: rgba(255, 255, 255, 0.74);
          font-size: 11px;
          font-weight: 500;
        }
  
        .playground-tasks-thread-pill {
          cursor: pointer;
        }
  
        .playground-tasks-toggle-card {
          display: flex;
          align-items: flex-start;
          justify-content: space-between;
          gap: 12px;
          padding: 12px 14px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 14px;
          background: rgba(255, 255, 255, 0.03);
        }
  
        .playground-tasks-toggle-copy {
          font-size: 12px;
          line-height: 1.5;
          color: rgba(255, 255, 255, 0.52);
        }
  
        .playground-tasks-inline-actions {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          flex-wrap: wrap;
        }
  
        .runner-host {
          display: flex;
          height: 100%;
          min-height: 0;
          border-radius: inherit;
          overflow: hidden;
        }
  
        .playground-thread-runner {
          border-radius: inherit;
          overflow: hidden;
        }
  
        .playground-evaluator-thread-prompt {
          display: inline;
          color: rgba(255, 255, 255, 0.9);
          font-size: 14px;
          line-height: 1.45;
          font-weight: 500;
        }
  
        .playground-evaluator-thread-link {
          appearance: none;
          border: 0;
          margin: 0;
          padding: 0;
          background: transparent;
          color: rgb(96, 165, 250);
          font: inherit;
          cursor: pointer;
        }
  
        .playground-evaluator-thread-link:hover,
        .playground-evaluator-thread-link:focus-visible {
          color: rgb(147, 197, 253);
          text-decoration: underline;
          outline: none;
        }
  
        .playground-evaluator-thread-muted {
          color: rgba(255, 255, 255, 0.72);
          font-weight: 400;
        }
  
        .playground-evaluator-summary-kpi-card .tb-run-summary-json-header {
          min-height: 46px;
        }
  
        .playground-evaluator-summary-kpi-grid {
          display: grid;
          grid-template-columns: repeat(3, minmax(0, 1fr));
          gap: 0;
          padding: 14px 16px;
        }
  
        .playground-evaluator-summary-kpi {
          min-width: 0;
          display: flex;
          flex-direction: column;
          gap: 6px;
        }
  
        .playground-evaluator-summary-kpi + .playground-evaluator-summary-kpi {
          padding-left: 16px;
          border-left: 1px solid rgba(255, 255, 255, 0.08);
        }
  
        .playground-evaluator-summary-kpi-label {
          color: rgba(255, 255, 255, 0.52);
          font-size: 11px;
          line-height: 1;
          font-weight: 500;
        }
  
        .playground-evaluator-summary-kpi-value {
          display: inline-flex;
          align-items: center;
          gap: 7px;
          min-width: 0;
          color: rgba(255, 255, 255, 0.94);
          font-size: 17px;
          line-height: 1.1;
          font-weight: 500;
        }
  
        .playground-evaluator-summary-kpi-status-icon {
          width: 16px;
          height: 16px;
          flex: 0 0 auto;
        }
  
        .playground-evaluator-summary-kpi-status-icon.is-passed {
          color: rgb(52, 211, 153);
        }
  
        .playground-evaluator-summary-kpi-status-icon.is-failed {
          color: rgb(248, 113, 113);
        }
  
  ${METRONOME_SHELL_STYLE_FRAGMENTS.runTrace}
  
        .playground-demo-page,
        .playground-demo-thread-shell,
        .playground-demo-changes-shell {
          width: 100%;
          height: 100%;
          min-height: 0;
          overflow: auto;
          padding: 32px;
          box-sizing: border-box;
        }
  
        .playground-demo-page {
          display: flex;
          flex-direction: column;
          gap: 22px;
        }
  
        .playground-demo-page-header {
          display: flex;
          flex-direction: column;
          gap: 10px;
          max-width: 760px;
        }
  
        .playground-demo-page-kicker,
        .playground-demo-thread-kicker {
          font-size: 12px;
          font-weight: 600;
          letter-spacing: 0.08em;
          text-transform: uppercase;
          color: rgba(255, 255, 255, 0.52);
        }
  
        .playground-demo-page-title,
        .playground-demo-thread-title {
          margin: 0;
          font-size: 32px;
          line-height: 1.05;
          font-weight: 500;
          color: #fff;
        }
  
        .playground-demo-page-copy,
        .playground-demo-thread-copy {
          margin: 0;
          max-width: 760px;
          font-size: 15px;
          line-height: 1.7;
          color: rgba(255, 255, 255, 0.72);
        }
  
        .playground-demo-card-grid {
          display: grid;
          grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
          gap: 16px;
        }
  
        .playground-demo-card {
          display: flex;
          flex-direction: column;
          gap: 14px;
          padding: 18px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 18px;
          background: rgba(255, 255, 255, 0.03);
          box-shadow: 0 18px 60px rgba(0, 0, 0, 0.24);
        }
  
        .playground-demo-card-icon {
          width: 36px;
          height: 36px;
          border-radius: 12px;
          background: rgba(255, 255, 255, 0.08);
          color: rgba(255, 255, 255, 0.92);
          display: inline-flex;
          align-items: center;
          justify-content: center;
        }
  
        .playground-demo-card-icon svg {
          width: 16px;
          height: 16px;
        }
  
        .playground-demo-card-title {
          margin: 0;
          font-size: 15px;
          font-weight: 600;
          color: #fff;
        }
  
        .playground-demo-card-copy {
          margin: 0;
          font-size: 13px;
          line-height: 1.65;
          color: rgba(255, 255, 255, 0.66);
        }
  
        .playground-demo-callout {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 16px;
          padding: 18px 20px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 18px;
          background: linear-gradient(135deg, rgba(255, 255, 255, 0.055), rgba(255, 255, 255, 0.02));
        }
  
        .playground-demo-callout-copy {
          display: flex;
          flex-direction: column;
          gap: 6px;
        }
  
        .playground-demo-callout-title {
          margin: 0;
          font-size: 14px;
          font-weight: 600;
          color: #fff;
        }
  
        .playground-demo-callout-text {
          margin: 0;
          font-size: 13px;
          line-height: 1.6;
          color: rgba(255, 255, 255, 0.66);
        }
  
        .playground-demo-callout-actions {
          display: inline-flex;
          align-items: center;
          gap: 10px;
          flex-wrap: wrap;
        }
  
        .playground-demo-action,
        .playground-demo-action-secondary {
          min-height: 36px;
          padding: 0 14px;
          border-radius: 999px;
          border: 1px solid rgba(255, 255, 255, 0.1);
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 8px;
          cursor: pointer;
          text-decoration: none;
          transition: transform 140ms ease, background-color 140ms ease, border-color 140ms ease, color 140ms ease;
        }
  
        .playground-demo-action {
          background: #fff;
          color: #111;
        }
  
        .playground-demo-action-secondary {
          background: rgba(255, 255, 255, 0.03);
          color: rgba(255, 255, 255, 0.86);
        }
  
        .playground-demo-action:hover,
        .playground-demo-action-secondary:hover {
          transform: translateY(-1px);
        }
  
        .playground-demo-thread-shell {
          display: flex;
          flex-direction: column;
          gap: 18px;
        }
  
        .playground-demo-thread-header {
          display: flex;
          flex-direction: column;
          gap: 12px;
        }
  
        .playground-demo-thread-badges {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          flex-wrap: wrap;
        }
  
        .playground-demo-thread-badge {
          display: inline-flex;
          align-items: center;
          gap: 6px;
          min-height: 28px;
          padding: 0 10px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 999px;
          background: rgba(255, 255, 255, 0.04);
          font-size: 12px;
          font-weight: 500;
          color: rgba(255, 255, 255, 0.72);
        }
  
        .playground-demo-thread-badge.is-strong {
          color: #fff;
        }
  
        .playground-demo-thread-badge svg {
          width: 13px;
          height: 13px;
        }
  
        .playground-demo-thread-timeline {
          display: flex;
          flex-direction: column;
          gap: 12px;
        }
  
        .playground-demo-thread-entry {
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 20px;
          background: rgba(255, 255, 255, 0.03);
          overflow: hidden;
        }
  
        .playground-demo-thread-entry-header {
          display: flex;
          align-items: center;
          gap: 8px;
          padding: 14px 18px 0;
        }
  
        .playground-demo-thread-entry-header svg {
          width: 14px;
          height: 14px;
          color: rgba(255, 255, 255, 0.62);
        }
  
        .playground-demo-thread-entry-title {
          font-size: 12px;
          font-weight: 600;
          color: rgba(255, 255, 255, 0.68);
          text-transform: uppercase;
          letter-spacing: 0.06em;
        }
  
        .playground-demo-thread-entry-body {
          padding: 10px 18px 16px;
          font-size: 14px;
          line-height: 1.7;
          color: rgba(255, 255, 255, 0.84);
        }
  
        .playground-demo-thread-entry.is-user .playground-demo-thread-entry-body,
        .playground-demo-thread-entry.is-message .playground-demo-thread-entry-body {
          color: #fff;
        }
  
        .playground-demo-thread-entry.is-tool .playground-demo-thread-entry-body {
          font-family: "SFMono-Regular", SFMono-Regular, ui-monospace, Menlo, Monaco, Consolas, monospace;
          font-size: 13px;
        }
  
        .playground-demo-thread-composer {
          margin-top: auto;
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 16px;
          padding: 18px 20px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 20px;
          background: rgba(8, 8, 10, 0.72);
        }
  
        .playground-demo-thread-composer-copy {
          display: flex;
          flex-direction: column;
          gap: 6px;
        }
  
        .playground-demo-thread-composer-title {
          margin: 0;
          font-size: 14px;
          font-weight: 600;
          color: #fff;
        }
  
        .playground-demo-thread-composer-text {
          margin: 0;
          font-size: 13px;
          line-height: 1.6;
          color: rgba(255, 255, 255, 0.64);
        }
  
        .playground-demo-changes-list {
          display: flex;
          flex-direction: column;
          gap: 12px;
        }
  
        .playground-demo-change-entry {
          display: flex;
          align-items: flex-start;
          gap: 12px;
          padding: 16px 18px;
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 18px;
          background: rgba(255, 255, 255, 0.03);
        }
  
        .playground-demo-change-entry svg {
          width: 15px;
          height: 15px;
          margin-top: 2px;
          color: rgba(255, 255, 255, 0.72);
        }
  
        .playground-demo-change-path {
          margin: 0;
          font-size: 13px;
          font-weight: 600;
          color: #fff;
        }
  
        .playground-demo-change-copy {
          margin: 4px 0 0;
          font-size: 13px;
          line-height: 1.65;
          color: rgba(255, 255, 255, 0.64);
        }
  
        @media (max-width: 980px) {
          .playground-demo-page,
          .playground-demo-thread-shell,
          .playground-demo-changes-shell {
            padding: 18px;
          }
  
          .playground-demo-callout,
          .playground-demo-thread-composer {
            flex-direction: column;
            align-items: flex-start;
          }
        }
  
  ${FILES_STYLE_FRAGMENTS.responsive}
  
        @media (max-width: 980px) {
          .playground-environments-shell {
            grid-template-columns: minmax(0, 1fr);
          }
          .playground-environments-list-pane {
            border-right: 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.08);
          }
          .playground-environments-summary-grid,
          .playground-environments-field-grid {
            grid-template-columns: minmax(0, 1fr);
          }
          .playground-settings-plan-grid,
          .playground-settings-kpi-grid,
          .playground-settings-form-grid,
          .playground-settings-usage-analytics-grid,
          .playground-settings-usage-balance-grid,
          .playground-settings-usage-secondary-grid {
            grid-template-columns: minmax(0, 1fr);
          }
          .playground-settings-usage-chart-kpis {
            grid-template-columns: repeat(2, minmax(0, 1fr));
          }
          .playground-settings-usage-period-header,
          .playground-settings-usage-period-heading,
          .playground-settings-usage-card-header,
          .playground-settings-usage-legend-row,
          .playground-settings-usage-source-row-top,
          .playground-settings-plans-budget-card-top,
          .playground-settings-plans-budget-breakdown-row {
            flex-direction: column;
            align-items: stretch;
          }
          .playground-settings-usage-card-header-legend,
          .playground-settings-usage-inline-legend {
            justify-content: flex-start;
          }
          .playground-settings-usage-donut-layout {
            grid-template-columns: minmax(0, 1fr);
            gap: 16px;
          }
          .playground-settings-usage-chart-card.is-donut {
            width: 100%;
            justify-self: stretch;
          }
          .playground-settings-usage-donut-layout > .playground-settings-usage-donut-frame {
            order: -1;
          }
          .playground-settings-usage-donut-column {
            flex-direction: row;
            flex-wrap: wrap;
            gap: 12px;
          }
          .playground-settings-field-span-2 {
            grid-column: span 1;
          }
          .playground-settings-connection-grid {
            grid-template-columns: minmax(0, 1fr);
          }
          .playground-settings-usage-row,
          .playground-settings-daily-row {
            grid-template-columns: minmax(0, 1fr);
          }
          .playground-settings-inline-row,
          .playground-settings-inline-row-main,
          .playground-settings-inline-actions,
          .playground-settings-card-header,
          .playground-settings-card-footer,
          .playground-settings-code-row {
            flex-direction: column;
            align-items: stretch;
          }
          .playground-settings-integration-card-row,
          .playground-settings-integration-top,
          .playground-settings-integration-account-group {
            align-items: stretch;
            flex-direction: column;
          }
          .playground-settings-integration-form-row {
            flex-direction: column;
          }
          .playground-settings-integration-input.is-email,
          .playground-settings-integration-input.is-code {
            max-width: none;
          }
          .playground-settings-chip-row {
            justify-content: flex-start;
          }
          .playground-settings-records-current-row {
            align-items: flex-start;
            flex-direction: column;
          }
          .playground-settings-records-current-actions {
            width: 100%;
            justify-content: space-between;
          }
          .playground-settings-created-key-row,
          .playground-settings-api-key-header {
            flex-direction: column;
            align-items: stretch;
          }
          .playground-settings-api-key-stats {
            grid-template-columns: minmax(0, 1fr);
          }
          .playground-agents-model-metrics,
          .playground-agents-skills-grid,
          .playground-agents-team-grid {
            grid-template-columns: minmax(0, 1fr);
          }
          .playground-tasks-shell,
          .playground-tasks-summary-grid,
          .playground-tasks-project-grid,
          .playground-tasks-project-panel-grid,
          .playground-tasks-calendar-grid,
          .playground-tasks-sprint-composer {
            grid-template-columns: minmax(0, 1fr);
          }
          .playground-tasks-main {
            border-right: 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.08);
          }
          .playground-tasks-scheduler,
          .playground-tasks-scheduler-card-stats,
          .playground-tasks-scheduler-form-grid {
            grid-template-columns: minmax(0, 1fr);
          }
          .playground-tasks-header,
          .playground-tasks-board-toolbar,
          .playground-tasks-calendar-toolbar {
            grid-template-columns: minmax(0, 1fr);
            flex-direction: column;
            align-items: stretch;
          }
          .playground-tasks-scheduler-surface,
          .playground-tasks-scheduler-empty-state {
            min-height: 640px;
          }
          .playground-tasks-scheduler-sidebar,
          .playground-tasks-scheduler-editor,
          .playground-tasks-scheduler-details {
            padding-left: 16px;
            padding-right: 16px;
          }
          .playground-tasks-scheduler-detail-actions,
          .playground-tasks-scheduler-editor-actions,
          .playground-tasks-scheduler-runs-header {
            flex-wrap: wrap;
          }
          .playground-tasks-backlog-title {
            white-space: normal;
          }
          .playground-tasks-backlog-composer-shell {
            padding-bottom: 0;
          }
          .playground-tasks-toolbar {
            justify-content: flex-start;
          }
          .playground-tasks-project-modal-backdrop {
            padding: 14px;
          }
          .playground-tasks-project-modal {
            padding: 16px;
            border-radius: 20px;
          }
          .playground-tasks-project-icon-picker {
            width: min(224px, calc(100vw - 60px));
          }
          .playground-tasks-project-wallpaper-grid {
            grid-template-columns: repeat(2, minmax(0, 1fr));
          }
          .playground-environments-detail-header {
            flex-direction: column;
            align-items: stretch;
          }
          .playground-environments-detail-actions {
            justify-content: flex-start;
          }
          .playground-environments-inline-row,
          .playground-environments-inline-row-paired,
          .playground-environments-inline-row-top,
          .playground-environments-runtime-row {
            grid-template-columns: minmax(0, 1fr);
          }
          .playground-environments-remove-button {
            width: 100%;
          }
          .playground-settings-profile-card,
          .playground-settings-connection-meta-row {
            flex-direction: column;
            align-items: flex-start;
          }
  ${APP_SIDEBAR_STYLE_FRAGMENTS.responsive}
          .playground-main {
            min-height: 68vh;
          }
          .changes-view {
            grid-template-columns: minmax(0, 1fr);
          }
        }
  ${MODELS_PAGE_CSS}
  ${MARKETPLACE_PAGE_CSS}
  ${PLAYGROUND_FINE_TUNING_CSS}
  ${PLATFORM_UI_PRIMITIVES_CSS}
  ${SETTINGS_MODAL_CSS}
      </style>
      <link rel="stylesheet" href="/dist/platform-ui/components/ui/button/button.css" />
      <link rel="stylesheet" href="/dist/platform-ui/components/ui/label/label.css" />
      <link rel="stylesheet" href="/dist/platform-ui/components/ui/search/search.css" />
      <link rel="stylesheet" href="/dist/platform-ui/components/ui/selector/selector.css" />
      <link rel="stylesheet" href="/dist/platform-ui/components/ui/switch/switch.css" />
  
      <script type="importmap">
        {
          "imports": {
            "react": "https://esm.sh/react@18.3.1",
            "react/jsx-runtime": "https://esm.sh/react@18.3.1/jsx-runtime",
            "react-dom": "https://esm.sh/react-dom@18.3.1?external=react",
            "react-dom/client": "https://esm.sh/react-dom@18.3.1/client",
            "@monaco-editor/react": "https://esm.sh/@monaco-editor/react@4.7.0?external=react,react-dom",
            "@git-diff-view/react": "https://esm.sh/@git-diff-view/react@0.1.3?bundle&external=react",
            "@git-diff-view/file": "https://esm.sh/@git-diff-view/file@0.1.3?bundle",
            "docx-preview": "https://esm.sh/docx-preview@0.3.7?bundle",
            "dompurify": "https://esm.sh/dompurify@3.3.3?bundle",
            "pdfjs-dist/build/pdf.mjs": "https://esm.sh/pdfjs-dist@5.4.624/build/pdf.mjs?bundle",
            "date-fns": "https://esm.sh/date-fns@4.1.0?bundle",
            "date-fns/locale/en-US": "https://esm.sh/date-fns@4.1.0/locale/en-US?bundle",
            "lucide-react": "https://esm.sh/lucide-react@0.575.0?external=react",
            "@xyflow/react": "https://esm.sh/@xyflow/react@12.8.4?external=react,react-dom",
            "react-big-calendar": "https://esm.sh/react-big-calendar@1.19.4?bundle&external=react,react-dom",
            "react-markdown": "https://esm.sh/react-markdown@10.1.0?bundle&external=react",
            "rehype-raw": "https://esm.sh/rehype-raw@7.0.0?bundle",
            "remark-gfm": "https://esm.sh/remark-gfm@4.0.1?bundle",
            "unist-util-visit": "https://esm.sh/unist-util-visit@5.0.0",
            "chart.js/auto": "https://esm.sh/chart.js@4.5.1/auto?bundle",
            "@tanstack/react-table": "https://esm.sh/@tanstack/react-table@8.21.3?bundle&external=react",
            "pptx-preview": "https://esm.sh/pptx-preview@1.0.7?bundle",
            "jszip": "https://esm.sh/jszip@3.10.1?bundle",
            "xlsx": "/vendor/xlsx/xlsx.mjs"
          }
        }
      </script>
    </head>
    <body>
      <div id="app">
        <div class="playground-static-boot" role="status" aria-live="polite">
          <div class="playground-static-boot-card">
            <div class="playground-static-boot-row">
              <span class="playground-static-boot-dots" aria-hidden="true">
                <span></span><span></span><span></span>
                <span></span><span></span><span></span>
                <span></span><span></span><span></span>
              </span>
              <span class="playground-static-boot-title">Agentic Compute Platform</span>
            </div>
            <p class="playground-static-boot-copy">Opening your workspace and preparing your agents.</p>
          </div>
        </div>
      </div>
  
      <script type="module">
        import React, { useCallback, useEffect, useLayoutEffect, useMemo, useRef, useState } from "react";
        import { createPortal } from "react-dom";
        import { createRoot } from "react-dom/client";
  ${CALENDAR_BROWSER_FOUNDATION_FRAGMENTS.dateImports}
        import DOMPurify from "dompurify";
  ${CALENDAR_BROWSER_FOUNDATION_FRAGMENTS.calendarImport}
        import ReactMarkdown from "react-markdown";
        import rehypeRaw from "rehype-raw";
        import remarkGfm from "remark-gfm";
        import { visit as unistVisit } from "unist-util-visit";
        import Chart from "chart.js/auto";
        import { addEdge, Background, BaseEdge, Controls, EdgeLabelRenderer, getSimpleBezierPath, Handle, MarkerType, NodeResizer, Position, ReactFlow, ReactFlowProvider, useEdgesState, useNodesState, useReactFlow } from "@xyflow/react";
        import { getApps, initializeApp } from "https://esm.sh/firebase@10.12.2/app";
        import { browserLocalPersistence, getAuth, GoogleAuthProvider, onIdTokenChanged, setPersistence, signInWithEmailAndPassword, signInWithPopup, signOut as signOutFirebaseAuth } from "https://esm.sh/firebase@10.12.2/auth";
  	      import { AlertCircle, ArrowDown, ArrowDownToLine, ArrowLeft, ArrowRight, ArrowUp, ArrowUpDown, ArrowUpFromLine, ArrowUpRight, AudioLines, Award, Battery, BatteryFull, BatteryLow, BatteryMedium, Bell, Bold, BookOpen, Bookmark, Bot, Braces, Brain, Building2, Cable, Calendar as CalendarIcon, Calculator, Camera, ChartColumnIncreasing, ChartNoAxesColumnIncreasing, Check, ChevronDown, ChevronLeft, ChevronRight, ChevronUp, ChevronsUpDown, Circle, CircleCheckBig, CircleHelp, Clapperboard, Clock, Cloud, Code, Code2, CodeXml, Coins, Copy, Cpu, Crop, Database, DollarSign, Download, Ellipsis, EllipsisVertical, Equal, ExternalLink, Eye, EyeOff, File, FilePlus2, FileText, Film, Filter, FingerprintPattern, Flame, Folder, FolderOpen, FunctionSquare, Ghost, GitBranch, GitBranchPlus, GitCommitHorizontal, GitFork, Globe, Grid3x3, Hand, HardDrive, Heart, History, House, Image as ImageIcon, Info, Italic, Key, KeyRound, LassoSelect, Layers, LayoutDashboard, LayoutGrid, LibraryBig, Lightbulb, Link2, List, ListOrdered, ListTodo, Loader2, LogIn, LogOut, Mail, MapPin, Maximize2, MessageCircle, MessageSquare, Metronome, Mic, Minimize2, Minus, Monitor, MousePointer2, Package, Paintbrush, PanelLeft, PanelLeftClose, PanelLeftOpen, PanelRight, Paperclip, PauseCircle, PenTool, PencilRuler, Pin, Play, Plus, ReceiptText, Redo2, RefreshCw, Rocket, RotateCcw, RotateCw, Save, Scan, Search, Server, Settings, Settings2, Shield, Slash, SlidersHorizontal, Sparkles, Split, Square, SquarePen, StickyNote, Tag, Telescope, Terminal, TestTubeDiagonal, Trash2, Underline, Undo2, Unlink, User, UserRound, Users, UsersRound, Wand2, Webhook, X, Zap } from "lucide-react";
  	      import { RunnerClient } from "/dist/index.js";
  	      import { RunnerChat, RunnerDocumentPreviewDrawer, RunnerFileDiffSurface, RunnerImagePreviewSurface } from "/dist/react/index.js";
  	      import { PlatformAnalyticsSection } from "/dist/platform-ui/components/composite/analytics/index.js";
  	      import { PlatformCodePreviewBox } from "/dist/platform-ui/components/composite/code-preview-box/index.js";
  	      import { PlatformDataTable } from "/dist/platform-ui/components/composite/data-table/index.js";
  	      import { PlatformDetailTabBar } from "/dist/platform-ui/components/composite/detail-tab-bar/index.js";
  	      import { PlatformInstructionsEditor } from "/dist/platform-ui/components/composite/instructions-editor/index.js";
  	      import { PlatformButton, PlatformPrimaryButton, PlatformSecondaryButton } from "/dist/platform-ui/components/ui/button/index.js";
  	      import { PlatformSearch } from "/dist/platform-ui/components/ui/search/index.js";
  	      import { PlatformButtonSelector, PlatformSelector } from "/dist/platform-ui/components/ui/selector/index.js";
  	      import { PlatformPopup, PlatformPopupDismissLayer, PlatformPopupSurface } from "/dist/platform-ui/components/composite/popup/index.js";
  	      import { PlatformModal, PlatformModalBackdrop, PlatformModalBody, PlatformModalFooter, PlatformModalHeader, PlatformModalSurface, PlatformUnsavedChangesModal } from "/dist/platform-ui/components/composite/modal/index.js";
  	      import { PlatformSwitch } from "/dist/platform-ui/components/ui/switch/index.js";
  	      import { PlatformCalendarWidget, PlatformProjectWidget, PlatformProjectWidgetEmpty, PlatformProjectWidgetEmptyState, PlatformProjectWidgetTask, PlatformProjectWidgetTaskList, PlatformUsageWidget } from "/dist/platform-ui/components/composite/widgets/index.js";
  	      import {
  	        PlatformPermissionMiniRingIcon,
  	        PlatformPermissionsPage,
  	        PlatformRolePermissionsPage,
  	        PLATFORM_PERMISSION_ACCESS_OPTIONS as PLAYGROUND_PERMISSION_ACCESS_OPTIONS,
  	        PLATFORM_PERMISSION_ACTION_DEFINITIONS as PLAYGROUND_PERMISSION_ACTION_DEFINITIONS,
  	        PLATFORM_PERMISSION_RING_DEFINITIONS as PLAYGROUND_PERMISSION_RING_DEFINITIONS,
  	        PLATFORM_PERMISSION_RING_IDS as PLAYGROUND_PERMISSION_RING_IDS,
  	        buildPlatformPermissionActionPolicy as buildPlaygroundPermissionActionPolicy,
  	        createPlatformDefaultPermissionActions as createPlaygroundDefaultPermissionActions,
  	        createPlatformDefaultPermissionRings as createPlaygroundDefaultPermissionRings,
  	        createPlatformDefaultPermissionSet as createPlaygroundDefaultPermissionSet,
  	        createPlatformFullAccessPermissionSet as createPlaygroundFullAccessPermissionSet,
  	        getPlatformPermissionAccessLabel as getPlaygroundPermissionAccessLabel,
  	        getPlatformPermissionActionDefinitionById as getPlaygroundPermissionActionDefinition,
  	        getPlatformPermissionActionExplicitAccessByDefinition as getPlaygroundPermissionActionExplicitAccess,
  	        getPlatformPermissionRingAccessById as getPlaygroundPermissionRingAccess,
  	        getPlatformPermissionRingDefinitionById as getPlaygroundPermissionRingDefinition,
  	        isPlatformPermissionRecord as isPlaygroundPermissionRecord,
  	        normalizePlatformPermissionAccessValue as normalizePlaygroundPermissionAccess,
  	        normalizePlatformPermissionRingId as normalizePlaygroundPermissionRingId,
  	        normalizePlatformPermissionSet as normalizePlaygroundPermissionSet,
  	      } from "/dist/platform-ui/pages/permissions/index.js";
  	      import { PlatformApplicationBoundary } from "/dist/platform-app/legacy/platform-application-boundary.js";
  	      import { AgentPermissionMeters, AgentPermissionRingIcons, AgentPublishControl, AgentsOverviewAnalyticsRequestError, ComputersOverviewAnalyticsRequestError, createAgentsOverviewAnalytics, createComputersOverviewAnalytics, deleteComputerResource, fetchAgentsOverviewAnalytics, fetchComputersOverviewAnalytics, getAgentPermissionSummary, invalidateAgentsOverviewAnalytics, invalidateComputersOverviewAnalytics, normalizeComputerOverviewRows, readCachedAgentsOverviewAnalytics, readCachedComputersOverviewAnalytics, saveComputerResource } from "/dist/platform-app/legacy/platform-resource-api.js";
  	      import { ApiKeysOverviewAnalyticsRequestError, createApiKeysOverviewAnalytics, createDevelopResourceOverviewRows, createDevelopVoiceAgentOverviewRows, deleteDevelopResource, fetchApiKeysOverviewAnalytics, invalidateApiKeysOverviewAnalytics, readCachedApiKeysOverviewAnalytics, saveDevelopResource } from "/dist/platform-app/legacy/platform-develop-api.js";
  	      import { AgentDetailPage, AgentPermissionsPage, AgentsOverviewPage, ComputersOverviewPage, ConfigureHomeOverviewPage, DevelopApiKeysOverviewPage, DevelopHomeOverviewPage, DevelopResourceOverviewRoute, DevelopVoiceAgentsOverviewPage, DevelopWebhooksOverviewPage, ModelsOverviewPage, PluginsOverviewPage, SkillsOverviewPage, TagsOverviewPage } from "/dist/platform-app/routing/platform-lazy-pages.js";
  	      import { openGoogleDrivePicker } from "/dist/platform-integrations/google-drive/google-drive-picker.js";
  
  	      function getPlaygroundSafeIconComponent(Icon, fallbackIcon = Circle) {
  	        return typeof Icon === "function" || (Icon && typeof Icon === "object") ? Icon : fallbackIcon;
  	      }
  
  	      function PlaygroundDetailSectionHeader({
  	        title,
  	        children = null,
  	        presentation = "default",
  	        className = "",
  	      }) {
  	        const normalizedPresentation = presentation === "static-transparent"
  	          ? "static-transparent"
  	          : "default";
  	        return React.createElement("div", {
  	            className: "playground-tasks-detail-section-header"
  	              + (normalizedPresentation === "static-transparent" ? " is-static-transparent" : "")
  	              + (className ? " " + className : ""),
  	            "data-header-presentation": normalizedPresentation,
  	          },
  	          React.createElement("div", { className: "playground-tasks-detail-section-title" }, title),
  	          children
  	        );
  	      }
  
  ${CALENDAR_BROWSER_FOUNDATION_FRAGMENTS.localizer}
        function PlaygroundSharedResourcesTab({
          rows = [],
          allRows = rows,
          searchQuery = "",
          onSearchQueryChange,
          toolbarPopover = "",
          onToolbarPopoverChange,
          filter = "all",
          onFilterChange,
          typeFilters = [],
          viewMode = "list",
          onViewModeChange,
          menuId = "",
          onMenuIdChange,
          getTypeMeta,
          getRowMenuId,
          renderIcon,
          renderCreator,
  	        renderSource,
  	        renderOwner,
  	        getRowActions,
  	        renderNewMenu,
  	        renderEmptyContent,
  	        onNewButtonClick,
  	        onRowOpen,
  	        onRowActionMenuOpen,
  	        onRowContextMenu,
  	        activeRowMenuId = "",
  	        searchPlaceholder = "Search resources",
          searchAriaLabel = "Search resources",
  	        newButtonLabel = "New",
  	        newButtonClassName = "",
  	        primaryHeader = "Resource",
          secondaryHeader = "Creator",
          sourceHeader = "Shared Through",
          tertiaryHeader = "Updated",
          ownerHeader = "Owner",
          emptyLabel = "No resources yet.",
          noMatchesLabel = "No resources match this view yet.",
          showNewButton = true,
          showFilterButton = true,
  	        showViewToggle = true,
  	        showListSubtitle = false,
  	        metaCellsStopRowOpen = true,
  	        showSelectionColumn = false,
  	        selectedRowIds = new Set(),
  		        getSelectionId,
  		        onToggleRowSelection,
  		        onToggleVisibleSelection,
  		        sortKey = "",
  		        sortDirection = "asc",
  		        sortableColumns = [],
  		        onSortChange,
  		      }) {
          const visibleRows = Array.isArray(rows) ? rows : [];
          const availableRows = Array.isArray(allRows) ? allRows : visibleRows;
          const activeViewMode = viewMode === "grid" ? "grid" : "list";
          const activeToolbarPopover = String(toolbarPopover || "");
          const activeFilter = String(filter || "all").trim() || "all";
          const resourceTypeFilters = Array.isArray(typeFilters) && typeFilters.length
            ? typeFilters
            : [{ id: "all", label: "All" }];
          const defaultTypeMeta = { label: "Resource", Icon: Layers };
  	        const hasSourceColumn = typeof renderSource === "function";
  	        const hasOwnerColumn = typeof renderOwner === "function";
  	        const selectionEnabled = Boolean(showSelectionColumn && activeViewMode === "list");
  	        const selectedResourceRowIds = selectedRowIds instanceof Set
  	          ? selectedRowIds
  	          : new Set(Array.isArray(selectedRowIds) ? selectedRowIds.map((value) => String(value || "").trim()).filter(Boolean) : []);
  	        const getResourceSelectionId = (row) => {
  	          if (typeof getSelectionId === "function") {
  	            return String(getSelectionId(row) || "").trim();
  	          }
  	          return String(row?.key || row?.id || row?.title || "").trim();
  	        };
  	        const visibleSelectionIds = selectionEnabled
  	          ? visibleRows.map((row) => getResourceSelectionId(row)).filter(Boolean)
  	          : [];
  	        const activeSortKey = String(sortKey || "").trim();
  	        const activeSortDirection = String(sortDirection || "").toLowerCase() === "desc" ? "desc" : "asc";
  	        const sortableColumnSet = new Set((Array.isArray(sortableColumns) ? sortableColumns : [])
  	          .map((columnKey) => String(columnKey || "").trim())
  	          .filter(Boolean));
  
          const setToolbarPopover = (nextValue) => {
            if (typeof onToolbarPopoverChange === "function") {
              onToolbarPopoverChange(nextValue);
            }
          };
          const closeRowMenu = () => {
            if (typeof onMenuIdChange === "function") {
              onMenuIdChange("");
            }
          };
          const getMeta = (row) => {
            if (typeof getTypeMeta === "function") {
              return getTypeMeta(row?.type || row?.resourceType || "");
            }
            return defaultTypeMeta;
          };
          const getMenuId = (row) => {
            if (typeof getRowMenuId === "function") {
              return getRowMenuId(row);
            }
            return "resource:" + String(row?.key || row?.id || row?.title || "").trim();
          };
          const openRow = (row) => {
            if (typeof onRowOpen === "function") {
              onRowOpen(row);
            }
          };
          const renderSharedIcon = (row, meta) => {
            if (typeof renderIcon === "function") {
              return renderIcon(row, meta);
            }
            const ResourceIcon = meta?.Icon || Layers;
            return React.createElement("span", { className: "playground-project-resource-title-icon" },
              React.createElement(ResourceIcon, { width: 16, height: 16, strokeWidth: 1.8 })
            );
          };
          const renderSecondaryCell = (row) => {
            if (typeof renderCreator === "function") {
              return renderCreator(row);
            }
            return React.createElement("span", null, row?.secondaryLabel || "-");
          };
          const renderSourceCell = (row) => {
            if (typeof renderSource === "function") {
              return renderSource(row);
            }
            return React.createElement("span", null, row?.sourceLabel || "-");
          };
          const renderOwnerCell = (row) => {
            if (typeof renderOwner === "function") {
              return renderOwner(row);
            }
            return React.createElement("span", null, row?.ownerLabel || "-");
          };
          const getMetaCellProps = (className) => metaCellsStopRowOpen
            ? {
                className,
                onClick: (event) => event.stopPropagation(),
                onKeyDown: (event) => event.stopPropagation(),
              }
            : { className };
          const renderFilterOption = (type) => {
            const typeId = String(type?.id || "all").trim() || "all";
            const active = activeFilter === typeId;
            return React.createElement("button", {
                key: typeId,
                type: "button",
                className: "tb-popup-row tb-popup-row-select" + (active ? " selected" : ""),
                onClick: () => {
                  closeRowMenu();
                  if (typeof onFilterChange === "function") {
                    onFilterChange(typeId);
                  }
                  setToolbarPopover("");
                },
              },
              React.createElement("span", { className: "tb-popup-check-slot" },
                active
                  ? React.createElement(Check, { className: "tb-popup-check", width: 14, height: 14, strokeWidth: 1.8 })
                  : null
              ),
              React.createElement("div", { className: "playground-tasks-toolbar-popup-item-copy" },
                React.createElement("span", null, type?.label || typeId),
                type?.description
                  ? React.createElement("span", null, type.description)
                  : null
              )
            );
          };
          const renderSharedFilterMenu = () => {
            if (activeToolbarPopover !== "filter") {
              return null;
            }
            return React.createElement(PlatformPopupSurface, {
                className: "playground-tasks-toolbar-popup-menu playground-project-resources-filter-menu playground-tasks-toolbar-popup-menu-animate-down-in",
                onClick: (event) => event.stopPropagation(),
              },
              resourceTypeFilters.map(renderFilterOption)
            );
          };
          const renderSharedNewMenu = () => {
            if (activeToolbarPopover !== "new") {
              return null;
            }
            if (typeof renderNewMenu === "function") {
              return renderNewMenu();
            }
            if (typeof onNewButtonClick !== "function") {
              return null;
            }
            return React.createElement(PlatformPopupSurface, {
                className: "playground-tasks-toolbar-popup-menu playground-project-resources-new-menu playground-tasks-toolbar-popup-menu-animate-down-in",
                onClick: (event) => event.stopPropagation(),
              },
              React.createElement("button", {
                  type: "button",
                  className: "tb-popup-row playground-project-team-menu-item",
                  onClick: () => {
                    setToolbarPopover("");
                    onNewButtonClick();
                  },
                },
                React.createElement(Plus, { width: 14, height: 14, strokeWidth: 1.8 }),
                React.createElement("span", null, "Add resource")
              )
            );
          };
          const renderEmptyRows = () => {
            if (availableRows.length === 0 && typeof renderEmptyContent === "function") {
              return renderEmptyContent();
            }
            return React.createElement("div", { className: "playground-project-resources-empty" },
              availableRows.length === 0 ? emptyLabel : noMatchesLabel
            );
          };
          const renderRows = () => {
            if (!availableRows.length || !visibleRows.length) {
              return renderEmptyRows();
            }
            if (activeViewMode === "grid") {
              return React.createElement("div", { className: "playground-project-resources-grid" },
                visibleRows.map((row) => {
                  const meta = getMeta(row) || defaultTypeMeta;
                  return React.createElement("button", {
                      key: row?.key || row?.id || row?.title,
                      type: "button",
                      className: "playground-project-resources-grid-card",
                      onClick: () => openRow(row),
                    },
                    React.createElement("div", { className: "playground-project-resources-grid-card-top" },
                      renderSharedIcon(row, meta),
                      React.createElement("span", { className: "playground-project-resource-title-copy" },
                        React.createElement("span", { className: "playground-project-resource-title-main" }, row?.title || "Untitled resource"),
                        row?.subtitle
                          ? React.createElement("span", { className: "playground-project-resource-title-sub" }, row.subtitle)
                          : null
                      )
                    ),
                    React.createElement("div", { className: "playground-project-resources-grid-card-meta" },
                      React.createElement("span", { className: "playground-project-resources-cell" }, meta?.label || "Resource"),
                      React.createElement("span", { className: "playground-project-resources-cell" }, row?.updatedLabel || "-")
                    )
                  );
                })
              );
  	          }
            const listColumns = [
              {
                id: "resource",
                header: primaryHeader,
                accessor: (row) => row?.title || "Untitled resource",
                sortable: sortableColumnSet.has("resource"),
                width: "minmax(220px, 1.45fr)",
                cell: ({ row }) => {
                  const meta = getMeta(row) || defaultTypeMeta;
                  return React.createElement("div", { className: "playground-project-resource-title-cell" },
                    renderSharedIcon(row, meta),
                    React.createElement("span", { className: "playground-project-resource-title-copy" },
                      React.createElement("span", { className: "playground-project-resource-title-main" }, row?.title || "Untitled resource"),
                      showListSubtitle && row?.subtitle
                        ? React.createElement("span", { className: "playground-project-resource-title-sub" }, row.subtitle)
                        : null
                    )
                  );
                },
              },
              {
                id: "access",
                header: secondaryHeader,
                accessor: (row) => row?.secondaryLabel || row?.accessLabel || "",
                sortable: sortableColumnSet.has("access"),
                width: "minmax(120px, 0.78fr)",
                cell: ({ row }) => renderSecondaryCell(row),
              },
              ...(hasSourceColumn ? [{
                id: "source",
                header: sourceHeader,
                accessor: (row) => row?.sourceLabel || "",
                sortable: sortableColumnSet.has("source"),
                width: "minmax(135px, 0.82fr)",
                hideBelow: 820,
                cell: ({ row }) => renderSourceCell(row),
              }] : []),
              {
                id: "updated",
                header: tertiaryHeader,
                accessor: (row) => row?.updatedAt || row?.updatedLabel || "",
                sortable: sortableColumnSet.has("updated"),
                sortDescFirst: true,
                width: "minmax(120px, 0.72fr)",
                hideBelow: 680,
                cell: ({ row }) => row?.updatedLabel || "-",
              },
              ...(hasOwnerColumn ? [{
                id: "owner",
                header: ownerHeader,
                accessor: (row) => row?.ownerLabel || "",
                sortable: sortableColumnSet.has("owner"),
                width: "minmax(135px, 0.85fr)",
                hideBelow: 940,
                cell: ({ row }) => renderOwnerCell(row),
              }] : []),
            ];
            return React.createElement(PlatformDataTable, {
              rows: visibleRows,
              columns: listColumns,
              getRowId: getResourceSelectionId,
              ariaLabel: searchAriaLabel,
              className: "playground-shared-resources-platform-data-table",
              surface: "plain",
              sticky: false,
              sorting: activeSortKey ? {
                value: { id: activeSortKey, direction: activeSortDirection },
                manual: true,
                onChange: (next) => {
                  if (next && typeof onSortChange === "function") onSortChange(next.id, next.direction);
                },
              } : undefined,
              selection: selectionEnabled ? {
                enabled: true,
                value: selectedResourceRowIds,
                ariaLabel: (row) => "Select " + (row?.title || "resource"),
                onChange: ({ selectedIds, reason }) => {
                  if (reason === "visible" && typeof onToggleVisibleSelection === "function") {
                    const selectVisible = visibleSelectionIds.some((id) => selectedIds.has(id));
                    onToggleVisibleSelection(visibleSelectionIds, selectVisible);
                    return;
                  }
                  if (typeof onToggleRowSelection !== "function") return;
                  const changedIds = new Set([...selectedResourceRowIds, ...selectedIds]);
                  changedIds.forEach((selectionId) => {
                    if (selectedResourceRowIds.has(selectionId) === selectedIds.has(selectionId)) return;
                    const row = visibleRows.find((candidate) => getResourceSelectionId(candidate) === selectionId);
                    onToggleRowSelection(selectionId, row);
                  });
                },
              } : undefined,
              onRowActivate: typeof onRowOpen === "function" ? openRow : undefined,
  	          getRowActions: typeof getRowActions === "function" ? getRowActions : undefined,
  	          onRowActionTrigger: typeof onRowActionMenuOpen === "function"
  	            ? (event, row) => {
  	                setToolbarPopover("");
  	                onRowActionMenuOpen(event, row, { openLeft: true });
  	              }
  	            : undefined,
  	          isRowActionOpen: typeof onRowActionMenuOpen === "function"
  	            ? (row) => Boolean(activeRowMenuId && getMenuId(row) === activeRowMenuId)
  	            : undefined,
              onRowContextMenu: typeof onRowContextMenu === "function" ? onRowContextMenu : undefined,
              emptyState: renderEmptyRows(),
              noResultsState: noMatchesLabel,
            });
          };
  
          return React.createElement("div", { className: "playground-project-overview-resources-home" },
            React.createElement("section", { className: "playground-project-resources-table-card" },
              React.createElement("div", { className: "playground-project-resources-table-inner" },
                React.createElement("div", { className: "playground-project-resources-toolbar playground-files-library-title-row" },
  	                React.createElement("label", { className: "playground-project-resources-search" },
  	                  React.createElement(Search, { className: "playground-files-library-search-icon", strokeWidth: 1.8 }),
  	                  React.createElement("input", {
  	                    type: "search",
  	                    value: searchQuery,
  	                    onChange: (event) => typeof onSearchQueryChange === "function" && onSearchQueryChange(event.target.value),
  	                    className: "playground-project-resources-search-input",
  	                    placeholder: searchPlaceholder,
  	                    "aria-label": searchAriaLabel,
  	                  })
                  ),
                  React.createElement("div", { className: "playground-files-library-actions playground-project-resources-toolbar-actions" },
                    showNewButton
                      ? React.createElement("div", { className: "playground-project-resources-new-shell playground-files-library-new-anchor playground-tasks-toolbar-popup-shell" + (activeToolbarPopover === "new" ? " is-open" : "") },
  	                        React.createElement("button", {
  	                          type: "button",
  	                          className: "playground-files-library-new-button" + (newButtonClassName ? " " + newButtonClassName : "") + (activeToolbarPopover === "new" ? " is-active" : ""),
                            onClick: (event) => {
                              event.stopPropagation();
                              closeRowMenu();
                              setToolbarPopover((current) => current === "new" ? "" : "new");
                            },
                          },
                            React.createElement("span", null, newButtonLabel),
                            React.createElement(ChevronDown, { width: 18, height: 18, strokeWidth: 1.8 })
                          ),
                          renderSharedNewMenu()
                        )
                      : null,
                    showFilterButton
                      ? React.createElement("div", { className: "playground-project-resources-filter-shell playground-files-library-control-anchor playground-tasks-toolbar-popup-shell" + (activeToolbarPopover === "filter" ? " is-open" : "") },
                          React.createElement("button", {
                            type: "button",
                            className: "playground-files-library-icon-button" + (activeToolbarPopover === "filter" || activeFilter !== "all" ? " is-active" : ""),
                            onClick: (event) => {
                              event.stopPropagation();
                              closeRowMenu();
                              setToolbarPopover((current) => current === "filter" ? "" : "filter");
                            },
                            title: "Filter resources",
                            "aria-label": "Filter resources",
                          }, React.createElement(SlidersHorizontal, { width: 19, height: 19, strokeWidth: 1.8 })),
                          renderSharedFilterMenu()
                        )
                      : null,
                    showViewToggle
                      ? React.createElement(React.Fragment, null,
                          React.createElement("span", { className: "playground-files-library-divider", "aria-hidden": "true" }),
                          React.createElement("button", {
                            type: "button",
                            className: "playground-files-library-icon-button" + (activeViewMode === "grid" ? " is-active" : ""),
                            onClick: () => {
                              closeRowMenu();
                              typeof onViewModeChange === "function" && onViewModeChange("grid");
                            },
                            title: "Grid view",
                            "aria-label": "Grid view",
                          }, React.createElement(Grid3x3, { width: 20, height: 20, strokeWidth: 1.8 })),
                          React.createElement("button", {
                            type: "button",
                            className: "playground-files-library-icon-button" + (activeViewMode === "list" ? " is-active" : ""),
                            onClick: () => {
                              closeRowMenu();
                              typeof onViewModeChange === "function" && onViewModeChange("list");
                            },
                            title: "List view",
                            "aria-label": "List view",
                          }, React.createElement(List, { width: 21, height: 21, strokeWidth: 1.8 }))
                        )
                      : null
                  )
                ),
  	              activeViewMode === "list"
  	                ? React.createElement("div", { className: "playground-project-resources-list-shell" }, renderRows())
  	                : renderRows()
  			                  )
  			                )
  			          );
        }
  
        function remarkPlaygroundSoftbreaksToBreaks() {
          return (tree) => {
            unistVisit(tree, "text", (node, index, parent) => {
              if (!parent || typeof index !== "number") return;
              if (!node?.value || typeof node.value !== "string" || !node.value.includes("\\n")) return;
  
              const parts = node.value.split("\\n");
              const replacement = [];
              for (let partIndex = 0; partIndex < parts.length; partIndex += 1) {
                if (parts[partIndex]) replacement.push({ type: "text", value: parts[partIndex] });
                if (partIndex < parts.length - 1) replacement.push({ type: "break" });
              }
  
              parent.children.splice(index, 1, ...replacement);
              return index + replacement.length;
            });
          };
        }
  
        const playgroundMarkdownComponents = {
          p: ({ node, ...props }) => React.createElement("p", { className: "tb-message-markdown-paragraph", ...props }),
          strong: ({ node, ...props }) => React.createElement("strong", { className: "tb-message-markdown-strong", ...props }),
          em: ({ node, ...props }) => React.createElement("em", { className: "tb-message-markdown-em", ...props }),
          code: ({ node, className, ...props }) => React.createElement("code", {
            className: className ? "tb-message-markdown-code" : "tb-message-markdown-inline-code",
            ...props,
          }),
          pre: ({ node, ...props }) => React.createElement("pre", { className: "tb-message-markdown-pre", ...props }),
          ul: ({ node, ...props }) => React.createElement("ul", { className: "tb-message-markdown-list", ...props }),
          ol: ({ node, ...props }) => React.createElement("ol", { className: "tb-message-markdown-list tb-message-markdown-list-ordered", ...props }),
          li: ({ node, ...props }) => React.createElement("li", { className: "tb-message-markdown-list-item", ...props }),
          h1: ({ node, ...props }) => React.createElement("h1", { className: "tb-message-markdown-heading", ...props }),
          h2: ({ node, ...props }) => React.createElement("h2", { className: "tb-message-markdown-heading", ...props }),
          h3: ({ node, ...props }) => React.createElement("h3", { className: "tb-message-markdown-heading", ...props }),
          h4: ({ node, ...props }) => React.createElement("h4", { className: "tb-message-markdown-heading", ...props }),
          a: ({ node, ...props }) => React.createElement("a", { className: "tb-message-markdown-link", target: "_blank", rel: "noopener noreferrer", ...props }),
          blockquote: ({ node, ...props }) => React.createElement("blockquote", { className: "tb-message-markdown-quote", ...props }),
          table: ({ node, ...props }) => React.createElement("div", { className: "tb-message-markdown-table-wrap" },
            React.createElement("table", { className: "tb-message-markdown-table", ...props })
          ),
          thead: ({ node, ...props }) => React.createElement("thead", { className: "tb-message-markdown-thead", ...props }),
          tbody: ({ node, ...props }) => React.createElement("tbody", props),
          tr: ({ node, ...props }) => React.createElement("tr", { className: "tb-message-markdown-row", ...props }),
          th: ({ node, ...props }) => React.createElement("th", { className: "tb-message-markdown-th", ...props }),
          td: ({ node, ...props }) => React.createElement("td", { className: "tb-message-markdown-td", ...props }),
          hr: ({ node, ...props }) => React.createElement("hr", { className: "tb-message-markdown-rule", ...props }),
          img: ({ node, ...props }) => React.createElement("img", { className: "tb-message-markdown-image", ...props }),
          u: ({ node, ...props }) => React.createElement("u", { className: "playground-tasks-detail-markdown-underline", ...props }),
        };
  
        function escapePlaygroundMarkdownHtml(text) {
          return String(text || "")
            .replace(/&/g, "&amp;")
            .replace(/</g, "&lt;")
            .replace(/>/g, "&gt;");
        }
  
        function PlaygroundSubscriptionSuccessModal({
          open,
          planLabel = "paid plan",
          onClose,
          onOpenBilling,
        }) {
          if (!open) {
            return null;
          }
  
          const normalizedPlanLabel = String(planLabel || "").trim() || "Enterprise";
  
          return React.createElement(PlatformModalBackdrop, {
              className: "playground-subscription-success-backdrop",
              onClick: () => {
                if (typeof onClose === "function") {
                  onClose();
                }
              },
            },
              React.createElement(PlatformModalSurface, {
                className: "playground-subscription-success-modal",
                onClick: (event) => event.stopPropagation(),
              },
                React.createElement("img", {
                  className: "playground-subscription-success-image",
                  src: "/img/002-hero/inside-rocket.jpg",
                  alt: "Subscription active",
                }),
                React.createElement("div", { className: "playground-subscription-success-copy" },
                  React.createElement("h2", { className: "playground-subscription-success-title" }, "Congratulations!"),
                  React.createElement("p", { className: "playground-subscription-success-description" },
                    "Thank you for subscribing to computer agents ACP! Your ",
                    normalizedPlanLabel,
                    " subscription is now active."
                  )
                ),
                React.createElement("div", { className: "playground-subscription-success-actions" },
                  React.createElement("button", {
                    type: "button",
                    className: "playground-subscription-success-button",
                    onClick: () => {
                      if (typeof onOpenBilling === "function") {
                        onOpenBilling();
                      }
                    },
                  }, "Open billing"),
                  React.createElement(PlatformPrimaryButton, {
                    size: "medium",
                    type: "button",
                    className: "playground-subscription-success-button is-primary",
                    onClick: () => {
                      if (typeof onClose === "function") {
                        onClose();
                      }
                    },
  		                }, "Go to platform")
  		              )
  		            )
  		          );
        }
  
        const STATUS_INDICATOR_PENDING_STORAGE_KEY = "runner_demo_pending_status_indicators_v1";
        const INTEGRATION_STATUS_STORAGE_KEY = "runner_demo_integration_status_v1";
        const HISTORY_MONACO_THEME_NAME = "runner-history-diff";
        const RUNNER_TRANSPARENT_LOGO_URL = "https://computer-agents.com/img/logos/runnertransparent.png";
        const COMPUTER_AGENTS_CREATOR_PROFILE_URL = "/img/agent-profile-pics/ca-profilepic.jpg";
        const PLAYGROUND_GITHUB_LOGO_URL = "https://upload.wikimedia.org/wikipedia/commons/9/91/Octicons-mark-github.svg";
        const PLAYGROUND_GITLAB_LOGO_URL = "https://upload.wikimedia.org/wikipedia/commons/e/e1/GitLab_logo.svg";
        const PLAYGROUND_GOOGLE_DRIVE_LOGO_URL = "https://upload.wikimedia.org/wikipedia/commons/1/12/Google_Drive_icon_%282020%29.svg";
        const PLAYGROUND_ONEDRIVE_LOGO_URL = "https://upload.wikimedia.org/wikipedia/commons/5/59/Microsoft_Office_OneDrive_%282019%E2%80%932025%29.svg";
        const PLAYGROUND_NOTION_LOGO_URL = "https://upload.wikimedia.org/wikipedia/commons/e/e9/Notion-logo.svg";
        const PLAYGROUND_GMAIL_LOGO_URL = "https://upload.wikimedia.org/wikipedia/commons/7/7e/Gmail_icon_%282020%29.svg";
        const PLAYGROUND_ONBOARDING_QUERY_PARAM = "showOnboarding";
        const PLAYGROUND_ONBOARDING_STEP_QUERY_PARAM = "onboardingStep";
        const PLAYGROUND_SUBSCRIPTION_SUCCESS_QUERY_PARAM = "showSubscriptionSuccess";
        const PLAYGROUND_INITIAL_PROMPT_QUERY_PARAM = "initialPrompt";
        const PLAYGROUND_ONBOARDING_STATE_KEY = "runner_demo_playground_onboarding_v1";
        const PLAYGROUND_AUTH_REDIRECT_STATE_KEY = "runner_demo_auth_redirect_v1";
        const PLAYGROUND_AUTH_SESSION_MARKER_KEY = "runner_demo_auth_session_marker_v1";
        const PLAYGROUND_INTEGRATION_REDIRECT_STATE_KEY = "runner_demo_integration_redirect_v1";
        const PLAYGROUND_CONNECTOR_BROWSER_RESTORE_STATE_KEY = "runner_demo_connector_browser_restore_v1";
        const PLAYGROUND_PROJECT_COMPOSER_CONNECTOR_RESTORE_STATE_KEY = "runner_demo_project_composer_connector_restore_v1";
  ${ORGANIZATIONS_DOMAIN_SCRIPT_FRAGMENTS.constants}      const PLAYGROUND_CONNECTOR_BROWSER_RESTORE_QUERY_PARAMS = [
          "connectorProvider",
          "connectorMode",
          "connectorSource",
          "connectorProjectId",
          "connectorView",
          "connectorSavedAt",
        ];
        const INITIAL_THREAD_LAST_VISIT_STORAGE_KEY = "runner_playground_initial_thread_last_visited_at_v1";
  ${CONFIGURE_HOME_DOMAIN_SCRIPT_FRAGMENTS.constants}      const PLAYGROUND_AGENTS_APP_ICON_URL = ${JSON.stringify(aiosOrigin + "/img/logos/agentsappicon.png")};
        const PLAYGROUND_BROWSER_APP_ICON_URL = ${JSON.stringify(aiosOrigin + "/img/logos/browsericon.png")};
        const PLAYGROUND_FILES_APP_ICON_URL = ${JSON.stringify(aiosOrigin + "/img/logos/filesicon.png")};
        const PLAYGROUND_TERMINAL_APP_ICON_URL = ${JSON.stringify(aiosOrigin + "/img/logos/terminalicon.png")};
        const PLAYGROUND_ENVIRONMENTS_APP_ICON_URL = ${JSON.stringify(aiosOrigin + "/img/logos/envappicon.png")};
        const PLAYGROUND_SKILLS_APP_ICON_URL = ${JSON.stringify(aiosOrigin + "/img/logos/skillsicon.webp")};
        const PLAYGROUND_FOLDER_ICON_URL = ${JSON.stringify(aiosOrigin + "/img/logos/folder.png")};
        const PLAYGROUND_TEXT_FILE_ICON_URL = ${JSON.stringify(aiosOrigin + "/img/logos/txtfile.png")};
        const FIREBASE_WEB_API_KEY = ${JSON.stringify(process.env.NEXT_PUBLIC_FIREBASE_API_KEY || "AIzaSyC_aSR8bjU02Kb1ROYUA7Yki_2Fogvs6-o")};
        const FIREBASE_AUTH_DOMAIN = ${JSON.stringify(process.env.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN || "testbaseai.firebaseapp.com")};
        const FIREBASE_PROJECT_ID = ${JSON.stringify(process.env.NEXT_PUBLIC_FIREBASE_PROJECT_ID || "testbaseai")};
        const FIREBASE_STORAGE_BUCKET = ${JSON.stringify(process.env.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET || "testbaseai.firebasestorage.app")};
        const FIREBASE_MESSAGING_SENDER_ID = ${JSON.stringify(process.env.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID || "65067873832")};
        const FIREBASE_APP_ID = ${JSON.stringify(process.env.NEXT_PUBLIC_FIREBASE_APP_ID || "1:65067873832:web:15722955fece43cdf9671d")};
        const SEARCH_THREAD_FETCH_LIMIT = 20;
        const SEARCH_THREAD_EXPANDED_FETCH_LIMIT = 240;
        const SETTINGS_CT_PER_DOLLAR = 100;
        const PLAYGROUND_SERVER_IDLE_RATE_PER_MINUTE = {
          website: 0.000005,
          web_app: 0.000008333333,
          api: 0.000006944444,
          function: 0,
          auth: 0.000004166667,
          agent_runtime: 0.0000125,
          secrets: 0,
        };
        const SETTINGS_SOURCE_CHANNELS = {
          ios: "native",
          macos: "native",
          runner: "web",
          agent_runtime: "web",
          web: "web",
          "runner-web-sdk-demo": "web",
          compass: "web",
          browser: "web",
          cocreate: "web",
          editor: "web",
          comma: "web",
          api: "api",
          automations: "integrations",
          mail: "integrations",
          discord: "integrations",
          telegram: "integrations",
          whatsapp: "integrations",
          unattributed: "unattributed",
        };
        const SETTINGS_SOURCE_LABELS = {
          ios: "Native App",
          macos: "macOS App",
          runner: "Runtime",
          agent_runtime: "Runtime",
          web: "Web App",
          "runner-web-sdk-demo": "Web App",
          compass: "Compass",
          browser: "Compass",
          cocreate: "CoCreate",
          editor: "CoCreate",
          comma: "Comma",
          api: "API",
          automations: "Automations",
          mail: "Email",
          discord: "Discord",
          telegram: "Telegram",
          whatsapp: "WhatsApp",
          unattributed: "Unattributed",
        };
        const SETTINGS_CHANNEL_LABELS = {
          native: "Native App",
          web: "Web App",
          api: "API",
          integrations: "Integrations",
          unattributed: "Unattributed",
        };
        const PLAYGROUND_FIREBASE_APP_NAME = "runner-platform-auth";
        let playgroundFirebaseAuthInstance = null;
        let playgroundFirebaseAuthReadyPromise = null;
        const SETTINGS_CHANNEL_COLORS = {
          native: "rgb(143,196,255)",
          web: "rgb(255,255,255)",
          api: "rgb(103,80,255)",
          integrations: "rgb(94,234,212)",
          unattributed: "rgb(148,163,184)",
        };
  ${PLAYGROUND_BILLING_CATALOG_SCRIPT}
        const SETTINGS_PLATFORM_CONFIG_STORAGE_KEY = "runner_demo_settings_platform_config_v1";
        const SETTINGS_DEFAULT_BILLING_PREFERENCES = {
          usageBillingEnabled: false,
          monthlyResourceSpendLimit: 0,
          pauseOnLimit: true,
          emailAlerts: true,
        };
  ${INFERENCE_DOMAIN_SCRIPT_FRAGMENTS.constants}${API_KEYS_DOMAIN_SCRIPT_FRAGMENTS.scopePresets}      const SETTINGS_TRIGGER_SOURCE_OPTIONS = [
          { value: "github", label: "GitHub" },
          { value: "gitlab", label: "GitLab" },
          { value: "slack", label: "Slack" },
          { value: "webhook", label: "Custom Webhook" },
        ];
        const SETTINGS_GITHUB_EVENTS = ["push", "pull_request", "issues", "issue_comment", "release", "create", "delete", "workflow_run"];
        const SETTINGS_GITHUB_PULL_REQUEST_ACTION_EVENTS = ["pull_request", "issue_comment"];
        const SETTINGS_GITLAB_EVENTS = ["Push Hook", "Merge Request Hook", "Note Hook", "Tag Push Hook", "Pipeline Hook"];
        const SETTINGS_GITLAB_MERGE_REQUEST_ACTION_EVENTS = ["Merge Request Hook", "Note Hook"];
        const SETTINGS_TRIGGER_ACTION_OPTIONS = [
          { value: "send_message", label: "Start thread" },
          { value: "comment_pull_request", label: "Comment on pull request" },
          { value: "comment_merge_request", label: "Comment on merge request" },
        ];
        const PLAYGROUND_CODE_EDITOR_THEME_NAME = "runner-playground-code-editor";
        const RUNNER_WELCOME_GREETING_TEMPLATES = [
          "Welcome back, <name>!",
          "Greetings, <name>!",
          "What's on your mind, <name>?",
          "Welcome, <name>!",
          "What are we working on, <name>?",
          "Where should we begin, <name>?",
          "Good to see you, <name>!",
          "Ready to create, <name>!",
          "Let's get started, <name>!",
        ];
        let historyMonacoModuleLoader = null;
        let historyMonacoThemeRegistered = false;
        let playgroundCodeEditorModuleLoader = null;
        let playgroundCodeEditorThemeRegistered = false;
  
        function loadHistoryMonacoModule() {
          if (!historyMonacoModuleLoader) {
            historyMonacoModuleLoader = import("@monaco-editor/react").catch(() => null);
          }
          return historyMonacoModuleLoader;
        }
  
        function ensureHistoryMonacoTheme(monaco) {
          if (historyMonacoThemeRegistered) {
            return;
          }
  
          monaco.editor.defineTheme(HISTORY_MONACO_THEME_NAME, {
            base: "vs-dark",
            inherit: true,
            rules: [],
            colors: {
              "editor.background": "#00000000",
              "editorGutter.background": "#00000000",
              "editorLineNumber.foreground": "#ffffff52",
              "editorLineNumber.activeForeground": "#ffffff72",
              "editorLineHighlightBackground": "#00000000",
              "editor.selectionBackground": "#ffffff12",
              "editor.inactiveSelectionBackground": "#ffffff08",
              "scrollbar.shadow": "#00000000",
              "diffEditor.insertedTextBackground": "#2ea04333",
              "diffEditor.removedTextBackground": "#f8514933",
              "diffEditor.insertedLineBackground": "#2ea04324",
              "diffEditor.removedLineBackground": "#f8514924",
              "diffEditor.diagonalFill": "#00000000",
            },
          });
          historyMonacoThemeRegistered = true;
        }
  
        function loadPlaygroundCodeEditorModule() {
          if (!playgroundCodeEditorModuleLoader) {
            playgroundCodeEditorModuleLoader = import("@monaco-editor/react").catch((error) => {
              playgroundCodeEditorModuleLoader = null;
              throw error;
            });
          }
          return playgroundCodeEditorModuleLoader;
        }
  
        function ensurePlaygroundCodeEditorTheme(monaco) {
          if (playgroundCodeEditorThemeRegistered || !monaco?.editor?.defineTheme) {
            return;
          }
  
          monaco.editor.defineTheme(PLAYGROUND_CODE_EDITOR_THEME_NAME, {
            base: "vs-dark",
            inherit: true,
            rules: [],
            colors: {
              "editor.background": "#0d1117",
              "editorGutter.background": "#0d1117",
              "editorLineNumber.foreground": "#546173",
              "editorLineNumber.activeForeground": "#d8e1eb",
              "editorLineHighlightBackground": "#141d2800",
              "editor.selectionBackground": "#2f81f733",
              "editor.inactiveSelectionBackground": "#2f81f71f",
              "editorCursor.foreground": "#8ec8ff",
              "editorWhitespace.foreground": "#314055",
              "editorIndentGuide.background1": "#223041",
              "editorIndentGuide.activeBackground1": "#4f6278",
              "scrollbar.shadow": "#00000000",
            },
          });
          playgroundCodeEditorThemeRegistered = true;
        }
  
        function readPendingStatusIndicatorIds() {
          try {
            const raw = sessionStorage.getItem(STATUS_INDICATOR_PENDING_STORAGE_KEY);
            const parsed = raw ? JSON.parse(raw) : [];
            return Array.isArray(parsed) ? parsed.filter((value) => typeof value === "string") : [];
          } catch {
            return [];
          }
        }
  
        function writePendingStatusIndicatorIds(ids) {
          try {
            sessionStorage.setItem(STATUS_INDICATOR_PENDING_STORAGE_KEY, JSON.stringify(ids));
          } catch {}
        }
  
        function addPendingStatusIndicatorId(id) {
          const current = readPendingStatusIndicatorIds();
          if (current.includes(id)) return;
          writePendingStatusIndicatorIds([...current, id]);
        }
  
        function removePendingStatusIndicatorId(id) {
          const current = readPendingStatusIndicatorIds();
          if (!current.includes(id)) return;
          writePendingStatusIndicatorIds(current.filter((value) => value !== id));
        }
  
  ${CONFIGURE_HOME_DOMAIN_SCRIPT_FRAGMENTS.storage}
  ${CONFIGURE_HOME_DOMAIN_SCRIPT_FRAGMENTS.records}
  ${ORGANIZATIONS_DOMAIN_SCRIPT_FRAGMENTS.invitationNotifications}
        function readCachedIntegrationStatuses() {
          try {
            const raw = localStorage.getItem(INTEGRATION_STATUS_STORAGE_KEY);
            const parsed = raw ? JSON.parse(raw) : {};
            return parsed && typeof parsed === "object" ? parsed : {};
          } catch {
            return {};
          }
        }
  
        function readCachedIntegrationStatus(id) {
          const cachedStatuses = readCachedIntegrationStatuses();
          const cachedStatus = cachedStatuses[id];
          if (!cachedStatus || typeof cachedStatus !== "object") {
            return { connected: false };
          }
  
          return {
            connected: Boolean(cachedStatus.connected),
            profile: cachedStatus.profile && typeof cachedStatus.profile === "object" ? cachedStatus.profile : undefined,
          };
        }
  
        function writeCachedIntegrationStatus(id, status) {
          try {
            const cachedStatuses = readCachedIntegrationStatuses();
            if (status?.connected) {
              cachedStatuses[id] = {
                connected: true,
                profile: status.profile && typeof status.profile === "object" ? status.profile : undefined,
              };
            } else {
              delete cachedStatuses[id];
            }
            localStorage.setItem(INTEGRATION_STATUS_STORAGE_KEY, JSON.stringify(cachedStatuses));
          } catch {}
        }
  
  ${ORGANIZATIONS_DOMAIN_SCRIPT_FRAGMENTS.storage}
        function readCurrentSearchParam(name) {
          try {
            return new URLSearchParams(window.location.search).get(name) || "";
          } catch {
            return "";
          }
        }
  
        function readInitialThreadDeepLinkId() {
          const normalized = String(readCurrentSearchParam("thread") || readCurrentSearchParam("threadId") || "").trim();
          return /^thread[_-]/.test(normalized) ? normalized : "";
        }
  
        function normalizePlaygroundInitialPrompt(value) {
          return String(value || "")
            .split(String.fromCharCode(13) + String.fromCharCode(10))
            .join(String.fromCharCode(10))
            .trim()
            .slice(0, 5000);
        }
  
        function readPlaygroundOnboardingState() {
          try {
            const raw = sessionStorage.getItem(PLAYGROUND_ONBOARDING_STATE_KEY);
            const parsed = raw ? JSON.parse(raw) : null;
            return parsed && typeof parsed === "object" && !Array.isArray(parsed) ? parsed : null;
          } catch {
            return null;
          }
        }
  
        function writePlaygroundOnboardingState(value) {
          try {
            sessionStorage.setItem(PLAYGROUND_ONBOARDING_STATE_KEY, JSON.stringify(value || {}));
          } catch {}
        }
  
        function clearPlaygroundOnboardingState() {
          try {
            sessionStorage.removeItem(PLAYGROUND_ONBOARDING_STATE_KEY);
          } catch {}
        }
  
        function readPlaygroundAuthRedirectState() {
          try {
            const raw = sessionStorage.getItem(PLAYGROUND_AUTH_REDIRECT_STATE_KEY);
            const parsed = raw ? JSON.parse(raw) : null;
            return parsed && typeof parsed === "object" && !Array.isArray(parsed) ? parsed : null;
          } catch {
            return null;
          }
        }
  
        function writePlaygroundAuthRedirectState(value) {
          try {
            sessionStorage.setItem(PLAYGROUND_AUTH_REDIRECT_STATE_KEY, JSON.stringify(value || {}));
          } catch {}
        }
  
        function clearPlaygroundAuthRedirectState() {
          try {
            sessionStorage.removeItem(PLAYGROUND_AUTH_REDIRECT_STATE_KEY);
          } catch {}
        }
  
        function readPlaygroundAuthSessionMarker() {
          try {
            const raw = sessionStorage.getItem(PLAYGROUND_AUTH_SESSION_MARKER_KEY);
            const parsed = raw ? JSON.parse(raw) : null;
            return parsed && typeof parsed === "object" && !Array.isArray(parsed) ? parsed : null;
          } catch {
            return null;
          }
        }
  
        function writePlaygroundAuthSessionMarker(value) {
          try {
            sessionStorage.setItem(PLAYGROUND_AUTH_SESSION_MARKER_KEY, JSON.stringify(value || {}));
          } catch {}
        }
  
        function clearPlaygroundAuthSessionMarker() {
          try {
            sessionStorage.removeItem(PLAYGROUND_AUTH_SESSION_MARKER_KEY);
          } catch {}
        }
  
        function readPlaygroundIntegrationRedirectState() {
          try {
            const raw = sessionStorage.getItem(PLAYGROUND_INTEGRATION_REDIRECT_STATE_KEY);
            const parsed = raw ? JSON.parse(raw) : null;
            return parsed && typeof parsed === "object" && !Array.isArray(parsed) ? parsed : null;
          } catch {
            return null;
          }
        }
  
        function writePlaygroundIntegrationRedirectState(value) {
          try {
            sessionStorage.setItem(PLAYGROUND_INTEGRATION_REDIRECT_STATE_KEY, JSON.stringify(value || {}));
          } catch {}
        }
  
        function clearPlaygroundIntegrationRedirectState() {
          try {
            sessionStorage.removeItem(PLAYGROUND_INTEGRATION_REDIRECT_STATE_KEY);
          } catch {}
        }
  
        function normalizePlaygroundConnectorBrowserRestoreState(value) {
          if (!value || typeof value !== "object" || Array.isArray(value)) {
            return null;
          }
          const savedAt = Number(value.savedAt || 0);
          if (!Number.isFinite(savedAt) || Date.now() - savedAt > 1000 * 60 * 20) {
            return null;
          }
          const source = getPlaygroundTaskConnectorSource(value.source) || "";
          const provider = getPlaygroundTaskConnectorSource(value.provider) || source;
          const mode = value.mode === "project" ? "project" : "";
          const projectId = typeof value.projectId === "string" ? value.projectId.trim() : "";
          const view = value.view === "overview" ? "overview" : value.view === "calendar" ? "calendar" : "backlog";
          if (!provider || !source || !mode || !projectId) {
            return null;
          }
          return {
            provider,
            source,
            mode,
            projectId,
            view,
            savedAt,
          };
        }
  
        function readPlaygroundConnectorBrowserRestoreState() {
          try {
            const raw = sessionStorage.getItem(PLAYGROUND_CONNECTOR_BROWSER_RESTORE_STATE_KEY);
            const normalized = normalizePlaygroundConnectorBrowserRestoreState(raw ? JSON.parse(raw) : null);
            if (!normalized) {
              sessionStorage.removeItem(PLAYGROUND_CONNECTOR_BROWSER_RESTORE_STATE_KEY);
            }
            return normalized;
          } catch {
            return null;
          }
        }
  
        function writePlaygroundConnectorBrowserRestoreState(value) {
          try {
            const normalized = normalizePlaygroundConnectorBrowserRestoreState({
              ...(value && typeof value === "object" && !Array.isArray(value) ? value : {}),
              savedAt: Number(value?.savedAt || 0) || Date.now(),
            });
            if (normalized) {
              sessionStorage.setItem(PLAYGROUND_CONNECTOR_BROWSER_RESTORE_STATE_KEY, JSON.stringify(normalized));
            }
          } catch {}
        }
  
        function clearPlaygroundConnectorBrowserRestoreState() {
          try {
            sessionStorage.removeItem(PLAYGROUND_CONNECTOR_BROWSER_RESTORE_STATE_KEY);
          } catch {}
        }
  
        function readPlaygroundConnectorBrowserRestoreUrlState() {
          try {
            const url = new URL(window.location.href);
            const hasRestoreParams = PLAYGROUND_CONNECTOR_BROWSER_RESTORE_QUERY_PARAMS.some((name) => url.searchParams.has(name));
            if (!hasRestoreParams) {
              return null;
            }
            return normalizePlaygroundConnectorBrowserRestoreState({
              provider: url.searchParams.get("connectorProvider") || "",
              mode: url.searchParams.get("connectorMode") || "",
              source: url.searchParams.get("connectorSource") || "",
              projectId: url.searchParams.get("connectorProjectId") || "",
              view: url.searchParams.get("connectorView") || "overview",
              savedAt: Number(url.searchParams.get("connectorSavedAt") || 0) || Date.now(),
            });
          } catch {
            return null;
          }
        }
  
        function clearPlaygroundConnectorBrowserRestoreUrlState() {
          try {
            const url = new URL(window.location.href);
            let changed = false;
            PLAYGROUND_CONNECTOR_BROWSER_RESTORE_QUERY_PARAMS.forEach((name) => {
              if (url.searchParams.has(name)) {
                url.searchParams.delete(name);
                changed = true;
              }
            });
            if (changed) {
              window.history.replaceState({}, "", url.pathname + url.search + url.hash);
            }
          } catch {}
        }
  
        function consumePlaygroundConnectorBrowserRestoreUrlState() {
          const restoreState = readPlaygroundConnectorBrowserRestoreUrlState();
          if (restoreState) {
            writePlaygroundConnectorBrowserRestoreState(restoreState);
          }
          clearPlaygroundConnectorBrowserRestoreUrlState();
          return restoreState;
        }
  
        function buildPlaygroundConnectorBrowserRestoreRedirectUrl(value) {
          const restoreState = normalizePlaygroundConnectorBrowserRestoreState({
            ...(value && typeof value === "object" && !Array.isArray(value) ? value : {}),
            savedAt: Number(value?.savedAt || 0) || Date.now(),
          });
          if (!restoreState) {
            return "";
          }
          try {
            const url = new URL(window.location.href);
            url.searchParams.set("connectorProvider", restoreState.provider);
            url.searchParams.set("connectorMode", restoreState.mode);
            url.searchParams.set("connectorSource", restoreState.source);
            url.searchParams.set("connectorProjectId", restoreState.projectId);
            url.searchParams.set("connectorView", restoreState.view);
            url.searchParams.set("connectorSavedAt", String(restoreState.savedAt));
            return url.toString();
          } catch {
            return "";
          }
        }
  
        function removeCurrentSearchParam(name) {
          try {
            const nextUrl = new URL(window.location.href);
            nextUrl.searchParams.delete(name);
            window.history.replaceState({}, "", nextUrl.toString());
          } catch {}
        }
  
        function isUnauthorizedStatus(status) {
          return status === 401 || status === 403;
        }
  
        function buildStatusIndicatorItem(id, profile) {
          if (id === "google-drive") {
            return {
              id: "google-drive",
              title: "Google Drive connected",
              copy: profile?.email || profile?.username || "Successfully connected",
              logoUrl: "https://upload.wikimedia.org/wikipedia/commons/1/12/Google_Drive_icon_%282020%29.svg",
            };
          }
  
          if (id === "one-drive") {
            return {
              id: "one-drive",
              title: "OneDrive connected",
              copy: profile?.email || profile?.username || "Successfully connected",
              logoUrl: "https://upload.wikimedia.org/wikipedia/commons/5/59/Microsoft_Office_OneDrive_%282019%E2%80%932025%29.svg",
            };
          }
  
          if (id === "github") {
            return {
              id: "github",
              title: "GitHub connected",
              copy: profile?.email || profile?.login || profile?.name || "Successfully connected",
              brand: "github",
            };
          }
  
          if (id === "notion") {
            return {
              id: "notion",
              title: "Notion connected",
              copy: profile?.workspaceName || profile?.name || profile?.email || "Successfully connected",
              logoUrl: "https://upload.wikimedia.org/wikipedia/commons/e/e9/Notion-logo.svg",
            };
          }
  
          if (id === "gmail") {
            return {
              id: "gmail",
              title: "Gmail connected",
              copy: profile?.email || profile?.username || "Successfully connected",
              logoUrl: PLAYGROUND_GMAIL_LOGO_URL,
            };
          }
  
          return null;
        }
  
        function buildServerDeployStatusIndicatorItem(deployState) {
          const normalizedServerId = typeof deployState?.serverId === "string" ? deployState.serverId.trim() : "";
          if (!normalizedServerId) {
            return null;
          }
  
          const serverLabel = typeof deployState?.serverName === "string" && deployState.serverName.trim()
            ? deployState.serverName.trim()
            : "Server";
          const phase = typeof deployState?.phase === "string" ? deployState.phase.trim().toLowerCase() : "";
  
          if (phase === "starting" || phase === "running") {
            return {
              id: "server-deploy:" + normalizedServerId,
              title: "Deployment started",
              copy: serverLabel,
              indeterminate: true,
            };
          }
  
          if (phase === "finished") {
            return {
              id: "server-deploy:" + normalizedServerId,
              title: "Deployment finished",
              copy: typeof deployState?.serviceUrl === "string" && deployState.serviceUrl.trim()
                ? deployState.serviceUrl.trim()
                : serverLabel,
              progress: 100,
            };
          }
  
          if (phase === "failed") {
            return {
              id: "server-deploy:" + normalizedServerId,
              title: "Deployment failed",
              copy: typeof deployState?.error === "string" && deployState.error.trim()
                ? deployState.error.trim()
                : serverLabel,
            };
          }
  
          return null;
        }
  
        function getAccountInitials(value) {
          const normalized = String(value || "").trim();
          if (!normalized) {
            return "CA";
          }
  
          const emailPrefix = normalized.includes("@") ? normalized.split("@")[0] : normalized;
          const parts = emailPrefix
            .split(/[^a-zA-Z0-9]+/)
            .map((part) => part.trim())
            .filter(Boolean);
  
          if (parts.length === 0) {
            return emailPrefix.slice(0, 2).toUpperCase();
          }
  
          if (parts.length === 1) {
            return parts[0].slice(0, 2).toUpperCase();
          }
  
          return (parts[0][0] + parts[1][0]).toUpperCase();
        }
  
        function formatEmailPrefixDisplayName(value) {
          const normalizedValue = String(value || "").trim();
          if (!normalizedValue) {
            return "";
          }
  
          const emailPrefix = normalizedValue.includes("@") ? normalizedValue.split("@")[0] : normalizedValue;
          const parts = emailPrefix
            .split(/[^a-zA-Z0-9]+/)
            .map((part) => part.trim())
            .filter(Boolean);
  
          if (parts.length === 0) {
            return emailPrefix;
          }
  
          return parts
            .map((part) => part.charAt(0).toUpperCase() + part.slice(1))
            .join(" ");
        }
  
        function getTrustedDisplayName(displayName, email) {
          const normalizedDisplayName = String(displayName || "").replace(/\\s+/g, " ").trim();
          if (!normalizedDisplayName) {
            return "";
          }
  
          const normalizedEmail = String(email || "").trim().toLowerCase();
          if (normalizedEmail && normalizedDisplayName.toLowerCase() === normalizedEmail) {
            return "";
          }
  
          const derivedEmailName = formatEmailPrefixDisplayName(email);
          if (
            derivedEmailName
            && normalizedDisplayName.localeCompare(derivedEmailName, undefined, { sensitivity: "accent" }) === 0
          ) {
            return "";
          }
  
          return normalizedDisplayName;
        }
  
        function formatAccountDisplayName(displayName, email, fallbackValue) {
          const trustedDisplayName = getTrustedDisplayName(displayName, email);
          if (trustedDisplayName) {
            return trustedDisplayName;
          }
  
          const emailDerivedDisplayName = formatEmailPrefixDisplayName(email);
          if (emailDerivedDisplayName) {
            return emailDerivedDisplayName;
          }
  
          const normalizedFallback = String(fallbackValue || "").trim();
          if (normalizedFallback) {
            return normalizedFallback;
          }
  
          return "Computer Agents";
        }
  
        function getWelcomeGreetingName(displayName, email) {
          const trustedDisplayName = getTrustedDisplayName(displayName, email);
          const preferredSource = trustedDisplayName || formatEmailPrefixDisplayName(email);
          const normalizedSource = String(preferredSource || "").replace(/\\s+/g, " ").trim();
  
          if (!normalizedSource) {
            return "there";
          }
  
          const firstSegment = normalizedSource.split(" ")[0] || normalizedSource;
          return firstSegment || "there";
        }
  
        function buildWelcomeGreeting(displayName, email) {
          const greetingName = getWelcomeGreetingName(displayName, email);
          const templateIndex = Math.floor(Math.random() * RUNNER_WELCOME_GREETING_TEMPLATES.length);
          return RUNNER_WELCOME_GREETING_TEMPLATES[templateIndex].replace("<name>", greetingName);
        }
  
        function buildDemoWelcomeWidgetsState(referenceDate = new Date()) {
          const demoNow = referenceDate instanceof Date ? new Date(referenceDate) : new Date(referenceDate || Date.now());
          const todayAt = (hours, minutes = 0) => {
            const date = new Date(demoNow);
            date.setHours(hours, minutes, 0, 0);
            return date.toISOString();
          };
          const createdAt = new Date(demoNow.getTime() - 3 * 24 * 60 * 60 * 1000).toISOString();
          const updatedAt = new Date(demoNow.getTime() - 35 * 60 * 1000).toISOString();
          const projectId = "project_demo_crm";
          const project = normalizePlaygroundProjectRecord({
            id: projectId,
            name: "CRM App",
            description: "Demo customer workspace for the landing preview.",
            icon: "layers",
            wallpaperId: "gradient-orange",
            color: "#f4b85f",
            summary: {
              tasksCount: 20,
              openTasksCount: 20,
              environmentsCount: 1,
              threadsCount: 6,
              activeThreadsCount: 2,
            },
            createdAt,
            updatedAt,
          });
          const tasks = [
            {
              id: "task_demo_crm_foundation",
              projectId,
              title: "Backend & Database Foundation",
              status: "in_progress",
              priority: "medium",
              createdAt,
              updatedAt: new Date(demoNow.getTime() - 8 * 60 * 1000).toISOString(),
            },
            {
              id: "task_demo_crm_perf",
              projectId,
              title: "Performance optimization and debugging",
              status: "todo",
              priority: "medium",
              createdAt,
              updatedAt: new Date(demoNow.getTime() - 18 * 60 * 1000).toISOString(),
            },
            {
              id: "task_demo_crm_e2e",
              projectId,
              title: "End-to-end integration testing",
              status: "todo",
              priority: "medium",
              createdAt,
              updatedAt: new Date(demoNow.getTime() - 28 * 60 * 1000).toISOString(),
            },
            {
              id: "task_demo_crm_leads",
              projectId,
              title: "Build lead details edit dialog",
              status: "todo",
              priority: "medium",
              createdAt,
              updatedAt: new Date(demoNow.getTime() - 42 * 60 * 1000).toISOString(),
            },
          ].map((task) => normalizePlaygroundTaskRecord(task));
          const schedules = [
            {
              id: "schedule_demo_daily_briefing",
              name: "Daily Briefing",
              task: "Deliver the morning status briefing for the CRM workspace.",
              projectId,
              environmentName: "Default",
              contextName: "Default",
              metadata: {
                projectId,
                projectName: project.name,
              },
              scheduleType: "one-time",
              scheduledTime: todayAt(8, 0),
              enabled: true,
              createdAt,
              updatedAt,
            },
          ].map((schedule) => normalizePlaygroundScheduleRecord(schedule));
  
          return {
            status: "ready",
            error: "",
            projectId,
            project,
            tasks,
            schedules,
          };
        }
  
        function formatSettingsCurrency(value) {
          return new Intl.NumberFormat("en-US", {
            style: "currency",
            currency: "USD",
            minimumFractionDigits: 2,
            maximumFractionDigits: 2,
          }).format(Number.isFinite(Number(value)) ? Number(value) : 0);
        }
  
        function settingsDollarsToComputeTokens(value) {
          const numericValue = Number(value);
          if (!Number.isFinite(numericValue)) {
            return 0;
          }
  
          return Math.max(0, Math.round(numericValue * SETTINGS_CT_PER_DOLLAR));
        }
  
        function normalizeSettingsCostKey(value) {
          return String(value || "")
            .replace(/([a-z0-9])([A-Z])/g, "$1_$2")
            .replace(/[-\s]+/g, "_")
            .replace(/__+/g, "_")
            .replace(/^_+|_+$/g, "")
            .toLowerCase();
        }
  
        function getSettingsRecordPathValue(record, path) {
          if (!record || typeof record !== "object" || Array.isArray(record)) {
            return undefined;
          }
          const parts = String(path || "").split(".").filter(Boolean);
          let current = record;
          for (const part of parts) {
            if (!current || typeof current !== "object" || Array.isArray(current) || !(part in current)) {
              return undefined;
            }
            current = current[part];
          }
          return current;
        }
  
        function readSettingsFiniteNumber(record, keys = []) {
          const source = record && typeof record === "object" && !Array.isArray(record) ? record : null;
          if (!source) return null;
          for (const key of keys) {
            const value = getSettingsRecordPathValue(source, key);
            if (value == null || value === "") continue;
            const numericValue = Number(value);
            if (Number.isFinite(numericValue)) {
              return numericValue;
            }
          }
          return null;
        }
  
        function buildSettingsUsdAmountKeys(currencyKey, ctKey) {
          const keys = [];
          const add = (value) => {
            const key = String(value || "").trim();
            if (key && !keys.includes(key)) keys.push(key);
          };
          const addKeyGroup = (value) => {
            const key = String(value || "").trim();
            if (!key) return;
            const snake = normalizeSettingsCostKey(key);
            const camelUsd = key.endsWith("Usd") || key.endsWith("USD") ? key : key + "Usd";
            const upperUsd = key.endsWith("Usd") || key.endsWith("USD") ? key : key + "USD";
            add(camelUsd);
            add(upperUsd);
            if (snake) add(snake.endsWith("_usd") ? snake : snake + "_usd");
            add(key);
            if (snake) add(snake);
          };
  
          addKeyGroup(currencyKey);
          const normalizedCurrencyKey = normalizeSettingsCostKey(currencyKey);
          const normalizedCtKey = normalizeSettingsCostKey(ctKey);
          if (normalizedCurrencyKey.includes("agent") || normalizedCtKey.includes("agent")) {
            [
              "agentCostUsd", "agent_cost_usd", "agentUsd", "agent_usd",
              "llmCostUsd", "llm_cost_usd", "modelCostUsd", "model_cost_usd",
              "inferenceCostUsd", "inference_cost_usd", "agentCost", "agent_cost",
            ].forEach(add);
          } else if (normalizedCurrencyKey.includes("environment") || normalizedCtKey.includes("environment")) {
            [
              "environmentCostUsd", "environment_cost_usd", "environmentUsd", "environment_usd",
              "runtimeCostUsd", "runtime_cost_usd", "resourceCostUsd", "resource_cost_usd",
              "computerCostUsd", "computer_cost_usd", "environmentCost", "environment_cost",
            ].forEach(add);
          } else {
            [
              "totalCostUsd", "total_cost_usd", "totalUsd", "total_usd",
              "costUsd", "cost_usd", "usdCost", "usd_cost",
              "amountUsd", "amount_usd", "totalCost", "total_cost", "cost", "amount",
            ].forEach(add);
          }
  
          ["usage", "cost", "costs", "billing", "metadata"].forEach((prefix) => {
            keys.slice().forEach((key) => add(prefix + "." + key));
          });
          return keys;
        }
  
        function buildSettingsLegacyComputeTokenKeys(ctKey) {
          const keys = [];
          const add = (value) => {
            const key = String(value || "").trim();
            if (key && !keys.includes(key)) keys.push(key);
          };
          const addKeyGroup = (value) => {
            const key = String(value || "").trim();
            if (!key) return;
            const snake = normalizeSettingsCostKey(key);
            add(key);
            if (snake) add(snake);
          };
          addKeyGroup(ctKey);
          const normalizedCtKey = normalizeSettingsCostKey(ctKey);
          if (normalizedCtKey.includes("agent")) {
            ["agentCT", "agentCt", "agent_ct", "llmCT", "llmCt", "llm_ct", "modelCT", "modelCt", "model_ct", "inferenceCT", "inferenceCt", "inference_ct"].forEach(add);
          } else if (normalizedCtKey.includes("environment")) {
            ["environmentCT", "environmentCt", "environment_ct", "runtimeCT", "runtimeCt", "runtime_ct", "resourceCT", "resourceCt", "resource_ct", "computerCT", "computerCt", "computer_ct"].forEach(add);
          } else {
            ["totalCT", "totalCt", "total_ct", "totalCostCT", "totalCostCt", "total_cost_ct", "costCT", "costCt", "cost_ct", "computeTokens", "compute_tokens", "ct"].forEach(add);
          }
          ["usage", "cost", "costs", "billing", "metadata"].forEach((prefix) => {
            keys.slice().forEach((key) => add(prefix + "." + key));
          });
          return keys;
        }
  
        function clampSettingsPercentage(value) {
          const numericValue = Number(value);
          if (!Number.isFinite(numericValue)) {
            return 0;
          }
  
          return Math.max(0, Math.min(100, Math.round(numericValue)));
        }
  
        function readSettingsComputeTokens(record, ctKey, currencyKey) {
          const usdValue = readSettingsFiniteNumber(record, buildSettingsUsdAmountKeys(currencyKey, ctKey));
          if (usdValue !== null) {
            return settingsDollarsToComputeTokens(Math.max(0, usdValue));
          }
  
          const ctValue = readSettingsFiniteNumber(record, buildSettingsLegacyComputeTokenKeys(ctKey));
          return ctValue !== null ? Math.max(0, Math.round(ctValue)) : 0;
        }
  
        function readSettingsUsdAmount(record, keys = []) {
          const candidates = [];
          const add = (value) => {
            const key = String(value || "").trim();
            if (key && !candidates.includes(key)) candidates.push(key);
          };
          (Array.isArray(keys) ? keys : [keys]).forEach((key) => {
            const normalizedKey = String(key || "").trim();
            if (!normalizedKey) return;
            const snake = normalizeSettingsCostKey(normalizedKey);
            add(normalizedKey.endsWith("Usd") || normalizedKey.endsWith("USD") ? normalizedKey : normalizedKey + "Usd");
            add(normalizedKey.endsWith("Usd") || normalizedKey.endsWith("USD") ? normalizedKey : normalizedKey + "USD");
            if (snake) add(snake.endsWith("_usd") ? snake : snake + "_usd");
            add(normalizedKey);
            if (snake) add(snake);
          });
          ["billing", "usage", "metadata"].forEach((prefix) => {
            candidates.slice().forEach((key) => add(prefix + "." + key));
          });
          const value = readSettingsFiniteNumber(record, candidates);
          return value !== null ? Math.max(0, value) : 0;
        }
  
        function formatSettingsComputeTokens(value) {
          const numericValue = Math.max(0, Math.round(Number.isFinite(Number(value)) ? Number(value) : 0));
          return formatSettingsCurrency(numericValue / SETTINGS_CT_PER_DOLLAR);
        }
  
        function formatSettingsUsdCredits(value) {
          const numericValue = Math.max(0, Number.isFinite(Number(value)) ? Number(value) : 0);
          return formatSettingsCurrency(numericValue);
        }
  
        function readSettingsTopUpCreditsUsd(pkg) {
          const directCredits = Number(pkg?.creditsUsd);
          if (Number.isFinite(directCredits) && directCredits > 0) {
            return directCredits;
          }
  
          const legacyTokens = Number(pkg?.computeTokens);
          return Number.isFinite(legacyTokens) && legacyTokens > 0
            ? legacyTokens / SETTINGS_CT_PER_DOLLAR
            : 0;
        }
  
        function formatSettingsAxisComputeTokens(value) {
          const numericValue = Math.max(0, Math.round(Number.isFinite(Number(value)) ? Number(value) : 0));
          return formatSettingsCurrency(numericValue / SETTINGS_CT_PER_DOLLAR);
        }
  
        function getSettingsSourceLabel(sourceId) {
          return SETTINGS_SOURCE_LABELS[sourceId] || sourceId;
        }
  
        function getSettingsSourceChannel(sourceId) {
          return SETTINGS_SOURCE_CHANNELS[sourceId] || "integrations";
        }
  
        function formatSettingsUsageResourceKind(value) {
          switch (String(value || "").trim().toLowerCase()) {
            case "llm":
              return "LLM";
            case "thread_runtime":
              return "Thread Runtime";
            case "mcp":
              return "MCP";
            case "computer":
              return "Computer";
            case "website":
              return "Website";
            case "web_app":
              return "Web App";
            case "api":
              return "API";
  	          case "function":
  	            return "Function";
  	          case "auth":
  	            return "Authentication";
  	          case "agent_runtime":
  	            return "Agent Runtime";
            case "database":
              return "Database";
            default:
              return value ? String(value) : "Resource";
          }
        }
  
        function formatSettingsBillingReason(value) {
          const normalized = String(value || "").trim();
          if (!normalized) {
            return "";
          }
  
          return normalized.replace(/_/g, " ");
        }
  
        function createEmptySettingsUsageSummary() {
          return {
            startDate: "",
            endDate: "",
            totals: {
              totalCT: 0,
              agentCT: 0,
              environmentCT: 0,
              totalThreads: 0,
            },
            byDay: [],
          };
        }
  
        function normalizeSettingsSpendLimit(value) {
          const numericValue = Number(value);
          if (!Number.isFinite(numericValue)) {
            return 0;
          }
          return Math.max(0, Math.min(50000, Math.round(numericValue)));
        }
  
        function normalizeDemoSettingsBillingPreferences(value) {
          const source = value && typeof value === "object" ? value : {};
          const normalizedUsageBillingEnabled = Boolean(source.usageBillingEnabled);
          const normalizedSpendLimit = normalizeSettingsSpendLimit(source.monthlyResourceSpendLimit);
          const legacyImplicitDefault = !normalizedUsageBillingEnabled && normalizedSpendLimit === 200;
          const hasEmailAlerts = Object.prototype.hasOwnProperty.call(source, "emailAlerts");
          const hasResourceEmailAlerts = Object.prototype.hasOwnProperty.call(source, "resourceEmailAlerts");
          return {
            usageBillingEnabled: normalizedUsageBillingEnabled,
            monthlyResourceSpendLimit: legacyImplicitDefault ? 0 : normalizedSpendLimit,
            pauseOnLimit: Object.prototype.hasOwnProperty.call(source, "pauseOnLimit")
              ? Boolean(source.pauseOnLimit)
              : SETTINGS_DEFAULT_BILLING_PREFERENCES.pauseOnLimit,
            emailAlerts: hasEmailAlerts
              ? Boolean(source.emailAlerts)
              : hasResourceEmailAlerts
                ? Boolean(source.resourceEmailAlerts)
                : SETTINGS_DEFAULT_BILLING_PREFERENCES.emailAlerts,
          };
        }
  
  ${INFERENCE_DOMAIN_SCRIPT_FRAGMENTS.settings}
        function normalizeDemoSettingsSkillsConfig(source) {
          const normalizedSource = source && typeof source === "object" ? source : {};
          const deepResearchModel = PLAYGROUND_AGENT_DEEP_RESEARCH_MODEL_OPTIONS.some((option) => option.id === normalizedSource.deepResearchModel)
            ? normalizedSource.deepResearchModel
            : "gemini-3-flash-preview";
          const imageGenerationModel = PLAYGROUND_SKILL_IMAGE_MODEL_OPTIONS.some((option) => option.id === normalizedSource.imageGenerationModel)
            ? normalizedSource.imageGenerationModel
            : "gpt-image-2";
          const imageGenerationQuality = PLAYGROUND_SKILL_IMAGE_QUALITY_OPTIONS.some((option) => option.id === normalizedSource.imageGenerationQuality)
            ? normalizedSource.imageGenerationQuality
            : "medium";
          const videoGenerationModel = normalizePlaygroundVideoGenerationModelId(normalizedSource.videoGenerationModel);
          return {
            deepResearchModel,
            imageGenerationModel,
            imageGenerationQuality,
            videoGenerationModel,
          };
        }
  
        function getDemoDefaultDeepResearchModel(config) {
          return normalizeDemoSettingsSkillsConfig(config?.skills).deepResearchModel;
        }
  
        function getDemoDefaultImageGenerationModel(config) {
          return normalizeDemoSettingsSkillsConfig(config?.skills).imageGenerationModel;
        }
  
        function getDemoDefaultImageGenerationQuality(config) {
          return normalizeDemoSettingsSkillsConfig(config?.skills).imageGenerationQuality;
        }
  
        function getDemoDefaultVideoGenerationModel(config) {
          return normalizeDemoSettingsSkillsConfig(config?.skills).videoGenerationModel;
        }
  
        function readDemoSettingsPlatformConfig() {
          try {
            const raw = localStorage.getItem(SETTINGS_PLATFORM_CONFIG_STORAGE_KEY);
            const parsed = raw ? JSON.parse(raw) : {};
            const source = parsed && typeof parsed === "object" ? parsed : {};
            return {
              billing: normalizeDemoSettingsBillingPreferences(source.billing),
              inference: normalizeDemoSettingsInferenceSettings(source.inference),
              skills: normalizeDemoSettingsSkillsConfig(source.skills),
            };
          } catch {
            return {
              billing: normalizeDemoSettingsBillingPreferences(null),
              inference: normalizeDemoSettingsInferenceSettings(null),
              skills: normalizeDemoSettingsSkillsConfig(null),
            };
          }
        }
  
        function writeDemoSettingsPlatformConfig(config) {
          try {
            localStorage.setItem(SETTINGS_PLATFORM_CONFIG_STORAGE_KEY, JSON.stringify({
              billing: normalizeDemoSettingsBillingPreferences(config?.billing),
              inference: normalizeDemoSettingsInferenceSettings(config?.inference),
              skills: normalizeDemoSettingsSkillsConfig(config?.skills),
            }));
          } catch {}
        }
  
  ${INFERENCE_DOMAIN_SCRIPT_FRAGMENTS.modelOptions}
        function formatSettingsDate(value) {
          if (!value) {
            return "Unavailable";
          }
  
          const date = new Date(value);
          if (Number.isNaN(date.getTime())) {
            return "Unavailable";
          }
  
          return date.toLocaleDateString("en-US", {
            month: "short",
            day: "numeric",
            year: "numeric",
          });
        }
  
        function formatSettingsDateTime(value) {
          if (!value) {
            return "Unavailable";
          }
  
          const date = new Date(value);
          if (Number.isNaN(date.getTime())) {
            return "Unavailable";
          }
  
          return date.toLocaleString("en-US", {
            month: "short",
            day: "numeric",
            year: "numeric",
            hour: "numeric",
            minute: "2-digit",
          });
        }
  
  ${INFERENCE_DOMAIN_SCRIPT_FRAGMENTS.runtime}
  ${API_KEYS_DOMAIN_SCRIPT_FRAGMENTS.helpers}
        function getSettingsTriggerSourceMeta(source) {
          switch (String(source || "").trim().toLowerCase()) {
            case "github":
              return {
                label: "GitHub",
                icon: GitCommitHorizontal,
              };
            case "gitlab":
              return {
                label: "GitLab",
                icon: GitCommitHorizontal,
              };
            case "slack":
              return {
                label: "Slack",
                icon: MessageCircle,
              };
            default:
              return {
                label: "Webhook",
                icon: Webhook,
              };
          }
        }
  
        function getSettingsTriggerActionLabel(action) {
          const actionType = String(action?.type || "").trim().toLowerCase();
          if (actionType === "comment_pull_request") {
            return "Comment on PR";
          }
          if (actionType === "comment_merge_request") {
            return "Comment on MR";
          }
          return "Start thread";
        }
  
        function isSettingsTriggerActionSupportedForSource(source, actionType) {
          const normalizedSource = String(source || "").trim().toLowerCase();
          const normalizedActionType = String(actionType || "").trim().toLowerCase();
          if (normalizedActionType === "comment_pull_request") {
            return normalizedSource === "github";
          }
          if (normalizedActionType === "comment_merge_request") {
            return normalizedSource === "gitlab";
          }
          return true;
        }
  
        function getSettingsTriggerDefaultEvent(source, actionType) {
          const normalizedSource = String(source || "").trim().toLowerCase();
          const normalizedActionType = String(actionType || "").trim().toLowerCase();
          if (normalizedSource === "github") {
            return normalizedActionType === "comment_pull_request" ? "pull_request" : "push";
          }
          if (normalizedSource === "gitlab") {
            return normalizedActionType === "comment_merge_request" ? "Merge Request Hook" : "Push Hook";
          }
          return "";
        }
  
        function getSettingsTriggerEventOptions(source, actionType) {
          const normalizedSource = String(source || "").trim().toLowerCase();
          const normalizedActionType = String(actionType || "").trim().toLowerCase();
          if (normalizedSource === "github") {
            return normalizedActionType === "comment_pull_request"
              ? SETTINGS_GITHUB_PULL_REQUEST_ACTION_EVENTS
              : SETTINGS_GITHUB_EVENTS;
          }
          if (normalizedSource === "gitlab") {
            return normalizedActionType === "comment_merge_request"
              ? SETTINGS_GITLAB_MERGE_REQUEST_ACTION_EVENTS
              : SETTINGS_GITLAB_EVENTS;
          }
          return [];
        }
  
        function getSettingsTriggerPromptPlaceholder(source, actionType) {
          const normalizedSource = String(source || "").trim().toLowerCase();
          const normalizedActionType = String(actionType || "").trim().toLowerCase();
          if (normalizedActionType === "comment_pull_request") {
            return "Review the pull request changes and write a concise, helpful GitHub comment.";
          }
          if (normalizedActionType === "comment_merge_request") {
            return "Review the merge request changes and write a concise, helpful GitLab comment.";
          }
          if (normalizedSource === "gitlab") {
            return "Inspect the GitLab event, pull the latest changes if needed, and summarize what happened.";
          }
          return "Pull the latest changes, inspect the webhook payload, and summarize what happened.";
        }
  
        function mapFirebaseAuthErrorMessage(code, fallbackMessage) {
          const normalized = String(code || "").trim().toUpperCase();
          if (!normalized) {
            return fallbackMessage;
          }
          if (normalized === "INVALID_LOGIN_CREDENTIALS" || normalized === "INVALID_PASSWORD") {
            return "Current password is incorrect.";
          }
          if (normalized === "EMAIL_NOT_FOUND" || normalized === "USER_NOT_FOUND") {
            return "This account could not be verified.";
          }
          if (normalized.startsWith("WEAK_PASSWORD")) {
            return "New password must be at least 6 characters.";
          }
          if (normalized === "USER_DISABLED") {
            return "This account is disabled.";
          }
          if (normalized === "TOO_MANY_ATTEMPTS_TRY_LATER") {
            return "Too many attempts. Try again later.";
          }
          if (normalized === "OPERATION_NOT_ALLOWED") {
            return "Password authentication is not enabled for this account.";
          }
          return fallbackMessage;
        }
  
        function buildFirebaseRestUrl(pathname) {
          return "https://identitytoolkit.googleapis.com/v1/" + pathname + "?key=" + encodeURIComponent(FIREBASE_WEB_API_KEY);
        }
  
        function getFirebaseSessionCookieDomain() {
          if (typeof window === "undefined") {
            return "";
          }
          const hostname = String(window.location.hostname || "").trim();
          if (!hostname || hostname === "localhost" || hostname === "127.0.0.1") {
            return "";
          }
          const parts = hostname.split(".").filter(Boolean);
          if (parts.length < 2) {
            return "";
          }
          return "." + parts.slice(-2).join(".");
        }
  
        function writeFirebaseSessionCookie(token) {
          if (typeof document === "undefined") {
            return;
          }
          const normalizedToken = typeof token === "string" ? token.trim() : "";
          const secure = typeof window !== "undefined" ? window.location.protocol === "https:" : true;
          const maxAge = 7 * 24 * 60 * 60;
          const baseCookie = "__session=" + encodeURIComponent(normalizedToken)
            + "; Path=/; Max-Age=" + maxAge
            + "; SameSite=Lax"
            + (secure ? "; Secure" : "");
          const domain = getFirebaseSessionCookieDomain();
          document.cookie = baseCookie + (domain ? "; Domain=" + domain : "");
        }
  
        function clearFirebaseSessionCookie() {
          if (typeof document === "undefined") {
            return;
          }
          const secure = typeof window !== "undefined" ? window.location.protocol === "https:" : true;
          const expiredCookie = "__session=; Path=/; Expires=Thu, 01 Jan 1970 00:00:00 GMT; SameSite=Lax"
            + (secure ? "; Secure" : "");
          const domain = getFirebaseSessionCookieDomain();
          document.cookie = expiredCookie;
          if (domain) {
            document.cookie = expiredCookie + "; Domain=" + domain;
          }
        }
  
        function getPlaygroundFirebaseAuth() {
          if (!FIREBASE_WEB_API_KEY || !FIREBASE_AUTH_DOMAIN || !FIREBASE_PROJECT_ID) {
            return null;
          }
          if (playgroundFirebaseAuthInstance) {
            return playgroundFirebaseAuthInstance;
          }
  
          const firebaseConfig = {
            apiKey: FIREBASE_WEB_API_KEY,
            authDomain: FIREBASE_AUTH_DOMAIN,
            projectId: FIREBASE_PROJECT_ID,
            storageBucket: FIREBASE_STORAGE_BUCKET,
            messagingSenderId: FIREBASE_MESSAGING_SENDER_ID,
            appId: FIREBASE_APP_ID,
          };
          try {
            const existingApp = getApps().find((app) => app.name === PLAYGROUND_FIREBASE_APP_NAME);
            const firebaseApp = existingApp || initializeApp(firebaseConfig, PLAYGROUND_FIREBASE_APP_NAME);
            playgroundFirebaseAuthInstance = getAuth(firebaseApp);
            return playgroundFirebaseAuthInstance;
          } catch {
            return null;
          }
        }
  
        async function ensurePlaygroundFirebaseAuth() {
          const auth = getPlaygroundFirebaseAuth();
          if (!auth) {
            return null;
          }
          if (!playgroundFirebaseAuthReadyPromise) {
            playgroundFirebaseAuthReadyPromise = setPersistence(auth, browserLocalPersistence).catch(() => {});
          }
          await playgroundFirebaseAuthReadyPromise;
          return auth;
        }
  
        function createFetchAbortReason(name, message) {
          if (typeof DOMException === "function") {
            return new DOMException(message, name);
          }
          const error = new Error(message);
          error.name = name;
          return error;
        }
  
        function getSignalAbortReason(signal, fallbackMessage) {
          return signal && "reason" in signal && signal.reason
            ? signal.reason
            : createFetchAbortReason("AbortError", fallbackMessage);
        }
  
        function isFetchAbortError(error) {
          const name = String(error?.name || "").toLowerCase();
          const message = String(error?.message || error || "").toLowerCase();
          return name === "aborterror"
            || name === "timeouterror"
            || message.includes("aborted")
            || message.includes("signal is aborted")
            || message.includes("aborterror")
            || message.includes("timeouterror");
        }
  
        async function fetchJsonWithTimeout(input, init = {}, timeoutMs = 8000) {
          const controller = new AbortController();
          const externalSignal = init?.signal;
          const abortFromExternalSignal = () => {
            if (!controller.signal.aborted) {
              controller.abort(getSignalAbortReason(externalSignal, "Request was cancelled."));
            }
          };
          if (externalSignal) {
            if (externalSignal.aborted) {
              controller.abort(getSignalAbortReason(externalSignal, "Request was cancelled."));
            } else {
              externalSignal.addEventListener("abort", abortFromExternalSignal, { once: true });
            }
          }
          const normalizedTimeoutMs = Math.max(250, Number(timeoutMs) || 0);
          const timeoutId = window.setTimeout(() => {
            if (!controller.signal.aborted) {
              controller.abort(createFetchAbortReason("TimeoutError", "Request timed out after " + String(normalizedTimeoutMs) + "ms."));
            }
          }, normalizedTimeoutMs);
  
          try {
            const response = await fetch(input, {
              ...init,
              signal: controller.signal,
            });
            const data = await response.json().catch(() => ({}));
            return { response, data };
          } finally {
            window.clearTimeout(timeoutId);
            if (externalSignal) {
              externalSignal.removeEventListener("abort", abortFromExternalSignal);
            }
          }
        }
  
        function normalizeSessionIdentityValue(value) {
          return String(value || "").replace(/\\s+/g, " ").trim();
        }
  
        function getSessionIdentityObject(value) {
          return value && typeof value === "object" && !Array.isArray(value) ? value : null;
        }
  
        function pickFirstSessionIdentityValue(...values) {
          for (const value of values) {
            const normalizedValue = normalizeSessionIdentityValue(value);
            if (normalizedValue) {
              return normalizedValue;
            }
          }
          return "";
        }
  
        function normalizeSessionPhotoUrl(value) {
          const normalizedValue = normalizeSessionIdentityValue(value);
          if (!normalizedValue) {
            return "";
          }
          if (normalizedValue.startsWith("//")) {
            return "https:" + normalizedValue;
          }
          return normalizedValue;
        }
  
        function getPreferredSessionProviderProfile(...sources) {
          const providerEntries = [];
          sources.forEach((source) => {
            const providerUserInfo = Array.isArray(source?.providerUserInfo) ? source.providerUserInfo : [];
            providerUserInfo.forEach((entry) => {
              const normalizedEntry = getSessionIdentityObject(entry);
              if (normalizedEntry) {
                providerEntries.push(normalizedEntry);
              }
            });
          });
  
          if (!providerEntries.length) {
            return null;
          }
  
          return (
            providerEntries.find((entry) => normalizeSessionIdentityValue(entry.providerId).toLowerCase() === "google.com")
            || providerEntries.find((entry) => (
              pickFirstSessionIdentityValue(
                entry.displayName,
                entry.name,
                entry.email,
                entry.photoUrl,
                entry.photoURL,
                entry.avatarUrl,
                entry.picture
              )
            ))
            || null
          );
        }
  
        function extractSessionIdentityFromPayload(payload) {
          const root = getSessionIdentityObject(payload);
          const profile = getSessionIdentityObject(root?.profile);
          const user = getSessionIdentityObject(root?.user);
          const authProfile = getSessionIdentityObject(root?.authProfile);
          const preferredProvider = getPreferredSessionProviderProfile(root, profile, user, authProfile);
  
          const email = pickFirstSessionIdentityValue(
            root?.email,
            profile?.email,
            user?.email,
            authProfile?.email,
            preferredProvider?.email
          );
          const displayName = getTrustedDisplayName(
            pickFirstSessionIdentityValue(
              profile?.displayName,
              profile?.display_name,
              profile?.name,
              root?.displayName,
              root?.display_name,
              root?.name,
              user?.displayName,
              user?.display_name,
              user?.name,
              authProfile?.displayName,
              authProfile?.display_name,
              authProfile?.name,
              preferredProvider?.displayName,
              preferredProvider?.name
            ),
            email
          ) || formatEmailPrefixDisplayName(email);
          const photoURL = normalizeSessionPhotoUrl(
            pickFirstSessionIdentityValue(
              profile?.photoURL,
              profile?.photoUrl,
              profile?.photo_url,
              profile?.avatarUrl,
              profile?.avatarURL,
              profile?.avatar,
              profile?.picture,
              root?.photoURL,
              root?.photoUrl,
              root?.photo_url,
              root?.avatarUrl,
              root?.avatar,
              root?.picture,
              user?.photoURL,
              user?.photoUrl,
              user?.photo_url,
              user?.avatarUrl,
              user?.avatar,
              user?.picture,
              authProfile?.photoURL,
              authProfile?.photoUrl,
              authProfile?.photo_url,
              authProfile?.avatarUrl,
              authProfile?.avatar,
              authProfile?.picture,
              preferredProvider?.photoURL,
              preferredProvider?.photoUrl,
              preferredProvider?.photo_url,
              preferredProvider?.avatarUrl,
              preferredProvider?.avatar,
              preferredProvider?.picture
            )
          );
          const emailVerified = typeof root?.emailVerified === "boolean"
            ? root.emailVerified
            : (typeof authProfile?.emailVerified === "boolean" ? authProfile.emailVerified : undefined);
  
          return {
            email,
            displayName,
            photoURL: canRenderAvatarImage(photoURL) ? photoURL : "",
            emailVerified,
          };
        }
  
        function readNamedCookie(name) {
          if (typeof document === "undefined") {
            return "";
          }
          const normalizedName = String(name || "").trim();
          if (!normalizedName) {
            return "";
          }
          const escapedName = normalizedName.replace(/[.*+?^{}$()|[\\]\\\\]/g, "\\\\$&");
          const match = document.cookie.match(new RegExp("(?:^|; )" + escapedName + "=([^;]*)"));
          if (!match || typeof match[1] !== "string") {
            return "";
          }
          try {
            return decodeURIComponent(match[1]);
          } catch {
            return match[1];
          }
        }
  
        async function lookupFirebaseSessionIdentity() {
          const sessionToken = readNamedCookie("__session");
          if (!sessionToken) {
            return null;
          }
  
          const { response, data } = await fetchJsonWithTimeout(buildFirebaseRestUrl("accounts:lookup"), {
            method: "POST",
            headers: {
              "Content-Type": "application/json",
            },
            body: JSON.stringify({
              idToken: sessionToken,
            }),
          }, 3000);
          if (!response.ok) {
            return null;
          }
  
          const user = Array.isArray(data?.users) ? getSessionIdentityObject(data.users[0]) : null;
          if (!user) {
            return null;
          }
  
          const identity = extractSessionIdentityFromPayload({
            email: user.email,
            emailVerified: user.emailVerified,
            user: {
              email: user.email,
              uid: user.localId || user.userId || user.id || "",
              displayName: user.displayName,
              name: user.displayName,
              photoURL: user.photoURL || user.photoUrl,
              photoUrl: user.photoUrl || user.photoURL,
              providerUserInfo: Array.isArray(user.providerUserInfo) ? user.providerUserInfo : [],
            },
          });
          return {
            ...identity,
            userId: pickFirstSessionIdentityValue(user.localId, user.userId, user.id),
          };
        }
  
        async function syncFirebaseSessionCookieFromCurrentUser(forceRefresh = false) {
          try {
            const auth = await ensurePlaygroundFirebaseAuth();
            const currentUser = auth?.currentUser || null;
            if (!currentUser) {
              return false;
            }
            const idToken = await currentUser.getIdToken(Boolean(forceRefresh));
            if (!idToken) {
              return false;
            }
            writeFirebaseSessionCookie(idToken);
            return true;
          } catch {
            return false;
          }
        }
  
        async function copyTextToClipboard(value) {
          try {
            await navigator.clipboard.writeText(String(value || ""));
            return true;
          } catch {
            return false;
          }
        }
  
        function canRenderAvatarImage(value) {
          const normalized = normalizeSessionPhotoUrl(value);
          return normalized.startsWith("data:image/")
            || /^https?:\\/\\//i.test(normalized)
            || normalized.startsWith("/img/");
        }
  
        function AccountAvatar({ className, imageClassName, fallbackLabel, photoUrl }) {
          const [imageReady, setImageReady] = useState(false);
          const normalizedPhotoUrl = normalizeSessionPhotoUrl(photoUrl);
          const normalizedFallbackLabel = normalizeSessionIdentityValue(fallbackLabel) || "CA";
          const fallbackClassName = className + "-fallback";
          const shouldAttemptImage = canRenderAvatarImage(normalizedPhotoUrl);
  
          useEffect(() => {
            let cancelled = false;
            setImageReady(false);
  
            if (!shouldAttemptImage) {
              return undefined;
            }
  
            const probe = new Image();
            probe.onload = () => {
              if (!cancelled) {
                setImageReady(true);
              }
            };
            probe.onerror = () => {
              if (!cancelled) {
                setImageReady(false);
              }
            };
            probe.src = normalizedPhotoUrl;
  
            return () => {
              cancelled = true;
            };
          }, [normalizedPhotoUrl, shouldAttemptImage]);
  
          return React.createElement("div", { className },
            imageReady && shouldAttemptImage
              ? React.createElement("img", {
                  className: imageClassName,
                  src: normalizedPhotoUrl,
                  alt: "",
                  "aria-hidden": "true",
                  referrerPolicy: "no-referrer",
                })
              : React.createElement("span", { className: fallbackClassName, "aria-hidden": "true" }, normalizedFallbackLabel)
          );
        }
  
        function PlaygroundDotLoader({
          dotCount = 9,
          dotSize = 4,
          gap = 3,
          speed = 800,
        }) {
          const [activeIndex, setActiveIndex] = useState(0);
          const gridSize = dotCount === 4 ? 2 : 3;
          const pattern = dotCount === 4
            ? [1, 0, 2, 3, -1, -1, -1]
            : [2, 1, 0, 3, 6, 7, 8, 5, 4, -1, -1, -1, -1];
  
          useEffect(() => {
            const interval = window.setInterval(() => {
              setActiveIndex((current) => (current + 1) % pattern.length);
            }, speed / pattern.length);
            return () => window.clearInterval(interval);
          }, [pattern.length, speed]);
  
          function getDotOpacity(dotIndex) {
            const currentPatternValue = pattern[activeIndex];
  
            if (currentPatternValue === -1) {
              return 0.1;
            }
  
            const patternPosition = pattern.indexOf(dotIndex);
            if (patternPosition === -1) {
              return 0.1;
            }
  
            const distance = (activeIndex - patternPosition + pattern.length) % pattern.length;
  
            if (distance === 0) return 1;
            if (distance === 1) return 0.5;
            if (distance === 2) return 0.25;
            return 0.1;
          }
  
          return React.createElement("div", {
              className: "grid",
              style: {
                display: "grid",
                gridTemplateColumns: "repeat(" + gridSize + ", " + dotSize + "px)",
                gap: String(gap) + "px",
              },
            },
            Array.from({ length: dotCount }).map((_, index) =>
              React.createElement("span", {
                key: "playground-dot-loader:" + index,
                className: "rounded-full",
                style: {
                  width: dotSize,
                  height: dotSize,
                  borderRadius: "999px",
                  backgroundColor: "rgba(255, 255, 255, " + getDotOpacity(index) + ")",
                  transition: "background-color 200ms ease-out",
                },
              })
            )
          );
        }
  
        function PlaygroundAppLoadingScreen({ label = "Agentic Compute Platform" }) {
          return React.createElement("div", {
              style: {
                position: "fixed",
                inset: 0,
                zIndex: 100,
                display: "flex",
                alignItems: "center",
                justifyContent: "center",
                background: "#000",
                pointerEvents: "none",
                opacity: 1,
                transition: "opacity 0.2s ease-out",
              },
            },
            React.createElement("div", {
                style: {
                  display: "flex",
                  alignItems: "center",
                  gap: "16px",
                },
              },
              React.createElement(PlaygroundDotLoader, {
                dotCount: 9,
                dotSize: 4,
                gap: 3,
                speed: 800,
              }),
              React.createElement("span", {
                style: {
                  fontSize: "18px",
                  lineHeight: 1,
                  fontWeight: 600,
                  color: "#fff",
                },
              }, label)
            )
          );
        }
  
        function readFileAsDataUrl(file) {
          return new Promise((resolve, reject) => {
            const reader = new FileReader();
            reader.onload = () => resolve(typeof reader.result === "string" ? reader.result : "");
            reader.onerror = () => reject(reader.error || new Error("Failed to read file"));
            reader.readAsDataURL(file);
          });
        }
  
        async function readFileAsBase64(file) {
          const dataUrl = await readFileAsDataUrl(file);
          const commaIndex = dataUrl.indexOf(",");
          return commaIndex >= 0 ? dataUrl.slice(commaIndex + 1) : dataUrl;
        }
  
        function loadImageElement(src) {
          return new Promise((resolve, reject) => {
            const image = new Image();
            image.onload = () => resolve(image);
            image.onerror = () => reject(new Error("Failed to load selected image"));
            image.src = src;
          });
        }
  
        async function createProfilePhotoDataUrl(file) {
          const sourceDataUrl = await readFileAsDataUrl(file);
          const image = await loadImageElement(sourceDataUrl);
          const canvas = document.createElement("canvas");
          const targetSize = 320;
          const sourceWidth = image.naturalWidth || image.width || targetSize;
          const sourceHeight = image.naturalHeight || image.height || targetSize;
          const squareSize = Math.min(sourceWidth, sourceHeight);
          const sourceX = Math.max(0, Math.floor((sourceWidth - squareSize) / 2));
          const sourceY = Math.max(0, Math.floor((sourceHeight - squareSize) / 2));
  
          canvas.width = targetSize;
          canvas.height = targetSize;
  
          const context = canvas.getContext("2d");
          if (!context) {
            throw new Error("Failed to prepare profile picture preview");
          }
  
          context.clearRect(0, 0, targetSize, targetSize);
          context.drawImage(
            image,
            sourceX,
            sourceY,
            squareSize,
            squareSize,
            0,
            0,
            targetSize,
            targetSize
          );
  
          return canvas.toDataURL("image/jpeg", 0.84);
        }
  
        function StatusIndicatorCloseIcon() {
          return React.createElement("svg", {
            className: "status-indicator-close-icon",
            viewBox: "0 0 24 24",
            fill: "none",
            stroke: "currentColor",
            strokeWidth: "2",
            strokeLinecap: "round",
            strokeLinejoin: "round",
            "aria-hidden": "true"
          },
            React.createElement("path", { d: "M18 6 6 18" }),
            React.createElement("path", { d: "m6 6 12 12" })
          );
        }
  
        function StatusIndicatorGithubLogo() {
          return React.createElement("svg", {
            className: "status-indicator-logo",
            viewBox: "0 0 24 24",
            fill: "currentColor",
            "aria-hidden": "true"
          },
            React.createElement("path", {
              d: "M12 0C5.37 0 0 5.37 0 12c0 5.3 3.44 9.8 8.21 11.39.6.11.79-.26.79-.58v-2.23c-3.34.72-4.03-1.42-4.03-1.42-.55-1.38-1.33-1.75-1.33-1.75-1.09-.75.08-.73.08-.73 1.2.08 1.84 1.24 1.84 1.24 1.07 1.83 2.81 1.3 3.49 1 .11-.78.42-1.31.76-1.61-2.67-.3-5.47-1.33-5.47-5.93 0-1.31.47-2.38 1.24-3.22-.12-.3-.54-1.52.12-3.17 0 0 1.01-.32 3.3 1.23A11.5 11.5 0 0 1 12 6.8c1.02 0 2.05.14 3 .4 2.29-1.55 3.3-1.23 3.3-1.23.66 1.65.24 2.87.12 3.17.77.84 1.24 1.91 1.24 3.22 0 4.61-2.81 5.62-5.48 5.92.43.37.82 1.1.82 2.22v3.29c0 .32.19.69.8.58A12 12 0 0 0 24 12c0-6.63-5.37-12-12-12Z"
            })
          );
        }
  
        function StatusIndicator({ title, copy, logoUrl, brand, progress, indeterminate, onDismiss }) {
          const [isVisible, setIsVisible] = useState(false);
          const [isExiting, setIsExiting] = useState(false);
          const hasProgress = Number.isFinite(progress) || indeterminate;
          const normalizedProgress = Number.isFinite(progress) ? Math.max(0, Math.min(100, Number(progress))) : 0;
  
          useEffect(() => {
            const frameId = window.requestAnimationFrame(() => setIsVisible(true));
            return () => window.cancelAnimationFrame(frameId);
          }, []);
  
          function handleDismiss() {
            setIsExiting(true);
            window.setTimeout(() => {
              onDismiss?.();
            }, 180);
          }
  
          return React.createElement("div", {
            className: "status-indicator" + (isVisible && !isExiting ? " is-visible" : "") + (isExiting ? " is-exiting" : "")
          },
            (logoUrl || brand)
              ? React.createElement("div", { className: "status-indicator-media" },
                  brand === "github"
                    ? React.createElement(StatusIndicatorGithubLogo)
                    :
                  React.createElement("img", {
                    className: "status-indicator-logo",
                    src: logoUrl,
                    alt: "",
                    "aria-hidden": "true",
                    draggable: false
                  })
                )
              : null,
            React.createElement("div", { className: "status-indicator-body" },
              React.createElement("div", { className: "status-indicator-title" }, title),
              React.createElement("div", { className: "status-indicator-copy" }, copy),
              hasProgress
                ? React.createElement("div", { className: "status-indicator-progress" },
                    React.createElement("div", {
                      className: "status-indicator-progress-fill" + (indeterminate ? " is-indeterminate" : ""),
                      style: indeterminate ? undefined : { width: normalizedProgress + "%" },
                    })
                  )
                : null
            ),
            React.createElement("button", {
              type: "button",
              className: "status-indicator-close",
              onClick: handleDismiss,
              "aria-label": "Dismiss " + title
            }, React.createElement(StatusIndicatorCloseIcon))
          );
        }
  
        function StatusIndicatorStack({ items, emptyText, dismissedIds, onDismiss }) {
          const visibleItems = items.filter((item) => !dismissedIds.includes(item.id));
          if (visibleItems.length === 0) {
            return null;
          }
  
          return React.createElement("div", { className: "status-indicator-stack" },
            React.createElement("div", { className: "status-indicator-list" },
              visibleItems.map((item) =>
                React.createElement(StatusIndicator, {
                  key: item.id,
                  title: item.title,
                  copy: item.copy,
                  logoUrl: item.logoUrl,
                  brand: item.brand,
                  progress: item.progress,
                  indeterminate: item.indeterminate,
                  onDismiss: () => onDismiss(item.id)
                })
              )
            )
          );
        }
  
        const DEMO_PINNED_THREADS = [
          { id: "pin_runner_playground", title: "Improve runner playground sidebar", positive: "+2", negative: "-1", ageLabel: "2 H" },
          { id: "pin_sdk_foundation", title: "Runner SDK foundation cleanup", positive: "+4", negative: "-0", ageLabel: "1 D" }
        ];
  
        const DEMO_RECENT_THREADS = [
          { id: "demo_thread_sidebar", title: "on localhost:4177 we have this runner playground sample application running", messageCount: 12, createdAt: new Date(Date.now() - 1000 * 60 * 30).toISOString() },
          { id: "demo_thread_settings", title: "Plan improved aiOS settings modal structure", messageCount: 3, createdAt: new Date(Date.now() - 1000 * 60 * 60 * 5).toISOString() },
          { id: "demo_thread_context", title: "Build thread context operations popup and slash command staging", messageCount: 6, createdAt: new Date(Date.now() - 1000 * 60 * 60 * 26).toISOString() },
          { id: "demo_thread_files", title: "Unify file explorer integrations for Google Drive, OneDrive, GitHub, and Notion", messageCount: 8, createdAt: new Date(Date.now() - 1000 * 60 * 60 * 24 * 6).toISOString() },
          { id: "demo_thread_speech", title: "Ship speech to text and microphone states in runner task input", messageCount: 5, createdAt: new Date(Date.now() - 1000 * 60 * 60 * 24 * 18).toISOString() }
        ];
  
        const DEMO_SCHEDULED_THREADS = [
          { id: "demo_scheduled_publish", title: "Publish runner SDK changelog", messageCount: 2, createdAt: new Date(Date.now() - 1000 * 60 * 60 * 3).toISOString(), nextRunAt: new Date(Date.now() + 1000 * 60 * 60 * 2).toISOString(), isScheduled: true },
          { id: "demo_scheduled_review", title: "Review open playground polish tasks", messageCount: 1, createdAt: new Date(Date.now() - 1000 * 60 * 60 * 8).toISOString(), nextRunAt: new Date(Date.now() + 1000 * 60 * 60 * 24).toISOString(), isScheduled: true }
        ];
  
        const SEEDED_DEMO_THREAD_ID = DEMO_RECENT_THREADS[0]?.id || "demo_thread_sidebar";
        const DEFAULT_DEMO_THREAD_ID = "";
  
        const DEMO_THREAD_EXPERIENCE_MAP = {
          demo_thread_sidebar: {
            prompt: "Redesign the ACP sidebar so power users can scan activity faster without losing density.",
            agent: "Efficient Agent",
            model: "gpt-5.4-mini",
            runtime: "claw",
            environment: "Default",
            summary: "I separated pinned, recent, and scheduled work, kept the collapsed rail fast, and made active runs more legible without sacrificing density.",
            events: [
              { kind: "user", title: "Prompt", body: "Redesign the ACP sidebar so power users can scan activity faster without losing density." },
              { kind: "reasoning", title: "Planning", body: "Review the current information hierarchy, then rebalance the shell around fast scanning, pinned context, and active work." },
              { kind: "tool", title: "bash", body: "rg -n \\\"sidebar-thread|sidebar-action|sidebar-rail\\\" apps/platform/server/index.mjs" },
              { kind: "reasoning", title: "Refining layout", body: "Preserve the current rhythm, but pull pinned work forward, simplify meta labels, and sharpen the active state." },
              { kind: "tool", title: "apply_patch", body: "Restructure the thread groups, strengthen selected states, and rebalance the right-side metadata." },
              { kind: "message", title: "Result", body: "Pinned, recent, and scheduled work now read as separate layers, active runs stand out immediately, and the rail still stays compact." },
            ],
            changes: [
              { path: "apps/platform/server/index.mjs", copy: "Rebalanced sidebar grouping and active-state treatment." },
              { path: "apps/platform/server/index.mjs", copy: "Tightened thread metadata so status and recency compete less." },
              { path: "apps/platform/server/index.mjs", copy: "Kept the collapsed rail behavior intact for fast switching." },
            ],
          },
          demo_thread_settings: {
            prompt: "Plan an enterprise settings flow for usage billing, BYOM inference, and seat-based team plans.",
            agent: "Strategist Agent",
            model: "gpt-5.4-mini",
            runtime: "claw",
            environment: "Default",
            summary: "The plan unifies Builder, Team, Business, and Enterprise, keeps product access on subscription, and meters infrastructure separately after included organization credits.",
            events: [
              { kind: "user", title: "Prompt", body: "Plan an enterprise settings flow for usage billing, BYOM inference, and seat-based team plans." },
              { kind: "reasoning", title: "Framing", body: "Separate product entitlement from metered usage, then place customer-hosted inference under Team, Business, and Enterprise." },
              { kind: "tool", title: "notes", body: "Review current billing controls, organization inference settings, and plan naming inside ACP." },
              { kind: "reasoning", title: "Designing the flow", body: "Plans should govern entitlement. Inference settings should govern endpoint configuration. Usage billing should remain a separate safety control." },
              { kind: "message", title: "Result", body: "Add organization-scoped inference controls, monthly spend limits, and a dedicated Inference section for endpoint health, model discovery, and encrypted credentials." },
            ],
            changes: [
              { path: "settings/plans", copy: "Added canonical organization plans with metered usage controls." },
              { path: "settings/inference", copy: "Defined BYOM endpoint inputs and connection testing." },
            ],
          },
          demo_thread_context: {
            prompt: "Design a thread context popup so users can attach files, repos, and previous work before starting a run.",
            agent: "Assistant",
            model: "gpt-5.4-mini",
            runtime: "claw",
            environment: "Default",
            summary: "The popup stages context before execution, so threads start with the right files, repos, and historical references already attached.",
            events: [
              { kind: "user", title: "Prompt", body: "Design a thread context popup so users can attach files, repos, and previous work before starting a run." },
              { kind: "reasoning", title: "Scope", body: "Keep the composer clean. Move context staging into a dedicated surface with visible chips and reusable presets." },
              { kind: "tool", title: "bash", body: "rg -n \\\"context|attachment|slash command\\\" apps/platform/server/index.mjs" },
              { kind: "message", title: "Result", body: "The popup groups repos, files, skills, and previous thread state so the run begins with deliberate context instead of ad-hoc prompts." },
            ],
            changes: [
              { path: "threads/context", copy: "Defined staged context groups for repos, attachments, and prior runs." },
              { path: "composer", copy: "Kept the thread composer focused by moving heavy context into a popup." },
            ],
          },
        };
  
        function getDemoThreadExperience(thread) {
          const normalizedId = typeof thread?.id === "string" ? thread.id.trim() : "";
          const mapped = normalizedId ? DEMO_THREAD_EXPERIENCE_MAP[normalizedId] : null;
          if (mapped) {
            return mapped;
          }
  
          const title = typeof thread?.title === "string" && thread.title.trim()
            ? thread.title.trim()
            : "Explore the Agentic Compute Platform demo";
  
          return {
            prompt: title,
            agent: "Efficient Agent",
            model: "gpt-5.4-mini",
            runtime: "claw",
            environment: "Default",
            summary: "This seeded thread shows how ACP combines threads, agents, environments, and tool execution in one workspace.",
            events: [
              { kind: "user", title: "Prompt", body: title },
              { kind: "reasoning", title: "Planning", body: "Review the workspace context, decide what to inspect first, and prepare the next tool action." },
              { kind: "tool", title: "bash", body: "pwd" },
              { kind: "message", title: "Result", body: "This demo thread is here to show the shell, logs, and orchestration flow before you sign in and run real work." },
            ],
            changes: [
              { path: "workspace/demo", copy: "Seeded walkthrough content for the platform preview." },
            ],
          };
        }
  
        function formatRelativeThreadTime(value) {
          if (!value) return "";
          const date = new Date(value);
          if (Number.isNaN(date.getTime())) return "";
          const diffMs = Math.max(0, Date.now() - date.getTime());
          const minuteMs = 60 * 1000;
          const hourMs = 60 * minuteMs;
          const dayMs = 24 * hourMs;
          const weekMs = 7 * dayMs;
          const monthMs = 30 * dayMs;
  
          if (diffMs < hourMs) return Math.max(1, Math.round(diffMs / minuteMs)) + " M";
          if (diffMs < dayMs) return Math.max(1, Math.round(diffMs / hourMs)) + " H";
          if (diffMs < weekMs) return Math.max(1, Math.round(diffMs / dayMs)) + " D";
          if (diffMs < monthMs) return Math.max(1, Math.round(diffMs / weekMs)) + " W";
          return Math.max(1, Math.round(diffMs / monthMs)) + " M";
        }
  
        function formatCompactThreadActivityTime(value) {
          if (!value) return "";
          const date = new Date(value);
          if (Number.isNaN(date.getTime())) return "";
          const diffMs = Math.max(0, Date.now() - date.getTime());
          const minuteMs = 60 * 1000;
          const hourMs = 60 * minuteMs;
          const dayMs = 24 * hourMs;
          const weekMs = 7 * dayMs;
          const monthMs = 30 * dayMs;
          const yearMs = 365 * dayMs;
  
          if (diffMs < hourMs) return Math.max(1, Math.round(diffMs / minuteMs)) + "M";
          if (diffMs < dayMs) return Math.max(1, Math.round(diffMs / hourMs)) + "H";
          if (diffMs < weekMs) return Math.max(1, Math.round(diffMs / dayMs)) + "D";
          if (diffMs < monthMs) return Math.max(1, Math.round(diffMs / weekMs)) + "W";
          if (diffMs < yearMs) {
            const months = Math.max(1, Math.round(diffMs / monthMs));
            if (months <= 9) {
              return months + "MO";
            }
          }
          return Math.max(1, Math.round(diffMs / yearMs)) + "Y";
        }
  
        function normalizeThreadItem(thread) {
          const rawId = typeof thread?.id === "string"
            ? thread.id
            : typeof thread?.id === "number"
              ? String(thread.id)
              : "";
          const rawTitle = typeof thread?.title === "string"
            ? thread.title
            : typeof thread?.title === "number"
              ? String(thread.title)
              : "";
          const rawStatus = typeof thread?.status === "string"
            ? thread.status
            : typeof thread?.status === "number"
              ? String(thread.status)
              : "";
          const rawAgentId = typeof thread?.agentId === "string"
            ? thread.agentId
            : typeof thread?.agentId === "number"
              ? String(thread.agentId)
              : "";
          const metadata = thread?.metadata && typeof thread.metadata === "object" && !Array.isArray(thread.metadata)
            ? thread.metadata
            : {};
          const runnerPlaygroundMetadata = metadata?.runnerPlayground && typeof metadata.runnerPlayground === "object" && !Array.isArray(metadata.runnerPlayground)
            ? metadata.runnerPlayground
            : {};
          const runnerTaskPreview = runnerPlaygroundMetadata?.taskPreview && typeof runnerPlaygroundMetadata.taskPreview === "object" && !Array.isArray(runnerPlaygroundMetadata.taskPreview)
            ? runnerPlaygroundMetadata.taskPreview
            : {};
          const runnerMissionControl = runnerPlaygroundMetadata?.missionControl && typeof runnerPlaygroundMetadata.missionControl === "object" && !Array.isArray(runnerPlaygroundMetadata.missionControl)
            ? runnerPlaygroundMetadata.missionControl
            : {};
          const threadProjectRecord = thread?.project && typeof thread.project === "object" && !Array.isArray(thread.project)
            ? thread.project
            : {};
          const threadEnvironmentRecord = thread?.environment && typeof thread.environment === "object" && !Array.isArray(thread.environment)
            ? thread.environment
            : thread?.computer && typeof thread.computer === "object" && !Array.isArray(thread.computer)
              ? thread.computer
              : {};
          const threadAgentRecord = thread?.agent && typeof thread.agent === "object" && !Array.isArray(thread.agent)
            ? thread.agent
            : {};
          const rawProjectId = typeof thread?.projectId === "string"
            ? thread.projectId
            : typeof thread?.project_id === "string"
              ? thread.project_id
              : typeof threadProjectRecord?.id === "string"
                ? threadProjectRecord.id
              : typeof metadata?.projectId === "string"
                ? metadata.projectId
                : typeof metadata?.project_id === "string"
                  ? metadata.project_id
                  : typeof runnerTaskPreview?.projectId === "string"
                    ? runnerTaskPreview.projectId
                    : typeof runnerMissionControl?.projectId === "string"
                      ? runnerMissionControl.projectId
                      : "";
          const rawProjectName = typeof thread?.projectName === "string"
            ? thread.projectName
            : typeof thread?.project_name === "string"
              ? thread.project_name
              : typeof threadProjectRecord?.name === "string"
                ? threadProjectRecord.name
              : typeof metadata?.projectName === "string"
                ? metadata.projectName
                : typeof metadata?.project_name === "string"
                  ? metadata.project_name
                  : typeof runnerTaskPreview?.projectName === "string"
                    ? runnerTaskPreview.projectName
                    : typeof runnerMissionControl?.projectName === "string"
                      ? runnerMissionControl.projectName
                      : "";
          const rawEnvironmentId = typeof thread?.environmentId === "string"
            ? thread.environmentId
            : typeof thread?.environment_id === "string"
              ? thread.environment_id
              : typeof threadEnvironmentRecord?.id === "string"
                ? threadEnvironmentRecord.id
              : typeof metadata?.environmentId === "string"
                ? metadata.environmentId
                : typeof metadata?.environment_id === "string"
                  ? metadata.environment_id
                  : typeof runnerPlaygroundMetadata?.environmentId === "string"
                    ? runnerPlaygroundMetadata.environmentId
                    : typeof runnerTaskPreview?.environmentId === "string"
                      ? runnerTaskPreview.environmentId
                      : "";
          const rawEnvironmentName = typeof thread?.environmentName === "string"
            ? thread.environmentName
            : typeof thread?.environment_name === "string"
              ? thread.environment_name
              : typeof thread?.computerName === "string"
                ? thread.computerName
                : typeof thread?.computer_name === "string"
                  ? thread.computer_name
                  : typeof threadEnvironmentRecord?.name === "string"
                    ? threadEnvironmentRecord.name
                  : typeof metadata?.environmentName === "string"
                    ? metadata.environmentName
                    : typeof metadata?.environment_name === "string"
                      ? metadata.environment_name
                      : typeof runnerPlaygroundMetadata?.environmentName === "string"
                        ? runnerPlaygroundMetadata.environmentName
                        : typeof runnerTaskPreview?.environmentName === "string"
                          ? runnerTaskPreview.environmentName
                          : "";
          const rawAgentName = typeof thread?.agentName === "string"
            ? thread.agentName
            : typeof thread?.agent_name === "string"
              ? thread.agent_name
              : typeof threadAgentRecord?.name === "string"
                ? threadAgentRecord.name
                : typeof threadAgentRecord?.label === "string"
                  ? threadAgentRecord.label
                  : typeof metadata?.agentName === "string"
                    ? metadata.agentName
                    : typeof metadata?.agent_name === "string"
                      ? metadata.agent_name
                      : typeof runnerPlaygroundMetadata?.agentName === "string"
                        ? runnerPlaygroundMetadata.agentName
                        : typeof runnerPlaygroundMetadata?.agent_name === "string"
                          ? runnerPlaygroundMetadata.agent_name
                          : typeof runnerTaskPreview?.agentName === "string"
                            ? runnerTaskPreview.agentName
                            : typeof runnerTaskPreview?.agent_name === "string"
                              ? runnerTaskPreview.agent_name
                              : "";
          const rawAgentPhotoUrl = typeof thread?.agentPhotoUrl === "string"
            ? thread.agentPhotoUrl
            : typeof thread?.agent_photo_url === "string"
              ? thread.agent_photo_url
              : typeof thread?.agentAvatarUrl === "string"
                ? thread.agentAvatarUrl
                : typeof thread?.agent_avatar_url === "string"
                  ? thread.agent_avatar_url
                  : typeof threadAgentRecord?.profilePhotoUrl === "string"
                    ? threadAgentRecord.profilePhotoUrl
                    : typeof threadAgentRecord?.photoUrl === "string"
                      ? threadAgentRecord.photoUrl
                      : typeof threadAgentRecord?.avatarUrl === "string"
                        ? threadAgentRecord.avatarUrl
                        : typeof metadata?.agentPhotoUrl === "string"
                          ? metadata.agentPhotoUrl
                          : typeof metadata?.agent_photo_url === "string"
                            ? metadata.agent_photo_url
                            : typeof metadata?.agentAvatarUrl === "string"
                              ? metadata.agentAvatarUrl
                              : typeof metadata?.agent_avatar_url === "string"
                                ? metadata.agent_avatar_url
                                : typeof runnerPlaygroundMetadata?.agentPhotoUrl === "string"
                                  ? runnerPlaygroundMetadata.agentPhotoUrl
                                  : typeof runnerPlaygroundMetadata?.agent_photo_url === "string"
                                    ? runnerPlaygroundMetadata.agent_photo_url
                                    : typeof runnerPlaygroundMetadata?.agentAvatarUrl === "string"
                                      ? runnerPlaygroundMetadata.agentAvatarUrl
                                      : typeof runnerPlaygroundMetadata?.agent_avatar_url === "string"
                                        ? runnerPlaygroundMetadata.agent_avatar_url
                                        : typeof runnerTaskPreview?.agentPhotoUrl === "string"
                                          ? runnerTaskPreview.agentPhotoUrl
                                          : typeof runnerTaskPreview?.agent_photo_url === "string"
                                            ? runnerTaskPreview.agent_photo_url
                                            : typeof runnerTaskPreview?.agentAvatarUrl === "string"
                                              ? runnerTaskPreview.agentAvatarUrl
                                              : typeof runnerTaskPreview?.agent_avatar_url === "string"
                                                ? runnerTaskPreview.agent_avatar_url
                                                : "";
          const rawCreatedAt = typeof thread?.createdAt === "string" ? thread.createdAt : "";
          const rawUpdatedAt = typeof thread?.updatedAt === "string" ? thread.updatedAt : "";
          const rawStartedAt = typeof thread?.startedAt === "string"
            ? thread.startedAt
            : typeof thread?.started_at === "string"
              ? thread.started_at
              : "";
          const rawCompletedAt = typeof thread?.completedAt === "string"
            ? thread.completedAt
            : typeof thread?.completed_at === "string"
              ? thread.completed_at
              : "";
          const rawFinishedAt = typeof thread?.finishedAt === "string"
            ? thread.finishedAt
            : typeof thread?.finished_at === "string"
              ? thread.finished_at
              : "";
          const rawEndedAt = typeof thread?.endedAt === "string"
            ? thread.endedAt
            : typeof thread?.ended_at === "string"
              ? thread.ended_at
              : "";
          const rawNextRunAt = typeof thread?.nextRunAt === "string"
            ? thread.nextRunAt
            : typeof thread?.scheduledTime === "string"
              ? thread.scheduledTime
              : "";
          const resolvedStatus = resolveThreadDisplayStatus(rawStatus, rawCompletedAt || rawFinishedAt || rawEndedAt);
          return {
            id: rawId || generateId("thread"),
            title: rawTitle || "Untitled thread",
            status: resolvedStatus,
            agentId: rawAgentId,
            agentName: rawAgentName,
            agentPhotoUrl: rawAgentPhotoUrl,
            agentAvatarUrl: rawAgentPhotoUrl,
            projectId: rawProjectId,
            projectName: rawProjectName,
            environmentId: rawEnvironmentId,
            environmentName: rawEnvironmentName,
            messageCount: Number.isFinite(thread.messageCount) ? thread.messageCount : 0,
            createdAt: rawCreatedAt,
            updatedAt: rawUpdatedAt,
            startedAt: rawStartedAt,
            completedAt: rawCompletedAt,
            finishedAt: rawFinishedAt,
            endedAt: rawEndedAt,
            nextRunAt: rawNextRunAt,
            isScheduled: Boolean(rawNextRunAt),
            totalCT: readSettingsComputeTokens(thread, "totalCT", "totalCost"),
            agentCT: readSettingsComputeTokens(thread, "agentCT", "agentCost"),
            environmentCT: readSettingsComputeTokens(thread, "environmentCT", "environmentCost"),
            totalCost: Number.isFinite(Number(thread?.totalCost)) ? Number(thread.totalCost) : 0,
            agentCost: Number.isFinite(Number(thread?.agentCost)) ? Number(thread.agentCost) : 0,
            environmentCost: Number.isFinite(Number(thread?.environmentCost)) ? Number(thread.environmentCost) : 0,
            inputTokens: Number.isFinite(Number(thread?.inputTokens)) ? Number(thread.inputTokens) : 0,
            outputTokens: Number.isFinite(Number(thread?.outputTokens)) ? Number(thread.outputTokens) : 0,
            cacheTokens: Number.isFinite(Number(thread?.cacheTokens)) ? Number(thread.cacheTokens) : 0,
            totalTokens: Number.isFinite(Number(thread?.totalTokens)) ? Number(thread.totalTokens) : 0,
            metadata,
            isPinned: Boolean(runnerPlaygroundMetadata.pinnedInSidebar),
            pinnedAt: typeof runnerPlaygroundMetadata.pinnedAt === "string" ? runnerPlaygroundMetadata.pinnedAt : "",
            positive: typeof thread?.positive === "string" ? thread.positive : "",
            negative: typeof thread?.negative === "string" ? thread.negative : "",
            ageLabel: typeof thread?.ageLabel === "string" ? thread.ageLabel : "",
            attachments: Array.isArray(thread?.attachments) ? thread.attachments : [],
          };
        }
  
        function getThreadTaskPreview(thread) {
          const metadata = thread?.metadata && typeof thread.metadata === "object" && !Array.isArray(thread.metadata)
            ? thread.metadata
            : null;
          const runnerPlaygroundMetadata = metadata?.runnerPlayground && typeof metadata.runnerPlayground === "object" && !Array.isArray(metadata.runnerPlayground)
            ? metadata.runnerPlayground
            : null;
          const taskPreview = runnerPlaygroundMetadata?.taskPreview && typeof runnerPlaygroundMetadata.taskPreview === "object" && !Array.isArray(runnerPlaygroundMetadata.taskPreview)
            ? runnerPlaygroundMetadata.taskPreview
            : null;
          return taskPreview?.taskId ? taskPreview : null;
        }
  
        function getThreadMissionControlMetadata(thread) {
          const metadata = thread?.metadata && typeof thread.metadata === "object" && !Array.isArray(thread.metadata)
            ? thread.metadata
            : null;
          const runnerPlaygroundMetadata = metadata?.runnerPlayground && typeof metadata.runnerPlayground === "object" && !Array.isArray(metadata.runnerPlayground)
            ? metadata.runnerPlayground
            : null;
          const missionControl = runnerPlaygroundMetadata?.missionControl && typeof runnerPlaygroundMetadata.missionControl === "object" && !Array.isArray(runnerPlaygroundMetadata.missionControl)
            ? runnerPlaygroundMetadata.missionControl
            : null;
          return missionControl?.source === "project_backlog_mission_control"
            ? missionControl
            : null;
        }
  
        function getPlaygroundThreadActorInfo(thread, agentsById = {}, fallbackName = "No agent") {
          const safeThread = normalizeThreadItem(thread);
          const threadMetadata = safeThread?.metadata && typeof safeThread.metadata === "object" && !Array.isArray(safeThread.metadata)
            ? safeThread.metadata
            : {};
          const runnerPlaygroundMetadata = threadMetadata?.runnerPlayground && typeof threadMetadata.runnerPlayground === "object" && !Array.isArray(threadMetadata.runnerPlayground)
            ? threadMetadata.runnerPlayground
            : {};
          const taskPreview = runnerPlaygroundMetadata?.taskPreview && typeof runnerPlaygroundMetadata.taskPreview === "object" && !Array.isArray(runnerPlaygroundMetadata.taskPreview)
            ? runnerPlaygroundMetadata.taskPreview
            : {};
          const agentId = String(
            safeThread?.agentId
            || safeThread?.agent?.id
            || safeThread?.metadata?.agentId
            || runnerPlaygroundMetadata?.agentId
            || taskPreview?.agentId
            || ""
          ).trim();
          const explicitAgentName = String(
            safeThread?.agentName
            || safeThread?.agent_name
            || threadMetadata?.agentName
            || threadMetadata?.agent_name
            || runnerPlaygroundMetadata?.agentName
            || runnerPlaygroundMetadata?.agent_name
            || taskPreview?.agentName
            || taskPreview?.agent_name
            || ""
          ).trim();
          const explicitAgentPhotoUrl = normalizeSessionPhotoUrl(
            safeThread?.agentPhotoUrl
            || safeThread?.agent_photo_url
            || safeThread?.agentAvatarUrl
            || safeThread?.agent_avatar_url
            || threadMetadata?.agentPhotoUrl
            || threadMetadata?.agent_photo_url
            || threadMetadata?.agentAvatarUrl
            || threadMetadata?.agent_avatar_url
            || runnerPlaygroundMetadata?.agentPhotoUrl
            || runnerPlaygroundMetadata?.agent_photo_url
            || runnerPlaygroundMetadata?.agentAvatarUrl
            || runnerPlaygroundMetadata?.agent_avatar_url
            || taskPreview?.agentPhotoUrl
            || taskPreview?.agent_photo_url
            || taskPreview?.agentAvatarUrl
            || taskPreview?.agent_avatar_url
            || ""
          );
          if (getThreadMissionControlMetadata(safeThread)) {
            return {
              id: agentId,
              name: "Mission Control Agent",
              photoUrl: explicitAgentPhotoUrl,
              kind: "mission-control",
            };
          }
  
          const agentRecord = agentId && agentsById && agentsById[agentId]
            ? agentsById[agentId]
            : null;
          if (agentRecord) {
            const agentPhotoUrl = typeof getPlaygroundAgentProfilePhotoUrl === "function"
              ? normalizeSessionPhotoUrl(getPlaygroundAgentProfilePhotoUrl(agentRecord))
              : "";
            return {
              id: agentId,
              name: agentRecord.name || agentRecord.label || explicitAgentName || agentId,
              photoUrl: agentPhotoUrl || explicitAgentPhotoUrl,
              kind: "agent",
            };
          }
  
          return {
            id: agentId,
            name: explicitAgentName || agentId || fallbackName,
            photoUrl: explicitAgentPhotoUrl,
            kind: agentId ? "agent" : "none",
          };
        }
  
        function getSidebarThreadTitleParts(thread) {
          const safeThread = normalizeThreadItem(thread);
          const safeThreadTitle = typeof safeThread.title === "string" && safeThread.title.trim()
            ? safeThread.title.trim()
            : "Untitled thread";
          const taskPreview = getThreadTaskPreview(safeThread);
          const previewTicketNumber = typeof taskPreview?.ticketNumber === "string" && taskPreview.ticketNumber.trim()
            ? taskPreview.ticketNumber.trim()
            : "";
          const prefixedTicketMatch = safeThreadTitle.match(/^((?:[A-Z]{1,10}-)?\\d{1,6})\\s+(.+)$/);
          const ticketNumber = previewTicketNumber || (prefixedTicketMatch ? prefixedTicketMatch[1].trim() : "");
          const displayThreadTitle = ticketNumber && safeThreadTitle.startsWith(ticketNumber)
            ? (safeThreadTitle.slice(ticketNumber.length).trimStart() || safeThreadTitle)
            : prefixedTicketMatch?.[2]?.trim() || safeThreadTitle;
  
          return {
            safeThread,
            taskPreview,
            taskTicketNumber: ticketNumber,
            displayThreadTitle,
          };
        }
  
        function renderPlaygroundThreadOverviewPersonAvatar(label, photoUrl = "") {
          const normalizedLabel = String(label || "").trim() || "User";
          const fallbackLabel = typeof getAccountInitials === "function"
            ? getAccountInitials(normalizedLabel)
            : normalizedLabel.charAt(0).toUpperCase() || "U";
          const normalizedPhotoUrl = typeof normalizeSessionPhotoUrl === "function"
            ? normalizeSessionPhotoUrl(photoUrl)
            : String(photoUrl || "").trim();
          return React.createElement("div", {
              className: "playground-agents-detail-thread-user-avatar",
              title: normalizedLabel,
              "aria-hidden": "true",
            },
            typeof canRenderAvatarImage === "function" && canRenderAvatarImage(normalizedPhotoUrl)
              ? React.createElement("img", {
                  className: "playground-agents-detail-thread-user-avatar-image",
                  src: normalizedPhotoUrl,
                  alt: fallbackLabel,
                })
              : React.createElement("span", { className: "playground-agents-detail-thread-user-avatar-fallback" }, fallbackLabel)
          );
        }
  
  
        function renderPlaygroundThreadOverviewTable({
          threads = [],
          rowOptions = {},
          tableOptions = {},
        } = {}) {
          const safeThreads = Array.isArray(threads) ? threads : [];
          const selectable = Boolean(rowOptions.selectable);
          const sortable = tableOptions.sortable !== false;
          const selectedIds = rowOptions.selectedIds instanceof Set
            ? rowOptions.selectedIds
            : new Set(Array.isArray(rowOptions.selectedIds) ? rowOptions.selectedIds.map((id) => String(id || "").trim()).filter(Boolean) : []);
          const readThreadContext = (thread) => {
            const titleParts = typeof getSidebarThreadTitleParts === "function"
              ? getSidebarThreadTitleParts(thread)
              : { safeThread: thread, displayThreadTitle: thread?.title || "Untitled thread" };
            return {
              safeThread: titleParts.safeThread || thread,
              displayThreadTitle: titleParts.displayThreadTitle || "Untitled thread",
              threadId: String(titleParts.safeThread?.id || thread?.id || "").trim(),
            };
          };
          const triggerThreadActions = (event, thread) => {
            const { safeThread, threadId } = readThreadContext(thread);
            if (!threadId) return;
            if (typeof rowOptions.onThreadActions === "function") {
              rowOptions.onThreadActions(event, threadId, safeThread, thread);
              return;
            }
            if (typeof rowOptions.onOpenThread === "function") rowOptions.onOpenThread(threadId, safeThread, thread);
          };
          const readThreadDateLabel = (thread) => {
            const safeThread = readThreadContext(thread).safeThread;
            return String(typeof rowOptions.getDateLabel === "function"
              ? rowOptions.getDateLabel(thread, safeThread)
              : (typeof formatThreadSearchTimestamp === "function"
                  ? formatThreadSearchTimestamp(typeof resolveThreadSortTimestamp === "function" ? resolveThreadSortTimestamp(safeThread) : (thread?.updatedAt || thread?.createdAt || ""))
                  : "")
            ).trim() || "—";
          };
          const readThreadSortTimestamp = (thread) => {
            const safeThread = readThreadContext(thread).safeThread;
            const value = typeof resolveThreadSortTimestamp === "function"
              ? resolveThreadSortTimestamp(safeThread)
              : (safeThread?.updatedAt || safeThread?.createdAt || thread?.updatedAt || thread?.createdAt || "");
            const timestamp = new Date(value || 0).getTime();
            return Number.isFinite(timestamp) ? timestamp : 0;
          };
          const dataTable = React.createElement(PlatformDataTable, {
            key: tableOptions.key,
            rows: safeThreads,
            getRowId: (thread) => readThreadContext(thread).threadId,
            ariaLabel: tableOptions.ariaLabel || "Threads",
            className: ["playground-thread-overview-platform-table", tableOptions.className || ""].filter(Boolean).join(" "),
            surface: tableOptions.surface || "plain",
            layout: tableOptions.layout || "content",
            variant: tableOptions.variant || "minimalistic-ui",
            sticky: Boolean(tableOptions.sticky),
            stickyTop: tableOptions.stickyTop,
            rowMinHeight: tableOptions.rowMinHeight,
            sorting: tableOptions.sorting,
            pagination: tableOptions.pagination === undefined ? false : tableOptions.pagination,
            toolbar: tableOptions.toolbar,
            loading: Boolean(tableOptions.loading),
            error: tableOptions.error || undefined,
            emptyState: tableOptions.emptyState || "No threads yet.",
            noResultsState: tableOptions.noResultsState || "No matching threads.",
            footer: tableOptions.footer,
            columns: [
              {
                id: "title",
                header: "Title",
                accessor: (thread) => readThreadContext(thread).displayThreadTitle,
                sortable,
                width: "minmax(180px, 1.7fr)",
                cell: ({ row: thread }) => React.createElement("div", { className: "playground-plugin-row-title" }, readThreadContext(thread).displayThreadTitle),
              },
              {
                id: "source",
                header: "Source",
                accessor: (thread) => String(typeof rowOptions.getSourceLabel === "function" ? rowOptions.getSourceLabel(thread, readThreadContext(thread).safeThread) : "").trim() || "Chat",
                width: "minmax(90px, 0.8fr)",
              },
              {
                id: "environment",
                header: "Environment",
                accessor: (thread) => String(typeof rowOptions.getEnvironmentLabel === "function" ? rowOptions.getEnvironmentLabel(thread, readThreadContext(thread).safeThread) : "").trim() || "Workspace",
                width: "minmax(110px, 1fr)",
                hideBelow: 720,
              },
              {
                id: "triggered-by",
                header: "Triggered by",
                accessor: (thread) => String(typeof rowOptions.getTriggeredByLabel === "function" ? rowOptions.getTriggeredByLabel(thread, readThreadContext(thread).safeThread) : "").trim() || "-",
                width: "minmax(120px, 1fr)",
                hideBelow: 900,
                cell: ({ row: thread }) => {
                  const safeThread = readThreadContext(thread).safeThread;
                  const label = String(typeof rowOptions.getTriggeredByLabel === "function" ? rowOptions.getTriggeredByLabel(thread, safeThread) : "").trim() || "-";
                  const avatarUrl = typeof rowOptions.getTriggeredByAvatarUrl === "function" ? rowOptions.getTriggeredByAvatarUrl(thread, safeThread) : "";
                  return label !== "-"
                    ? React.createElement("span", { className: "playground-agents-detail-thread-triggered-by" },
                        renderPlaygroundThreadOverviewPersonAvatar(label, avatarUrl),
                        React.createElement("span", { className: "playground-agents-detail-thread-triggered-by-label" }, label)
                      )
                    : "-";
                },
              },
              {
                id: "date",
                header: "Date",
                accessor: readThreadSortTimestamp,
                sortable,
                sortDescFirst: true,
                width: "minmax(100px, 0.85fr)",
                align: "end",
                cell: ({ row: thread }) => readThreadDateLabel(thread),
              },
            ],
            selection: selectable ? {
              enabled: true,
              value: selectedIds,
              ariaLabel: (thread) => "Select " + readThreadContext(thread).displayThreadTitle,
              onChange: ({ selectedIds: nextIds, reason }) => {
                if (reason === "visible") {
                  rowOptions.onToggleVisibleSelection?.();
                  return;
                }
                const changedId = Array.from(new Set([...selectedIds, ...nextIds])).find((id) => selectedIds.has(id) !== nextIds.has(id));
                if (changedId) {
                  const changedThread = safeThreads.find((thread) => readThreadContext(thread).threadId === changedId);
                  rowOptions.onToggleSelection?.(changedId, readThreadContext(changedThread).safeThread, changedThread);
                }
              },
            } : undefined,
            onRowActivate: (thread) => {
              const { safeThread, threadId } = readThreadContext(thread);
              if (threadId && typeof rowOptions.onOpenThread === "function") rowOptions.onOpenThread(threadId, safeThread, thread);
            },
            onRowActionTrigger: triggerThreadActions,
            onRowContextMenu: (event, thread) => {
              event.preventDefault();
              triggerThreadActions(event, thread);
            },
            isRowActionOpen: (thread) => Boolean(typeof rowOptions.isActionOpen === "function" && rowOptions.isActionOpen(readThreadContext(thread).threadId, readThreadContext(thread).safeThread, thread)),
            getRowAriaLabel: (thread) => "Open thread " + readThreadContext(thread).displayThreadTitle,
          });
          return rowOptions.toolbarContent
            ? React.createElement("div", { className: "playground-thread-overview-table-shell" },
                rowOptions.toolbarContent,
                dataTable
              )
            : dataTable;
        }
  
  ${METRONOME_SHELL_RUNTIME_SCRIPT}
  
        function normalizeThreadList(items) {
          const next = [];
          const seenIds = new Set();
  
          (Array.isArray(items) ? items : []).forEach((item) => {
            try {
              const normalizedThread = normalizeThreadItem(item);
              const normalizedId = typeof normalizedThread?.id === "string" ? normalizedThread.id.trim() : "";
              const threadId = normalizedId || generateId("thread");
              if (seenIds.has(threadId)) {
                return;
              }
              seenIds.add(threadId);
              next.push({
                ...normalizedThread,
                id: threadId,
              });
            } catch (error) {
              console.error("Failed to normalize thread item", error, item);
            }
          });
  
          return next;
        }
  
        function getThreadPinnedSortTimestamp(thread) {
          if (!thread?.pinnedAt) return 0;
          const timestamp = new Date(thread.pinnedAt).getTime();
          return Number.isFinite(timestamp) ? timestamp : 0;
        }
  
        function isRealThreadId(value) {
          const normalized = String(value || "").trim();
          return /^thread[_-]/.test(normalized);
        }
  
        function isPrivateThreadRecord(thread) {
          const metadata = thread?.metadata && typeof thread.metadata === "object" && !Array.isArray(thread.metadata)
            ? thread.metadata
            : {};
          const runnerPlayground = metadata?.runnerPlayground && typeof metadata.runnerPlayground === "object" && !Array.isArray(metadata.runnerPlayground)
            ? metadata.runnerPlayground
            : {};
          return Boolean(
            thread?.privateMode === true
            || thread?.isPrivate === true
            || thread?.temporary === true
            || metadata.privateMode === true
            || metadata.temporary === true
            || String(metadata.visibility || "").trim().toLowerCase() === "private"
            || runnerPlayground.privateMode === true
          );
        }
  
        function threadMetaLabel(thread) {
          if (thread.nextRunAt) {
            return "Scheduled";
          }
          if (typeof thread?.ageLabel === "string" && thread.ageLabel.trim()) {
            return thread.ageLabel.trim();
          }
          if (!isRealThreadId(thread?.id) && thread?.createdAt) {
            return formatRelativeThreadTime(thread.createdAt);
          }
          return "";
        }
  
        function formatPlaygroundFileSize(bytes) {
          if (!Number.isFinite(bytes) || bytes <= 0) return "-";
          if (bytes < 1024) return bytes + " B";
          if (bytes < 1024 * 1024) return (bytes / 1024).toFixed(1) + " KB";
          if (bytes < 1024 * 1024 * 1024) return (bytes / (1024 * 1024)).toFixed(1) + " MB";
          return (bytes / (1024 * 1024 * 1024)).toFixed(1) + " GB";
        }
  
        function formatPlaygroundFileDate(value) {
          if (!value) return "-";
          const date = new Date(value);
          if (Number.isNaN(date.getTime())) return "-";
  
          const diffMs = Math.max(0, Date.now() - date.getTime());
          const minuteMs = 60 * 1000;
          const hourMs = 60 * minuteMs;
          const dayMs = 24 * hourMs;
  
          if (diffMs < hourMs) {
            return Math.max(1, Math.round(diffMs / minuteMs)) + "m ago";
          }
          if (diffMs < dayMs) {
            return Math.max(1, Math.round(diffMs / hourMs)) + "h ago";
          }
          if (diffMs < 7 * dayMs) {
            return Math.max(1, Math.round(diffMs / dayMs)) + "d ago";
          }
          return date.toLocaleDateString();
        }
  
        function formatPlaygroundRelativeTime(value) {
          return formatPlaygroundFileDate(value);
        }
  
        function formatPlaygroundExactDate(value) {
          if (!value) return "—";
          const date = new Date(value);
          if (Number.isNaN(date.getTime())) return "—";
          return new Intl.DateTimeFormat("en-GB", {
            day: "2-digit",
            month: "2-digit",
            year: "numeric",
          }).format(date);
        }
  
        function formatPlaygroundFolderItemCount(entry) {
          const itemCount = Array.isArray(entry?.children) ? entry.children.length : 0;
          if (itemCount > 0 || entry?.hasChildren === false) {
            return itemCount + " items";
          }
          return "Contains items";
        }
  
        const PLAYGROUND_MASKED_SECRET_VALUE = "••••••••";
        const PLAYGROUND_ENVIRONMENT_DRAFT_ID = "__playground_new_environment__";
        const PLAYGROUND_SERVER_DRAFT_ID = "__playground_new_server__";
        const PLAYGROUND_DATABASE_DRAFT_ID = "__playground_new_database__";
        const PLAYGROUND_DEFAULT_FUNCTION_SOURCE_PATH = "index.js";
        const PLAYGROUND_DEFAULT_FUNCTION_PACKAGE_PATH = "package.json";
        const PLAYGROUND_DEFAULT_FUNCTION_SOURCE_CONTENT = [
          "export default async function handler(request) {",
          "  return {",
          "    status: 200,",
          "    headers: {",
          '      "content-type": "application/json; charset=utf-8",',
          "    },",
          "    body: JSON.stringify({",
          '      message: "Hello from Computer Agents",',
          "    }),",
          "  };",
          "}",
        ].join("\\n");
        const PLAYGROUND_DEFAULT_FUNCTION_PACKAGE_CONTENT = JSON.stringify({
          name: "computer-agents-function",
          version: "1.0.0",
          private: true,
          type: "module",
          scripts: {
            start: "node index.js",
          },
          engines: {
            node: ">=22",
          },
          dependencies: {
            "computer-agents": "latest",
            zod: "latest",
            nanoid: "latest",
            "date-fns": "latest",
          },
        }, null, 2) + "\\n";
        const PLAYGROUND_DEFAULT_WEB_APP_SOURCE_PATH = "index.html";
        const PLAYGROUND_DEFAULT_WEB_APP_PACKAGE_PATH = "package.json";
        const PLAYGROUND_DEFAULT_WEB_APP_SOURCE_CONTENT = [
          "<!doctype html>",
          '<html lang="en">',
          "  <head>",
          '    <meta charset="utf-8" />',
          '    <meta name="viewport" content="width=device-width, initial-scale=1" />',
          "    <title>Computer Agents Web App</title>",
          "    <style>",
          "      :root { color-scheme: dark; font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, sans-serif; }",
          "      * { box-sizing: border-box; }",
          "      body { margin: 0; min-height: 100vh; display: grid; place-items: center; background: #000; color: #fff; }",
          "      main { width: min(720px, calc(100vw - 40px)); padding: 48px; border: 1px solid rgba(255,255,255,.1); border-radius: 24px; background: rgba(255,255,255,.05); }",
          "      p { color: rgba(255,255,255,.68); line-height: 1.6; }",
          "      button { border: 0; border-radius: 999px; padding: 12px 18px; background: #fff; color: #000; font: inherit; cursor: pointer; }",
          "    </style>",
          "  </head>",
          "  <body>",
          "    <main>",
          "      <h1>Computer Agents Web App</h1>",
          "      <p>This starter page is ready to deploy. Add files, connect resources, and let your agents build from here.</p>",
          '      <button id="action-button">Run action</button>',
          "    </main>",
          "    <" + "script>",
          '      document.querySelector("#action-button")?.addEventListener("click", () => {',
          '        alert("Your web app is running.");',
          "      });",
          "    <" + "/script>",
          "  </body>",
          "</html>",
        ].join("\\n");
        const PLAYGROUND_DEFAULT_WEB_APP_PACKAGE_CONTENT = JSON.stringify({
          name: "computer-agents-web-app",
          version: "1.0.0",
          private: true,
          type: "module",
          scripts: {
            start: "serve -s . -l $" + "{PORT:-8080}",
          },
          dependencies: {
            serve: "latest",
          },
        }, null, 2) + "\\n";
        const PLAYGROUND_AGENT_DRAFT_ID = "__playground_new_agent__";
        const PLAYGROUND_SPARK_AGENT_PROFILE_URL = "/img/agent-profile-pics/spark.webp";
        const PLAYGROUND_FORGE_AGENT_PROFILE_URL = "/img/agent-profile-pics/forge.webp";
        const PLAYGROUND_FOUNDRY_AGENT_PROFILE_URL = "/img/agent-profile-pics/foundry.webp";
        const PLAYGROUND_AGENT_CREATOR_METADATA_ROLE = "agent_creator";
        const PLAYGROUND_AGENT_CREATOR_NAME = "Agent Creator";
        const PLAYGROUND_AGENT_CREATOR_DESCRIPTION = "Creates and improves ACP agent instructions from a requested use case.";
        const PLAYGROUND_AGENT_CREATOR_PROFILE_URL = "/img/agent-profile-pics/assistantastro-1.webp";
        const PLAYGROUND_MISSION_CONTROL_AGENT_METADATA_ROLE = "mission_control";
        const PLAYGROUND_MISSION_CONTROL_AGENT_NAME = "Mission Control";
        const PLAYGROUND_MISSION_CONTROL_AGENT_DESCRIPTION = "Runs project strategy, backlog planning, and delivery alignment for a selected project.";
        const PLAYGROUND_MISSION_CONTROL_AGENT_PROFILE_URL = "/img/agent-profile-pics/assistantastro-1.webp";
        const PLAYGROUND_DEFAULT_AVAILABLE_RUNTIMES = {
          python: ["3.9", "3.10", "3.11", "3.12", "3.13"],
          nodejs: ["18", "20", "22"],
          go: ["1.20", "1.21", "1.22", "1.23"],
          php: ["8.1", "8.2", "8.3"],
          java: ["11", "17", "21"],
          ruby: ["3.1", "3.2", "3.3"],
          rust: ["stable", "1.75", "1.76", "1.77"],
        };
        const PLAYGROUND_RUNTIME_DEFINITIONS = [
          { key: "python", label: "Python", defaultVersion: "last" },
          { key: "nodejs", label: "Node.js", defaultVersion: "last" },
          { key: "go", label: "Go", defaultVersion: "last" },
          { key: "java", label: "Java", defaultVersion: "last" },
          { key: "php", label: "PHP", defaultVersion: "last" },
          { key: "ruby", label: "Ruby", defaultVersion: "last" },
          { key: "rust", label: "Rust", defaultVersion: "first" },
        ];
        const PLAYGROUND_AGENT_MODEL_OPTIONS = [
          {
            id: "claude-opus-4-8",
            label: "Claude Opus 4.8",
            description: "Latest Anthropic flagship for complex reasoning, long-horizon coding, and high-autonomy agent work.",
            intelligence: "Highest",
            contextWindow: "1M",
            speed: "Medium",
          },
          {
            id: "claude-opus-4-7",
            label: "Claude Opus 4.7",
            description: "Latest Anthropic flagship for complex reasoning and agentic coding.",
            intelligence: "Highest",
            contextWindow: "1M",
            speed: "Medium",
          },
          {
            id: "claude-opus-4-6",
            label: "Claude Opus 4.6",
            description: "Previous-generation Anthropic flagship for complex reasoning.",
            intelligence: "Highest",
            contextWindow: "1M",
            speed: "Medium",
          },
          {
            id: "claude-sonnet-4-5",
            label: "Claude Sonnet 4.5",
            description: "Balanced flagship model for everyday coding work.",
            intelligence: "High",
            contextWindow: "200k",
            speed: "Fast",
          },
          {
            id: "claude-haiku-4-5",
            label: "Claude Haiku 4.5",
            description: "Fast and efficient for quick iterations.",
            intelligence: "Good",
            contextWindow: "200k",
            speed: "Very Fast",
          },
          {
            id: "gpt-5.5-pro",
            label: "GPT-5.5 Pro",
            description: "OpenAI highest-accuracy model on Clawcode for the hardest professional and agentic work.",
            intelligence: "Highest",
            contextWindow: "1M",
            speed: "Medium",
          },
          {
            id: "gpt-5.5",
            label: "GPT-5.5",
            description: "OpenAI frontier model on Clawcode for coding, professional work, and long-context agents.",
            intelligence: "Highest",
            contextWindow: "1M",
            speed: "Fast",
          },
          {
            id: "gpt-5.4",
            label: "GPT-5.4",
            description: "OpenAI flagship model running through Clawcode for advanced coding and planning.",
            intelligence: "Highest",
            contextWindow: "1M",
            speed: "Fast",
          },
          {
            id: "gpt-5.4-mini",
            label: "GPT-5.4 mini",
            description: "OpenAI mini model on Clawcode for coding, subagents, and computer use.",
            intelligence: "High",
            contextWindow: "400k",
            speed: "Fast",
          },
          {
            id: "gpt-5.4-nano",
            label: "GPT-5.4 nano",
            description: "OpenAI nano model on Clawcode for lightweight, high-volume workflows.",
            intelligence: "Good",
            contextWindow: "400k",
            speed: "Very Fast",
          },
          {
            id: "grok-4.5",
            label: "Grok 4.5",
            description: "xAI frontier model for coding, agentic tasks, and knowledge work.",
            intelligence: "Highest",
            contextWindow: "500k",
            speed: "Fast",
            providerType: "xai",
          },
          {
            id: "gemini-3-flash",
            label: "Gemini 3 Flash",
            description: "Fast default model for broad agent execution.",
            intelligence: "Good",
            contextWindow: "1M",
            speed: "Very Fast",
          },
          {
            id: "gemini-3-1-flash",
            label: "Gemini 3.1 Flash",
            description: "Fast Gemini tier that currently routes through the same runtime as Gemini 3 Flash.",
            intelligence: "Good",
            contextWindow: "1M",
            speed: "Very Fast",
          },
          {
            id: "gemini-3-1-pro",
            label: "Gemini 3.1 Pro",
            description: "Long-context reasoning for deeper planning.",
            intelligence: "High",
            contextWindow: "1M",
            speed: "Fast",
          },
          {
            id: "deepseek-v4-pro",
            label: "DeepSeek V4 Pro",
            description: "DeepSeek flagship on Clawcode for agentic coding and long-context reasoning.",
            intelligence: "High",
            contextWindow: "1M",
            speed: "Fast",
          },
          {
            id: "deepseek-v4-flash",
            label: "DeepSeek V4 Flash",
            description: "Fast DeepSeek V4 model on Clawcode for efficient agent execution.",
            intelligence: "High",
            contextWindow: "1M",
            speed: "Very Fast",
          },
          {
            id: "minimax-m3",
            label: "MiniMax M3",
            description: "MiniMax long-context model via Cloudflare for efficient coding, tool use, and digital work.",
            intelligence: "High",
            contextWindow: "1M",
            speed: "Fast",
            providerType: "minimax",
          },
          {
            id: "kimi-k2.6",
            label: "Kimi K2.6",
            description: "Moonshot flagship via Cloudflare Workers AI running on Clawcode for long-horizon coding.",
            intelligence: "High",
            contextWindow: "262k",
            speed: "Fast",
          },
          {
            id: "kimi-k2.7-code",
            label: "Kimi K2.7 Code",
            description: "Moonshot coding model via Cloudflare Workers AI for long-horizon software engineering.",
            intelligence: "High",
            contextWindow: "262k",
            speed: "Fast",
            providerType: "kimi",
          },
          {
            id: "glm-5.2",
            label: "ZAI GLM 5.2",
            description: "Z.ai flagship agentic coding model via Cloudflare Workers AI for software engineering and planning.",
            intelligence: "Highest",
            contextWindow: "262k",
            speed: "Very Fast",
            providerType: "zai",
          },
          {
            id: "qwen3.5-397b-a17b",
            label: "Qwen 3.5 397B A17B",
            description: "Alibaba Qwen mixture-of-experts model via Cloudflare AI for reasoning, coding, and multimodal agent work.",
            intelligence: "High",
            contextWindow: "262k",
            speed: "Fast",
            providerType: "qwen",
          },
        ];
        const PLAYGROUND_AGENT_DEEP_RESEARCH_MODEL_OPTIONS = [
          {
            id: "gemini-3-flash-preview",
            label: "Gemini 3.1 Flash",
            description: "Faster deep research path tuned for speed and lower cost.",
          },
          {
            id: "gemini-3-pro-preview",
            label: "Gemini 3.1 Pro",
            description: "Higher-depth deep research path tuned for stronger coverage.",
          },
        ];
        const PLAYGROUND_SKILL_IMAGE_MODEL_OPTIONS = [
          {
            id: "gpt-image-2",
            label: "GPT Image 2",
            provider: "OpenAI",
            description: "Highest-fidelity OpenAI image model with USD-credit billing by output quality.",
            pricing: {
              textInputUsdPerMillion: 5,
              imageInputUsdPerMillion: 8,
              imageOutputUsdPerMillion: 30,
            },
            qualityOutputTokens: {
              low: 272,
              medium: 1056,
              high: 4160,
            },
          },
          {
            id: "gemini-3.1-flash-image-preview",
            label: "Gemini 3.1 Flash Image",
            provider: "Google DeepMind",
            description: "Fast multimodal image generation and editing preview with a flat USD-credit estimate per image.",
            computeTokensPerImage: 7,
          },
        ];
        const PLAYGROUND_SKILL_IMAGE_QUALITY_OPTIONS = [
          {
            id: "low",
            label: "Low",
            description: "Fast draft output",
            squareOutputTokens: 272,
          },
          {
            id: "medium",
            label: "Medium",
            description: "Balanced default quality",
            squareOutputTokens: 1056,
          },
          {
            id: "high",
            label: "High",
            description: "Highest detail output",
            squareOutputTokens: 4160,
          },
        ];
        const PLAYGROUND_SKILL_VIDEO_MODEL_OPTIONS = [
          {
            id: "seedance-2.0-fast",
            label: "Seedance 2.0 Fast",
            provider: "ByteDance",
            description: "Fast default video generation for short clips and quick iterations.",
          },
          {
            id: "seedance-2.0",
            label: "Seedance 2.0",
            provider: "ByteDance",
            description: "Higher-quality Seedance video generation with reference media support.",
          },
          {
            id: "grok-imagine-video",
            label: "Grok Imagine Video",
            provider: "xAI",
            description: "Alternative video model for imaginative motion and stylized clips.",
          },
        ];
  
        function normalizePlaygroundVideoGenerationModelId(value) {
          const normalized = String(value || "").trim().toLowerCase();
          if (normalized === "seedance-2.0" || normalized === "seedance-2" || normalized === "bytedance/seedance-2.0") {
            return "seedance-2.0";
          }
          if (normalized === "grok" || normalized === "grok-imagine" || normalized === "grok-imagine-video") {
            return "grok-imagine-video";
          }
          return "seedance-2.0-fast";
        }
  
        function getPlaygroundVideoGenerationModelMeta(modelId) {
          const normalizedModelId = normalizePlaygroundVideoGenerationModelId(modelId);
          return PLAYGROUND_SKILL_VIDEO_MODEL_OPTIONS.find((option) => option.id === normalizedModelId)
            || PLAYGROUND_SKILL_VIDEO_MODEL_OPTIONS[0];
        }
  
        function getPlaygroundImageGenerationQualityMeta(qualityId) {
          return PLAYGROUND_SKILL_IMAGE_QUALITY_OPTIONS.find((option) => option.id === qualityId)
            || PLAYGROUND_SKILL_IMAGE_QUALITY_OPTIONS.find((option) => option.id === "medium")
            || PLAYGROUND_SKILL_IMAGE_QUALITY_OPTIONS[0];
        }
  
        function getPlaygroundImageGenerationModelMeta(modelId) {
          return PLAYGROUND_SKILL_IMAGE_MODEL_OPTIONS.find((option) => option.id === modelId)
            || PLAYGROUND_SKILL_IMAGE_MODEL_OPTIONS[0];
        }
  
        function getPlaygroundImageGenerationComputeTokensPerImage(modelId, qualityId) {
          const modelMeta = getPlaygroundImageGenerationModelMeta(modelId);
          const qualityMeta = getPlaygroundImageGenerationQualityMeta(qualityId);
          const directCost = Number(modelMeta?.computeTokensPerImage);
          if (Number.isFinite(directCost) && directCost > 0) {
            return Math.max(1, Math.round(directCost));
          }
  
          const qualityOutputTokens = modelMeta?.qualityOutputTokens && typeof modelMeta.qualityOutputTokens === "object"
            ? Number(modelMeta.qualityOutputTokens[qualityMeta.id])
            : Number(qualityMeta?.squareOutputTokens);
          const outputPricePerMillion = Number(modelMeta?.pricing?.imageOutputUsdPerMillion);
          if (Number.isFinite(qualityOutputTokens) && qualityOutputTokens > 0 && Number.isFinite(outputPricePerMillion) && outputPricePerMillion > 0) {
            return Math.max(1, settingsDollarsToComputeTokens((qualityOutputTokens / 1_000_000) * outputPricePerMillion));
          }
  
          return 1;
        }
  
        function getDemoImageGenerationSkillDefaults(config = readDemoSettingsPlatformConfig()) {
          const normalizedConfig = normalizeDemoSettingsSkillsConfig(config?.skills);
          return {
            deepResearch: {
              model: normalizedConfig.deepResearchModel,
            },
            imageGeneration: {
              model: normalizedConfig.imageGenerationModel,
              quality: normalizedConfig.imageGenerationQuality,
              computeTokensPerImage: getPlaygroundImageGenerationComputeTokensPerImage(
                normalizedConfig.imageGenerationModel,
                normalizedConfig.imageGenerationQuality
              ),
            },
            videoGeneration: {
              model: normalizedConfig.videoGenerationModel,
            },
          };
        }
  
        function applyDemoSkillDefaultsToEnabledSkillsPayload(payload) {
          const target = payload && typeof payload === "object" ? payload : {};
          const skillDefaults = getDemoImageGenerationSkillDefaults();
          if (target.imageGeneration && skillDefaults.imageGeneration) {
            target.imageGenerationModel = skillDefaults.imageGeneration.model;
            target.imageGenerationQuality = skillDefaults.imageGeneration.quality;
            target.imageGenerationComputeTokensPerImage = skillDefaults.imageGeneration.computeTokensPerImage;
            target.imageGenerationConfig = skillDefaults.imageGeneration;
          }
          if (target.videoGeneration && skillDefaults.videoGeneration) {
            target.videoGenerationModel = skillDefaults.videoGeneration.model;
            target.videoGenerationConfig = skillDefaults.videoGeneration;
          }
          if ((target.deepResearch || target.research) && skillDefaults.deepResearch) {
            target.deepResearchModel = skillDefaults.deepResearch.model;
            target.deepResearchConfig = skillDefaults.deepResearch;
          }
          return target;
        }
  
        const PLAYGROUND_AGENT_TEAM_EXECUTION_MODE = "claude_subagents_v1";
        const PLAYGROUND_AGENT_EMAIL_DOMAIN = "agent.computer-agents.com";
        const PLAYGROUND_AGENT_PROFILE_PRESET_OPTIONS = [
          { id: "spark", label: "Spark", url: PLAYGROUND_SPARK_AGENT_PROFILE_URL },
          { id: "forge", label: "Forge", url: PLAYGROUND_FORGE_AGENT_PROFILE_URL },
          { id: "foundry", label: "Foundry", url: PLAYGROUND_FOUNDRY_AGENT_PROFILE_URL },
          { id: "assistantastro", label: "Assistant Astro", url: "/img/agent-profile-pics/assistantastro-1.webp" },
          { id: "devastro", label: "Developer Astro", url: "/img/agent-profile-pics/devastro.webp" },
          { id: "researchastro", label: "Research Astro", url: "/img/agent-profile-pics/researchastro.webp" },
          { id: "blueastro", label: "Blue Astro", url: "/img/agent-profile-pics/blueastro.webp" },
          { id: "orangeastro", label: "Orange Astro", url: "/img/agent-profile-pics/orangeastro.webp" },
          { id: "suitastro", label: "Suit Astro", url: "/img/agent-profile-pics/suitastro.webp" },
        ];
        const PLAYGROUND_AGENTS_SHELL_BACKGROUND = "#000000";
        const PLAYGROUND_AGENT_SKILL_OPTIONS = [
          {
            id: "image_generation",
            label: "Image Generation",
            description: "Generate and edit images with configured image models.",
          },
          {
            id: "video_generation",
            label: "Video Generation",
            description: "Generate short videos with configured video models.",
          },
          {
            id: "frontend_design",
            label: "Hallmark Frontend Design",
            description: "Use Hallmark by default for websites, web apps, landing pages, audits, and redesigns.",
          },
          {
            id: "deep_research",
            label: "Deep Research",
            description: "Enable longer-form research runs with a dedicated research model.",
          },
          {
            id: "memory",
            label: "Memory",
            description: "Allow the agent to reuse persistent memory context when available.",
          },
          {
            id: "task_management",
            label: "Task Management",
            description: "Let the agent create, organize, and comment on planning tasks and projects.",
          },
          {
            id: "computer_agents",
            label: "Computer Agents",
            description: "Inspect and manage agents, environments, skills, and threads from inside the run.",
          },
          {
            id: "app_platform",
            label: "App Platform",
            description: "Create, connect, deploy, and debug web apps, functions, databases, and auth resources.",
          },
          {
            id: "email",
            label: "Email",
            description: "Search, read, summarize, and send email through the connected Gmail account.",
          },
        ];
        const PLAYGROUND_RUNNER_SKILL_ID_ALIASES = {
          imageGeneration: "image_generation",
          image_generation: "image_generation",
          "image-generation": "image_generation",
          videoGeneration: "video_generation",
          video_generation: "video_generation",
          "video-generation": "video_generation",
          webSearch: "web_search",
          web_search: "web_search",
          "web-search": "web_search",
          frontendDesign: "frontend_design",
          frontend_design: "frontend_design",
          "frontend-design": "frontend_design",
          hallmark: "frontend_design",
          hallmarkFrontendDesign: "frontend_design",
          hallmark_frontend_design: "frontend_design",
          "hallmark-frontend-design": "frontend_design",
          deepResearch: "deep_research",
          deep_research: "deep_research",
          "deep-research": "deep_research",
          research: "deep_research",
          pdf: "pdf",
          pdf_processing: "pdf",
          "pdf-processing": "pdf",
          pptx: "pptx",
          powerpoint: "pptx",
          "powerpoint-pptx": "pptx",
          memory: "memory",
          taskManagement: "task_management",
          task_management: "task_management",
          "task-management": "task_management",
          computerAgents: "computer_agents",
          computer_agents: "computer_agents",
          "computer-agents": "computer_agents",
          appPlatform: "app_platform",
          app_platform: "app_platform",
          "app-platform": "app_platform",
          gmail: "email",
          mail: "email",
          email: "email",
        };
        const PLAYGROUND_RUNNER_ENABLED_SKILLS_STORAGE_KEY = "tb_runner_chat_enabled_skills_v3:runner-web-sdk-demo";
        const PLAYGROUND_DEFAULT_ENABLED_SKILL_IDS = [
          "image_generation",
          "video_generation",
          "web_search",
          "deep_research",
          "browser",
          "memory",
          "task_management",
          "computer_agents",
          "email",
        ];
  
        function dedupePlaygroundAgentIds(ids) {
          const next = [];
          const seen = new Set();
          (Array.isArray(ids) ? ids : []).forEach((value) => {
            const normalized = String(value || "").trim();
            if (!normalized || seen.has(normalized)) return;
            seen.add(normalized);
            next.push(normalized);
          });
          return next;
        }
  
        function getPlaygroundAgentTeamMetadata(metadata) {
          if (!metadata || typeof metadata !== "object" || Array.isArray(metadata)) {
            return null;
          }
  
          const kind = typeof metadata.kind === "string" ? metadata.kind : "";
          const team = metadata.team && typeof metadata.team === "object" && !Array.isArray(metadata.team)
            ? metadata.team
            : null;
  
          if (kind !== "team" || !team) {
            return null;
          }
  
          const orchestratorAgentId = typeof team.orchestratorAgentId === "string"
            ? team.orchestratorAgentId.trim()
            : "";
          const subagentIds = dedupePlaygroundAgentIds(team.subagentIds);
  
          if (!orchestratorAgentId) {
            return null;
          }
  
          return {
            kind: "team",
            executionMode: typeof metadata.executionMode === "string" && metadata.executionMode.trim()
              ? metadata.executionMode
              : PLAYGROUND_AGENT_TEAM_EXECUTION_MODE,
            team: {
              version: 1,
              orchestratorAgentId,
              subagentIds,
            },
          };
        }
  
        function getPlaygroundAgentProfileMetadata(metadata) {
          if (!metadata || typeof metadata !== "object" || Array.isArray(metadata)) {
            return null;
          }
  
          const profile = metadata.profile && typeof metadata.profile === "object" && !Array.isArray(metadata.profile)
            ? metadata.profile
            : null;
          if (!profile) {
            return null;
          }
  
          const email = typeof profile.email === "string" ? profile.email.trim() : "";
          const photoURL = normalizeSessionPhotoUrl(
            profile.photoURL
            || profile.photoUrl
            || profile.avatarUrl
            || profile.avatarURL
            || profile.avatar
            || profile.picture
            || ""
          );
  
          if (!email && !photoURL) {
            return null;
          }
  
          return {
            email,
            photoURL,
          };
        }
  
        function slugifyPlaygroundAgentEmailLocalPart(value) {
          const normalized = String(value || "")
            .trim()
            .toLowerCase()
            .replace(/[^a-z0-9]+/g, "-")
            .replace(/^-+|-+$/g, "");
          return normalized || "agent";
        }
  
        function isPlaygroundGeneratedAgentEmailAddress(value) {
          const normalizedEmail = String(value || "").trim().toLowerCase();
          const suffix = "@" + PLAYGROUND_AGENT_EMAIL_DOMAIN.toLowerCase();
          return Boolean(normalizedEmail && normalizedEmail.endsWith(suffix));
        }
  
        function getPlaygroundAgentCreatorRole(agent) {
          const metadata = agent?.metadata && typeof agent.metadata === "object" && !Array.isArray(agent.metadata)
            ? agent.metadata
            : null;
          if (!metadata) {
            return "";
          }
  
          const runnerPlaygroundMetadata = metadata.runnerPlayground && typeof metadata.runnerPlayground === "object" && !Array.isArray(metadata.runnerPlayground)
            ? metadata.runnerPlayground
            : null;
          return String(
            runnerPlaygroundMetadata?.role
            || runnerPlaygroundMetadata?.agentRole
            || metadata.agentRole
            || ""
          ).trim().toLowerCase();
        }
  
        function isPlaygroundAgentCreatorAgent(agent) {
          if (!agent || typeof agent !== "object") {
            return false;
          }
  
          if (getPlaygroundAgentCreatorRole(agent) === PLAYGROUND_AGENT_CREATOR_METADATA_ROLE) {
            return true;
          }
  
          const normalizedName = String(agent.name || "").trim().toLowerCase();
          const normalizedInstructions = String(agent.instructions || "").trim().toLowerCase();
          const enabledSkills = normalizePlaygroundEnabledSkillIds(agent.enabledSkills);
          return normalizedName === PLAYGROUND_AGENT_CREATOR_NAME.toLowerCase()
            && enabledSkills.includes("computer_agents")
            && normalizedInstructions.includes("internal agent creator");
        }
  
        function isPlaygroundMissionControlAgent(agent) {
          if (!agent || typeof agent !== "object") {
            return false;
          }
  
          if (getPlaygroundAgentCreatorRole(agent) === PLAYGROUND_MISSION_CONTROL_AGENT_METADATA_ROLE) {
            return true;
          }
  
          const normalizedName = String(agent.name || "").trim().toLowerCase();
          const normalizedInstructions = String(agent.instructions || "").trim().toLowerCase();
          const enabledSkills = normalizePlaygroundEnabledSkillIds(agent.enabledSkills);
          return normalizedName === PLAYGROUND_MISSION_CONTROL_AGENT_NAME.toLowerCase()
            && enabledSkills.includes("task_management")
            && enabledSkills.includes("computer_agents")
            && normalizedInstructions.includes("internal mission control");
        }
  
        function isPlaygroundDeveloperAgent(agent) {
          const normalizedName = String(agent?.name || "").trim().toLowerCase();
          const normalizedId = String(agent?.id || "").trim().toLowerCase();
          return normalizedName === "forge"
            || normalizedName === "developer"
            || normalizedId === "agent_default"
            || normalizedId === "agent-default"
            || (normalizedId.startsWith("agent-default-") && (normalizedName.includes("forge") || normalizedName.includes("developer")));
        }
  
        function isPlaygroundResearcherAgent(agent) {
          const normalizedName = String(agent?.name || "").trim().toLowerCase();
          const normalizedId = String(agent?.id || "").trim().toLowerCase();
          return normalizedName === "foundry"
            || normalizedName === "researcher agent"
            || normalizedName === "researcher"
            || normalizedName === "research agent"
            || normalizedId === "agent_research"
            || normalizedId === "agent-research"
            || normalizedId.startsWith("agent-research-");
        }
  
        function isPlaygroundAssistantAgent(agent) {
          const normalizedName = String(agent?.name || "").trim().toLowerCase();
          const normalizedId = String(agent?.id || "").trim().toLowerCase();
          return normalizedName === "spark"
            || normalizedName === "assistant"
            || normalizedName === "default"
            || normalizedName === "default agent"
            || normalizedId === "agent_assistant"
            || normalizedId === "agent-assistant"
            || normalizedId.startsWith("agent-assistant-");
        }
  
        function isPlaygroundDefaultAgentConfigurationLocked(agent) {
          if (!agent || getPlaygroundAgentListMode(agent) === "teams" || agent?.agentType === "team") {
            return false;
          }
          const normalizedName = String(agent?.name || "").trim().toLowerCase();
          const normalizedId = String(agent?.id || "").trim().toLowerCase();
          const hasDefaultMarker = agent?.isDefault === true || agent?.isSystem === true;
          const hasDefaultIdentity = isPlaygroundDeveloperAgent(agent)
            || isPlaygroundResearcherAgent(agent)
            || isPlaygroundAssistantAgent(agent);
          const hasBuiltinName = normalizedName === "forge" || normalizedName === "spark" || normalizedName === "foundry";
          const hasBuiltinId = normalizedId === "agent_default"
            || normalizedId === "agent-default"
            || normalizedId.startsWith("agent-default-")
            || normalizedId === "agent_research"
            || normalizedId === "agent-research"
            || normalizedId.startsWith("agent-research-")
            || normalizedId === "agent_assistant"
            || normalizedId === "agent-assistant"
            || normalizedId.startsWith("agent-assistant-");
          return hasDefaultIdentity && (hasDefaultMarker || hasBuiltinName || hasBuiltinId);
        }
  
        function isPlaygroundFreePlanLockedComposerAgent(agent) {
          return isPlaygroundDeveloperAgent(agent) || isPlaygroundResearcherAgent(agent) || getPlaygroundAgentListMode(agent) === "teams";
        }
  
        function renderPlaygroundAgentUpgradeModal({
          isOpen,
          titleId = "playground-agent-upgrade-title",
          onClose,
          onCheckout,
          checkoutLoading = false,
          checkoutDisabled = false,
        } = {}) {
          if (!isOpen) {
            return null;
          }
  
          const features = [
            { icon: Bot, title: "Forge and Foundry", copy: "Use higher-capacity default agents for implementation, synthesis, and reasoning-heavy work." },
            { icon: Plus, title: "Custom agents", copy: "Create specialized agents with their own instructions, skills, permissions, and models." },
            { icon: UsersRound, title: "Agent Squads", copy: "Coordinate fixed squads with orchestrators and subagents for repeatable workflows." },
            { icon: Brain, title: "Premium models", copy: "Access the managed model catalog beyond Spark, including long-context and frontier models." },
            { icon: Sparkles, title: "Credits included", copy: "Start with $5.00 in monthly usage credits and organization budget controls." },
            { icon: Key, title: "API access", copy: "Use Computer Agents APIs and budget controls for production workflows." },
          ];
  
          return React.createElement(PlatformModalBackdrop, {
              className: "playground-calendar-upgrade-backdrop",
              role: "dialog",
              "aria-modal": "true",
              "aria-labelledby": titleId,
            },
            React.createElement("button", {
              type: "button",
              className: "playground-files-header-icon-button is-plain playground-calendar-upgrade-close",
              onClick: onClose,
              "aria-label": "Close upgrade modal",
              disabled: checkoutLoading,
            }, React.createElement(X, { width: 18, height: 18, strokeWidth: 1.8 })),
            React.createElement("div", { className: "playground-calendar-upgrade-shell" },
              React.createElement("h2", {
                id: titleId,
                className: "playground-calendar-upgrade-headline",
              },
                "Unlock the full platform with ",
                React.createElement("span", { className: "playground-calendar-upgrade-headline-price" }, "Builder")
              ),
              React.createElement("div", { className: "playground-calendar-upgrade-pill" }, "Custom agents, projects, workflows, resources, and API access"),
              React.createElement(PlatformModalSurface, {
                  className: "playground-calendar-upgrade-modal",
                  role: "document",
                },
                React.createElement("div", { className: "playground-calendar-upgrade-modal-top" },
                  React.createElement("div", { className: "playground-calendar-upgrade-modal-header" },
                    React.createElement("div", { className: "playground-calendar-upgrade-modal-title" }, "Builder"),
                    React.createElement("div", { className: "playground-calendar-upgrade-modal-offer" }, "$5 usage credit included")
                  ),
                  React.createElement("div", { className: "playground-calendar-upgrade-price-row" },
                    React.createElement("span", { className: "playground-calendar-upgrade-price-new" }, "$24"),
                    React.createElement("span", null, " / month")
                  ),
                  React.createElement("p", { className: "playground-calendar-upgrade-modal-copy" },
                    "Use Spark on Sandbox. Upgrade to create custom agents and operate projects, workflows, and developer resources."
                  ),
                  React.createElement("button", {
                    type: "button",
                    className: "playground-calendar-upgrade-modal-button",
                    onClick: onCheckout,
                    disabled: checkoutLoading || checkoutDisabled,
                  }, checkoutLoading ? "Opening checkout..." : "Upgrade to Builder")
                ),
                React.createElement("div", { className: "playground-calendar-upgrade-feature-list" },
                  features.map((feature) => {
                    const Icon = feature.icon;
                    return React.createElement("div", {
                        key: feature.title,
                        className: "playground-calendar-upgrade-modal-feature",
                      },
                      React.createElement("span", { className: "playground-calendar-upgrade-modal-feature-icon" },
                        React.createElement(Icon, { width: 16, height: 16, strokeWidth: 1.8 })
                      ),
                      React.createElement("div", null,
                        React.createElement("div", null, feature.title),
                        React.createElement("small", null, feature.copy)
                      )
                    );
                  })
                )
              )
            )
          );
        }
  
        function isPlaygroundDefaultNamedAgent(agent) {
          const normalizedName = String(agent?.name || "").trim().toLowerCase();
          return normalizedName === "default" || normalizedName === "default agent" || agent?.isDefault === true;
        }
  
        function getPlaygroundSystemAgentSortRank(agent) {
          if (isPlaygroundAssistantAgent(agent)) {
            return 0;
          }
          if (isPlaygroundDeveloperAgent(agent)) {
            return 1;
          }
          if (isPlaygroundResearcherAgent(agent)) {
            return 2;
          }
          return 10;
        }
  
        function getPlaygroundPreferredDefaultAgent(agents) {
          const normalizedAgents = Array.isArray(agents) ? agents.filter(Boolean) : [];
          if (normalizedAgents.length === 0) {
            return null;
          }
  
          const singleAgents = normalizedAgents.filter((agent) => getPlaygroundAgentListMode(agent) !== "teams");
          const candidateAgents = singleAgents.length > 0 ? singleAgents : normalizedAgents;
  
          const explicitAssistant = candidateAgents.find((agent) => isPlaygroundAssistantAgent(agent));
          if (explicitAssistant) {
            return explicitAssistant;
          }
  
          const explicitDefault = candidateAgents.find((agent) => agent?.isDefault);
          if (explicitDefault) {
            return explicitDefault;
          }
  
          const explicitDeveloper = candidateAgents.find((agent) => isPlaygroundDeveloperAgent(agent));
          if (explicitDeveloper) {
            return explicitDeveloper;
          }
  
          return candidateAgents[0] || null;
        }
  
        function getPlaygroundAgentEmailAddress(agent) {
          const profileMetadata = getPlaygroundAgentProfileMetadata(agent?.metadata);
          if (profileMetadata?.email && !isPlaygroundGeneratedAgentEmailAddress(profileMetadata.email)) {
            return profileMetadata.email;
          }
  
          const localPart = slugifyPlaygroundAgentEmailLocalPart(agent?.name || agent?.id || "");
          return localPart + "@" + PLAYGROUND_AGENT_EMAIL_DOMAIN;
        }
  
        function getPlaygroundAgentProfilePhotoUrl(agent) {
          if (isPlaygroundMissionControlAgent(agent)) {
            return PLAYGROUND_MISSION_CONTROL_AGENT_PROFILE_URL;
          }
  
          const profileMetadata = getPlaygroundAgentProfileMetadata(agent?.metadata);
          if (profileMetadata?.photoURL) {
            return profileMetadata.photoURL;
          }
  
          if (isPlaygroundDeveloperAgent(agent)) {
            return PLAYGROUND_FORGE_AGENT_PROFILE_URL;
          }
  
          if (isPlaygroundResearcherAgent(agent)) {
            return PLAYGROUND_FOUNDRY_AGENT_PROFILE_URL;
          }
  
          if (isPlaygroundAgentCreatorAgent(agent)) {
            return PLAYGROUND_AGENT_CREATOR_PROFILE_URL;
          }
  
          if (isPlaygroundAssistantAgent(agent)) {
            return PLAYGROUND_SPARK_AGENT_PROFILE_URL;
          }
  
          if (isPlaygroundDefaultNamedAgent(agent)) {
            return PLAYGROUND_SPARK_AGENT_PROFILE_URL;
          }
  
          return PLAYGROUND_SPARK_AGENT_PROFILE_URL;
        }
  
        function getPlaygroundAgentRunnerPhotoUrl(agent) {
          if (isPlaygroundMissionControlAgent(agent)) {
            return normalizeSessionPhotoUrl(PLAYGROUND_MISSION_CONTROL_AGENT_PROFILE_URL);
          }
  
          const explicitPhotoUrl = normalizeSessionPhotoUrl(
            agent?.photoUrl
            || agent?.photoURL
            || agent?.avatarUrl
            || agent?.avatarURL
            || agent?.avatar
            || agent?.picture
            || ""
          );
          return explicitPhotoUrl || normalizeSessionPhotoUrl(getPlaygroundAgentProfilePhotoUrl(agent));
        }
  
        function buildPlaygroundRunnerAgentOption(agent, overrides = {}) {
          const baseAgent = isPlaygroundMissionControlAgent(agent)
            ? {
                ...agent,
                name: PLAYGROUND_MISSION_CONTROL_AGENT_NAME,
                description: String(agent?.description || "").trim() || PLAYGROUND_MISSION_CONTROL_AGENT_DESCRIPTION,
              }
            : agent;
          const photoUrl = getPlaygroundAgentRunnerPhotoUrl(baseAgent);
          return {
            ...baseAgent,
            ...(photoUrl ? { photoUrl, photoURL: photoUrl } : {}),
            ...(overrides && typeof overrides === "object" ? overrides : {}),
          };
        }
  
        function buildPlaygroundComposerDefaultAgentFallback(kind) {
          const normalizedKind = String(kind || "").trim().toLowerCase();
          if (normalizedKind === "forge") {
            return buildPlaygroundRunnerAgentOption({
              id: "agent_default",
              name: "Forge",
              description: "Implementation-heavy execution and technical work.",
              model: "minimax-m3",
              isDefault: true,
              isSystem: true,
              enabledSkills: PLAYGROUND_DEFAULT_ENABLED_SKILL_IDS,
            });
          }
          if (normalizedKind === "foundry") {
            return buildPlaygroundRunnerAgentOption({
              id: "agent_research",
              name: "Foundry",
              description: "High-rigor synthesis, reasoning, and review.",
              model: "claude-opus-4-8",
              isDefault: true,
              isSystem: true,
              enabledSkills: PLAYGROUND_DEFAULT_ENABLED_SKILL_IDS,
            });
          }
          return buildPlaygroundRunnerAgentOption({
            id: "agent_assistant",
            name: "Spark",
            description: "Fast everyday execution for digital work.",
            model: "deepseek-v4-flash",
            isDefault: true,
            isSystem: true,
            enabledSkills: PLAYGROUND_DEFAULT_ENABLED_SKILL_IDS,
          });
        }
  
        function buildPlaygroundComposerDefaultTeamFallback(agents) {
          const normalizedAgents = Array.isArray(agents) ? agents.filter(Boolean) : [];
          const sparkAgent = normalizedAgents.find((agent) => isPlaygroundAssistantAgent(agent)) || buildPlaygroundComposerDefaultAgentFallback("spark");
          const forgeAgent = normalizedAgents.find((agent) => isPlaygroundDeveloperAgent(agent)) || buildPlaygroundComposerDefaultAgentFallback("forge");
          const foundryAgent = normalizedAgents.find((agent) => isPlaygroundResearcherAgent(agent)) || buildPlaygroundComposerDefaultAgentFallback("foundry");
          const orchestratorAgentId = String(sparkAgent?.id || "agent_assistant").trim();
          const subagentIds = dedupePlaygroundAgentIds([
            forgeAgent?.id || "agent_default",
            foundryAgent?.id || "agent_research",
          ]);
          return buildPlaygroundRunnerAgentOption({
            id: "agent_default_team",
            name: "Default Team",
            description: "Spark, Forge, and Foundry coordinated for project work.",
            agentType: "team",
            isDefault: true,
            isSystem: true,
            metadata: {
              kind: "team",
              executionMode: PLAYGROUND_AGENT_TEAM_EXECUTION_MODE,
              team: {
                version: 1,
                orchestratorAgentId,
                subagentIds,
              },
            },
          });
        }
  
        function ensurePlaygroundComposerDefaultChoices(agents) {
          const nextAgents = (Array.isArray(agents) ? agents : []).filter(Boolean).slice();
          if (!nextAgents.some((agent) => isPlaygroundAssistantAgent(agent))) {
            nextAgents.push(buildPlaygroundComposerDefaultAgentFallback("spark"));
          }
          if (!nextAgents.some((agent) => isPlaygroundDeveloperAgent(agent))) {
            nextAgents.push(buildPlaygroundComposerDefaultAgentFallback("forge"));
          }
          if (!nextAgents.some((agent) => isPlaygroundResearcherAgent(agent))) {
            nextAgents.push(buildPlaygroundComposerDefaultAgentFallback("foundry"));
          }
          const hasDefaultTeam = nextAgents.some((agent) => (
            getPlaygroundAgentListMode(agent) === "teams"
            && (
              agent?.isDefault
              || String(agent?.id || "").trim() === "agent_default_team"
              || String(agent?.name || "").trim().toLowerCase() === "default team"
            )
          ));
          if (!hasDefaultTeam) {
            nextAgents.push(buildPlaygroundComposerDefaultTeamFallback(nextAgents));
          }
          return nextAgents;
        }
  
        function buildPlaygroundAgentProfileMetadata(agent) {
          const email = getPlaygroundAgentEmailAddress(agent);
          const photoURL = normalizeSessionPhotoUrl(getPlaygroundAgentProfilePhotoUrl(agent));
          const nextProfile = {};
  
          if (email) {
            nextProfile.email = email;
          }
          if (photoURL) {
            nextProfile.photoURL = photoURL;
          }
  
          return Object.keys(nextProfile).length > 0 ? nextProfile : null;
        }
  
        function isPlaygroundTeamAgent(agent) {
          return Boolean(getPlaygroundAgentTeamMetadata(agent?.metadata));
        }
  
        function getPlaygroundAgentListMode(agent) {
          return agent?.agentType === "team" || isPlaygroundTeamAgent(agent) ? "teams" : "agents";
        }
  
        function getPlaygroundTaskAssigneePopupMode(actor) {
          if (isPlaygroundHumanAssigneeId(actor?.id)) {
            return "humans";
          }
          return getPlaygroundAgentListMode(actor);
        }
  
        function buildPlaygroundFallbackAgentModelMeta(modelId) {
          const raw = String(modelId || "").trim();
          if (raw.toLowerCase().startsWith("external:")) {
            const parts = raw.split(":");
            const encodedModelId = parts.slice(2).join(":");
            let decodedModelId = encodedModelId;
            try {
              decodedModelId = decodeURIComponent(encodedModelId);
            } catch {}
            return {
              id: raw,
              label: decodedModelId || "External Model",
              description: "Workspace-managed external model",
              intelligence: "Custom",
              contextWindow: "Custom",
              speed: "Custom",
              source: "external",
            };
          }
          return {
            id: raw || "claude-haiku-4-5",
            label: raw || "Claude Haiku 4.5",
            description: "Selected model",
            intelligence: "Good",
            contextWindow: "Custom",
            speed: "Custom",
            source: "managed",
          };
        }
  
        function getPlaygroundAgentModelMeta(modelId, options) {
          const candidateOptions = Array.isArray(options) && options.length > 0
            ? options
            : PLAYGROUND_AGENT_MODEL_OPTIONS;
          const matched = candidateOptions.find((option) => option.id === modelId);
          if (matched) {
            return matched;
          }
          return buildPlaygroundFallbackAgentModelMeta(modelId);
        }
  
        function getPlaygroundAgentModelProviderType(model) {
          const explicitProvider = String(model?.providerType || "").trim().toLowerCase();
          const modelId = String(model?.id || "").trim().toLowerCase();
          if (modelId.startsWith("minimax-") || modelId === "minimax/m3") return "minimax";
          if (modelId.startsWith("kimi-") || modelId.includes("moonshot")) return "kimi";
          if (modelId.startsWith("glm-") || modelId.includes("zai") || modelId.includes("zhipu")) return "zai";
          if (modelId.startsWith("qwen") || modelId.includes("alibaba/qwen")) return "qwen";
          if (modelId.startsWith("grok-") || modelId.includes("xai")) return "xai";
          if (explicitProvider) {
            if (explicitProvider.includes("anthropic")) return "anthropic";
            if (explicitProvider.includes("google") || explicitProvider.includes("gemini")) return "google";
            if (explicitProvider.includes("openai")) return "openai";
            if (explicitProvider.includes("xai") || explicitProvider.includes("grok")) return "xai";
            if (explicitProvider.includes("deepseek")) return "deepseek";
            if (explicitProvider.includes("minimax")) return "minimax";
            if (explicitProvider.includes("moonshot") || explicitProvider.includes("kimi") || explicitProvider.includes("cloudflare")) return "kimi";
            if (explicitProvider.includes("zai") || explicitProvider.includes("zhipu")) return "zai";
            if (explicitProvider.includes("qwen") || explicitProvider.includes("alibaba")) return "qwen";
          }
          if (modelId.startsWith("claude-")) return "anthropic";
          if (modelId.startsWith("gemini-")) return "google";
          if (modelId.startsWith("gpt-")) return "openai";
          if (modelId.startsWith("deepseek-")) return "deepseek";
          if (modelId.startsWith("kimi-")) return "kimi";
          return "generic";
        }
  
        function getPlaygroundAgentModelProviderLabel(model) {
          const providerType = getPlaygroundAgentModelProviderType(model);
          if (providerType === "anthropic") return "Anthropic";
          if (providerType === "google") return "Google";
          if (providerType === "openai") return "OpenAI";
          if (providerType === "xai") return "xAI";
          if (providerType === "deepseek") return "DeepSeek";
          if (providerType === "minimax") return "MiniMax";
          if (providerType === "kimi") return "Moonshot";
          if (providerType === "zai") return "ZAI";
          if (providerType === "qwen") return "Qwen";
          return "Provider";
        }
  
        function getPlaygroundAgentModelProviderFilterKey(model) {
          const normalizedContextWindow = String(model?.contextWindow || "").trim().toLowerCase();
          if (!normalizedContextWindow || normalizedContextWindow === "custom") {
            return "custom";
          }
          const providerType = getPlaygroundAgentModelProviderType(model);
          if (providerType === "anthropic" || providerType === "google" || providerType === "openai" || providerType === "xai" || providerType === "deepseek" || providerType === "minimax" || providerType === "kimi" || providerType === "zai" || providerType === "qwen") {
            return providerType;
          }
          const normalizedSource = String(model?.source || "").trim().toLowerCase();
          const normalizedProviderType = String(model?.providerType || "").trim().toLowerCase();
          if (
            normalizedSource === "external"
            || normalizedSource === "custom"
            || normalizedProviderType.includes("custom")
            || normalizedProviderType.includes("generic")
          ) {
            return "custom";
          }
          return "custom";
        }
  
        function getPlaygroundAgentModelProviderIcon(model) {
          const providerType = getPlaygroundAgentModelProviderType(model);
          if (providerType === "anthropic") {
            return { src: "/img/05-model-provider-icons/claude.png", alt: "Anthropic", className: "" };
          }
          if (providerType === "google") {
            return { src: "/img/05-model-provider-icons/gemini.png", alt: "Google", className: "" };
          }
          if (providerType === "openai") {
            return { src: "/img/05-model-provider-icons/openai.svg", alt: "OpenAI", className: "is-openai" };
          }
          if (providerType === "xai") {
            return { src: "/img/05-model-provider-icons/xai.svg", alt: "xAI", className: "is-openai" };
          }
          if (providerType === "deepseek") {
            return { src: "/img/05-model-provider-icons/deepseek.png", alt: "DeepSeek", className: "" };
          }
          if (providerType === "minimax") {
            return { src: "/img/05-model-provider-icons/minimax.svg", alt: "MiniMax", className: "" };
          }
          if (providerType === "kimi") {
            return { src: "/img/05-model-provider-icons/kimi.png", alt: "Moonshot", className: "" };
          }
          if (providerType === "zai") {
            return { src: "/img/05-model-provider-icons/zai.webp", alt: "ZAI", className: "" };
          }
          if (providerType === "qwen") {
            return { src: "/img/05-model-provider-icons/qwen.svg", alt: "Qwen", className: "is-openai" };
          }
          return null;
        }
  
        const PLAYGROUND_AGENT_MODEL_PRICING_BY_ID = {
          "claude-haiku-4-5": { input: 1.0, cached: 0.1, output: 5.0 },
          "claude-sonnet-4-5": { input: 3.0, cached: 0.3, output: 15.0 },
          "claude-opus-4-6": { input: 5.0, cached: 0.5, output: 25.0 },
          "claude-opus-4-7": { input: 5.0, cached: 0.5, output: 25.0 },
          "claude-opus-4-8": { input: 5.0, cached: 0.5, output: 25.0 },
          "gpt-5.5-pro": { input: 30.0, cached: null, output: 180.0 },
          "gpt-5.5": { input: 5.0, cached: 0.5, output: 30.0 },
          "gpt-5.4": { input: 2.5, cached: 0.25, output: 15.0 },
          "gpt-5.4-mini": { input: 0.75, cached: 0.075, output: 4.5 },
          "gpt-5.4-nano": { input: 0.2, cached: 0.02, output: 1.25 },
          "grok-4.5": { input: 2.0, cached: 2.0, output: 6.0 },
          "gemini-3-flash": { input: 0.5, cached: 0.05, output: 3.0 },
          "gemini-3-1-flash": { input: 0.5, cached: 0.05, output: 3.0 },
          "gemini-3-1-pro": { input: 2.0, cached: 0.2, output: 12.0 },
          "deepseek-v4-pro": { input: 0.435, cached: 0.003625, output: 0.87 },
          "deepseek-v4-flash": { input: 0.14, cached: 0.0028, output: 0.28 },
          "minimax-m3": { input: 0.6, cached: 0.12, output: 2.4 },
          "kimi-k2.6": { input: 0.95, cached: 0.16, output: 4.0 },
          "kimi-k2.7-code": { input: 0.95, cached: 0.19, output: 4.0 },
          "glm-5.2": { input: 1.4, cached: 0.26, output: 4.4 },
          "qwen3.5-397b-a17b": { input: 0.6, cached: 0.6, output: 3.6 },
        };
  
        function getPlaygroundAgentModelPricing(modelId) {
          return PLAYGROUND_AGENT_MODEL_PRICING_BY_ID[String(modelId || "").trim()] || null;
        }
  
        function readPlaygroundAgentModelPricingNumber(value) {
          const numericValue = Number(value);
          return Number.isFinite(numericValue) ? numericValue : null;
        }
  
        function getPlaygroundAgentModelWeightedCost(modelId) {
          const pricing = getPlaygroundAgentModelPricing(modelId);
          if (!pricing) {
            return 0;
          }
          const inputCost = readPlaygroundAgentModelPricingNumber(pricing.input);
          const cachedCost = readPlaygroundAgentModelPricingNumber(pricing.cached);
          const outputCost = readPlaygroundAgentModelPricingNumber(pricing.output);
          if (inputCost === null || outputCost === null) {
            return 0;
          }
          return (inputCost * 0.7) + ((cachedCost === null ? inputCost : cachedCost) * 0.1) + (outputCost * 0.2);
        }
  
        function getPlaygroundAgentModelCostMultiplier(modelId) {
          const baseline = getPlaygroundAgentModelWeightedCost("claude-haiku-4-5");
          const target = getPlaygroundAgentModelWeightedCost(modelId);
          if (!baseline || !target) {
            return 1;
          }
          return target / baseline;
        }
  
        function formatPlaygroundAgentModelComputeTokenCost(modelId) {
          const weightedCost = getPlaygroundAgentModelWeightedCost(modelId);
          if (!weightedCost) {
            return "Custom";
          }
          return formatSettingsCurrency(weightedCost * 1.1) + " / 1M";
        }
  
        function formatPlaygroundAgentModelCostMultiplier(multiplier) {
          const normalized = Number.isFinite(Number(multiplier)) ? Number(multiplier) : 1;
          if (Math.abs(normalized - 1) < 0.01) {
            return "1x";
          }
          if (normalized < 1) {
            return normalized.toFixed(2) + "x";
          }
          return normalized.toFixed(1) + "x";
        }
  
        function getPlaygroundAgentIntelligenceLevel(label) {
          const normalized = String(label || "").trim().toLowerCase();
          if (normalized === "highest") {
            return 4;
          }
          if (normalized === "high" || normalized === "medium") {
            return 3;
          }
          if (normalized === "good") {
            return 2;
          }
          return 1;
        }
  
        const PLAYGROUND_ENVIRONMENT_COMPUTE_PROFILES = [
          {
            id: "lite",
            label: "Lite",
            description: "Lowest-cost profile for fast CLI-first work.",
            cpuCores: 0.5,
            memoryMb: 1536,
            guiEnabled: false,
            officeAppsEnabled: false,
            minutePrice: 0.0026,
          },
          {
            id: "standard",
            label: "Standard",
            description: "Balanced default for most coding and automation work.",
            cpuCores: 1,
            memoryMb: 2048,
            guiEnabled: false,
            officeAppsEnabled: false,
            minutePrice: 0.0052,
          },
          {
            id: "power",
            label: "Power",
            description: "More CPU and memory for heavier builds and multi-step tasks.",
            cpuCores: 2,
            memoryMb: 4096,
            guiEnabled: false,
            officeAppsEnabled: false,
            minutePrice: 0.00975,
          },
          {
            id: "desktop",
            label: "Desktop",
            description: "GUI-enabled profile for browser and desktop-app workflows.",
            cpuCores: 2,
            memoryMb: 4096,
            guiEnabled: true,
            officeAppsEnabled: false,
            minutePrice: 0.013,
          },
        ];
        const PLAYGROUND_DEFAULT_USER_ENVIRONMENT_COMPUTE_PROFILE = "lite";
        const PLAYGROUND_DEFAULT_CUSTOM_ENVIRONMENT_COMPUTE_PROFILE = "standard";
  
        function normalizePlaygroundEnvironmentComputeProfileId(value) {
          const normalized = String(value || "").trim().toLowerCase();
          if (normalized === "lite" || normalized === "standard" || normalized === "power" || normalized === "desktop") {
            return normalized;
          }
          return "";
        }
  
        function getPlaygroundEnvironmentComputeProfileConfig(profileId) {
          const normalized = normalizePlaygroundEnvironmentComputeProfileId(profileId) || PLAYGROUND_DEFAULT_CUSTOM_ENVIRONMENT_COMPUTE_PROFILE;
          return PLAYGROUND_ENVIRONMENT_COMPUTE_PROFILES.find((profile) => profile.id === normalized)
            || PLAYGROUND_ENVIRONMENT_COMPUTE_PROFILES[1];
        }
  
        function clonePlaygroundEnvironmentMetadata(metadata) {
          return metadata && typeof metadata === "object" && !Array.isArray(metadata)
            ? JSON.parse(JSON.stringify(metadata))
            : {};
        }
  
        function resolvePlaygroundEnvironmentComputeProfileId(environment) {
          const metadata = environment?.metadata && typeof environment.metadata === "object" && !Array.isArray(environment.metadata)
            ? environment.metadata
            : null;
          const explicitProfile = normalizePlaygroundEnvironmentComputeProfileId(environment?.computeProfile || metadata?.computeProfile);
          if (explicitProfile) {
            return explicitProfile;
          }
  
          const guiEnabled = typeof environment?.guiEnabled === "boolean"
            ? environment.guiEnabled
            : metadata?.guiEnabled === true;
          const officeAppsEnabled = typeof environment?.officeAppsEnabled === "boolean"
            ? environment.officeAppsEnabled
            : metadata?.officeAppsEnabled === true;
  
          if (guiEnabled || officeAppsEnabled) {
            return "desktop";
          }
  
          return environment?.isDefault
            ? PLAYGROUND_DEFAULT_USER_ENVIRONMENT_COMPUTE_PROFILE
            : PLAYGROUND_DEFAULT_CUSTOM_ENVIRONMENT_COMPUTE_PROFILE;
        }
  
        function getPlaygroundEnvironmentRatePerMinute(environment) {
          const profile = getPlaygroundEnvironmentComputeProfileConfig(resolvePlaygroundEnvironmentComputeProfileId(environment));
          return profile.minutePrice;
        }
  
        function applyPlaygroundEnvironmentComputeProfileDraft(environment, profileId, options = {}) {
          const base = environment && typeof environment === "object"
            ? environment
            : {};
          const profile = getPlaygroundEnvironmentComputeProfileConfig(profileId);
          const metadata = clonePlaygroundEnvironmentMetadata(base.metadata);
          const pricing = metadata.pricing && typeof metadata.pricing === "object" && !Array.isArray(metadata.pricing)
            ? { ...metadata.pricing }
            : {};
          pricing.minutePrice = profile.minutePrice;
  
          const requestedOfficeAppsEnabled = Object.prototype.hasOwnProperty.call(options, "officeAppsEnabled")
            ? options.officeAppsEnabled === true
            : base.officeAppsEnabled === true;
          const officeAppsEnabled = profile.id === "desktop" ? requestedOfficeAppsEnabled : false;
  
          return {
            ...base,
            computeProfile: profile.id,
            guiEnabled: profile.guiEnabled,
            officeAppsEnabled,
            estimatedCostPerMinute: profile.minutePrice,
            metadata: {
              ...metadata,
              computeProfile: profile.id,
              computeResources: {
                cpuCores: profile.cpuCores,
                memoryMb: profile.memoryMb,
              },
              pricing,
              guiEnabled: profile.guiEnabled,
              officeAppsEnabled,
            },
          };
        }
  
        function setPlaygroundEnvironmentOfficeAppsEnabled(environment, enabled) {
          if (enabled) {
            return applyPlaygroundEnvironmentComputeProfileDraft(environment, "desktop", { officeAppsEnabled: true });
          }
  
          return applyPlaygroundEnvironmentComputeProfileDraft(
            environment,
            resolvePlaygroundEnvironmentComputeProfileId(environment),
            { officeAppsEnabled: false }
          );
        }
  
        function buildPlaygroundDefaultEnvironmentDraft() {
          const now = new Date().toISOString();
          const defaultRuntimes = {
            python: "3.12",
            nodejs: "22",
          };
  
          const draft = {
            id: "",
            userId: "",
            name: "New Environment",
            description: "",
            runtimes: defaultRuntimes,
            packageVersions: defaultRuntimes,
            packages: {
              system: [],
              python: [],
              node: [],
            },
            environmentVariables: [],
            secrets: [],
            setupScripts: [],
            mcpServers: [],
            documentation: [],
            internetAccess: true,
            guiEnabled: false,
            officeAppsEnabled: false,
            computeProfile: PLAYGROUND_DEFAULT_CUSTOM_ENVIRONMENT_COMPUTE_PROFILE,
            dockerfileExtensions: "",
            baseImage: "",
            metadata: null,
            status: "stopped",
            estimatedStorageMB: 0,
            estimatedCostPerMinute: 0,
            isSystem: false,
            isDefault: false,
            isActive: true,
            createdAt: now,
            updatedAt: now,
          };
  
          return applyPlaygroundEnvironmentComputeProfileDraft(
            draft,
            PLAYGROUND_DEFAULT_CUSTOM_ENVIRONMENT_COMPUTE_PROFILE,
            { officeAppsEnabled: false }
          );
        }
  
        function buildPlaygroundDefaultServerDraft() {
          const now = new Date().toISOString();
          return {
            id: "",
            userId: "",
            projectId: null,
            name: "New Resource",
            description: "",
            kind: "web_app",
            sourceType: "manual",
            sourceEnvironmentId: null,
            sourcePath: "",
            region: "europe-west1",
            runtime: "nodejs22",
            authMode: "public",
            serviceUrl: "",
            customDomain: "",
            cloudRunServiceName: "",
            imageUrl: "",
            status: "draft",
            lastDeployedAt: "",
            template: "blank",
            templateAgentId: "",
            templateEnvironmentId: "",
            templateDeploy: false,
            databaseMode: "none",
            databaseId: "",
            databaseName: "",
            databaseDescription: "",
            databaseLocation: "eur3",
            metadata: null,
            createdAt: now,
            updatedAt: now,
          };
        }
  
        function buildPlaygroundDefaultDatabaseDraft() {
          const now = new Date().toISOString();
          return {
            id: "",
            userId: "",
            projectId: null,
            name: "New Database",
            description: "",
            provider: "firestore",
            location: "eur3",
            status: "active",
            firestoreNamespace: "",
            permissionSet: createPlaygroundDefaultPermissionSet("database"),
            metadata: null,
            createdAt: now,
            updatedAt: now,
          };
        }
  
        function buildPlaygroundDefaultAgentDraft(kind = "single") {
          const now = new Date().toISOString();
          const defaultDeepResearchModel = getDemoDefaultDeepResearchModel(readDemoSettingsPlatformConfig());
          return {
            id: PLAYGROUND_AGENT_DRAFT_ID,
            name: kind === "team" ? "New Squad" : "New Agent",
            description: "",
            model: "deepseek-v4-flash",
            instructions: "",
            binary: "Claude Code CLI",
            reasoningEffort: "medium",
            enabledSkills: ["frontend_design"],
            guardrailSetIds: [],
            deepResearchModel: defaultDeepResearchModel,
            permissionSet: createPlaygroundFullAccessPermissionSet("agent"),
            voiceMode: "web",
            voiceProvider: "xai",
            voiceModel: "grok-voice-latest",
            voiceId: "eve",
            voiceInstructions: "",
            voiceLanguageHint: "",
            voiceTurnDetection: null,
            voicePronunciationReplacements: null,
            agentType: kind === "team" ? "team" : "single",
            teamOrchestratorAgentId: "",
            teamSubagentIds: [],
            teamExecutionMode: PLAYGROUND_AGENT_TEAM_EXECUTION_MODE,
            createdAt: now,
            updatedAt: now,
            lastRunAt: "",
            isActive: true,
            isDefault: false,
            isSystem: false,
            metadata: null,
          };
        }
  
        function buildPlaygroundAgentCreatorInstructions() {
          return [
            "You are the internal Agent Creator for Testbase ACP.",
            "Your only job is to create or improve instructions for a target ACP agent. You do not implement the user's underlying business task.",
            "",
            "Workflow:",
            "1. Read the target agent context and user use case.",
            "2. If the use case is underspecified, ask a compact numbered set of clarifying questions and stop. Ask about audience, recurring workflow, tools/data sources, output format, approval boundaries, and success criteria only when relevant.",
            "3. If enough information is available, write the final instructions to a temporary markdown file and run: python3 /workspace/.claude/skills/computer-agents/scripts/computer-agents.py agents update <target_agent_id> --instructions-file <instructions_file>. Do not list all agents, inspect unrelated resources, browse documentation, or run help first unless this update command is unavailable or fails because the syntax is unknown.",
            "4. After the update succeeds, briefly summarize what changed and mention that the details page will reflect the new instructions.",
            "",
            "Instruction quality requirements:",
            "- Write production-ready operational instructions, not a casual summary.",
            "- Include role, scope, normal workflow, tool-use policy, clarification rules, output expectations, and constraints implied by the use case.",
            "- Preserve any user-provided name, description, model, permissions, and boundaries.",
            "- Keep the instructions specific to the target agent and avoid creating new agents, teams, environments, skills, or threads.",
            "- Never claim the instructions were updated until the Computer Agents skill update succeeds.",
          ].join("\\n");
        }
  
        function selectPlaygroundAgentModelId(modelOptions, candidateModelIds) {
          const candidateOptions = Array.isArray(modelOptions) && modelOptions.length > 0
            ? modelOptions
            : PLAYGROUND_AGENT_MODEL_OPTIONS;
          const candidates = Array.isArray(candidateModelIds) ? candidateModelIds : [];
          for (const modelId of candidates) {
            const normalizedModelId = String(modelId || "").trim();
            if (!normalizedModelId) continue;
            const matchingOption = candidateOptions.find((option) => option?.id === normalizedModelId);
            if (matchingOption && !matchingOption.locked) {
              return matchingOption.id;
            }
          }
          return getPlaygroundAgentModelMeta(candidates[0] || "gemini-3-flash", candidateOptions).id;
        }
  
        function resolvePlaygroundAgentCreatorModelId(modelOptions = PLAYGROUND_AGENT_MODEL_OPTIONS, subscriptionTierId = "") {
          const isFreeTier = (normalizeSettingsTierId(subscriptionTierId) || "sandbox") === "sandbox";
          return selectPlaygroundAgentModelId(
            modelOptions,
            isFreeTier
              ? ["gemini-3-flash", "gemini-3-1-flash", "gemini-3-flash-preview", "claude-haiku-4-5", "claude-sonnet-4-5"]
              : ["claude-sonnet-4-5", "gemini-3-flash", "gemini-3-1-flash", "claude-haiku-4-5"]
          );
        }
  
        function resolvePlaygroundAgentDraftModelId(modelOptions = PLAYGROUND_AGENT_MODEL_OPTIONS, subscriptionTierId = "") {
          const isFreeTier = (normalizeSettingsTierId(subscriptionTierId) || "sandbox") === "sandbox";
          return selectPlaygroundAgentModelId(
            modelOptions,
            isFreeTier
              ? ["deepseek-v4-flash", "claude-haiku-4-5", "gemini-3-flash", "gemini-3-1-flash", "gemini-3-flash-preview"]
              : ["deepseek-v4-flash", "claude-haiku-4-5", "gemini-3-flash", "gemini-3-1-flash"]
          );
        }
  
        function isPlaygroundPaidModelSubscriptionError(message) {
          const text = String(message || "");
          return /requires a paid subscription/i.test(text)
            || (/please upgrade/i.test(text) && /gemini-3-flash/i.test(text));
        }
  
        function buildPlaygroundAgentCreatorDraft(modelOptions = PLAYGROUND_AGENT_MODEL_OPTIONS, subscriptionTierId = "") {
          const draft = buildPlaygroundDefaultAgentDraft("single");
          const modelMeta = getPlaygroundAgentModelMeta(
            resolvePlaygroundAgentCreatorModelId(modelOptions, subscriptionTierId),
            modelOptions
          );
          return normalizePlaygroundAgentRecord({
            ...draft,
            id: PLAYGROUND_AGENT_DRAFT_ID,
            name: PLAYGROUND_AGENT_CREATOR_NAME,
            description: PLAYGROUND_AGENT_CREATOR_DESCRIPTION,
            model: modelMeta.id,
            instructions: buildPlaygroundAgentCreatorInstructions(),
            enabledSkills: ["computer_agents"],
            permissionSet: createPlaygroundDefaultPermissionSet("agent"),
            metadata: {
              runnerPlayground: {
                role: PLAYGROUND_AGENT_CREATOR_METADATA_ROLE,
                internal: true,
                managedBy: "runner-web-sdk-demo",
                version: 1,
              },
              profile: {
                email: slugifyPlaygroundAgentEmailLocalPart(PLAYGROUND_AGENT_CREATOR_NAME) + "@" + PLAYGROUND_AGENT_EMAIL_DOMAIN,
                photoURL: PLAYGROUND_AGENT_CREATOR_PROFILE_URL,
              },
            },
          });
        }
  
        function buildPlaygroundMissionControlAgentInstructions() {
          return [
            "You are the internal mission control agent for Testbase ACP.",
            "Your job is to run project strategy and backlog planning for the exact project supplied in the user prompt. You do not ask what Mission Control means.",
            "",
            "Workflow:",
            "1. Read the provided project goal, operating profile, existing structured strategy, outcomes, rules, connectors, attachments, milestones, tasks, agents, environments, and operator directive.",
            "2. Treat the project operating profile as the default shape of the project: it defines likely resources, setup steps, dashboard focus, collaboration style, and sync targets. Override it only when the actual project context proves a better path.",
            "3. Use the Task Management skill for milestone, task, subtask, blocker, dependency, connector, comment, and assignment mutations whenever the project structure needs to change.",
            "4. Use the Computer Agents skill to inspect live agents, environments, skills, and thread context before assigning work or assuming IDs.",
            "5. Keep the project's selected default environment as the execution environment unless the prompt or available context proves another environment is better.",
            "6. Update the compact strategy, outcomes, and project rules only when the new project context justifies it. Preserve existing outcome IDs and rule meaning whenever possible.",
            "7. Produce a concise project strategy document and the required mission_control_json block. Do not add conversational prefaces or outros.",
            "",
            "Planning standards:",
            "- Prefer milestone-level structure before creating loose tasks.",
            "- Create execution-ready tasks with assignee, environment, skills, dependencies, connector context, and acceptance criteria when the context supports it.",
            "- Keep tickets specific, short, and actionable.",
            "- Keep outcomes user/business-facing and map them to releaseIds (milestone ids) when milestones clearly contribute to them. Use releaseId only for legacy single-milestone mappings.",
            "- Keep project rules concrete, operational, and phrased as instructions agents can follow while executing tickets.",
            "- Use comments to preserve important rationale, sequencing, and handoff context.",
            "- If the project is underspecified, still create a useful strategy from the available context and state the most important assumptions in the strategy document.",
          ].join("\\n");
        }
  
        function resolvePlaygroundMissionControlAgentModelId(modelOptions = PLAYGROUND_AGENT_MODEL_OPTIONS, subscriptionTierId = "") {
          const isFreeTier = (normalizeSettingsTierId(subscriptionTierId) || "sandbox") === "sandbox";
          return selectPlaygroundAgentModelId(
            modelOptions,
            isFreeTier
              ? ["claude-haiku-4-5", "gemini-3-flash", "gemini-3-1-flash", "gemini-3-flash-preview"]
              : ["minimax-m3", "deepseek-v4-pro", "claude-sonnet-4-5", "claude-haiku-4-5", "gemini-3-flash", "gemini-3-1-flash"]
          );
        }
  
        function buildPlaygroundMissionControlAgentDraft(modelOptions = PLAYGROUND_AGENT_MODEL_OPTIONS, subscriptionTierId = "") {
          const draft = buildPlaygroundDefaultAgentDraft("single");
          const modelMeta = getPlaygroundAgentModelMeta(
            resolvePlaygroundMissionControlAgentModelId(modelOptions, subscriptionTierId),
            modelOptions
          );
          return normalizePlaygroundAgentRecord({
            ...draft,
            id: PLAYGROUND_AGENT_DRAFT_ID,
            name: PLAYGROUND_MISSION_CONTROL_AGENT_NAME,
            description: PLAYGROUND_MISSION_CONTROL_AGENT_DESCRIPTION,
            model: modelMeta.id,
            instructions: buildPlaygroundMissionControlAgentInstructions(),
            enabledSkills: ["task_management", "computer_agents"],
            permissionSet: createPlaygroundDefaultPermissionSet("agent"),
            metadata: {
              runnerPlayground: {
                role: PLAYGROUND_MISSION_CONTROL_AGENT_METADATA_ROLE,
                internal: true,
                managedBy: "runner-web-sdk-demo",
                version: 1,
              },
              profile: {
                email: slugifyPlaygroundAgentEmailLocalPart(PLAYGROUND_MISSION_CONTROL_AGENT_NAME) + "@" + PLAYGROUND_AGENT_EMAIL_DOMAIN,
                photoURL: PLAYGROUND_MISSION_CONTROL_AGENT_PROFILE_URL,
              },
            },
          });
        }
  
        const PLAYGROUND_TEAM_ROLE_DEFINITIONS = [
          {
            id: "owner",
            label: "Owner",
            description: "Has permanent full control of the team, role permissions, shared resources, and governance settings.",
          },
          {
            id: "member",
            label: "Member",
            description: "Can participate in shared team work, with elevated team operations routed through approval.",
          },
          {
            id: "contributor",
            label: "Contributor",
            description: "Can contribute to shared resources and projects, while administrative team changes remain protected.",
          },
          {
            id: "admin",
            label: "Admin",
            description: "Can manage team membership, shared resources, role permissions, and team settings.",
          },
        ];
  
        const PLAYGROUND_ASSIGNABLE_TEAM_ROLE_DEFINITIONS = PLAYGROUND_TEAM_ROLE_DEFINITIONS.filter((role) => role.id !== "owner");
        const PLAYGROUND_TEAM_ROLE_IDS = PLAYGROUND_TEAM_ROLE_DEFINITIONS.map((role) => role.id);
  ${ORGANIZATIONS_DOMAIN_SCRIPT_FRAGMENTS.roleDefinitions}      const PLAYGROUND_TEAM_LEGACY_ROLE_MAP = {
          create: "member",
          member: "member",
          viewer: "member",
          configure: "contributor",
          develop: "contributor",
          contributor: "contributor",
          editor: "contributor",
          admin: "admin",
          owner: "owner",
          manage: "admin",
        };
        const PLAYGROUND_TEAM_ROLE_API_VALUE_MAP = {
          member: "create",
          contributor: "develop",
          admin: "admin",
          owner: "owner",
        };
  
        function normalizePlaygroundTeamRoleId(value, fallback = "member") {
          const normalized = String(value || "").trim().toLowerCase();
          const mapped = PLAYGROUND_TEAM_LEGACY_ROLE_MAP[normalized] || normalized;
          return PLAYGROUND_TEAM_ROLE_IDS.includes(mapped) ? mapped : fallback;
        }
  
        function getPlaygroundTeamRoleDefinition(roleId) {
          const normalizedRoleId = normalizePlaygroundTeamRoleId(roleId, "member");
          return PLAYGROUND_TEAM_ROLE_DEFINITIONS.find((role) => role.id === normalizedRoleId)
            || PLAYGROUND_TEAM_ROLE_DEFINITIONS[0];
        }
  
        function getPlaygroundTeamRoleApiValue(roleId) {
          const normalizedRoleId = normalizePlaygroundTeamRoleId(roleId, "member");
          return PLAYGROUND_TEAM_ROLE_API_VALUE_MAP[normalizedRoleId] || normalizedRoleId;
        }
  
  ${ORGANIZATIONS_DOMAIN_SCRIPT_FRAGMENTS.roleIdentity}
        function createPlaygroundTeamRolePermissionSet(roleId) {
          const normalizedRoleId = normalizePlaygroundTeamRoleId(roleId, "member");
          if (normalizedRoleId === "owner") {
            return createPlaygroundFullAccessPermissionSet("team_role");
          }
          const permissionSet = createPlaygroundDefaultPermissionSet("team_role");
          const applyRingAccess = (ringId, access) => {
            permissionSet.rings[ringId] = {
              ...(permissionSet.rings[ringId] || {}),
              defaultAccess: normalizePlaygroundPermissionAccess(access, "ask_for_permission"),
            };
          };
          const applyActionAccess = (actionId, ringId, access) => {
            permissionSet.actions[actionId] = {
              ...(permissionSet.actions[actionId] || {}),
              ringId: normalizePlaygroundPermissionRingId(ringId, "ring_1"),
              access: normalizePlaygroundPermissionAccess(access, "ask_for_permission"),
            };
          };
  
          if (normalizedRoleId === "admin") {
            PLAYGROUND_PERMISSION_RING_IDS.forEach((ringId) => applyRingAccess(ringId, "full_access"));
            [
              "team_member_invite",
              "team_member_remove",
              "team_role_update",
              "team_shared_resource_manage",
              "team_permission_request_review",
              "team_settings_update",
              "team_workspace_view",
            ].forEach((actionId) => {
              const actionDefinition = getPlaygroundPermissionActionDefinition(actionId);
              applyActionAccess(actionId, actionDefinition?.ringId || "ring_2", "full_access");
            });
            return permissionSet;
          }
  
          if (normalizedRoleId === "contributor") {
            applyRingAccess("ring_1", "full_access");
            applyRingAccess("ring_2", "ask_for_permission");
            applyRingAccess("ring_3", "ask_for_permission");
            applyActionAccess("team_workspace_view", "ring_1", "full_access");
            applyActionAccess("shared_resource_write", "ring_2", "ask_for_permission");
            applyActionAccess("team_shared_resource_manage", "ring_2", "ask_for_permission");
            applyActionAccess("team_member_invite", "ring_2", "no_access");
            applyActionAccess("team_member_remove", "ring_2", "no_access");
            applyActionAccess("team_role_update", "ring_2", "no_access");
            applyActionAccess("team_permission_request_review", "ring_2", "no_access");
            applyActionAccess("team_settings_update", "ring_3", "no_access");
            return permissionSet;
          }
  
          applyRingAccess("ring_1", "read_only");
          applyRingAccess("ring_2", "ask_for_permission");
          applyRingAccess("ring_3", "no_access");
          applyActionAccess("team_workspace_view", "ring_1", "read_only");
          applyActionAccess("shared_resource_write", "ring_2", "ask_for_permission");
          applyActionAccess("team_shared_resource_manage", "ring_2", "ask_for_permission");
          applyActionAccess("team_member_invite", "ring_2", "no_access");
          applyActionAccess("team_member_remove", "ring_2", "no_access");
          applyActionAccess("team_role_update", "ring_2", "no_access");
          applyActionAccess("team_permission_request_review", "ring_2", "no_access");
          applyActionAccess("team_settings_update", "ring_3", "no_access");
          return permissionSet;
        }
  
        function createPlaygroundDatabaseTeamRolePermissionSet(roleId) {
          const normalizedRoleId = normalizePlaygroundTeamRoleId(roleId, "member");
          if (normalizedRoleId === "owner") {
            return createPlaygroundFullAccessPermissionSet("database");
          }
          const permissionSet = createPlaygroundDefaultPermissionSet("database");
          const applyRingAccess = (ringId, access) => {
            permissionSet.rings[ringId] = {
              ...(permissionSet.rings[ringId] || {}),
              defaultAccess: normalizePlaygroundPermissionAccess(access, "ask_for_permission"),
            };
          };
          const applyActionAccess = (actionId, access) => {
            const actionDefinition = getPlaygroundPermissionActionDefinition(actionId);
            permissionSet.actions[actionId] = {
              ...(permissionSet.actions[actionId] || {}),
              ringId: actionDefinition?.ringId || "ring_1",
              access: normalizePlaygroundPermissionAccess(access, "ask_for_permission"),
            };
          };
  
          if (normalizedRoleId === "admin") {
            PLAYGROUND_PERMISSION_RING_IDS.forEach((ringId) => applyRingAccess(ringId, "full_access"));
            PLAYGROUND_PERMISSION_ACTION_DEFINITIONS
              .filter((action) => action.subjectTypes?.includes("database"))
              .forEach((action) => applyActionAccess(action.id, "full_access"));
            return permissionSet;
          }
  
          if (normalizedRoleId === "contributor") {
            applyRingAccess("ring_1", "full_access");
            applyRingAccess("ring_2", "full_access");
            applyRingAccess("ring_3", "ask_for_permission");
            ["database_schema_read", "database_data_read", "database_query", "database_export", "database_document_create", "database_document_update"]
              .forEach((actionId) => applyActionAccess(actionId, "full_access"));
            applyActionAccess("database_document_delete", "ask_for_permission");
            applyActionAccess("database_schema_manage", "ask_for_permission");
            applyActionAccess("database_access_manage", "no_access");
            return permissionSet;
          }
  
          applyRingAccess("ring_1", "read_only");
          applyRingAccess("ring_2", "ask_for_permission");
          applyRingAccess("ring_3", "no_access");
          ["database_schema_read", "database_data_read", "database_query"]
            .forEach((actionId) => applyActionAccess(actionId, "read_only"));
          applyActionAccess("database_export", "ask_for_permission");
          ["database_document_create", "database_document_update"]
            .forEach((actionId) => applyActionAccess(actionId, "ask_for_permission"));
          ["database_document_delete", "database_schema_manage", "database_access_manage"]
            .forEach((actionId) => applyActionAccess(actionId, "no_access"));
          return permissionSet;
        }
  
        function createPlaygroundServerTeamRolePermissionSet(roleId) {
          const normalizedRoleId = normalizePlaygroundTeamRoleId(roleId, "member");
          if (normalizedRoleId === "owner") {
            return createPlaygroundFullAccessPermissionSet("server");
          }
          const permissionSet = createPlaygroundDefaultPermissionSet("server");
          const applyRingAccess = (ringId, access) => {
            permissionSet.rings[ringId] = {
              ...(permissionSet.rings[ringId] || {}),
              defaultAccess: normalizePlaygroundPermissionAccess(access, "ask_for_permission"),
            };
          };
          const applyActionAccess = (actionId, access) => {
            const actionDefinition = getPlaygroundPermissionActionDefinition(actionId);
            permissionSet.actions[actionId] = {
              ...(permissionSet.actions[actionId] || {}),
              ringId: actionDefinition?.ringId || "ring_1",
              access: normalizePlaygroundPermissionAccess(access, "ask_for_permission"),
            };
          };
  
          if (normalizedRoleId === "admin") {
            PLAYGROUND_PERMISSION_RING_IDS.forEach((ringId) => applyRingAccess(ringId, "full_access"));
            PLAYGROUND_PERMISSION_ACTION_DEFINITIONS
              .filter((action) => action.subjectTypes?.includes("server"))
              .forEach((action) => applyActionAccess(action.id, "full_access"));
            return permissionSet;
          }
  
          if (normalizedRoleId === "contributor") {
            applyRingAccess("ring_1", "full_access");
            applyRingAccess("ring_2", "full_access");
            applyRingAccess("ring_3", "ask_for_permission");
            ["server_source_read", "server_invoke", "server_logs_read", "server_source_write", "server_connection_manage"]
              .forEach((actionId) => applyActionAccess(actionId, "full_access"));
            applyActionAccess("server_deploy", "ask_for_permission");
            applyActionAccess("server_access_manage", "no_access");
            applyActionAccess("server_delete", "no_access");
            return permissionSet;
          }
  
          applyRingAccess("ring_1", "read_only");
          applyRingAccess("ring_2", "ask_for_permission");
          applyRingAccess("ring_3", "no_access");
          ["server_source_read", "server_logs_read"].forEach((actionId) => applyActionAccess(actionId, "read_only"));
          applyActionAccess("server_invoke", "full_access");
          ["server_source_write", "server_connection_manage"].forEach((actionId) => applyActionAccess(actionId, "ask_for_permission"));
          ["server_deploy", "server_access_manage", "server_delete"].forEach((actionId) => applyActionAccess(actionId, "no_access"));
          return permissionSet;
        }
  
        function normalizePlaygroundTeamRolePermissionSets(value) {
          const inputSets = isPlaygroundPermissionRecord(value) ? value : {};
          return PLAYGROUND_TEAM_ROLE_DEFINITIONS.reduce((rolePermissionSets, role) => {
            if (role.id === "owner") {
              rolePermissionSets[role.id] = createPlaygroundTeamRolePermissionSet(role.id);
              return rolePermissionSets;
            }
            rolePermissionSets[role.id] = normalizePlaygroundPermissionSet(
              inputSets[role.id] || createPlaygroundTeamRolePermissionSet(role.id),
              "team_role"
            );
            return rolePermissionSets;
          }, {});
        }
  
  ${ORGANIZATIONS_DOMAIN_SCRIPT_FRAGMENTS.rolePermissions}
        function normalizePlaygroundPackages(packages) {
          const source = packages && typeof packages === "object" ? packages : {};
          return {
            system: Array.isArray(source.system) ? source.system.map((value) => String(value || "")).filter(Boolean) : [],
            python: Array.isArray(source.python) ? source.python.map((value) => String(value || "")).filter(Boolean) : [],
            node: Array.isArray(source.node) ? source.node.map((value) => String(value || "")).filter(Boolean) : [],
          };
        }
  
        function normalizePlaygroundEnvironmentVariables(items) {
          return (Array.isArray(items) ? items : []).map((item) => ({
            key: typeof item?.key === "string" ? item.key : "",
            value: typeof item?.value === "string" ? item.value : "",
          }));
        }
  
        function normalizePlaygroundMcpServers(items) {
          return (Array.isArray(items) ? items : []).map((item, index) => ({
            id: typeof item?.id === "string" && item.id.trim() ? item.id : "mcp-" + index + "-" + Date.now().toString(36),
            name: typeof item?.name === "string" ? item.name : "",
            enabled: item?.enabled !== false,
            type: item?.type === "http" ? "http" : "stdio",
            command: typeof item?.command === "string" ? item.command : "",
            url: typeof item?.url === "string" ? item.url : "",
            bearerToken: typeof item?.bearerToken === "string" ? item.bearerToken : "",
          }));
        }
  
        function normalizePlaygroundDocumentationFiles(items) {
          return (Array.isArray(items) ? items : []).map((item, index) => {
            if (typeof item === "string") {
              return {
                id: "doc-" + index + "-" + Date.now().toString(36),
                name: "Document " + (index + 1),
                content: item,
                mimeType: "text/plain",
              };
            }
  
            return {
              id: typeof item?.id === "string" && item.id.trim() ? item.id : "doc-" + index + "-" + Date.now().toString(36),
              name: typeof item?.name === "string" ? item.name : "Document " + (index + 1),
              content: typeof item?.content === "string" ? item.content : "",
              mimeType: typeof item?.mimeType === "string" ? item.mimeType : "text/plain",
            };
          });
        }
  
        function normalizePlaygroundEnvironmentRecord(environment) {
          if (!environment || typeof environment !== "object") {
            return buildPlaygroundDefaultEnvironmentDraft();
          }
  
          const draft = buildPlaygroundDefaultEnvironmentDraft();
          const rawRuntimes = environment?.runtimes && typeof environment.runtimes === "object"
            ? environment.runtimes
            : environment?.packageVersions && typeof environment.packageVersions === "object"
              ? environment.packageVersions
              : draft.runtimes;
          const runtimes = Object.keys(rawRuntimes || {}).length > 0
            ? Object.fromEntries(
                Object.entries(rawRuntimes).filter(([, value]) => typeof value === "string" && value.trim())
              )
            : draft.runtimes;
          const normalizedMetadata = environment?.metadata && typeof environment.metadata === "object" && !Array.isArray(environment.metadata)
            ? environment.metadata
            : null;
          const computeProfile = resolvePlaygroundEnvironmentComputeProfileId({
            ...environment,
            metadata: normalizedMetadata,
          });
          const profileConfig = getPlaygroundEnvironmentComputeProfileConfig(computeProfile);
          const guiEnabled = typeof environment?.guiEnabled === "boolean"
            ? environment.guiEnabled
            : normalizedMetadata?.guiEnabled === true
              ? true
              : profileConfig.guiEnabled;
          const officeAppsEnabled = typeof environment?.officeAppsEnabled === "boolean"
            ? environment.officeAppsEnabled
            : normalizedMetadata?.officeAppsEnabled === true;
          const normalizedEnvironment = {
            ...draft,
            id: typeof environment.id === "string" ? environment.id : draft.id,
            userId: typeof environment.userId === "string" ? environment.userId : draft.userId,
            name: typeof environment.name === "string" && environment.name.trim() ? environment.name : draft.name,
            description: typeof environment.description === "string" ? environment.description : draft.description,
            runtimes,
            packageVersions: {
              ...(runtimes || {}),
            },
            packages: normalizePlaygroundPackages(environment.packages),
            environmentVariables: normalizePlaygroundEnvironmentVariables(environment.environmentVariables),
            secrets: normalizePlaygroundEnvironmentVariables(environment.secrets),
            setupScripts: Array.isArray(environment.setupScripts)
              ? environment.setupScripts.map((value) => String(value || ""))
              : [],
            mcpServers: normalizePlaygroundMcpServers(environment.mcpServers),
            documentation: normalizePlaygroundDocumentationFiles(environment.documentation),
            internetAccess: environment.internetAccess !== false,
            guiEnabled,
            officeAppsEnabled,
            computeProfile,
            dockerfileExtensions: typeof environment.dockerfileExtensions === "string" ? environment.dockerfileExtensions : "",
            baseImage: typeof environment.baseImage === "string" ? environment.baseImage : "",
            metadata: normalizedMetadata,
            status: ["running", "starting", "stopped", "error"].includes(String(environment.status || "").trim().toLowerCase())
              ? String(environment.status || "").trim().toLowerCase()
              : draft.status,
            estimatedStorageMB: Number.isFinite(environment.estimatedStorageMB) ? environment.estimatedStorageMB : draft.estimatedStorageMB,
            estimatedCostPerMinute: getPlaygroundEnvironmentRatePerMinute({
              ...environment,
              computeProfile,
              metadata: normalizedMetadata,
              guiEnabled,
              officeAppsEnabled,
            }),
            isSystem: Boolean(environment.isSystem),
            isDefault: Boolean(environment.isDefault),
            isActive: environment.isActive !== false,
            createdAt: typeof environment.createdAt === "string" && environment.createdAt ? environment.createdAt : draft.createdAt,
            updatedAt: typeof environment.updatedAt === "string" && environment.updatedAt ? environment.updatedAt : draft.updatedAt,
          };
  
          return applyPlaygroundEnvironmentComputeProfileDraft(
            normalizedEnvironment,
            computeProfile,
            { officeAppsEnabled }
          );
        }
  
        function parsePlaygroundEnvironmentListResponse(data) {
          const items = Array.isArray(data?.data)
            ? data.data
            : Array.isArray(data?.environments)
              ? data.environments
              : Array.isArray(data?.items)
                ? data.items
                : [];
          return items.map(normalizePlaygroundEnvironmentRecord);
        }
  
        function getPlaygroundEnvironmentResponseRecord(data) {
          const source = data?.environment || data?.data || data;
          return source && typeof source === "object" && typeof source.id === "string"
            ? normalizePlaygroundEnvironmentRecord(source)
            : null;
        }
  
        function stripPlaygroundEnvironmentVersionMetadata(metadata) {
          const source = metadata && typeof metadata === "object" && !Array.isArray(metadata)
            ? { ...metadata }
            : {};
          delete source.environmentVersions;
          delete source.environment_versions;
          delete source.computerVersions;
          delete source.computer_versions;
          delete source.versions;
          delete source.activeEnvironmentVersionId;
          delete source.active_environment_version_id;
          delete source.activeComputerVersionId;
          delete source.active_computer_version_id;
          delete source.activeEnvironmentVersionNumber;
          delete source.active_environment_version_number;
          delete source.activeComputerVersionNumber;
          delete source.active_computer_version_number;
          delete source.restoredFromEnvironmentVersionId;
          delete source.restored_from_environment_version_id;
          delete source.restoredFromComputerVersionId;
          delete source.restored_from_computer_version_id;
          delete source.restoredFromEnvironmentVersionNumber;
          delete source.restored_from_environment_version_number;
          delete source.restoredFromComputerVersionNumber;
          delete source.restored_from_computer_version_number;
          delete source.publishedAt;
          delete source.published_at;
          delete source.unpublishedAt;
          delete source.unpublished_at;
          return source;
        }
  
        function normalizePlaygroundEnvironmentVersion(rawVersion, fallbackIndex = 0) {
          const version = rawVersion && typeof rawVersion === "object" && !Array.isArray(rawVersion) ? rawVersion : {};
          const snapshot = version.snapshot && typeof version.snapshot === "object" && !Array.isArray(version.snapshot)
            ? version.snapshot
            : {};
          const createdAt = String(version.createdAt || version.created_at || version.publishedAt || version.published_at || new Date().toISOString()).trim();
          const id = String(version.id || version.versionId || version.version_id || ("environment_version_" + (fallbackIndex + 1))).trim();
          const versionNumber = Number(version.version || version.versionNumber || version.version_number || 0) || (fallbackIndex + 1);
          const rawStatus = String(version.status || "").trim().toLowerCase();
          const status = rawStatus === "published"
            ? "active"
            : ["active", "saved", "superseded", "unpublished"].includes(rawStatus) ? rawStatus : "saved";
          const rawLifecycleState = String(version.lifecycleState || version.lifecycle_state || "").trim().toLowerCase();
          const lifecycleState = ["published", "saved", "draft", "deprecated", "unpublished", "archived"].includes(rawLifecycleState)
            ? rawLifecycleState
            : status === "active"
              ? "published"
              : status === "superseded"
                ? "deprecated"
                : status === "unpublished"
                  ? "unpublished"
                  : "saved";
          const rawRuntimes = version.runtimes && typeof version.runtimes === "object" && !Array.isArray(version.runtimes)
            ? version.runtimes
            : snapshot.runtimes && typeof snapshot.runtimes === "object" && !Array.isArray(snapshot.runtimes)
              ? snapshot.runtimes
              : {};
          const runtimes = Object.fromEntries(
            Object.entries(rawRuntimes).filter(([, value]) => typeof value === "string" && value.trim())
          );
          const normalizedSnapshot = {
            name: String(version.name || snapshot.name || "").trim(),
  	          description: typeof snapshot.description === "string"
  	            ? snapshot.description
  	            : typeof version.agentDescription === "string"
  	              ? version.agentDescription
  	              : typeof version.agent_description === "string"
  	                ? version.agent_description
  	                : typeof version.description === "string"
  	                  ? version.description
  	                  : "",
            runtimes,
            packageVersions: { ...runtimes },
            packages: normalizePlaygroundPackages(version.packages || snapshot.packages),
            environmentVariables: normalizePlaygroundEnvironmentVariables(version.environmentVariables || snapshot.environmentVariables),
            secrets: normalizePlaygroundEnvironmentVariables(version.secrets || snapshot.secrets),
            setupScripts: Array.isArray(version.setupScripts)
              ? version.setupScripts.map((value) => String(value || ""))
              : Array.isArray(snapshot.setupScripts)
                ? snapshot.setupScripts.map((value) => String(value || ""))
                : [],
            mcpServers: normalizePlaygroundMcpServers(version.mcpServers || snapshot.mcpServers),
            documentation: normalizePlaygroundDocumentationFiles(version.documentation || snapshot.documentation),
            internetAccess: (Object.prototype.hasOwnProperty.call(version, "internetAccess") ? version.internetAccess : snapshot.internetAccess) !== false,
            guiEnabled: Boolean(Object.prototype.hasOwnProperty.call(version, "guiEnabled") ? version.guiEnabled : snapshot.guiEnabled),
            officeAppsEnabled: Boolean(Object.prototype.hasOwnProperty.call(version, "officeAppsEnabled") ? version.officeAppsEnabled : snapshot.officeAppsEnabled),
            computeProfile: normalizePlaygroundEnvironmentComputeProfileId(version.computeProfile || snapshot.computeProfile)
              || PLAYGROUND_DEFAULT_CUSTOM_ENVIRONMENT_COMPUTE_PROFILE,
            dockerfileExtensions: typeof version.dockerfileExtensions === "string"
              ? version.dockerfileExtensions
              : typeof snapshot.dockerfileExtensions === "string"
                ? snapshot.dockerfileExtensions
                : "",
            baseImage: typeof version.baseImage === "string"
              ? version.baseImage
              : typeof snapshot.baseImage === "string"
                ? snapshot.baseImage
                : "",
            metadata: stripPlaygroundEnvironmentVersionMetadata(snapshot.metadata),
          };
  
          return {
            id,
            version: versionNumber,
            label: String(version.label || version.name || ("Version " + versionNumber)).trim(),
            description: String(version.description || version.summary || "").trim(),
            status,
            lifecycleState,
            lifecycle_state: lifecycleState,
            createdAt,
            created_at: createdAt,
            updatedAt: String(version.updatedAt || version.updated_at || "").trim(),
            updated_at: String(version.updatedAt || version.updated_at || "").trim(),
            publishedAt: String(version.publishedAt || version.published_at || "").trim(),
            published_at: String(version.publishedAt || version.published_at || "").trim(),
            unpublishedAt: String(version.unpublishedAt || version.unpublished_at || "").trim(),
            unpublished_at: String(version.unpublishedAt || version.unpublished_at || "").trim(),
            revisionId: String(version.revisionId || version.revision_id || "").trim(),
            revision_id: String(version.revisionId || version.revision_id || "").trim(),
            baseRevisionId: String(version.baseRevisionId || version.base_revision_id || "").trim(),
            base_revision_id: String(version.baseRevisionId || version.base_revision_id || "").trim(),
            revisionNumber: Number(version.revisionNumber || version.revision_number || versionNumber || 0) || versionNumber,
            revision_number: Number(version.revisionNumber || version.revision_number || versionNumber || 0) || versionNumber,
            createdBy: normalizePlaygroundVersionActor(version.createdBy || version.created_by),
            created_by: normalizePlaygroundVersionActor(version.createdBy || version.created_by),
            updatedBy: normalizePlaygroundVersionActor(version.updatedBy || version.updated_by),
            updated_by: normalizePlaygroundVersionActor(version.updatedBy || version.updated_by),
            publishedBy: normalizePlaygroundVersionActor(version.publishedBy || version.published_by),
            published_by: normalizePlaygroundVersionActor(version.publishedBy || version.published_by),
            unpublishedBy: normalizePlaygroundVersionActor(version.unpublishedBy || version.unpublished_by),
            unpublished_by: normalizePlaygroundVersionActor(version.unpublishedBy || version.unpublished_by),
            deploymentId: String(version.deploymentId || version.deployment_id || "").trim(),
            deployment_id: String(version.deploymentId || version.deployment_id || "").trim(),
            deploymentStatus: String(version.deploymentStatus || version.deployment_status || "").trim(),
            deployment_status: String(version.deploymentStatus || version.deployment_status || "").trim(),
            name: normalizedSnapshot.name,
            computeProfile: normalizedSnapshot.computeProfile,
            runtimeCount: Object.keys(normalizedSnapshot.runtimes || {}).length,
            snapshot: normalizedSnapshot,
          };
        }
  
        function normalizePlaygroundEnvironmentVersions(value) {
          const rawItems = Array.isArray(value) ? value : [];
          return rawItems
            .map((version, index) => normalizePlaygroundEnvironmentVersion(version, index))
            .filter((version) => version.id)
            .sort((a, b) => {
              const versionDelta = Number(b.version || 0) - Number(a.version || 0);
              if (versionDelta) return versionDelta;
              return new Date(b.publishedAt || b.createdAt || 0).getTime() - new Date(a.publishedAt || a.createdAt || 0).getTime();
            });
        }
  
        function readPlaygroundEnvironmentVersions(environment) {
          const metadata = environment?.metadata && typeof environment.metadata === "object" && !Array.isArray(environment.metadata)
            ? environment.metadata
            : {};
          return normalizePlaygroundEnvironmentVersions(
            environment?.environmentVersions
            || environment?.computerVersions
            || environment?.versions
            || metadata.environmentVersions
            || metadata.environment_versions
            || metadata.computerVersions
            || metadata.computer_versions
            || metadata.versions
            || []
          );
        }
  
        function createPlaygroundEnvironmentVersionId() {
          return "environment_version_" + Date.now().toString(36) + "_" + Math.random().toString(36).slice(2, 8);
        }
  
        function createPlaygroundEnvironmentVersionRevisionId() {
          return "environment_revision_" + Date.now().toString(36) + "_" + Math.random().toString(36).slice(2, 8);
        }
  
        function createPlaygroundEnvironmentDeploymentId(versionId = "") {
          const normalizedVersionId = String(versionId || "").trim();
          return "environment_deployment_" + (normalizedVersionId || (Date.now().toString(36) + "_" + Math.random().toString(36).slice(2, 8)));
        }
  
        function buildPlaygroundEnvironmentVersionSnapshot(environment) {
          const normalizedEnvironment = normalizePlaygroundEnvironmentRecord(environment || buildPlaygroundDefaultEnvironmentDraft());
          const runtimes = Object.fromEntries(
            Object.entries(normalizedEnvironment.runtimes || {}).filter(([, value]) => typeof value === "string" && value.trim())
          );
          return {
            name: String(normalizedEnvironment.name || "").trim() || "Untitled Computer",
            description: typeof normalizedEnvironment.description === "string" ? normalizedEnvironment.description : "",
            runtimes,
            packageVersions: { ...runtimes },
            packages: normalizePlaygroundPackages(normalizedEnvironment.packages),
            environmentVariables: normalizePlaygroundEnvironmentVariables(normalizedEnvironment.environmentVariables),
            secrets: normalizePlaygroundEnvironmentVariables(normalizedEnvironment.secrets),
            setupScripts: Array.isArray(normalizedEnvironment.setupScripts)
              ? normalizedEnvironment.setupScripts.map((value) => String(value || ""))
              : [],
            mcpServers: normalizePlaygroundMcpServers(normalizedEnvironment.mcpServers),
            documentation: normalizePlaygroundDocumentationFiles(normalizedEnvironment.documentation),
            internetAccess: normalizedEnvironment.internetAccess !== false,
            guiEnabled: normalizedEnvironment.guiEnabled === true,
            officeAppsEnabled: normalizedEnvironment.officeAppsEnabled === true,
            computeProfile: normalizePlaygroundEnvironmentComputeProfileId(normalizedEnvironment.computeProfile)
              || PLAYGROUND_DEFAULT_CUSTOM_ENVIRONMENT_COMPUTE_PROFILE,
            dockerfileExtensions: typeof normalizedEnvironment.dockerfileExtensions === "string" ? normalizedEnvironment.dockerfileExtensions : "",
            baseImage: typeof normalizedEnvironment.baseImage === "string" ? normalizedEnvironment.baseImage : "",
            metadata: stripPlaygroundEnvironmentVersionMetadata(normalizedEnvironment.metadata),
          };
        }
  
        function createPlaygroundEnvironmentVersion(environment, existingVersions = [], options = {}) {
          const now = new Date().toISOString();
          const normalizedExisting = normalizePlaygroundEnvironmentVersions(existingVersions);
          const nextVersion = normalizedExisting.reduce((maxVersion, version) => Math.max(maxVersion, Number(version.version || 0)), 0) + 1;
          const requestedStatus = String(options?.status || "saved").trim().toLowerCase();
          const status = requestedStatus === "active" ? "active" : "saved";
          const snapshot = buildPlaygroundEnvironmentVersionSnapshot(environment);
          const actor = normalizePlaygroundVersionActor(options?.actor);
          const revisionId = createPlaygroundEnvironmentVersionRevisionId();
          const versionId = createPlaygroundEnvironmentVersionId();
          const deploymentId = status === "active" ? createPlaygroundEnvironmentDeploymentId(versionId) : "";
          return normalizePlaygroundEnvironmentVersion({
            id: versionId,
            version: nextVersion,
            label: String(options?.label || ("Version " + nextVersion)).trim(),
            description: String(options?.description || "").trim(),
            status,
            lifecycleState: status === "active" ? "published" : "saved",
            lifecycle_state: status === "active" ? "published" : "saved",
            createdAt: now,
            created_at: now,
            updatedAt: now,
            updated_at: now,
            createdBy: actor,
            created_by: actor,
            updatedBy: actor,
            updated_by: actor,
            publishedAt: status === "active" ? now : "",
            published_at: status === "active" ? now : "",
            publishedBy: status === "active" ? actor : null,
            published_by: status === "active" ? actor : null,
            revisionId,
            revision_id: revisionId,
            revisionNumber: nextVersion,
            revision_number: nextVersion,
            deploymentId,
            deployment_id: deploymentId,
            deploymentStatus: status === "active" ? "published" : "",
            deployment_status: status === "active" ? "published" : "",
            name: snapshot.name,
            computeProfile: snapshot.computeProfile,
            runtimes: snapshot.runtimes,
            snapshot,
          }, nextVersion - 1);
        }
  
        function createPlaygroundEnvironmentWithVersionList(environment, versions, preferredSelectedId = "") {
          const baseEnvironment = normalizePlaygroundEnvironmentRecord(environment || buildPlaygroundDefaultEnvironmentDraft());
          const normalizedVersions = normalizePlaygroundEnvironmentVersions(versions);
          const metadata = baseEnvironment.metadata && typeof baseEnvironment.metadata === "object" && !Array.isArray(baseEnvironment.metadata)
            ? { ...baseEnvironment.metadata }
            : {};
          const previousSelectedId = String(
            metadata.restoredFromEnvironmentVersionId
            || metadata.restored_from_environment_version_id
            || metadata.restoredFromComputerVersionId
            || metadata.restored_from_computer_version_id
            || metadata.activeEnvironmentVersionId
            || metadata.active_environment_version_id
            || metadata.activeComputerVersionId
            || metadata.active_computer_version_id
            || ""
          ).trim();
          const selectedVersion = normalizedVersions.find((version) => version.id === String(preferredSelectedId || "").trim())
            || normalizedVersions.find((version) => version.id === previousSelectedId)
            || normalizedVersions.find((version) => version.status === "active")
            || normalizedVersions[0]
            || null;
          const activeVersion = normalizedVersions.find((version) => version.status === "active")
            || normalizedVersions.find((version) => (
              version.id === String(metadata.activeEnvironmentVersionId || metadata.active_environment_version_id || metadata.activeComputerVersionId || metadata.active_computer_version_id || "").trim()
            ))
            || null;
          metadata.environmentVersions = normalizedVersions;
          metadata.environment_versions = normalizedVersions;
          metadata.computerVersions = normalizedVersions;
          metadata.computer_versions = normalizedVersions;
          metadata.activeEnvironmentVersionId = activeVersion?.id || "";
          metadata.active_environment_version_id = activeVersion?.id || "";
          metadata.activeComputerVersionId = activeVersion?.id || "";
          metadata.active_computer_version_id = activeVersion?.id || "";
          metadata.activeEnvironmentVersionNumber = activeVersion?.version || 0;
          metadata.active_environment_version_number = activeVersion?.version || 0;
          metadata.activeComputerVersionNumber = activeVersion?.version || 0;
          metadata.active_computer_version_number = activeVersion?.version || 0;
          metadata.restoredFromEnvironmentVersionId = selectedVersion?.id || "";
          metadata.restored_from_environment_version_id = selectedVersion?.id || "";
          metadata.restoredFromComputerVersionId = selectedVersion?.id || "";
          metadata.restored_from_computer_version_id = selectedVersion?.id || "";
          metadata.restoredFromEnvironmentVersionNumber = selectedVersion?.version || 0;
          metadata.restored_from_environment_version_number = selectedVersion?.version || 0;
          metadata.restoredFromComputerVersionNumber = selectedVersion?.version || 0;
          metadata.restored_from_computer_version_number = selectedVersion?.version || 0;
          if (activeVersion?.publishedAt) {
            metadata.publishedAt = activeVersion.publishedAt;
            metadata.published_at = activeVersion.publishedAt;
          } else {
            delete metadata.publishedAt;
            delete metadata.published_at;
          }
          return normalizePlaygroundEnvironmentRecord({
            ...baseEnvironment,
            metadata,
            publishedAt: activeVersion?.publishedAt || "",
          });
        }
  
        function createPlaygroundEnvironmentFromVersionSnapshot(environment, version, versions, preferredSelectedId = "") {
          const baseEnvironment = normalizePlaygroundEnvironmentRecord(environment || buildPlaygroundDefaultEnvironmentDraft());
          const normalizedVersion = normalizePlaygroundEnvironmentVersion(version || {});
          const snapshot = normalizedVersion.snapshot || {};
          const baseMetadata = stripPlaygroundEnvironmentVersionMetadata(baseEnvironment.metadata);
          const snapshotMetadata = stripPlaygroundEnvironmentVersionMetadata(snapshot.metadata);
          const nextEnvironment = normalizePlaygroundEnvironmentRecord({
            ...baseEnvironment,
            name: snapshot.name || baseEnvironment.name,
            description: typeof snapshot.description === "string" ? snapshot.description : baseEnvironment.description,
            runtimes: snapshot.runtimes || baseEnvironment.runtimes,
            packageVersions: snapshot.packageVersions || snapshot.runtimes || baseEnvironment.packageVersions,
            packages: snapshot.packages || baseEnvironment.packages,
            environmentVariables: Array.isArray(snapshot.environmentVariables) ? snapshot.environmentVariables : baseEnvironment.environmentVariables,
            secrets: Array.isArray(snapshot.secrets) ? snapshot.secrets : baseEnvironment.secrets,
            setupScripts: Array.isArray(snapshot.setupScripts) ? snapshot.setupScripts : baseEnvironment.setupScripts,
            mcpServers: Array.isArray(snapshot.mcpServers) ? snapshot.mcpServers : baseEnvironment.mcpServers,
            documentation: Array.isArray(snapshot.documentation) ? snapshot.documentation : baseEnvironment.documentation,
            internetAccess: snapshot.internetAccess !== false,
            guiEnabled: snapshot.guiEnabled === true,
            officeAppsEnabled: snapshot.officeAppsEnabled === true,
            computeProfile: snapshot.computeProfile || baseEnvironment.computeProfile,
            dockerfileExtensions: typeof snapshot.dockerfileExtensions === "string" ? snapshot.dockerfileExtensions : baseEnvironment.dockerfileExtensions,
            baseImage: typeof snapshot.baseImage === "string" ? snapshot.baseImage : baseEnvironment.baseImage,
            metadata: {
              ...baseMetadata,
              ...snapshotMetadata,
            },
          });
          return createPlaygroundEnvironmentWithVersionList(nextEnvironment, versions, preferredSelectedId || normalizedVersion.id);
        }
  
        function normalizePlaygroundServerRecord(server) {
          if (!server || typeof server !== "object") {
            return buildPlaygroundDefaultServerDraft();
          }
  
          const draft = buildPlaygroundDefaultServerDraft();
          const metadata = server?.metadata && typeof server.metadata === "object" && !Array.isArray(server.metadata)
            ? server.metadata
            : null;
          const runnerPlayground = metadata?.runnerPlayground && typeof metadata.runnerPlayground === "object" && !Array.isArray(metadata.runnerPlayground)
            ? metadata.runnerPlayground
            : null;
          const databaseBinding = runnerPlayground?.database && typeof runnerPlayground.database === "object" && !Array.isArray(runnerPlayground.database)
            ? runnerPlayground.database
            : null;
          const databaseId = typeof server.databaseId === "string" && server.databaseId.trim()
            ? server.databaseId.trim()
            : typeof databaseBinding?.id === "string" && databaseBinding.id.trim()
              ? databaseBinding.id.trim()
              : "";
          const databaseName = typeof server.databaseName === "string" && server.databaseName.trim()
            ? server.databaseName.trim()
            : typeof databaseBinding?.name === "string"
              ? databaseBinding.name
              : "";
          const databaseDescription = typeof server.databaseDescription === "string"
            ? server.databaseDescription
            : typeof databaseBinding?.description === "string"
              ? databaseBinding.description
              : "";
          const databaseLocation = typeof server.databaseLocation === "string" && server.databaseLocation.trim()
            ? server.databaseLocation.trim()
            : typeof databaseBinding?.location === "string" && databaseBinding.location.trim()
              ? databaseBinding.location.trim()
              : draft.databaseLocation;
  	        const databaseMode = typeof server.databaseMode === "string" && ["none", "existing", "create"].includes(server.databaseMode)
  	          ? server.databaseMode
  	          : databaseId
  	            ? "existing"
  	            : databaseName || databaseDescription
  	              ? "create"
  	              : "none";
  	        const normalizedKind = canonicalizePlaygroundServerKind(server.kind || draft.kind);
  
  	        return {
  	          ...draft,
            id: typeof server.id === "string" ? server.id : draft.id,
            userId: typeof server.userId === "string" ? server.userId : draft.userId,
            projectId: typeof server.projectId === "string" && server.projectId.trim() ? server.projectId.trim() : null,
            name: typeof server.name === "string" && server.name.trim() ? server.name : draft.name,
            description: typeof server.description === "string" ? server.description : draft.description,
  	          kind: normalizedKind,
            sourceType: ["manual", "computer", "git"].includes(server.sourceType) ? server.sourceType : draft.sourceType,
            sourceEnvironmentId: typeof server.sourceEnvironmentId === "string" && server.sourceEnvironmentId.trim() ? server.sourceEnvironmentId.trim() : null,
            sourcePath: typeof server.sourcePath === "string" ? server.sourcePath : draft.sourcePath,
            region: typeof server.region === "string" && server.region.trim() ? server.region.trim() : draft.region,
            runtime: typeof server.runtime === "string" && server.runtime.trim() ? server.runtime.trim() : draft.runtime,
  	          authMode: normalizedKind === "agent_runtime" ? "private" : (["public", "private"].includes(server.authMode) ? server.authMode : draft.authMode),
            serviceUrl: typeof server.serviceUrl === "string" ? server.serviceUrl : draft.serviceUrl,
            customDomain: typeof server.customDomain === "string" ? server.customDomain : draft.customDomain,
            cloudRunServiceName: typeof server.cloudRunServiceName === "string" ? server.cloudRunServiceName : draft.cloudRunServiceName,
            imageUrl: typeof server.imageUrl === "string" ? server.imageUrl : draft.imageUrl,
            status: ["draft", "deploying", "deployed", "failed", "inactive"].includes(server.status) ? server.status : draft.status,
            lastDeployedAt: typeof server.lastDeployedAt === "string" ? server.lastDeployedAt : draft.lastDeployedAt,
            template: typeof server.template === "string" && ["blank", "ai_chat_app"].includes(server.template) ? server.template : draft.template,
            templateAgentId: typeof server.templateAgentId === "string" ? server.templateAgentId : draft.templateAgentId,
            templateEnvironmentId: typeof server.templateEnvironmentId === "string" ? server.templateEnvironmentId : draft.templateEnvironmentId,
            templateDeploy: server.templateDeploy === true,
            databaseMode,
            databaseId,
            databaseName,
            databaseDescription,
            databaseLocation,
            metadata,
            createdAt: typeof server.createdAt === "string" && server.createdAt ? server.createdAt : draft.createdAt,
            updatedAt: typeof server.updatedAt === "string" && server.updatedAt ? server.updatedAt : draft.updatedAt,
          };
        }
  
        function stripPlaygroundServerVersionMetadata(metadata) {
          const source = metadata && typeof metadata === "object" && !Array.isArray(metadata)
            ? { ...metadata }
            : {};
          delete source.serverVersions;
          delete source.server_versions;
          delete source.versions;
          delete source.activeServerVersionId;
          delete source.active_server_version_id;
          delete source.activeServerVersionNumber;
          delete source.active_server_version_number;
          delete source.restoredFromServerVersionId;
          delete source.restored_from_server_version_id;
          delete source.restoredFromServerVersionNumber;
          delete source.restored_from_server_version_number;
          delete source.publishedAt;
          delete source.published_at;
          delete source.unpublishedAt;
          delete source.unpublished_at;
          delete source.activeServerDeployment;
          delete source.active_server_deployment;
          return source;
        }
  
        function normalizePlaygroundServerVersionSourceFiles(value) {
          return (Array.isArray(value) ? value : [])
            .map((entry, index) => {
              const path = normalizeHistoryPath(entry?.path || entry?.name || "");
              if (!path) {
                return null;
              }
              return {
                id: String(entry?.id || path || ("source-" + index)).trim(),
                name: String(entry?.name || path.split("/").pop() || path).trim(),
                path,
                isFolder: Boolean(entry?.isFolder),
                size: Number.isFinite(Number(entry?.size)) ? Number(entry.size) : 0,
                type: String(entry?.type || entry?.mimeType || "").trim(),
                modifiedTime: String(entry?.modifiedTime || entry?.updatedAt || "").trim(),
              };
            })
            .filter(Boolean)
            .sort((left, right) => String(left.path || "").localeCompare(String(right.path || "")));
        }
  
        function normalizePlaygroundServerVersionSourceFileContents(value) {
          const source = value && typeof value === "object" && !Array.isArray(value) ? value : {};
          return Object.fromEntries(
            Object.entries(source)
              .map(([path, content]) => [normalizeHistoryPath(path), typeof content === "string" ? content : String(content ?? "")])
              .filter(([path]) => path)
              .sort(([left], [right]) => left.localeCompare(right))
          );
        }
  
        function normalizePlaygroundServerVersion(rawVersion, fallbackIndex = 0) {
          const version = rawVersion && typeof rawVersion === "object" && !Array.isArray(rawVersion) ? rawVersion : {};
          const snapshot = version.snapshot && typeof version.snapshot === "object" && !Array.isArray(version.snapshot)
            ? version.snapshot
            : {};
          const createdAt = String(version.createdAt || version.created_at || version.publishedAt || version.published_at || new Date().toISOString()).trim();
          const id = String(version.id || version.versionId || version.version_id || ("server_version_" + (fallbackIndex + 1))).trim();
          const versionNumber = Number(version.version || version.versionNumber || version.version_number || 0) || (fallbackIndex + 1);
          const rawStatus = String(version.status || "").trim().toLowerCase();
          const status = ["active", "saved", "superseded", "unpublished"].includes(rawStatus) ? rawStatus : "saved";
          const rawLifecycleState = String(version.lifecycleState || version.lifecycle_state || "").trim().toLowerCase();
          const lifecycleState = ["published", "saved", "draft", "deprecated", "unpublished", "archived"].includes(rawLifecycleState)
            ? rawLifecycleState
            : status === "active"
              ? "published"
              : status === "superseded"
                ? "deprecated"
                : status === "unpublished"
                  ? "unpublished"
                  : "saved";
          const normalizedKind = canonicalizePlaygroundServerKind(version.kind || snapshot.kind || "web_app");
          const normalizedSnapshot = {
            name: String(version.name || snapshot.name || "").trim(),
            description: typeof snapshot.description === "string"
              ? snapshot.description
              : typeof version.description === "string"
                ? version.description
                : "",
            kind: normalizedKind,
            sourceType: ["manual", "computer", "git"].includes(snapshot.sourceType || version.sourceType) ? (snapshot.sourceType || version.sourceType) : "manual",
            sourceEnvironmentId: typeof snapshot.sourceEnvironmentId === "string" ? snapshot.sourceEnvironmentId : (typeof version.sourceEnvironmentId === "string" ? version.sourceEnvironmentId : ""),
            sourcePath: typeof snapshot.sourcePath === "string" ? snapshot.sourcePath : (typeof version.sourcePath === "string" ? version.sourcePath : ""),
            region: String(snapshot.region || version.region || "us-central1").trim() || "us-central1",
            runtime: String(version.runtime || snapshot.runtime || "nodejs22").trim() || "nodejs22",
            authMode: ["public", "private"].includes(snapshot.authMode || version.authMode) ? (snapshot.authMode || version.authMode) : "public",
            template: String(snapshot.template || version.template || "blank").trim() || "blank",
            templateAgentId: String(snapshot.templateAgentId || version.templateAgentId || "").trim(),
            templateEnvironmentId: String(snapshot.templateEnvironmentId || version.templateEnvironmentId || "").trim(),
            databaseMode: ["none", "existing", "create"].includes(snapshot.databaseMode || version.databaseMode) ? (snapshot.databaseMode || version.databaseMode) : "none",
            databaseId: String(snapshot.databaseId || version.databaseId || "").trim(),
            databaseName: String(snapshot.databaseName || version.databaseName || "").trim(),
            databaseDescription: typeof snapshot.databaseDescription === "string" ? snapshot.databaseDescription : "",
            databaseLocation: String(snapshot.databaseLocation || version.databaseLocation || "eur3").trim() || "eur3",
            customDomain: typeof snapshot.customDomain === "string" ? snapshot.customDomain : "",
            serviceUrl: typeof snapshot.serviceUrl === "string" ? snapshot.serviceUrl : "",
            cloudRunServiceName: typeof snapshot.cloudRunServiceName === "string" ? snapshot.cloudRunServiceName : "",
            imageUrl: typeof snapshot.imageUrl === "string" ? snapshot.imageUrl : "",
            lastDeployedAt: typeof snapshot.lastDeployedAt === "string" ? snapshot.lastDeployedAt : "",
            sourceFiles: normalizePlaygroundServerVersionSourceFiles(snapshot.sourceFiles || version.sourceFiles),
            sourceFileContents: normalizePlaygroundServerVersionSourceFileContents(snapshot.sourceFileContents || version.sourceFileContents),
            metadata: stripPlaygroundServerVersionMetadata(snapshot.metadata),
          };
  
          return {
            id,
            version: versionNumber,
            label: String(version.label || version.name || ("Version " + versionNumber)).trim(),
            description: String(version.description || version.summary || "").trim(),
            status,
            lifecycleState,
            lifecycle_state: lifecycleState,
            createdAt,
            created_at: createdAt,
            updatedAt: String(version.updatedAt || version.updated_at || "").trim(),
            updated_at: String(version.updatedAt || version.updated_at || "").trim(),
            publishedAt: String(version.publishedAt || version.published_at || "").trim(),
            published_at: String(version.publishedAt || version.published_at || "").trim(),
            unpublishedAt: String(version.unpublishedAt || version.unpublished_at || "").trim(),
            unpublished_at: String(version.unpublishedAt || version.unpublished_at || "").trim(),
            revisionId: String(version.revisionId || version.revision_id || "").trim(),
            revision_id: String(version.revisionId || version.revision_id || "").trim(),
            baseRevisionId: String(version.baseRevisionId || version.base_revision_id || "").trim(),
            base_revision_id: String(version.baseRevisionId || version.base_revision_id || "").trim(),
            revisionNumber: Number(version.revisionNumber || version.revision_number || versionNumber || 0) || versionNumber,
            revision_number: Number(version.revisionNumber || version.revision_number || versionNumber || 0) || versionNumber,
            createdBy: normalizePlaygroundVersionActor(version.createdBy || version.created_by),
            created_by: normalizePlaygroundVersionActor(version.createdBy || version.created_by),
            updatedBy: normalizePlaygroundVersionActor(version.updatedBy || version.updated_by),
            updated_by: normalizePlaygroundVersionActor(version.updatedBy || version.updated_by),
            publishedBy: normalizePlaygroundVersionActor(version.publishedBy || version.published_by),
            published_by: normalizePlaygroundVersionActor(version.publishedBy || version.published_by),
            unpublishedBy: normalizePlaygroundVersionActor(version.unpublishedBy || version.unpublished_by),
            unpublished_by: normalizePlaygroundVersionActor(version.unpublishedBy || version.unpublished_by),
            deploymentId: String(version.deploymentId || version.deployment_id || "").trim(),
            deployment_id: String(version.deploymentId || version.deployment_id || "").trim(),
            deploymentStatus: String(version.deploymentStatus || version.deployment_status || "").trim(),
            deployment_status: String(version.deploymentStatus || version.deployment_status || "").trim(),
            name: normalizedSnapshot.name,
            kind: normalizedSnapshot.kind,
            runtime: normalizedSnapshot.runtime,
            sourceFileCount: normalizedSnapshot.sourceFiles.filter((entry) => !entry.isFolder).length,
            snapshot: normalizedSnapshot,
          };
        }
  
        function normalizePlaygroundServerVersions(value) {
          const rawItems = Array.isArray(value) ? value : [];
          return rawItems
            .map((version, index) => normalizePlaygroundServerVersion(version, index))
            .filter((version) => version.id)
            .sort((a, b) => {
              const versionDelta = Number(b.version || 0) - Number(a.version || 0);
              if (versionDelta) return versionDelta;
              return new Date(b.publishedAt || b.createdAt || 0).getTime() - new Date(a.publishedAt || a.createdAt || 0).getTime();
            });
        }
  
        function readPlaygroundServerVersions(server) {
          const metadata = server?.metadata && typeof server.metadata === "object" && !Array.isArray(server.metadata)
            ? server.metadata
            : {};
          return normalizePlaygroundServerVersions(
            server?.serverVersions
            || server?.versions
            || metadata.serverVersions
            || metadata.server_versions
            || metadata.versions
            || []
          );
        }
  
        function createPlaygroundServerVersionId() {
          return "server_version_" + Date.now().toString(36) + "_" + Math.random().toString(36).slice(2, 8);
        }
  
        function createPlaygroundServerVersionRevisionId() {
          return "server_revision_" + Date.now().toString(36) + "_" + Math.random().toString(36).slice(2, 8);
        }
  
        function createPlaygroundServerDeploymentId(versionId = "") {
          const normalizedVersionId = String(versionId || "").trim();
          return "server_deployment_" + (normalizedVersionId || (Date.now().toString(36) + "_" + Math.random().toString(36).slice(2, 8)));
        }
  
        function buildPlaygroundServerVersionSnapshot(server, options = {}) {
          const normalizedServer = normalizePlaygroundServerRecord(server || buildPlaygroundDefaultServerDraft());
          const sourceFileContents = normalizePlaygroundServerVersionSourceFileContents(options.sourceFileContents || {});
          const sourceFiles = normalizePlaygroundServerVersionSourceFiles(options.sourceFiles || []);
          return {
            name: String(normalizedServer.name || "").trim() || "Untitled Server",
            description: typeof normalizedServer.description === "string" ? normalizedServer.description : "",
            kind: canonicalizePlaygroundServerKind(normalizedServer.kind),
            sourceType: normalizedServer.sourceType || "manual",
            sourceEnvironmentId: normalizedServer.sourceEnvironmentId || "",
            sourcePath: normalizedServer.sourcePath || "",
            region: normalizedServer.region || "us-central1",
            runtime: normalizedServer.runtime || "nodejs22",
            authMode: normalizedServer.authMode || "public",
            template: normalizedServer.template || "blank",
            templateAgentId: normalizedServer.templateAgentId || "",
            templateEnvironmentId: normalizedServer.templateEnvironmentId || "",
            databaseMode: normalizedServer.databaseMode || "none",
            databaseId: normalizedServer.databaseId || "",
            databaseName: normalizedServer.databaseName || "",
            databaseDescription: typeof normalizedServer.databaseDescription === "string" ? normalizedServer.databaseDescription : "",
            databaseLocation: normalizedServer.databaseLocation || "eur3",
            customDomain: typeof normalizedServer.customDomain === "string" ? normalizedServer.customDomain : "",
            serviceUrl: typeof normalizedServer.serviceUrl === "string" ? normalizedServer.serviceUrl : "",
            cloudRunServiceName: typeof normalizedServer.cloudRunServiceName === "string" ? normalizedServer.cloudRunServiceName : "",
            imageUrl: typeof normalizedServer.imageUrl === "string" ? normalizedServer.imageUrl : "",
            lastDeployedAt: typeof normalizedServer.lastDeployedAt === "string" ? normalizedServer.lastDeployedAt : "",
            sourceFiles,
            sourceFileContents,
            metadata: stripPlaygroundServerVersionMetadata(buildPlaygroundServerMetadata(normalizedServer)),
          };
        }
  
        function createPlaygroundServerVersion(server, existingVersions = [], options = {}) {
          const now = new Date().toISOString();
          const normalizedExisting = normalizePlaygroundServerVersions(existingVersions);
          const nextVersion = normalizedExisting.reduce((maxVersion, version) => Math.max(maxVersion, Number(version.version || 0)), 0) + 1;
          const requestedStatus = String(options?.status || "saved").trim().toLowerCase();
          const status = requestedStatus === "active" ? "active" : "saved";
          const snapshot = options?.snapshot && typeof options.snapshot === "object" && !Array.isArray(options.snapshot)
            ? normalizePlaygroundServerVersion({ snapshot: options.snapshot }).snapshot
            : buildPlaygroundServerVersionSnapshot(server, options);
          const actor = normalizePlaygroundVersionActor(options?.actor);
          const revisionId = createPlaygroundServerVersionRevisionId();
          const versionId = createPlaygroundServerVersionId();
          const deploymentId = status === "active" ? createPlaygroundServerDeploymentId(versionId) : "";
          return normalizePlaygroundServerVersion({
            id: versionId,
            version: nextVersion,
            label: String(options?.label || ("Version " + nextVersion)).trim(),
            description: String(options?.description || "").trim(),
            status,
            lifecycleState: status === "active" ? "published" : "saved",
            lifecycle_state: status === "active" ? "published" : "saved",
            createdAt: now,
            created_at: now,
            updatedAt: now,
            updated_at: now,
            createdBy: actor,
            created_by: actor,
            updatedBy: actor,
            updated_by: actor,
            publishedAt: status === "active" ? now : "",
            published_at: status === "active" ? now : "",
            publishedBy: status === "active" ? actor : null,
            published_by: status === "active" ? actor : null,
            revisionId,
            revision_id: revisionId,
            revisionNumber: nextVersion,
            revision_number: nextVersion,
            deploymentId,
            deployment_id: deploymentId,
            deploymentStatus: status === "active" ? "published" : "",
            deployment_status: status === "active" ? "published" : "",
            name: snapshot.name,
            kind: snapshot.kind,
            runtime: snapshot.runtime,
            sourceFiles: snapshot.sourceFiles,
            sourceFileContents: snapshot.sourceFileContents,
            snapshot,
          }, nextVersion - 1);
        }
  
        function createPlaygroundServerWithVersionList(server, versions, preferredSelectedId = "") {
          const baseServer = normalizePlaygroundServerRecord(server || buildPlaygroundDefaultServerDraft());
          const normalizedVersions = normalizePlaygroundServerVersions(versions);
          const metadata = baseServer.metadata && typeof baseServer.metadata === "object" && !Array.isArray(baseServer.metadata)
            ? { ...baseServer.metadata }
            : {};
          const previousSelectedId = String(
            metadata.restoredFromServerVersionId
            || metadata.restored_from_server_version_id
            || metadata.activeServerVersionId
            || metadata.active_server_version_id
            || ""
          ).trim();
          const selectedVersion = normalizedVersions.find((version) => version.id === String(preferredSelectedId || "").trim())
            || normalizedVersions.find((version) => version.id === previousSelectedId)
            || normalizedVersions.find((version) => version.status === "active")
            || normalizedVersions[0]
            || null;
          const activeVersion = normalizedVersions.find((version) => version.status === "active")
            || normalizedVersions.find((version) => version.id === String(metadata.activeServerVersionId || metadata.active_server_version_id || "").trim())
            || null;
          metadata.serverVersions = normalizedVersions;
          metadata.server_versions = normalizedVersions;
          metadata.activeServerVersionId = activeVersion?.id || "";
          metadata.active_server_version_id = activeVersion?.id || "";
          metadata.activeServerVersionNumber = activeVersion?.version || 0;
          metadata.active_server_version_number = activeVersion?.version || 0;
          metadata.restoredFromServerVersionId = selectedVersion?.id || "";
          metadata.restored_from_server_version_id = selectedVersion?.id || "";
          metadata.restoredFromServerVersionNumber = selectedVersion?.version || 0;
          metadata.restored_from_server_version_number = selectedVersion?.version || 0;
          if (activeVersion?.publishedAt) {
            metadata.publishedAt = activeVersion.publishedAt;
            metadata.published_at = activeVersion.publishedAt;
            metadata.activeServerDeployment = {
              id: activeVersion.deploymentId || activeVersion.deployment_id || createPlaygroundServerDeploymentId(activeVersion.id),
              versionId: activeVersion.id,
              version: activeVersion.version,
              status: "published",
              publishedAt: activeVersion.publishedAt,
              publishedBy: activeVersion.publishedBy || activeVersion.published_by || null,
            };
            metadata.active_server_deployment = metadata.activeServerDeployment;
          } else {
            delete metadata.publishedAt;
            delete metadata.published_at;
            delete metadata.activeServerDeployment;
            delete metadata.active_server_deployment;
          }
          return normalizePlaygroundServerRecord({
            ...baseServer,
            metadata,
            publishedAt: activeVersion?.publishedAt || "",
          });
        }
  
        function createPlaygroundServerFromVersionSnapshot(server, version, versions, preferredSelectedId = "") {
          const baseServer = normalizePlaygroundServerRecord(server || buildPlaygroundDefaultServerDraft());
          const normalizedVersion = normalizePlaygroundServerVersion(version || {});
          const snapshot = normalizedVersion.snapshot || {};
          const baseMetadata = stripPlaygroundServerVersionMetadata(baseServer.metadata);
          const snapshotMetadata = stripPlaygroundServerVersionMetadata(snapshot.metadata);
          const nextServer = normalizePlaygroundServerRecord({
            ...baseServer,
            name: snapshot.name || baseServer.name,
            description: typeof snapshot.description === "string" ? snapshot.description : baseServer.description,
            kind: snapshot.kind || baseServer.kind,
            sourceType: snapshot.sourceType || baseServer.sourceType,
            sourceEnvironmentId: snapshot.sourceEnvironmentId || baseServer.sourceEnvironmentId,
            sourcePath: typeof snapshot.sourcePath === "string" ? snapshot.sourcePath : baseServer.sourcePath,
            region: snapshot.region || baseServer.region,
            runtime: snapshot.runtime || baseServer.runtime,
            authMode: snapshot.authMode || baseServer.authMode,
            template: snapshot.template || baseServer.template,
            templateAgentId: typeof snapshot.templateAgentId === "string" ? snapshot.templateAgentId : baseServer.templateAgentId,
            templateEnvironmentId: typeof snapshot.templateEnvironmentId === "string" ? snapshot.templateEnvironmentId : baseServer.templateEnvironmentId,
            databaseMode: snapshot.databaseMode || baseServer.databaseMode,
            databaseId: typeof snapshot.databaseId === "string" ? snapshot.databaseId : baseServer.databaseId,
            databaseName: typeof snapshot.databaseName === "string" ? snapshot.databaseName : baseServer.databaseName,
            databaseDescription: typeof snapshot.databaseDescription === "string" ? snapshot.databaseDescription : baseServer.databaseDescription,
            databaseLocation: snapshot.databaseLocation || baseServer.databaseLocation,
            customDomain: typeof snapshot.customDomain === "string" ? snapshot.customDomain : baseServer.customDomain,
            serviceUrl: typeof snapshot.serviceUrl === "string" ? snapshot.serviceUrl : baseServer.serviceUrl,
            cloudRunServiceName: typeof snapshot.cloudRunServiceName === "string" ? snapshot.cloudRunServiceName : baseServer.cloudRunServiceName,
            imageUrl: typeof snapshot.imageUrl === "string" ? snapshot.imageUrl : baseServer.imageUrl,
            lastDeployedAt: typeof snapshot.lastDeployedAt === "string" ? snapshot.lastDeployedAt : baseServer.lastDeployedAt,
            metadata: {
              ...baseMetadata,
              ...snapshotMetadata,
            },
          });
          return createPlaygroundServerWithVersionList(nextServer, versions, preferredSelectedId || normalizedVersion.id);
        }
  
        function getPlaygroundPaymentsMetadata(server) {
          const normalized = normalizePlaygroundServerRecord(server);
          const metadata = normalized?.metadata && typeof normalized.metadata === "object" && !Array.isArray(normalized.metadata)
            ? normalized.metadata
            : null;
          const payments = metadata?.payments && typeof metadata.payments === "object" && !Array.isArray(metadata.payments)
            ? metadata.payments
            : null;
          return payments || {};
        }
  
        function formatPlaygroundPaymentMoney(cents, currency = "usd") {
          const normalizedCurrency = String(currency || "usd").trim().toUpperCase() || "USD";
          const amount = Math.max(0, Number(cents || 0)) / 100;
          try {
            return new Intl.NumberFormat("en-US", {
              style: "currency",
              currency: normalizedCurrency,
              maximumFractionDigits: amount % 1 === 0 ? 0 : 2,
            }).format(amount);
          } catch {
            return "$" + amount.toFixed(amount % 1 === 0 ? 0 : 2);
          }
        }
  
        function formatPlaygroundPaymentsStatus(metadata) {
          const status = String(metadata?.onboardingStatus || "").trim().toLowerCase();
          if (status === "active" || (metadata?.chargesEnabled && metadata?.payoutsEnabled)) {
            return "Active";
          }
          if (status === "review") {
            return "In review";
          }
          if (status === "restricted") {
            return "Restricted";
          }
          if (metadata?.detailsSubmitted) {
            return "In review";
          }
          if (metadata?.stripeAccountId) {
            return "Setup incomplete";
          }
          return "Not connected";
        }
  
        function getPlaygroundServerDatabaseBinding(server) {
          const normalized = normalizePlaygroundServerRecord(server);
          return {
            mode: ["none", "existing", "create"].includes(normalized.databaseMode) ? normalized.databaseMode : "none",
            id: String(normalized.databaseId || "").trim(),
            name: String(normalized.databaseName || "").trim(),
            description: typeof normalized.databaseDescription === "string" ? normalized.databaseDescription : "",
            location: String(normalized.databaseLocation || "").trim() || "eur3",
          };
        }
  
        function getPlaygroundServerAgentRuntimeConfig(server) {
          const normalized = normalizePlaygroundServerRecord(server);
          const metadata = normalized?.metadata && typeof normalized.metadata === "object" && !Array.isArray(normalized.metadata)
            ? normalized.metadata
            : null;
          const agentRuntime = metadata?.agentRuntime && typeof metadata.agentRuntime === "object" && !Array.isArray(metadata.agentRuntime)
            ? metadata.agentRuntime
            : null;
          const skillPolicy = agentRuntime?.skills && typeof agentRuntime.skills === "object" && !Array.isArray(agentRuntime.skills)
            ? agentRuntime.skills
            : null;
          return {
            agentId: typeof agentRuntime?.agentId === "string" ? agentRuntime.agentId : "",
            executionMode: agentRuntime?.executionMode === "sync" ? "sync" : "async",
            streamingEnabled: agentRuntime?.streamingEnabled !== false,
            maxRuntimeSeconds: Number.isFinite(Number(agentRuntime?.maxRuntimeSeconds)) ? Math.max(30, Number(agentRuntime.maxRuntimeSeconds)) : 1800,
            skillsMode: skillPolicy?.mode === "override" ? "override" : "inherit",
            enabledSkills: normalizePlaygroundEnabledSkillIds(skillPolicy?.enabledSkills),
          };
        }
  
        function normalizePlaygroundServerCustomDomainState(domainState, fallbackDomain) {
          const primary = domainState && typeof domainState === "object" && !Array.isArray(domainState)
            ? domainState
            : null;
          const domain = String(primary?.domain || fallbackDomain || "").trim();
          if (!domain) {
            return null;
          }
          return {
            domain,
            provider: String(primary?.provider || "firebase_hosting"),
            status: String(primary?.status || "pending"),
            url: typeof primary?.url === "string" ? primary.url : "",
            records: Array.isArray(primary?.records) ? primary.records : [],
            conditions: Array.isArray(primary?.conditions) ? primary.conditions : [],
            verification: primary?.verification && typeof primary.verification === "object" && !Array.isArray(primary.verification) ? primary.verification : null,
            siteId: typeof primary?.siteId === "string"
              ? primary.siteId
              : typeof primary?.hosting?.siteId === "string"
                ? primary.hosting.siteId
                : "",
            hostState: typeof primary?.hostState === "string" ? primary.hostState : "",
            ownershipState: typeof primary?.ownershipState === "string" ? primary.ownershipState : "",
            certState: typeof primary?.certState === "string" ? primary.certState : "",
            lastCheckedAt: typeof primary?.lastCheckedAt === "string" ? primary.lastCheckedAt : "",
          };
        }
  
        function getPlaygroundServerCustomDomainStates(server) {
          const normalized = normalizePlaygroundServerRecord(server);
          const metadata = normalized?.metadata && typeof normalized.metadata === "object" && !Array.isArray(normalized.metadata)
            ? normalized.metadata
            : null;
          const domains = metadata?.customDomains && typeof metadata.customDomains === "object" && !Array.isArray(metadata.customDomains)
            ? Object.values(metadata.customDomains)
            : [];
          const primary = metadata?.customDomain && typeof metadata.customDomain === "object" && !Array.isArray(metadata.customDomain)
            ? metadata.customDomain
            : null;
          const states = [];
          const seen = new Set();
          [primary, ...domains].forEach((item) => {
            const normalizedState = normalizePlaygroundServerCustomDomainState(item, normalized.customDomain);
            if (!normalizedState || seen.has(normalizedState.domain)) {
              return;
            }
            seen.add(normalizedState.domain);
            states.push(normalizedState);
          });
          return states.sort((left, right) => {
            const leftTime = Date.parse(left.lastCheckedAt || "") || 0;
            const rightTime = Date.parse(right.lastCheckedAt || "") || 0;
            return rightTime - leftTime;
          });
        }
  
        function getPlaygroundServerCustomDomainState(server) {
          return getPlaygroundServerCustomDomainStates(server)[0] || null;
        }
  
        function formatPlaygroundCustomDomainStatus(value) {
          const status = String(value || "").trim();
          if (status === "connected") return "Connected";
          if (status === "verification_required") return "Verify domain";
          if (status === "needs_setup") return "Needs setup";
          if (status === "pending_dns") return "Pending DNS";
          return "Pending";
        }
  
        function buildPlaygroundServerMetadata(server) {
          const normalized = normalizePlaygroundServerRecord(server);
          const currentMetadata = normalized.metadata && typeof normalized.metadata === "object" && !Array.isArray(normalized.metadata)
            ? { ...normalized.metadata }
            : {};
          const currentRunnerPlayground = currentMetadata.runnerPlayground && typeof currentMetadata.runnerPlayground === "object" && !Array.isArray(currentMetadata.runnerPlayground)
            ? { ...currentMetadata.runnerPlayground }
            : {};
          const databaseBinding = getPlaygroundServerDatabaseBinding(normalized);
  
          if (databaseBinding.id) {
            currentRunnerPlayground.database = {
              id: databaseBinding.id,
              name: databaseBinding.name || "",
              description: databaseBinding.description || "",
              location: databaseBinding.location || "eur3",
            };
          } else {
            delete currentRunnerPlayground.database;
          }
  
          if (Object.keys(currentRunnerPlayground).length > 0) {
            currentMetadata.runnerPlayground = currentRunnerPlayground;
          } else {
            delete currentMetadata.runnerPlayground;
          }
  
          return Object.keys(currentMetadata).length > 0 ? currentMetadata : null;
        }
  
        function buildPlaygroundServerWithLinkedDatabase(server, database, mode = "existing") {
          const normalizedServer = normalizePlaygroundServerRecord(server);
          const normalizedDatabase = normalizePlaygroundDatabaseRecord(database);
          return normalizePlaygroundServerRecord({
            ...normalizedServer,
            databaseMode: mode === "create" ? "create" : "existing",
            databaseId: normalizedDatabase.id || "",
            databaseName: normalizedDatabase.name || "",
            databaseDescription: normalizedDatabase.description || "",
            databaseLocation: normalizedDatabase.location || "eur3",
          });
        }
  
        function clearPlaygroundServerDatabaseBinding(server) {
          const normalizedServer = normalizePlaygroundServerRecord(server);
          return normalizePlaygroundServerRecord({
            ...normalizedServer,
            databaseMode: "none",
            databaseId: "",
            databaseName: "",
            databaseDescription: "",
            databaseLocation: normalizedServer.databaseLocation || "eur3",
          });
        }
  
        function parsePlaygroundServerListResponse(data) {
          const items = Array.isArray(data?.data)
            ? data.data
            : Array.isArray(data?.servers)
              ? data.servers
              : Array.isArray(data?.items)
                ? data.items
                : [];
          return items.map(normalizePlaygroundServerRecord);
        }
  
        function getPlaygroundServerResponseRecord(data) {
          const source = data?.server || data?.data || data;
          return source && typeof source === "object" && typeof source.id === "string"
            ? normalizePlaygroundServerRecord(source)
            : null;
        }
  
        function normalizePlaygroundDatabaseRecord(database) {
          if (!database || typeof database !== "object") {
            return buildPlaygroundDefaultDatabaseDraft();
          }
  
          const draft = buildPlaygroundDefaultDatabaseDraft();
          const metadata = database?.metadata && typeof database.metadata === "object" && !Array.isArray(database.metadata)
            ? database.metadata
            : null;
          const permissionSetSource = database?.permissionSet && typeof database.permissionSet === "object" && !Array.isArray(database.permissionSet)
            ? database.permissionSet
            : metadata?.permissionSet && typeof metadata.permissionSet === "object" && !Array.isArray(metadata.permissionSet)
              ? metadata.permissionSet
              : metadata?.permission_set && typeof metadata.permission_set === "object" && !Array.isArray(metadata.permission_set)
                ? metadata.permission_set
                : null;
  
          return {
            ...draft,
            id: typeof database.id === "string" ? database.id : draft.id,
            userId: typeof database.userId === "string" ? database.userId : draft.userId,
            projectId: typeof database.projectId === "string" && database.projectId.trim() ? database.projectId.trim() : null,
            name: typeof database.name === "string" && database.name.trim() ? database.name : draft.name,
            description: typeof database.description === "string" ? database.description : draft.description,
            provider: "firestore",
            location: typeof database.location === "string" && database.location.trim() ? database.location.trim() : draft.location,
            status: ["active", "provisioning", "error"].includes(database.status) ? database.status : draft.status,
            firestoreNamespace: typeof database.firestoreNamespace === "string" ? database.firestoreNamespace : draft.firestoreNamespace,
            permissionSet: normalizePlaygroundPermissionSet(permissionSetSource, "database"),
            metadata,
            createdAt: typeof database.createdAt === "string" && database.createdAt ? database.createdAt : draft.createdAt,
            updatedAt: typeof database.updatedAt === "string" && database.updatedAt ? database.updatedAt : draft.updatedAt,
          };
        }
  
        function parsePlaygroundDatabaseListResponse(data) {
          const items = Array.isArray(data?.data)
            ? data.data
            : Array.isArray(data?.databases)
              ? data.databases
              : Array.isArray(data?.items)
                ? data.items
                : [];
          return items.map(normalizePlaygroundDatabaseRecord);
        }
  
        const PLAYGROUND_DATABASE_LIST_CACHE_TTL_MS = 30_000;
        const PLAYGROUND_DATABASE_LIST_STORAGE_PREFIX = "runner-playground:database-list:";
        const playgroundDatabaseListCache = new Map();
        const PLAYGROUND_DATABASE_ANALYTICS_CACHE_TTL_MS = 60_000;
        const PLAYGROUND_DATABASE_DETAIL_CACHE_TTL_MS = 30_000;
        const PLAYGROUND_DATABASE_COLLECTIONS_CACHE_TTL_MS = 300_000;
        const PLAYGROUND_DATABASE_DOCUMENTS_CACHE_TTL_MS = 10_000;
        const PLAYGROUND_DATABASE_RESOURCE_STORAGE_PREFIX = "runner-playground:database-resource:";
        const playgroundDatabaseResourceRequestCache = new Map();
  
        function getPlaygroundRequestHeaderValue(headers, names) {
          const normalizedNames = (Array.isArray(names) ? names : [names]).map((name) => String(name || "").toLowerCase());
          try {
            const normalizedHeaders = new Headers(headers || {});
            for (const name of normalizedNames) {
              const value = String(normalizedHeaders.get(name) || "").trim();
              if (value) return value;
            }
          } catch {}
          return "";
        }
  
        function hashPlaygroundDatabaseListScope(value) {
          let hash = 2166136261;
          const source = String(value || "");
          for (let index = 0; index < source.length; index += 1) {
            hash ^= source.charCodeAt(index);
            hash = Math.imul(hash, 16777619);
          }
          return (hash >>> 0).toString(36);
        }
  
        function buildPlaygroundDatabaseListScopeKey(backendUrl, headers, identity = "") {
          const organizationId = getPlaygroundRequestHeaderValue(headers, [
            "x-organization-id",
            "x-organization",
            "organization-id",
          ]);
          const authenticationIdentity = String(identity || "").trim()
            || getPlaygroundRequestHeaderValue(headers, "x-api-key")
            || "session";
          return String(backendUrl || "").replace(new RegExp("/+$"), "")
            + "|" + organizationId
            + "|" + hashPlaygroundDatabaseListScope(authenticationIdentity);
        }
  
        async function fetchPlaygroundCachedDatabaseResourceJson(url, headers, options = {}) {
          const normalizedUrl = String(url || "").trim();
          if (!normalizedUrl) {
            throw new Error("Database request URL is required.");
          }
          const scopeKey = String(options?.scopeKey || "database").trim();
          const cacheKey = scopeKey + "|" + normalizedUrl;
          const ttlMs = Math.max(0, Number(options?.ttlMs || 0));
          const force = options?.force === true;
          let cachedRecord = playgroundDatabaseResourceRequestCache.get(cacheKey);
          if (!cachedRecord && options?.persist === true) {
            try {
              const storageKey = PLAYGROUND_DATABASE_RESOURCE_STORAGE_PREFIX + hashPlaygroundDatabaseListScope(cacheKey);
              const storedRecord = JSON.parse(window.sessionStorage.getItem(storageKey) || "null");
              if (storedRecord?.cacheKey === cacheKey && storedRecord?.data) {
                cachedRecord = {
                  data: storedRecord.data,
                  loadedAt: Math.max(0, Number(storedRecord.loadedAt || 0)),
                  promise: null,
                };
                playgroundDatabaseResourceRequestCache.set(cacheKey, cachedRecord);
              }
            } catch {}
          }
          if (cachedRecord?.promise) {
            if (!force && options?.staleWhileRevalidate === true && cachedRecord?.data) {
              return cachedRecord.data;
            }
            if (!force) {
              return cachedRecord.promise;
            }
            try {
              await cachedRecord.promise;
            } catch {}
            cachedRecord = playgroundDatabaseResourceRequestCache.get(cacheKey);
          }
          const cacheAgeMs = cachedRecord ? Date.now() - Number(cachedRecord.loadedAt || 0) : Infinity;
          if (!force && cachedRecord?.data && cacheAgeMs >= 0 && cacheAgeMs < ttlMs) {
            return cachedRecord.data;
          }
          const shouldReturnStale = !force && options?.staleWhileRevalidate === true && Boolean(cachedRecord?.data);
  
          const request = (async () => {
            const response = await fetch(normalizedUrl, {
              method: "GET",
              headers,
              cache: "no-store",
              signal: options?.signal,
              priority: options?.priority || "auto",
            });
            const data = await response.json().catch(() => ({}));
            if (!response.ok) {
              throw new Error(data?.message || data?.error || "Failed to load database data.");
            }
            playgroundDatabaseResourceRequestCache.set(cacheKey, {
              data,
              loadedAt: Date.now(),
              promise: null,
            });
            if (options?.persist === true) {
              try {
                const storageKey = PLAYGROUND_DATABASE_RESOURCE_STORAGE_PREFIX + hashPlaygroundDatabaseListScope(cacheKey);
                window.sessionStorage.setItem(storageKey, JSON.stringify({
                  cacheKey,
                  data,
                  loadedAt: Date.now(),
                }));
              } catch {}
            }
            return data;
          })();
          playgroundDatabaseResourceRequestCache.set(cacheKey, {
            data: cachedRecord?.data || null,
            loadedAt: Number(cachedRecord?.loadedAt || 0),
            promise: request,
          });
          void request.finally(() => {
            const currentRecord = playgroundDatabaseResourceRequestCache.get(cacheKey);
            if (currentRecord?.promise === request) {
              playgroundDatabaseResourceRequestCache.set(cacheKey, {
                data: currentRecord.data || null,
                loadedAt: Number(currentRecord.loadedAt || 0),
                promise: null,
              });
            }
          }).catch(() => {});
          if (shouldReturnStale) {
            return cachedRecord.data;
          }
          return request;
        }
  
        function readPlaygroundDatabaseListCache(scopeKey) {
          const memoryRecord = playgroundDatabaseListCache.get(scopeKey);
          if (memoryRecord && Array.isArray(memoryRecord.items)) {
            return memoryRecord;
          }
          try {
            const storageKey = PLAYGROUND_DATABASE_LIST_STORAGE_PREFIX + hashPlaygroundDatabaseListScope(scopeKey);
            const parsed = JSON.parse(window.sessionStorage.getItem(storageKey) || "null");
            if (parsed?.scopeKey !== scopeKey || !Array.isArray(parsed?.items)) {
              return null;
            }
            const record = {
              items: parsed.items.map(normalizePlaygroundDatabaseRecord),
              loadedAt: Math.max(0, Number(parsed.loadedAt || 0)),
              promise: null,
            };
            playgroundDatabaseListCache.set(scopeKey, record);
            return record;
          } catch {
            return null;
          }
        }
  
        function writePlaygroundDatabaseListCache(scopeKey, items, loadedAt = Date.now()) {
          const normalizedItems = (Array.isArray(items) ? items : []).map(normalizePlaygroundDatabaseRecord);
          const existingRecord = playgroundDatabaseListCache.get(scopeKey);
          const record = {
            items: normalizedItems,
            loadedAt,
            promise: existingRecord?.promise || null,
          };
          playgroundDatabaseListCache.set(scopeKey, record);
          try {
            const storageKey = PLAYGROUND_DATABASE_LIST_STORAGE_PREFIX + hashPlaygroundDatabaseListScope(scopeKey);
            window.sessionStorage.setItem(storageKey, JSON.stringify({
              scopeKey,
              items: normalizedItems,
              loadedAt,
            }));
          } catch {}
          return record;
        }
  
        async function fetchPlaygroundDatabaseList(backendUrl, headers, options = {}) {
          const scopeKey = buildPlaygroundDatabaseListScopeKey(backendUrl, headers, options?.identity);
          const cachedRecord = readPlaygroundDatabaseListCache(scopeKey);
          if (cachedRecord?.promise) {
            return cachedRecord.promise;
          }
          const cacheAgeMs = cachedRecord ? Date.now() - Number(cachedRecord.loadedAt || 0) : Infinity;
          if (options?.force !== true && cachedRecord && cacheAgeMs >= 0 && cacheAgeMs < PLAYGROUND_DATABASE_LIST_CACHE_TTL_MS) {
            return cachedRecord.items;
          }
  
          const request = (async () => {
            const response = await fetch(String(backendUrl || "").replace(new RegExp("/+$"), "") + "/databases", {
              method: "GET",
              headers,
              cache: "no-store",
              signal: options?.signal,
              priority: options?.priority || "auto",
            });
            const data = await response.json().catch(() => ({}));
            if (!response.ok) {
              throw new Error(data?.message || data?.error || "Failed to load databases.");
            }
            const items = parsePlaygroundDatabaseListResponse(data);
            writePlaygroundDatabaseListCache(scopeKey, items);
            return items;
          })();
  
          playgroundDatabaseListCache.set(scopeKey, {
            items: cachedRecord?.items || [],
            loadedAt: Number(cachedRecord?.loadedAt || 0),
            promise: request,
          });
          void request.finally(() => {
            const currentRecord = playgroundDatabaseListCache.get(scopeKey);
            if (currentRecord?.promise === request) {
              playgroundDatabaseListCache.set(scopeKey, {
                ...currentRecord,
                promise: null,
              });
            }
          }).catch(() => {});
          return request;
        }
  
        function getPlaygroundDatabaseResponseRecord(data) {
          const source = data?.database || data?.data || data;
          return source && typeof source === "object" && typeof source.id === "string"
            ? normalizePlaygroundDatabaseRecord(source)
            : null;
        }
  
  ${GUARDRAILS_AGENT_SCRIPT_FRAGMENTS.domain}      function normalizePlaygroundAgentRecord(agent) {
          if (!agent || typeof agent !== "object") {
            return buildPlaygroundDefaultAgentDraft();
          }
  
          const draft = buildPlaygroundDefaultAgentDraft();
          const teamMetadata = getPlaygroundAgentTeamMetadata(agent.metadata);
          const id = typeof agent.id === "string" && agent.id.trim() ? agent.id.trim() : draft.id;
          const isDefaultByPattern = id.startsWith("agent-default-")
            || id.startsWith("agent-research-")
            || id.startsWith("agent-assistant-")
            || id.startsWith("agent-computer-use-");
          const enabledSkills = Array.isArray(agent.enabledSkills)
            ? agent.enabledSkills.map((value) => String(value || "").trim()).filter(Boolean)
            : [];
          const guardrailSetIds = getPlaygroundAgentGuardrailSetIds(agent);
          const guardrails = getPlaygroundAgentGuardrailSnapshots(agent);
          const metadata = agent.metadata && typeof agent.metadata === "object" && !Array.isArray(agent.metadata)
            ? agent.metadata
            : {};
          const promptAdaptations = normalizePlaygroundPromptAdaptations(
            agent.promptAdaptations
            || agent.promptAdaptions
            || agent.invisiblePromptAdaptations
            || agent.invisiblePromptAdaptions
            || metadata.promptAdaptations
            || metadata.prompt_adaptations
            || metadata.promptAdaptions
            || metadata.prompt_adaptions
            || metadata.invisiblePromptAdaptations
            || metadata.invisible_prompt_adaptations
            || metadata.invisiblePromptAdaptions
            || metadata.invisible_prompt_adaptions
          );
          const deepResearchModel = PLAYGROUND_AGENT_DEEP_RESEARCH_MODEL_OPTIONS.some((option) => option.id === agent.deepResearchModel)
            ? agent.deepResearchModel
            : draft.deepResearchModel;
          const reasoningEffort = ["minimal", "low", "medium", "high"].includes(agent.reasoningEffort)
            ? agent.reasoningEffort
            : draft.reasoningEffort;
          const voiceMode = normalizePlaygroundVoiceAgentMode(agent.voiceMode);
          const voiceModel = PLAYGROUND_VOICE_AGENT_MODEL_OPTIONS.some((option) => option.id === agent.voiceModel)
            ? agent.voiceModel
            : draft.voiceModel;
  
          return {
            ...draft,
            ...agent,
            id,
            name: typeof agent.name === "string" ? agent.name : draft.name,
            description: typeof agent.description === "string" ? agent.description : draft.description,
            model: getPlaygroundAgentModelMeta(typeof agent.model === "string" ? agent.model : "").id,
            instructions: typeof agent.instructions === "string" ? agent.instructions : draft.instructions,
            binary: typeof agent.binary === "string" && agent.binary.trim() ? agent.binary : draft.binary,
            reasoningEffort,
            enabledSkills,
            guardrailSetIds,
            guardrails,
            promptAdaptations,
            invisiblePromptAdaptations: promptAdaptations,
            deepResearchModel,
            voiceMode,
            voiceProvider: "xai",
            voiceModel,
            voiceId: typeof agent.voiceId === "string" && agent.voiceId.trim() ? agent.voiceId.trim() : draft.voiceId,
            voiceInstructions: typeof agent.voiceInstructions === "string" ? agent.voiceInstructions : draft.voiceInstructions,
            voiceLanguageHint: typeof agent.voiceLanguageHint === "string" ? agent.voiceLanguageHint : draft.voiceLanguageHint,
            voiceTurnDetection: agent.voiceTurnDetection && typeof agent.voiceTurnDetection === "object" && !Array.isArray(agent.voiceTurnDetection)
              ? agent.voiceTurnDetection
              : null,
            voicePronunciationReplacements: agent.voicePronunciationReplacements && typeof agent.voicePronunciationReplacements === "object" && !Array.isArray(agent.voicePronunciationReplacements)
              ? agent.voicePronunciationReplacements
              : null,
            agentType: teamMetadata ? "team" : "single",
            teamOrchestratorAgentId: teamMetadata?.team?.orchestratorAgentId || "",
            teamSubagentIds: teamMetadata?.team?.subagentIds || [],
            teamExecutionMode: teamMetadata?.executionMode || PLAYGROUND_AGENT_TEAM_EXECUTION_MODE,
            createdAt: typeof agent.createdAt === "string" && agent.createdAt ? agent.createdAt : draft.createdAt,
            updatedAt: typeof agent.updatedAt === "string" && agent.updatedAt ? agent.updatedAt : draft.updatedAt,
            lastRunAt: typeof agent.lastRunAt === "string" ? agent.lastRunAt : draft.lastRunAt,
            isActive: agent.isActive !== false,
            isDefault: Boolean(agent.isDefault) || isDefaultByPattern,
            isSystem: Boolean(agent.isSystem) || isDefaultByPattern,
            permissionSet: normalizePlaygroundPermissionSet(agent.permissionSet, "agent"),
            metadata: Object.keys(metadata).length > 0
              ? metadata
              : null,
          };
        }
  
        function stripPlaygroundAgentVersionMetadata(metadata) {
          const source = metadata && typeof metadata === "object" && !Array.isArray(metadata)
            ? { ...metadata }
            : {};
          delete source.agentVersions;
          delete source.agent_versions;
          delete source.versions;
          delete source.activeAgentVersionId;
          delete source.active_agent_version_id;
          delete source.activeAgentVersionNumber;
          delete source.active_agent_version_number;
          delete source.restoredFromAgentVersionId;
          delete source.restored_from_agent_version_id;
          delete source.restoredFromAgentVersionNumber;
          delete source.restored_from_agent_version_number;
          delete source.publishedAt;
          delete source.published_at;
          delete source.unpublishedAt;
          delete source.unpublished_at;
          delete source.runnerVersioning;
          delete source.runner_versioning;
          delete source.versioning;
          delete source.activeAgentDeployment;
          delete source.active_agent_deployment;
          return source;
        }
  
        function createPlaygroundAgentVersionRevisionId() {
          return "agent_revision_" + Date.now().toString(36) + "_" + Math.random().toString(36).slice(2, 8);
        }
  
        function createPlaygroundAgentDeploymentId(versionId = "") {
          const suffix = String(versionId || "").trim() || (Date.now().toString(36) + "_" + Math.random().toString(36).slice(2, 8));
          return "agent_deployment_" + suffix.replace(/[^A-Za-z0-9_-]+/g, "_");
        }
  
        function normalizePlaygroundVersionActor(actor) {
          const source = actor && typeof actor === "object" && !Array.isArray(actor) ? actor : {};
          const id = String(source.id || source.userId || source.user_id || source.uid || source.email || "").trim();
          const name = String(source.name || source.displayName || source.display_name || source.fullName || source.full_name || source.email || "").trim();
          const email = String(source.email || source.emailAddress || source.email_address || "").trim().toLowerCase();
          const avatarUrl = String(source.avatarUrl || source.avatar_url || source.photoURL || source.photoUrl || source.photo_url || source.picture || "").trim();
          if (!id && !name && !email && !avatarUrl) {
            return null;
          }
          return {
            id,
            name,
            email,
            avatarUrl,
          };
        }
  
        function normalizePlaygroundAgentVersionLifecycleState(version, status = "") {
          const rawState = String(
            version?.lifecycleState
            || version?.lifecycle_state
            || version?.state
            || ""
          ).trim().toLowerCase();
          if (["draft", "saved", "published", "deprecated", "archived", "unpublished"].includes(rawState)) {
            return rawState;
          }
          const normalizedStatus = String(status || version?.status || "").trim().toLowerCase();
          if (normalizedStatus === "active") return "published";
          if (normalizedStatus === "superseded") return "deprecated";
          if (normalizedStatus === "unpublished") return "unpublished";
          return "saved";
        }
  
        function normalizePlaygroundAgentVersionDeployment(rawDeployment, fallback = {}) {
          const source = rawDeployment && typeof rawDeployment === "object" && !Array.isArray(rawDeployment)
            ? rawDeployment
            : {};
          const id = String(source.id || source.deploymentId || source.deployment_id || fallback.id || "").trim();
          const status = String(source.status || source.deploymentStatus || source.deployment_status || fallback.status || "").trim().toLowerCase();
          const versionId = String(source.versionId || source.version_id || fallback.versionId || "").trim();
          const publishedAt = String(source.publishedAt || source.published_at || fallback.publishedAt || "").trim();
          const publishedBy = normalizePlaygroundVersionActor(source.publishedBy || source.published_by || fallback.publishedBy);
          if (!id && !status && !versionId && !publishedAt && !publishedBy) {
            return null;
          }
          return {
            id,
            versionId,
            status: status || "published",
            publishedAt,
            publishedBy,
          };
        }
  
        function normalizePlaygroundAgentVersion(rawVersion, fallbackIndex = 0) {
          const version = rawVersion && typeof rawVersion === "object" && !Array.isArray(rawVersion) ? rawVersion : {};
          const snapshot = version.snapshot && typeof version.snapshot === "object" && !Array.isArray(version.snapshot)
            ? version.snapshot
            : {};
          const createdAt = String(version.createdAt || version.created_at || version.publishedAt || version.published_at || new Date().toISOString()).trim();
          const id = String(version.id || version.versionId || version.version_id || ("agent_version_" + (fallbackIndex + 1))).trim();
          const versionNumber = Number(version.version || version.versionNumber || version.version_number || 0) || (fallbackIndex + 1);
          const rawStatus = String(version.status || "").trim().toLowerCase();
          const status = ["active", "saved", "superseded", "unpublished"].includes(rawStatus) ? rawStatus : "saved";
          const lifecycleState = normalizePlaygroundAgentVersionLifecycleState(version, status);
          const agentType = version.agentType === "team" || snapshot.agentType === "team" ? "team" : "single";
          const enabledSkills = Array.isArray(version.enabledSkills)
            ? version.enabledSkills
            : Array.isArray(snapshot.enabledSkills)
              ? snapshot.enabledSkills
              : [];
          const guardrailSetIds = getPlaygroundAgentGuardrailSetIds({
            ...snapshot,
            ...version,
            metadata: version.metadata || snapshot.metadata,
          });
          const guardrails = getPlaygroundAgentGuardrailSnapshots({
            ...snapshot,
            ...version,
            metadata: version.metadata || snapshot.metadata,
          });
          const promptAdaptations = normalizePlaygroundPromptAdaptations(
            version.promptAdaptations
            || version.promptAdaptions
            || version.invisiblePromptAdaptations
            || version.invisiblePromptAdaptions
            || snapshot.promptAdaptations
            || snapshot.promptAdaptions
            || snapshot.invisiblePromptAdaptations
            || snapshot.invisiblePromptAdaptions
            || version.metadata?.promptAdaptations
            || version.metadata?.promptAdaptions
            || snapshot.metadata?.promptAdaptations
            || snapshot.metadata?.promptAdaptions
          );
          const teamSubagentIds = Array.isArray(version.teamSubagentIds)
            ? version.teamSubagentIds
            : Array.isArray(snapshot.teamSubagentIds)
              ? snapshot.teamSubagentIds
              : [];
          const normalizedSnapshot = {
            name: String(version.name || snapshot.name || "").trim(),
            description: typeof snapshot.description === "string"
              ? snapshot.description
              : typeof version.agentDescription === "string"
                ? version.agentDescription
                : typeof version.agent_description === "string"
                  ? version.agent_description
                  : "",
            model: getPlaygroundAgentModelMeta(typeof version.model === "string" ? version.model : snapshot.model || "").id,
            instructions: typeof version.instructions === "string"
              ? version.instructions
              : typeof snapshot.instructions === "string"
                ? snapshot.instructions
                : "",
            binary: String(version.binary || snapshot.binary || "Claude Code CLI").trim() || "Claude Code CLI",
            reasoningEffort: ["minimal", "low", "medium", "high"].includes(version.reasoningEffort)
              ? version.reasoningEffort
              : ["minimal", "low", "medium", "high"].includes(snapshot.reasoningEffort)
                ? snapshot.reasoningEffort
                : "medium",
            enabledSkills: enabledSkills.map((value) => String(value || "").trim()).filter(Boolean),
            guardrailSetIds,
            guardrails,
            promptAdaptations,
            invisiblePromptAdaptations: promptAdaptations,
            deepResearchModel: PLAYGROUND_AGENT_DEEP_RESEARCH_MODEL_OPTIONS.some((option) => option.id === version.deepResearchModel)
              ? version.deepResearchModel
              : PLAYGROUND_AGENT_DEEP_RESEARCH_MODEL_OPTIONS.some((option) => option.id === snapshot.deepResearchModel)
                ? snapshot.deepResearchModel
                : null,
            voiceMode: normalizePlaygroundVoiceAgentMode(version.voiceMode || snapshot.voiceMode),
            voiceProvider: "xai",
            voiceModel: PLAYGROUND_VOICE_AGENT_MODEL_OPTIONS.some((option) => option.id === version.voiceModel)
              ? version.voiceModel
              : PLAYGROUND_VOICE_AGENT_MODEL_OPTIONS.some((option) => option.id === snapshot.voiceModel)
                ? snapshot.voiceModel
                : "grok-voice-latest",
            voiceId: String(version.voiceId || snapshot.voiceId || "eve").trim() || "eve",
            voiceInstructions: typeof version.voiceInstructions === "string"
              ? version.voiceInstructions
              : typeof snapshot.voiceInstructions === "string"
                ? snapshot.voiceInstructions
                : "",
            voiceLanguageHint: String(version.voiceLanguageHint || snapshot.voiceLanguageHint || "").trim(),
            permissionSet: normalizePlaygroundPermissionSet(version.permissionSet || snapshot.permissionSet, "agent"),
            agentType,
            teamOrchestratorAgentId: String(version.teamOrchestratorAgentId || snapshot.teamOrchestratorAgentId || "").trim(),
            teamSubagentIds: dedupePlaygroundAgentIds(teamSubagentIds),
            teamExecutionMode: agentType === "team" ? PLAYGROUND_AGENT_TEAM_EXECUTION_MODE : "",
            metadata: stripPlaygroundAgentVersionMetadata(snapshot.metadata),
          };
  
          return {
            id,
            version: versionNumber,
            label: String(version.label || version.name || ("Version " + versionNumber)).trim(),
            description: String(version.description || version.summary || "").trim(),
            status,
            lifecycleState,
            lifecycle_state: lifecycleState,
            revisionId: String(version.revisionId || version.revision_id || "").trim(),
            revision_id: String(version.revisionId || version.revision_id || "").trim(),
            baseRevisionId: String(version.baseRevisionId || version.base_revision_id || "").trim(),
            base_revision_id: String(version.baseRevisionId || version.base_revision_id || "").trim(),
            revisionNumber: Number(version.revisionNumber || version.revision_number || versionNumber || 0) || versionNumber,
            revision_number: Number(version.revisionNumber || version.revision_number || versionNumber || 0) || versionNumber,
            createdAt,
            updatedAt: String(version.updatedAt || version.updated_at || "").trim(),
            publishedAt: String(version.publishedAt || version.published_at || "").trim(),
            unpublishedAt: String(version.unpublishedAt || version.unpublished_at || "").trim(),
            createdBy: normalizePlaygroundVersionActor(version.createdBy || version.created_by),
            created_by: normalizePlaygroundVersionActor(version.createdBy || version.created_by),
            updatedBy: normalizePlaygroundVersionActor(version.updatedBy || version.updated_by),
            updated_by: normalizePlaygroundVersionActor(version.updatedBy || version.updated_by),
            publishedBy: normalizePlaygroundVersionActor(version.publishedBy || version.published_by),
            published_by: normalizePlaygroundVersionActor(version.publishedBy || version.published_by),
            unpublishedBy: normalizePlaygroundVersionActor(version.unpublishedBy || version.unpublished_by),
            unpublished_by: normalizePlaygroundVersionActor(version.unpublishedBy || version.unpublished_by),
            deploymentId: String(version.deploymentId || version.deployment_id || version.deployment?.id || "").trim(),
            deployment_id: String(version.deploymentId || version.deployment_id || version.deployment?.id || "").trim(),
            deploymentStatus: String(version.deploymentStatus || version.deployment_status || version.deployment?.status || "").trim().toLowerCase(),
            deployment_status: String(version.deploymentStatus || version.deployment_status || version.deployment?.status || "").trim().toLowerCase(),
            deployment: normalizePlaygroundAgentVersionDeployment(version.deployment, {
              id: version.deploymentId || version.deployment_id,
              versionId: id,
              status: version.deploymentStatus || version.deployment_status,
              publishedAt: version.publishedAt || version.published_at,
              publishedBy: version.publishedBy || version.published_by,
            }),
            name: normalizedSnapshot.name,
            model: normalizedSnapshot.model,
            skillCount: normalizedSnapshot.enabledSkills.length,
            permissionSet: normalizedSnapshot.permissionSet,
            snapshot: normalizedSnapshot,
          };
        }
  
        function normalizePlaygroundAgentVersions(value) {
          const rawItems = Array.isArray(value) ? value : [];
          return rawItems
            .map((version, index) => normalizePlaygroundAgentVersion(version, index))
            .filter((version) => version.id)
            .sort((a, b) => {
              const versionDelta = Number(b.version || 0) - Number(a.version || 0);
              if (versionDelta) return versionDelta;
              return new Date(b.publishedAt || b.createdAt || 0).getTime() - new Date(a.publishedAt || a.createdAt || 0).getTime();
            });
        }
  
        function readPlaygroundAgentVersions(agent) {
          const metadata = agent?.metadata && typeof agent.metadata === "object" && !Array.isArray(agent.metadata)
            ? agent.metadata
            : {};
          return normalizePlaygroundAgentVersions(
            agent?.agentVersions
            || agent?.versions
            || metadata.agentVersions
            || metadata.agent_versions
            || metadata.versions
            || []
          );
        }
  
        function createPlaygroundAgentVersionId() {
          return "agent_version_" + Date.now().toString(36) + "_" + Math.random().toString(36).slice(2, 8);
        }
  
        function buildPlaygroundAgentVersionSnapshot(agent) {
          const normalizedAgent = normalizePlaygroundAgentRecord(agent || buildPlaygroundDefaultAgentDraft());
          return {
            name: String(normalizedAgent.name || "").trim() || (normalizedAgent.agentType === "team" ? "New Squad" : "New Agent"),
            description: typeof normalizedAgent.description === "string" ? normalizedAgent.description : "",
            model: getPlaygroundAgentModelMeta(normalizedAgent.model || "").id,
            instructions: typeof normalizedAgent.instructions === "string" ? normalizedAgent.instructions : "",
            binary: String(normalizedAgent.binary || "Claude Code CLI").trim() || "Claude Code CLI",
            reasoningEffort: ["minimal", "low", "medium", "high"].includes(normalizedAgent.reasoningEffort)
              ? normalizedAgent.reasoningEffort
              : "medium",
            enabledSkills: normalizePlaygroundEnabledSkillIds(normalizedAgent.enabledSkills),
            guardrailSetIds: normalizePlaygroundGuardrailSetIds(normalizedAgent.guardrailSetIds),
            guardrails: getPlaygroundAgentGuardrailSnapshots(normalizedAgent),
            promptAdaptations: normalizePlaygroundPromptAdaptations(normalizedAgent.promptAdaptations || normalizedAgent.invisiblePromptAdaptations),
            invisiblePromptAdaptations: normalizePlaygroundPromptAdaptations(normalizedAgent.promptAdaptations || normalizedAgent.invisiblePromptAdaptations),
            deepResearchModel: PLAYGROUND_AGENT_DEEP_RESEARCH_MODEL_OPTIONS.some((option) => option.id === normalizedAgent.deepResearchModel)
              ? normalizedAgent.deepResearchModel
              : null,
            voiceMode: normalizePlaygroundVoiceAgentMode(normalizedAgent.voiceMode),
            voiceProvider: "xai",
            voiceModel: PLAYGROUND_VOICE_AGENT_MODEL_OPTIONS.some((option) => option.id === normalizedAgent.voiceModel)
              ? normalizedAgent.voiceModel
              : "grok-voice-latest",
            voiceId: String(normalizedAgent.voiceId || "eve").trim() || "eve",
            voiceInstructions: typeof normalizedAgent.voiceInstructions === "string" ? normalizedAgent.voiceInstructions : "",
            voiceLanguageHint: String(normalizedAgent.voiceLanguageHint || "").trim(),
            permissionSet: normalizePlaygroundPermissionSet(normalizedAgent.permissionSet, "agent"),
            agentType: normalizedAgent.agentType === "team" ? "team" : "single",
            teamOrchestratorAgentId: String(normalizedAgent.teamOrchestratorAgentId || "").trim(),
            teamSubagentIds: dedupePlaygroundAgentIds(normalizedAgent.teamSubagentIds),
            teamExecutionMode: normalizedAgent.agentType === "team" ? PLAYGROUND_AGENT_TEAM_EXECUTION_MODE : "",
            metadata: stripPlaygroundAgentVersionMetadata(normalizedAgent.metadata),
          };
        }
  
        function createPlaygroundAgentVersion(agent, existingVersions = [], options = {}) {
          const now = new Date().toISOString();
          const normalizedExisting = normalizePlaygroundAgentVersions(existingVersions);
          const nextVersion = normalizedExisting.reduce((maxVersion, version) => Math.max(maxVersion, Number(version.version || 0)), 0) + 1;
          const requestedStatus = String(options?.status || "saved").trim().toLowerCase();
          const status = requestedStatus === "active" ? "active" : "saved";
          const lifecycleState = status === "active" ? "published" : "saved";
          const actor = normalizePlaygroundVersionActor(options?.actor);
          const id = createPlaygroundAgentVersionId();
          const revisionId = createPlaygroundAgentVersionRevisionId();
          const deploymentId = status === "active" ? createPlaygroundAgentDeploymentId(id) : "";
          const snapshot = buildPlaygroundAgentVersionSnapshot(agent);
          return normalizePlaygroundAgentVersion({
            id,
            version: nextVersion,
            label: String(options?.label || ("Version " + nextVersion)).trim(),
            description: String(options?.description || "").trim(),
            status,
            lifecycleState,
            lifecycle_state: lifecycleState,
            revisionId,
            revision_id: revisionId,
            baseRevisionId: String(options?.baseRevisionId || options?.base_revision_id || "").trim(),
            base_revision_id: String(options?.baseRevisionId || options?.base_revision_id || "").trim(),
            revisionNumber: nextVersion,
            revision_number: nextVersion,
            createdAt: now,
            createdBy: actor,
            created_by: actor,
            updatedAt: now,
            updated_at: now,
            updatedBy: actor,
            updated_by: actor,
            publishedAt: status === "active" ? now : "",
            published_at: status === "active" ? now : "",
            publishedBy: status === "active" ? actor : null,
            published_by: status === "active" ? actor : null,
            deploymentId,
            deployment_id: deploymentId,
            deploymentStatus: status === "active" ? "published" : "",
            deployment_status: status === "active" ? "published" : "",
            deployment: deploymentId
              ? {
                  id: deploymentId,
                  versionId: id,
                  status: "published",
                  publishedAt: now,
                  publishedBy: actor,
                }
              : null,
            name: snapshot.name,
            model: snapshot.model,
            enabledSkills: snapshot.enabledSkills,
            guardrailSetIds: snapshot.guardrailSetIds,
            permissionSet: snapshot.permissionSet,
            snapshot,
          }, nextVersion - 1);
        }
  
        function createPlaygroundAgentWithVersionList(agent, versions, preferredSelectedId = "") {
          const baseAgent = normalizePlaygroundAgentRecord(agent || buildPlaygroundDefaultAgentDraft());
          const normalizedVersions = normalizePlaygroundAgentVersions(versions);
          const metadata = baseAgent.metadata && typeof baseAgent.metadata === "object" && !Array.isArray(baseAgent.metadata)
            ? { ...baseAgent.metadata }
            : {};
          const previousSelectedId = String(metadata.restoredFromAgentVersionId || metadata.restored_from_agent_version_id || metadata.activeAgentVersionId || metadata.active_agent_version_id || "").trim();
          const selectedVersion = normalizedVersions.find((version) => version.id === String(preferredSelectedId || "").trim())
            || normalizedVersions.find((version) => version.id === previousSelectedId)
            || normalizedVersions.find((version) => version.status === "active")
            || normalizedVersions[0]
            || null;
          const activeVersion = normalizedVersions.find((version) => version.status === "active")
            || normalizedVersions.find((version) => version.id === String(metadata.activeAgentVersionId || metadata.active_agent_version_id || "").trim())
            || null;
          metadata.agentVersions = normalizedVersions;
          metadata.agent_versions = normalizedVersions;
          metadata.activeAgentVersionId = activeVersion?.id || "";
          metadata.active_agent_version_id = activeVersion?.id || "";
          metadata.activeAgentVersionNumber = activeVersion?.version || 0;
          metadata.active_agent_version_number = activeVersion?.version || 0;
          metadata.restoredFromAgentVersionId = selectedVersion?.id || "";
          metadata.restored_from_agent_version_id = selectedVersion?.id || "";
          metadata.restoredFromAgentVersionNumber = selectedVersion?.version || 0;
          metadata.restored_from_agent_version_number = selectedVersion?.version || 0;
          if (activeVersion?.publishedAt) {
            metadata.publishedAt = activeVersion.publishedAt;
            metadata.published_at = activeVersion.publishedAt;
          } else {
            delete metadata.publishedAt;
            delete metadata.published_at;
          }
          return normalizePlaygroundAgentRecord({
            ...baseAgent,
            metadata,
            publishedAt: activeVersion?.publishedAt || "",
          });
        }
  
        function createPlaygroundAgentFromVersionSnapshot(agent, version, versions, preferredSelectedId = "") {
          const baseAgent = normalizePlaygroundAgentRecord(agent || buildPlaygroundDefaultAgentDraft());
          const normalizedVersion = normalizePlaygroundAgentVersion(version || {});
          const snapshot = normalizedVersion.snapshot || {};
          const baseMetadata = stripPlaygroundAgentVersionMetadata(baseAgent.metadata);
          const snapshotMetadata = stripPlaygroundAgentVersionMetadata(snapshot.metadata);
          const nextAgent = normalizePlaygroundAgentRecord({
            ...baseAgent,
            name: snapshot.name || baseAgent.name,
            description: typeof snapshot.description === "string" ? snapshot.description : baseAgent.description,
            model: snapshot.model || baseAgent.model,
            instructions: typeof snapshot.instructions === "string" ? snapshot.instructions : baseAgent.instructions,
            binary: snapshot.binary || baseAgent.binary,
            reasoningEffort: snapshot.reasoningEffort || baseAgent.reasoningEffort,
            enabledSkills: Array.isArray(snapshot.enabledSkills) ? snapshot.enabledSkills : baseAgent.enabledSkills,
            guardrailSetIds: Array.isArray(snapshot.guardrailSetIds) ? snapshot.guardrailSetIds : baseAgent.guardrailSetIds,
            guardrails: Array.isArray(snapshot.guardrails) ? snapshot.guardrails : baseAgent.guardrails,
            promptAdaptations: Array.isArray(snapshot.promptAdaptations) ? snapshot.promptAdaptations : baseAgent.promptAdaptations,
            invisiblePromptAdaptations: Array.isArray(snapshot.invisiblePromptAdaptations) ? snapshot.invisiblePromptAdaptations : baseAgent.invisiblePromptAdaptations,
            deepResearchModel: snapshot.deepResearchModel || baseAgent.deepResearchModel,
            voiceMode: normalizePlaygroundVoiceAgentMode(snapshot.voiceMode || baseAgent.voiceMode),
            voiceProvider: "xai",
            voiceModel: PLAYGROUND_VOICE_AGENT_MODEL_OPTIONS.some((option) => option.id === snapshot.voiceModel)
              ? snapshot.voiceModel
              : baseAgent.voiceModel,
            voiceId: String(snapshot.voiceId || baseAgent.voiceId || "eve").trim() || "eve",
            voiceInstructions: typeof snapshot.voiceInstructions === "string" ? snapshot.voiceInstructions : baseAgent.voiceInstructions,
            voiceLanguageHint: String(snapshot.voiceLanguageHint || baseAgent.voiceLanguageHint || "").trim(),
            permissionSet: snapshot.permissionSet || baseAgent.permissionSet,
            agentType: snapshot.agentType === "team" ? "team" : baseAgent.agentType,
            teamOrchestratorAgentId: snapshot.teamOrchestratorAgentId || baseAgent.teamOrchestratorAgentId,
            teamSubagentIds: Array.isArray(snapshot.teamSubagentIds) ? snapshot.teamSubagentIds : baseAgent.teamSubagentIds,
            teamExecutionMode: snapshot.agentType === "team" ? PLAYGROUND_AGENT_TEAM_EXECUTION_MODE : baseAgent.teamExecutionMode,
            metadata: {
              ...baseMetadata,
              ...snapshotMetadata,
            },
          });
          return createPlaygroundAgentWithVersionList(nextAgent, versions, preferredSelectedId || normalizedVersion.id);
        }
  
        function parsePlaygroundAgentListResponse(data) {
          const items = Array.isArray(data?.data)
            ? data.data
            : Array.isArray(data?.agents)
              ? data.agents
              : Array.isArray(data?.items)
                ? data.items
                : [];
          return items.map(normalizePlaygroundAgentRecord);
        }
  
        function getPlaygroundAgentResponseRecord(data) {
          const source = data?.agent || data?.data || data;
          return source && typeof source === "object" && typeof source.id === "string"
            ? normalizePlaygroundAgentRecord(source)
            : null;
        }
  
        const PLAYGROUND_VOICE_AGENT_MODE_OPTIONS = [
          { id: "off", label: "Off" },
          { id: "web", label: "Web" },
          { id: "phone", label: "Phone" },
          { id: "web_and_phone", label: "Web + Phone" },
        ];
  
        const PLAYGROUND_VOICE_AGENT_MODEL_OPTIONS = [
          { id: "grok-voice-latest", label: "grok-voice-latest" },
          { id: "grok-voice-think-fast-1.0", label: "grok-voice-think-fast-1.0" },
          { id: "grok-voice-fast-1.0", label: "grok-voice-fast-1.0" },
        ];
  
        const PLAYGROUND_VOICE_AGENT_VOICE_OPTIONS = [
          { id: "eve", label: "Eve Voice" },
          { id: "ara", label: "Ara Voice" },
          { id: "rex", label: "Rex Voice" },
          { id: "sal", label: "Sal Voice" },
          { id: "leo", label: "Leo Voice" },
        ];
  
        function normalizePlaygroundVoiceAgentMode(value) {
          const normalized = String(value || "off").trim().toLowerCase().replace(/[\s-]+/g, "_");
          if (normalized === "web_phone") return "web_and_phone";
          return PLAYGROUND_VOICE_AGENT_MODE_OPTIONS.some((option) => option.id === normalized)
            ? normalized
            : "off";
        }
  
        function isPlaygroundVoiceAgentWebEnabled(mode) {
          const normalizedMode = normalizePlaygroundVoiceAgentMode(mode);
          return normalizedMode === "web" || normalizedMode === "web_and_phone";
        }
  
        function isPlaygroundVoiceAgentPhoneEnabled(mode) {
          const normalizedMode = normalizePlaygroundVoiceAgentMode(mode);
          return normalizedMode === "phone" || normalizedMode === "web_and_phone";
        }
  
        function normalizePlaygroundVoiceAgentRecord(record) {
          const source = record && typeof record === "object" && !Array.isArray(record) ? record : {};
          const voiceSource = source.voice && typeof source.voice === "object" && !Array.isArray(source.voice) ? source.voice : {};
          const agentSource = source.agent && typeof source.agent === "object" && !Array.isArray(source.agent) ? source.agent : source;
          const agent = normalizePlaygroundAgentRecord(agentSource);
          const voiceMode = normalizePlaygroundVoiceAgentMode(voiceSource.mode || agent.voiceMode);
          const voiceModel = String(voiceSource.model || agent.voiceModel || "grok-voice-latest").trim() || "grok-voice-latest";
          const voiceId = String(voiceSource.voiceId || agent.voiceId || "eve").trim() || "eve";
          const languageHint = String(voiceSource.languageHint || agent.voiceLanguageHint || "").trim();
          const phoneNumber = source.phoneNumber && typeof source.phoneNumber === "object" && !Array.isArray(source.phoneNumber)
            ? source.phoneNumber
            : null;
          return {
            ...source,
            agent,
            voice: {
              mode: voiceMode,
              provider: "xai",
              model: voiceModel,
              voiceId,
              languageHint,
              enabled: voiceMode !== "off",
            },
            phoneNumber,
            recentSessions: Array.isArray(source.recentSessions) ? source.recentSessions : [],
          };
        }
  
        function buildPlaygroundVoiceAgentDraft(record) {
          const normalizedRecord = normalizePlaygroundVoiceAgentRecord(record);
          const agent = normalizedRecord.agent || {};
          return {
            agentId: String(agent.id || "").trim(),
            voiceMode: normalizePlaygroundVoiceAgentMode(normalizedRecord.voice?.mode || agent.voiceMode),
            voiceModel: String(normalizedRecord.voice?.model || agent.voiceModel || "grok-voice-latest").trim() || "grok-voice-latest",
            voiceId: String(normalizedRecord.voice?.voiceId || agent.voiceId || "eve").trim() || "eve",
            voiceLanguageHint: String(normalizedRecord.voice?.languageHint || agent.voiceLanguageHint || "").trim(),
            voiceInstructions: typeof agent.voiceInstructions === "string" ? agent.voiceInstructions : "",
          };
        }
  
        function buildPlaygroundVoiceAgentUpdatePayload(draft) {
          const normalizedDraft = draft && typeof draft === "object" && !Array.isArray(draft) ? draft : {};
          return {
            voiceMode: normalizePlaygroundVoiceAgentMode(normalizedDraft.voiceMode),
            voiceProvider: "xai",
            voiceModel: String(normalizedDraft.voiceModel || "grok-voice-latest").trim() || "grok-voice-latest",
            voiceId: String(normalizedDraft.voiceId || "").trim() || null,
            voiceLanguageHint: String(normalizedDraft.voiceLanguageHint || "").trim() || null,
            voiceInstructions: String(normalizedDraft.voiceInstructions || "").trim() || null,
          };
        }
  
  ${PROJECTS_DOMAIN_FOUNDATION_SCRIPT}
  ${MARKETPLACE_DOMAIN_SCRIPT_FRAGMENTS.catalog}${MARKETPLACE_DOMAIN_SCRIPT_FRAGMENTS.metadata}${MARKETPLACE_DOMAIN_SCRIPT_FRAGMENTS.serverFiles}${MARKETPLACE_DOMAIN_SCRIPT_FRAGMENTS.database}${MARKETPLACE_DOMAIN_SCRIPT_FRAGMENTS.resources}
  ${PROJECTS_DOMAIN_RUNTIME_SCRIPT}
  ${PROJECTS_INTEGRATIONS_RUNTIME_SCRIPT}
  
        function getPlaygroundThreadResponseRecord(data) {
          if (data?.thread && typeof data.thread === "object") {
            return data.thread;
          }
          if (data?.data && typeof data.data === "object") {
            return data.data;
          }
          return data && typeof data === "object" ? data : {};
        }
  
  ${FILES_DOMAIN_FRAGMENTS.preview}
  
        const PLAYGROUND_RUNNER_WORKSPACE_SELECTION_STORAGE_KEY_PREFIX = "tb_runner_chat_workspace_selection_v1";
        const PLAYGROUND_RUNNER_CHAT_APP_ID = "runner-web-sdk-demo";
  
        function sanitizePlaygroundBackendUrl(url) {
          return String(url || "").trim().replace(/\\/+$/, "");
        }
  
        function buildPlaygroundWorkspaceSelectionStorageKey(appId, backendUrl) {
          return PLAYGROUND_RUNNER_WORKSPACE_SELECTION_STORAGE_KEY_PREFIX
            + ":"
            + (appId || "runner-web-sdk")
            + ":"
            + (sanitizePlaygroundBackendUrl(backendUrl) || "default");
        }
  
        function normalizePlaygroundWorkspaceSelectorMode(value) {
          return value === "projects" ? "projects" : "computers";
        }
  
        function loadPlaygroundPersistedWorkspaceSelection(appId, backendUrl) {
          if (typeof window === "undefined") {
            return null;
          }
          try {
            const raw = window.localStorage.getItem(buildPlaygroundWorkspaceSelectionStorageKey(appId, backendUrl));
            if (!raw) {
              return null;
            }
            const parsed = JSON.parse(raw);
            if (!parsed || typeof parsed !== "object" || Array.isArray(parsed)) {
              return null;
            }
            return {
              mode: normalizePlaygroundWorkspaceSelectorMode(parsed.mode),
              environmentId: typeof parsed.environmentId === "string" ? parsed.environmentId.trim() : "",
              projectId: typeof parsed.projectId === "string" ? parsed.projectId.trim() : "",
            };
          } catch {
            return null;
          }
        }
  
        function persistPlaygroundWorkspaceSelection(appId, backendUrl, selection) {
          if (typeof window === "undefined") {
            return;
          }
          try {
            window.localStorage.setItem(
              buildPlaygroundWorkspaceSelectionStorageKey(appId, backendUrl),
              JSON.stringify({
                mode: normalizePlaygroundWorkspaceSelectorMode(selection?.mode),
                environmentId: String(selection?.environmentId || "").trim(),
                projectId: String(selection?.projectId || "").trim(),
              })
            );
          } catch {
            // Local persistence is best effort only.
          }
        }
  
        function resolvePlaygroundPersistedWorkspaceEnvironmentId(appId, backendUrl, environments = [], projects = []) {
          const persisted = loadPlaygroundPersistedWorkspaceSelection(appId, backendUrl);
          if (!persisted) {
            return "";
          }
          if (persisted.mode === "projects" && persisted.projectId) {
            const persistedProject = projects.find((project) => project?.id === persisted.projectId) || null;
            const projectEnvironmentId = String(persistedProject?.defaultEnvironmentId || "").trim();
            return projectEnvironmentId && environments.some((environment) => environment?.id === projectEnvironmentId)
              ? projectEnvironmentId
              : "";
          }
          const persistedEnvironmentId = String(persisted.environmentId || "").trim();
          return persistedEnvironmentId && environments.some((environment) => environment?.id === persistedEnvironmentId)
            ? persistedEnvironmentId
            : "";
        }
  
  ${FILES_DOMAIN_FRAGMENTS.listUrl}
  
        function buildPlaygroundEnvironmentAnalyticsUrl(backendUrl, environmentId) {
          if (!backendUrl || !environmentId) return "";
          return backendUrl + "/environments/" + encodeURIComponent(environmentId) + "/analytics";
        }
  
        function buildPlaygroundEnvironmentHomeAnalyticsUrl(backendUrl) {
          if (!backendUrl) return "";
          return backendUrl + "/environments/analytics/overview";
        }
  
        function buildPlaygroundCostsSummaryUrl(backendUrl, period) {
          if (!backendUrl) return "";
          const params = new URLSearchParams();
          if (period) {
            params.set("period", period);
          }
          return backendUrl + "/costs/summary" + (params.toString() ? "?" + params.toString() : "");
        }
  
        function buildPlaygroundCostsSummaryRangeUrl(backendUrl, startDate, endDate) {
          if (!backendUrl) return "";
          const params = new URLSearchParams();
          if (startDate) {
            params.set("startDate", startDate);
          }
          if (endDate) {
            params.set("endDate", endDate);
          }
          return backendUrl + "/costs/summary" + (params.toString() ? "?" + params.toString() : "");
        }
  
        function buildPlaygroundCostsBreakdownUrl(backendUrl, groupBy, period) {
          if (!backendUrl) return "";
          const params = new URLSearchParams();
          if (groupBy) {
            params.set("groupBy", groupBy);
          }
          if (period) {
            params.set("period", period);
          }
          return backendUrl + "/costs/breakdown" + (params.toString() ? "?" + params.toString() : "");
        }
  
        function buildPlaygroundCostsBreakdownRangeUrl(backendUrl, groupBy, startDate, endDate) {
          if (!backendUrl) return "";
          const params = new URLSearchParams();
          if (groupBy) {
            params.set("groupBy", groupBy);
          }
          if (startDate) {
            params.set("startDate", startDate);
          }
          if (endDate) {
            params.set("endDate", endDate);
          }
          return backendUrl + "/costs/breakdown" + (params.toString() ? "?" + params.toString() : "");
        }
  
        function normalizePlaygroundEnvironmentHomeChartPeriod(period) {
          const normalizedPeriod = String(period || "").trim();
          return ["day", "week", "month"].includes(normalizedPeriod) ? normalizedPeriod : "month";
        }
  
        function buildPlaygroundEnvironmentHomeActivityBuckets(period) {
          const normalizedPeriod = normalizePlaygroundEnvironmentHomeChartPeriod(period);
          if (normalizedPeriod === "day") {
            const formatter = new Intl.DateTimeFormat("en-US", { hour: "numeric" });
            return Array.from({ length: 24 }, (_, index) => {
              const date = new Date();
              date.setMinutes(0, 0, 0);
              date.setHours(date.getHours() - (23 - index));
              return {
                key: date.toISOString().slice(0, 13),
                hourKey: date.toISOString().slice(0, 13),
                label: formatter.format(date),
                startMs: date.getTime(),
                endMs: date.getTime() + 60 * 60 * 1000,
              };
            });
          }
          if (normalizedPeriod === "week") {
            const formatter = new Intl.DateTimeFormat("en-US", { weekday: "short" });
            return Array.from({ length: 7 }, (_, index) => {
              const date = new Date();
              date.setHours(0, 0, 0, 0);
              date.setDate(date.getDate() - (6 - index));
              return {
                key: date.toISOString().slice(0, 10),
                label: formatter.format(date),
                startMs: date.getTime(),
                endMs: date.getTime() + 24 * 60 * 60 * 1000,
              };
            });
          }
          const formatter = new Intl.DateTimeFormat("en-US", { month: "short", day: "numeric" });
          return Array.from({ length: 30 }, (_, index) => {
            const date = new Date();
            date.setHours(0, 0, 0, 0);
            date.setDate(date.getDate() - (29 - index));
            return {
              key: date.toISOString().slice(0, 10),
              label: formatter.format(date),
              startMs: date.getTime(),
              endMs: date.getTime() + 24 * 60 * 60 * 1000,
            };
          });
        }
  
        function buildPlaygroundAgentAnalyticsUrl(backendUrl, agentId) {
          if (!backendUrl || !agentId) return "";
          return backendUrl + "/agents/" + encodeURIComponent(agentId) + "/analytics";
        }
  
        function buildPlaygroundVoiceAgentsUrl(backendUrl) {
          if (!backendUrl) return "";
          return backendUrl + "/voice-agents";
        }
  
        function buildPlaygroundVoiceAgentUrl(backendUrl, agentId) {
          if (!backendUrl || !agentId) return "";
          return backendUrl + "/voice-agents/agents/" + encodeURIComponent(agentId);
        }
  
        function buildPlaygroundVoiceAgentPhoneNumberUrl(backendUrl, agentId) {
          if (!backendUrl || !agentId) return "";
          return backendUrl + "/voice-agents/agents/" + encodeURIComponent(agentId) + "/phone-number";
        }
  
        function buildPlaygroundVoiceAgentSessionsUrl(backendUrl, agentId) {
          if (!backendUrl || !agentId) return "";
          return backendUrl + "/voice-agents/agents/" + encodeURIComponent(agentId) + "/sessions";
        }
  
        function buildPlaygroundServerFilesListUrl(backendUrl, serverId, folderPath = "", depth = 1) {
          if (!backendUrl || !serverId) return "";
          const normalizedFolderPath = normalizeHistoryPath(folderPath);
          const params = new URLSearchParams();
          params.set("depth", String(depth));
          if (normalizedFolderPath) {
            params.set("path", normalizedFolderPath);
          }
          return backendUrl + "/servers/" + encodeURIComponent(serverId) + "/files?" + params.toString();
        }
  
        function buildPlaygroundServerFileContentUrl(backendUrl, serverId, filePath = "") {
          if (!backendUrl || !serverId || !filePath) return "";
          const normalizedPath = normalizeHistoryPath(filePath);
          if (!normalizedPath) return "";
          return backendUrl + "/servers/" + encodeURIComponent(serverId) + "/files/content/" + normalizedPath
            .split("/")
            .filter(Boolean)
            .map((segment) => encodeURIComponent(segment))
            .join("/");
        }
  
        function buildPlaygroundServerAnalyticsUrl(backendUrl, serverId, period = "day") {
          if (!backendUrl || !serverId) return "";
          const params = new URLSearchParams();
          params.set("period", normalizePlaygroundEnvironmentHomeChartPeriod(period));
          return backendUrl + "/servers/" + encodeURIComponent(serverId) + "/analytics?" + params.toString();
        }
  
        function buildPlaygroundServerAnalyticsStateKey(serverId, period = "day") {
          const normalizedServerId = String(serverId || "").trim();
          if (!normalizedServerId) return "";
          return normalizedServerId + ":" + normalizePlaygroundEnvironmentHomeChartPeriod(period);
        }
  
        function buildPlaygroundServerBindingsUrl(backendUrl, serverId) {
          if (!backendUrl || !serverId) return "";
          return backendUrl + "/servers/" + encodeURIComponent(serverId) + "/bindings";
        }
  
        function buildPlaygroundServerContextUrl(backendUrl, serverId) {
          if (!backendUrl || !serverId) return "";
          return backendUrl + "/servers/" + encodeURIComponent(serverId) + "/context";
        }
  
        function buildPlaygroundServerRuntimeConfigUrl(backendUrl, serverId) {
          if (!backendUrl || !serverId) return "";
          return backendUrl + "/servers/" + encodeURIComponent(serverId) + "/runtime-config";
        }
  
        function buildPlaygroundServerRuntimeSdkUrl(backendUrl, serverId, target = "server") {
          if (!backendUrl || !serverId) return "";
          const normalizedTarget = String(target || "").trim().toLowerCase() === "browser" ? "browser" : "server";
          return backendUrl + "/servers/" + encodeURIComponent(serverId) + "/runtime-sdk/" + encodeURIComponent(normalizedTarget);
        }
  
        function buildPlaygroundServerBindingTargetUrl(backendUrl, serverId, targetType) {
          if (!backendUrl || !serverId || !targetType) return "";
          return backendUrl + "/servers/" + encodeURIComponent(serverId) + "/bindings/" + encodeURIComponent(targetType);
        }
  
        function buildPlaygroundServerPaymentsConnectUrl(backendUrl, serverId) {
          if (!backendUrl || !serverId) return "";
          return backendUrl + "/servers/" + encodeURIComponent(serverId) + "/payments/connect-account";
        }
  
        function buildPlaygroundServerPaymentsSyncUrl(backendUrl, serverId) {
          if (!backendUrl || !serverId) return "";
          return backendUrl + "/servers/" + encodeURIComponent(serverId) + "/payments/sync";
        }
  
        function buildPlaygroundServerAuthUsersUrl(backendUrl, serverId, limit = 200) {
          if (!backendUrl || !serverId) return "";
          const params = new URLSearchParams();
          params.set("limit", String(Math.max(1, Math.min(1000, Number(limit) || 200))));
          return backendUrl + "/servers/" + encodeURIComponent(serverId) + "/auth-users?" + params.toString();
        }
  
        function buildPlaygroundServerSecretsUrl(backendUrl, serverId) {
          if (!backendUrl || !serverId) return "";
          return backendUrl + "/servers/" + encodeURIComponent(serverId) + "/secrets";
        }
  
        function buildPlaygroundServerSecretUrl(backendUrl, serverId, secretId) {
          if (!backendUrl || !serverId || !secretId) return "";
          return backendUrl + "/servers/" + encodeURIComponent(serverId) + "/secrets/" + encodeURIComponent(secretId);
        }
  
        function buildPlaygroundServerRunsUrl(backendUrl, serverId, limit = 50) {
          if (!backendUrl || !serverId) return "";
          const params = new URLSearchParams();
          params.set("limit", String(Math.max(1, Math.min(200, Number(limit) || 50))));
          return backendUrl + "/servers/" + encodeURIComponent(serverId) + "/runs?" + params.toString();
        }
  
        function buildPlaygroundServerRunUrl(backendUrl, serverId, runId, includeEvents = false) {
          if (!backendUrl || !serverId || !runId) return "";
          const base = backendUrl + "/servers/" + encodeURIComponent(serverId) + "/runs/" + encodeURIComponent(runId);
          return includeEvents ? base + "/events" : base;
        }
  
        function buildPlaygroundServerRunCancelUrl(backendUrl, serverId, runId) {
          if (!backendUrl || !serverId || !runId) return "";
          return backendUrl + "/servers/" + encodeURIComponent(serverId) + "/runs/" + encodeURIComponent(runId) + "/cancel";
        }
  
        function normalizePlaygroundRealApiBackendUrl(backendUrl) {
          const fallback = typeof window !== "undefined" && window.location?.origin
            ? window.location.origin + "/api/real"
            : "/api/real";
          const rawBackendUrl = String(backendUrl || "").trim().replace(/\\/+$/, "");
          if (!rawBackendUrl) {
            return fallback;
          }
  
          try {
            const currentOrigin = typeof window !== "undefined" && window.location?.origin ? window.location.origin : "";
            const parsedUrl = new URL(rawBackendUrl, currentOrigin || "http://localhost");
            const normalizedPathname = String(parsedUrl.pathname || "/").replace(/\\/+$/, "") || "/";
            if (
              currentOrigin
              && parsedUrl.origin === currentOrigin
              && normalizedPathname !== "/api/real"
              && !normalizedPathname.startsWith("/api/real/")
            ) {
              return parsedUrl.origin + "/api/real";
            }
          } catch {
          }
  
          return rawBackendUrl;
        }
  
        function buildPlaygroundServerLogsUrl(backendUrl, serverId, kind = "request", limit = 80) {
          if (!backendUrl || !serverId) return "";
          const params = new URLSearchParams();
          params.set("kind", String(kind || "request"));
          params.set("limit", String(Math.max(1, Math.min(250, Number(limit) || 80))));
          return backendUrl + "/servers/" + encodeURIComponent(serverId) + "/logs?" + params.toString();
        }
  
        function buildPlaygroundServerDeploymentsUrl(backendUrl, serverId) {
          if (!backendUrl || !serverId) return "";
          return backendUrl + "/servers/" + encodeURIComponent(serverId) + "/deployments";
        }
  
        function buildPlaygroundServerRollbackUrl(backendUrl, serverId) {
          if (!backendUrl || !serverId) return "";
          return backendUrl + "/servers/" + encodeURIComponent(serverId) + "/rollback";
        }
  
        function buildPlaygroundDatabaseBootstrapUrl(backendUrl, databaseId, documentsLimit = 25) {
          if (!backendUrl || !databaseId) return "";
          const params = new URLSearchParams();
          params.set("documentsLimit", String(Math.max(1, Math.min(100, Number(documentsLimit) || 25))));
          return backendUrl + "/databases/" + encodeURIComponent(databaseId) + "/bootstrap?" + params.toString();
        }
  
        function buildPlaygroundDatabaseCollectionsUrl(backendUrl, databaseId) {
          if (!backendUrl || !databaseId) return "";
          return backendUrl + "/databases/" + encodeURIComponent(databaseId) + "/collections";
        }
  
        function buildPlaygroundDatabaseDocumentsUrl(backendUrl, databaseId, collectionId, limit = 100) {
          if (!backendUrl || !databaseId || !collectionId) return "";
          const params = new URLSearchParams();
          params.set("limit", String(Math.max(1, Math.min(250, Number(limit) || 100))));
          return backendUrl
            + "/databases/" + encodeURIComponent(databaseId)
            + "/collections/" + encodeURIComponent(collectionId)
            + "/documents?" + params.toString();
        }
  
        function buildPlaygroundDatabaseDocumentUrl(backendUrl, databaseId, collectionId, documentId) {
          if (!backendUrl || !databaseId || !collectionId || !documentId) return "";
          return backendUrl
            + "/databases/" + encodeURIComponent(databaseId)
            + "/collections/" + encodeURIComponent(collectionId)
            + "/documents/" + encodeURIComponent(documentId);
        }
  
        function buildPlaygroundDatabaseAnalyticsUrl(backendUrl, databaseId, period = "day") {
          if (!backendUrl || !databaseId) return "";
  	    const params = new URLSearchParams();
  	    params.set("period", normalizePlaygroundEnvironmentHomeChartPeriod(period));
  	    return backendUrl + "/databases/" + encodeURIComponent(databaseId) + "/analytics?" + params.toString();
  	  }
  
  	  function buildPlaygroundDatabaseAnalyticsStateKey(databaseId, period = "day") {
  	    const normalizedDatabaseId = String(databaseId || "").trim();
  	    if (!normalizedDatabaseId) return "";
  	    return normalizedDatabaseId + ":" + normalizePlaygroundEnvironmentHomeChartPeriod(period);
        }
  
        function canonicalizePlaygroundServerKind(kind) {
          const normalizedKind = String(kind || "").trim().toLowerCase();
          if (normalizedKind === "web app" || normalizedKind === "website") return "web_app";
          if (normalizedKind === "function") return "function";
          if (normalizedKind === "database") return "database";
          if (normalizedKind === "api") return "api";
          if (normalizedKind === "auth") return "auth";
          if (normalizedKind === "agent runtime") return "agent_runtime";
          if (normalizedKind === "agent_runtime") return "agent_runtime";
          if (normalizedKind === "voice agent" || normalizedKind === "voice agents") return "voice_agent";
          if (normalizedKind === "voice_agent" || normalizedKind === "voice_agents") return "voice_agent";
          if (normalizedKind === "secrets") return "secrets";
          if (normalizedKind === "payments" || normalizedKind === "payment") return "payments";
          return "web_app";
        }
  
  	      function formatPlaygroundServerKindLabel(kind) {
  	        const normalizedKind = canonicalizePlaygroundServerKind(kind);
  	        if (normalizedKind === "function") return "Function";
  	        if (normalizedKind === "database") return "Database";
  	        if (normalizedKind === "api") return "APIs";
  	        if (normalizedKind === "auth") return "Authentication";
  	        if (normalizedKind === "agent_runtime") return "Agent Runtime";
  	        if (normalizedKind === "voice_agent") return "Voice Agent";
  	        if (normalizedKind === "secrets") return "Secrets";
  	        if (normalizedKind === "payments") return "Payments";
  	        return "Web App";
  	      }
  
        function normalizePlaygroundServerOverviewKind(kind) {
          const rawKind = String(kind || "").trim();
          if (!rawKind) {
            return "";
          }
          const normalizedRawKind = rawKind.toLowerCase();
          if (!["web_app", "web app", "website", "function", "database", "api", "auth", "agent_runtime", "agent runtime", "voice_agent", "voice_agents", "voice agent", "voice agents", "secrets", "payments", "payment"].includes(normalizedRawKind)) {
            return "";
          }
          const normalizedKind = canonicalizePlaygroundServerKind(rawKind);
          return ["web_app", "function", "database", "api", "auth", "agent_runtime", "voice_agent", "secrets", "payments"].includes(normalizedKind)
            ? normalizedKind
            : "";
        }
  
        function formatPlaygroundServerKindPluralLabel(kind) {
  	        const normalizedKind = canonicalizePlaygroundServerKind(kind);
  	        if (normalizedKind === "web_app") return "Web Apps";
  	        if (normalizedKind === "api") return "API";
  	        if (normalizedKind === "auth") return "Authentication";
  	        if (normalizedKind === "agent_runtime") return "Agent Runtime";
  	        if (normalizedKind === "voice_agent") return "Voice Agents";
  	        if (normalizedKind === "secrets") return "Secrets";
  	        if (normalizedKind === "payments") return "Payments";
  	        return formatPlaygroundServerKindLabel(normalizedKind) + "s";
  	      }
  
        function normalizePlaygroundServerBindingRecord(binding) {
          if (!binding || typeof binding !== "object") {
            return null;
          }
          const targetType = String(binding.targetType || "").trim().toLowerCase();
          if (!["database", "auth", "agent_runtime", "secrets", "payments"].includes(targetType)) {
            return null;
          }
          const resource = binding.resource && typeof binding.resource === "object" && !Array.isArray(binding.resource)
            ? binding.resource
            : null;
          return {
            id: typeof binding.id === "string" ? binding.id : "",
            serverId: typeof binding.serverId === "string" ? binding.serverId : "",
            targetType,
            targetId: typeof binding.targetId === "string" ? binding.targetId : "",
            alias: typeof binding.alias === "string" ? binding.alias : "",
            accessMode: typeof binding.accessMode === "string" ? binding.accessMode : "default",
            metadata: binding.metadata && typeof binding.metadata === "object" && !Array.isArray(binding.metadata) ? binding.metadata : null,
            createdAt: typeof binding.createdAt === "string" ? binding.createdAt : "",
            updatedAt: typeof binding.updatedAt === "string" ? binding.updatedAt : "",
            resource: resource
              ? {
                  id: typeof resource.id === "string" ? resource.id : "",
                  name: typeof resource.name === "string" ? resource.name : "",
                  kind: canonicalizePlaygroundServerKind(resource.kind),
                  location: typeof resource.location === "string" ? resource.location : "",
                  status: typeof resource.status === "string" ? resource.status : "",
                  documentCount: Number.isFinite(Number(resource.documentCount)) ? Number(resource.documentCount) : null,
                }
              : null,
          };
        }
  
        function normalizePlaygroundSecretRecord(secret) {
          if (!secret || typeof secret !== "object") {
            return null;
          }
          const id = typeof secret.id === "string" ? secret.id.trim() : "";
          const name = typeof secret.name === "string" ? secret.name.trim() : "";
          if (!id && !name) {
            return null;
          }
          return {
            id: id || name,
            name: name || id,
            description: typeof secret.description === "string" ? secret.description : "",
            maskedValue: typeof secret.maskedValue === "string"
              ? secret.maskedValue
              : typeof secret.value === "string"
                ? secret.value
                : "••••••••",
            valueEncrypted: secret.valueEncrypted !== false,
            metadata: secret.metadata && typeof secret.metadata === "object" && !Array.isArray(secret.metadata) ? secret.metadata : null,
            createdAt: typeof secret.createdAt === "string" ? secret.createdAt : "",
            updatedAt: typeof secret.updatedAt === "string" ? secret.updatedAt : "",
            lastAccessedAt: typeof secret.lastAccessedAt === "string" ? secret.lastAccessedAt : "",
          };
        }
  
        function normalizePlaygroundServerDeploymentRecord(deployment) {
          if (!deployment || typeof deployment !== "object") {
            return null;
          }
          const id = typeof deployment.id === "string" && deployment.id.trim()
            ? deployment.id.trim()
            : typeof deployment.deploymentId === "string" && deployment.deploymentId.trim()
              ? deployment.deploymentId.trim()
              : "";
          const revision = typeof deployment.revision === "string" ? deployment.revision.trim() : "";
          const at = typeof deployment.at === "string" && deployment.at ? deployment.at : "";
          if (!id && !revision && !at) {
            return null;
          }
          return {
            id: id || revision || at,
            at,
            outcome: String(deployment.outcome || "").trim().toLowerCase() || "success",
            type: typeof deployment.type === "string" ? deployment.type : "",
            serviceName: typeof deployment.serviceName === "string" ? deployment.serviceName : "",
            region: typeof deployment.region === "string" ? deployment.region : "",
            revision,
            serviceUrl: typeof deployment.serviceUrl === "string" ? deployment.serviceUrl : "",
            imageUrl: typeof deployment.imageUrl === "string" ? deployment.imageUrl : "",
            runtime: typeof deployment.runtime === "string" ? deployment.runtime : "",
            sourceEnvironmentId: typeof deployment.sourceEnvironmentId === "string" ? deployment.sourceEnvironmentId : "",
            sourcePath: typeof deployment.sourcePath === "string" ? deployment.sourcePath : "",
            error: typeof deployment.error === "string" ? deployment.error : "",
            rolledBackToDeploymentId: typeof deployment.rolledBackToDeploymentId === "string" ? deployment.rolledBackToDeploymentId : "",
          };
        }
  
        function buildPlaygroundServerCurrentDeploymentFallback(server) {
          if (!server || typeof server !== "object") {
            return null;
          }
          const metadata = server.metadata && typeof server.metadata === "object" && !Array.isArray(server.metadata)
            ? server.metadata
            : {};
          const lastDeployment = metadata.lastDeployment && typeof metadata.lastDeployment === "object" && !Array.isArray(metadata.lastDeployment)
            ? metadata.lastDeployment
            : null;
          const serviceName = String(lastDeployment?.serviceName || server.cloudRunServiceName || metadata.serviceName || "").trim();
          const revision = String(lastDeployment?.revision || metadata.latestRevision || "").trim();
          const at = String(lastDeployment?.at || server.lastDeployedAt || server.updatedAt || "").trim();
          const serviceUrl = String(lastDeployment?.serviceUrl || server.serviceUrl || "").trim();
          const imageUrl = String(lastDeployment?.imageUrl || server.imageUrl || "").trim();
          const activeDeploymentId = String(metadata.activeDeploymentId || "").trim();
          const id = String(lastDeployment?.id || lastDeployment?.deploymentId || activeDeploymentId || revision || at || serviceName || "").trim();
          const hasDeploymentSignal = Boolean(
            id
            || revision
            || at
            || serviceUrl
            || serviceName
            || imageUrl
            || String(server.status || "").trim().toLowerCase() === "deployed"
          );
          if (!hasDeploymentSignal) {
            return null;
          }
          return normalizePlaygroundServerDeploymentRecord({
            id: id || "dep_current",
            at,
            outcome: String(lastDeployment?.outcome || server.status || "").trim().toLowerCase() === "failed" ? "failed" : "success",
            type: lastDeployment?.type || metadata.deploymentType || "",
            serviceName,
            region: lastDeployment?.region || server.region || "",
            revision,
            serviceUrl,
            imageUrl,
            runtime: lastDeployment?.runtime || server.runtime || "",
            sourceEnvironmentId: lastDeployment?.sourceEnvironmentId || server.sourceEnvironmentId || "",
            sourcePath: lastDeployment?.sourcePath || server.sourcePath || "",
            error: lastDeployment?.error || metadata.deploymentError || "",
          });
        }
  
        function normalizePlaygroundServerContextRecord(context) {
          if (!context || typeof context !== "object") {
            return null;
          }
          const runtime = context.runtime && typeof context.runtime === "object" && !Array.isArray(context.runtime)
            ? context.runtime
            : null;
          const diagnostics = context.diagnostics && typeof context.diagnostics === "object" && !Array.isArray(context.diagnostics)
            ? context.diagnostics
            : null;
          return {
            serverId: typeof context.serverId === "string" ? context.serverId : "",
            bindings: Array.isArray(context.bindings)
              ? context.bindings.map(normalizePlaygroundServerBindingRecord).filter(Boolean)
              : [],
            runtime,
            diagnostics: diagnostics
              ? {
                  ...diagnostics,
                  warnings: Array.isArray(diagnostics.warnings)
                    ? diagnostics.warnings.filter((warning) => warning && typeof warning === "object").map((warning) => ({
                        code: typeof warning.code === "string" ? warning.code : "",
                        level: String(warning.level || "").trim().toLowerCase() === "warning" ? "warning" : "info",
                        message: typeof warning.message === "string" ? warning.message : "",
                      }))
                    : [],
                }
              : null,
          };
        }
  
        function formatPlaygroundAuthProviderLabel(providerId) {
          const normalized = String(providerId || "").trim().toLowerCase();
          if (!normalized) return "Unknown";
          if (normalized === "google.com" || normalized === "google") return "Google";
          if (normalized === "microsoft.com" || normalized === "microsoft") return "Microsoft";
          if (normalized === "password" || normalized === "email") return "Email";
          if (normalized === "phone" || normalized === "phone.com") return "Phone";
          if (normalized === "github.com" || normalized === "github") return "GitHub";
          return normalized.replace(/\\.com$/g, "").replace(/[_-]+/g, " ").replace(/\\b\\w/g, (value) => value.toUpperCase());
        }
  
        function getPlaygroundAuthProviderTone(providerId) {
          const normalized = String(providerId || "").trim().toLowerCase();
          if (normalized === "google.com" || normalized === "google") return "google";
          if (normalized === "microsoft.com" || normalized === "microsoft") return "microsoft";
          if (normalized === "password" || normalized === "email") return "email";
          if (normalized === "phone" || normalized === "phone.com") return "phone";
          if (normalized === "github.com" || normalized === "github") return "github";
          return "default";
        }
  
        function getPlaygroundAuthUserIdentifier(user) {
          return String(user?.email || "").trim()
            || String(user?.phoneNumber || "").trim()
            || String(user?.displayName || "").trim()
            || String(user?.uid || "").trim()
            || "Unknown user";
        }
  
        function getPlaygroundAuthUserSearchText(user) {
          const providers = Array.isArray(user?.providers) ? user.providers.join(" ") : "";
          return [
            user?.email || "",
            user?.phoneNumber || "",
            user?.displayName || "",
            user?.uid || "",
            providers,
          ].join(" ").toLowerCase();
        }
  
        function formatPlaygroundServerLatency(value) {
          const numericValue = Number(value);
          if (!Number.isFinite(numericValue) || numericValue < 0) {
            return "—";
          }
          if (numericValue >= 1000) {
            return (Math.round((numericValue / 1000) * 10) / 10).toFixed(1).replace(/\.0$/, "") + "s";
          }
          return Math.round(numericValue) + "ms";
        }
  
        function formatPlaygroundServerRate(value) {
          const numericValue = Number(value);
          if (!Number.isFinite(numericValue) || numericValue < 0) {
            return "—";
          }
          return (Math.round(numericValue * 10) / 10).toFixed(1).replace(/\.0$/, "") + "%";
        }
  
        function formatPlaygroundExecutionDuration(value) {
          const numericValue = Number(value);
          if (!Number.isFinite(numericValue) || numericValue < 0) {
            return "—";
          }
          if (numericValue >= 60 * 60 * 1000) {
            return (Math.round((numericValue / (60 * 60 * 1000)) * 10) / 10).toFixed(1).replace(/\.0$/, "") + "h";
          }
          if (numericValue >= 60 * 1000) {
            return (Math.round((numericValue / (60 * 1000)) * 10) / 10).toFixed(1).replace(/\.0$/, "") + "m";
          }
          if (numericValue >= 1000) {
            return (Math.round((numericValue / 1000) * 10) / 10).toFixed(1).replace(/\.0$/, "") + "s";
          }
          return Math.round(numericValue) + "ms";
        }
  
        function formatPlaygroundServerLogKindLabel(kind) {
          const normalizedKind = String(kind || "").trim().toLowerCase();
          if (normalizedKind === "runtime") return "Console";
          if (normalizedKind === "deployment") return "Deploy";
          return "Requests";
        }
  
        function formatPlaygroundServerRequestStatus(status) {
          const numericStatus = Number(status);
          return Number.isFinite(numericStatus) && numericStatus > 0 ? String(Math.round(numericStatus)) : "—";
        }
  
        function formatPlaygroundDatabaseDocumentJson(value) {
          try {
            return JSON.stringify(value && typeof value === "object" ? value : {}, null, 2);
          } catch {
            return "{}";
          }
        }
  
        function getPlaygroundDatabaseDocumentResponseRecord(payload, fallback = null) {
          const candidates = [payload?.document, payload?.data, payload?.item, payload];
          const record = candidates.find((candidate) => (
            candidate
            && typeof candidate === "object"
            && !Array.isArray(candidate)
            && (candidate.id != null || candidate.data != null)
          )) || null;
          if (!record) {
            return fallback;
          }
          return {
            ...(fallback && typeof fallback === "object" ? fallback : {}),
            ...record,
            id: String(record.id || fallback?.id || "").trim(),
            data: record.data && typeof record.data === "object" && !Array.isArray(record.data)
              ? record.data
              : {},
          };
        }
  
        function isPlaygroundDatabasePlainObject(value) {
          return Boolean(value) && typeof value === "object" && !Array.isArray(value);
        }
  
        function parsePlaygroundDatabaseDocumentObject(value) {
          if (typeof value === "string") {
            try {
              const parsed = JSON.parse(value || "{}");
              return isPlaygroundDatabasePlainObject(parsed) ? parsed : {};
            } catch {
              return null;
            }
          }
          return isPlaygroundDatabasePlainObject(value) ? value : {};
        }
  
        function clonePlaygroundDatabaseValue(value) {
          try {
            return JSON.parse(JSON.stringify(value));
          } catch {
            if (Array.isArray(value)) return value.slice();
            if (isPlaygroundDatabasePlainObject(value)) return { ...value };
            return value;
          }
        }
  
        function getPlaygroundDatabasePathKey(path) {
          return Array.isArray(path) && path.length ? path.join(".") : "__root__";
        }
  
        function getPlaygroundDatabaseFieldType(value) {
          if (Array.isArray(value)) return "array";
          if (isPlaygroundDatabasePlainObject(value)) return "map";
          if (value === null) return "null";
          if (typeof value === "boolean") return "boolean";
          if (typeof value === "number") return "number";
          return "string";
        }
  
        function getPlaygroundDatabaseValueAtPath(rootValue, path) {
          if (!Array.isArray(path) || !path.length) {
            return rootValue;
          }
          let current = rootValue;
          for (const segment of path) {
            if (Array.isArray(current)) {
              const index = Number(segment);
              if (!Number.isInteger(index) || index < 0 || index >= current.length) {
                return undefined;
              }
              current = current[index];
              continue;
            }
            if (isPlaygroundDatabasePlainObject(current)) {
              current = current[segment];
              continue;
            }
            return undefined;
          }
          return current;
        }
  
        function setPlaygroundDatabaseValueAtPath(rootValue, path, nextValue) {
          if (!Array.isArray(path) || !path.length) {
            return clonePlaygroundDatabaseValue(nextValue);
          }
  
          const clonedRoot = clonePlaygroundDatabaseValue(rootValue);
          let current = clonedRoot;
  
          for (let index = 0; index < path.length - 1; index += 1) {
            const segment = path[index];
            if (Array.isArray(current)) {
              const arrayIndex = Number(segment);
              if (!Number.isInteger(arrayIndex) || arrayIndex < 0 || arrayIndex >= current.length) {
                return clonedRoot;
              }
              current[arrayIndex] = clonePlaygroundDatabaseValue(current[arrayIndex]);
              current = current[arrayIndex];
              continue;
            }
            if (!isPlaygroundDatabasePlainObject(current)) {
              return clonedRoot;
            }
            current[segment] = clonePlaygroundDatabaseValue(current[segment]);
            current = current[segment];
          }
  
          const leafSegment = path[path.length - 1];
          if (Array.isArray(current)) {
            const arrayIndex = Number(leafSegment);
            if (!Number.isInteger(arrayIndex) || arrayIndex < 0 || arrayIndex >= current.length) {
              return clonedRoot;
            }
            current[arrayIndex] = nextValue;
            return clonedRoot;
          }
          if (isPlaygroundDatabasePlainObject(current)) {
            current[leafSegment] = nextValue;
          }
          return clonedRoot;
        }
  
        function deletePlaygroundDatabaseValueAtPath(rootValue, path) {
          if (!Array.isArray(path) || !path.length) {
            return clonePlaygroundDatabaseValue(rootValue);
          }
  
          const clonedRoot = clonePlaygroundDatabaseValue(rootValue);
          let current = clonedRoot;
  
          for (let index = 0; index < path.length - 1; index += 1) {
            const segment = path[index];
            if (Array.isArray(current)) {
              const arrayIndex = Number(segment);
              if (!Number.isInteger(arrayIndex) || arrayIndex < 0 || arrayIndex >= current.length) {
                return clonedRoot;
              }
              current[arrayIndex] = clonePlaygroundDatabaseValue(current[arrayIndex]);
              current = current[arrayIndex];
              continue;
            }
            if (!isPlaygroundDatabasePlainObject(current)) {
              return clonedRoot;
            }
            current[segment] = clonePlaygroundDatabaseValue(current[segment]);
            current = current[segment];
          }
  
          const leafSegment = path[path.length - 1];
          if (Array.isArray(current)) {
            const arrayIndex = Number(leafSegment);
            if (Number.isInteger(arrayIndex) && arrayIndex >= 0 && arrayIndex < current.length) {
              current.splice(arrayIndex, 1);
            }
            return clonedRoot;
          }
          if (isPlaygroundDatabasePlainObject(current)) {
            delete current[leafSegment];
          }
          return clonedRoot;
        }
  
        function createPlaygroundDatabaseFieldValue(type, rawValue) {
          const normalizedType = String(type || "string").trim().toLowerCase();
          if (normalizedType === "number") {
            const numericValue = Number(String(rawValue || "").trim());
            if (!Number.isFinite(numericValue)) {
              throw new Error("Number value is invalid.");
            }
            return numericValue;
          }
          if (normalizedType === "boolean") {
            return String(rawValue || "").trim().toLowerCase() === "true";
          }
          if (normalizedType === "null") {
            return null;
          }
          if (normalizedType === "map") {
            return {};
          }
          if (normalizedType === "array") {
            return [];
          }
          return String(rawValue || "");
        }
  
        function formatPlaygroundDatabaseFieldPreview(value) {
          const type = getPlaygroundDatabaseFieldType(value);
          if (type === "map") {
            const size = Object.keys(value || {}).length;
            return size + " field" + (size === 1 ? "" : "s");
          }
          if (type === "array") {
            const size = Array.isArray(value) ? value.length : 0;
            return size + " item" + (size === 1 ? "" : "s");
          }
          if (type === "null") {
            return "null";
          }
          if (type === "boolean" || type === "number") {
            return String(value);
          }
          return JSON.stringify(String(value || ""));
        }
  
  ${FILES_DOMAIN_FRAGMENTS.inventory}
  
  ${FILES_DOMAIN_FRAGMENTS.transfer}
  
        function resolveThreadSortTimestamp(thread) {
          return thread.updatedAt || thread.createdAt || thread.nextRunAt || "";
        }
  
        function compareThreadsByRecent(left, right) {
          const leftTime = new Date(resolveThreadSortTimestamp(left)).getTime();
          const rightTime = new Date(resolveThreadSortTimestamp(right)).getTime();
          return (Number.isFinite(rightTime) ? rightTime : 0) - (Number.isFinite(leftTime) ? leftTime : 0);
        }
  
        function isCompletedThreadStatus(status) {
          const normalizedStatus = String(status || "").trim().toLowerCase();
          return ["completed", "complete", "done", "succeeded", "success", "finished"].includes(normalizedStatus);
        }
  
        function isTerminalThreadDisplayStatus(status) {
          const normalizedStatus = String(status || "").trim().toLowerCase();
          return ["completed", "complete", "done", "succeeded", "success", "finished", "failed", "cancelled", "canceled"].includes(normalizedStatus);
        }
  
        function isRunningThreadDisplayStatus(status) {
          const normalizedStatus = String(status || "").trim().toLowerCase();
          return ["running", "queued", "pending", "scheduled", "starting", "created", "ready"].includes(normalizedStatus);
        }
  
        function shouldPreserveThreadDisplayStatus(status) {
          return isTerminalThreadDisplayStatus(status) || isPendingPermissionThreadDisplayStatus(status) || isRunningThreadDisplayStatus(status);
        }
  
        function resolveThreadDisplayStatus(status, completedAt) {
          const normalizedCompletedAt = String(completedAt || "").trim();
          if (isPendingPermissionThreadDisplayStatus(status)) {
            return "permission_asked";
          }
          if (normalizedCompletedAt && !shouldPreserveThreadDisplayStatus(status)) {
            return "completed";
          }
          return typeof status === "string" ? status.trim() : "";
        }
  
        function threadHasStaleCompletionAfterUpdate(thread) {
          const completedAtMs = new Date(String(thread?.completedAt || thread?.finishedAt || thread?.endedAt || "")).getTime();
          const updatedAtMs = new Date(String(thread?.updatedAt || "")).getTime();
          return Number.isFinite(completedAtMs) && Number.isFinite(updatedAtMs) && updatedAtMs > completedAtMs + 1000;
        }
  
        function preserveActiveThreadStatusForStaleCompletion(existingThread, incomingThread, focusedThreadId) {
          const normalizedThreadId = String(incomingThread?.id || "").trim();
          if (!normalizedThreadId || normalizedThreadId !== String(focusedThreadId || "").trim()) {
            return incomingThread;
          }
          if (!isActiveThreadDisplayStatus(existingThread?.status) || !isCompletedThreadStatus(incomingThread?.status)) {
            return incomingThread;
          }
          if (!threadHasStaleCompletionAfterUpdate(incomingThread)) {
            return incomingThread;
          }
          return {
            ...incomingThread,
            status: existingThread.status,
          };
        }
  
        function isActiveThreadDisplayStatus(status) {
          const normalizedStatus = String(status || "").trim().toLowerCase();
          return ["running", "queued", "pending", "scheduled", "starting", "created", "ready"].includes(normalizedStatus);
        }
  
        function isPendingPermissionThreadDisplayStatus(status) {
          const normalizedStatus = String(status || "").trim().toLowerCase();
          return normalizedStatus === "permission_asked" || normalizedStatus === "permission asked";
        }
  
        function normalizeThreadTodoListItems(items) {
          if (!Array.isArray(items)) {
            return [];
          }
          return items
            .map((item) => {
              if (typeof item === "string") {
                const text = item.trim();
                return text ? { text, completed: false } : null;
              }
              if (!item || typeof item !== "object") {
                return null;
              }
              const text = String(item.text || item.content || item.title || item.message || "").trim();
              if (!text) {
                return null;
              }
              const normalizedStatus = String(item.status || item.state || "").trim().toLowerCase();
              const completed = Boolean(item.completed)
                || normalizedStatus === "completed"
                || normalizedStatus === "complete"
                || normalizedStatus === "done";
              return { text, completed };
            })
            .filter(Boolean);
        }
  
        function parseThreadTodoListCandidateValue(value, depth = 0) {
          if (depth > 3 || value == null) {
            return [];
          }
          if (typeof value === "string") {
            const trimmed = value.trim();
            if (!trimmed || (!trimmed.startsWith("[") && !trimmed.startsWith("{"))) {
              return [];
            }
            try {
              return parseThreadTodoListCandidateValue(JSON.parse(trimmed), depth + 1);
            } catch {
              return [];
            }
          }
          const directItems = normalizeThreadTodoListItems(value);
          if (directItems.length > 0) {
            return directItems;
          }
          if (!value || typeof value !== "object" || Array.isArray(value)) {
            return [];
          }
          const record = value;
          const directKeys = ["todos", "todoItems", "todo_items", "taskList", "task_list", "todoList", "todo_list", "items", "tasks"];
          for (const key of directKeys) {
            const items = parseThreadTodoListCandidateValue(record[key], depth + 1);
            if (items.length > 0) {
              return items;
            }
          }
          const nestedKeys = ["metadata", "logMetadata", "log_metadata", "result", "output", "data", "payload", "body", "args", "arguments", "input"];
          for (const key of nestedKeys) {
            const items = parseThreadTodoListCandidateValue(record[key], depth + 1);
            if (items.length > 0) {
              return items;
            }
          }
          return [];
        }
  
        function getThreadTodoLogArray(data) {
          if (Array.isArray(data?.logs)) {
            return data.logs;
          }
          if (Array.isArray(data?.data)) {
            return data.data;
          }
          return Array.isArray(data) ? data : [];
        }
  
        function getThreadTodoLogMetadata(log) {
          if (!log || typeof log !== "object") {
            return {};
          }
          if (log.metadata && typeof log.metadata === "object" && !Array.isArray(log.metadata)) {
            return log.metadata;
          }
          if (log.logMetadata && typeof log.logMetadata === "object" && !Array.isArray(log.logMetadata)) {
            return log.logMetadata;
          }
          return {};
        }
  
        function extractLatestThreadTodoList(data) {
          const logs = getThreadTodoLogArray(data);
          const candidates = [];
          logs.forEach((log, index) => {
            if (!log || typeof log !== "object") {
              return;
            }
            const metadata = getThreadTodoLogMetadata(log);
            const eventType = String(log.eventType || log.event_type || log.kind || "").trim().toLowerCase();
            const toolName = String(metadata.toolName || metadata.tool_name || "").trim().toLowerCase();
            const isTodoListSignal = eventType === "todo_list"
              || toolName === "todowrite"
              || toolName === "todo_write"
              || toolName === "todo-write";
            if (eventType && !isTodoListSignal) {
              return;
            }
            const todos = parseThreadTodoListCandidateValue(log);
            if (todos.length === 0 && !isTodoListSignal) {
              return;
            }
            const timestamp = String(log.createdAt || log.created_at || log.timestamp || log.time || "").trim();
            const timestampMs = Date.parse(timestamp);
            candidates.push({
              todos,
              index,
              timestampMs: Number.isFinite(timestampMs) ? timestampMs : null,
            });
          });
          if (candidates.length === 0) {
            return [];
          }
          candidates.sort((left, right) => {
            if (left.timestampMs !== null && right.timestampMs !== null && left.timestampMs !== right.timestampMs) {
              return left.timestampMs - right.timestampMs;
            }
            if (left.timestampMs !== null && right.timestampMs === null) {
              return 1;
            }
            if (left.timestampMs === null && right.timestampMs !== null) {
              return -1;
            }
            return left.index - right.index;
          });
          return candidates[candidates.length - 1].todos;
        }
  
        function resolveThreadCompletionTimestampMs(thread) {
          const metadata = thread?.metadata && typeof thread.metadata === "object" && !Array.isArray(thread.metadata)
            ? thread.metadata
            : {};
          const runnerPlaygroundMetadata = metadata?.runnerPlayground && typeof metadata.runnerPlayground === "object" && !Array.isArray(metadata.runnerPlayground)
            ? metadata.runnerPlayground
            : {};
          const candidates = [
            thread?.completedAt,
            thread?.completed_at,
            thread?.finishedAt,
            thread?.finished_at,
            thread?.endedAt,
            thread?.ended_at,
            metadata?.completedAt,
            metadata?.completed_at,
            metadata?.finishedAt,
            metadata?.finished_at,
            metadata?.endedAt,
            metadata?.ended_at,
            runnerPlaygroundMetadata?.completedAt,
            runnerPlaygroundMetadata?.completed_at,
            thread?.updatedAt,
            resolveThreadSortTimestamp(thread),
            thread?.createdAt,
          ];
          for (const candidate of candidates) {
            const timestamp = new Date(String(candidate || "")).getTime();
            if (Number.isFinite(timestamp)) {
              return timestamp;
            }
          }
          return 0;
        }
  
        function formatThreadSearchTimestamp(value) {
          if (!value) return "";
          const date = new Date(value);
          if (Number.isNaN(date.getTime())) return "";
  
          const diffMs = Math.max(0, Date.now() - date.getTime());
          const minuteMs = 60 * 1000;
          const hourMs = 60 * minuteMs;
          const dayMs = 24 * hourMs;
          const weekMs = 7 * dayMs;
  
          if (diffMs < hourMs) {
            const minutes = Math.max(1, Math.round(diffMs / minuteMs));
            return minutes + " min ago";
          }
  
          if (diffMs < dayMs) {
            const hours = Math.max(1, Math.round(diffMs / hourMs));
            return hours + " hr" + (hours === 1 ? "" : "s") + " ago";
          }
  
          if (diffMs < weekMs) {
            const days = Math.max(1, Math.round(diffMs / dayMs));
            return days + " day" + (days === 1 ? "" : "s") + " ago";
          }
  
          const weeks = Math.max(1, Math.round(diffMs / weekMs));
          return weeks + " week" + (weeks === 1 ? "" : "s") + " ago";
        }
  
        function getThreadSearchBucket(value) {
          if (!value) {
            return { key: "older", label: "Older" };
          }
  
          const date = new Date(value);
          if (Number.isNaN(date.getTime())) {
            return { key: "older", label: "Older" };
          }
  
          const diffMs = Math.max(0, Date.now() - date.getTime());
          const dayMs = 24 * 60 * 60 * 1000;
  
          if (diffMs < dayMs) {
            return { key: "today", label: "Today" };
          }
  
          if (diffMs < 2 * dayMs) {
            return { key: "yesterday", label: "Yesterday" };
          }
  
          if (diffMs < 7 * dayMs) {
            return { key: "last-7-days", label: "Last 7 Days" };
          }
  
          if (diffMs < 30 * dayMs) {
            return { key: "last-30-days", label: "Last 30 Days" };
          }
  
          return { key: "older", label: "Older" };
        }
  
        function normalizeHistoryPath(value) {
          const raw = String(value || "").trim();
          if (!raw) return "";
          let normalized = raw.split("\\\\").join("/");
          while (normalized.startsWith("/")) {
            normalized = normalized.slice(1);
          }
          if (normalized.startsWith("workspace/")) {
            normalized = normalized.slice("workspace/".length);
          }
          return normalized;
        }
  
        function shouldHideHistoryChangePath(value) {
          const normalized = normalizeHistoryPath(value);
          return Boolean(normalized) && /\\.jsonl$/i.test(normalized);
        }
  
        function historyPathsMatch(left, right) {
          return normalizeHistoryPath(left) === normalizeHistoryPath(right);
        }
  
        function uniqueHistoryPaths(values) {
          const seen = new Set();
          return values
            .map(normalizeHistoryPath)
            .filter((value) => {
              if (!value || seen.has(value)) return false;
              seen.add(value);
              return true;
            });
        }
  
        function formatHistoryTimestamp(value) {
          if (!value) return "";
          const date = new Date(value);
          if (Number.isNaN(date.getTime())) return "";
          return new Intl.DateTimeFormat(undefined, {
            month: "short",
            day: "numeric",
            hour: "numeric",
            minute: "2-digit",
          }).format(date);
        }
  
        function formatHistoryDateHeading(value) {
          if (!value) return "Changes";
          const date = new Date(value);
          if (Number.isNaN(date.getTime())) return "Changes";
          return "Changes on " + new Intl.DateTimeFormat(undefined, {
            month: "short",
            day: "numeric",
            year: "numeric",
          }).format(date);
        }
  
        function getHistoryDateGroupKey(value) {
          if (!value) return "unknown";
          const date = new Date(value);
          if (Number.isNaN(date.getTime())) return "unknown";
          return [
            date.getFullYear(),
            String(date.getMonth() + 1).padStart(2, "0"),
            String(date.getDate()).padStart(2, "0"),
          ].join("-");
        }
  
        function normalizeHistoryPreviewText(value, maxLength = 140) {
          const normalized = String(value || "").replace(/\\s+/g, " ").trim();
          if (!normalized) return "";
          return normalized.length > maxLength ? normalized.slice(0, maxLength - 1).trimEnd() + "…" : normalized;
        }
  
        function getHistoryLogCommand(log) {
          const metadata = log && log.metadata && typeof log.metadata === "object" ? log.metadata : {};
          return typeof metadata.command === "string" ? metadata.command : "";
        }
  
        function isReadOnlyHistorySyntheticFileCommand(command) {
          const normalized = String(command || "").trim().replace(/^\\$\\s*/, "");
          if (!normalized) {
            return false;
          }
  
          if (/(^|[^>])>>?/.test(normalized)) {
            return false;
          }
  
          const readOnlyCommands = new Set([
            "cat",
            "diff",
            "du",
            "env",
            "exiftool",
            "file",
            "find",
            "git",
            "grep",
            "head",
            "identify",
            "less",
            "ls",
            "od",
            "pwd",
            "readlink",
            "realpath",
            "rg",
            "sed",
            "sort",
            "stat",
            "tail",
            "wc",
            "which",
          ]);
          const writeCommands = new Set([
            "cp",
            "curl",
            "ffmpeg",
            "install",
            "magick",
            "mkdir",
            "mv",
            "rm",
            "tee",
            "touch",
            "wget",
          ]);
  
          const segments = normalized.split(/\\s*(?:&&|\\|\\||;)\\s*/).map((segment) => segment.trim()).filter(Boolean);
          if (segments.length === 0) {
            return false;
          }
  
          function resolveCommandName(segment) {
            const tokens = String(segment || "").split(/\\s+/).filter(Boolean);
            let index = 0;
            while (index < tokens.length && /^[A-Za-z_][A-Za-z0-9_]*=/.test(tokens[index])) {
              index += 1;
            }
            if (tokens[index] === "sudo") {
              index += 1;
            }
            return (tokens[index] || "").toLowerCase();
          }
  
          let sawReadOnly = false;
          for (const segment of segments) {
            const commandName = resolveCommandName(segment);
            if (!commandName) {
              return false;
            }
            if (writeCommands.has(commandName)) {
              return false;
            }
            if (!readOnlyCommands.has(commandName)) {
              return false;
            }
            sawReadOnly = true;
          }
  
          return sawReadOnly;
        }
  
        function shouldIgnoreSyntheticHistoryFileStep(step, historyLogsById) {
          if (!step || step.eventType !== "file_change") {
            return false;
          }
          const metadata = step && step.metadata && typeof step.metadata === "object" ? step.metadata : {};
          const routeSource = typeof metadata.routeSource === "string" ? metadata.routeSource : "";
          if (routeSource !== "threads.command_execution_generated_image") {
            return false;
          }
          if (!historyLogsById || typeof step.sourceMessageId !== "string" || !step.sourceMessageId) {
            return false;
          }
          const hasDiffs = metadata.diffs && typeof metadata.diffs === "object" && Object.keys(metadata.diffs).length > 0;
          const hasFileContents = metadata.fileContents && typeof metadata.fileContents === "object" && Object.keys(metadata.fileContents).length > 0;
          if (hasDiffs || hasFileContents) {
            return false;
          }
          const sourceLog = historyLogsById.get(step.sourceMessageId);
          if (!sourceLog || sourceLog.eventType !== "command_execution") {
            return false;
          }
          return isReadOnlyHistorySyntheticFileCommand(getHistoryLogCommand(sourceLog));
        }
  
        function extractStepChangedPaths(step, historyLogsById) {
          if (shouldIgnoreSyntheticHistoryFileStep(step, historyLogsById)) {
            return [];
          }
          const metadata = step && step.metadata && typeof step.metadata === "object" ? step.metadata : {};
          const values = [];
          if (Array.isArray(metadata.changedPaths)) values.push(...metadata.changedPaths);
          if (Array.isArray(metadata.filePaths)) values.push(...metadata.filePaths);
          if (metadata.diffs && typeof metadata.diffs === "object") values.push(...Object.keys(metadata.diffs));
          if (typeof metadata.path === "string") values.push(metadata.path);
          return uniqueHistoryPaths(values).filter((value) => !shouldHideHistoryChangePath(value));
        }
  
        function getHistoryPathName(value) {
          const normalized = normalizeHistoryPath(value);
          if (!normalized) return "";
          const segments = normalized.split("/").filter(Boolean);
          return segments[segments.length - 1] || normalized;
        }
  
  ${FILES_DOMAIN_FRAGMENTS.filename}
  
        function isHistoryImagePath(value) {
          return /\\.(png|jpe?g|gif|webp|svg|avif|bmp)$/i.test(normalizeHistoryPath(value));
        }
  
        function normalizeGeneratedHistoryImagePath(value) {
          const normalized = normalizeHistoryPath(value);
          return normalized && isHistoryImagePath(normalized) ? normalized : "";
        }
  
        function normalizeHistoryChangeKind(value) {
          const normalized = typeof value === "string" ? value.trim().toLowerCase() : "";
          return normalized === "created" || normalized === "modified" || normalized === "deleted"
            ? normalized
            : null;
        }
  
        function buildHistoryStructuredPatchDiff(filePath, patches, operation) {
          const normalizedPath = normalizeHistoryPath(filePath);
          if (!normalizedPath || !Array.isArray(patches) || patches.length === 0) {
            return "";
          }
          const oldHeaderPath = operation === "created" ? "/dev/null" : "a/" + normalizedPath;
          const newHeaderPath = operation === "deleted" ? "/dev/null" : "b/" + normalizedPath;
          const lines = ["--- " + oldHeaderPath, "+++ " + newHeaderPath];
  
          for (const patch of patches) {
            const oldStart = Number.isFinite(Number(patch?.oldStart)) ? Number(patch.oldStart) : 1;
            const oldLines = Number.isFinite(Number(patch?.oldLines)) ? Number(patch.oldLines) : 0;
            const newStart = Number.isFinite(Number(patch?.newStart)) ? Number(patch.newStart) : 1;
            const newLines = Number.isFinite(Number(patch?.newLines)) ? Number(patch.newLines) : 0;
            lines.push("@@ -" + oldStart + "," + oldLines + " +" + newStart + "," + newLines + " @@");
            if (Array.isArray(patch?.lines)) {
              lines.push(...patch.lines.map((entry) => String(entry)));
            }
          }
  
          return lines.join("\\n");
        }
  
        function extractHistoryStructuredWritePayload(output) {
          const normalizeOperation = (value) => {
            const normalized = typeof value === "string" ? value.trim().toLowerCase() : "";
            if (!normalized) return null;
            if (normalized === "created" || normalized === "create" || normalized === "new" || normalized === "write") return "created";
            if (
              normalized === "modified"
              || normalized === "modify"
              || normalized === "updated"
              || normalized === "update"
              || normalized === "edit"
              || normalized === "edited"
              || normalized === "replace"
            ) {
              return "modified";
            }
            if (normalized === "delete" || normalized === "deleted" || normalized === "remove" || normalized === "removed") {
              return "deleted";
            }
            return null;
          };
  
          const visit = (value) => {
            if (value == null) return null;
            if (Array.isArray(value)) {
              for (const entry of value) {
                const nested = visit(entry);
                if (nested) return nested;
              }
              return null;
            }
            if (typeof value === "string") {
              const trimmed = value.trim();
              if (!trimmed || (!trimmed.startsWith("{") && !trimmed.startsWith("["))) {
                return null;
              }
              try {
                return visit(JSON.parse(trimmed));
              } catch {
                return null;
              }
            }
            if (typeof value !== "object") return null;
  
            const record = value;
            const filePathCandidate =
              typeof record.filePath === "string" ? record.filePath
              : typeof record.file_path === "string" ? record.file_path
              : typeof record.path === "string" ? record.path
              : "";
            const contentCandidate =
              typeof record.content === "string" ? record.content
              : typeof record.text === "string" ? record.text
              : typeof record.newContent === "string" ? record.newContent
              : typeof record.newString === "string" ? record.newString
              : "";
            const operationCandidate =
              normalizeOperation(record.operationKind)
              || normalizeOperation(record.operation_kind)
              || normalizeOperation(record.operation)
              || normalizeOperation(record.type)
              || normalizeOperation(record.mode);
            const structuredPatch = Array.isArray(record.structuredPatch)
              ? record.structuredPatch
              : Array.isArray(record.structured_patch)
                ? record.structured_patch
                : [];
            const gitDiff =
              typeof record.gitDiff === "string" && record.gitDiff.trim()
                ? record.gitDiff
                : typeof record.diff === "string" && record.diff.trim()
                  ? record.diff
                  : typeof record.changes === "string" && record.changes.trim()
                    ? record.changes
                    : "";
            const diffText =
              gitDiff
              || (filePathCandidate && structuredPatch.length > 0
                ? buildHistoryStructuredPatchDiff(filePathCandidate, structuredPatch, operationCandidate || "modified")
                : "");
            const diffStats = diffText ? summarizeUnifiedDiffStats(diffText) : null;
  
            if (filePathCandidate || contentCandidate || operationCandidate || diffText) {
              return {
                ...(filePathCandidate ? { filePath: filePathCandidate } : {}),
                ...(contentCandidate ? { content: contentCandidate } : {}),
                ...(operationCandidate ? { operation: operationCandidate } : {}),
                ...(diffText ? { diffText } : {}),
                ...(diffStats ? { additions: diffStats.additions, deletions: diffStats.deletions } : {}),
              };
            }
  
            const nestedCandidates = [
              record.file,
              record.result,
              record.payload,
              record.data,
              record.structuredContent,
              record.structured_content,
            ];
            for (const candidate of nestedCandidates) {
              const nested = visit(candidate);
              if (nested) return nested;
            }
            return null;
          };
  
          return visit(output);
        }
  
        function extractDeletedHistoryPathFromCommandOutput(log) {
          if (!log || log.eventType !== "command_execution") {
            return "";
          }
          const metadata = log.metadata && typeof log.metadata === "object" ? log.metadata : {};
          const output = typeof metadata.output === "string" ? metadata.output : "";
          if (!output) {
            return "";
          }
  
          let combinedOutput = output;
          try {
            const parsed = JSON.parse(output);
            const stdout = typeof parsed?.stdout === "string" ? parsed.stdout : "";
            const stderr = typeof parsed?.stderr === "string" ? parsed.stderr : "";
            combinedOutput = [stdout, stderr, output].filter(Boolean).join("\\n");
          } catch {}
  
          if (!/no such file or directory/i.test(combinedOutput)) {
            return "";
          }
  
          const pathMatch =
            combinedOutput.match(/cannot access ['"]([^'"]+)['"]:\\s+No such file or directory/i)
            || combinedOutput.match(/cannot remove ['"]([^'"]+)['"]:\\s+No such file or directory/i);
          return pathMatch?.[1] ? normalizeHistoryPath(pathMatch[1]) : "";
        }
  
        function createHistoryImageFileEntry(filePath, changeKind) {
          const normalizedPath = normalizeGeneratedHistoryImagePath(filePath);
          if (!normalizedPath) {
            return null;
          }
          return {
            path: normalizedPath,
            name: getHistoryPathName(normalizedPath),
            type: "file",
            size: null,
            changeKind: normalizeHistoryChangeKind(changeKind),
            additions: null,
            deletions: null,
            diffText: "",
            isChanged: true,
          };
        }
  
        function asHistoryObjectRecord(value) {
          return value && typeof value === "object" && !Array.isArray(value) ? value : null;
        }
  
        function asHistoryOptionalTrimmedString(value) {
          return typeof value === "string" && value.trim() ? value.trim() : "";
        }
  
        function normalizeHistoryNumericValue(value) {
          const parsed = typeof value === "number" ? value : Number(value);
          return Number.isFinite(parsed) ? parsed : null;
        }
  
        function normalizeHistoryTaskTicketNumber(value) {
          const normalized = String(value || "").trim();
          if (!normalized) return "";
          const digits = Array.from(normalized).filter((character) => character >= "0" && character <= "9").join("");
          const parsed = Number.parseInt(digits || normalized, 10);
          if (!Number.isFinite(parsed) || parsed <= 0) {
            return "";
          }
          return String(parsed).padStart(3, "0");
        }
  
        function normalizeHistoryTaskStatus(value) {
          const normalized = String(value || "").trim().toLowerCase();
          if (normalized === "todo" || normalized === "backlog") return "todo";
          if (normalized === "in_progress" || normalized === "blocked" || normalized === "done") return normalized;
          return "";
        }
  
        function normalizeHistoryTaskPriority(value) {
          const normalized = String(value || "").trim().toLowerCase();
          if (normalized === "low" || normalized === "medium" || normalized === "high" || normalized === "critical") {
            return normalized;
          }
          return "";
        }
  
        function normalizeHistoryTaskType(value) {
          const normalized = String(value || "").trim().toLowerCase();
          if (normalized === "subtask") return "subtask";
          if (normalized === "loop" || normalized === "loop_task" || normalized === "metronome_loop") return "loop";
          if (normalized === "task") return "task";
          return "";
        }
  
        function splitHistoryTaskTitleAndTicket(rawTitle) {
          const trimmedTitle = String(rawTitle || "").trim();
          const prefixedTicketMatch = trimmedTitle.match(/^((?:[A-Z]+-\\d+)|(?:\\d{2,4}))(?:(?:\\s*[·\\-:]\\s*)|\\s+)(.+)$/);
          if (prefixedTicketMatch?.[1] && prefixedTicketMatch[2]) {
            return {
              ticketNumber: prefixedTicketMatch[1].trim(),
              title: prefixedTicketMatch[2].trim() || trimmedTitle,
            };
          }
          return { title: trimmedTitle, ticketNumber: "" };
        }
  
        function formatHistoryResourceStatusLabel(value) {
          const normalized = String(value || "").trim().toLowerCase();
          if (!normalized) return "";
          if (normalized === "todo") return "To do";
          if (normalized === "in_progress") return "In progress";
          if (normalized === "done") return "Done";
          return normalized
            .split(/[_\\s-]+/)
            .filter(Boolean)
            .map((segment) => segment.charAt(0).toUpperCase() + segment.slice(1))
            .join(" ");
        }
  
        function formatHistoryTaskPriorityLabel(value) {
          const normalized = String(value || "").trim().toLowerCase();
          if (!normalized) return "";
          return normalized.charAt(0).toUpperCase() + normalized.slice(1);
        }
  
        function formatHistoryTaskTypeLabel(value) {
          return String(value || "").trim().toLowerCase() === "subtask" ? "Subtask" : "Task";
        }
  
        function normalizeHistoryResourceType(value) {
          const normalized = typeof value === "string" ? value.trim().toLowerCase() : "";
          if (!normalized) return "";
          if (normalized === "agent" || normalized === "agents") return "agent";
          if (normalized === "skill" || normalized === "skills") return "skill";
          if (normalized === "task" || normalized === "tasks" || normalized === "subtask" || normalized === "subtasks") return "task";
          if (normalized === "release" || normalized === "releases") return "release";
          if (
            normalized === "environment"
            || normalized === "environments"
            || normalized === "env"
            || normalized === "envs"
          ) {
            return "environment";
          }
          return "";
        }
  
        function inferHistoryResourceTypeFromId(value) {
          const normalized = String(value || "").trim().toLowerCase();
          if (!normalized) return "";
          if (normalized.startsWith("agent_")) return "agent";
          if (normalized.startsWith("skill_")) return "skill";
          if (normalized.startsWith("task_")) return "task";
          if (normalized.startsWith("release_")) return "release";
          if (normalized.startsWith("env_") || normalized.startsWith("environment_")) return "environment";
          return "";
        }
  
        function getHistoryResourceTypeLabel(resourceType) {
          if (resourceType === "agent") return "Agent";
          if (resourceType === "skill") return "Skill";
          if (resourceType === "task") return "Task";
          if (resourceType === "release") return "Milestone";
          if (resourceType === "environment") return "Environment";
          return "Resource";
        }
  
        function getHistoryResourceSyntheticPath(resourceType, resourceId, name) {
          const typeSegment = normalizeHistoryResourceType(resourceType) || "resource";
          const rawIdentifier = String(resourceId || name || "").trim();
          const normalizedIdentifier = rawIdentifier
            ? rawIdentifier.replace(/[^A-Za-z0-9._-]+/g, "-").replace(/^-+|-+$/g, "")
            : "";
          return "__computer_agents__/" + typeSegment + "/" + (normalizedIdentifier || "created");
        }
  
        function extractHistoryCommandFlagValue(command, flagPattern) {
          const flagRegex = new RegExp("(?:^|\\s)(?:" + flagPattern + ")\\s+", "i");
          const match = String(command || "").match(flagRegex);
          if (!match || match.index === undefined) return null;
          const rest = String(command || "").slice(match.index + match[0].length);
          const first = rest[0];
          if (first === '"' || first === "'") {
            let value = "";
            for (let index = 1; index < rest.length; index += 1) {
              const current = rest[index];
              if (current === String.fromCharCode(92) && index + 1 < rest.length) {
                value += rest[index + 1];
                index += 1;
                continue;
              }
              if (current === first) break;
              value += current;
            }
            return value.trim();
          }
          const unquoted = rest.match(/^(\\S+)/);
          return unquoted ? unquoted[1].trim() : null;
        }
  
        function inferComputerAgentsResourceTypeFromCommand(command) {
          const normalized = String(command || "").trim();
          if (!normalized) return "";
          if (/computer-agents\\.py[\\s\\S]*\\bagents\\s+create\\b/i.test(normalized)) return "agent";
          if (/computer-agents\\.py[\\s\\S]*\\bskills\\s+create\\b/i.test(normalized)) return "skill";
          if (/computer-agents\\.py[\\s\\S]*\\benvironments\\s+create\\b/i.test(normalized)) return "environment";
          if (/computer-agents\\.py[\\s\\S]*\\benvironments\\s+clone\\b/i.test(normalized)) return "environment";
          return "";
        }
  
        function inferComputerAgentsResourceTypeFromToolLog(log) {
          const metadata = log && log.metadata && typeof log.metadata === "object" ? log.metadata : {};
          const serverName = String(metadata.serverName || "").trim().toLowerCase();
          const toolName = String(metadata.toolName || "").trim().toLowerCase();
          const combined = [serverName, toolName].filter(Boolean).join(" ");
          if (!combined) return "";
          if (!/(computer[_ -]?agents?|agents?|skills?|environments?|envs?)/.test(combined)) {
            return "";
          }
          if (/agents?/.test(combined) && /create|new/.test(combined)) return "agent";
          if (/skills?/.test(combined) && /create|new/.test(combined)) return "skill";
          if (/(?:environments?|envs?)/.test(combined) && /create|clone|new/.test(combined)) return "environment";
          return "";
        }
  
        function isComputerAgentsCreateHistoryLog(log) {
          if (!log || typeof log !== "object") return false;
          const metadata = log.metadata && typeof log.metadata === "object" ? log.metadata : {};
          return Boolean(
            inferComputerAgentsResourceTypeFromCommand(metadata.command)
            || inferComputerAgentsResourceTypeFromToolLog(log)
          );
        }
  
        function createHistoryResourceEntry(resourceType, record) {
          const resource = record && typeof record === "object" ? record : {};
          const metadata = asHistoryObjectRecord(resource.metadata);
          const runnerPlayground = asHistoryObjectRecord(metadata?.runnerPlayground);
          const assigneeRecord = asHistoryObjectRecord(resource.assignee) || asHistoryObjectRecord(resource.assigneeAgent);
          const normalizedType =
            normalizeHistoryResourceType(resourceType)
            || normalizeHistoryResourceType(resource.resourceType)
            || normalizeHistoryResourceType(resource.object)
            || normalizeHistoryResourceType(resource.type)
            || inferHistoryResourceTypeFromId(resource.id);
          if (!normalizedType) {
            return null;
          }
          const resourceId =
            asHistoryOptionalTrimmedString(resource.id)
            || asHistoryOptionalTrimmedString(resource.taskId)
            || asHistoryOptionalTrimmedString(resource.task_id)
            || asHistoryOptionalTrimmedString(resource.environmentId)
            || asHistoryOptionalTrimmedString(resource.environment_id)
            || asHistoryOptionalTrimmedString(resource.releaseId)
            || asHistoryOptionalTrimmedString(resource.release_id);
          const rawResourceName =
            asHistoryOptionalTrimmedString(resource.name)
            || asHistoryOptionalTrimmedString(resource.title)
            || asHistoryOptionalTrimmedString(resource.label)
            || asHistoryOptionalTrimmedString(resource.releaseName)
            || asHistoryOptionalTrimmedString(resource.release_name);
          const taskTitleParts = normalizedType === "task" ? splitHistoryTaskTitleAndTicket(rawResourceName) : null;
          const resourceName = normalizedType === "task" ? (taskTitleParts?.title || rawResourceName) : rawResourceName;
          if (!resourceId && !resourceName) {
            return null;
          }
          const description = normalizeHistoryPreviewText(
            resource.description
            || resource.instructions
            || resource.markdown
            || resource.documentation
            || "",
            220
          );
          const taskStatus = normalizeHistoryTaskStatus(
            asHistoryOptionalTrimmedString(resource.status)
            || asHistoryOptionalTrimmedString(runnerPlayground?.status)
          );
          const taskPriority = normalizeHistoryTaskPriority(
            asHistoryOptionalTrimmedString(resource.priority)
            || asHistoryOptionalTrimmedString(runnerPlayground?.priority)
          );
          const explicitTaskType =
            normalizeHistoryTaskType(resource.taskType)
            || normalizeHistoryTaskType(resource.task_type)
            || normalizeHistoryTaskType(resource.type)
            || normalizeHistoryTaskType(runnerPlayground?.taskType);
          const parentTaskId =
            asHistoryOptionalTrimmedString(resource.parentTaskId)
            || asHistoryOptionalTrimmedString(resource.parent_task_id)
            || asHistoryOptionalTrimmedString(runnerPlayground?.parentTaskId);
          const ticketNumber = normalizeHistoryTaskTicketNumber(
            asHistoryOptionalTrimmedString(resource.ticketNumber)
            || asHistoryOptionalTrimmedString(resource.ticket_number)
            || asHistoryOptionalTrimmedString(resource.ticket)
            || asHistoryOptionalTrimmedString(runnerPlayground?.ticketNumber)
            || (taskTitleParts ? taskTitleParts.ticketNumber : "")
          );
          const projectId =
            asHistoryOptionalTrimmedString(resource.projectId)
            || asHistoryOptionalTrimmedString(resource.project_id)
            || asHistoryOptionalTrimmedString(metadata?.projectId)
            || asHistoryOptionalTrimmedString(metadata?.project_id);
          const projectName =
            asHistoryOptionalTrimmedString(resource.projectName)
            || asHistoryOptionalTrimmedString(resource.project_name);
          const environmentId =
            asHistoryOptionalTrimmedString(resource.environmentId)
            || asHistoryOptionalTrimmedString(resource.environment_id)
            || asHistoryOptionalTrimmedString(runnerPlayground?.environmentId);
          const assigneeName =
            asHistoryOptionalTrimmedString(resource.assigneeName)
            || asHistoryOptionalTrimmedString(resource.assignee_name)
            || asHistoryOptionalTrimmedString(resource.assigneeAgentName)
            || asHistoryOptionalTrimmedString(resource.assigneeActorName)
            || asHistoryOptionalTrimmedString(resource.assignedToName)
            || asHistoryOptionalTrimmedString(assigneeRecord?.name)
            || asHistoryOptionalTrimmedString(assigneeRecord?.displayName);
          return {
            path: getHistoryResourceSyntheticPath(normalizedType, resourceId, resourceName),
            name: resourceName || getHistoryResourceTypeLabel(normalizedType),
            type: "resource",
            resourceType: normalizedType,
            resourceTypeLabel: getHistoryResourceTypeLabel(normalizedType),
            resourceId: resourceId || "",
            changeKind: "created",
            additions: null,
            deletions: null,
            diffText: "",
            isChanged: true,
            description,
            category: asHistoryOptionalTrimmedString(resource.category),
            model:
              asHistoryOptionalTrimmedString(resource.model)
              || asHistoryOptionalTrimmedString(resource.deepResearchModel),
            projectId,
            projectName,
            environmentId,
            isDefault: Boolean(resource.isDefault),
            ticketNumber,
            status: normalizedType === "task" ? taskStatus : asHistoryOptionalTrimmedString(resource.status),
            priority: normalizedType === "task" ? taskPriority : "",
            taskType: normalizedType === "task" ? (explicitTaskType || (parentTaskId ? "subtask" : "task")) : "",
            assigneeName,
            startAt: asHistoryOptionalTrimmedString(resource.startAt),
            endAt: asHistoryOptionalTrimmedString(resource.endAt),
            taskCount: normalizeHistoryNumericValue(resource.taskCount),
            openTaskCount: normalizeHistoryNumericValue(resource.openTaskCount),
          };
        }
  
        function extractComputerAgentsResourceEntriesFromValue(value, fallbackType) {
          const entries = new Map();
  
          function push(resourceType, record) {
            const entry = createHistoryResourceEntry(resourceType, record);
            if (!entry) return;
            const existing = entries.get(entry.path);
            entries.set(entry.path, existing ? { ...existing, ...entry } : entry);
          }
  
          function visit(candidate, hintedType) {
            if (!candidate) return;
            if (typeof candidate === "string") {
              const trimmed = candidate.trim();
              if ((trimmed.startsWith("{") || trimmed.startsWith("[")) && trimmed.length > 1) {
                try {
                  visit(JSON.parse(trimmed), hintedType);
                } catch {}
              }
              return;
            }
            if (Array.isArray(candidate)) {
              for (const item of candidate) visit(item, hintedType);
              return;
            }
            if (typeof candidate !== "object") {
              return;
            }
  
            const record = candidate;
            if (record.agent && typeof record.agent === "object") push("agent", record.agent);
            if (record.skill && typeof record.skill === "object") push("skill", record.skill);
            if (record.environment && typeof record.environment === "object") push("environment", record.environment);
  
            if (Array.isArray(record.agents)) {
              for (const item of record.agents) push("agent", item);
            }
            if (Array.isArray(record.skills)) {
              for (const item of record.skills) push("skill", item);
            }
            if (Array.isArray(record.environments)) {
              for (const item of record.environments) push("environment", item);
            }
  
            const inferredType =
              normalizeHistoryResourceType(hintedType)
              || normalizeHistoryResourceType(record.resourceType)
              || normalizeHistoryResourceType(record.object)
              || normalizeHistoryResourceType(record.type)
              || inferHistoryResourceTypeFromId(record.id);
            if (inferredType && (record.id || record.name || record.title)) {
              push(inferredType, record);
            }
  
            for (const nestedValue of Object.values(record)) {
              if (nestedValue && typeof nestedValue === "object") {
                visit(nestedValue, null);
              } else if (typeof nestedValue === "string") {
                visit(nestedValue, null);
              }
            }
          }
  
          visit(value, fallbackType);
          return Array.from(entries.values());
        }
  
        function extractComputerAgentsResourceEntriesFromHistoryOutput(output, fallbackType) {
          const entries = new Map();
          const normalizedOutput = typeof output === "string" ? output : "";
          if (!normalizedOutput.trim()) {
            return [];
          }
  
          for (const entry of extractComputerAgentsResourceEntriesFromValue(normalizedOutput, fallbackType)) {
            entries.set(entry.path, entry);
          }
  
          const fallbackTypeLabel = normalizeHistoryResourceType(fallbackType);
          const creationPatterns = [
            { type: "agent", pattern: /\\bagent created\\b[:\\s-]*(.+?)\\s*\\((agent_[A-Za-z0-9_-]+)\\)/ig },
            { type: "skill", pattern: /\\bskill created\\b[:\\s-]*(.+?)\\s*\\((skill_[A-Za-z0-9_-]+)\\)/ig },
            { type: "environment", pattern: /\\benvironment created\\b[:\\s-]*(.+?)\\s*\\(((?:env|environment)_[A-Za-z0-9_-]+)\\)/ig },
          ];
          for (const { type, pattern } of creationPatterns) {
            for (const match of normalizedOutput.matchAll(pattern)) {
              const entry = createHistoryResourceEntry(type, {
                id: match[2],
                name: String(match[1] || "").trim(),
              });
              if (entry) {
                entries.set(entry.path, entry);
              }
            }
          }
  
          if (entries.size === 0 && fallbackTypeLabel) {
            const idPattern =
              fallbackTypeLabel === "agent"
                ? /(agent_[A-Za-z0-9_-]+)/i
                : fallbackTypeLabel === "skill"
                  ? /(skill_[A-Za-z0-9_-]+)/i
                  : /((?:env|environment)_[A-Za-z0-9_-]+)/i;
            const idMatch = normalizedOutput.match(idPattern);
            if (idMatch) {
              const entry = createHistoryResourceEntry(fallbackTypeLabel, {
                id: idMatch[1],
              });
              if (entry) {
                entries.set(entry.path, entry);
              }
            }
          }
  
          return Array.from(entries.values());
        }
  
        function extractComputerAgentsResourceEntriesFromHistoryLog(log) {
          const metadata = log && log.metadata && typeof log.metadata === "object" ? log.metadata : {};
          const command = typeof metadata.command === "string" ? metadata.command : "";
          const output = typeof metadata.output === "string" ? metadata.output : "";
          const result = metadata.result;
          const args = metadata.args;
          const fileContents = metadata.fileContents && typeof metadata.fileContents === "object"
            ? Object.values(metadata.fileContents)
            : [];
          const fallbackType =
            inferComputerAgentsResourceTypeFromCommand(command)
            || inferComputerAgentsResourceTypeFromToolLog(log);
          const entries = new Map();
  
          function append(list) {
            for (const entry of Array.isArray(list) ? list : []) {
              if (entry && typeof entry === "object" && entry.path) {
                const existing = entries.get(entry.path);
                entries.set(entry.path, existing ? { ...existing, ...entry } : entry);
              }
            }
          }
  
          append(extractComputerAgentsResourceEntriesFromValue(result, fallbackType));
          append(extractComputerAgentsResourceEntriesFromValue(args, fallbackType));
          append(extractComputerAgentsResourceEntriesFromHistoryOutput(output, fallbackType));
          append(extractComputerAgentsResourceEntriesFromHistoryOutput(log?.message || "", fallbackType));
          for (const value of fileContents) {
            append(extractComputerAgentsResourceEntriesFromHistoryOutput(typeof value === "string" ? value : "", fallbackType));
          }
  
          if (entries.size === 0 && (fallbackType || isComputerAgentsCreateHistoryLog(log))) {
            const fallbackName = extractHistoryCommandFlagValue(command, "--name");
            if (fallbackName) {
              const fallbackEntry = createHistoryResourceEntry(fallbackType, {
                id: extractHistoryCommandFlagValue(command, "--id") || "",
                name: fallbackName,
                description: extractHistoryCommandFlagValue(command, "--description") || "",
                model: extractHistoryCommandFlagValue(command, "--model") || "",
                category: extractHistoryCommandFlagValue(command, "--category") || "",
                projectId: extractHistoryCommandFlagValue(command, "--project-id") || "",
              });
              if (fallbackEntry) {
                entries.set(fallbackEntry.path, fallbackEntry);
              }
            }
          }
  
          return Array.from(entries.values());
        }
  
        function isTaskManagementCreateCommand(command) {
          return /manage-tasks\\.py[\\s\\S]*\\btasks\\s+create\\b/i.test(String(command || ""));
        }
  
        function isTaskManagementCreateToolInvocation(log) {
          const metadata = log && log.metadata && typeof log.metadata === "object" ? log.metadata : {};
          const serverName = String(metadata.serverName || "").trim().toLowerCase();
          const toolName = String(metadata.toolName || "").trim().toLowerCase();
          return (
            /task/.test(serverName || toolName)
            && (
              /(?:^|[._/-])create(?:[._/-])?tasks?(?:$|[._/-])/.test(toolName)
              || /(?:^|[._/-])tasks?(?:[._/-])create(?:$|[._/-])/.test(toolName)
            )
          );
        }
  
        function isTaskManagementCreateHistoryLog(log) {
          const metadata = log && log.metadata && typeof log.metadata === "object" ? log.metadata : {};
          return Boolean(isTaskManagementCreateCommand(metadata.command) || isTaskManagementCreateToolInvocation(log));
        }
  
        function buildHistoryTaskEntryFromTaskPayload(value) {
          const payload = asHistoryObjectRecord(value);
          if (!payload) return null;
          const taskRecord = asHistoryObjectRecord(payload.task);
          if (!taskRecord) return null;
          const details = asHistoryObjectRecord(payload.details);
          const project = asHistoryObjectRecord(details?.project);
          const assignee = asHistoryObjectRecord(details?.assignee);
          return createHistoryResourceEntry("task", {
            ...taskRecord,
            ...(project && !Object.prototype.hasOwnProperty.call(taskRecord, "projectName")
              ? {
                  projectName:
                    asHistoryOptionalTrimmedString(project.name)
                    || asHistoryOptionalTrimmedString(project.title)
                    || "",
                  projectId: asHistoryOptionalTrimmedString(project.id) || "",
                }
              : {}),
            ...(assignee && !Object.prototype.hasOwnProperty.call(taskRecord, "assigneeName")
              ? {
                  assigneeName:
                    asHistoryOptionalTrimmedString(assignee.name)
                    || asHistoryOptionalTrimmedString(assignee.displayName)
                    || "",
                  assigneeAgentId: asHistoryOptionalTrimmedString(assignee.id) || "",
                }
              : {}),
          });
        }
  
        function normalizeTaskManagementTaskEntry(value) {
          const record = asHistoryObjectRecord(value);
          if (!record) return null;
          const metadata = asHistoryObjectRecord(record.metadata);
          const runnerPlayground = asHistoryObjectRecord(metadata?.runnerPlayground);
          const title = asHistoryOptionalTrimmedString(record.title)
            || asHistoryOptionalTrimmedString(record.name)
            || asHistoryOptionalTrimmedString(record.taskTitle);
          const titleParts = splitHistoryTaskTitleAndTicket(title);
          const ticketNumber = normalizeHistoryTaskTicketNumber(
            asHistoryOptionalTrimmedString(record.ticketNumber)
            || asHistoryOptionalTrimmedString(record.ticket_number)
            || asHistoryOptionalTrimmedString(record.ticket)
            || asHistoryOptionalTrimmedString(runnerPlayground?.ticketNumber)
            || titleParts.ticketNumber
          );
          const explicitTaskType =
            normalizeHistoryTaskType(record.taskType)
            || normalizeHistoryTaskType(record.task_type)
            || (["task", "subtask", "loop"].includes(String(record.type || "").trim().toLowerCase()) ? normalizeHistoryTaskType(record.type) : "")
            || normalizeHistoryTaskType(runnerPlayground?.taskType);
          const hasExplicitTaskIdentifier =
            String(record.id || "").trim().startsWith("task_")
            || Object.prototype.hasOwnProperty.call(record, "taskId")
            || Object.prototype.hasOwnProperty.call(record, "task_id");
          const runnerPlaygroundHasTaskSignals =
            runnerPlayground !== null
            && (
              Boolean(asHistoryOptionalTrimmedString(runnerPlayground.ticketNumber))
              || Boolean(normalizeHistoryTaskType(runnerPlayground.taskType))
              || Boolean(asHistoryOptionalTrimmedString(runnerPlayground.taskColor))
              || Boolean(asHistoryOptionalTrimmedString(runnerPlayground.assigneeActorId))
              || Array.isArray(runnerPlayground.dependencyIds)
              || Array.isArray(runnerPlayground.linkedThreadIds)
            );
          const looksLikeTaskRecord =
            Boolean(titleParts.title)
            && (
              hasExplicitTaskIdentifier
              || Boolean(ticketNumber)
              || Boolean(explicitTaskType)
              || Object.prototype.hasOwnProperty.call(record, "assigneeAgentId")
              || Object.prototype.hasOwnProperty.call(record, "parentTaskId")
              || Object.prototype.hasOwnProperty.call(record, "linkedThreadIds")
              || Object.prototype.hasOwnProperty.call(record, "dependencyIds")
              || runnerPlaygroundHasTaskSignals
            );
          if (!looksLikeTaskRecord) {
            return null;
          }
          return createHistoryResourceEntry("task", {
            ...record,
            title: titleParts.title || title,
            ticketNumber: ticketNumber || undefined,
            taskType: explicitTaskType || undefined,
          });
        }
  
        function extractTaskManagementTaskEntriesFromHistoryValue(value) {
          const entries = new Map();
          const visited = new WeakSet();
  
          function append(entry) {
            if (!entry || !entry.path) return;
            const existing = entries.get(entry.path);
            entries.set(entry.path, existing ? { ...existing, ...entry } : entry);
          }
  
          function visit(current, depth) {
            if (!current || depth > 6) return;
            if (Array.isArray(current)) {
              current.forEach((item) => visit(item, depth + 1));
              return;
            }
            if (typeof current === "string") {
              const trimmed = current.trim();
              if ((trimmed.startsWith("{") || trimmed.startsWith("[")) && trimmed.length > 1) {
                try {
                  visit(JSON.parse(trimmed), depth + 1);
                } catch {}
              }
              return;
            }
            const record = asHistoryObjectRecord(current);
            if (!record || visited.has(record)) return;
            visited.add(record);
  
            append(buildHistoryTaskEntryFromTaskPayload(record));
            append(normalizeTaskManagementTaskEntry(record));
  
            for (const nestedValue of Object.values(record)) {
              if (nestedValue && typeof nestedValue === "object") {
                visit(nestedValue, depth + 1);
              }
            }
          }
  
          visit(value, 0);
          return Array.from(entries.values());
        }
  
        function extractTaskManagementTaskEntriesFromHistoryText(text) {
          const normalized = typeof text === "string" ? text.trim() : "";
          if (!normalized) return [];
          try {
            const parsed = JSON.parse(normalized);
            const structuredEntries = extractTaskManagementTaskEntriesFromHistoryValue(parsed);
            if (structuredEntries.length > 0) {
              return structuredEntries;
            }
          } catch {}
  
          const entries = [];
          const lines = normalized.split(/\\r?\\n/);
          for (const rawLine of lines) {
            const line = rawLine.trim();
            if (!line) continue;
            const createdMatch = line.match(/^(?:[+*-]\\s*)?(?:✓\\s*)?Created:\\s*(.+?)(?:\\s+\\((task_[^)]+)\\))?\\s*$/i);
            if (!createdMatch?.[1]) {
              continue;
            }
            const titleParts = splitHistoryTaskTitleAndTicket(createdMatch[1]);
            const entry = createHistoryResourceEntry("task", {
              id: createdMatch[2] || "",
              title: titleParts.title,
              ticketNumber: titleParts.ticketNumber || "",
              status: "todo",
              priority: "medium",
              taskType: "task",
            });
            if (entry) {
              entries.push(entry);
            }
          }
          return entries;
        }
  
        function extractTaskManagementTaskEntriesFromHistoryLog(log) {
          const metadata = log && log.metadata && typeof log.metadata === "object" ? log.metadata : {};
          const command = typeof metadata.command === "string" ? metadata.command : "";
          const output = typeof metadata.output === "string" ? metadata.output : "";
          const result = metadata.result;
          const args = metadata.args;
          const fileContents = metadata.fileContents && typeof metadata.fileContents === "object"
            ? Object.values(metadata.fileContents)
            : [];
          const entries = new Map();
  
          function append(list) {
            for (const entry of Array.isArray(list) ? list : []) {
              if (entry && typeof entry === "object" && entry.path) {
                const existing = entries.get(entry.path);
                entries.set(entry.path, existing ? { ...existing, ...entry } : entry);
              }
            }
          }
  
          append(extractTaskManagementTaskEntriesFromHistoryValue(result));
          append(extractTaskManagementTaskEntriesFromHistoryValue(args));
          append(extractTaskManagementTaskEntriesFromHistoryText(output));
          append(extractTaskManagementTaskEntriesFromHistoryText(log?.message || ""));
          for (const value of fileContents) {
            append(extractTaskManagementTaskEntriesFromHistoryText(typeof value === "string" ? value : ""));
          }
  
          if (entries.size === 0 && isTaskManagementCreateHistoryLog(log)) {
            const fallbackTitle = extractHistoryCommandFlagValue(command, "--title");
            if (fallbackTitle) {
              const titleParts = splitHistoryTaskTitleAndTicket(fallbackTitle);
              const fallbackEntry = createHistoryResourceEntry("task", {
                id: extractHistoryCommandFlagValue(command, "--task-id") || extractHistoryCommandFlagValue(command, "--id") || "",
                title: titleParts.title,
                ticketNumber: titleParts.ticketNumber || "",
                status: "todo",
                priority: "medium",
                taskType: extractHistoryCommandFlagValue(command, "--task-type") || "",
                projectId: extractHistoryCommandFlagValue(command, "--project-id") || "",
              });
              if (fallbackEntry) {
                entries.set(fallbackEntry.path, fallbackEntry);
              }
            }
          }
  
          return Array.from(entries.values());
        }
  
        function isTaskManagementReleaseCreateCommand(command) {
          return /manage-tasks\\.py[\\s\\S]*\\breleases\\s+create\\b/i.test(String(command || ""));
        }
  
        function isTaskManagementReleaseCreateToolInvocation(log) {
          const metadata = log && log.metadata && typeof log.metadata === "object" ? log.metadata : {};
          const serverName = String(metadata.serverName || "").trim().toLowerCase();
          const toolName = String(metadata.toolName || "").trim().toLowerCase();
          return (
            /task/.test(serverName || toolName)
            && (
              /(?:^|[._/-])create(?:[._/-])?releases?(?:$|[._/-])/.test(toolName)
              || /(?:^|[._/-])releases?(?:[._/-])create(?:$|[._/-])/.test(toolName)
            )
          );
        }
  
        function isTaskManagementReleaseCreateHistoryLog(log) {
          const metadata = log && log.metadata && typeof log.metadata === "object" ? log.metadata : {};
          return Boolean(isTaskManagementReleaseCreateCommand(metadata.command) || isTaskManagementReleaseCreateToolInvocation(log));
        }
  
        function normalizeTaskManagementReleaseEntry(value) {
          const record = asHistoryObjectRecord(value);
          if (!record) return null;
          const metadata = asHistoryObjectRecord(record.metadata);
          const normalizedType =
            normalizeHistoryResourceType(record.resourceType)
            || normalizeHistoryResourceType(record.object)
            || normalizeHistoryResourceType(record.type)
            || inferHistoryResourceTypeFromId(record.id);
          const id =
            asHistoryOptionalTrimmedString(record.id)
            || asHistoryOptionalTrimmedString(record.releaseId)
            || asHistoryOptionalTrimmedString(record.release_id);
          const name =
            asHistoryOptionalTrimmedString(record.name)
            || asHistoryOptionalTrimmedString(record.title)
            || asHistoryOptionalTrimmedString(record.releaseName)
            || asHistoryOptionalTrimmedString(record.release_name);
          const looksLikeReleaseRecord =
            normalizedType === "release"
            || id.startsWith("release_")
            || Object.prototype.hasOwnProperty.call(record, "startAt")
            || Object.prototype.hasOwnProperty.call(record, "endAt")
            || Object.prototype.hasOwnProperty.call(record, "openTaskCount")
            || Object.prototype.hasOwnProperty.call(record, "taskCount")
            || Object.prototype.hasOwnProperty.call(record, "releaseId");
          if (!looksLikeReleaseRecord || (!id && !name)) {
            return null;
          }
          return createHistoryResourceEntry("release", {
            ...record,
            id,
            name: name || "Untitled Milestone",
            projectId:
              asHistoryOptionalTrimmedString(record.projectId)
              || asHistoryOptionalTrimmedString(record.project_id)
              || asHistoryOptionalTrimmedString(metadata?.projectId)
              || "",
          });
        }
  
        function extractTaskManagementReleaseEntriesFromHistoryValue(value) {
          const entries = new Map();
          const visited = new WeakSet();
  
          function append(entry) {
            if (!entry || !entry.path) return;
            const existing = entries.get(entry.path);
            entries.set(entry.path, existing ? { ...existing, ...entry } : entry);
          }
  
          function visit(current, depth) {
            if (!current || depth > 6) return;
            if (Array.isArray(current)) {
              current.forEach((item) => visit(item, depth + 1));
              return;
            }
            if (typeof current === "string") {
              const trimmed = current.trim();
              if ((trimmed.startsWith("{") || trimmed.startsWith("[")) && trimmed.length > 1) {
                try {
                  visit(JSON.parse(trimmed), depth + 1);
                } catch {}
              }
              return;
            }
            const record = asHistoryObjectRecord(current);
            if (!record || visited.has(record)) return;
            visited.add(record);
  
            if (record.release && typeof record.release === "object") {
              append(normalizeTaskManagementReleaseEntry(record.release));
            }
            if (Array.isArray(record.releases)) {
              record.releases.forEach((item) => append(normalizeTaskManagementReleaseEntry(item)));
            }
            append(normalizeTaskManagementReleaseEntry(record));
  
            for (const nestedValue of Object.values(record)) {
              if (nestedValue && typeof nestedValue === "object") {
                visit(nestedValue, depth + 1);
              }
            }
          }
  
          visit(value, 0);
          return Array.from(entries.values());
        }
  
        function extractTaskManagementReleaseEntriesFromHistoryText(text) {
          const normalized = typeof text === "string" ? text.trim() : "";
          if (!normalized) return [];
          try {
            const parsed = JSON.parse(normalized);
            const structuredEntries = extractTaskManagementReleaseEntriesFromHistoryValue(parsed);
            if (structuredEntries.length > 0) {
              return structuredEntries;
            }
          } catch {}
  
          const entries = [];
          const lines = normalized.split(/\\r?\\n/);
          for (const rawLine of lines) {
            const line = rawLine.trim();
            if (!line) continue;
            const match = line.match(/^(?:[+*-]\\s*)?(?:✓\\s*)?(?:(?:Release|Milestone) created|Created (?:release|milestone)):\\s*(.+?)(?:\\s+\\((release_[^)]+)\\))?\\s*$/i);
            if (!match?.[1]) {
              continue;
            }
            const entry = createHistoryResourceEntry("release", {
              id: match[2] || "",
              name: match[1],
            });
            if (entry) {
              entries.push(entry);
            }
          }
          return entries;
        }
  
        function extractTaskManagementReleaseEntriesFromHistoryLog(log) {
          const metadata = log && log.metadata && typeof log.metadata === "object" ? log.metadata : {};
          const command = typeof metadata.command === "string" ? metadata.command : "";
          const output = typeof metadata.output === "string" ? metadata.output : "";
          const result = metadata.result;
          const args = metadata.args;
          const fileContents = metadata.fileContents && typeof metadata.fileContents === "object"
            ? Object.values(metadata.fileContents)
            : [];
          const entries = new Map();
  
          function append(list) {
            for (const entry of Array.isArray(list) ? list : []) {
              if (entry && typeof entry === "object" && entry.path) {
                const existing = entries.get(entry.path);
                entries.set(entry.path, existing ? { ...existing, ...entry } : entry);
              }
            }
          }
  
          append(extractTaskManagementReleaseEntriesFromHistoryValue(result));
          append(extractTaskManagementReleaseEntriesFromHistoryValue(args));
          append(extractTaskManagementReleaseEntriesFromHistoryText(output));
          append(extractTaskManagementReleaseEntriesFromHistoryText(log?.message || ""));
          for (const value of fileContents) {
            append(extractTaskManagementReleaseEntriesFromHistoryText(typeof value === "string" ? value : ""));
          }
  
          if (entries.size === 0 && isTaskManagementReleaseCreateHistoryLog(log)) {
            const fallbackName = extractHistoryCommandFlagValue(command, "--name");
            if (fallbackName) {
              const fallbackEntry = createHistoryResourceEntry("release", {
                id: extractHistoryCommandFlagValue(command, "--id") || "",
                name: fallbackName,
                description: extractHistoryCommandFlagValue(command, "--description") || "",
                projectId: extractHistoryCommandFlagValue(command, "--project-id") || "",
                startAt: extractHistoryCommandFlagValue(command, "--start-at") || "",
                endAt: extractHistoryCommandFlagValue(command, "--end-at") || "",
                status: "planned",
              });
              if (fallbackEntry) {
                entries.set(fallbackEntry.path, fallbackEntry);
              }
            }
          }
  
          return Array.from(entries.values());
        }
  
        function extractGeneratedImagePathsFromHistoryOutput(output) {
          if (typeof output !== "string" || !output.trim()) {
            return [];
          }
  
          const matches = new Set();
          const patterns = [
            /image saved to:\\s*["']?([^\\s"'\\n]+\\.(?:png|jpe?g|gif|webp|svg|avif|bmp))["']?/ig,
            /saved to:\\s*["']?([^\\s"'\\n]+\\.(?:png|jpe?g|gif|webp|svg|avif|bmp))["']?/ig,
            /((?:\\/workspace\\/|workspace\\/)?[A-Za-z0-9_./-]+\\.(?:png|jpe?g|gif|webp|svg|avif|bmp))/ig,
          ];
  
          for (const pattern of patterns) {
            for (const match of output.matchAll(pattern)) {
              const normalized = normalizeGeneratedHistoryImagePath(match[1]);
              if (normalized) {
                matches.add(normalized);
              }
            }
          }
  
          return Array.from(matches);
        }
  
        function extractGeneratedImagePathsFromHistoryResult(result) {
          const matches = new Set();
  
          function visit(value) {
            if (!value) return;
            if (typeof value === "string") {
              for (const match of extractGeneratedImagePathsFromHistoryOutput(value)) {
                matches.add(match);
              }
              return;
            }
            if (Array.isArray(value)) {
              for (const item of value) visit(item);
              return;
            }
            if (typeof value === "object") {
              const record = value;
              const structured = record.structuredContent && typeof record.structuredContent === "object"
                ? record.structuredContent
                : record.structured_content && typeof record.structured_content === "object"
                  ? record.structured_content
                  : null;
              if (structured) {
                visit(structured.workspace_file_paths);
                visit(structured.file_paths);
                visit(structured.original_file_paths);
              }
              if (typeof record.path === "string") {
                const normalized = normalizeGeneratedHistoryImagePath(record.path);
                if (normalized) matches.add(normalized);
              }
              if (typeof record.outputPath === "string") {
                const normalized = normalizeGeneratedHistoryImagePath(record.outputPath);
                if (normalized) matches.add(normalized);
              }
              if (typeof record.output_path === "string") {
                const normalized = normalizeGeneratedHistoryImagePath(record.output_path);
                if (normalized) matches.add(normalized);
              }
              for (const nestedValue of Object.values(record)) {
                visit(nestedValue);
              }
            }
          }
  
          visit(result);
          return Array.from(matches);
        }
  
        function extractGeneratedImageEntriesFromHistoryLog(log) {
          const metadata = log && log.metadata && typeof log.metadata === "object" ? log.metadata : {};
          const command = typeof metadata.command === "string" ? metadata.command : "";
          const output = typeof metadata.output === "string" ? metadata.output : "";
          const eventType = typeof log?.eventType === "string" ? log.eventType : "";
          const savedImagePath = typeof metadata.savedImagePath === "string" ? metadata.savedImagePath : "";
          const explicitFilePaths = Array.isArray(metadata.filePaths) ? metadata.filePaths : [];
          const explicitChangeKinds = Array.isArray(metadata.changeKinds) ? metadata.changeKinds : [];
          const result = metadata.result;
          const structuredResultPaths = extractGeneratedImagePathsFromHistoryResult(result);
  
          const looksLikeImageGeneration =
            metadata.isImageGeneration
            || command.includes("generate-image.py")
            || command.includes(".claude/skills/image-generation/")
            || /image saved to:/i.test(output);
          const hasExplicitImageMetadata = Boolean(savedImagePath) || explicitFilePaths.length > 0 || structuredResultPaths.length > 0;
  
          if (eventType !== "file_change" && isReadOnlyHistorySyntheticFileCommand(command)) {
            return [];
          }
  
          if (!looksLikeImageGeneration && eventType !== "file_change") {
            return [];
          }
  
          if (eventType !== "file_change" && !hasExplicitImageMetadata) {
            return [];
          }
  
          const entries = new Map();
          const fallbackChangeKind =
            normalizeHistoryChangeKind(eventType === "file_change" ? explicitChangeKinds[0] : null)
            || (looksLikeImageGeneration ? "created" : "modified");
          const push = (value, changeKind) => {
            const entry = createHistoryImageFileEntry(value, changeKind || fallbackChangeKind);
            if (entry) {
              const existing = entries.get(entry.path);
              entries.set(entry.path, existing ? { ...existing, ...entry, changeKind: existing.changeKind || entry.changeKind } : entry);
            }
          };
  
          push(savedImagePath, looksLikeImageGeneration ? "created" : fallbackChangeKind);
          for (let index = 0; index < explicitFilePaths.length; index += 1) {
            push(explicitFilePaths[index], explicitChangeKinds[index]);
          }
          for (const filePath of structuredResultPaths) push(filePath, fallbackChangeKind);
  
          return Array.from(entries.values());
        }
  
        function buildSupplementalHistoryStepEntriesById(steps, logs, historyLogsById) {
          const nextMap = new Map();
          const orderedSteps = Array.isArray(steps)
            ? [...steps].sort((left, right) => String(left?.createdAt || "").localeCompare(String(right?.createdAt || "")))
            : [];
          const orderedTurnSteps = orderedSteps.filter((step) => step && step.eventType === "turn_completed");
          const orderedLogs = Array.isArray(logs)
            ? logs
                .filter((log) => log && typeof log.createdAt === "string" && log.createdAt)
                .sort((left, right) => String(left.createdAt).localeCompare(String(right.createdAt)))
            : [];
          const assignedLogIds = new Set();
  
          function appendEntries(stepId, entries) {
            if (!stepId || !Array.isArray(entries) || entries.length === 0) {
              return;
            }
            const currentEntries = nextMap.get(stepId) || [];
            const entriesByPath = new Map(currentEntries.map((entry) => [normalizeHistoryPath(entry.path), entry]));
            for (const entry of entries) {
              const normalizedPath = normalizeHistoryPath(entry.path);
              if (!normalizedPath) {
                continue;
              }
              const existing = entriesByPath.get(normalizedPath);
              entriesByPath.set(
                normalizedPath,
                existing
                  ? {
                      ...existing,
                      ...entry,
                      changeKind: existing.changeKind || entry.changeKind,
                    }
                  : entry,
              );
            }
            nextMap.set(stepId, Array.from(entriesByPath.values()));
          }
  
          const stepsBySourceMessageId = new Map();
          for (const step of orderedSteps) {
            if (step && typeof step.sourceMessageId === "string" && step.sourceMessageId) {
              stepsBySourceMessageId.set(step.sourceMessageId, step);
            }
          }
  
          for (const log of orderedLogs) {
            if (!log || typeof log.id !== "string" || !log.id) {
              continue;
            }
            const targetStep = stepsBySourceMessageId.get(log.id);
            if (!targetStep) {
              continue;
            }
            const supplementalEntries = [
              ...extractGeneratedImageEntriesFromHistoryLog(log),
              ...extractLoggedFileChangeEntriesFromHistoryLog(log),
              ...extractComputerAgentsResourceEntriesFromHistoryLog(log),
              ...extractTaskManagementTaskEntriesFromHistoryLog(log),
              ...extractTaskManagementReleaseEntriesFromHistoryLog(log),
            ];
            if (supplementalEntries.length === 0) {
              continue;
            }
            assignedLogIds.add(log.id);
            appendEntries(targetStep.id, supplementalEntries);
          }
  
          const timelineAnchorSteps = orderedTurnSteps.length > 0 ? orderedTurnSteps : orderedSteps;
  
          for (let index = 0; index < timelineAnchorSteps.length; index += 1) {
            const step = timelineAnchorSteps[index];
            const windowStart = index > 0 ? String(timelineAnchorSteps[index - 1].createdAt || "") : "";
            const windowEnd = String(step.createdAt || "");
            const explicitChangedSteps = orderedSteps.filter((candidate) => {
              if (!candidate) return false;
              const createdAt = String(candidate.createdAt || "");
              if (!createdAt || createdAt > windowEnd || (windowStart && createdAt <= windowStart)) {
                return false;
              }
              return extractStepChangedFileEntries(candidate, historyLogsById).length > 0;
            });
  
            for (const log of orderedLogs) {
              if (typeof log?.id === "string" && assignedLogIds.has(log.id)) {
                continue;
              }
              const createdAt = String(log.createdAt || "");
              if (!createdAt || createdAt > windowEnd || (windowStart && createdAt <= windowStart)) {
                continue;
              }
              const supplementalEntries = [
                ...extractGeneratedImageEntriesFromHistoryLog(log),
                ...extractLoggedFileChangeEntriesFromHistoryLog(log),
                ...extractComputerAgentsResourceEntriesFromHistoryLog(log),
                ...extractTaskManagementTaskEntriesFromHistoryLog(log),
                ...extractTaskManagementReleaseEntriesFromHistoryLog(log),
              ];
              if (supplementalEntries.length === 0) {
                continue;
              }
              const targetStep = explicitChangedSteps.find((candidate) => String(candidate.createdAt || "") >= createdAt)
                || explicitChangedSteps[explicitChangedSteps.length - 1]
                || step;
              appendEntries(targetStep.id, supplementalEntries);
            }
          }
  
          return nextMap;
        }
  
        function buildThreadStepFileDownloadUrl(threadId, stepId, filePath) {
          const search = new URLSearchParams({ path: normalizeHistoryPath(filePath) });
          return "/api/real/threads/" + encodeURIComponent(threadId) + "/steps/" + encodeURIComponent(stepId) + "/file/download?" + search.toString();
        }
  
        function resolveHistoryDiffEntry(diffs, path) {
          if (!diffs || typeof diffs !== "object") return null;
          const normalizedPath = normalizeHistoryPath(path);
          if (!normalizedPath) return null;
          const candidates = [
            path,
            normalizedPath,
            "/workspace/" + normalizedPath,
            "workspace/" + normalizedPath,
          ].filter(Boolean);
          for (const candidate of candidates) {
            const entry = diffs[candidate];
            if (entry && typeof entry === "object") {
              return entry;
            }
          }
          return null;
        }
  
        function inferHistoryChangeKindFromDiffText(diffText) {
          const normalizedDiff = typeof diffText === "string" ? diffText.trim() : "";
          if (!normalizedDiff) return null;
          if (/^new file mode\\b/m.test(normalizedDiff)) return "created";
          if (/^deleted file mode\\b/m.test(normalizedDiff)) return "deleted";
          return "modified";
        }
  
        function extractLoggedFileChangeEntriesFromHistoryLog(log) {
          if (!log) {
            return [];
          }
  
          const metadata = log.metadata && typeof log.metadata === "object" ? log.metadata : {};
          const filePaths = Array.isArray(metadata.filePaths) ? metadata.filePaths : [];
          const changeKinds = Array.isArray(metadata.changeKinds) ? metadata.changeKinds : [];
          const diffs = metadata.diffs && typeof metadata.diffs === "object" ? metadata.diffs : {};
          const entries = new Map();
  
          function upsertEntry(rawPath, extra) {
            const normalizedPath = normalizeHistoryPath(rawPath);
            if (!normalizedPath || shouldHideHistoryChangePath(normalizedPath)) return;
            const existing = entries.get(normalizedPath) || {
              path: normalizedPath,
              name: getHistoryPathName(normalizedPath),
              type: "file",
              size: null,
              changeKind: null,
              additions: null,
              deletions: null,
              diffText: "",
              isChanged: true,
            };
            entries.set(normalizedPath, {
              ...existing,
              ...(extra || {}),
              path: normalizedPath,
              name: getHistoryPathName(normalizedPath),
              type: "file",
              size: null,
              isChanged: true,
            });
          }
  
          if (log.eventType === "command_execution") {
            const command = typeof metadata.command === "string" ? metadata.command.trim().toLowerCase() : "";
            const structuredWrite = extractHistoryStructuredWritePayload(typeof metadata.output === "string" ? metadata.output : "");
            if (command === "write_file" || command === "edit_file") {
              const filePath = structuredWrite?.filePath || filePaths[0];
              const diffText = typeof structuredWrite?.diffText === "string" ? structuredWrite.diffText.trim() : "";
              const derivedStats = diffText ? summarizeUnifiedDiffStats(diffText) : null;
              const changeKind =
                normalizeHistoryChangeKind(changeKinds[0])
                || normalizeHistoryChangeKind(structuredWrite?.operation)
                || inferHistoryChangeKindFromDiffText(diffText)
                || (command === "write_file" ? "created" : "modified");
              upsertEntry(filePath, {
                changeKind,
                additions:
                  Number.isFinite(structuredWrite?.additions) && structuredWrite.additions > 0
                    ? structuredWrite.additions
                    : derivedStats?.additions ?? null,
                deletions:
                  Number.isFinite(structuredWrite?.deletions) && structuredWrite.deletions > 0
                    ? structuredWrite.deletions
                    : derivedStats?.deletions ?? null,
                diffText,
              });
            }
  
            const deletedPath = extractDeletedHistoryPathFromCommandOutput(log);
            if (deletedPath) {
              upsertEntry(deletedPath, {
                changeKind: "deleted",
              });
            }
  
            return Array.from(entries.values());
          }
  
          if (log.eventType !== "file_change") {
            return [];
          }
  
          for (let index = 0; index < filePaths.length; index += 1) {
            const filePath = filePaths[index];
            const diffEntry = resolveHistoryDiffEntry(diffs, filePath);
            const diffText =
              typeof diffEntry?.diff === "string" && diffEntry.diff.trim()
                ? diffEntry.diff
                : typeof diffEntry?.changes === "string" && diffEntry.changes.trim()
                  ? diffEntry.changes
                  : "";
            const derivedStats = diffText ? summarizeUnifiedDiffStats(diffText) : null;
            const changeKind =
              normalizeHistoryChangeKind(changeKinds[index])
              || inferHistoryChangeKindFromDiffText(diffText);
            upsertEntry(filePath, {
              changeKind,
              additions: Number.isFinite(diffEntry?.additions) && diffEntry.additions > 0 ? diffEntry.additions : derivedStats?.additions ?? null,
              deletions: Number.isFinite(diffEntry?.deletions) && diffEntry.deletions > 0 ? diffEntry.deletions : derivedStats?.deletions ?? null,
              diffText,
            });
          }
  
          for (const [diffPath, rawDiffEntry] of Object.entries(diffs)) {
            const diffEntry = rawDiffEntry && typeof rawDiffEntry === "object" ? rawDiffEntry : {};
            const diffText =
              typeof diffEntry?.diff === "string" && diffEntry.diff.trim()
                ? diffEntry.diff
                : typeof diffEntry?.changes === "string" && diffEntry.changes.trim()
                  ? diffEntry.changes
                  : "";
            const derivedStats = diffText ? summarizeUnifiedDiffStats(diffText) : null;
            upsertEntry(diffPath, {
              changeKind: inferHistoryChangeKindFromDiffText(diffText),
              additions: Number.isFinite(diffEntry?.additions) && diffEntry.additions > 0 ? diffEntry.additions : derivedStats?.additions ?? null,
              deletions: Number.isFinite(diffEntry?.deletions) && diffEntry.deletions > 0 ? diffEntry.deletions : derivedStats?.deletions ?? null,
              diffText,
            });
          }
  
          return Array.from(entries.values());
        }
  
        function extractStepChangedFileEntries(step, historyLogsById) {
          if (shouldIgnoreSyntheticHistoryFileStep(step, historyLogsById)) {
            return [];
          }
          const metadata = step && step.metadata && typeof step.metadata === "object" ? step.metadata : {};
          const filePaths = Array.isArray(metadata.filePaths) ? metadata.filePaths : [];
          const changeKinds = Array.isArray(metadata.changeKinds) ? metadata.changeKinds : [];
          const savedImagePath = typeof metadata.savedImagePath === "string" ? metadata.savedImagePath : "";
          const isImageGeneration = Boolean(metadata.isImageGeneration);
          const diffs = metadata.diffs && typeof metadata.diffs === "object" ? metadata.diffs : {};
          const entries = new Map();
  
          function upsertEntry(rawPath, extra) {
            const normalizedPath = normalizeHistoryPath(rawPath);
            if (!normalizedPath || shouldHideHistoryChangePath(normalizedPath)) return;
            const existing = entries.get(normalizedPath) || {
              path: normalizedPath,
              name: getHistoryPathName(normalizedPath),
              type: "file",
              size: null,
              changeKind: null,
              additions: null,
              deletions: null,
              diffText: "",
              isChanged: true,
            };
            const nextEntry = {
              ...existing,
              ...(extra || {}),
              path: normalizedPath,
              name: getHistoryPathName(normalizedPath),
              isChanged: true,
            };
            entries.set(normalizedPath, nextEntry);
          }
  
          for (let index = 0; index < filePaths.length; index += 1) {
            const filePath = filePaths[index];
            const diffEntry = resolveHistoryDiffEntry(diffs, filePath);
            const diffText =
              typeof diffEntry?.diff === "string" && diffEntry.diff.trim()
                ? diffEntry.diff
                : typeof diffEntry?.changes === "string" && diffEntry.changes.trim()
                  ? diffEntry.changes
                  : "";
            const derivedStats = diffText ? summarizeUnifiedDiffStats(diffText) : null;
            const rawChangeKind = typeof changeKinds[index] === "string" ? changeKinds[index].trim().toLowerCase() : "";
            const changeKind = rawChangeKind === "created" || rawChangeKind === "modified" || rawChangeKind === "deleted"
              ? rawChangeKind
              : null;
            upsertEntry(filePath, {
              changeKind,
              additions: Number.isFinite(diffEntry?.additions) && diffEntry.additions > 0 ? diffEntry.additions : derivedStats?.additions ?? null,
              deletions: Number.isFinite(diffEntry?.deletions) && diffEntry.deletions > 0 ? diffEntry.deletions : derivedStats?.deletions ?? null,
              diffText,
            });
          }
  
          for (const [diffPath, rawDiffEntry] of Object.entries(diffs)) {
            const diffEntry = rawDiffEntry && typeof rawDiffEntry === "object" ? rawDiffEntry : {};
            const diffText =
              typeof diffEntry?.diff === "string" && diffEntry.diff.trim()
                ? diffEntry.diff
                : typeof diffEntry?.changes === "string" && diffEntry.changes.trim()
                  ? diffEntry.changes
                  : "";
            const derivedStats = diffText ? summarizeUnifiedDiffStats(diffText) : null;
            upsertEntry(diffPath, {
              additions: Number.isFinite(diffEntry?.additions) && diffEntry.additions > 0 ? diffEntry.additions : derivedStats?.additions ?? null,
              deletions: Number.isFinite(diffEntry?.deletions) && diffEntry.deletions > 0 ? diffEntry.deletions : derivedStats?.deletions ?? null,
              diffText,
            });
          }
  
          for (const changedPath of extractStepChangedPaths(step, historyLogsById)) {
            upsertEntry(changedPath, {});
          }
  
          if (savedImagePath && step?.eventType === "file_change") {
            upsertEntry(savedImagePath, {
              changeKind: normalizeHistoryChangeKind(changeKinds[0]) || (isImageGeneration ? "created" : null),
            });
          }
  
          return Array.from(entries.values());
        }
  
        function buildStepFileEntries(step, diffSummary, snapshotFiles, supplementalEntries, historyLogsById) {
          const entries = new Map();
  
          function upsertEntry(rawPath, extra) {
            const normalizedPath = normalizeHistoryPath(rawPath);
            if (!normalizedPath || shouldHideHistoryChangePath(normalizedPath)) return;
            const existing = entries.get(normalizedPath) || {
              path: normalizedPath,
              name: getHistoryPathName(normalizedPath),
              type: "file",
              size: null,
              changeKind: null,
              additions: null,
              deletions: null,
              diffText: "",
              isChanged: false,
            };
            const nextName =
              extra && extra.type === "resource" && extra.name
                ? extra.name
                : existing.type === "resource" && existing.name
                  ? existing.name
                  : getHistoryPathName(normalizedPath);
            entries.set(normalizedPath, {
              ...existing,
              ...(extra || {}),
              path: normalizedPath,
              name: nextName,
            });
          }
  
          for (const entry of extractStepChangedFileEntries(step, historyLogsById)) {
            upsertEntry(entry.path, entry);
          }
  
          for (const entry of Array.isArray(supplementalEntries) ? supplementalEntries : []) {
            if (entry && typeof entry === "object") {
              upsertEntry(entry.path, entry);
            }
          }
  
          const diffChangedPaths = Array.isArray(diffSummary?.changedPaths) ? diffSummary.changedPaths : [];
          const singleChangedPath = diffChangedPaths.length === 1 ? diffChangedPaths[0] : null;
          for (const changedPath of diffChangedPaths) {
            upsertEntry(changedPath, {
              isChanged: true,
              additions:
                singleChangedPath && historyPathsMatch(singleChangedPath, changedPath) && Number.isFinite(diffSummary?.additions)
                  ? diffSummary.additions
                  : entries.get(normalizeHistoryPath(changedPath))?.additions ?? null,
              deletions:
                singleChangedPath && historyPathsMatch(singleChangedPath, changedPath) && Number.isFinite(diffSummary?.deletions)
                  ? diffSummary.deletions
                  : entries.get(normalizeHistoryPath(changedPath))?.deletions ?? null,
            });
          }
  
          const normalizedSnapshotFiles = Array.isArray(snapshotFiles)
            ? snapshotFiles.map((entry) => ({
                ...entry,
                path: normalizeHistoryPath(entry.path),
                name: getHistoryPathName(entry.path),
              })).filter((entry) => entry.path)
            : [];
  
          for (const snapshotEntry of normalizedSnapshotFiles) {
            const existing = entries.get(snapshotEntry.path);
            if (!existing) continue;
            upsertEntry(snapshotEntry.path, {
              type: snapshotEntry.type,
              size: snapshotEntry.size,
            });
          }
  
          if (entries.size > 0) {
            return Array.from(entries.values());
          }
  
          return normalizedSnapshotFiles
            .sort((left, right) => {
              if (left.type !== right.type) {
                return left.type === "directory" ? -1 : 1;
              }
              return left.path.localeCompare(right.path);
            })
            .map((entry) => ({
              ...entry,
              changeKind: null,
              additions: null,
              deletions: null,
              diffText: "",
              isChanged: false,
            }));
        }
  
        function getHistoryChangeKindLabel(changeKind) {
          if (changeKind === "created") return "Created";
          if (changeKind === "modified") return "Modified";
          if (changeKind === "deleted") return "Deleted";
          return "";
        }
  
        function renderHistoryResourceIcon(resourceType) {
          if (resourceType === "agent") {
            return React.createElement(Bot, { strokeWidth: 1.8 });
          }
          if (resourceType === "skill") {
            return React.createElement(Cpu, { strokeWidth: 1.8 });
          }
          if (resourceType === "task") {
            return React.createElement(ListTodo, { strokeWidth: 1.8 });
          }
          if (resourceType === "release") {
            return React.createElement(CalendarIcon, { strokeWidth: 1.8 });
          }
          if (resourceType === "environment") {
            return React.createElement(HardDrive, { strokeWidth: 1.8 });
          }
          return React.createElement(File, { strokeWidth: 1.8 });
        }
  
        function buildHistoryResourceMetaRows(entry) {
          const rows = [];
          if (!entry || entry.type !== "resource") {
            return rows;
          }
          if (entry.ticketNumber) {
            rows.push({ label: "Ticket", value: entry.ticketNumber });
          }
          if (entry.resourceId) {
            rows.push({ label: "ID", value: entry.resourceId });
          }
          if (entry.taskType) {
            rows.push({ label: "Type", value: formatHistoryTaskTypeLabel(entry.taskType) });
          }
          if (entry.status) {
            rows.push({ label: "Status", value: formatHistoryResourceStatusLabel(entry.status) });
          }
          if (entry.priority) {
            rows.push({ label: "Priority", value: formatHistoryTaskPriorityLabel(entry.priority) });
          }
          if (entry.assigneeName) {
            rows.push({ label: "Assignee", value: entry.assigneeName });
          }
          if (entry.model) {
            rows.push({ label: "Model", value: entry.model });
          }
          if (entry.category) {
            rows.push({ label: "Category", value: entry.category });
          }
          if (entry.environmentId) {
            rows.push({ label: "Environment", value: entry.environmentId });
          }
          if (entry.projectId) {
            rows.push({ label: "Project", value: entry.projectId });
          }
          if (entry.startAt || entry.endAt) {
            rows.push({
              label: "Window",
              value: [entry.startAt || "", entry.endAt || ""].filter(Boolean).join(" → "),
            });
          }
          if (typeof entry.taskCount === "number") {
            rows.push({ label: "Tasks", value: String(entry.taskCount) });
          }
          if (typeof entry.openTaskCount === "number") {
            rows.push({ label: "Open tasks", value: String(entry.openTaskCount) });
          }
          if (entry.isDefault) {
            rows.push({ label: "Default", value: "Yes" });
          }
          return rows;
        }
  
        function buildHistoryResourceSubtitle(entry) {
          if (!entry || entry.type !== "resource") {
            return "";
          }
          if (entry.resourceType === "task") {
            return [
              entry.ticketNumber || "",
              formatHistoryResourceStatusLabel(entry.status),
              entry.assigneeName || "",
            ].filter(Boolean).join(" · ");
          }
          if (entry.resourceType === "release") {
            const dateWindow = [entry.startAt || "", entry.endAt || ""].filter(Boolean).join(" → ");
            return [
              formatHistoryResourceStatusLabel(entry.status),
              dateWindow,
              typeof entry.openTaskCount === "number" && typeof entry.taskCount === "number"
                ? entry.openTaskCount + "/" + entry.taskCount + " open"
                : "",
            ].filter(Boolean).join(" · ");
          }
          if (entry.resourceType === "agent") {
            return [entry.model || "", entry.resourceId || ""].filter(Boolean).join(" · ");
          }
          if (entry.resourceType === "skill") {
            return [entry.category || "", entry.resourceId || ""].filter(Boolean).join(" · ");
          }
          if (entry.resourceType === "environment") {
            return [entry.projectId || "", entry.isDefault ? "Default" : "", entry.resourceId || ""].filter(Boolean).join(" · ");
          }
          return entry.resourceId || entry.description || "";
        }
  
        function readRevertedChangeStepId(step) {
          const metadata = step && typeof step === "object" && step.metadata && typeof step.metadata === "object"
            ? step.metadata
            : null;
          const revertedChangeStepId = metadata && typeof metadata.revertedChangeStepId === "string"
            ? metadata.revertedChangeStepId.trim()
            : "";
          return revertedChangeStepId || "";
        }
  
        function readHistoryOperation(step) {
          const metadata = step && typeof step === "object" && step.metadata && typeof step.metadata === "object"
            ? step.metadata
            : null;
          const historyOperation = metadata && typeof metadata.historyOperation === "string"
            ? metadata.historyOperation.trim().toLowerCase()
            : "";
          return historyOperation === "reapply" ? "reapply" : historyOperation === "revert" ? "revert" : "";
        }
  
        function shouldDisplayHistoryStep(step, historyLogsById) {
          if (!step || typeof step !== "object") return false;
          return extractStepChangedFileEntries(step, historyLogsById).length > 0;
        }
  
        function getPreferredHistoryStep(steps, historyLogsById) {
          if (!Array.isArray(steps) || steps.length === 0) return null;
          return (
            steps.find((step) => extractStepChangedFileEntries(step, historyLogsById).length > 0)
            || steps.find((step) => shouldDisplayHistoryStep(step, historyLogsById))
            || steps[0]
            || null
          );
        }
  
        function parseUnifiedDiffRange(value) {
          const parsed = Number(value || 0);
          return Number.isFinite(parsed) ? parsed : 0;
        }
  
        function summarizeUnifiedDiffStats(diffText) {
          const lines = String(diffText || "").replace(/\\r\\n/g, "\\n").split("\\n");
          let additions = 0;
          let deletions = 0;
          let insideHunk = false;
  
          for (const line of lines) {
            if (line.startsWith("@@")) {
              insideHunk = true;
              continue;
            }
            if (!insideHunk || line === "\\\\ No newline at end of file") {
              continue;
            }
            if (line.startsWith("+++")) continue;
            if (line.startsWith("---")) continue;
            if (line.startsWith("+")) {
              additions += 1;
              continue;
            }
            if (line.startsWith("-")) {
              deletions += 1;
            }
          }
  
          return { additions, deletions };
        }
  
        function parseUnifiedDiffRows(diffText) {
          const lines = String(diffText || "").replace(/\\r\\n/g, "\\n").split("\\n");
          const rows = [];
          let oldLineNumber = 0;
          let newLineNumber = 0;
          let insideHunk = false;
  
          for (const line of lines) {
            if (line.startsWith("@@")) {
              const match = line.match(/^@@\\s+-(\\d+)(?:,(\\d+))?\\s+\\+(\\d+)(?:,(\\d+))?\\s+@@/);
              oldLineNumber = match ? parseUnifiedDiffRange(match[1]) : 0;
              newLineNumber = match ? parseUnifiedDiffRange(match[3]) : 0;
              insideHunk = true;
              rows.push({ kind: "hunk", content: line, oldLineNumber: null, newLineNumber: null });
              continue;
            }
  
            if (line === "\\\\ No newline at end of file") {
              rows.push({ kind: "note", content: line, oldLineNumber: null, newLineNumber: null });
              continue;
            }
  
            if (!insideHunk || line.startsWith("diff --git ") || line.startsWith("index ") || line.startsWith("--- ") || line.startsWith("+++ ")) {
              rows.push({ kind: "meta", content: line, oldLineNumber: null, newLineNumber: null });
              continue;
            }
  
            if (line.startsWith("+")) {
              rows.push({
                kind: "added",
                content: line.slice(1),
                oldLineNumber: null,
                newLineNumber: newLineNumber || 0,
              });
              newLineNumber += 1;
              continue;
            }
  
            if (line.startsWith("-")) {
              rows.push({
                kind: "removed",
                content: line.slice(1),
                oldLineNumber: oldLineNumber || 0,
                newLineNumber: null,
              });
              oldLineNumber += 1;
              continue;
            }
  
            if (line.startsWith(" ")) {
              rows.push({
                kind: "context",
                content: line.slice(1),
                oldLineNumber: oldLineNumber || 0,
                newLineNumber: newLineNumber || 0,
              });
              oldLineNumber += 1;
              newLineNumber += 1;
              continue;
            }
  
            rows.push({ kind: "meta", content: line, oldLineNumber: null, newLineNumber: null });
          }
  
          return rows;
        }
  
        function buildDiffSnippetFromUnifiedDiff(diffText) {
          const lines = String(diffText || "").replace(/\\r\\n/g, "\\n").split("\\n");
          const original = [];
          const modified = [];
          let insideHunk = false;
  
          for (const line of lines) {
            if (line.startsWith("@@")) {
              insideHunk = true;
              continue;
            }
            if (!insideHunk || line === "\\\\ No newline at end of file") {
              continue;
            }
            if (line.startsWith("+")) {
              if (!line.startsWith("+++")) modified.push(line.slice(1));
              continue;
            }
            if (line.startsWith("-")) {
              if (!line.startsWith("---")) original.push(line.slice(1));
              continue;
            }
            if (line.startsWith(" ")) {
              const content = line.slice(1);
              original.push(content);
              modified.push(content);
            }
          }
  
          return {
            original: original.join("\\n"),
            modified: modified.join("\\n"),
          };
        }
  
        function buildDiffModels(diffText, fileContent) {
          const normalizedDiff = typeof diffText === "string" ? diffText.replace(/\\r\\n/g, "\\n") : "";
          const normalizedFileContent = typeof fileContent === "string" ? fileContent.replace(/\\r\\n/g, "\\n") : "";
          if (!normalizedDiff.trim()) {
            return {
              original: "",
              modified: normalizedFileContent,
            };
          }
  
          const lines = normalizedDiff.split("\\n");
          const hunkRegex = new RegExp("^@@\\\\s+-(\\\\d+)(?:,(\\\\d+))?\\\\s+\\\\+(\\\\d+)(?:,(\\\\d+))?\\\\s+@@");
          const hunks = [];
          let currentHunk = null;
  
          for (const line of lines) {
            const hunkMatch = line.match(hunkRegex);
            if (hunkMatch) {
              currentHunk = {
                oldStart: parseUnifiedDiffRange(hunkMatch[1]),
                oldCount: parseUnifiedDiffRange(hunkMatch[2] || "1"),
                newStart: parseUnifiedDiffRange(hunkMatch[3]),
                newCount: parseUnifiedDiffRange(hunkMatch[4] || "1"),
                lines: [],
              };
              hunks.push(currentHunk);
              continue;
            }
            if (!currentHunk) continue;
            if (line === "\\\\ No newline at end of file") continue;
            currentHunk.lines.push(line);
          }
  
          if (!normalizedFileContent.trim() || hunks.length === 0) {
            return buildDiffSnippetFromUnifiedDiff(normalizedDiff);
          }
  
          const modifiedLines = normalizedFileContent.split("\\n");
          const originalLines = [...modifiedLines];
  
          for (let index = hunks.length - 1; index >= 0; index -= 1) {
            const hunk = hunks[index];
            const oldSegment = [];
            for (const line of hunk.lines) {
              if (line.startsWith("+")) continue;
              if (line.startsWith("-") || line.startsWith(" ")) {
                oldSegment.push(line.slice(1));
              }
            }
            const spliceIndex = Math.max((hunk.newStart || 1) - 1, 0);
            originalLines.splice(spliceIndex, hunk.newCount, ...oldSegment);
          }
  
          return {
            original: originalLines.join("\\n"),
            modified: modifiedLines.join("\\n"),
          };
        }
  
        function inferHistoryLanguage(filePath) {
          const lower = String(filePath || "").toLowerCase();
          if (lower.endsWith(".tsx") || lower.endsWith(".ts")) return "typescript";
          if (lower.endsWith(".jsx") || lower.endsWith(".js") || lower.endsWith(".mjs") || lower.endsWith(".cjs")) return "javascript";
          if (lower.endsWith(".json")) return "json";
          if (lower.endsWith(".html")) return "html";
          if (lower.endsWith(".css")) return "css";
          if (lower.endsWith(".md")) return "markdown";
          if (lower.endsWith(".py")) return "python";
          if (lower.endsWith(".sh")) return "shell";
          if (lower.endsWith(".yml") || lower.endsWith(".yaml")) return "yaml";
          return "plaintext";
        }
  
        function HistoryDiffViewer({ diffText, fileContent, filePath, emptyMessage }) {
          if (!(diffText && diffText.trim())) {
            return React.createElement("div", { className: "changes-code-block is-empty" }, emptyMessage);
          }
  
          const rows = parseUnifiedDiffRows(diffText);
          return React.createElement("div", { className: "changes-diff-view", "aria-label": "Unified diff for " + (filePath || "selected file") },
            rows.map((row, index) =>
              React.createElement("div", {
                key: row.kind + ":" + index,
                className: "changes-diff-row is-" + row.kind,
              },
                React.createElement("span", { className: "changes-diff-line-number" }, Number.isFinite(row.oldLineNumber) ? String(row.oldLineNumber) : ""),
                React.createElement("span", { className: "changes-diff-line-number" }, Number.isFinite(row.newLineNumber) ? String(row.newLineNumber) : ""),
                React.createElement("span", { className: "changes-diff-line-prefix", "aria-hidden": "true" },
                  row.kind === "added"
                    ? "+"
                    : row.kind === "removed"
                      ? "-"
                      : row.kind === "context"
                        ? " "
                        : row.kind === "hunk"
                          ? "@"
                          : ""
                ),
                React.createElement("span", { className: "changes-diff-line-content" }, row.content || " ")
              )
            )
          );
        }
  
        function buildSyntheticHistoryDiff({ filePath, fileContent, changeKind }) {
          const normalizedPath = normalizeHistoryPath(filePath) || "unknown-file";
          const normalizedContent = typeof fileContent === "string" ? fileContent.replace(/\\r\\n/g, "\\n") : "";
          const lines = normalizedContent.length > 0 ? normalizedContent.split("\\n") : [];
  
          if (changeKind === "created") {
            const header = [
              "diff --git a/" + normalizedPath + " b/" + normalizedPath,
              "new file mode 100644",
              "--- /dev/null",
              "+++ b/" + normalizedPath,
            ];
            if (lines.length === 0) {
              return header.join("\\n");
            }
            return header.concat(
              "@@ -0,0 +" + 1 + "," + lines.length + " @@",
              lines.map((line) => "+" + line)
            ).join("\\n");
          }
  
          if (changeKind === "deleted" && lines.length > 0) {
            return [
              "diff --git a/" + normalizedPath + " b/" + normalizedPath,
              "deleted file mode 100644",
              "--- a/" + normalizedPath,
              "+++ /dev/null",
              "@@ -" + 1 + "," + lines.length + " +0,0 @@",
              ...lines.map((line) => "-" + line),
            ].join("\\n");
          }
  
          return "";
        }
  
        function buildHistorySplitDiffRows(diffText) {
          const lines = String(diffText || "").replace(/\\r\\n/g, "\\n").split("\\n");
          const rows = [];
          let oldLineNumber = 0;
          let newLineNumber = 0;
          let insideHunk = false;
          let pendingRemoved = [];
  
          function flushPendingRemoved() {
            if (pendingRemoved.length === 0) return;
            for (const removedCell of pendingRemoved) {
              rows.push({
                kind: "content",
                left: removedCell,
                right: null,
              });
            }
            pendingRemoved = [];
          }
  
          for (const line of lines) {
            if (line.startsWith("@@")) {
              flushPendingRemoved();
              const match = line.match(/^@@\\s+-(\\d+)(?:,(\\d+))?\\s+\\+(\\d+)(?:,(\\d+))?\\s+@@/);
              oldLineNumber = match ? parseUnifiedDiffRange(match[1]) : 0;
              newLineNumber = match ? parseUnifiedDiffRange(match[3]) : 0;
              insideHunk = true;
              rows.push({ kind: "hunk", content: line });
              continue;
            }
  
            if (line === "\\\\ No newline at end of file") {
              flushPendingRemoved();
              rows.push({ kind: "note", content: line });
              continue;
            }
  
            if (!insideHunk || line.startsWith("diff --git ") || line.startsWith("index ") || line.startsWith("--- ") || line.startsWith("+++ ")) {
              continue;
            }
  
            if (line.startsWith("-")) {
              pendingRemoved.push({
                kind: "removed",
                lineNumber: oldLineNumber || 0,
                content: line.slice(1),
              });
              oldLineNumber += 1;
              continue;
            }
  
            if (line.startsWith("+")) {
              const addedCell = {
                kind: "added",
                lineNumber: newLineNumber || 0,
                content: line.slice(1),
              };
              const pairedRemoved = pendingRemoved.shift() || null;
              rows.push({
                kind: "content",
                left: pairedRemoved,
                right: addedCell,
              });
              newLineNumber += 1;
              continue;
            }
  
            if (line.startsWith(" ")) {
              flushPendingRemoved();
              const content = line.slice(1);
              rows.push({
                kind: "content",
                left: {
                  kind: "context",
                  lineNumber: oldLineNumber || 0,
                  content,
                },
                right: {
                  kind: "context",
                  lineNumber: newLineNumber || 0,
                  content,
                },
              });
              oldLineNumber += 1;
              newLineNumber += 1;
            }
          }
  
          flushPendingRemoved();
          return rows;
        }
  
        function HistorySplitDiffCell({ side, cell }) {
          const tone = cell?.kind || "empty";
          return React.createElement("div", {
            className: "changes-split-diff-cell is-" + side + " is-" + tone,
          },
            React.createElement("span", {
              className: "changes-split-diff-line-number",
            }, Number.isFinite(cell?.lineNumber) ? String(cell.lineNumber) : ""),
            React.createElement("span", {
              className: "changes-split-diff-line-content",
            }, cell?.content || " ")
          );
        }
  
        function HistorySplitDiffFallbackViewer({ diffText, filePath, emptyMessage }) {
          if (!(diffText && diffText.trim())) {
            return React.createElement("div", { className: "changes-code-block is-empty" }, emptyMessage);
          }
  
          const rows = buildHistorySplitDiffRows(diffText);
          return React.createElement("div", {
            className: "changes-split-diff-view",
            "aria-label": "Split diff for " + (filePath || "selected file"),
          },
            rows.length === 0
              ? React.createElement("div", { className: "changes-code-block is-empty" }, emptyMessage)
              : rows.map((row, index) => {
                  if (row.kind === "hunk") {
                    return React.createElement("div", {
                      key: "hunk:" + index,
                      className: "changes-split-diff-hunk",
                    }, row.content);
                  }
                  if (row.kind === "note") {
                    return React.createElement("div", {
                      key: "note:" + index,
                      className: "changes-split-diff-note",
                    }, row.content);
                  }
                  return React.createElement("div", {
                    key: "row:" + index,
                    className: "changes-split-diff-row",
                  },
                    React.createElement(HistorySplitDiffCell, {
                      side: "left",
                      cell: row.left,
                    }),
                    React.createElement(HistorySplitDiffCell, {
                      side: "right",
                      cell: row.right,
                    })
                  );
                })
          );
        }
  
        function HistoryDiffToggleButton({ isActive, onClick, children }) {
          return React.createElement("button", {
            type: "button",
            className: "changes-diff-toggle-button" + (isActive ? " is-active" : ""),
            onClick,
          }, children);
        }
  
        function HistorySplitDiffViewer({ diffText, fileContent, filePath, emptyMessage, additions, deletions }) {
          const [DiffEditorComponent, setDiffEditorComponent] = useState(null);
          const [viewMode, setViewMode] = useState("split");
          const [viewMenuOpen, setViewMenuOpen] = useState(false);
          const [hideUnchanged, setHideUnchanged] = useState(true);
          const [editorHeight, setEditorHeight] = useState(180);
          const [diffEditorInstance, setDiffEditorInstance] = useState(null);
          const viewMenuRef = useRef(null);
  
          useEffect(() => {
            let cancelled = false;
            void loadHistoryMonacoModule().then((module) => {
              const nextComponent = module?.DiffEditor || null;
              if (!cancelled && nextComponent) {
                setDiffEditorComponent(() => nextComponent);
              }
            });
            return () => {
              cancelled = true;
            };
          }, []);
  
          useEffect(() => {
            if (!viewMenuOpen) {
              return undefined;
            }
            const handlePointerDown = (event) => {
              if (viewMenuRef.current && !viewMenuRef.current.contains(event.target)) {
                setViewMenuOpen(false);
              }
            };
            const handleKeyDown = (event) => {
              if (event.key === "Escape") {
                setViewMenuOpen(false);
              }
            };
            document.addEventListener("pointerdown", handlePointerDown);
            document.addEventListener("keydown", handleKeyDown);
            return () => {
              document.removeEventListener("pointerdown", handlePointerDown);
              document.removeEventListener("keydown", handleKeyDown);
            };
          }, [viewMenuOpen]);
  
          if (!(diffText && diffText.trim())) {
            return React.createElement("div", { className: "changes-code-block is-empty" }, emptyMessage);
          }
  
          const models = buildDiffModels(diffText, fileContent);
  
          useEffect(() => {
            if (!diffEditorInstance) {
              return;
            }
  
            const updateHeight = () => {
              const contentHeight = typeof diffEditorInstance.getContentHeight === "function"
                ? diffEditorInstance.getContentHeight()
                : 0;
              const minHeight = 72;
              const maxHeight = hideUnchanged ? 680 : 760;
              setEditorHeight(Math.min(Math.max(contentHeight, minHeight), maxHeight));
            };
  
            updateHeight();
            const disposable = typeof diffEditorInstance.onDidContentSizeChange === "function"
              ? diffEditorInstance.onDidContentSizeChange(() => {
                  updateHeight();
                })
              : null;
  
            return () => {
              if (disposable && typeof disposable.dispose === "function") {
                disposable.dispose();
              }
            };
          }, [diffEditorInstance, hideUnchanged, viewMode, diffText]);
  
          return React.createElement("section", { className: "changes-diff-box" },
            React.createElement("div", { className: "changes-diff-box-header" },
              React.createElement("div", { className: "changes-diff-box-path" }, filePath || "Selected file"),
              React.createElement("div", { className: "changes-diff-box-header-right" },
                React.createElement("div", { className: "changes-diff-stats is-box-header" },
                  React.createElement("span", { className: "changes-diff-stat additions" }, "+" + additions),
                  React.createElement("span", { className: "changes-diff-stat deletions" }, "-" + deletions)
                ),
                React.createElement(HistoryDiffToggleButton, {
                  isActive: hideUnchanged,
                  onClick: () => setHideUnchanged((current) => !current),
                }, hideUnchanged ? "Collapsed" : "Expanded"),
                React.createElement("div", { className: "changes-diff-view-menu-shell", ref: viewMenuRef },
                  React.createElement("button", {
                      type: "button",
                      className: "changes-diff-menu-trigger" + (viewMenuOpen ? " is-active" : ""),
                      onClick: () => setViewMenuOpen((current) => !current),
                      "aria-haspopup": "menu",
                      "aria-expanded": viewMenuOpen ? "true" : "false",
                      "aria-label": "Diff view options",
                    },
                    React.createElement(EllipsisVertical, { width: 16, height: 16, strokeWidth: 1.8 })
                  ),
                  viewMenuOpen
                    ? React.createElement(PlatformPopupSurface, { className: "changes-diff-view-menu", role: "menu" },
                        [
                          { id: "split", label: "Split view" },
                          { id: "unified", label: "Unified view" },
                        ].map((item) =>
                          React.createElement("button", {
                              key: item.id,
                              type: "button",
                              role: "menuitemradio",
                              "aria-checked": viewMode === item.id ? "true" : "false",
                              className: "changes-diff-view-menu-item" + (viewMode === item.id ? " is-active" : ""),
                              onClick: () => {
                                setViewMode(item.id);
                                setViewMenuOpen(false);
                              },
                            },
                            React.createElement("span", { className: "changes-diff-view-menu-check" },
                              viewMode === item.id
                                ? React.createElement(Check, { width: 13, height: 13, strokeWidth: 2 })
                                : null
                            ),
                            React.createElement("span", null, item.label)
                          )
                        )
                      )
                    : null
                )
              )
            ),
            React.createElement("div", { className: "changes-diff-box-body" },
              DiffEditorComponent
                ? React.createElement(DiffEditorComponent, {
                    key: [
                      "monaco-diff",
                      filePath || "selected-file",
                      viewMode,
                      hideUnchanged ? "collapsed" : "expanded",
                      diffText.length,
                      models.original.length,
                      models.modified.length,
                    ].join(":"),
                    beforeMount: ensureHistoryMonacoTheme,
                    onMount: (editor) => {
                      setDiffEditorInstance(editor);
                    },
                    theme: HISTORY_MONACO_THEME_NAME,
                    original: models.original,
                    modified: models.modified,
                    language: inferHistoryLanguage(filePath),
                    height: editorHeight,
                    options: {
                      readOnly: true,
                      originalEditable: false,
                      renderSideBySide: viewMode === "split",
                      useInlineViewWhenSpaceIsLimited: false,
                      enableSplitViewResizing: true,
                      renderIndicators: true,
                      hideUnchangedRegions: {
                        enabled: hideUnchanged,
                        contextLineCount: 4,
                        minimumLineCount: 3,
                        revealLineCount: 3,
                      },
                      diffAlgorithm: "advanced",
                      ignoreTrimWhitespace: false,
                      renderOverviewRuler: false,
                      overviewRulerBorder: false,
                      minimap: { enabled: false },
                      scrollBeyondLastLine: false,
                      automaticLayout: true,
                      fontSize: 12,
                      lineHeight: 20,
                      lineNumbers: "on",
                      lineNumbersMinChars: 4,
                      glyphMargin: false,
                      folding: false,
                      wordWrap: "off",
                      diffWordWrap: "off",
                      renderValidationDecorations: "off",
                      codeLens: false,
                      contextmenu: false,
                      selectionHighlight: false,
                      occurrencesHighlight: "off",
                      renderLineHighlight: "none",
                      stickyScroll: { enabled: false },
                      scrollbar: {
                        vertical: "auto",
                        horizontal: "auto",
                        verticalScrollbarSize: 8,
                        horizontalScrollbarSize: 8,
                        alwaysConsumeMouseWheel: false,
                      },
                    },
                  })
                : React.createElement(HistorySplitDiffFallbackViewer, {
                    diffText,
                    filePath,
                    emptyMessage,
                  })
            )
          );
        }
  
        function getPreferredHistoryEntryPath(entries, changedPaths) {
          const changedSet = new Set(uniqueHistoryPaths(changedPaths));
          const files = entries.filter((entry) => entry.type === "file");
          const changedMatch = files.find((entry) => changedSet.has(normalizeHistoryPath(entry.path)));
          return changedMatch?.path || files[0]?.path || entries[0]?.path || "";
        }
  
        async function fetchHistoryThreadMessages({ backendUrl, threadId, headers }) {
          const pageSize = 200;
          const messages = [];
          let offset = 0;
  
          while (true) {
            const response = await fetch(
              backendUrl + "/threads/" + encodeURIComponent(threadId) + "/messages?limit=" + pageSize + "&offset=" + offset + "&compact=1",
              {
                method: "GET",
                headers,
              }
            );
  
            const bodyText = await response.text();
            let parsed = {};
            try {
              parsed = bodyText ? JSON.parse(bodyText) : {};
            } catch {
              parsed = {};
            }
  
            if (!response.ok) {
              throw new Error(parsed.message || parsed.error || ("Failed to load thread messages (" + response.status + ")"));
            }
  
            const pageItems = Array.isArray(parsed.data) ? parsed.data : [];
            messages.push(...pageItems);
  
            if (!parsed.has_more || pageItems.length === 0) {
              break;
            }
  
            offset += pageItems.length;
          }
  
          return messages;
        }
  
        async function fetchHistoryThreadLogs({ client, backendUrl, threadId, headers }) {
          return client.getThreadLogs({
            backendUrl,
            threadId,
            headers,
            compact: true,
            includeConversation: false,
          });
        }
  
        function normalizeTraceItems(data, primaryKey, fallbackKey) {
          if (Array.isArray(data)) {
            return data;
          }
          if (Array.isArray(data?.[primaryKey])) {
            return data[primaryKey];
          }
          if (fallbackKey && Array.isArray(data?.[fallbackKey])) {
            return data[fallbackKey];
          }
          if (Array.isArray(data?.data)) {
            return data.data;
          }
          return [];
        }
  
        async function fetchThreadTraceDetails({ backendUrl, threadId, headers, messageLimit = 160, stepLimit = 160 }) {
          const normalizedThreadId = String(threadId || "").trim();
          if (!normalizedThreadId) {
            throw new Error("Thread id is required.");
          }
  
          const fetchJson = async (path) => {
            const response = await fetch(backendUrl + path, {
              method: "GET",
              headers,
            });
            const data = await response.json().catch(() => ({}));
            if (!response.ok) {
              throw new Error(data?.message || data?.error || "Failed to load trace data.");
            }
            return data;
          };
  
          const encodedThreadId = encodeURIComponent(normalizedThreadId);
          const [threadResult, logsResult, messagesResult, stepsResult, traceClustersResult] = await Promise.allSettled([
            fetchJson("/threads/" + encodedThreadId),
            fetchJson("/threads/" + encodedThreadId + "/logs?compact=1&includeConversation=0"),
            fetchJson("/threads/" + encodedThreadId + "/messages?limit=" + encodeURIComponent(String(messageLimit)) + "&compact=1"),
            fetchJson("/threads/" + encodedThreadId + "/steps?limit=" + encodeURIComponent(String(stepLimit)) + "&compact=1"),
            fetchJson("/threads/" + encodedThreadId + "/trace-clusters?limit=" + encodeURIComponent(String(stepLimit))),
          ]);
  
          const firstRejected = [threadResult, logsResult, messagesResult, stepsResult].find((result) => result.status === "rejected");
          const threadData = threadResult.status === "fulfilled" ? threadResult.value : {};
          const logsData = logsResult.status === "fulfilled" ? logsResult.value : {};
          const messagesData = messagesResult.status === "fulfilled" ? messagesResult.value : {};
          const stepsData = stepsResult.status === "fulfilled" ? stepsResult.value : {};
          const traceClustersData = traceClustersResult.status === "fulfilled" ? traceClustersResult.value : {};
          const traceClusters = traceClustersData?.traceClusters || traceClustersData?.data?.traceClusters || null;
          const traceClusterSequences = Array.isArray(traceClusters?.sequences)
            ? traceClusters.sequences
            : normalizeTraceItems(traceClustersData, "sequences");
          const traceClusterActions = Array.isArray(traceClusters?.actions)
            ? traceClusters.actions
            : normalizeTraceItems(traceClustersData, "actions");
          const nextDetails = {
            status: "loaded",
            error: firstRejected?.reason instanceof Error ? firstRejected.reason.message : "",
            thread: threadData?.thread || threadData?.data || null,
            logs: normalizeTraceItems(logsData, "logs"),
            messages: normalizeTraceItems(messagesData, "messages"),
            steps: normalizeTraceItems(stepsData, "steps"),
            traceClusters: traceClusters
              ? {
                  ...traceClusters,
                  sequences: traceClusterSequences,
                  actions: traceClusterActions,
                }
              : null,
            traceClusterError: traceClustersResult.status === "rejected"
              ? traceClustersResult.reason instanceof Error ? traceClustersResult.reason.message : "Failed to load trace clusters."
              : "",
            decisions: [
              ...traceClusterSequences,
              ...normalizeTraceItems(threadData, "decisions"),
              ...normalizeTraceItems(stepsData, "decisions"),
            ].filter((decision, index, decisions) => {
              const decisionId = String(decision?.id || decision?.decisionId || decision?.decision_id || "").trim();
              if (!decisionId) return true;
              return decisions.findIndex((candidate) => (
                String(candidate?.id || candidate?.decisionId || candidate?.decision_id || "").trim() === decisionId
              )) === index;
            }),
            loadedAt: new Date().toISOString(),
          };
  
          if (firstRejected && !nextDetails.thread && nextDetails.logs.length === 0 && nextDetails.messages.length === 0 && nextDetails.steps.length === 0 && nextDetails.decisions.length === 0) {
            throw firstRejected.reason instanceof Error ? firstRejected.reason : new Error("Failed to load trace data.");
          }
  
          return nextDetails;
        }
  
        function parseTraceTimestampValue(value) {
          if (value == null || value === "") {
            return 0;
          }
          if (typeof value === "number") {
            if (!Number.isFinite(value) || value <= 0) {
              return 0;
            }
            return value > 1000000000000 ? value : value > 1000000000 ? value * 1000 : 0;
          }
          if (value instanceof Date) {
            const timestamp = value.getTime();
            return Number.isFinite(timestamp) ? timestamp : 0;
          }
          if (value && typeof value === "object") {
            const seconds = Number(value.seconds ?? value._seconds ?? value.secs ?? value._secs);
            if (Number.isFinite(seconds) && seconds > 0) {
              const nanos = Number(value.nanoseconds ?? value._nanoseconds ?? value.nanos ?? value._nanos ?? 0);
              return Math.round((seconds * 1000) + (Number.isFinite(nanos) ? nanos / 1000000 : 0));
            }
            if (typeof value.toDate === "function") {
              try {
                const timestamp = value.toDate().getTime();
                return Number.isFinite(timestamp) ? timestamp : 0;
              } catch {}
            }
          }
          const timestamp = Date.parse(String(value || ""));
          return Number.isFinite(timestamp) ? timestamp : 0;
        }
  
        function readTraceTimestamp(record) {
          const metadata = record?.metadata && typeof record.metadata === "object" && !Array.isArray(record.metadata) ? record.metadata : {};
          const candidates = [
            record?.timestamp,
            record?.created_at,
            record?.createdAt,
            record?.updated_at,
            record?.updatedAt,
            record?.started_at,
            record?.startedAt,
            record?.completed_at,
            record?.completedAt,
            record?.finished_at,
            record?.finishedAt,
            record?.ended_at,
            record?.endedAt,
            metadata?.timestamp,
            metadata?.created_at,
            metadata?.createdAt,
            metadata?.updated_at,
            metadata?.updatedAt,
            metadata?.started_at,
            metadata?.startedAt,
            metadata?.completed_at,
            metadata?.completedAt,
            metadata?.finished_at,
            metadata?.finishedAt,
            metadata?.ended_at,
            metadata?.endedAt,
          ];
          for (const value of candidates) {
            const timestamp = parseTraceTimestampValue(value);
            if (timestamp > 0) {
              return timestamp;
            }
          }
          return 0;
        }
  
        function getTraceEventCreatedAt(record) {
          const metadata = record?.metadata && typeof record.metadata === "object" && !Array.isArray(record.metadata) ? record.metadata : {};
          const candidates = [
            record?.createdAt,
            record?.created_at,
            record?.timestamp,
            record?.startedAt,
            record?.started_at,
            metadata?.createdAt,
            metadata?.created_at,
            metadata?.timestamp,
            metadata?.startedAt,
            metadata?.started_at,
          ];
          for (const value of candidates) {
            if (value == null || value === "") {
              continue;
            }
            const timestamp = parseTraceTimestampValue(value);
            if (timestamp > 0) {
              return new Date(timestamp).toISOString();
            }
          }
          return "";
        }
  
        function formatTraceTimestampLabel(value) {
          const timestamp = Number(value);
          if (!Number.isFinite(timestamp) || timestamp <= 0) {
            return "—";
          }
          return new Intl.DateTimeFormat("en-GB", {
            hour: "2-digit",
            minute: "2-digit",
            second: "2-digit",
          }).format(new Date(timestamp));
        }
  
        function formatTraceRowTimeLabel(entry) {
          if (entry?.durationMs != null) {
            return formatTraceDurationMs(entry.durationMs);
          }
          return formatTraceTimestampLabel(entry?.timestamp);
        }
  
        function readTraceText(value, maxLength = 260) {
          const normalize = (text) => String(text || "").replace(/\\s+/g, " ").trim();
          let text = "";
  
          if (typeof value === "string" || typeof value === "number" || typeof value === "boolean") {
            text = String(value);
          } else if (Array.isArray(value)) {
            text = value
              .map((entry) => readTraceText(entry, maxLength))
              .filter(Boolean)
              .join(" ");
          } else if (value && typeof value === "object") {
            if (typeof value.content === "string") {
              text = value.content;
            } else if (typeof value.text === "string") {
              text = value.text;
            } else if (value.text && typeof value.text === "object" && typeof value.text.value === "string") {
              text = value.text.value;
            } else if (typeof value.message === "string") {
              text = value.message;
            } else if (typeof value.summary === "string") {
              text = value.summary;
            } else if (typeof value.body === "string") {
              text = value.body;
            } else if (typeof value.name === "string") {
              text = value.name;
            } else if (Array.isArray(value.content)) {
              text = value.content.map((entry) => readTraceText(entry, maxLength)).filter(Boolean).join(" ");
            } else {
              try {
                text = JSON.stringify(value);
              } catch {
                text = "";
              }
            }
          }
  
          const normalized = normalize(text);
          if (!maxLength || normalized.length <= maxLength) {
            return normalized;
          }
          return normalized.slice(0, Math.max(0, maxLength - 1)).trimEnd() + "...";
        }
  
        function getTraceThreadTitle(thread, fallbackTitle = "") {
          const normalizedThread = thread ? normalizeThreadItem(thread) : null;
          const titleParts = normalizedThread ? getSidebarThreadTitleParts(normalizedThread) : null;
          return titleParts?.displayThreadTitle || String(fallbackTitle || "").trim() || normalizedThread?.title || "Untitled thread";
        }
  
        function getTraceStatusLabel(value) {
          const normalized = String(value || "").trim().replace(/_/g, " ");
          return normalized ? normalized.charAt(0).toUpperCase() + normalized.slice(1) : "Unknown";
        }
  
        function getTraceEventType(log) {
          return String(log?.eventType || log?.type || log?.logType || log?.kind || "").trim().toLowerCase();
        }
  
        function getTraceEventTitle(log) {
          const eventType = getTraceEventType(log);
          const metadata = log?.metadata && typeof log.metadata === "object" && !Array.isArray(log.metadata) ? log.metadata : {};
          const action = String(log?.action || metadata?.action || metadata?.toolName || metadata?.command || "").trim();
          if (action) {
            return action.length > 80 ? action.slice(0, 79).trimEnd() + "..." : action;
          }
          if (eventType === "agent_message" || eventType === "assistant_message") return "Assistant message";
          if (eventType === "user_message") return "User message";
          if (eventType === "reasoning") return "Reasoning";
          if (eventType === "command_execution") return "Command execution";
          if (eventType === "mcp_tool_call") return "Tool call";
          if (eventType === "subagent_invocation") return "Subagent";
          if (eventType === "permission_request") return "Permission request";
          if (eventType === "turn_completed") return "Run summary";
          if (eventType === "file_change") return "File change";
          return eventType ? getTraceStatusLabel(eventType) : "Log event";
        }
  
        function getTraceEventCopy(log) {
          const metadata = log?.metadata && typeof log.metadata === "object" && !Array.isArray(log.metadata) ? log.metadata : {};
          return readTraceText(
            log?.message
            || log?.content
            || log?.summary
            || metadata?.summary
            || metadata?.message
            || metadata?.stdout
            || metadata?.stderr
            || metadata?.result
            || "",
            180
          );
        }
  
        function getTraceEventIcon(eventType) {
          const type = String(eventType || "").toLowerCase();
          if (type.includes("message")) return MessageSquare;
          if (type.includes("command") || type.includes("tool")) return Terminal;
          if (type.includes("subagent")) return Bot;
          if (type.includes("permission")) return Shield;
          if (type.includes("file")) return FileText;
          if (type.includes("reasoning") || type.includes("summary")) return Lightbulb;
          return Clock;
        }
  
        function getTraceEventKindClass(eventType) {
          const type = String(eventType || "").toLowerCase();
          if (type.includes("command") || type.includes("tool")) return "tool";
          if (type.includes("permission")) return "permission";
          if (type.includes("subagent")) return "subagent";
          if (type.includes("message")) return "message";
          return "log";
        }
  
        function getTraceRecordMetadata(record) {
          return record?.metadata && typeof record.metadata === "object" && !Array.isArray(record.metadata)
            ? record.metadata
            : {};
        }
  
        function getTraceStepType(step) {
          return String(step?.stepKind || step?.step_kind || step?.eventType || step?.event_type || step?.type || "step")
            .trim()
            .toLowerCase();
        }
  
        function getTraceStepTitle(step) {
          const metadata = getTraceRecordMetadata(step);
          const explicitTitle = String(step?.title || metadata?.title || metadata?.decision || "").trim();
          if (explicitTitle) {
            return explicitTitle.length > 96 ? explicitTitle.slice(0, 95).trimEnd() + "..." : explicitTitle;
          }
          const stepType = getTraceStepType(step);
          if (stepType === "user_message") return "User intent received";
          if (stepType === "assistant_message") return "Assistant response";
          if (stepType === "reasoning") return "Reasoning checkpoint";
          if (stepType === "command_execution") return "Ran command";
          if (stepType === "mcp_tool_call") return "Called tool";
          if (stepType === "file_change") return "Changed files";
          if (stepType === "turn_completed") return "Turn completed";
          if (stepType === "system") return "System event";
          return stepType ? getTraceStatusLabel(stepType) : "Trace step";
        }
  
        function getTraceStepCopy(step) {
          const metadata = getTraceRecordMetadata(step);
          return readTraceText(
            metadata?.decision
            || metadata?.summary
            || metadata?.message
            || metadata?.reasoning
            || metadata?.command
            || metadata?.toolName
            || metadata?.tool_name
            || metadata?.filePath
            || metadata?.file_path
            || step?.summary
            || step?.message
            || step?.content
            || "",
            220
          );
        }
  
        function getTraceDecisionTitle(record) {
          const metadata = getTraceRecordMetadata(record);
          const explicitTitle = readTraceText(
            record?.title
            || record?.decision
            || record?.summary
            || metadata?.title
            || metadata?.decision
            || metadata?.summary
            || "",
            96
          );
          return explicitTitle || getTraceStepTitle(record);
        }
  
        function getTraceDecisionCopy(record) {
          const metadata = getTraceRecordMetadata(record);
          return readTraceText(
            record?.rationale
            || record?.reason
            || record?.summary
            || record?.description
            || record?.decision
            || record?.content
            || metadata?.rationale
            || metadata?.reason
            || metadata?.summary
            || metadata?.description
            || metadata?.decision
            || "",
            240
          ) || getTraceStepCopy(record);
        }
  
        function getTraceDecisionStepId(record) {
          const metadata = getTraceRecordMetadata(record);
          const stepIds = Array.isArray(record?.stepIds)
            ? record.stepIds
            : Array.isArray(record?.step_ids)
              ? record.step_ids
              : Array.isArray(metadata?.stepIds)
                ? metadata.stepIds
                : Array.isArray(metadata?.step_ids)
                  ? metadata.step_ids
                  : [];
          return String(
            record?.stepId
            || record?.step_id
            || record?.startStepId
            || record?.start_step_id
            || record?.firstStepId
            || record?.first_step_id
            || metadata?.stepId
            || metadata?.step_id
            || metadata?.startStepId
            || metadata?.start_step_id
            || metadata?.firstStepId
            || metadata?.first_step_id
            || stepIds[0]
            || ""
          ).trim();
        }
  
        function getTraceDecisionStepSequence(record) {
          const metadata = getTraceRecordMetadata(record);
          const value = Number(
            record?.stepSequence
            ?? record?.step_sequence
            ?? record?.startStepSequence
            ?? record?.start_step_sequence
            ?? metadata?.stepSequence
            ?? metadata?.step_sequence
            ?? metadata?.startStepSequence
            ?? metadata?.start_step_sequence
          );
          return Number.isFinite(value) ? value : null;
        }
  
        function getTraceDecisionSnapshotId(record, side) {
          const metadata = getTraceRecordMetadata(record);
          const isBefore = side === "before";
          return String(
            isBefore
              ? record?.snapshotBeforeId
                || record?.snapshot_before_id
                || record?.startSnapshotId
                || record?.start_snapshot_id
                || metadata?.snapshotBeforeId
                || metadata?.snapshot_before_id
                || metadata?.startSnapshotId
                || metadata?.start_snapshot_id
                || ""
              : record?.snapshotAfterId
                || record?.snapshot_after_id
                || record?.endSnapshotId
                || record?.end_snapshot_id
                || metadata?.snapshotAfterId
                || metadata?.snapshot_after_id
                || metadata?.endSnapshotId
                || metadata?.end_snapshot_id
                || ""
          ).trim();
        }
  
        function isTraceDecisionEntry(entry) {
          return entry?.source === "step" || entry?.source === "decision" || entry?.source === "trace_sequence";
        }
  
        function collectTraceTouchedResources(record) {
          const metadata = getTraceRecordMetadata(record);
          const resources = [];
          const addResource = (value) => {
            if (value == null || value === "") return;
            if (Array.isArray(value)) {
              value.forEach(addResource);
              return;
            }
            if (value && typeof value === "object") {
              addResource(value.path || value.filePath || value.file_path || value.name || value.id || value.resourceId || value.resource_id);
              return;
            }
            const text = String(value || "").trim();
            if (!text) return;
            if (!resources.includes(text)) {
              resources.push(text);
            }
          };
  
          [
            record?.touchedResources,
            record?.touched_resources,
            record?.environmentId,
            record?.environment_id,
            record?.sourceMessageId,
            record?.source_message_id,
            metadata?.filePath,
            metadata?.file_path,
            metadata?.path,
            metadata?.relativePath,
            metadata?.relative_path,
            metadata?.changedFile,
            metadata?.changed_file,
            metadata?.changedFiles,
            metadata?.changed_files,
            metadata?.filenames,
            metadata?.fileNames,
            metadata?.files,
            metadata?.resourceId,
            metadata?.resource_id,
            metadata?.toolName,
            metadata?.tool_name,
            metadata?.command,
          ].forEach(addResource);
  
          return resources.slice(0, 8);
        }
  
        function getTraceRingNumber(record) {
          const stepType = getTraceStepType(record);
          const eventType = getTraceEventType(record);
          const type = String(stepType && stepType !== "step" ? stepType : (eventType || stepType)).toLowerCase();
          const metadata = getTraceRecordMetadata(record);
          const directRing = Number(record?.ring ?? record?.permissionRing ?? record?.permission_ring ?? metadata?.ring ?? metadata?.permissionRing ?? metadata?.permission_ring);
          if (directRing === 1 || directRing === 2 || directRing === 3) {
            return directRing;
          }
          const directRingId = String(record?.ringId || record?.ring_id || record?.permissionRingId || record?.permission_ring_id || metadata?.ringId || metadata?.ring_id || metadata?.permissionRingId || metadata?.permission_ring_id || "").trim().toLowerCase();
          if (directRingId === "ring_3" || directRingId === "ring-3" || directRingId === "ring3") return 3;
          if (directRingId === "ring_2" || directRingId === "ring-2" || directRingId === "ring2") return 2;
          if (directRingId === "ring_1" || directRingId === "ring-1" || directRingId === "ring1") return 1;
          const searchableText = [
            type,
            record?.title,
            record?.message,
            record?.summary,
            metadata?.action,
            metadata?.toolName,
            metadata?.tool_name,
            metadata?.command,
            metadata?.summary,
            metadata?.message,
            metadata?.resourceType,
            metadata?.resource_type,
          ].map((value) => readTraceText(value, 240)).join(" ").toLowerCase();
  
          if (
            type.includes("permission")
            || searchableText.includes("payment")
            || searchableText.includes("stripe")
            || searchableText.includes("send email")
            || searchableText.includes("email send")
            || searchableText.includes("delete")
            || searchableText.includes("secret")
            || searchableText.includes("external api")
          ) {
            return 3;
          }
          if (
            searchableText.includes("deploy")
            || searchableText.includes("publish")
            || searchableText.includes("webhook")
            || searchableText.includes("github")
            || searchableText.includes("google drive")
            || searchableText.includes("onedrive")
            || searchableText.includes("notion")
          ) {
            return 2;
          }
          return 1;
        }
  
        function getTraceRingLabel(ring) {
          const numericRing = Number(ring);
          if (numericRing === 3) return "Ring 3";
          if (numericRing === 2) return "Ring 2";
          return "Ring 1";
        }
  
        function getTracePermissionRingId(ring) {
          const numericRing = Number(ring);
          if (numericRing === 3) return "ring_3";
          if (numericRing === 2) return "ring_2";
          return "ring_1";
        }
  
        function renderTracePermissionRingIcon(ring) {
          return React.createElement(PlatformPermissionMiniRingIcon, {
            ringId: getTracePermissionRingId(ring),
          });
        }
  
        function getTraceRingDescription(ring) {
          const numericRing = Number(ring);
          if (numericRing === 3) return "High-blast-radius action";
          if (numericRing === 2) return "Externally visible action";
          return "Sandbox-local action";
        }
  
        function getTraceDecisionStatus(record) {
          const metadata = getTraceRecordMetadata(record);
          const status = String(
            record?.decisionStatus
            || record?.decision_status
            || metadata?.decisionStatus
            || metadata?.decision_status
            || metadata?.status
            || ""
          ).trim().toLowerCase();
          const supersededBy = String(record?.supersededBy || record?.superseded_by || metadata?.supersededBy || metadata?.superseded_by || "").trim();
          const needsReview = Boolean(
            record?.needsReview
            || record?.needs_review
            || metadata?.needsReview
            || metadata?.needs_review
            || metadata?.requiresReview
            || metadata?.requires_review
          );
          if (supersededBy || status === "superseded") return "superseded";
          if (needsReview || status === "needs_review" || status === "review" || status === "blocked") return "needs-review";
          if (status === "verified" || status === "accepted" || status === "complete") return "verified";
          return "grounded";
        }
  
        function getTraceDecisionStatusLabel(status) {
          const normalized = String(status || "").trim().toLowerCase();
          if (normalized === "superseded") return "Superseded";
          if (normalized === "needs-review") return "Needs review";
          if (normalized === "verified") return "Verified";
          return "Grounded";
        }
  
        function getTraceEvidenceLabel(entry) {
          if (!entry || typeof entry !== "object") return "No evidence";
          const parts = [];
          if (entry.stepSequence != null) {
            parts.push("step #" + entry.stepSequence);
          } else if (entry.stepId) {
            parts.push("step");
          }
          const touchedCount = Array.isArray(entry.touchedResources) ? entry.touchedResources.length : 0;
          if (touchedCount > 0) {
            parts.push(touchedCount + " " + (touchedCount === 1 ? "resource" : "resources"));
          }
          if (entry.snapshotBeforeId || entry.snapshotAfterId) {
            parts.push("snapshots");
          }
          return parts.length > 0 ? parts.join(" · ") : "event evidence";
        }
  
        function getTraceEventDurationMs(record) {
          const metadata = record?.metadata && typeof record.metadata === "object" && !Array.isArray(record.metadata) ? record.metadata : {};
          const candidates = [
            record?.durationMs,
            record?.duration_ms,
            record?.duration,
            record?.elapsedMs,
            record?.elapsed_ms,
            metadata?.durationMs,
            metadata?.duration_ms,
            metadata?.duration,
            metadata?.elapsedMs,
            metadata?.elapsed_ms,
          ];
          for (const value of candidates) {
            const numeric = Number(value);
            if (Number.isFinite(numeric) && numeric >= 0) {
              return numeric;
            }
          }
          const startedAt = Date.parse(String(record?.startedAt || record?.createdAt || metadata?.startedAt || ""));
          const endedAt = Date.parse(String(record?.completedAt || record?.finishedAt || record?.endedAt || metadata?.completedAt || ""));
          if (Number.isFinite(startedAt) && Number.isFinite(endedAt) && endedAt >= startedAt) {
            return endedAt - startedAt;
          }
          return null;
        }
  
        function readTraceTokenNumber(value) {
          const numeric = Number(value);
          return Number.isFinite(numeric) && numeric > 0 ? numeric : 0;
        }
  
        function readTraceTokensFromObject(value) {
          if (!value || typeof value !== "object" || Array.isArray(value)) {
            return 0;
          }
  
          const totalCandidates = [
            value.totalTokens,
            value.total_tokens,
            value.tokenCount,
            value.token_count,
            value.tokens,
            value.total,
            value.completionTokensTotal,
            value.completion_tokens_total,
            value.llmTokens,
            value.llm_tokens,
            value["usage.total_tokens"],
            value["gen_ai.usage.total_tokens"],
            value["llm.token_count.total"],
          ];
          for (const candidate of totalCandidates) {
            const numeric = readTraceTokenNumber(candidate);
            if (numeric) {
              return Math.round(numeric);
            }
          }
  
          const inputCandidates = [
            value.inputTokens,
            value.input_tokens,
            value.promptTokens,
            value.prompt_tokens,
            value.cachedInputTokens,
            value.cached_input_tokens,
            value["usage.input_tokens"],
            value["usage.prompt_tokens"],
            value["gen_ai.usage.input_tokens"],
            value["llm.token_count.prompt"],
          ];
          const outputCandidates = [
            value.outputTokens,
            value.output_tokens,
            value.completionTokens,
            value.completion_tokens,
            value.reasoningTokens,
            value.reasoning_tokens,
            value["usage.output_tokens"],
            value["usage.completion_tokens"],
            value["gen_ai.usage.output_tokens"],
            value["llm.token_count.completion"],
          ];
          const inputTotal = inputCandidates.reduce((total, candidate) => total + readTraceTokenNumber(candidate), 0);
          const outputTotal = outputCandidates.reduce((total, candidate) => total + readTraceTokenNumber(candidate), 0);
          return inputTotal + outputTotal > 0 ? Math.round(inputTotal + outputTotal) : 0;
        }
  
        function getTraceEventTokens(record) {
          const metadata = record?.metadata && typeof record.metadata === "object" && !Array.isArray(record.metadata) ? record.metadata : {};
          const directCandidates = [
            record,
            metadata,
            record?.usage,
            metadata?.usage,
            record?.response?.usage,
            metadata?.response?.usage,
            record?.result?.usage,
            metadata?.result?.usage,
            record?.data?.usage,
            metadata?.data?.usage,
            record?.spanData,
            record?.span_data,
            metadata?.spanData,
            metadata?.span_data,
            record?.attributes,
            metadata?.attributes,
          ];
          for (const candidate of directCandidates) {
            const total = readTraceTokensFromObject(candidate);
            if (total) {
              return total;
            }
          }
  
          const visited = new WeakSet();
          const searchNested = (value, depth = 0) => {
            if (!value || typeof value !== "object" || depth > 4) {
              return 0;
            }
            if (visited.has(value)) {
              return 0;
            }
            visited.add(value);
  
            const localTotal = readTraceTokensFromObject(value);
            if (localTotal) {
              return localTotal;
            }
  
            if (Array.isArray(value)) {
              for (const entry of value) {
                const nestedTotal = searchNested(entry, depth + 1);
                if (nestedTotal) {
                  return nestedTotal;
                }
              }
              return 0;
            }
  
            for (const entry of Object.values(value)) {
              const nestedTotal = searchNested(entry, depth + 1);
              if (nestedTotal) {
                return nestedTotal;
              }
            }
            return 0;
          };
  
          return searchNested(record);
        }
  
        function formatTraceTokenLabel(value, suffix = " total") {
          const numeric = Number(value);
          if (!Number.isFinite(numeric) || numeric <= 0) {
            return "-";
          }
          return Math.round(numeric).toLocaleString("en-US") + suffix;
        }
  
        function formatTraceDateTime(value) {
          if (!value) return "-";
          const date = new Date(value);
          if (Number.isNaN(date.getTime())) return "-";
          return new Intl.DateTimeFormat("en-GB", {
            day: "2-digit",
            month: "short",
            year: "numeric",
            hour: "2-digit",
            minute: "2-digit",
          }).format(date);
        }
  
        function formatTraceDurationMs(value) {
          const numericValue = Number(value);
          if (!Number.isFinite(numericValue) || numericValue < 0) {
            return "—";
          }
          return Math.round(numericValue).toLocaleString("en-US") + " ms";
        }
  
        function formatTraceRawJson(value) {
          try {
            return JSON.stringify(value || {}, null, 2);
          } catch {
            return "{}";
          }
        }
  
        function ThreadTraceRawEventEditor({ eventId, value }) {
          const [editorModule, setEditorModule] = useState(null);
          const [editorModuleError, setEditorModuleError] = useState("");
          const rawText = useMemo(() => formatTraceRawJson(value), [value]);
          const editorHeight = useMemo(() => {
            const lineCount = String(rawText || "").split(/\\n/).length;
            return Math.min(420, Math.max(160, (lineCount + 2) * 20)) + "px";
          }, [rawText]);
          const MonacoEditorComponent = editorModule?.default || null;
  
          useEffect(() => {
            let cancelled = false;
  
            void loadPlaygroundCodeEditorModule()
              .then((module) => {
                if (cancelled || !module) return;
                setEditorModule(module);
                void module.loader?.init?.()
                  .then((monaco) => {
                    if (!cancelled) {
                      ensurePlaygroundCodeEditorTheme(monaco);
                    }
                  })
                  .catch(() => {});
              })
              .catch((error) => {
                if (cancelled) return;
                setEditorModuleError(error instanceof Error ? error.message : "Failed to load editor.");
              });
  
            return () => {
              cancelled = true;
            };
          }, []);
  
          if (MonacoEditorComponent) {
            const normalizedEventId = String(eventId || "event").replace(/[^A-Za-z0-9_.:-]+/g, "_");
            return React.createElement("div", {
                className: "playground-code-preview-editor-shell playground-thread-observability-expanded-raw-editor",
                style: { height: editorHeight },
              },
              React.createElement(MonacoEditorComponent, {
                path: "thread-trace-raw-" + normalizedEventId + ".json",
                height: editorHeight,
                language: "json",
                theme: PLAYGROUND_CODE_EDITOR_THEME_NAME,
                value: rawText,
                beforeMount: ensurePlaygroundCodeEditorTheme,
                options: {
                  automaticLayout: true,
                  minimap: { enabled: false },
                  scrollBeyondLastLine: false,
                  smoothScrolling: true,
                  readOnly: true,
                  fontSize: 12,
                  lineHeight: 20,
                  tabSize: 2,
                  insertSpaces: true,
                  renderLineHighlight: "none",
                  lineNumbersMinChars: 3,
                  overviewRulerBorder: false,
                  hideCursorInOverviewRuler: true,
                  wordWrap: "on",
                  padding: { top: 12, bottom: 12 },
                  fontFamily: 'ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace',
                },
              })
            );
          }
  
          if (!editorModuleError) {
            return React.createElement("div", {
              className: "playground-code-preview-state playground-thread-observability-expanded-raw-editor",
              style: { height: editorHeight },
            },
              React.createElement(Loader2, { className: "playground-files-state-loader", strokeWidth: 1.75 }),
              React.createElement("span", null, "Loading editor...")
            );
          }
  
          return React.createElement("pre", { className: "playground-thread-observability-expanded-raw" }, rawText);
        }
  
        function buildThreadTraceTimeline(thread, details, fallbackTitle = "") {
          const normalizedThread = thread ? normalizeThreadItem(thread) : normalizeThreadItem({
            id: "",
            title: fallbackTitle || "Thread",
          });
          const logs = Array.isArray(details?.logs) ? details.logs : [];
          const messages = Array.isArray(details?.messages) ? details.messages : [];
          const steps = Array.isArray(details?.steps) ? details.steps : [];
          const traceSequences = Array.isArray(details?.traceClusters?.sequences) ? details.traceClusters.sequences : [];
          const decisions = traceSequences.length > 0 ? traceSequences : Array.isArray(details?.decisions) ? details.decisions : [];
          const timeline = [];
          const startedAt = normalizedThread.startedAt || normalizedThread.createdAt || details?.thread?.startedAt || details?.thread?.createdAt || "";
          const completedAt = normalizedThread.completedAt || normalizedThread.finishedAt || normalizedThread.endedAt || details?.thread?.completedAt || details?.thread?.finishedAt || details?.thread?.endedAt || "";
          const threadStartMs = Date.parse(String(startedAt || "")) || 0;
          const threadEndMs = Date.parse(String(completedAt || "")) || 0;
          const threadDurationMs = threadStartMs && threadEndMs && threadEndMs >= threadStartMs
            ? threadEndMs - threadStartMs
            : Number(details?.thread?.durationMs || details?.thread?.duration || 0) || null;
  
          timeline.push({
            id: "thread",
            Icon: GitFork,
            kind: "thread",
            type: "thread",
            title: getTraceThreadTitle(normalizedThread, fallbackTitle),
            copy: normalizedThread.status ? getTraceStatusLabel(normalizedThread.status) : "Thread",
            createdAt: startedAt,
            timestamp: threadStartMs,
            durationMs: threadDurationMs,
            raw: normalizedThread,
            tokenCount: getTraceEventTokens(normalizedThread) || getTraceEventTokens(details?.thread || {}),
          });
  
          const stepEvents = steps.map((step, index) => {
            const type = getTraceStepType(step);
            const ring = getTraceRingNumber(step);
            return {
              source: "step",
              record: step,
              index,
              idPrefix: "step",
              type,
              title: getTraceStepTitle(step),
              copy: getTraceStepCopy(step),
              createdAt: getTraceEventCreatedAt(step),
              raw: step,
              stepId: String(step?.id || "").trim(),
              stepSequence: Number.isFinite(Number(step?.sequence)) ? Number(step.sequence) : null,
              snapshotBeforeId: String(step?.snapshotBeforeId || step?.snapshot_before_id || "").trim(),
              snapshotAfterId: String(step?.snapshotAfterId || step?.snapshot_after_id || "").trim(),
              ring,
              ringLabel: getTraceRingLabel(ring),
              ringDescription: getTraceRingDescription(ring),
              decisionStatus: getTraceDecisionStatus(step),
              touchedResources: collectTraceTouchedResources(step),
            };
          });
  
          const decisionEvents = decisions.map((decision, index) => {
            const type = getTraceStepType(decision);
            const ring = getTraceRingNumber(decision);
            const source = String(decision?.source || "").trim() === "trace_sequence" ? "trace_sequence" : "decision";
            return {
              source,
              record: decision,
              index,
              idPrefix: source === "trace_sequence" ? "trace-sequence" : "decision",
              type,
              title: getTraceDecisionTitle(decision),
              copy: getTraceDecisionCopy(decision),
              createdAt: getTraceEventCreatedAt(decision),
              raw: decision,
              stepId: getTraceDecisionStepId(decision),
              stepSequence: getTraceDecisionStepSequence(decision),
              snapshotBeforeId: getTraceDecisionSnapshotId(decision, "before"),
              snapshotAfterId: getTraceDecisionSnapshotId(decision, "after"),
              ring,
              ringLabel: getTraceRingLabel(ring),
              ringDescription: getTraceRingDescription(ring),
              decisionStatus: getTraceDecisionStatus(decision),
              touchedResources: collectTraceTouchedResources(decision),
            };
          });
  
          const sourceEvents = decisionEvents.length > 0
            ? decisionEvents
            : stepEvents.length > 0
            ? stepEvents
            : logs.length > 0
            ? logs.map((log, index) => ({
                source: "log",
                record: log,
                index,
                idPrefix: "log",
                type: getTraceEventType(log),
                title: getTraceEventTitle(log),
                copy: getTraceEventCopy(log),
                createdAt: getTraceEventCreatedAt(log),
                raw: log,
              }))
            : messages.map((message, index) => ({
                source: "message",
                record: message,
                index,
                idPrefix: "message",
                type: String(message?.role || "message").trim().toLowerCase() + "_message",
                title: getTraceStatusLabel(message?.role || "message") + " message",
                copy: readTraceText(message?.content || "", 180),
                createdAt: getTraceEventCreatedAt(message),
                raw: message,
              }));
  
          sourceEvents
            .map((event, index) => ({
              id: String(event.record?.id || event.idPrefix + ":" + index),
              kind: getTraceEventKindClass(event.type),
              source: event.source,
              type: event.type,
              timestamp: readTraceTimestamp(event.record),
              title: event.title,
              copy: event.copy,
              createdAt: event.createdAt,
              durationMs: getTraceEventDurationMs(event.record),
              raw: event.raw,
              tokenCount: getTraceEventTokens(event.record),
              actionCount: Number.isFinite(Number(event.record?.actionCount || event.record?.action_count))
                ? Number(event.record?.actionCount || event.record?.action_count)
                : null,
              stepId: event.stepId || "",
              stepSequence: event.stepSequence,
              snapshotBeforeId: event.snapshotBeforeId || "",
              snapshotAfterId: event.snapshotAfterId || "",
              ring: event.ring || getTraceRingNumber(event.record),
              ringLabel: event.ringLabel || getTraceRingLabel(getTraceRingNumber(event.record)),
              ringDescription: event.ringDescription || getTraceRingDescription(getTraceRingNumber(event.record)),
              decisionStatus: event.decisionStatus || getTraceDecisionStatus(event.record),
              touchedResources: Array.isArray(event.touchedResources) ? event.touchedResources : collectTraceTouchedResources(event.record),
            }))
            .filter((entry) => entry.title || entry.copy)
            .sort((left, right) => {
              if (left.timestamp !== right.timestamp) {
                return left.timestamp - right.timestamp;
              }
              return left.id.localeCompare(right.id);
            })
            .forEach((entry) => {
              timeline.push({
                ...entry,
                Icon: getTraceEventIcon(entry.type),
              });
            });
  
          return timeline;
        }
  
        function getTraceClusterActionCandidates(record, actionById = new Map()) {
          const safeRecord = record && typeof record === "object" && !Array.isArray(record) ? record : {};
          const metadata = getTraceRecordMetadata(safeRecord);
          const raw = safeRecord.raw && typeof safeRecord.raw === "object" && !Array.isArray(safeRecord.raw) ? safeRecord.raw : {};
          const candidates = [];
          const appendActionList = (value) => {
            if (!Array.isArray(value)) return;
            value.forEach((entry) => {
              if (entry && typeof entry === "object" && !Array.isArray(entry)) {
                candidates.push(entry);
              } else if (typeof entry === "string" && entry.trim()) {
                candidates.push({ id: entry, message: entry, title: entry });
              }
            });
          };
          appendActionList(safeRecord.actions);
          appendActionList(safeRecord.traceActions);
          appendActionList(safeRecord.trace_actions);
          appendActionList(metadata.actions);
          appendActionList(metadata.traceActions);
          appendActionList(metadata.trace_actions);
          appendActionList(raw.actions);
          appendActionList(raw.traceActions);
          appendActionList(raw.trace_actions);
  
          const actionIds = [];
          const appendIds = (value) => {
            if (Array.isArray(value)) {
              value.forEach(appendIds);
              return;
            }
            const text = String(value || "").trim();
            if (text) actionIds.push(text);
          };
          appendIds(safeRecord.actionIds);
          appendIds(safeRecord.action_ids);
          appendIds(metadata.actionIds);
          appendIds(metadata.action_ids);
          appendIds(raw.actionIds);
          appendIds(raw.action_ids);
          actionIds.forEach((actionId) => {
            const action = actionById.get(actionId);
            if (action) candidates.push(action);
          });
  
          const seen = new Set();
          return candidates.filter((action, index) => {
            const id = String(action?.id || action?.sourceId || action?.source_id || action?.stepId || action?.step_id || index).trim();
            if (seen.has(id)) return false;
            seen.add(id);
            return true;
          });
        }
  
        function getTraceClusterActionEntries(record, actionById = new Map()) {
          return getTraceClusterActionCandidates(record, actionById).map((action, index) => {
            const metadata = getTraceRecordMetadata(action);
            const type = getTraceStepType(action) || getTraceEventType(action) || String(action?.type || action?.source || "action").trim().toLowerCase();
            const ring = getTraceRingNumber(action);
            const createdAt = getTraceEventCreatedAt(action);
            return {
              id: String(action?.id || action?.sourceId || action?.source_id || action?.stepId || action?.step_id || "action:" + index).trim(),
              type,
              title: getTraceDecisionTitle(action) || getTraceStepTitle(action) || getTraceEventTitle(action) || "Trace action",
              copy: getTraceDecisionCopy(action) || getTraceStepCopy(action) || getTraceEventCopy(action) || "",
              createdAt,
              timestamp: readTraceTimestamp(action),
              durationMs: getTraceEventDurationMs(action),
              tokenCount: getTraceEventTokens(action),
              ring,
              ringLabel: action?.ringLabel || action?.ring_label || getTraceRingLabel(ring),
              ringDescription: action?.ringDescription || action?.ring_description || getTraceRingDescription(ring),
              stepId: String(action?.stepId || action?.step_id || (action?.source === "step" ? action?.id : "") || "").trim(),
              stepSequence: Number.isFinite(Number(action?.stepSequence || action?.step_sequence || action?.sequence))
                ? Number(action?.stepSequence || action?.step_sequence || action?.sequence)
                : null,
              touchedResources: collectTraceTouchedResources(action),
              raw: action,
              category: String(metadata?.category || action?.category || "").trim(),
            };
          });
        }
  
        function getTraceClusterSearchText(record, actionById = new Map()) {
          const actionText = getTraceClusterActionEntries(record, actionById)
            .map((action) => [
              action.id,
              action.title,
              action.copy,
              action.type,
              action.ringLabel,
              action.touchedResources.join(" "),
            ].join(" "))
            .join(" ");
          return actionText.toLowerCase();
        }
  
        function renderThreadTraceDetailView({
          thread,
          details,
          fallbackThreadId = "",
          fallbackTitle = "",
          loadingLabel = "Loading trace...",
          backendUrl = "",
          requestHeaders = null,
          onOpenChat,
          onOpenTrace,
          onForkCreated,
          traceSearchQuery = "",
          traceEventFilter = "all",
          traceEventSort = "timeline",
        }) {
          return React.createElement(ThreadTraceDetailInspector, {
            thread,
            details,
            fallbackThreadId,
            fallbackTitle,
            loadingLabel,
            backendUrl,
            requestHeaders,
            onOpenChat,
            onOpenTrace,
            onForkCreated,
            traceSearchQuery,
            traceEventFilter,
            traceEventSort,
          });
        }
  
        function ThreadTraceDetailInspector({
          thread,
          details,
          fallbackThreadId = "",
          fallbackTitle = "",
          loadingLabel = "Loading trace...",
          backendUrl = "",
          requestHeaders = null,
          onOpenChat,
          onOpenTrace,
          onForkCreated,
          traceSearchQuery = "",
          traceEventFilter = "all",
          traceEventSort = "timeline",
        }) {
          if (!details || details.status === "loading") {
            return React.createElement("div", { className: "playground-agents-observability-detail is-loading" }, loadingLabel);
          }
  
          if (details.status === "error") {
            return React.createElement("div", { className: "playground-agents-observability-detail is-error" }, details.error || "Failed to load trace data.");
          }
  
          return React.createElement(ThreadTraceDetailLoadedInspector, {
            thread,
            details,
            fallbackThreadId,
            fallbackTitle,
            backendUrl,
            requestHeaders,
            onOpenChat,
            onOpenTrace,
            onForkCreated,
            traceSearchQuery,
            traceEventFilter,
            traceEventSort,
          });
        }
  
        function ThreadTraceDetailLoadedInspector({
          thread,
          details,
          fallbackThreadId = "",
          fallbackTitle = "",
          backendUrl = "",
          requestHeaders = null,
          onOpenChat,
          onOpenTrace,
          onForkCreated,
          traceSearchQuery = "",
          traceEventFilter = "all",
          traceEventSort = "timeline",
        }) {
          const mergedThread = details.thread || thread || { id: fallbackThreadId, title: fallbackTitle };
          const normalizedThread = normalizeThreadItem({
            ...mergedThread,
            id: String(mergedThread?.id || fallbackThreadId || "").trim() || String(fallbackThreadId || "").trim(),
            title: String(mergedThread?.title || fallbackTitle || "").trim() || getTraceThreadTitle(thread, fallbackTitle),
          });
          const messages = Array.isArray(details.messages) ? details.messages : [];
          const steps = Array.isArray(details.steps) ? details.steps : [];
          const logs = Array.isArray(details.logs) ? details.logs : [];
          const traceClusterActions = Array.isArray(details?.traceClusters?.actions) ? details.traceClusters.actions : [];
          const traceClusterActionById = useMemo(() => {
            const next = new Map();
            traceClusterActions.forEach((action, index) => {
              [
                action?.id,
                action?.sourceId,
                action?.source_id,
                action?.stepId,
                action?.step_id,
                action?.logId,
                action?.log_id,
                index,
              ].forEach((candidate) => {
                const key = String(candidate || "").trim();
                if (key && !next.has(key)) {
                  next.set(key, action);
                }
              });
            });
            return next;
          }, [traceClusterActions]);
          const [traceForkState, setTraceForkState] = useState({
            itemId: "",
            status: "",
            error: "",
          });
          const timeline = useMemo(() => buildThreadTraceTimeline(normalizedThread, details, fallbackTitle), [
            details,
            fallbackTitle,
            normalizedThread.completedAt,
            normalizedThread.createdAt,
            normalizedThread.endedAt,
            normalizedThread.finishedAt,
            normalizedThread.id,
            normalizedThread.startedAt,
            normalizedThread.status,
            normalizedThread.title,
          ]);
          const visibleTimeline = useMemo(() => {
            const normalizedSearchQuery = String(traceSearchQuery || "").trim().toLowerCase();
            const normalizedFilter = String(traceEventFilter || "all").trim().toLowerCase() || "all";
            const normalizedSort = String(traceEventSort || "timeline").trim().toLowerCase() || "timeline";
            const hasDecisionEntries = timeline.some((entry) => isTraceDecisionEntry(entry));
            const matchesFilter = (entry) => {
              if (normalizedFilter === "all") return true;
              if (normalizedFilter === "decisions") return hasDecisionEntries ? isTraceDecisionEntry(entry) : true;
              if (normalizedFilter === "needs-review") return entry.decisionStatus === "needs-review";
              if (normalizedFilter === "ring-2-plus") return Number(entry.ring || 1) >= 2;
              if (normalizedFilter === "ring-3") return Number(entry.ring || 1) >= 3;
              if (normalizedFilter === "messages") return entry.kind === "message";
              if (normalizedFilter === "tools") return entry.kind === "tool";
              if (normalizedFilter === "reasoning") return String(entry.type || "").includes("reasoning") || String(entry.type || "").includes("summary");
              if (normalizedFilter === "subagents") return entry.kind === "subagent";
              if (normalizedFilter === "permissions") return entry.kind === "permission";
              return true;
            };
            const matchesSearch = (entry) => {
              if (!normalizedSearchQuery) return true;
              const haystack = [
                entry.id,
                entry.title,
                entry.copy,
                entry.type,
                entry.kind,
                entry.source,
                entry.ringLabel,
                entry.decisionStatus,
                getTraceEvidenceLabel(entry),
                entry.source === "trace_sequence" ? getTraceClusterSearchText(entry.raw || entry, traceClusterActionById) : "",
              ].join(" ").toLowerCase();
              return haystack.includes(normalizedSearchQuery);
            };
            const items = timeline
              .filter((entry) => matchesFilter(entry) && matchesSearch(entry));
            return items.sort((left, right) => {
              if (normalizedSort === "newest") {
                return (right.timestamp || 0) - (left.timestamp || 0);
              }
              if (normalizedSort === "duration") {
                const leftDuration = Number(left.durationMs || 0);
                const rightDuration = Number(right.durationMs || 0);
                if (leftDuration !== rightDuration) return rightDuration - leftDuration;
              } else if (normalizedSort === "tokens") {
                const leftTokens = Number(left.tokenCount || 0);
                const rightTokens = Number(right.tokenCount || 0);
                if (leftTokens !== rightTokens) return rightTokens - leftTokens;
              }
              if ((left.timestamp || 0) !== (right.timestamp || 0)) {
                return (left.timestamp || 0) - (right.timestamp || 0);
              }
              return String(left.id || "").localeCompare(String(right.id || ""));
            });
          }, [timeline, traceClusterActionById, traceEventFilter, traceEventSort, traceSearchQuery]);
          const [selectedTraceItemId, setSelectedTraceItemId] = useState("");
          useEffect(() => {
            if (visibleTimeline.length === 0) {
              setSelectedTraceItemId("");
              return;
            }
            setSelectedTraceItemId((current) => (
              current && visibleTimeline.some((entry) => entry.id === current) ? current : ""
            ));
          }, [visibleTimeline]);
          const selectedTraceItem = visibleTimeline.find((entry) => entry.id === selectedTraceItemId) || null;
          const selectedTraceClusterActions = useMemo(
            () => getTraceClusterActionEntries(selectedTraceItem?.raw || selectedTraceItem, traceClusterActionById),
            [selectedTraceItem, traceClusterActionById]
          );
          const handleForkFromSelectedTraceItem = useCallback(async () => {
            const normalizedThreadId = String(normalizedThread.id || fallbackThreadId || "").trim();
            const normalizedStepId = String(selectedTraceItem?.stepId || "").trim();
            const normalizedBackendUrl = String(backendUrl || "").trim().replace(new RegExp("/+$"), "");
            if (!normalizedThreadId || !normalizedStepId || !normalizedBackendUrl) {
              return;
            }
  
            setTraceForkState({
              itemId: selectedTraceItem.id,
              status: "loading",
              error: "",
            });
  
            try {
              const response = await fetch(
                normalizedBackendUrl
                  + "/threads/" + encodeURIComponent(normalizedThreadId)
                  + "/steps/" + encodeURIComponent(normalizedStepId)
                  + "/fork",
                {
                  method: "POST",
                  headers: {
                    ...(requestHeaders || {}),
                    "Content-Type": "application/json",
                  },
                  body: JSON.stringify({
                    mode: "historical",
                    title: "Fork from " + (selectedTraceItem.title || "trace decision"),
                  }),
                }
              );
              const data = await response.json().catch(() => ({}));
              if (!response.ok) {
                throw new Error(data?.message || data?.error || "Failed to fork from this decision.");
              }
              const nextThread = data?.thread || data?.data?.thread || null;
              setTraceForkState({
                itemId: selectedTraceItem.id,
                status: "success",
                error: "",
              });
              if (nextThread?.id && typeof onForkCreated === "function") {
                onForkCreated(nextThread);
              }
            } catch (error) {
              setTraceForkState({
                itemId: selectedTraceItem.id,
                status: "error",
                error: error instanceof Error ? error.message : "Failed to fork from this decision.",
              });
            }
          }, [
            backendUrl,
            fallbackThreadId,
            normalizedThread.id,
            onForkCreated,
            requestHeaders,
            selectedTraceItem?.id,
            selectedTraceItem?.stepId,
            selectedTraceItem?.title,
          ]);
          const timelineRangeSource = visibleTimeline.length > 0 ? visibleTimeline : timeline;
          const timelineStartMs = Math.min(...timelineRangeSource.map((entry) => entry.timestamp).filter((value) => Number.isFinite(value) && value > 0), Date.now());
          const timelineEndMs = Math.max(
            ...timelineRangeSource.map((entry) => {
              const start = Number(entry.timestamp || 0);
              const duration = Number(entry.durationMs || 0);
              return start > 0 ? start + Math.max(duration, 1) : duration;
            }),
            timelineStartMs + 1
          );
          const timelineRangeMs = Math.max(1, timelineEndMs - timelineStartMs);
          const selectedRaw = selectedTraceItem?.raw || {};
          const selectedMetadata = selectedRaw?.metadata && typeof selectedRaw.metadata === "object" && !Array.isArray(selectedRaw.metadata)
            ? selectedRaw.metadata
            : {};
          const selectedContent = readTraceText(
            selectedRaw?.summary
            || selectedRaw?.message
            || selectedRaw?.content
            || selectedMetadata?.summary
            || selectedMetadata?.message
            || selectedMetadata?.stdout
            || selectedMetadata?.stderr
            || selectedTraceItem?.copy
            || "",
            1800
          );
          const selectedDurationLabel = selectedTraceItem?.durationMs == null
            ? "-"
            : formatTraceDurationMs(selectedTraceItem.durationMs);
          const selectedTokenLabel = formatTraceTokenLabel(selectedTraceItem?.tokenCount || 0);
          const SelectedTraceIcon = selectedTraceItem?.Icon || Clock;
          const selectedTouchedResources = Array.isArray(selectedTraceItem?.touchedResources) ? selectedTraceItem.touchedResources : [];
          const selectedDecisionStatus = selectedTraceItem?.decisionStatus || getTraceDecisionStatus(selectedRaw);
          const selectedForkStatus = traceForkState.itemId === selectedTraceItem?.id ? traceForkState.status : "";
          const selectedForkError = traceForkState.itemId === selectedTraceItem?.id ? traceForkState.error : "";
          const traceDecisionCount = timeline.filter((entry) => isTraceDecisionEntry(entry)).length;
          const traceRingTwoPlusCount = timeline.filter((entry) => Number(entry.ring || 1) >= 2).length;
          const traceNeedsReviewCount = timeline.filter((entry) => entry.decisionStatus === "needs-review").length;
          const traceForkPointCount = timeline.filter((entry) => entry.stepId && entry.snapshotBeforeId).length;
          const traceSummaryItems = [
            { label: "Trace clusters", value: String(traceDecisionCount || 0) },
            { label: "Ring 2+", value: String(traceRingTwoPlusCount || 0) },
            { label: "Needs review", value: String(traceNeedsReviewCount || 0) },
            { label: "Fork points", value: String(traceForkPointCount || 0) },
          ];
          const selectedProperties = [
            { label: "Created", value: formatTraceDateTime(selectedTraceItem?.createdAt || selectedRaw?.createdAt || selectedRaw?.timestamp || "") },
            { label: "ID", value: selectedTraceItem?.id || "-" },
            { label: "Type", value: getTraceStatusLabel(selectedTraceItem?.type || selectedTraceItem?.kind || "") },
            { label: "Source", value: selectedTraceItem?.source === "trace_sequence" ? "Trace sequence" : isTraceDecisionEntry(selectedTraceItem) ? "Decision record" : getTraceStatusLabel(selectedTraceItem?.source || "event") },
            selectedTraceItem?.stepSequence != null ? { label: "Step", value: "#" + selectedTraceItem.stepSequence } : null,
            selectedTraceItem?.actionCount ? { label: "Actions", value: String(selectedTraceItem.actionCount) } : null,
            isTraceDecisionEntry(selectedTraceItem) ? { label: "Decision status", value: getTraceDecisionStatusLabel(selectedDecisionStatus) } : null,
            { label: "Evidence", value: getTraceEvidenceLabel(selectedTraceItem) },
            selectedTraceItem?.snapshotBeforeId ? { label: "Snapshot before", value: selectedTraceItem.snapshotBeforeId } : null,
            selectedTraceItem?.snapshotAfterId ? { label: "Snapshot after", value: selectedTraceItem.snapshotAfterId } : null,
            selectedTouchedResources.length > 0 ? { label: "Touched", value: selectedTouchedResources.join(", ") } : null,
            { label: "Tokens", value: selectedTokenLabel },
            { label: "Logs", value: String(logs.length) },
            { label: "Steps", value: String(steps.length) },
          ].filter(Boolean);
  
          return React.createElement("div", { className: "playground-agents-observability-detail" },
            React.createElement("div", { className: "playground-thread-observability-summary" },
              traceSummaryItems.map((item) =>
                React.createElement("div", { key: item.label, className: "playground-thread-observability-summary-item" },
                  React.createElement("span", { className: "playground-thread-observability-summary-label" }, item.label),
                  React.createElement("span", { className: "playground-thread-observability-summary-value" }, item.value)
                )
              )
            ),
            React.createElement("div", { className: "playground-agents-observability-tree" },
              visibleTimeline.length > 0
                ? visibleTimeline.map((entry) => {
                    const isSelected = selectedTraceItem?.id === entry.id;
                    const isTraceCluster = entry.source === "trace_sequence";
                    const entryActions = isTraceCluster ? getTraceClusterActionEntries(entry.raw || entry, traceClusterActionById) : [];
                    const entryActionCount = Number(entry.actionCount || entryActions.length || 0);
                    const hasTiming = Number.isFinite(entry.timestamp) && entry.timestamp > 0;
                    const leftPercent = hasTiming
                      ? Math.max(0, Math.min(100, ((entry.timestamp - timelineStartMs) / timelineRangeMs) * 100))
                      : 0;
                    const widthPercent = entry.durationMs == null
                      ? 1.2
                      : Math.max(1.2, Math.min(100 - leftPercent, (Math.max(1, entry.durationMs) / timelineRangeMs) * 100));
                    return React.createElement("div", { key: entry.id, className: "playground-thread-observability-row" },
                      React.createElement("button", {
                          type: "button",
                          className: "playground-agents-observability-node is-" + (entry.kind || "log")
                            + (isTraceDecisionEntry(entry) ? " is-step" : "")
                            + (entry.ring ? " is-ring-" + entry.ring : "")
                            + (isSelected ? " is-selected" : ""),
                          onClick: () => setSelectedTraceItemId((current) => current === entry.id ? "" : entry.id),
                        },
                        React.createElement("span", { className: "playground-agents-observability-node-icon", "aria-hidden": "true" },
                          renderTracePermissionRingIcon(entry.ring || 1)
                        ),
                        React.createElement("div", { className: "playground-agents-observability-node-copy" },
                          React.createElement("div", {
                            className: "playground-agents-observability-node-title",
                            title: entry.title,
                          }, entry.title),
                          isTraceDecisionEntry(entry)
                            ? React.createElement("div", { className: "playground-agents-observability-node-badges" },
                                isTraceCluster && entryActionCount
                                  ? React.createElement("span", { className: "playground-agents-observability-node-badge" }, entryActionCount + " " + (entryActionCount === 1 ? "action" : "actions"))
                                  : React.createElement("span", { className: "playground-agents-observability-node-badge" }, getTraceDecisionStatusLabel(entry.decisionStatus)),
                                React.createElement("span", {
                                  className: "playground-agents-observability-node-badge is-ring is-ring-" + (entry.ring || 1),
                                  title: entry.ringDescription || "",
                                }, entry.ringLabel || getTraceRingLabel(entry.ring))
                              )
                            : null,
                          entry.copy || entry.createdAt
                            ? React.createElement("div", {
                                className: "playground-agents-observability-node-meta",
                                title: [entry.copy, entry.createdAt ? formatTraceDateTime(entry.createdAt) : ""].filter(Boolean).join(" - "),
                              }, entry.copy || formatTraceDateTime(entry.createdAt))
                            : null
                        ),
                        React.createElement("div", { className: "playground-agents-observability-node-duration" },
                          React.createElement("span", null, isTraceCluster && entryActionCount ? entryActionCount + "x" : formatTraceRowTimeLabel(entry)),
                          isSelected
                            ? React.createElement(ChevronDown, { width: 14, height: 14, strokeWidth: 1.8 })
                            : React.createElement(ChevronRight, { width: 14, height: 14, strokeWidth: 1.8 })
                        ),
                        React.createElement("div", { className: "playground-agents-observability-node-bar", "aria-hidden": "true" },
                          React.createElement("span", {
                            className: "playground-agents-observability-node-bar-fill",
                            style: {
                              left: leftPercent.toFixed(3) + "%",
                              width: widthPercent.toFixed(3) + "%",
                            },
                          })
                        )
                      ),
                      isSelected
                        ? React.createElement("div", { className: "playground-thread-observability-expanded" },
                            React.createElement("div", { className: "playground-thread-observability-expanded-header" },
                              React.createElement("div", { className: "playground-thread-observability-expanded-title" },
                                selectedTraceItem?.title || "Trace decision"
                              ),
                              React.createElement("div", { className: "playground-thread-observability-expanded-chips" },
                                selectedTraceItem?.ring
                                  ? React.createElement("span", {
                                      className: "playground-thread-observability-expanded-chip is-ring is-ring-" + selectedTraceItem.ring,
                                      title: selectedTraceItem.ringDescription || "",
                                    },
                                      React.createElement(Shield, { strokeWidth: 1.8 }),
                                      React.createElement("span", null, selectedTraceItem.ringLabel || getTraceRingLabel(selectedTraceItem.ring))
                                    )
                                  : null,
                                React.createElement("span", { className: "playground-thread-observability-expanded-chip is-kind" },
                                  React.createElement(SelectedTraceIcon, { strokeWidth: 1.8 }),
                                  React.createElement("span", null, getTraceStatusLabel(selectedTraceItem?.type || selectedTraceItem?.kind || ""))
                                ),
                                isTraceDecisionEntry(selectedTraceItem)
                                  ? React.createElement("span", {
                                      className: "playground-thread-observability-expanded-chip is-decision-status is-" + selectedDecisionStatus,
                                    },
                                      React.createElement(Check, { strokeWidth: 1.8 }),
                                      React.createElement("span", null, getTraceDecisionStatusLabel(selectedDecisionStatus))
                                    )
                                  : null,
                                selectedTraceItem?.tokenCount
                                  ? React.createElement("span", { className: "playground-thread-observability-expanded-chip" },
                                      React.createElement(Coins, { strokeWidth: 1.8 }),
                                      React.createElement("span", null, formatTraceTokenLabel(selectedTraceItem.tokenCount, "t"))
                                    )
                                  : null,
                                selectedTraceItem?.durationMs != null
                                  ? React.createElement("span", { className: "playground-thread-observability-expanded-chip" },
                                      React.createElement(Clock, { strokeWidth: 1.8 }),
                                      React.createElement("span", null, selectedDurationLabel)
                                    )
                                  : null
                              )
                            ),
                            selectedTraceItem?.stepId
                              ? React.createElement("div", { className: "playground-thread-observability-expanded-actions" },
                                  React.createElement("button", {
                                    type: "button",
                                    className: "playground-environments-action-button" + (selectedForkStatus === "loading" ? " is-loading" : ""),
                                    onClick: handleForkFromSelectedTraceItem,
                                    disabled: selectedForkStatus === "loading",
                                  },
                                    selectedForkStatus === "loading"
                                      ? React.createElement(Loader2, { className: "playground-files-state-loader", strokeWidth: 1.75 })
                                      : React.createElement(GitFork, { width: 14, height: 14, strokeWidth: 1.8 }),
                                    React.createElement("span", null, selectedForkStatus === "success" ? "Fork created" : "Fork from here")
                                  ),
                                  selectedForkError
                                    ? React.createElement("span", { className: "playground-thread-observability-expanded-error" }, selectedForkError)
                                    : null
                                )
                              : null,
                            isTraceDecisionEntry(selectedTraceItem) && Number(selectedTraceItem?.ring || 1) >= 2
                              ? React.createElement("div", { className: "playground-thread-observability-expanded-warning" },
                                  "Forking from this point restores the local thread and computer state only. Externally visible actions after this decision are not undone."
                                )
                              : null,
                            selectedTraceClusterActions.length > 0
                              ? React.createElement("div", { className: "playground-thread-observability-expanded-section" },
                                  React.createElement("div", { className: "playground-thread-observability-expanded-section-header" },
                                    React.createElement("span", null, "Concrete steps"),
                                    React.createElement("span", { className: "playground-thread-observability-expanded-section-count" },
                                      selectedTraceClusterActions.length + " " + (selectedTraceClusterActions.length === 1 ? "action" : "actions")
                                    )
                                  ),
                                  React.createElement("div", { className: "playground-thread-observability-action-list" },
                                    selectedTraceClusterActions.map((action, actionIndex) => {
                                      return React.createElement("div", {
                                          key: action.id || actionIndex,
                                          className: "playground-thread-observability-action-item is-ring-" + (action.ring || 1),
                                        },
                                        React.createElement("span", { className: "playground-thread-observability-action-icon", "aria-hidden": "true" },
                                          renderTracePermissionRingIcon(action.ring || 1)
                                        ),
                                        React.createElement("div", { className: "playground-thread-observability-action-copy" },
                                          React.createElement("div", { className: "playground-thread-observability-action-title", title: action.title },
                                            action.title || "Trace action"
                                          ),
                                          action.copy
                                            ? React.createElement("div", { className: "playground-thread-observability-action-summary", title: action.copy }, action.copy)
                                            : null,
                                          action.touchedResources.length > 0
                                            ? React.createElement("div", { className: "playground-thread-observability-action-resources" },
                                                action.touchedResources.slice(0, 4).map((resource) =>
                                                  React.createElement("span", { key: resource, title: resource }, resource)
                                                )
                                              )
                                            : null
                                        ),
                                        React.createElement("div", { className: "playground-thread-observability-action-meta" },
                                          React.createElement("span", {
                                            className: "playground-thread-observability-action-ring is-ring-" + (action.ring || 1),
                                            title: action.ringDescription || "",
                                          }, action.ringLabel || getTraceRingLabel(action.ring)),
                                          action.stepSequence != null
                                            ? React.createElement("span", null, "#" + action.stepSequence)
                                            : action.createdAt
                                              ? React.createElement("span", null, formatTraceTimestampLabel(action.createdAt))
                                              : null
                                        )
                                      );
                                    })
                                  )
                                )
                              : null,
                            React.createElement("div", { className: "playground-thread-observability-expanded-section" },
                              React.createElement("div", { className: "playground-thread-observability-expanded-section-header" },
                                React.createElement("span", null, "Properties"),
                                React.createElement(ChevronDown, { width: 14, height: 14, strokeWidth: 1.8 })
                              ),
                              React.createElement("div", { className: "playground-thread-observability-expanded-properties" },
                                selectedProperties.map((item) =>
                                  React.createElement("div", { key: item.label, className: "playground-thread-observability-expanded-property" },
                                    React.createElement("div", { className: "playground-thread-observability-expanded-property-label" }, item.label),
                                    React.createElement("div", {
                                      className: "playground-thread-observability-expanded-property-value",
                                      title: item.value,
                                    }, item.value)
                                  )
                                )
                              )
                            ),
                            React.createElement("div", { className: "playground-thread-observability-expanded-section" },
                              React.createElement("div", { className: "playground-thread-observability-expanded-section-header" },
                                React.createElement("span", null, selectedTraceItem?.kind === "thread" ? "Thread context" : isTraceDecisionEntry(selectedTraceItem) ? "Decision summary" : "Event content"),
                                React.createElement(ChevronDown, { width: 14, height: 14, strokeWidth: 1.8 })
                              ),
                              React.createElement("div", { className: "playground-thread-observability-expanded-message" },
                                selectedContent || selectedTraceItem?.copy || "No content recorded for this event."
                              )
                            ),
                            React.createElement("div", { className: "playground-thread-observability-expanded-section is-raw-event" },
                              React.createElement("div", { className: "playground-thread-observability-expanded-section-header" },
                                React.createElement("span", null, "Raw event"),
                                React.createElement(ChevronDown, { width: 14, height: 14, strokeWidth: 1.8 })
                              ),
                              React.createElement(ThreadTraceRawEventEditor, {
                                eventId: selectedTraceItem?.id || "trace-event",
                                value: selectedRaw,
                              })
                            ),
                            onOpenChat || onOpenTrace
                              ? React.createElement("div", { className: "playground-agents-observability-actions" },
                                  onOpenChat
                                    ? React.createElement("button", {
                                        type: "button",
                                        className: "playground-environments-action-button",
                                        onClick: onOpenChat,
                                      }, "Open Chat")
                                    : null,
                                  onOpenTrace
                                    ? React.createElement(PlatformPrimaryButton, {
                                      size: "medium",
                                        type: "button",
                                        className: "playground-environments-action-button is-primary",
                                        onClick: onOpenTrace,
                                      }, "Open Trace")
                                    : null
                                )
                              : null
                          )
                        : null
                    );
                  })
                : React.createElement("div", { className: "playground-agents-observability-empty" }, "No matching trace events.")
            )
          );
        }
  
        function ThreadObservabilityView({ threadId, threadTitle, backendUrl, requestHeaders, hasRealAccess, onOpenChat, onForkCreated }) {
          const traceToolbarRef = useRef(null);
          const [traceDetails, setTraceDetails] = useState(null);
          const [traceSearchQuery, setTraceSearchQuery] = useState("");
          const [traceEventSort, setTraceEventSort] = useState("timeline");
          const [traceEventFilter, setTraceEventFilter] = useState("decisions");
          const [traceToolbarPopover, setTraceToolbarPopover] = useState("");
  
          useEffect(() => {
            if (!traceToolbarPopover) return undefined;
  
            function handleTraceToolbarPointerDown(event) {
              const target = event?.target instanceof Node ? event.target : null;
              if (!target || !traceToolbarRef.current || traceToolbarRef.current.contains(target)) {
                return;
              }
              setTraceToolbarPopover("");
            }
  
            document.addEventListener("mousedown", handleTraceToolbarPointerDown);
            return () => document.removeEventListener("mousedown", handleTraceToolbarPointerDown);
          }, [traceToolbarPopover]);
  
          useEffect(() => {
            let cancelled = false;
            let refreshTimer = null;
            const normalizedThreadId = String(threadId || "").trim();
            setTraceDetails(null);
  
            if (!normalizedThreadId) {
              setTraceDetails({
                status: "error",
                error: "Select a thread to inspect its trace.",
              });
              return () => {
                cancelled = true;
              };
            }
  
            if (!hasRealAccess) {
              setTraceDetails({
                status: "error",
                error: "Sign in with your Computer Agents account to inspect thread traces.",
              });
              return () => {
                cancelled = true;
              };
            }
  
            const scheduleRefreshIfRunning = (details) => {
              if (cancelled) return;
              const status = String(details?.thread?.status || "").trim().toLowerCase();
              if (status !== "running" && status !== "queued" && status !== "requires_action" && status !== "waiting") {
                return;
              }
              refreshTimer = window.setTimeout(() => {
                void loadTraceDetails(false);
              }, 8000);
            };
  
            const loadTraceDetails = async (showLoading) => {
              if (refreshTimer) {
                window.clearTimeout(refreshTimer);
                refreshTimer = null;
              }
              if (showLoading) {
                setTraceDetails({ status: "loading", error: "" });
              }
              try {
                const details = await fetchThreadTraceDetails({
                  backendUrl,
                  threadId: normalizedThreadId,
                  headers: requestHeaders,
                  messageLimit: 160,
                  stepLimit: 160,
                });
                if (!cancelled) {
                  setTraceDetails(details);
                  scheduleRefreshIfRunning(details);
                }
              } catch (error) {
                if (!cancelled) {
                  setTraceDetails((current) => (
                    current && current.status === "loaded"
                      ? { ...current, error: error instanceof Error ? error.message : "Failed to refresh trace data." }
                      : {
                          status: "error",
                          error: error instanceof Error ? error.message : "Failed to load trace data.",
                        }
                  ));
                }
              }
            };
  
            void loadTraceDetails(true);
  
            return () => {
              cancelled = true;
              if (refreshTimer) {
                window.clearTimeout(refreshTimer);
                refreshTimer = null;
              }
            };
          }, [backendUrl, hasRealAccess, requestHeaders, threadId]);
  
          const normalizedThreadId = String(threadId || "").trim();
          const sortOptions = [
            { id: "timeline", label: "Timeline" },
            { id: "newest", label: "Newest first" },
            { id: "duration", label: "Slowest first" },
            { id: "tokens", label: "Most tokens" },
          ];
          const filterOptions = [
            { id: "decisions", label: "Clusters" },
            { id: "needs-review", label: "Needs review" },
            { id: "ring-2-plus", label: "Ring 2+" },
            { id: "ring-3", label: "Ring 3" },
            { id: "all", label: "All events" },
            { id: "messages", label: "Messages" },
            { id: "tools", label: "Tools" },
            { id: "reasoning", label: "Reasoning" },
            { id: "subagents", label: "Subagents" },
            { id: "permissions", label: "Permissions" },
          ];
          const activeTraceFilterLabel = filterOptions.find((option) => option.id === traceEventFilter)?.label || "All events";
          return React.createElement("div", { className: "playground-thread-observability-view" },
            React.createElement("div", { className: "playground-thread-observability-card" },
              React.createElement("div", { className: "playground-plugins-search-row playground-agents-overview-search-row playground-thread-observability-toolbar", ref: traceToolbarRef },
                React.createElement("div", { className: "playground-plugins-search-shell" },
                  React.createElement(Search, { className: "playground-plugins-search-icon", width: 14, height: 14, strokeWidth: 1.8 }),
                  React.createElement("input", {
                    type: "search",
                    value: traceSearchQuery,
                    onChange: (event) => setTraceSearchQuery(event.target.value),
                    className: "playground-plugins-search",
                    placeholder: "Search trace events",
                  })
                ),
                React.createElement("div", { className: "playground-plugins-toolbar-controls" },
                  React.createElement("div", { className: "playground-files-toolbar-anchor playground-tasks-toolbar-popup-shell playground-plugins-sort-shell" },
                    React.createElement("button", {
                      type: "button",
                      className: "playground-files-control-button is-bare is-backlog-sort" + (traceToolbarPopover === "sort" || traceEventSort !== "timeline" ? " is-active" : ""),
                      onClick: () => setTraceToolbarPopover((current) => current === "sort" ? "" : "sort"),
                    },
                      React.createElement(ArrowUpDown, { width: 14, height: 14, strokeWidth: 1.8 }),
                      React.createElement("span", null, "Sort")
                    ),
                    traceToolbarPopover === "sort"
                      ? React.createElement(PlatformPopupSurface, { className: "playground-tasks-toolbar-popup-menu playground-tasks-toolbar-popup-menu-wide playground-tasks-toolbar-popup-menu-animate-down-in" },
                          sortOptions.map((option) =>
                            React.createElement("button", {
                                key: option.id,
                                type: "button",
                                className: "tb-popup-row tb-popup-row-select" + (traceEventSort === option.id ? " selected" : ""),
                                onClick: () => {
                                  setTraceEventSort(option.id);
                                  setTraceToolbarPopover("");
                                },
                              },
                              React.createElement("span", { className: "tb-popup-check-slot" },
                                traceEventSort === option.id
                                  ? React.createElement(Check, { className: "tb-popup-check", width: 14, height: 14, strokeWidth: 1.8 })
                                  : null
                              ),
                              React.createElement("div", { className: "playground-tasks-toolbar-popup-item-copy" },
                                React.createElement("span", null, option.label)
                              )
                            )
                          )
                        )
                      : null
                  ),
                  React.createElement("div", { className: "playground-files-toolbar-anchor playground-tasks-toolbar-popup-shell playground-plugins-filter-shell" },
                    React.createElement("button", {
                      type: "button",
                      className: "playground-files-control-button is-bare is-backlog-filter" + (traceToolbarPopover === "filter" || traceEventFilter !== "all" ? " is-active" : ""),
                      onClick: () => setTraceToolbarPopover((current) => current === "filter" ? "" : "filter"),
  	                  },
  	                    React.createElement(SlidersHorizontal, { width: 14, height: 14, strokeWidth: 1.8 }),
  	                    React.createElement("span", null, "Filter: " + activeTraceFilterLabel)
  	                  ),
                    traceToolbarPopover === "filter"
                      ? React.createElement(PlatformPopupSurface, { className: "playground-tasks-toolbar-popup-menu playground-tasks-toolbar-popup-menu-wide playground-tasks-toolbar-popup-menu-animate-down-in" },
                          filterOptions.map((option) =>
                            React.createElement("button", {
                                key: option.id,
                                type: "button",
                                className: "tb-popup-row tb-popup-row-select" + (traceEventFilter === option.id ? " selected" : ""),
                                onClick: () => {
                                  setTraceEventFilter(option.id);
                                  setTraceToolbarPopover("");
                                },
                              },
                              React.createElement("span", { className: "tb-popup-check-slot" },
                                traceEventFilter === option.id
                                  ? React.createElement(Check, { className: "tb-popup-check", width: 14, height: 14, strokeWidth: 1.8 })
                                  : null
                              ),
                              React.createElement("div", { className: "playground-tasks-toolbar-popup-item-copy" },
                                React.createElement("span", null, option.label)
                              )
                            )
                          )
                        )
                      : null
                  )
                )
              ),
              renderThreadTraceDetailView({
                thread: traceDetails?.thread || { id: normalizedThreadId, title: threadTitle },
                details: traceDetails,
                fallbackThreadId: normalizedThreadId,
                fallbackTitle: threadTitle,
                backendUrl,
                requestHeaders,
                onOpenChat: null,
                onOpenTrace: null,
                onForkCreated,
                traceSearchQuery,
                traceEventFilter,
                traceEventSort,
              })
            )
          );
        }
  
        function resolveHistoryStepPrompt(step, messages) {
          if (!step || typeof step !== "object") {
            return "";
          }
  
          if (Array.isArray(messages) && messages.length > 0) {
            const sourceIndex = typeof step.sourceMessageId === "string" && step.sourceMessageId
              ? messages.findIndex((message) => message && message.id === step.sourceMessageId)
              : -1;
            if (sourceIndex >= 0) {
              for (let index = sourceIndex; index >= 0; index -= 1) {
                const candidate = messages[index];
                if (candidate?.role === "user") {
                  const preview = normalizeHistoryPreviewText(candidate.content, 160);
                  if (preview) return preview;
                }
              }
            }
  
            const stepTimestamp = step.createdAt ? new Date(step.createdAt).getTime() : Number.NaN;
            if (Number.isFinite(stepTimestamp)) {
              let fallbackCandidate = null;
              for (const message of messages) {
                if (!message || message.role !== "user") continue;
                const createdAt = message.createdAt ? new Date(message.createdAt).getTime() : Number.NaN;
                if (!Number.isFinite(createdAt) || createdAt > stepTimestamp) continue;
                fallbackCandidate = message;
              }
              if (fallbackCandidate?.content) {
                const preview = normalizeHistoryPreviewText(fallbackCandidate.content, 160);
                if (preview) return preview;
              }
            }
          }
  
          return normalizeHistoryPreviewText(step.title || "Changes", 160);
        }
  
        function ThreadChangesView({ threadId, threadTitle, backendUrl, apiKey, upstreamUrl, hasRealAccess, onThreadMutated, navigationTarget, onNavigationTargetHandled }) {
          const client = useMemo(() => new RunnerClient(), []);
          const historyHeaders = useMemo(() => ({
            ...(apiKey.trim() ? { "X-API-Key": apiKey.trim() } : {}),
            "X-Runner-Upstream-Url": upstreamUrl,
          }), [apiKey, upstreamUrl]);
  
          const [steps, setSteps] = useState([]);
          const [stepsLoading, setStepsLoading] = useState(false);
          const [stepsLoadedOnce, setStepsLoadedOnce] = useState(false);
          const [stepsError, setStepsError] = useState("");
          const [threadMessages, setThreadMessages] = useState([]);
          const [threadLogs, setThreadLogs] = useState([]);
          const [changesScreenMode, setChangesScreenMode] = useState("timeline");
          const [selectedStepId, setSelectedStepId] = useState("");
          const [stepFiles, setStepFiles] = useState([]);
          const [stepDiffSummary, setStepDiffSummary] = useState(null);
          const [stepDataLoading, setStepDataLoading] = useState(false);
          const [stepDataError, setStepDataError] = useState("");
          const [selectedFilePath, setSelectedFilePath] = useState("");
          const [selectedFileContent, setSelectedFileContent] = useState("");
          const [selectedFileDiff, setSelectedFileDiff] = useState(null);
          const [filePreviewLoading, setFilePreviewLoading] = useState(false);
          const [filePreviewError, setFilePreviewError] = useState("");
          const [historyMutationLoading, setHistoryMutationLoading] = useState(null);
          const [historyMutationError, setHistoryMutationError] = useState("");
          const [reloadNonce, setReloadNonce] = useState(0);
  
          const historyLogsById = useMemo(() => {
            const nextMap = new Map();
            for (const log of Array.isArray(threadLogs) ? threadLogs : []) {
              if (log && typeof log.id === "string" && log.id) {
                nextMap.set(log.id, log);
              }
            }
            return nextMap;
          }, [threadLogs]);
  
          const supplementalStepEntriesById = useMemo(() => {
            return buildSupplementalHistoryStepEntriesById(steps, threadLogs, historyLogsById);
          }, [historyLogsById, steps, threadLogs]);
  
          const displaySteps = useMemo(() => {
            return steps.filter((step) => {
              return shouldDisplayHistoryStep(step, historyLogsById) || (supplementalStepEntriesById.get(step.id) || []).length > 0;
            });
          }, [historyLogsById, supplementalStepEntriesById, steps]);
  
          const sortedSteps = useMemo(() => {
            return [...displaySteps].sort((left, right) => right.sequence - left.sequence);
          }, [displaySteps]);
  
          const allStepsBySequence = useMemo(() => {
            return [...steps].sort((left, right) => right.sequence - left.sequence);
          }, [steps]);
  
          const revertedChangeStepIds = useMemo(() => {
            const nextSet = new Set();
            const orderedSteps = [...steps].sort((left, right) => left.sequence - right.sequence);
            for (const step of orderedSteps) {
              const revertedChangeStepId = readRevertedChangeStepId(step);
              if (!revertedChangeStepId) {
                continue;
              }
              if (readHistoryOperation(step) === "reapply") {
                nextSet.delete(revertedChangeStepId);
              } else {
                nextSet.add(revertedChangeStepId);
              }
            }
            return nextSet;
          }, [steps]);
  
          const selectedStep = useMemo(() => {
            return sortedSteps.find((step) => step.id === selectedStepId) || null;
          }, [selectedStepId, sortedSteps]);
  
          const selectedStepPrompt = selectedStep ? resolveHistoryStepPrompt(selectedStep, threadMessages) : "";
          const selectedStepReverted = Boolean(selectedStep?.id && revertedChangeStepIds.has(selectedStep.id));
          const revertTargetStep = useMemo(() => {
            if (!selectedStep) {
              return null;
            }
  
            if (selectedStep.snapshotBeforeId) {
              const matchedBySnapshot = allStepsBySequence.find((step) => {
                return (
                  step.id !== selectedStep.id
                  && (
                    (step.snapshotAfterId && step.snapshotAfterId === selectedStep.snapshotBeforeId)
                    || (step.snapshotBeforeId && step.snapshotBeforeId === selectedStep.snapshotBeforeId)
                  )
                );
              });
              if (matchedBySnapshot) {
                return matchedBySnapshot;
              }
            }
  
            const previousSteps = allStepsBySequence
              .filter((step) => {
                return (
                  step.id !== selectedStep.id
                  && Number.isFinite(step.sequence)
                  && step.sequence < selectedStep.sequence
                  && (step.snapshotAfterId || step.snapshotBeforeId)
                );
              })
              .sort((left, right) => right.sequence - left.sequence);
  
            return previousSteps[0] || null;
          }, [allStepsBySequence, selectedStep]);
  
          const previewStepFilesById = useMemo(() => {
            const nextMap = new Map();
            for (const step of sortedSteps) {
              nextMap.set(step.id, buildStepFileEntries(step, null, [], supplementalStepEntriesById.get(step.id) || [], historyLogsById));
            }
            if (selectedStep?.id) {
              nextMap.set(
                selectedStep.id,
                stepFiles.length > 0
                  ? stepFiles
                  : buildStepFileEntries(selectedStep, stepDiffSummary, [], supplementalStepEntriesById.get(selectedStep.id) || [], historyLogsById)
              );
            }
            return nextMap;
          }, [historyLogsById, selectedStep, sortedSteps, stepDiffSummary, stepFiles, supplementalStepEntriesById]);
  
          const selectedStepFiles = selectedStep ? (previewStepFilesById.get(selectedStep.id) || []) : [];
  
          const changedPaths = useMemo(() => {
            return uniqueHistoryPaths([
              ...(selectedStep ? extractStepChangedPaths(selectedStep, historyLogsById) : []),
              ...(Array.isArray(stepDiffSummary?.changedPaths) ? stepDiffSummary.changedPaths : []),
            ]);
          }, [historyLogsById, selectedStep, stepDiffSummary]);
  
          const selectedFileEntry = useMemo(() => {
            return selectedStepFiles.find((entry) => historyPathsMatch(entry.path, selectedFilePath)) || null;
          }, [selectedFilePath, selectedStepFiles]);
          const selectedFileIsImage = Boolean(selectedFileEntry && selectedFileEntry.type === "file" && isHistoryImagePath(selectedFileEntry.path));
          const selectedFileImageUrl = selectedFileEntry && selectedStep && selectedFileIsImage
            ? buildThreadStepFileDownloadUrl(threadId, selectedStep.id, selectedFileEntry.path)
            : "";
  
          const groupedTimelineSteps = useMemo(() => {
            const groups = [];
            let currentGroup = null;
  
            for (const step of sortedSteps) {
              const groupKey = getHistoryDateGroupKey(step.createdAt);
              if (!currentGroup || currentGroup.key !== groupKey) {
                currentGroup = {
                  key: groupKey,
                  label: formatHistoryDateHeading(step.createdAt),
                  steps: [],
                };
                groups.push(currentGroup);
              }
  
                currentGroup.steps.push({
                step,
                prompt: resolveHistoryStepPrompt(step, threadMessages),
                files: previewStepFilesById.get(step.id) || [],
              });
            }
  
            return groups;
          }, [previewStepFilesById, sortedSteps, threadMessages]);
  
          useEffect(() => {
            let cancelled = false;
  
            setSteps([]);
            setStepsError("");
            setStepsLoadedOnce(false);
            setThreadMessages([]);
            setThreadLogs([]);
            setChangesScreenMode("timeline");
            setSelectedStepId("");
            setStepFiles([]);
            setStepDiffSummary(null);
            setStepDataError("");
            setSelectedFilePath("");
            setSelectedFileContent("");
            setSelectedFileDiff(null);
            setFilePreviewError("");
            setHistoryMutationError("");
  
            if (!threadId) {
              setStepsLoading(false);
              setStepsLoadedOnce(true);
              return () => {
                cancelled = true;
              };
            }
  
            if (!hasRealAccess) {
              setStepsLoading(false);
              setStepsError("Sign in with your Computer Agents account to inspect thread changes.");
              setStepsLoadedOnce(true);
              return () => {
                cancelled = true;
              };
            }
  
            setStepsLoading(true);
  
            Promise.allSettled([
              client.listThreadSteps({
                backendUrl,
                threadId,
                limit: 500,
                headers: historyHeaders,
              }),
              fetchHistoryThreadMessages({
                backendUrl,
                threadId,
                headers: historyHeaders,
              }),
              fetchHistoryThreadLogs({
                client,
                backendUrl,
                threadId,
                headers: historyHeaders,
              }),
            ]).then((results) => {
              if (cancelled) return;
              const [stepsResult, messagesResult, logsResult] = results;
  
              if (stepsResult.status === "fulfilled") {
                setSteps(Array.isArray(stepsResult.value) ? stepsResult.value : []);
                setStepsError("");
              } else {
                setSteps([]);
                setStepsError(stepsResult.reason instanceof Error ? stepsResult.reason.message : "Failed to load thread changes.");
              }
  
              if (messagesResult.status === "fulfilled") {
                setThreadMessages(Array.isArray(messagesResult.value) ? messagesResult.value : []);
              } else {
                setThreadMessages([]);
              }
  
              if (logsResult.status === "fulfilled") {
                setThreadLogs(Array.isArray(logsResult.value) ? logsResult.value : []);
              } else {
                setThreadLogs([]);
              }
            }).finally(() => {
              if (!cancelled) {
                setStepsLoading(false);
                setStepsLoadedOnce(true);
              }
            });
  
            return () => {
              cancelled = true;
            };
          }, [backendUrl, client, hasRealAccess, historyHeaders, reloadNonce, threadId]);
  
          useEffect(() => {
            if (!navigationTarget || !threadId || navigationTarget.threadId !== threadId) {
              return;
            }
  
            const targetStep = sortedSteps.find((step) => step.id === navigationTarget.stepId);
            if (!targetStep) {
              if (stepsLoadedOnce) {
                onNavigationTargetHandled?.(navigationTarget.token);
              }
              return;
            }
  
            const fallbackPath = getPreferredHistoryEntryPath(previewStepFilesById.get(targetStep.id) || [], extractStepChangedPaths(targetStep, historyLogsById));
            setSelectedStepId(targetStep.id);
            setSelectedFilePath(navigationTarget.filePath || fallbackPath);
            setChangesScreenMode(navigationTarget.openDetail ? "detail" : "timeline");
            onNavigationTargetHandled?.(navigationTarget.token);
          }, [historyLogsById, navigationTarget, onNavigationTargetHandled, previewStepFilesById, sortedSteps, stepsLoadedOnce, threadId]);
  
          useEffect(() => {
            if (sortedSteps.length === 0) {
              setSelectedStepId("");
              return;
            }
  
            if (selectedStepId && sortedSteps.some((step) => step.id === selectedStepId)) {
              return;
            }
  
            const preferredStep = getPreferredHistoryStep(sortedSteps, historyLogsById);
            setSelectedStepId(preferredStep?.id || "");
          }, [historyLogsById, selectedStepId, sortedSteps]);
  
          useEffect(() => {
            let cancelled = false;
  
            setStepFiles([]);
            setStepDiffSummary(null);
            setStepDataError("");
  
            if (!threadId || !selectedStep || selectedStep.threadId !== threadId) {
              setStepDataLoading(false);
              return () => {
                cancelled = true;
              };
            }
  
            setStepDataLoading(true);
  
            client.getThreadStepDiff({
              backendUrl,
              threadId,
              stepId: selectedStep.id,
              headers: historyHeaders,
            }).then((nextDiffSummary) => {
              if (cancelled) return;
              setStepDiffSummary(nextDiffSummary);
              setStepFiles(buildStepFileEntries(selectedStep, nextDiffSummary, [], supplementalStepEntriesById.get(selectedStep.id) || [], historyLogsById));
              setStepDataError("");
            }).catch((error) => {
              if (cancelled) return;
              const fallbackFiles = buildStepFileEntries(selectedStep, null, [], supplementalStepEntriesById.get(selectedStep.id) || [], historyLogsById);
              setStepFiles(fallbackFiles);
              setStepDiffSummary(null);
              if (fallbackFiles.length === 0) {
                setStepDataError(error instanceof Error ? error.message : "Failed to load step state.");
              } else {
                setStepDataError("");
              }
            }).finally(() => {
              if (!cancelled) {
                setStepDataLoading(false);
              }
            });
  
            return () => {
              cancelled = true;
            };
          }, [backendUrl, client, historyHeaders, historyLogsById, selectedStep, supplementalStepEntriesById, threadId]);
  
          useEffect(() => {
            const currentEntryStillExists = selectedStepFiles.some((entry) => historyPathsMatch(entry.path, selectedFilePath));
            if (currentEntryStillExists) {
              return;
            }
            setSelectedFilePath(getPreferredHistoryEntryPath(selectedStepFiles, changedPaths));
          }, [changedPaths, selectedFilePath, selectedStepFiles]);
  
          useEffect(() => {
            let cancelled = false;
  
            setSelectedFileContent("");
            setSelectedFileDiff(null);
            setFilePreviewError("");
  
            if (!threadId || !selectedStep || selectedStep.threadId !== threadId || !selectedFileEntry || selectedFileEntry.type !== "file") {
              setFilePreviewLoading(false);
              return () => {
                cancelled = true;
              };
            }
  
            if (isHistoryImagePath(selectedFileEntry.path)) {
              setFilePreviewLoading(false);
              return () => {
                cancelled = true;
              };
            }
  
            setFilePreviewLoading(true);
            client.getThreadStepDiff({
              backendUrl,
              threadId,
              stepId: selectedStep.id,
              path: selectedFileEntry.path,
              headers: historyHeaders,
            }).then(async (diffResult) => {
              if (cancelled) return;
              setSelectedFileDiff(diffResult);
  
              const hasRenderableDiff = typeof diffResult?.diff === "string" && diffResult.diff.trim().length > 0;
              const hasStepMetadataDiff = typeof selectedFileEntry.diffText === "string" && selectedFileEntry.diffText.trim().length > 0;
              const hasSingleStepFallbackDiff =
                typeof stepDiffSummary?.diff === "string" &&
                selectedStepFiles.length === 1 &&
                stepDiffSummary.diff.trim().length > 0;
              if (hasRenderableDiff || hasStepMetadataDiff || hasSingleStepFallbackDiff || selectedFileEntry.changeKind === "deleted") {
                return;
              }
  
              try {
                const fileResult = await client.getThreadStepFile({
                  backendUrl,
                  threadId,
                  stepId: selectedStep.id,
                  path: selectedFileEntry.path,
                  headers: historyHeaders,
                });
                if (!cancelled) {
                  setSelectedFileContent(typeof fileResult?.content === "string" ? fileResult.content : "");
                }
              } catch {
                if (!cancelled) {
                  setFilePreviewError("Failed to load file preview for this step.");
                }
              }
            }).catch(async () => {
              if (cancelled) return;
              try {
                const fileResult = await client.getThreadStepFile({
                  backendUrl,
                  threadId,
                  stepId: selectedStep.id,
                  path: selectedFileEntry.path,
                  headers: historyHeaders,
                });
                if (!cancelled) {
                  setSelectedFileContent(typeof fileResult?.content === "string" ? fileResult.content : "");
                }
              } catch {
                if (!cancelled) {
                  setFilePreviewError("Failed to load file preview for this step.");
                }
              }
            }).finally(() => {
              if (!cancelled) {
                setFilePreviewLoading(false);
              }
            });
  
            return () => {
              cancelled = true;
            };
          }, [backendUrl, client, historyHeaders, selectedFileEntry, selectedStep, threadId]);
  
          const changedPathSet = useMemo(() => new Set(uniqueHistoryPaths(changedPaths)), [changedPaths]);
          const selectedFileChanged = selectedFileEntry ? changedPathSet.has(normalizeHistoryPath(selectedFileEntry.path)) : false;
          const detailTitle = selectedFileEntry?.name || selectedStep?.title || "Changes";
          const selectedResourceMetaRows = selectedFileEntry ? buildHistoryResourceMetaRows(selectedFileEntry) : [];
          const resolvedDiffText = selectedFileIsImage
            ? ""
            : selectedFileEntry?.type !== "file"
              ? ""
              : typeof selectedFileDiff?.diff === "string" && selectedFileDiff.diff.trim()
              ? selectedFileDiff.diff
              : typeof selectedFileEntry?.diffText === "string" && selectedFileEntry.diffText.trim()
                ? selectedFileEntry.diffText
                : typeof stepDiffSummary?.diff === "string" && selectedStepFiles.length === 1 && stepDiffSummary.diff.trim()
                  ? stepDiffSummary.diff
                  : buildSyntheticHistoryDiff({
                      filePath: selectedFileEntry?.path,
                      fileContent: selectedFileContent,
                      changeKind: selectedFileEntry?.changeKind,
                    });
          const derivedDiffStats = resolvedDiffText ? summarizeUnifiedDiffStats(resolvedDiffText) : { additions: 0, deletions: 0 };
          const additions = Number.isFinite(selectedFileDiff?.additions)
            ? selectedFileDiff.additions > 0 || !resolvedDiffText
              ? selectedFileDiff.additions
              : derivedDiffStats.additions
            : Number.isFinite(selectedFileEntry?.additions)
              ? selectedFileEntry.additions > 0 || !resolvedDiffText
                ? selectedFileEntry.additions
                : derivedDiffStats.additions
              : Number.isFinite(stepDiffSummary?.additions) && selectedStepFiles.length === 1
                ? stepDiffSummary.additions > 0 || !resolvedDiffText
                  ? stepDiffSummary.additions
                  : derivedDiffStats.additions
                : derivedDiffStats.additions;
          const deletions = Number.isFinite(selectedFileDiff?.deletions)
            ? selectedFileDiff.deletions > 0 || !resolvedDiffText
              ? selectedFileDiff.deletions
              : derivedDiffStats.deletions
            : Number.isFinite(selectedFileEntry?.deletions)
              ? selectedFileEntry.deletions > 0 || !resolvedDiffText
                ? selectedFileEntry.deletions
                : derivedDiffStats.deletions
              : Number.isFinite(stepDiffSummary?.deletions) && selectedStepFiles.length === 1
                ? stepDiffSummary.deletions > 0 || !resolvedDiffText
                  ? stepDiffSummary.deletions
                  : derivedDiffStats.deletions
                : derivedDiffStats.deletions;
          const diffEmptyMessage = selectedFileIsImage
            ? ""
            : selectedFileChanged
              ? "No stored diff is available for this file at the selected step."
              : "This file did not change in the selected step.";
          const filePreview = typeof selectedFileContent === "string" && selectedFileContent.length > 0
            ? selectedFileContent
            : selectedFileEntry && selectedFileEntry.type === "file" && selectedFileEntry.changeKind === "deleted"
              ? "This file was deleted at the selected step."
              : selectedFileEntry && selectedFileEntry.type === "file"
                ? "No file content is available for the selected step."
                : "Select a file to inspect its full content at this step.";
  
          function handleStepSelection(step, files) {
            setSelectedStepId(step.id);
            setSelectedFilePath(getPreferredHistoryEntryPath(files, extractStepChangedPaths(step, historyLogsById)));
          }
  
          function handleFileSelection(step, path) {
            setSelectedStepId(step.id);
            setSelectedFilePath(path);
            setChangesScreenMode("detail");
          }
  
          async function handleApplyHistoryAction(historyActionType) {
            const targetStep = historyActionType === "reapply" ? selectedStep : revertTargetStep;
  
            if (!threadId || !selectedStep || !targetStep || historyMutationLoading) {
              return;
            }
  
            setHistoryMutationLoading(historyActionType);
            setHistoryMutationError("");
  
            try {
              await client.revertThreadToStep({
                backendUrl,
                threadId,
                stepId: targetStep.id,
                headers: historyHeaders,
                historyActionType,
                revertedChangeStepId: selectedStep.id,
                revertedFilePath: selectedFileEntry?.path || undefined,
                revertedFileName: selectedFileEntry?.name || detailTitle,
              });
              setChangesScreenMode("timeline");
              setSelectedStepId("");
              setSelectedFilePath("");
              setSelectedFileContent("");
              setSelectedFileDiff(null);
              setReloadNonce((current) => current + 1);
              if (typeof onThreadMutated === "function") {
                await Promise.resolve(onThreadMutated());
              }
            } catch (error) {
              setHistoryMutationError(
                error instanceof Error
                  ? error.message
                  : historyActionType === "reapply"
                    ? "Failed to re-apply this change."
                    : "Failed to revert this change."
              );
            } finally {
              setHistoryMutationLoading(null);
            }
          }
  
          if (!threadId) {
            return React.createElement("div", { className: "changes-view is-timeline" },
              React.createElement("section", { className: "changes-panel", style: { gridColumn: "1 / -1" } },
                React.createElement("div", { className: "changes-empty-state" }, "Select a thread to inspect its changes.")
              )
            );
          }
  
          if (changesScreenMode === "detail") {
            return React.createElement("div", { className: "changes-view is-detail" },
              React.createElement("section", { className: "changes-panel changes-diff-screen-panel" },
                React.createElement("div", { className: "changes-diff-screen-header" },
                  React.createElement("button", {
                    type: "button",
                    className: "changes-diff-back-button",
                    onClick: () => setChangesScreenMode("timeline"),
                  },
                    React.createElement(ArrowLeft, null),
                    React.createElement("span", null, "Back")
                  ),
                  React.createElement("div", { className: "changes-diff-screen-header-main" },
                      React.createElement("div", { className: "changes-diff-screen-title-row" },
                        React.createElement("div", { className: "changes-detail-path-group" },
                          React.createElement("div", { className: "changes-detail-path" }, detailTitle)
                        ),
                        React.createElement("div", { className: "changes-diff-title-actions" },
                        React.createElement("div", { className: "changes-diff-title-meta" },
                          selectedStep
                            ? React.createElement("span", { className: "changes-diff-title-date" }, formatHistoryTimestamp(selectedStep.createdAt))
                            : null
                        ),
                        selectedStepReverted
                          ? React.createElement(React.Fragment, null,
                              React.createElement("span", { className: "changes-diff-reverted-label" }, "Reverted"),
                              React.createElement("button", {
                                type: "button",
                                className: "changes-diff-revert-button",
                                onClick: () => handleApplyHistoryAction("reapply"),
                                disabled: Boolean(historyMutationLoading),
                                title: "Re-apply this file change by restoring the thread to the selected step.",
                              },
                                React.createElement(RotateCw, { strokeWidth: 1.75 }),
                                React.createElement("span", null, historyMutationLoading === "reapply" ? "Re-Applying..." : "Re-Apply Changes")
                              )
                            )
                          : React.createElement("button", {
                              type: "button",
                              className: "changes-diff-revert-button",
                              onClick: () => handleApplyHistoryAction("revert"),
                              disabled: !revertTargetStep || Boolean(historyMutationLoading),
                              title: revertTargetStep
                                ? "Revert this change by restoring the thread to the state before this step."
                                : "No earlier historical state is available for this step.",
                            },
                              React.createElement(RotateCcw, { strokeWidth: 1.75 }),
                              React.createElement("span", null, historyMutationLoading === "revert" ? "Reverting..." : "Revert")
                            )
                      )
                    ),
                    selectedStepPrompt
                      ? React.createElement("div", { className: "changes-detail-context" }, selectedStepPrompt)
                      : null,
                    historyMutationError
                      ? React.createElement("div", { className: "changes-diff-revert-error" }, historyMutationError)
                      : null
                  )
                ),
                React.createElement("div", { className: "changes-panel-body changes-diff-screen-body" },
                  filePreviewLoading
                    ? React.createElement("div", { className: "changes-loading-state" }, "Loading diff…")
                    : filePreviewError
                      ? React.createElement("div", { className: "changes-empty-state" }, filePreviewError)
                      : selectedFileEntry && selectedFileEntry.type === "resource"
                        ? React.createElement("div", { className: "changes-resource-detail-card" },
                            React.createElement("div", { className: "changes-resource-detail-header" },
                              React.createElement("div", { className: "changes-resource-detail-icon", "aria-hidden": "true" }, renderHistoryResourceIcon(selectedFileEntry.resourceType)),
                              React.createElement("div", { className: "changes-resource-detail-copy" },
                                React.createElement("div", { className: "changes-resource-detail-label" }, selectedFileEntry.resourceTypeLabel || "Resource"),
                                React.createElement("div", { className: "changes-resource-detail-name" }, selectedFileEntry.name || "Created resource"),
                                selectedFileEntry.resourceId
                                  ? React.createElement("div", { className: "changes-resource-detail-id" }, selectedFileEntry.resourceId)
                                  : null
                              )
                            ),
                            selectedFileEntry.description
                              ? React.createElement("div", { className: "changes-resource-detail-description" }, selectedFileEntry.description)
                              : React.createElement("div", { className: "changes-empty-state" }, "This step created a " + String((selectedFileEntry.resourceTypeLabel || "resource")).toLowerCase() + "."),
                            selectedResourceMetaRows.length > 0
                              ? React.createElement("div", { className: "changes-resource-detail-meta" },
                                  selectedResourceMetaRows.map((row) =>
                                    React.createElement("div", { key: row.label, className: "changes-resource-detail-meta-row" },
                                      React.createElement("span", { className: "changes-resource-detail-meta-label" }, row.label),
                                      React.createElement("span", { className: "changes-resource-detail-meta-value" }, row.value)
                                    )
                                  )
                                )
                              : null
                          )
                      : selectedFileEntry && selectedFileEntry.type === "file" && selectedFileIsImage && selectedFileImageUrl
                        ? React.createElement("div", { className: "changes-detail-image-preview" },
                            React.createElement(RunnerImagePreviewSurface, {
                              src: selectedFileImageUrl,
                              alt: detailTitle,
                              fetchHeaders: historyHeaders,
                            })
                          )
                      : selectedFileEntry && selectedFileEntry.type === "file" && resolvedDiffText
                        ? React.createElement(RunnerFileDiffSurface, {
                            diffContent: resolvedDiffText,
                            fileContent: selectedFileContent,
                            filePath: selectedFileEntry.path,
                            emptyMessage: diffEmptyMessage,
                            additions,
                            deletions,
                          })
                        : React.createElement("pre", {
                            className: "changes-code-block" + (selectedFileEntry && selectedFileEntry.type === "file" ? "" : " is-empty")
                          }, selectedFileEntry && selectedFileEntry.type === "file" ? filePreview : (stepDataLoading ? "Loading selected step…" : stepDataError || "Select a changed file to inspect its diff."))
                )
              )
            );
  
          }
  
          return React.createElement("div", { className: "changes-view is-timeline" },
            React.createElement("section", { className: "changes-panel changes-timeline-panel" },
              React.createElement("div", { className: "changes-panel-header" },
                React.createElement("div", { className: "changes-panel-title" }, "Changes"),
                React.createElement("div", { className: "changes-panel-subtitle" }, sortedSteps.length > 0 ? sortedSteps.length + " " + (sortedSteps.length === 1 ? "step" : "steps") : "")
              ),
              React.createElement("div", { className: "changes-panel-body changes-timeline-body" },
                stepsLoading
                  ? React.createElement("div", { className: "changes-loading-state" }, "Loading thread steps…")
                  : stepsError
                    ? React.createElement("div", { className: "changes-empty-state" }, stepsError)
                    : sortedSteps.length === 0
                      ? React.createElement("div", { className: "changes-empty-state" }, "No step history has been recorded for this thread yet.")
                      : React.createElement("div", { className: "changes-timeline" },
                          groupedTimelineSteps.map((group) =>
                            React.createElement("section", { key: group.key, className: "changes-timeline-group" },
                              React.createElement("div", { className: "changes-timeline-track" },
                                group.steps.map(({ step, prompt, files }) => {
                                  const isActiveStep = selectedStepId === step.id;
                                  const changeCount = files.length;
                                  return React.createElement("article", {
                                    key: step.id,
                                    className: "changes-step-card" + (isActiveStep ? " is-active" : "")
                                  },
                                    React.createElement("div", { className: "changes-step-card-header" },
                                      React.createElement("span", {
                                        className: "changes-step-card-commit",
                                        "aria-hidden": "true",
                                      }, React.createElement(GitCommitHorizontal, null)),
                                      React.createElement("button", {
                                        type: "button",
                                        className: "changes-step-card-summary",
                                        onClick: () => handleStepSelection(step, files),
                                      },
                                        React.createElement("div", { className: "changes-step-card-title" }, prompt || step.title || "Changes"),
                                        React.createElement("div", { className: "changes-step-card-meta" },
                                          React.createElement("span", null, formatHistoryTimestamp(step.createdAt)),
                                          React.createElement("span", null, changeCount + " " + (changeCount === 1 ? "change" : "changes"))
                                        )
                                      )
                                    ),
                                    React.createElement("div", { className: "changes-step-card-files" },
                                      changeCount === 0
                                        ? React.createElement("div", { className: "changes-step-card-empty" }, "No changes were recorded for this step.")
                                        : files.map((entry) => {
                                            const isSelectedFile = isActiveStep && selectedFileEntry && historyPathsMatch(selectedFileEntry.path, entry.path);
                                            const changeKindLabel = getHistoryChangeKindLabel(entry.changeKind);
                                            const isRevertedStep = revertedChangeStepIds.has(step.id);
                                            const isImageFile = entry.type === "file" && isHistoryImagePath(entry.path);
                                            const imagePreviewUrl = isImageFile ? buildThreadStepFileDownloadUrl(threadId, step.id, entry.path) : "";
                                            const isResourceEntry = entry.type === "resource";
                                            const resourceLabel = isResourceEntry ? (entry.resourceTypeLabel || "Resource") : "";
                                            const resourceSubtitle = isResourceEntry
                                              ? buildHistoryResourceSubtitle(entry)
                                              : "";
                                            return React.createElement("div", {
                                              key: step.id + ":" + entry.path,
                                              className: "changes-step-file-row" + (isSelectedFile ? " is-active" : ""),
                                            },
                                              React.createElement("button", {
                                                type: "button",
                                                className: "changes-step-file-row-main",
                                                onClick: () => handleFileSelection(step, entry.path),
                                              },
                                                React.createElement("div", { className: "changes-step-file-row-head" },
                                                  React.createElement("div", { className: "changes-step-file-head-main" },
                                                    isResourceEntry
                                                      ? React.createElement("div", { className: "changes-step-file-icon", "aria-hidden": "true" }, renderHistoryResourceIcon(entry.resourceType))
                                                      : null,
                                                    React.createElement("div", { className: "changes-step-file-main" + (isResourceEntry ? " is-resource" : "") },
                                                      React.createElement("div", { className: "changes-step-file-name" }, entry.name || entry.path),
                                                      resourceSubtitle
                                                        ? React.createElement("div", { className: "changes-step-file-path" }, resourceSubtitle)
                                                        : null
                                                    )
                                                  ),
                                                  React.createElement("div", { className: "changes-step-file-right" },
                                                    changeKindLabel
                                                      ? React.createElement("span", { className: "changes-step-file-kind is-" + entry.changeKind }, changeKindLabel)
                                                      : null,
                                                    isResourceEntry
                                                      ? React.createElement("span", { className: "changes-step-file-kind" }, resourceLabel)
                                                      : null,
                                                    typeof entry.additions === "number" && entry.additions > 0
                                                      ? React.createElement("span", { className: "changes-step-file-stat is-added" }, "+" + entry.additions)
                                                      : null,
                                                    typeof entry.deletions === "number" && entry.deletions > 0
                                                      ? React.createElement("span", { className: "changes-step-file-stat is-removed" }, "-" + entry.deletions)
                                                      : null,
                                                    isRevertedStep
                                                      ? React.createElement("span", { className: "changes-step-file-kind is-reverted" }, "Reverted")
                                                      : null
                                                  )
                                                ),
                                                isImageFile && imagePreviewUrl
                                                  ? React.createElement("div", { className: "changes-step-file-preview" },
                                                      React.createElement(RunnerImagePreviewSurface, {
                                                        src: imagePreviewUrl,
                                                        alt: entry.name || entry.path,
                                                        fetchHeaders: historyHeaders,
                                                        interactive: false,
                                                      })
                                                    )
                                                  : null
                                              )
                                            );
                                          })
                                    )
                                  );
                                })
                              )
                            )
                          )
                        )
              )
            )
          );
        }
  
        function isPlaygroundRasterThumbnailCandidate(entry) {
          if (!entry || entry.isFolder) return false;
          const fileName = String(entry.name || entry.path || "").trim().toLowerCase();
          const extension = fileName.includes(".")
            ? fileName.split(".").pop().toLowerCase()
            : "";
          return ["avif", "bmp", "gif", "jpeg", "jpg", "png", "tif", "tiff", "webp"].includes(extension);
        }
  
  ${FILES_PREVIEW_COMPONENTS_SCRIPT}
  
  ${ENVIRONMENT_CHANGES_SCRIPT}
  ${IMAGINE_TEMPLATE_PAGE_SCRIPT}
  ${IMAGINE_PAGE_SCRIPT}
  ${VERSIONING_CORE_SCRIPT}
  ${VERSION_SIDEBAR_SCRIPT}
  ${METRONOME_PAGE_SCRIPT}
  ${MODELS_PAGE_SCRIPT}
  ${MARKETPLACE_PAGE_SCRIPT}
  ${PLAYGROUND_EVALUATIONS_SCRIPT}
  ${PLAYGROUND_FINE_TUNING_SCRIPT}
  ${PLATFORM_UI_PRIMITIVES_SCRIPT}
  
  ${FILES_PAGE_RUNTIME_SCRIPT}
  
        function handleComposerSubmitShortcut(event) {
          if (event.key !== "Enter" || (!event.metaKey && !event.ctrlKey) || event.altKey) {
            return;
          }
          if (!event.currentTarget || typeof event.currentTarget.requestSubmit !== "function") {
            return;
          }
          event.preventDefault();
          event.currentTarget.requestSubmit();
        }
  
        function getSideActionMenuPosition(event, menuHeight = 184, menuWidth = 220) {
          const rect = event.currentTarget.getBoundingClientRect();
          const viewportWidth = window.innerWidth || document.documentElement?.clientWidth || 0;
          const viewportHeight = window.innerHeight || document.documentElement?.clientHeight || 0;
          const gutter = 12;
          const sideGap = 8;
          const hasRoomOnLeft = rect.left - menuWidth - sideGap >= gutter;
          const preferredLeft = hasRoomOnLeft
            ? rect.left - menuWidth - sideGap
            : rect.right + sideGap;
          const maxLeft = Math.max(gutter, viewportWidth - menuWidth - gutter);
          const maxTop = Math.max(gutter, viewportHeight - menuHeight - gutter);
          return {
            top: Math.max(gutter, Math.min(maxTop, rect.top + rect.height / 2 - menuHeight / 2)),
            left: Math.max(gutter, Math.min(maxLeft, preferredLeft)),
          };
        }
  
  ${COMPUTE_RESOURCES_PAGE_SCRIPT}
  ${AGENTS_PAGE_SCRIPT}
  ${SKILLS_PAGE_SCRIPT}
  ${PROJECTS_PAGE_RUNTIME_SCRIPT}
  
  ${ONBOARDING_SCRIPT}
        const PLAYGROUND_PLATFORM_NAVIGATION_STATE_KEY = "__runnerPlatformNavigation";
  ${GUARDRAILS_DOMAIN_FRAGMENTS.catalog}      const PLAYGROUND_PLATFORM_NAVIGATION_HISTORY_LIMIT = 200;
        const PLAYGROUND_PLATFORM_NAVIGATION_RESTORE_SUPPRESSION_MS = 1800;
        const PLAYGROUND_PLATFORM_NAVIGATION_FIELDS = [
          "page",
          "mode",
          "view",
          "threadId",
          "contentMode",
          "projectId",
          "detailMode",
          "taskId",
          "scheduleId",
          "workflowId",
          "editorMode",
          "resourceView",
          "resourceType",
          "resourceId",
          "serverKind",
          "environmentId",
          "path",
          "toolsView",
          "pluginId",
          "skillId",
          "sectionId",
          "teamId",
  	        "teamTab",
  	        "teamRoleId",
  	        "organizationId",
  	        "organizationTab",
  	        "organizationBillingSection",
          "modelsTab",
          "templateType",
          "templateId",
          "guardrailId",
          "evaluationId",
          "evaluationRunId",
          "fineTuneJobId",
          "developSection",
          "imagineView",
          "mediaMode",
          "filterMode",
          "sortMode",
        ];
  
  ${GUARDRAILS_DOMAIN_FRAGMENTS.runtime}      function createPlaygroundPlatformNavigationToken() {
          return Date.now().toString(36) + Math.random().toString(36).slice(2);
        }
  
        function normalizePlaygroundPlatformNavigationValue(value) {
          if (value === undefined || value === null) return "";
          if (typeof value === "boolean") return value ? "true" : "";
          return String(value).trim();
        }
  
        function normalizePlaygroundPlatformNavigationEntry(entry) {
          if (!entry || typeof entry !== "object" || Array.isArray(entry)) {
            return null;
          }
          const normalized = {};
          PLAYGROUND_PLATFORM_NAVIGATION_FIELDS.forEach((field) => {
            const value = normalizePlaygroundPlatformNavigationValue(entry[field]);
            if (value || field === "page") {
              normalized[field] = value;
            }
          });
          if (!normalized.page) {
            return null;
          }
          return normalized;
        }
  
        function getPlaygroundPlatformNavigationEntryKey(entry) {
          const normalized = normalizePlaygroundPlatformNavigationEntry(entry);
          return normalized ? JSON.stringify(normalized) : "";
        }
  
        function buildPlaygroundPlatformNavigationState(entry) {
          const normalized = normalizePlaygroundPlatformNavigationEntry(entry);
          if (!normalized) return null;
          return {
            [PLAYGROUND_PLATFORM_NAVIGATION_STATE_KEY]: true,
            entry: normalized,
          };
        }
  
        function getPlaygroundPlatformNavigationStateEntry(state) {
          if (!state || typeof state !== "object" || !state[PLAYGROUND_PLATFORM_NAVIGATION_STATE_KEY]) {
            return null;
          }
          return normalizePlaygroundPlatformNavigationEntry(state.entry);
        }
  
        function appendPlaygroundPlatformNavigationHistory(history, entry) {
          const normalized = normalizePlaygroundPlatformNavigationEntry(entry);
          if (!normalized) return history;
          const entryKey = getPlaygroundPlatformNavigationEntryKey(normalized);
          const previous = history.length > 0 ? history[history.length - 1] : null;
          if (previous && getPlaygroundPlatformNavigationEntryKey(previous) === entryKey) {
            return history;
          }
          return history.concat({
            ...normalized,
            visitedAt: new Date().toISOString(),
          }).slice(-PLAYGROUND_PLATFORM_NAVIGATION_HISTORY_LIMIT);
        }
  
  ${PLATFORM_SHELL_SCRIPT}
        const root = createRoot(document.getElementById("app"));
        root.render(
          React.createElement(PlatformApplicationBoundary, {
            runtime: {
              apiOrigin: window.location.origin,
              appOrigin: ${JSON.stringify(platformOrigin)},
              aiosOrigin: ${JSON.stringify(aiosOrigin)},
              environment: ${JSON.stringify(process.env.NODE_ENV === "development" ? "development" : "production")},
            },
          }, React.createElement(LegacyPlatformApp))
        );
      </script>
    </body>
  </html>
  `;

  return inlinePlatformDocumentHtml;
}
