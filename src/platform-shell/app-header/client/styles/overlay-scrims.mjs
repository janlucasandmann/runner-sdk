export const APP_HEADER_OVERLAY_SCRIMS_CSS = `      .thread-search-scrim {
        z-index: 94;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 24px;
        background: rgba(0, 0, 0, 0.42);
        backdrop-filter: blur(14px);
      }

      .account-menu-scrim {
        z-index: 92;
      }

`;
