export const PLATFORM_NAVIGATION_GUARD_REFS_SCRIPT = `        const platformNavigationGuardRef = useRef(null);
        const platformNavigationPendingActionRef = useRef(null);
        const platformNavigationGuardBypassRef = useRef(false);
`;
