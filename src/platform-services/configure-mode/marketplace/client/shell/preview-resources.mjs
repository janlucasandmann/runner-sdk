export const MARKETPLACE_APP_PREVIEW_RESOURCES_SCRIPT = `        const resourceTemplatePreviewResources = useMemo(() => (
          buildPlaygroundResourceTemplatePreviewResources(PLAYGROUND_RESOURCE_TEMPLATE_DATA)
        ), []);
`;
