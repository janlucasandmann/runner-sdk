export const MODELS_APP_HISTORY_CAPTURE_SCRIPT = `          if (activePage === "models") {
            return {
              page: "models",
              modelsTab: modelsPageTab,
            };
          }

`;
