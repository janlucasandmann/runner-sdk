# Dot loader

The dot loader is a shared, dependency-free loading primitive. Platform UI
consumers import it through this directory's `index.ts`; the former
`src/react/dot-loader.tsx` entry remains a compatibility facade for Runner
consumers.
