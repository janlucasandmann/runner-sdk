# Profile image picker

`PlatformProfileImagePicker` is the shared profile-image control for editable
resource identities such as agents and teams. It owns popup positioning,
dismissal, selected-state rendering, image fallbacks, and the default platform
profile presets while leaving persistence to the resource service.
